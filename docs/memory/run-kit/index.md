---
description: "Web-based agent orchestration dashboard"
---
# run-kit Memory Domain

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [architecture](architecture.md) | System architecture, component responsibilities, data flow — backend libraries (incl. in-memory metrics + prstatus + ports/listening-HTTP-services collectors with HTTP-probe filter, internal/push Web Push subsystem), API endpoints (incl. POST /api/servers/order + rank field on /api/servers via @rk_server_rank), SSE hub + PR-status join + metrics-only server-neutral stream sentinel + server-independent event: services listening-HTTP-service broadcast (probe-filtered) + server-global event: server-order broadcast (single cached slot, all clients incl. ?metrics=1) + session-tile previews (dedicated event: preview with per-connection expanded-set scope via POST /api/preview-scope, reusing tmux.CapturePane), server-independent host-metrics subscription (useHostMetrics) + tracked host-metrics stream health (hostMetricsConnected: dedicated ?metrics=1 stream health with 3s disconnect debounce + per-server fan-out fallback, for the Cockpit connection dot), tmux/sessions enrichment, control-mode subscription, Web Push notifications (rk notify → /api/notify → webpush-go → service worker, VAPID + subscriptions under ~/.rk/) |
| [rk-riff](rk-riff.md) | `rk riff` subcommand — worktree + tmux window + Claude launcher with argv-ordered pane arrays, presets, named layouts, and parallel fan-out |
| [tmux-sessions](tmux-sessions.md) | Session enumeration, group filtering, direct-attach relay + move-based board pin-sessions (`_rk-pin-*`), window addressing, SSE poll-set reap on dead-server fetch error, unified test-socket naming + `rk reaper`, env-gated `RK_SERVER_ALLOWLIST` test-scoping |
| [ui-patterns](ui-patterns.md) | URL structure + canonical route page names (Cockpit / Server Cabin / Terminal / Board / Not Found — the spoken/doc vocabulary) + the breadcrumb-ends-at-parent / centered-heading-is-the-leaf vocabulary split, three-way server route guard + create-server/server-gone lifecycle, full-width-topbar Shell grid + TopBar universal-page-heading navbar (`1fr auto 1fr` grid with a universal `PageType: name` center heading on EVERY mode — `Terminal: <window>` editable + ▾ window switcher / `Board: <board>` display + ▾ board switcher / `Server Cabin: <server>` display / solo `Cockpit`; move-don't-copy so the leaf is the centered heading and the left breadcrumb ends at the parent; brand-as-root-crumb, cockpit mode, `›` separator, crumb affordance vocabulary — LINK_CRUMB_CLASS chip vs persistent ▾ caret) + site-wide hover-animation vocabulary (glitch=brand / boot-sweep=top-bar page heading (one inverse-video cursor over `PageType: name`: TypedLabel-style over prefix flowing into decode glyph-churn over name, shared useBootSweep/SweepCells) / brackets+caret=section headings (SectionHeading) / typed-sweep=section labels (TypedLabel invisible-hand typing, inverse-video cursor, ~350ms fixed pass) / CRT glint=buttons — all behind prefers-reduced-motion; utility classes in globals.css) + shared useWindowRename hook (top-bar heading + sidebar inline rename) + shared bracketed SectionHeading on cabin Sessions + the four Cockpit zones (retired PageHeading) + border-width system (1px hairline / 3px structural seam / 8px selection accent), Cockpit host-console home (HOST HEALTH + BOARDS + TMUX SERVERS + SERVICES zones on `/`; server-independent useHostMetrics + shared HostMetrics component; useHostServices listening-HTTP-service tiles (probe-backed — backend broadcasts only HTTP responders), "Open in window" → @rk_type=iframe /proxy/{port}/ gated solely on server existence; useBoards board tiles → /board/$name), SessionTiles density view (session→window tile grid on /$server with static capture-pane text previews via SSE event: preview; replaced the deleted Dashboard) + sidebar/boards components, shared PR-status line (Pane-panel only) + unified StatusDot (one lifecycle-journey dot — PR > fab > tmux precedence, hue=phase / shape=status — reused on sidebar row, SessionTiles window tile, pane-panel header) + custom StatusDotTip floating-ui hover-card (replaces native title; docs link + PR-phase Open-PR link; hover/focus open, no tooltip ARIA role), Web Push opt-in (terminal-themed Cmd+K palette entry, not a bell), chrome (top-bar right-cluster 3-level button pyramid (L1 terminal-only splits+fixed-width / L2 terminal+board Aa+✕ / L3 always notification→theme→refresh→help+connection-dot; dot-everywhere = per-page live-data health, board ✕ = unpin focused pane) + route-agnostic HelpLink docs-anchor chip → HELP_URL, with an AppShell-only "Help: Documentation" palette action), full-page RefreshButton recovery affordance + dual-mounted "View: Refresh Page" palette action (AppShell viewActions + board boardRouteActions; Cockpit mounts none), theme system, user-controllable global terminal font size (ChromeContext preference + top-bar combo + palette actions), adaptive terminal write flush + deferred per-connection reset + (server, owning session)-keyed relay connection identity + window-switch slide transition (View Transitions wrapper on navigateToWindow — flattened-sidebar-order direction data attribute, first-paint gate released at ws.onmessage receipt time, reduced-motion/no-support instant-switch fallback), keyboard shortcuts, sidebar tree keyboard navigation + W3C-APG tree ARIA, infra-server de-emphasis (isInfraServer/compareServers/DAEMON_SERVER in client.ts + single sort choke point at session-context.tsx fetchServers → infra-last order on all four surfaces + grey text-text-secondary tile names + rk-daemon kill-dialog warning in app.tsx) + rank-aware compareServersRanked composition ((infra-class, rank, name) wrapping compareServers at the same choke point + live re-sort on server-global event: server-order), server-tile drag reorder on both tile grids (shared useServerReorder hook — custom MIME, insert-before, derive-over-store override, debounce + drop-flush, infra excluded) + command-palette Move up/down for current server/session/window (lib/palette-move.ts pure helpers, boundary-hidden, no wraparound), component conventions |
