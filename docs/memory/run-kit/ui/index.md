---
description: "Frontend UI patterns, split by surface: routes + shell, top bar, sidebar, status signals, lenses + surface layout, boards, compose strip, keyboard + palette, terminal frontend, visual design, dialogs + client state, updates + notifications."
---
# Ui Documentation

> **Generated by `fab docs-index`** — do not hand-edit; only the manual block below is preserved.

<!-- fab docs-index:manual:start -->
<!-- Hand-managed; preserved verbatim on regeneration. Add rows the generator
     cannot produce: non-markdown files' descriptions (HTML, PDF), external links,
     custom groupings. A row for a file the walker also indexes keeps its place and
     label here, but its description follows that file's frontmatter. A row
     whose target no longer exists is flagged by `fab docs-index --check`. -->
<!-- fab docs-index:manual:end -->

| File | Description |
|------|-------------|
| [boards](boards.md) | Boards: link-based pinning, board page layout, pane cards, kill confirm, autofit, pane reorder, relay-connection suspension, mobile carousel, sidebar boards section, pin entry points (incl. the `rk board` CLI door), board/pin/view palette actions, frontend hooks, architecture placement. |
| [compose-and-bottom-bar](compose-and-bottom-bar.md) | Bottom bar (coarse-pointer-only) and docked compose strip: pointer-gated bar existence, mode-carrying send semantics, staged-text recovery and in-flight locking, the Enter classifier, drafts + sent history, readline chords, file upload, iOS keyboard support, the card/compact layout, edge-docked padding, the operator-page forced-on footer dock, the collapsed tongue, the on-by-default preference with its one-time notice, first-visit focus, the Escape hand-off, and the stateful compose chord. |
| [cron-console-tabs](cron-console-tabs.md) | The quake terminal's `Cron List` / `Cron Log` tabs — the cron clock's web UI on both form factors: the four-segment strip (tokens terminal|tasks|list|log, the `activity` alias), Cron List row anatomy/sort/dimming + `+ New entry`, Cron Log deliveries-only anatomy, the entry detail sheet (mute lease presets, pin, Edit, delete, inline variant), the create dialog's edit mode, CronStaleBanner placement, useCronData on the SSE cadence, e2e coverage. |
| [data-table](data-table.md) | The shared DataTable component over @tanstack/react-table — column contract, sortable aria-sort headers with the Control toggle vocabulary, fine-pointer column resizing over --rk-col-<id> CSS vars, the per-viewer runkit-table-<id> view-state store (clamped reads, pub/sub + storage event), the mounted-table registry, the Table: Reset columns palette action, and the four table ids (watched, watched-dense, cron-list, cron-log). |
| [dialogs-and-state](dialogs-and-state.md) | Component conventions; modal dialogs and popup surfaces (the spring-loaded marker pad, the GUI off-/restart-confirms on one shared shell with the gui.enabled off-interception and per-viewer rk-gui-* postures); the overlay-presence registry (the modal count every modal-class surface registers in — the hide signal for surfaces that cannot be painted over) and the status-bar toasts; clipboard and e2e host-state rules; the Zustand window store, optimistic mutation feedback, recently-closed mirror. |
| [focus-ownership](focus-ownership.md) | Per-window focus memory (tty/compose/code/gui) and the code-server steal guard: real-focus-only recording asymmetry, the desktop restore router with its tty first-visit default, the guard arm/disarm lifecycle, the in-frame focusin grab detector, the focus-seam consumers (the surface-chord family, the web tile's IframeWindow, the gui canvas's guard-free seam, the tty ⌘F claim), and the quake terminal's own guarded origin slot and the invariants that make it yield focus to a terminal pane. |
| [keyboard-and-palette](keyboard-and-palette.md) | Keyboard shortcuts: tiers, the e.code rule, defaults, restrictions, macros, overrides, claimed keys, the Shortcuts tab; palette mount + action registry — recovery, server lifecycle/color, operator requests, window identity, session sorting, cron clock, web zoom/tabs, Help: topics, the GUI: family (toggle, desktop picker, launch/resolution/quality, fullscreen, zoom/pointer/lock/capture, stats, hidpi/key-bar, send key, logs, reconnect), Tab: Marker, Tab: Reopen closed, the Easter egg: pair. |
| [lenses-and-layout](lenses-and-layout.md) | The window-view lens model (tty/web/code/gui), surface layouts, iframe/code/gui renderers. Web chrome + the per-tab engine seam (iframe + native), address-bar ladder, draft tabs, onboarding, FindBar, zoom, help topics; code workspace src map, code-root seed + follow, boot rescue, cross-window frame LRU; gui noVNC canvas, content states, gui.geometry resize, zoom/HiDPI host, trackpad layer, key bar, toolbar fold + key capture, quality/stats overlay; tty progress/export; tile focus/steal seams. |
| [quake-terminal](quake-terminal.md) | The quake terminal — the desktop quake overlay and the operator page (the operator window's route wearing the quake surface). Covers the rk:quake-terminal event seam, the ⌘J machine + docked compose strip (launcher collapsed while open or on the operator route) with its per-operator draft in the compose-draft store and per-server flags, ⤢ open as tab, ⌖ pin, resize geometry, glass (0.95), mobile navigation, file paste, operator-less availability (read-only compose + Start operator). |
| [routes-and-shell](routes-and-shell.md) | Route surface and app shell: URL structure as resumable bookmark, server-gone flip, session tiles + the Server-page WATCHED zone, shell grid + chrome architecture, quake-terminal overlay + operator-page variant, live safe-name conversion, session creation flows, instance display name, session-to-project mapping, activity status, host recovery zone, HOST HEALTH card, TMUX SERVERS server-class tile markers (scratch chip, protected shield, external ↗), and the dev-only `/__controls` gallery. |
| [screen-break-eggs](screen-break-eggs.md) | The screen-break Easter eggs — a one-shot, whole-viewport event layer clipping the .app-root glass with a real evenodd hole, a seeded crack web plus flickering dead-pixel LCD lines, falling shards, and a slow 12 s timeline; a green fist on PR merged (smash), an eye on update available (peek). The `easter_eggs` setting (default on) gates the automatic occasions; the two palette entries bypass it. Triggers fire once per identity; reduced motion never mounts; viewports < 640 px no-op. |
| [sidebar](sidebar.md) | Sidebar: row anatomy, marker strip and pad entry, row-hover flyout cards, coarse status rail, flair overlays, render performance, keyboard/tree ARIA, multi-select and broadcast, collapsible panels (PANE/Host), section rail, the watched underbar + opr register, host-page health/boards/services, server-class glyphs, kill controls, scroll fade, the operator pinned row + operator-less placeholder row, session-row viewer chip, the hover-reveal contract + tappable floors, and footer. |
| [status-signals](status-signals.md) | Status signaling: PR registers and glyphs, the compositional status dot, the server-class glyph axis (protected shield + external ↗), waiting/attention surfacing + the neutral count chip, the tty tile header's progress chip, the row flyout card (one shared shell on four mounts, both pointer classes), the two-tier tooltip taxonomy, and the desktop status bar (window-mirror + host clusters, the ◷ clock chip, raw-value copy segments, measured priority fold, PANE-on yield). |
| [terminal](terminal.md) | Terminal frontend: RelayMux singleton + TerminalClient port, hidden-page stream suspension, render-pause visibility shim (hidden prop), adaptive write batching + deferred reset/clear, byte-gated window-switch slide transition, xterm addons (search find, announced WebGL fallback) + the ⇩ export menu, Option-as-Meta handling, font size preference + bundling, device-split scrollback, unicode width handling, touch scroll, ctrl-wheel/pinch font-zoom gestures, coarse-pointer focus suppression. |
| [top-bar](top-bar.md) | Top-bar chrome: persistent root mount, the sidebar head (full-height brand + toggle over the bar's left end), 3-column grid, center heading + window rename + operator glyph + the quake launcher, control cluster + overflow menu + Help topics disclosure, flush surface-toggle group (desktop toggle / pinned mobile switch + the gui `⚙` overflow block), Open split-button, refresh, breadcrumb dropdowns + crumb collapse, host switcher, shell titlebar strip, and the shared control-glyph register. |
| [updates-and-notifications](updates-and-notifications.md) | Browser Web Push and desktop-shell OS notification opt-in, delivery, dedupe, and deep links; plus update-notification feeds, reload guard, UpdateChip, palette actions, maintenance entries, and version surfaces. |
| [visual-design](visual-design.md) | Visual design system: color tokens, borders, themes, hover-animation vocabulary, control-state rules (focus ring, pressed fill, select guard), the Control primitive (controlClass variants × states, structural REST-swap, latch well/open-latch, menu-row scale) owning the control vocabulary, controls.ts survivors, --ctl-* tokens, /__controls gallery drift guard, logo/heading treatments, color tinting, marker vocabulary, row textures, flair overlays, touch targets. |
