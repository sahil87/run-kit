You are the ship worker for change `260908-qgfk-operator-omnibox-focus-height-jitter` (id: `qgfk`), dispatched by `/fab-fff`.

**Read first, in order:**
1. `fab/project/config.yaml`
2. `fab/project/constitution.md`
3. `fab/project/context.md` (optional — skip gracefully if missing)
4. `fab/project/code-quality.md` (optional — skip gracefully if missing)
5. `fab/project/code-review.md` (optional — skip gracefully if missing)
6. `.claude/skills/git-pr/SKILL.md`

**Then run** the `/git-pr` behavior **targeting the explicit change `260908-qgfk-operator-omnibox-focus-height-jitter`** (not whatever change happens to be active) — invoke it exactly as `/git-pr 260908-qgfk-operator-omnibox-focus-height-jitter`. Verify the checked-out branch matches this change before mutating anything (the branch-guard `/git-pr` already performs).

**This is a self-managing stage**: you own the `ship` stage's own `fab status` start/finish/fail transitions yourself, exactly as `/git-pr` does when run standalone — commit, push, create the draft PR, and call `fab status finish qgfk ship` yourself on success (or `fab status fail qgfk ship` and stop on failure). Do NOT touch any other stage's transition.

**You MUST end by writing your result** to `.fab-dispatch/qgfk/ship-result.yaml`:
```yaml
stage: ship
status: success            # success | failure
pr_url: "https://github.com/{owner}/{repo}/pull/{N}"
summary: "committed, pushed, draft PR created"
# on failure only:
# reason: "..."
```

**You MUST end with a terminal** `fab status refresh qgfk` (pull-based recompute — required, not prohibited).
