## Backlog

- [ ] [63td] 2026-03-03: We would need buttons to delete this worktree and at the project level to delete all unused worktrees.
- [ ] [ar9l] 2026-03-03: Buttons to send Git PR and a way to keep checking its status.
- [ ] [6bdn] 2026-03-03: Buttons to control the 'wt-* workflow' - using wt-create to create a new worktree etc
- [ ] [oibl] 2026-03-14: unable to scroll terminal on mobile
- [ ] [rkx4] 2026-03-20: The text input dialog - show buttons at the bottom of that dialog in a section that represent 'most used commands' - Clicking on them enters that text in the input box - later parts of this list may also be exposed in the dashboard cards - a quick way send commands to different session. This could be used to send the text 'Create PR' or 'Merge PR' to sessions that are ready to do so
- [x] [a36m] 2026-06-02: Add a build-time 'help-dump' step that emits rk's CLI help tree as help/run-kit.json and PRs it into sahil87/shll.ai (the shll.ai landing site renders it as an expandable 'Command reference' on the run-kit tool page). CONTRACT (frozen — copy the reference sample at sahil87/shll.ai path help/wt.json): JSON shape is {tool, version, captured_at (ISO-8601 UTC), schema_version: 1, root: Node} where Node = {name, path (full invocation e.g. 'rk riff'), short (one-line desc), usage, text (the RAW -h output byte-for-byte, newlines preserved), commands: Node[] (recursive; empty array = leaf)}. NOTE the file is named help/run-kit.json (matches the tool's repo/site slug) even though the binary is 'rk' — set the top-level tool field to 'rk' (the invoked name). rk has genuine NESTED subcommands (e.g. rk riff ...), so the recursive commands[] is exercised here — capture the full depth. PRODUCER (rk is Cobra/Go, binary 'rk', module 'rk', main at app/backend/cmd/rk): walk the cobra command tree programmatically (rootCmd.Commands() recursively), NOT regex-parsing -h text; per node capture cmd.Name / cmd.CommandPath() / cmd.Short / cmd.UseLine() and cmd.UsageString() (or Long+UsageString) as 'text'. FILTER OUT cobra's auto-generated 'completion' and 'help' subcommands and any cmd.Hidden==true (rk has serve/internal-ish commands — audit which are user-facing). VERSION: read from the built binary (rootCmd.Version / embedded ldflags) — do NOT hardcode. PUSH: in CI after the Go build, run the dump, write help/run-kit.json, validate it parses, then open a PR into sahil87/shll.ai using the existing repo secret SHLLAI_TOKEN (contents + pull-request write) with auto-merge enabled (PR, not direct push to main, to avoid the multi-repo push race). This is rk's slice of a 7-tool rollout; the shll.ai site-side consumer (Astro loader + reference UI) is tracked separately in the shll.ai repo.
- [x] [72oq] 2026-06-02: sync-latency.spec.ts:162 ('3. Create window via sidebar + button') is a no-op latency test: it does a hardcoded waitForTimeout(3_000) then record()s the elapsed ~3012ms, so it ALWAYS reports the action as SLOW/SSE-dependent regardless of reality. It can neither detect a latency regression nor prove the optimistic win — the sidebar + button create path IS optimistic today (app.tsx:477-484 fires onOptimistic -> addGhostWindow with rollback; window-store.ts has full ghost-window-on-create + unit tests), yet the audit summary still prints '[SLOW] Create window (UI, + button): 3012ms ← SSE-dependent', a false signal. Fix: replace the fixed sleep with a measurement of time-to-first-ghost-appearance — wait for the ghost window row to appear in the sidebar (poll/expect with a short timeout) and record THAT latency, so the test fails if create regresses to SSE-dependent (>500ms threshold). Audit the other sync-latency.spec.ts actions for the same fixed-sleep anti-pattern while there. Distinct from sl03 (that optimistic-create work is DONE); this is purely test-quality.
- [ ] [4qq2] 2026-06-10: Eliminate the under-load echo slow mode at its source (relay/tmux attribution + pacing bypass). CONTEXT: PR #255 harness proved typing-while-pane-streams echo latency is bimodal ~4ms vs ~22ms (1:2 ratio) and that the slow mode is UPSTREAM of the browser flush — a controlled experiment replacing the PR #244 adaptive flush with a uniform setTimeout(0) flush reproduced the identical under-load histogram while regressing idle p50 9.6ms to 15.5ms (prototype reverted; adaptive flush stays). Reproduce with: just pw test echo-latency (under-load test + histograms in app/frontend/tests/e2e/echo-latency.spec.ts). PHASE 1 INSTRUMENT (the server-side timestamp split deliberately deferred in PR #243 — now actionable): in the Go relay (app/backend, relay.go) stamp t_ptyread (PTY chunk read) and t_wswrite (WS frame write) per chunk under an env-gated debug flag (e.g. RK_RELAY_TRACE=1; log or side-channel), correlate with the harness send/recv stamps to attribute the ~18ms gap to (a) tmux pane-output-to-attached-client delay, (b) relay PTY read scheduling, or (c) WS delivery / browser task queue. Note: network p50 measured 0.4ms when idle, so the steady-state path is fine — only the under-load window is in question. PHASE 2 FIX per attribution: if (a) tmux-side (most likely — tmux paces redraws/output to regular attached clients while a pane floods): test 1) sourcing pane bytes via control-mode %output events instead of the attached-client screen stream (backend already has tmuxctl control-mode infra — %output may not be subject to client redraw pacing), 2) pipe-pane raw streaming as a measurement probe (not necessarily the shipping mechanism — raw pane output lacks client screen state), 3) tmux options/version knobs affecting client write pacing. If (b) relay-side: inspect the read loop (4KB chunk reads) for buffering/syscall pacing. ACCEPTANCE: the under-load histogram in echo-latency.spec.ts collapses to a single fast cluster (p95 under ~10ms) with the 20k-line throughput guard unchanged; OR a written conclusion in the spec.md companion that the slow mode is structurally unavoidable with a regular attached client — which then raises the priority of predictive local echo (see dxqh). RELATED: dxqh (predictive echo — the client-side alternative), PRs #242-#245 (latency series), #255 (under-load benchmark), docs/memory/run-kit/ui-patterns.md adaptive-flush entry. INTERIM UX OPTION (separate, cheap): load-aware de-jitter buffer in terminal-client.tsx — fixed-delay echo scheduling only while the pane is actively streaming (uniform ~22ms during load, fast-uniform when idle); trades median for uniformity and becomes unnecessary if this lands.
- [ ] [dxqh] 2026-06-10: Predictive local echo (mosh-style) — uniform near-0ms perceived keystroke latency regardless of pane load or network. WHY: PR #255 proved under-load echo is bimodal ~4ms/~22ms with the slow mode upstream of the client flush (see [4qq2]); prediction is the only approach that is both uniform AND fast, and it is mandatory anyway if remote rk serve ever ships (over a real network the round-trip dominates and no server-side fix helps — see the pivot note in the latency-series history). MECHANISM: on terminal.onData for predictable inputs, paint the glyph locally immediately with tentative styling (mosh uses underline-until-confirmed), queue the prediction, and reconcile against the real inbound echo stream — confirm (restyle to normal) on match, rollback (let server truth repaint) on mismatch. KEY DESIGN DECISION up front: render predictions in an overlay layer (DOM or canvas positioned over the xterm cell grid) rather than writing into the xterm buffer — xterm has no undo, buffer writes would corrupt scrollback/state on misprediction, an overlay makes rollback trivial and keeps the inbound stream the single source of truth for the real buffer. SCOPE GUARDS (mosh-style adaptive confidence): start passive; begin predicting only after observed round-trips confirm the pane echoes typed printables within N ms; auto-disable on first mismatch and re-enter passive observation (covers password prompts, vim normal mode, full-screen TUIs); never predict in the alternate-screen buffer (term.buffer.active.type), never predict control chars or the EFFECTS of Enter (Enter flushes/confirms pending predictions only); restrict to printable ASCII first, then extend grapheme-aware (UnicodeGraphemesAddon is already loaded); skip during IME composition and bracketed paste; the compose-buffer path (compose-buffer.tsx) bypasses prediction entirely. RECONCILIATION: ordered pending-prediction queue keyed by expected echo bytes + predicted cell positions; match incoming chunk bytes prefix-wise and consume; ANY divergence clears the whole queue and overlay (the server repaint is already in flight, so recovery is automatic). CURSOR: predictions render at the live cursor cell and advance a shadow cursor; handle wrap at cols; predict backspace only against own queued predictions, never against real cells. MEASUREMENT/ACCEPTANCE: extend app/frontend/tests/e2e/echo-latency.spec.ts with a perceived-echo metric (keystroke to overlay paint) plus a misprediction counter per 1k keystrokes across scenarios: idle cat, the under-load tick stream (from PR #255), and a vim/alternate-screen pane where prediction must remain OFF; target under-load perceived p50 under ~5ms with a single-cluster histogram, zero visible mispredictions in non-echoing panes, throughput guard unchanged. RELATED: [4qq2] is the source-side alternative — if it fully collapses the slow mode locally, prediction remains the remote-serve lever; PRs #242-#245, #255.
- [x] [o20f] 2026-06-13: Sidebar improvements WAVE 1 (3 parallel changes, no deps, ship as each lands; split into 3 /fab-new intakes). [A] sidebar-triage-signal (S): wire already-transmitted needs-attention fields to the existing red token — fabDisplayState==="failed" should render fab-stage text (and/or activity dot) in text-red-400 (window-row.tsx:229 only special-cases "done" today, so "failed" looks identical to healthy); AND flag PR failure with a small red dot/glyph reusing isFailish (prChecks==="fail"||prReview==="changes_requested") from pr-status-line.tsx — NOT the full PR line (deliberately removed from rows in 260610-obky, keep tree compact). Files: window-row.tsx + window-row.test.tsx. [B] sidebar-drawer-a11y (S/M): mobile overlay declares role=dialog aria-modal=true (shell.tsx:131-132) but traps nothing — Tab leaks to terminal behind, Escape no-ops, no focus-on-open; add focus-into-drawer + Tab trap + Escape-close reusing Dialog/CommandPalette focus-cycle logic; bonus: scrollIntoView+focus the aria-current row (window-row.tsx:193) on sidebar open, mirroring server-panel.tsx:77-82. Files: shell/shell.tsx, sidebar/index.tsx. [C] palette-window-switch (M, lives in app.tsx): add 'Window: Switch to <session › name>' palette entries calling selectWindow(server,windowId) — palette today only mutates the current window; combined with no tree arrow-nav (Wave 3) there's no keyboard way to switch windows. Files: app.tsx windowActions useMemo. COORDINATION: land A before Wave 2 (both touch window-row.tsx); B and C are fully orthogonal. Context: docs/memory/run-kit/ui-patterns.md '## Sidebar'.
- [x] [ect6] 2026-06-13: Sidebar improvements WAVE 2 — sidebar-render-perf (M, depends on Wave 1 change A merging first; one /fab-new intake). The render-perf pair that MUST travel together. Today every SSE sessions tick (fires several times/sec — session-context rebuilds slice Maps as fresh refs) re-renders the ENTIRE sidebar tree (every ServerGroup/SessionRow/WindowRow); grep confirms NO React.memo anywhere in the frontend. (1) Lift nowSeconds out of render bodies: const nowSeconds=Math.floor(Date.now()/1000) at index.tsx:697 and :907 is threaded as a prop into every WindowRow (index.tsx:803,1211) + WindowPanel, so it changes every render and would defeat memo — move the 'now' tick into a leaf useNow() hook used only inside duration-showing components (getWindowDuration at window-row.tsx:85), or precompute duration strings. (2) Wrap WindowRow/SessionRow/ServerGroup in React.memo AND stabilize per-row closures (index.tsx ~804-871 builds ~30 arrow handlers per ServerGroup per render; ~1223-1236 per WindowRow) via a useCallback factory keyed by id or by passing server/session/windowId as props. DO NOT undo the derive-over-store session-order pattern (orderOverrideRef ?? SSE order + render-time arraysEqual reconcile, index.tsx ~757-771) — deliberate prior fix 260609-ebks. Files: index.tsx, window-row.tsx, session-row.tsx, new useNow hook. COORDINATION: rebase after Wave-1 change A (both edit window-row.tsx); land BEFORE Wave 3 so keyboard-nav builds on memoized stable-prop rows.
- [x] [wt1v] 2026-06-13: Sidebar improvements WAVE 3 — sidebar-keyboard-nav (M, depends on Wave 2; one /fab-new intake). Make the session/window tree keyboard-navigable and screen-reader-correct — a constitution V (keyboard-first) gap: there is ZERO arrow-key traversal anywhere in sidebar/ today (no keydown handlers beyond rename-input + pin Escape), so the only keyboard path into the tree is the command palette. Two paired concerns (same DOM, one change): (1) Roving-tabindex arrow navigation scoped to the focused tree <nav aria-label=Sessions> (index.tsx:700): Up/Down across visible rows, Left/Right collapse/expand the session group, Enter selects, Home/End jump — mirror the existing roving pattern used by CommandPalette/ThemeSelector (ui-patterns.md 'Keyboard-Navigable List Scroll Pattern'). Handlers live on the sidebar nav, NOT the terminal, so no xterm key conflict. (2) Tree ARIA: add role=tree/treeitem/group, aria-level/aria-setsize/aria-posinset, and associate the session chevron's aria-expanded (session-row.tsx:95) with the window list via aria-controls — today it's bare <button>s, announced as an undifferentiated list. Files: index.tsx (nav keydown + roving index state), window-row.tsx + session-row.tsx (tabindex/roles). COORDINATION: land after Wave 2 (builds on memoized rows with stabilized props; doing it before forces re-touching keyboard wiring when D stabilizes closures). NOTE: materially easier if the index.tsx god-orchestrator (1271 lines) is extracted into hooks first — out of scope for these 3 waves but flagged.
- [x] [xd9r] 2026-06-15: Web Push notifications for the run-kit web UI (feat). Goal: let any process on the box (primarily the fab-kit operator agent) send a real background mobile/desktop push that reaches the user even when the RunKit PWA tab is CLOSED. Consumer/origin: fab-kit change [mmmt] (260615-mmmt-non-blocking-operator-escalation) abstracts its operator notifications behind a single shell 'notify' command, defaulting to ntfy.sh today; 'rk notify' (this change) is the intended eventual channel so notifications flow through infra the user already runs instead of a third-party service. CURRENT STATE (verified 2026-06-15, rk 2.3.1): foundation exists but ZERO push is implemented. Present: PWA manifest app/frontend/public/manifest.json (display:standalone, icons) linked from app/frontend/index.html; SSE server->frontend channel app/backend/api/sse.go (route GET /api/sessions/stream, registered in app/backend/api/router.go ~line 369, chi router; consumed frontend-side in app/frontend/src/contexts/session-context.tsx and hooks/use-boards.ts); Go/chi backend with ~30 /api/* routes in router.go; cobra CLI tree in app/backend/cmd/rk/. MISSING (all net-new): no service worker (no sw.js, no navigator.serviceWorker.register anywhere in app/frontend/src), no Web Push / VAPID / PushManager code on either side, no 'rk notify' subcommand, no Notification API usage. SCOPE (Web Push delivery model, chosen with mmmt owner): (a) FRONTEND - add a service worker (sw.js) registered on app load, a PushManager.subscribe flow gated behind Notification.requestPermission, send the resulting PushSubscription to the backend; service worker handles the 'push' event and calls showNotification (title/body/icon from manifest). (b) BACKEND - generate/store a VAPID keypair (public key served to frontend, private key kept server-side, persisted across daemon restarts), add POST /api/push/subscribe to store subscriptions and POST /api/notify (or similar) to send a push to all stored subscriptions via the Web Push protocol (use a Go web-push lib, e.g. github.com/SherClockHolmes/webpush-go); subscriptions persisted (survive 'rk daemon restart'). (c) CLI - add 'rk notify <message>' (and/or '--title') subcommand under app/backend/cmd/rk/ that POSTs to the local server's /api/notify so the operator can fire 'rk notify "..."' from any shell; fail-silent if the server is unreachable (matches rk's documented fail-silent discipline). (d) Surface 'rk notify' in 'rk context' Capabilities so agents discover it. NOTES: Web Push requires HTTPS or localhost (a secure context) for the service worker + PushManager to work - document the constraint (the user typically hits run-kit on localhost:3000 or behind a reverse-proxy with TLS, both secure contexts). Aggregator property: a single user's subscriptions = one feed across all operators on the box, matching mmmt's one-shared-feed goal. Keep send fail-silent end to end so a notify failure never stalls the operator loop. Update README.md + docs/ and add the manifest's missing service-worker link. Cross-repo: this UNBLOCKS the eventual 'notify via rk' channel in fab-kit [mmmt]; mmmt is NOT blocked on this (ntfy.sh is its working default).
- [x] [vshd] 2026-07-01: Cockpit host-console home — turn the server-list route (/, ServerListPage, no sidebar) into a machine console: three zones — (1) HOST HEALTH, (2) TMUX SERVERS (existing tiles), (3) SERVICES (separate follow-on item — see the services backlog item). POSITIONING: makes the README/About "remote console for your tmux" claim true in the product (the Cockpit lineage), not just prose; / is the only surface that is about the BOX not a session, so host-global data belongs here. CONTEXT (verified rk 2.6.6): route map is / (ServerListPage, no sidebar), /$server (ServerShell empty state), /$server/$window (terminal), /board/$name; see app/frontend/src/router.tsx. The metrics data ALREADY flows: internal/metrics.Collector reads procfs (CPU 60-entry ring buffer, mem, load, disk, uptime) and the SSE hub broadcasts event:metrics to ALL clients regardless of ?server= (server-independent) — see docs/memory/run-kit/architecture.md and api/sse.go. So the frontend just needs to consume event:metrics on / (it may not subscribe to SSE there today — the / page is pre-server-selection). SCOPE: render host-health tiles/sparklines from the existing MetricsSnapshot (hostname, CPU sparkline from the ring buffer, mem used/total, load 1/5/15, disk, uptime) above or beside the existing server tiles; read-ONLY (no service/systemd/storage management — that would violate Constitution IV minimal-surface; this is Cockpit-the-awareness-view, NOT Cockpit-the-admin-panel). Keep the existing server-tile create/kill/switch behavior intact. FILES: app/frontend/src/components/server-list-page.tsx (add host zone + subscribe to event:metrics), reuse the metrics SSE payload; no backend change needed for health (collector + broadcast already exist). ACCEPTANCE: opening / shows live host metrics that update on the SSE metrics tick; server tiles unchanged; no new route; no database (Constitution II — metrics are in-memory/derived). RELATED: services item (zone 3, depends on this shell); tiles item (the /$server sessions grid, independent). CONSTITUTION: II (no DB — derive from procfs), IV (minimal surface — read-only, no new route), VII (convention over config).
- [x] [70a0] 2026-07-01: Session/window tile grid on /$server with live pane previews — give the /$server empty state (serverIndexRoute, currently a near-empty "select a window above" hint, router.tsx:42) a real job: a tile grid of the servers sessions that EXPAND into per-window tiles, each window tile showing a PREVIEW of what that pane is doing. This is the multi-agent monitoring-DENSITY view — the differentiator vs single-session tools (CC Remote Control, Happy) — "what is every agent doing right now" at a glance. PREVIEW MECHANISM (critical — do NOT use N live terminal relays): render a periodic TEXT SNAPSHOT via tmux capture-pane -p (last ~15-20 lines) pushed over the EXISTING SSE hub, as static text — NOT a live xterm per tile. Rationale: N concurrent live relays re-trigger the HTTP/1.1 6-per-origin connection-pool starvation already documented (see docs/memory/run-kit/ — board-route dynamic-import hang + pane-resize suspension entries); capture-pane over SSE is derive-from-tmux, adds no relay, no pool pressure. Upgrade a tile to a real live terminal ONLY on focus/click (navigate to /$server/$window). SCOPE: (a) backend — add capture-pane snapshotting to the sessions enrichment or a new lightweight SSE event (respect the 2.5s poll cadence + existing 500ms/5s caches; snapshot only visible/expanded windows to bound cost), via internal/tmux (exec.CommandContext + timeout, Constitution I); (b) frontend — new tiles view rendered by serverIndexRoute, session tile -> expand -> window tiles with the text preview, click routes to the terminal. Reuse the existing StatusDot on each tile. FILES: app/frontend/src/router.tsx (give serverIndexRoute a component), new components/session-tiles/*, internal/tmux/tmux.go (capture-pane helper), internal/sessions (join snapshot), api/sse.go (payload). ACCEPTANCE: /$server (no window) shows session tiles that expand to window tiles with a recent-output preview updating on the SSE tick; clicking a window opens its live terminal; NO new relay connections opened for previews; no new route; previews bounded (only expanded/visible windows captured). WATCH: this route has known mount-race/SSE fragility in E2E — be deliberate about SSE subscription lifecycle. RELATED: Cockpit item (the / host console — sibling surface); desktop-window item (a desktop tile would show a thumbnail instead of capture-pane text). CONSTITUTION: II (no DB), IV (no new route — enriches an empty one), I (exec with timeout).
- [x] [5d0z] 2026-07-01: Service tiles on the Cockpit host console — zone 3 of / (depends on the Cockpit host-console item [vshd]; assumes that view exists). Expose the machines LISTENING SERVICES as tiles on the server dashboard, because a listening port is a HOST property (not owned by any tmux window/session) — so / (the box-level console) is the correct ontological home, NOT the per-session sidebar. NOVEL COMBO no competitor has: your agent starts a dev server (e.g. just dev on :5173) and it auto-appears as a tile next to the agent editing it; one click opens it in an iframe window via the EXISTING proxy. CONTEXT (verified rk 2.6.6): the plumbing already exists — /proxy/{port}/* reverse-proxies to http://127.0.0.1:{port} (api/proxy.go, WebSocket-transparent, rewrites localhost refs), and iframe windows are a first-class window type via @rk_type=iframe + @rk_url (set through POST /api/windows/{id}/options; see architecture.md). What is MISSING is DISCOVERY: a listening-ports collector. SCOPE: (a) backend — new listening-TCP-port collector modeled on internal/metrics.Collector / internal/prstatus.Collector (in-memory, periodic, sync.RWMutex snapshot, Constitution II no-DB); derive ports from ss -ltnp or /proc/net/tcp+tcp6 (procfs preferred for no-dep parity with metrics; graceful zero on non-Linux); best-effort process attribution; run through exec.CommandContext + timeout if using ss (Constitution I). Surface via the SSE hub as a server-independent event (like event:metrics) or a GET endpoint. (b) frontend — render service tiles in zone 3 of server-list-page.tsx; each tile shows port + best-effort process/command; primary action "Open in window" creates an iframe window (@rk_type=iframe, @rk_url=/proxy/{port}/) via the existing client helper. KEEP IT DUMB FIRST: just list listening TCP ports and let the user open any — do NOT try to classify "is this an HTTP server" heuristically in v1. FILES: new internal/ports (or internal/services) collector, api wiring (sse.go or a new handler + router.go), server-list-page.tsx tiles. ACCEPTANCE: listening ports appear as tiles on /; clicking "Open in window" spawns a working iframe window through the proxy; collector is in-memory only; fail-silent/graceful on non-Linux or when ss is absent. RELATED: [vshd] Cockpit host console (this is its zone 3 — soft dependency, needs that shell); proxy + @rk_type iframe mechanism (reused, not rebuilt). CONSTITUTION: II (no DB), I (exec with timeout), IV (no new route — a section of /), VII (convention over config).
- [ ] [9lxa] 2026-07-01: Graphical desktop as a run-kit window TYPE (idea reference: PR #71 https://github.com/sahil87/run-kit/pull/71 — take the IDEA ONLY, do NOT merge/port that branch; it predates window-ID routing + the @rk_type seam and is stale). Larger bet, lower priority — sequence AFTER [vshd]/[70a0]. GOAL: stream a graphical desktop (Xvfb + x11vnc + noVNC) into the browser inside run-kits existing session/window model, so "a pane is just a pane" extends to GUIs — browser testing, GUI apps, visual debugging alongside agents. This is the purest expression of the agent-agnostic terminal-console positioning. REIMPLEMENT FRESH against todays architecture (verified rk 2.6.6): (1) model a desktop as a THIRD window type reusing the existing @rk_type option — add @rk_type=desktop alongside terminal/iframe (do NOT resurrect PR #71s name-prefix "desktop:" convention; the option-based seam shipped after that PR and is the canonical mechanism). (2) It renders inside the existing /$server/$window terminal route by branching on rkType (like iframe windows do) — NO new route. (3) Relay: the current relay is /relay/{windowId} (window-ID routing, session dropped from the route since 260529 — PR #71s /relay/{session}/{window} shape is OBSOLETE); a desktop window needs a VNC-over-WebSocket proxy branch keyed by windowId, with the VNC port stored in a window option (e.g. @rk_vnc_port) and dynamic display/port allocation (net.Listen(:0)). (4) Lifecycle via tmux window options (derive-dont-store, Constitution II) — killing the window tears down Xvfb/x11vnc. COSTS/GUARDS (be explicit): heavy NEW runtime deps (Xvfb, x11vnc, a WM) that undercut the single-static-Go-binary story and are Linux-desktop-only — gate behind rk doctor detecting the deps and fail cleanly when absent; SECURITY — a VNC proxy is a bigger attack surface than a PTY relay, review against Constitution I (exec with explicit argv + timeout, validate all inputs, no shell strings) and ensure the VNC socket is loopback-only behind the existing proxy/auth. FILES: internal/tmux (window-type + @rk_vnc_port option, Xvfb/x11vnc lifecycle via exec.CommandContext), api/relay.go (VNC WebSocket branch by windowId), frontend DesktopClient component (noVNC canvas, scaleViewport) branched from the terminal route by rkType, creation entry points (palette/breadcrumb). ACCEPTANCE: create a @rk_type=desktop window; it renders a live scaled desktop in /$server/$window; killing the window cleans up all backing processes (no orphans); rk doctor reports desktop-dep availability; disabled/clean-error when deps absent. RELATED: [70a0] tiles (a desktop tile shows a thumbnail preview instead of capture-pane text); the @rk_type iframe mechanism (the seam to extend). CONSTITUTION: I (security — VNC exec + inputs), II (no DB — tmux options), IV (no new route — a window type), VI (tmux-independent lifecycle).
- [ ] [hmd0] 2026-07-03: Add a help icon on the top bar (next to the theme icon) pointing to https://shll.ai/run-kit
- [ ] [bpnr] 2026-07-05: Allow reorganizing (dragging/rearranging) servers in the server tiles. This should impact the server tree below in the SESSIONS panel also. Ideally save the order for tiles using tmux server vars
- [ ] [8thb] 2026-07-05: Evaluate / discuss / and execute - conversion of the run-kit app into an SPA. right now its an MPA. I can see the navbar reload via flicker duing page chnages. My hope is after becoming an SPA, Navbar becomes a common component, and page changes just re-render it, removing the flicker.
