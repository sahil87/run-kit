---
description: "Session enumeration and group filtering; direct-attach relay (muxed /ws/terminals); link-based pin-sessions (_rk-pin-*); operator home _rk-operator; server-birth CWD pin; window @N addressing; exact-match =name: targets; MoveWindow active-window preservation; sort-windows; auto-naming; SSE dead-server reap; @rk_srv_protected/@rk_srv_managed provenance; the @rk_<scope>_<name> registry (indexed @rk_win_web_<n> web-tab family) + the migration sweep. Test sockets + rk mux reap in test-sockets.md."
type: memory
---
# tmux Session Enumeration

## Multi-Server Model

run-kit connects to **multiple tmux servers**. Server identity is part of the URL path: `/` shows the server list, `/$server` is the per-server shell, `/$server/$window` is the terminal route (there is no `$session` segment). The window identity is the stable `@N` everywhere in code/API; the page URL segment carries only its numeric part (`@N` sans `@`, e.g. `/$server/0`), parse-restored to `@N` (260703-8mpy-numeric-window-url). The user navigates between servers by changing the `$server` route parameter (via sidebar, command palette `Server: Switch to <name>`, or direct URL).

The backend is stateless — every API request carries the server identity (`?server={name}` query parameter for REST/SSE/WebSocket; `serverFromRequest(r)` validates and defaults to `"default"` when missing). All tmux operations use `tmuxExecServer(ctx, server, args...)` which prepends `-L {server}` for named servers. The `"default"` server uses no `-L` flag, connecting to the user's standard tmux server. The config flag `-f {path}` is applied to all named servers (not just runkit).

### Per-View Server Scope

While run-kit *as a whole* is multi-server, **any single view (terminal, dashboard, sidebar tree) is scoped to one server** — the one in the URL. Components that derive state from "current server" (sidebar tree, optimistic overlays, SSE polling) read it from the route via `useSessionContext()`. The `optimistic-context` filters ghosts/overlays by `currentServer` so cross-server in-flight mutations don't leak into the wrong view.

Features that span multiple servers (e.g., the boards view) open multiple concurrent connections — one SSE per contributing server, one WebSocket per pinned terminal — and tag each entry with its source server in API responses.

### Server Discovery

`ListServers(ctx)` discovers available tmux servers by scanning the socket directory at `/tmp/tmux-{uid}/`. Each socket file represents a running server. Returns sorted server names — only sockets whose probe succeeds (dead sockets are dropped). The frontend calls `listServers()` at mount and refreshes via SSE-driven invalidation.

The raw scan and the probe are factored into three shared helpers in `internal/tmux/tmux.go` so the `/tmp/tmux-{uid}` convention lives in exactly one place (consumed by both `ListServers` and the reaper — see § `rk mux reap`) (260529-fww2-rk-reaper-command):

| Helper | Responsibility |
|--------|----------------|
| `socketDirPath() string` | The single definition of the socket-dir path: `fmt.Sprintf("/tmp/tmux-%d", os.Getuid())`. |
| `ScanSocketDir(ctx) ([]string, error)` | Raw reapable candidate names via `os.ReadDir(socketDirPath())` + `filterSocketEntries`. Does NOT probe — **dead sockets ARE included**. Returns `nil, nil` when the dir is absent/unreadable (no servers running). |
| `filterSocketEntries(entries []os.DirEntry) []string` | Keeps the full reapable candidate set: `os.ModeSocket` entries (live/dead tmux servers) **plus** `*.lock` regular files. tmux `.lock` files are **regular files, not sockets**, so they are matched by name suffix (`LockSocketSuffix = ".lock"`) — a socket-mode-only filter would silently drop them and leave the reaper's `.lock` branch dead in real runs. Directories and non-`.lock` regular files are dropped. Split out so the filter is testable against a temp dir without the hardcoded path (`TestFilterSocketEntries`, which drives a real socket + real `.lock` file). |
| `probeServerAlive(ctx, name) bool` | Liveness probe: `tmux -L <name> list-sessions` via `exec.CommandContext` with a 2s timeout; `true` iff the command succeeds. |

`ListServers` calls `ScanSocketDir`, **skips any `.lock` candidate** (those are never servers — a probe would be a doomed subprocess), then probes each remaining candidate **concurrently** (bounded 10-slot semaphore) via `probeServerAlive`, appends only the live ones, and sorts. It returns only sorted probe-success names and silently drops dead sockets.

### Server Lifecycle

- **Create**: Implicit — `CreateSession("0", $HOME, serverName)` starts a new server when the first session is created on it
- **Kill**: `KillServer(server)` runs `tmux [-L server] kill-server`, destroying all sessions
- **Switch**: navigate to `/$newServer/...` — the route change re-mounts the SessionProvider with the new `server` prop and reconnects SSE/WebSocket with the new `?server=` param

## Session-Group Filtering

tmux has a **session groups** feature. When multiple clients attach to the same session (e.g., via `tmux attach`), tmux may create derived session-group copies. This means `tmux list-sessions` returns both the original and derived copies:

```
devshell     grouped=1  group=devshell    ← primary
devshell-82  grouped=1  group=devshell    ← derived copy
run-kit      grouped=0  group=            ← standalone (no group)
```

Grouped sessions share the same windows, so displaying both is incorrect — it shows duplicate projects in the dashboard.

## How We Filter

`parseSessions()` in `internal/tmux/tmux.go` parses the nine-field `ListSessions` format line (`#{session_name}`, `#{session_grouped}`, `#{session_group}`, `#{session_group_size}`, `#{@rk_ses_color}`, `#{session_windows}`, `#{@rk_ses_flair}`, `#{session_id}`, `#{session_path}`); the group filter keys on three of them:

| Variable | Meaning |
|----------|---------|
| `#{session_name}` | The session name (e.g., `devshell-82`) |
| `#{session_grouped}` | `1` if the session belongs to ANY group, `0` otherwise |
| `#{session_group}` | The group name (e.g., `devshell`) — empty if not grouped |

**Filter rule**: keep sessions where `grouped=0` OR `name === group`. Applied to the queried server's results.

- `devshell` → grouped=1, name=group → **keep** (primary)
- `devshell-82` → grouped=1, name≠group → **filter out** (derived copy)
- `run-kit` → grouped=0 → **keep** (standalone)

## Why `session_grouped` Alone Isn't Enough

`session_grouped=1` for ALL members of a group — including the primary session. You cannot simply filter out `grouped=1` sessions without also losing the primaries. The `name === group` check distinguishes primaries from copies.

## Terminal Relay — Direct Attach (no ephemeral)

The relay attaches the PTY **directly** to the session it resolves for the window — there is no per-WebSocket ephemeral grouped session (260602-qn62-move-based-board-pin-sessions). Under the link-based pin model a pinned window is a member of BOTH its home session and its pin-session (260718-co9z-link-based-board-pinning); the relay's **pin-session-first attach preference** (below) is what keeps a board pane's active-window pointer independent — attaching to the single-window pin-session (whose sole window is permanently active) means merely viewing a pinned window never moves its home session's active-window pointer.

**Transport**: all pane relay streams ride ONE muxed `/ws/terminals` socket per tab (handler `app/backend/api/terminals_ws.go`, sibling of `state_ws.go`) (260717-803u-relay-mux). The mux's per-stream `attachStream` does the pick-session → session-scoped select → direct attach → PTY pump. The **session pick** applies a pin-session-first preference (attach the pin-session when it exists, else resolve home — below) (260718-co9z-link-based-board-pinning). The wire protocol is binary `[u32 BE streamId][payload]` data frames plus JSON control ops — the per-stream `open`/`opened`/`resize`/`close`/`closed` set, plus an **id-less `{op:"ping"}`/`{op:"pong"}` liveness control op** (the client heartbeat + wake probes; the server pong rides `tc.enqueueControl`) (260723-rma2). See `architecture.md` § Terminal Relay (full protocol/scheduler) and § Client heartbeat + wake probes for the heartbeat; `ui/terminal.md` § Terminal Relay (frontend) for `RelayMux`.

**Per-stream attach flow** (the mux's `attachStream`, per `open` op): `validate.ValidateWindowID` validates the already-JSON-decoded `windowId` (a per-stream `closed` 4004, NOT a socket close, on failure — the shared validator `decodeWindowID` wraps, so REST and mux entry points cannot drift; #205). It then picks the attach session with a **pin-session-first preference** (260718-co9z-link-based-board-pinning): it probes `HasSession` on the derivable `PinSessionName(windowID)` FIRST and, when the pin-session exists, attaches there; otherwise `s.tmux.ResolveWindowSession(ctx, server, windowID)` (5s timeout) resolves the window's home (non-pin) session (a missing window yields `closed` 4004). Because a pinned window is LINKED into both its home session and its single-window pin-session, attaching to the pin-session gives the stream an independent current-window pointer and leaves the home session's active-window pointer untouched — merely viewing a pinned window (board pane or direct URL) never moves the home pointer. It then **session-scopes** the select via `s.tmux.SelectWindowInSession(session, windowID, server)` (`select-window -t =<session>:@N`, exact-match session part (260717-hikh); NOT a bare `-t @N` — a bare target is ambiguous inside a session group and must agree with the attach; for a pin-session the select is effectively a no-op, its sole window is already active, but the code path stays uniform), then `attach-session -t <session>` via `creack/pty` (initial PTY size rides the open op's cols/rows; attach failure → `closed` 4001). There is no defer-kill — the session is durable and owned by tmux.

**The relay answers terminal identification (DA1/DA2) on the tmux client's behalf** (260801-f715-relay-device-attributes-responder). Each rk relay stream is a real `tmux attach-session` client on a PTY, and on attach tmux feature-detects that client by sending Device Attributes queries to its tty. The per-stream PTY pump (`pumpPTY`) runs every read chunk through a `daFilter` (`app/backend/api/terminals_da.go`) that:

- **Answers at the ptmx** — on a match it writes the canned xterm.js-equivalent reply straight back to `st.ptmx`, which tmux reads as that client's input: `\x1b[?1;2c` for DA1, `\x1b[>0;276;0c` for DA2 (276 is xterm.js's hardcoded version, so tmux feature-detects exactly the capabilities a real xterm.js client advertises). Writes are best-effort — a write error means the PTY died and the pump's next read observes EOF.
- **Strips the query from the browser-bound stream** — matched query bytes are removed from the frame payload, so xterm.js never sees a query and never answers one. Replies are therefore synchronous and **exactly-once per attach**; the browser round-trip is gone structurally. A chunk whose filtered output is empty enqueues no data frame.
- **Carries a bounded partial match across the 4KB read boundary** — a query can straddle the `streamFrameSize` chunk edge (chunk N ends `…\x1b[>`, chunk N+1 starts `0c`). The filter holds a trailing valid query prefix (≤ 4 bytes — `\x1b[>0` awaiting `c`) and resumes matching on the next read; a held sequence that turns out not to be a query is released front-byte-first, forwarded unmodified and in order, with the remainder re-checked (so `\x1b\x1b[c`'s second ESC still starts a match). Bytes still held when the pump exits are dropped with the filter — the pane is closing.

Interception scope is exactly four query forms — DA1 `\x1b[c` / `\x1b[0c` and DA2 `\x1b[>c` / `\x1b[>0c`; no DSR/XTVERSION/OSC handling, and no filtering of the browser→ptmx keystroke direction. No query is a prefix of another, so a full match is never ambiguous. The filter is pure stream processing — no subprocesses (Constitution I untouched), no persisted state (Constitution II) — and one instance is a `pumpPTY` local, so its lifetime is the stream's. Fast path: a chunk with nothing held and no ESC byte passes through zero-copy. Frontend complement in `ui/terminal.md` § Terminal Write Batching.

**Accepted behavioural tradeoffs** (260602-qn62): a home session has a single active-window pointer shared across attachments, so multi-client navigation of an UNPINNED window's home session mutates its active window (two tabs on different windows of one session yank each other; a viewer navigating moves the pointer). A pinned window escapes this — the relay attaches its own single-window pin-session, whose sole window is permanently active, so viewing it is a no-op on the home pointer (the pin-session-first preference above).

**Filter at the chokepoint**: `parseSessions` in `internal/tmux/tmux.go` early-skips `_rk-pin-*` sessions and the `_rk-ctl` anchor. See § Pin Sessions and § parseSessions Filter Chokepoint below.

**Active-window highlight is event-derived per group** (260530-v6hm-active-window-event-derivation): the sidebar/URL highlight is derived from tmux control-mode `%session-window-changed` events tracked per session group (two-tier: event-tracked `@wid` authoritative, base `#{window_active}` only as a cold-start/reconnect fallback). The `internal/tmuxctl/*` derivation subsystem is driven by the `_rk-ctl` anchor and serves the SESSIONS highlight independent of boards. The `$sid`→group resolution it relies on does NOT filter `_rk-ctl` (the anchor must resolve to its base group). Design in `architecture.md` § Active-Window Event Derivation.

## Pin Sessions (`_rk-pin-*`) — Link-Based Board Membership

A window pinned to a board is **linked** (`tmux link-window`) into a **single-window session** named `_rk-pin-<windowDigits>`, so it is a member of **BOTH** sessions at once: its home session (visible natively at its real index in the SESSIONS sidebar) AND its pin-session (rendered as a BOARDS pane) (260718-co9z-link-based-board-pinning). Dual membership is what keeps the sidebar honest — tmux says the window is home, so the sidebar shows it (Constitution II — derive-from-tmux truth, no ghost-row synthesis) — while the board still derives entirely from `_rk-pin-*` session options. A "board" is **not a tmux session** — it is the *set of pin-sessions sharing an `@rk_ses_pin_board` value*. The pin-session stays single-window (the placeholder is killed after the link) so each board pane attaches to a session whose only window is the pinned one, keeping panes' current-window pointers independent (the relay's pin-session-first attach is what realizes it — see § Terminal Relay).

**Reserved naming + reversible helpers** (`internal/tmux/tmux.go`): `tmux.PinSessionPrefix = "_rk-pin-"`. The name is derived deterministically from the window's `@N` id by stripping the leading `@` (tmux session names disallow `@`): `PinSessionName("@42") → ("_rk-pin-42", true)`; the inverse `WindowIDFromPinSession("_rk-pin-42") → ("@42", true)`. Both validate via `ValidWindowID`. The pure, reversible mapping means membership needs **no** name→id lookup table. `ListPinSessionNames(ctx, server)` runs `list-sessions -F '#{session_name}'` and returns every `_rk-pin-*` name (nil, no error, when no server runs) — board membership reads flow through it.

**Membership = session-scoped vars on the pin-session** (`internal/tmux/board.go`), read via `show-options -v -t <pinSession> <opt>`:

| Var (constant) | Meaning |
|----------------|---------|
| `@rk_ses_pin_board` (`tmux.BoardOption`) | which board this pinned window belongs to |
| `@rk_ses_pin_home` (`tmux.HomeOption`) | the home session to restore the window to on unpin |
| `@rk_ses_pin_order` (`tmux.BoardOrderOption`) | fractional order key within the board (`ComputeOrderKey`) |

`@rk_ses_pin_board` is a per-pin-session SESSION-var key; `ComputeOrderKey` and `nextAppendKey` supply ordering. (260602-qn62)

**`Pin(ctx, server, windowID, board)`**: validates `windowID`/`board` (Constitution §I); idempotent — if `_rk-pin-<id>` already exists it is a no-op when the board matches (no re-link, no order-key churn), and **re-stamps `@rk_ses_pin_board`** when it differs (wrong-board re-pin must not silently leave the window on its old board, since the pin-session is the only authoritative place membership lives). Otherwise: resolve the home session via `ResolveWindowSession` (the window has exactly one link at this point, so home is unambiguous); compute the append order key (`nextAppendKey` over the board's existing keys, restricted to this board, computed BEFORE the link); `new-session -d -s _rk-pin-<id> -c <ServerBirthDir()>` (starts with a placeholder window; the `-c <home>` keeps the pin-session's `session_path` from ever dangling — distinct from seams 1–3's `cmd.Dir` pin, since the server already exists at Pin time so this is session-path hygiene, not a server birth — see § Server-Birth CWD Pin); capture the placeholder's `#{window_id}` via `list-windows` (robust to base-index config, not assumed index 0); **STAMP-BEFORE-LINK** the three vars `@rk_ses_pin_home`/`@rk_ses_pin_board`/`@rk_ses_pin_order` onto the still-empty pin-session; then `LinkWindowToSession` the target window in; `kill-window` the captured placeholder so the linked window is the session's sole window (a placeholder-kill failure is non-fatal — logged, cosmetic).
- **STAMP-BEFORE-LINK crash safety (load-bearing)**: stamping the vars while the pin-session is still empty (before the window is linked) means there is never a window-linked-but-unstamped state. A stamp failure strands nothing — the window has not been linked yet, so the empty placeholder-only pin-session is simply killed and the window is untouched in home. Once the link succeeds, `@rk_ses_pin_home` is already durably present, so the window can ALWAYS be unpinned; there is no double-fault rollback and no un-unpinnable pin-session. A LINK failure likewise strands nothing (the window stays home, link not move) — the stamped-but-windowless pin-session is killed.
- **`context.Background()` for teardown**: all rollback/teardown `KillSessionCtx` calls are rooted in `context.Background()`, NOT Pin's ctx — Pin's ctx may be at/near its deadline, and a cancelled parent would make the kill a no-op and orphan the session.

**`Unpin(ctx, server, windowID, board)`**: idempotent (missing pin-session → silent success) with the same validation + board-match guard (a wrong-board unpin is a no-op success so the handler's `board-changed` broadcast never names a board the window was not on). Reads `@rk_ses_pin_home`, then branches on whether the window still has a **live non-pin (home) membership** (`resolveHomeSession` — checked directly against live links rather than trusting `@rk_ses_pin_home`, since the window could have been re-homed):
- **Normal path** (a live home link survives): `killPinSessionIfPresent`. Because the window is LINKED into both its pin-session and its home session, and tmux destroys a window only when its LAST link dies, killing the pin-session drops just the pin link — the window survives in home at its EXISTING position (no move-back, no append, no index loss). The has-session guard tolerates an already-gone pin-session (concurrent kill).
- **Last-link recovery** (the pin link is the window's ONLY link — home died while pinned, or a legacy move-based pin that lives only in the pin-session): a plain `kill-session` would DESTROY the window, so instead recover it by the recorded `@rk_ses_pin_home`:
  - `@rk_ses_pin_home` names a session that is **still alive** (a legacy move-based pin whose home outlived the move) → `LinkWindowToSession` the window back into that live home, then kill the pin-session; the fresh home link keeps the window alive and it lands in its REAL home (not a stray `recovered<id>`). A link failure falls back to the recovered-name rename.
  - `@rk_ses_pin_home` names a session that is **gone** → `RenameSession` the single-window pin-session to the home name (recreating the dead home in place, no placeholder), guarded by a `has-session` precheck so it cannot collide; on a raced-into-existence collision, fall back to the recovered-name rename.
  - `@rk_ses_pin_home` is **empty/corrupt** (unreachable for pins created by this build — stamp-before-link guarantees it — but possible for legacy/corrupt pins) → `recoverPinToRenamedSession`.
  In every branch the three membership options are cleared and the window resurfaces in SESSIONS — a window is never left unrecoverable.

**`recoverPinToRenamedSession`** renames the pin-session to `recovered<windowDigits>`, **collision-guarded** by a bounded numeric-suffix probe (`recovered<id>`, then `-2`, `-3`, … via `has-session`, capped at 100 attempts) so a stale prior-recovery session never makes Unpin error and strand the window; then clears the membership options via `clearMembershipOptions`.

**Pinning a home's only window** does not empty the home — the window stays linked there (link, not move), so the home session keeps a member. **Pins are persistent across rk restarts** (durable user intent; tmux survives restarts per Constitution VI) → there is **NO restore-sweep**. **Legacy move-based pins** (windows whose only link is their pin-session, with no home link) get NO auto-migration (260718-co9z): they present as the last-link case (the window lives only in the pin-session), keep working for board rendering / relay attach / Reorder, and convert to link-based on the next unpin (last-link recovery restores them home) + re-pin.

**`Reorder(ctx, server, windowID, board, newOrderKey)`**: rewrites exactly the target pin-session's `@rk_ses_pin_order` var (via `setSessionOption`); errors if the pin-session is absent or its `@rk_ses_pin_board` ≠ `board`. No sibling renumber. The new key is computed server-side by `ReorderBoard` in `router.go` via `lookupNeighbourKeys` + `ComputeOrderKey`.

**Board listing** (all server-scoped — `move-window` can't cross tmux servers): `ListBoardEntries(ctx, server)` enumerates `_rk-pin-*` sessions on one server and derives `[]BoardEntry` via `pinEntry` (a session with no/invalid `@rk_ses_pin_board` is defensively skipped, not an error). `ListBoards(ctx)` iterates `ListServers(ctx)`, groups entries by `@rk_ses_pin_board`, returns an alphabetical `[]BoardSummary` with per-board pin counts. `GetBoard(ctx, name)` filters entries to one board across reachable servers, sorts by order key, with **NO stale write-back** — membership is derived live, so a killed pinned window's session simply disappears from the listing. A board exists only while ≥1 pin-session carries its name (**no empty boards, no name registry**); the last unpin makes the board vanish.

## Operator Session (`_rk-operator`) — Move-Based Operator Home

Setting `@rk_win_role=operator` on a window physically MOVES it into a per-server infra session named `_rk-operator` (`tmux.OperatorSessionName`, beside `PinSessionPrefix`/`ControlAnchorSessionName`); clearing the role moves a member window back out into the conventional session named after its active pane's cwd basename. Unlike the link-based `_rk-pin-*` pin-sessions (dual membership), the move is single-membership — `_rk-operator` is the operator window's ONLY session. The window's `@N` id survives `move-window` (§ Window Addressing Identity), so the relay, the pinned sidebar row, and `@N`-keyed URLs all hold across promote/demote. The trigger is the role-set/role-clear moment only — an explicit mutation the user (or fab-operator's self-mark) performs, never a detector loop — and an agent process exiting never auto-demotes. All targeting uses `ExactSessionTarget` / `@N` / `$N` forms — never a bare `-t _rk-operator` (§ Exact-Match Session Targets).

**Two role seams, one shared helper pair** (`internal/tmux/tmux.go`): the HTTP options handler (`api/windows.go` `handleWindowOptions`, the `roleSet`/role-clear paths) and the `rk role <operator|clear>` CLI (`cmd/rk/role.go` `runRole`, targeting the pane's own `$TMUX` socket via a `-S` prefix) both route through `MoveWindowIntoOperatorSession` / `DemoteWindowFromOperatorSession` — prefix-parameterized exactly like `ClearWindowRoleExcept`, so the API's `serverArgs(server)` prefix and the CLI's `-S <socket>` prefix both work (`*OnServer` wrappers adapt the server-name flavor for daemon/API callers). Both seams sequence **option-write-first**: the `@rk_win_role` set/unset completes before any move, so a mid-sequence failure degrades to role-set-but-unmoved (the cosmetic-only state the sidebar already handles), never a moved-but-roleless stray. The role write itself is unchanged — the server-scoped radio still clears `@rk_win_role` from every other window first.

**Promote** (`MoveWindowIntoOperatorSession(ctx, prefix, windowID)`): membership probe first — already a member ⇒ no-op (tmux refuses a same-session move, so the probe keeps a repeated promote idempotent). Exact-match `has-session` probe, then detached `new-session -d -s _rk-operator -c ServerBirthDir()` when missing (the board.go pin-session create precedent — `-c` is session-path hygiene on an already-live server, not a server birth; `EnsureOperatorSession` exposes the bare ensure), then `move-window -s @N -t =_rk-operator:`. A session created by this call is born with a placeholder window; the placeholder is captured and killed after the move (best-effort — a stray placeholder is cosmetic) so the session ends holding exactly the promoted window. A pre-existing session of the same name (user-created, foreign windows and all) is adopted as-is — no placeholder is removed.

**Demote** (`DemoteWindowFromOperatorSession(ctx, prefix, windowID)`): no-op when the window is NOT a member (role-clear on a window outside `_rk-operator` — e.g. a legacy cosmetic-era operator — stays a plain option unset). Destination = basename of the window's active-pane cwd (the folder auto-naming convention, § Managed Window Creation; pure `demoteDestinationSession`), created detached if missing with the same placeholder capture/kill, else moved into the existing session. An unreadable cwd or tmux-invalid basename falls back to the `ServerBirthDir()` basename, then to `rk-home`, so demotion stays total; a destination equal to `_rk-operator` itself leaves the window in place. When the moved window was the session's last, tmux destroys the emptied session — expected and clean; the next promote recreates it.

**Radio displacement demotes the displaced carrier**: `ClearWindowRoleExcept` returns the IDs of the windows it cleared, and both seams demote any displaced carrier out of `_rk-operator` within the same promote flow (displaced demotes run before the new operator moves in — an emptied session dies with its last window, and the move-in recreates it). A stranded roleless window would flip the session visible with a confusing mixed population; demote-out preserves the one-operator, hidden-home invariant on every transfer.

**Hiding is content-conditional, never a name-skip**: `parseSessions` deliberately does NOT early-skip `_rk-operator` (contrast the `_rk-pin-*` / `_rk-ctl` skips — § parseSessions Filter Chokepoint). The payload nests windows under sessions and the operator window is moved (single membership), so a parse-level skip would erase the pinned operator row's only data source — pin-sessions tolerate a name-skip because the LINK keeps the window's home membership. Instead `internal/sessions.FetchSessions` computes `ProjectSession.Hidden` (`json:"hidden,omitempty"`) at the join, post per-session window fan-out (window roles are known only there — the `ListWindows` format already carries `#{@rk_win_role}`), via the pure `operatorSessionHidden(name, windows)` rule: true iff the name is `OperatorSessionName` AND the session holds ≥1 window AND every window carries `role == "operator"`. The session and its windows STAY in the payload (REST `/api/sessions` + SSE `sessions` event); user-facing session enumerations exclude hidden sessions at render (see [sidebar](/run-kit/ui/sidebar.md) § Operator Pinned Row). A mixed or stray population (e.g. a dispatch pane worker landing beside the operator) yields `hidden` absent/false — the session surfaces normally, so no window can ever become invisible.

**Infra interactions**: `exit-empty off` is set per server (§ `_rk-ctl` Anchor Session), so an emptied `_rk-operator` dying with its last window is benign. The `rk mux reap` reaper sweeps tmux server SOCKETS by name-prefix + `@rk_srv_ephemeral` mark, never session names — `_rk-operator` is structurally out of its domain (pinned by a `classifyReap` assertion in `reaper_test.go`). Snapshot/restore captures `_rk-operator` as a regular session and re-applies `@rk_win_role` per window, so a promoted operator round-trips as hidden+pinned, not a visible stray (see [layout-snapshots](/run-kit/layout-snapshots.md) § Capture set).

## parseSessions Filter Chokepoint

`parseSessions` in `internal/tmux/tmux.go` is the single chokepoint feeding every user-facing session list — REST `/api/sessions`, SSE `event: sessions`, board derivation, server-aggregate `/api/servers`. It early-skips two name classes:

1. **`_rk-pin-*`** (`strings.HasPrefix(name, PinSessionPrefix)`) — the **pin-session** itself is never a SESSIONS entry (it renders only as a BOARDS pane); a single early-skip here guarantees no pin-session leaks into the SESSIONS UI regardless of future consumers. Under the link-based model the pinned window is ALSO a member of its home session, so it still appears in SESSIONS via that home membership — the skip hides only the `_rk-pin-*` session name, not the window.
2. **`_rk-ctl`** (`name == ControlAnchorSessionName`) — the tmuxctl control-mode anchor.

`_rk-operator` is deliberately NOT in this skip set — the operator window is MOVED into it (single membership), so a parse-level skip would erase the window's data from the payload entirely; hiding for it is content-conditional at the `FetchSessions` join instead (§ Operator Session, pinned by `TestParseSessionsKeepsOperatorSession`).

Beyond the filter, `parseSessions` carries two identity fields through: `#{session_id}` (the tmux session id, `$N` form — the canonical target handle) → `SessionInfo.ID` (`json:"id,omitempty"`) and `#{session_path}` (the session working directory, sent raw) → `SessionInfo.Path` (`json:"path,omitempty"`). `internal/sessions` threads both onto the `/api/sessions` JSON and the SSE `sessions` event as the optional `sessionId`/`sessionPath` keys (the `sessionColor` optional-field idiom — additive, absent on old-backend payloads), consumed by the sidebar session-row identity tip (see [sidebar](/run-kit/ui/sidebar.md)). Derived from tmux at request time, no storage (Constitution II); display-side `~` abbreviation of the path is a frontend concern (§ Design Decisions → Path abbreviation is a frontend display heuristic). (xb77)

## `_rk-ctl` Anchor Session (tmuxctl control mode)

`app/backend/internal/tmuxctl/` opens a long-running `tmux -CC` control-mode connection per tmux server (one Client per socket; see `architecture.md` § tmux Control-Mode Subscription for the package-level design). `tmux -CC` requires an attached session to emit notifications, so the Client creates a hidden anchor session named `_rk-ctl`.

The anchor is created **unconditionally on every observed server** — it is a permanent **session floor**, not a fallback: it keeps the session count above zero for the server's lifetime regardless of how many real sessions exist, so tmux's default `exit-empty on` can never reap the whole server when its last real session closes (260602-a1wo-prevent-exit-empty-server-death). The floor + `exit-empty off` is also what keeps an empty home session alive after its only window is pinned away — pin-session persistence relies on this backstop. **The floor is decoupled from the control-mode attach target** — see § Anchored Target Form below. (Test/e2e sockets are excluded from the control-mode candidate set entirely by `isTmuxSocketCandidate`→`IsTestServerName` in the supervisor, so no anchor is ever created on a leaked `rk-test-*` socket and the always-create path cannot resurrect them — see `architecture.md` § `IsTestServerName`.)

**Naming convention**: literal name `_rk-ctl`. The leading underscore follows the project's internal-entity convention. The single exported constant `tmux.ControlAnchorSessionName = "_rk-ctl"` lives alongside `tmux.PinSessionPrefix` in `app/backend/internal/tmux/tmux.go` (it is a literal name rather than a prefix because there is exactly one anchor per tmux server). It is the single source of truth for the literal — `tmuxctl.Client` and `parseSessions` reference the constant, and the anchor needs no marker option beyond the name.

**Anchored target form**: the Client invokes `tmux [-L <socket>] -CC attach-session -t =<first-existing-real-session> -r` when at least one real user session exists, else `-t =_rk-ctl -r`. The `=` prefix forces exact-match targeting and prevents prefix-match collisions, consistent with daemon detection's `=rk-daemon:=serve`. The `-r` flag puts the connection in **read-only mode** — restricts input only; notifications still emit. Defensive default — future refactors that accidentally wire commands through the control-mode connection cannot mutate tmux state.

**Floor vs. attach target are decoupled** (260602-a1wo-prevent-exit-empty-server-death). On every dial `resolveBootstrap` (`client.go`) does two separate things **once the server is confirmed alive** (see § Anchor is join-only below): (1) `createAnchor` (the floor — R1), then (2) pick the attach target — the first real session if one exists, else `_rk-ctl` (R2). The attach target is the first real name surfaced by `probeAndFirstSession`, which runs `tmux [-L <socket>] list-sessions -F '#{session_name}'` and **skips `_rk-ctl`** so the always-present anchor is never picked when a real session exists (`_rk-ctl` sorts ahead of a lowercase name like `runkit`, so an unskipped listing would wrongly select it and regress the "prefer a real session" contract). Attaching control mode to `_rk-ctl` would also be correct (`%session-window-changed` is global on tmux 3.6a — see [[tmux-control-mode-event-scope]]); preferring a real session is a minimal-diff, zero-event-scope-risk choice, not a correctness requirement.

**Anchor is join-only — never resurrects a killed server** (260602-poka-guard-anchor-no-resurrect). `createAnchor`'s `tmux new-session -d -s _rk-ctl` has an implicit side effect: it *starts* a server when none is listening on the socket. Because `resolveBootstrap` is also the reconnect FSM's dial (`productionDial`, ~250ms→5s backoff), an unguarded `createAnchor` would let a UI-initiated `kill-server` lose a race — the server dies, the FSM re-dials ~250ms later, and `createAnchor` recreates the dead server (the new socket file also fires an fsnotify `CREATE` that re-opens a fresh Client — a self-reinforcing resurrection loop). `createAnchor` therefore **only ever joins an already-running server**: `resolveBootstrap` runs a side-effect-free `list-sessions` probe FIRST via `probeAndFirstSession` (which doubles as the first-real-session selector, so net tmux round-trips stay flat at **3**: `SetExitEmptyOff`, the unified probe+first-session, `createAnchor`). A genuinely dead server — `list-sessions` exit 1 whose stderr matches `no server running` / `failed to connect` / `No such file or directory` (classified by `isServerDeadError` → `matchesServerDeadText`, which delegates to the shared `tmux.IsServerGone` so the three sentinels are defined in exactly one place (260603-gs2t-reap-dead-tmux-servers-sse); `internal/tmux`'s `ListKeys`/`KillServer` key off the same three including the socket-file variant, while `board.go`'s `isAbsentOption` keys off only the first two — `no server running` / `failed to connect`) — is declined with the `errServerDead` sentinel **before** `createAnchor` runs. `productionDial` propagates it, the reconnect FSM backs off instead of resurrecting, and the kill sticks. **Constitution VI floor is preserved**: an alive-but-zero-session server (probe exit 0, empty output) still passes and still gets its `_rk-ctl` anchor — only a truly dead server (exit 1) is declined. The probe lives locally in `tmuxctl` (mirroring `internal/tmux.probeServerAlive`'s `exec.CommandContext` + 2s-timeout pattern rather than calling cross-package) because `doc.go` documents `tmuxctl` as the sanctioned self-contained bypass of the `internal/tmux/` boundary, and `probeServerAlive` collapses the dead-vs-empty distinction this guard must surface.

**Birth hygiene — `anchorCommand` (CWD + env)** (260720-ji0k-pin-server-birth-cwd-home): `createAnchor`'s command is built by the extracted `anchorCommand(ctx, socket) (*exec.Cmd, *bytes.Buffer)`, which sets `cmd.Dir = tmux.ServerBirthDir()` (the server-birth CWD pin — `createAnchor`'s `new-session` remains birth-capable, so a resurrected/first-touched anchor server must not inherit rk's dead CWD; see § Server-Birth CWD Pin) AND `cmd.Env = tmux.CleanEnvForServer()` (the same env sanitization `CreateSession` uses cross-package, so an anchor-birthed server gets the from-home environment symmetric with the `CreateSession` birth path — no `RK_*`/`DIRENV_*` leakage). The builder also wires the stderr buffer it returns, so the **stderr is captured into the error** (load-bearing for `isDuplicateSessionError` — see the concurrent-rk race below). The builder makes the Dir/env/stderr wiring unit-assertable without a live server (`TestAnchorCommand`).

**Concurrent-rk race**: when two `rk serve` instances open a Client against the same fresh tmux server, only one `new-session -d` call succeeds; the loser gets tmux's "duplicate session" error. `tmuxctl.isDuplicateSessionError` treats this as benign and the loser proceeds to attach — multi-rk is supported by construction since `tmux -CC attach` is independent per client.

**Filtering at the chokepoint**: `parseSessions` in `app/backend/internal/tmux/tmux.go` early-skips any line whose `#{session_name}` equals `ControlAnchorSessionName` — parallel to the `_rk-pin-*` pin-session skip (260602-qn62). Single chokepoint — every user-facing list (REST `/api/sessions`, SSE `event: sessions`, board derivation, server-aggregate `/api/servers`) flows through `ListSessions`, so the anchor never leaks into the UI.

**Startup ordering**: `cmd/rk/serve.go` orders startup as `EnsureConfig → tmuxctl.Supervisor.Start → server.ListenAndServe` (no sweep step). The `rk mux reap` operator command hard-skips `_rk-ctl` (see § `rk mux reap`).

**Lifecycle**: the anchor outlives any single `Client` invocation. `Close()` does not delete it — closing the PTY only ends the subscription; the detached session lives on. This is intentional: subsequent `rk serve` invocations re-attach to the existing anchor rather than re-creating it. The anchor only disappears when the entire tmux server is killed.

**`exit-empty off` backstop** (260602-a1wo-prevent-exit-empty-server-death): the anchor floor is the primary guarantee, but it is briefly absent during the close-then-reopen window of a daemon restart/reconnect (the old `-CC` client is gone before the new one runs `createAnchor`). To close that reapable zero-session sliver, run-kit sets `set-option -g exit-empty off` **server-globally** on every socket it dials — imperatively via `tmux.SetExitEmptyOff(ctx, server)` called from `tmuxctl`'s `productionDial` **BEFORE** `resolveBootstrap` runs, on the initial dial **AND** every reconnect of the Client FSM (ordering matters: setting it after the anchor is created would leave the sliver reapable). It is best-effort — a failure logs at Debug and does not abort the dial; on a dead socket the call fails non-fatally and `resolveBootstrap`'s own probe then declines the dead server (see § Anchor is join-only above). The embedded `configs/tmux/default.conf` (build copy `app/backend/build/tmux.conf`) also carries `set -g exit-empty off`, so run-kit-*created* servers get the floor at birth via `-f` before the first dial; the imperative path covers the hand-created/foreign servers the embedded conf never reaches (the exact gap that let `kit` inherit tmux's default `on`).

**Lifetime contract — explicit kill only** (260602-a1wo-prevent-exit-empty-server-death): a managed tmux server is torn down **only** via `kill-server` / `rk mux reap` (260529-fww2-rk-reaper-command). Empty (anchor-only) servers persist by design — this is the deliberate tradeoff that makes accidental `exit-empty` death impossible. run-kit adds no auto-reaping of anchor-only servers and no cross-process refcounting / shared state (Constitution II forbids it — no single `rk serve` can know it is the last one that wants a given server's anchor).

**Why a hidden anchor, not an arbitrary user session**: attaching to a user session would couple the control-mode lifecycle to that session's lifetime — killing the user session would tear down the subscription. A purpose-built anchor decouples the two. `tmux -CC` cannot use `new-session -d` without attach (it requires an attached client to emit notifications), so "no anchor at all" is not an option.

## Window Addressing Identity (`@N`)

A *specific existing window* is addressed by its stable tmux **window ID** (`@N`, tmux's `#{window_id}`), not the mutable window *index* (260529-chgz-window-id-routing). A window ID is **server-global and a self-contained `-t` target** — `tmux kill-window -t @5` needs no session prefix — so window-targeting tmux commands pass the windowID directly (no `session:index` target string). The window **index** is used only for *positional* operations (reorder/move), which are inherently "move to position N" and cannot be expressed by ID. **Sessions are identified by name.**

This identity is consistent across all layers — `@N` is the window identity everywhere in code/API: URL (`/$server/$window`, no `$session` segment; the page URL segment carries only `@N`'s numeric part, sans `@`, parse-restored to `@N` (260703-8mpy-numeric-window-url)), HTTP API (`/api/windows/{windowId}/...`), the terminals-mux `open` op's `windowId` field (260717-803u-relay-mux), tmux targets, and the fab-tier derivation's window rollup (§ Fab-Tier Derivation). The window store and the boards feature (`@rk_ses_pin_board` stores `<window_id>:...`) are windowID-keyed. Path params are validated by `validate.ValidateWindowID` (`^@[0-9]+$`) — stricter than `ValidateName`, which permits `@` but not the `@N` shape (constitution §I). Window IDs are never user-typed; they originate from tmux's `#{window_id}`. See `architecture.md` § API Layer and § Terminal Relay, and `ui/routes-and-shell.md` § URL Structure.

## Name-Validation Charset Contract (new-name tightening vs. permissive lookups)

Session and window names carry a **two-tier validation contract** in `internal/validate` (`validate.go`), keyed on whether the value names a **to-be-created / rename-target** entity or an **existing** one:

- **New names** (create / rename-to targets) validate through `ValidateNewName(name, label)` — the permissive `ValidateName` rule plus a **"no spaces"** rejection. This makes the safe charset a real backend contract rather than only UI steering. Applied at exactly four handler call sites: session create and session rename-to (`api/sessions.go`), window create (non-empty name only) and window rename-to (`api/windows.go`).
- **Existing-name lookups** stay on the permissive `ValidateName` — session URL params for rename/color/kill/order (`api/sessions.go`), the `windows.go` session param and `MoveWindowToSession`'s `TargetSession`, the `upload.go` session param, and both `riff.go` session values (`handleRiffSpawn` `body.Session` and `handleRiffPresets` — riff derives the repo root from the named session's cwd, creating nothing under that name). Sessions/windows created outside run-kit (raw `tmux rename-session`) can carry spaces, so rename/kill/upload targeting a pre-existing spacey name must keep working.

`ValidateName` is the base rule: non-empty, ≤ `MaxNameLength` (128), no `forbiddenChars` (`; & | ` backtick ` $ ( ) { } [ ] < > ! # * ?` plus control chars), no `:`/`.`. `ValidateNewName` layers over it with the single space check.

**Hyphens stay legal on the backend for both tiers** — session names included. Internal sessions (`_rk-pin-*` pin-sessions, the `rk-test-*` sockets, session-group names) rely on hyphens, so rejecting them would break run-kit itself. The **session hyphen→`_` divergence is UI-only steering** (`ui/routes-and-shell.md` § Live Safe-Name Conversion): the frontend `toSafeSessionName` converts hyphens to avoid session-group naming collisions, while `toSafeWindowName` keeps them (`riff-*` windows use hyphens legitimately) — but the backend accepts hyphens for either kind. `ValidateServerName` (`^[a-zA-Z0-9_-]+$`) and `ValidateWorktreeName` (rejects spaces, leading `-`, `/`) are already strict.

**Compatibility posture** (user-accepted): a pre-existing spacey name remains fully operable (lookups permissive) but cannot be the *target* of a run-kit create/rename — the new-name rule rejects the space before any tmux call. `POST /api/sessions {"name":"My problem"}` returns 400 ("cannot contain spaces") and creates nothing. Table tests in `validate_test.go` cover space rejection, `ValidateName` inheritance, and boundary cases; handler tests in `api` cover create/rename space rejection plus a permissive spacey-old-name rename that succeeds. (260722-ln4n-auto-safe-name-conversion)

## Exact-Match Session Targets (`=name:` / `=session:windowSpec`)

**Every `internal/tmux` command that passes a session name as a `-t` target composes it through `tmux.ExactSessionTarget(session) → "=" + session + ":"`** (exported — `internal/riff` consumes it too) (260717-hikh-exact-tmux-session-targets). The leading `=` disables tmux's prefix/fnmatch name matching (exact match only); the trailing `:` forces the string to parse as a **session**, never a window name. Session-qualified *window* targets (`swap-window`, `select-window`) compose through the unexported `exactWindowInSession(session, windowSpec) → "=" + session + ":" + windowSpec`, where `windowSpec` is itself unambiguous within the session — a window ID (`@N`) or a numeric index. Both live in `internal/tmux/tmux.go`.

**Why the colon is load-bearing**: tmux resolves a bare `-t <session>` **differently per command**. For `list-windows` the `-t` is a *session* target (exact session wins). But for `new-window` and `list-panes` (even under `-s`) the `-t` is a *window* target — tmux matches the bare string against the **window names of the current/attached session BEFORE** trying it as a session name. Because run-kit auto-names **both windows AND sessions** from folder basenames (`automatic-rename-format '#{b:pane_current_path}'`, § Managed Window Creation), a window named like a session is **routine, not exotic** — so a bare name silently hijacks the command. Observed live (server `ext`, 2026-07-17): the attached session `0` had a window named `planner` while a session named `planner` also existed, so `new-window -a -t planner` (a UI "+ New Window" on session `planner`) created its window **in session `0`**, and `list-panes -s -t planner` returned **session 0's panes** (smoking gun: the same pane id `%0` appearing twice in one `/api/sessions` response). `=planner:` makes both resolve to the real session `planner` unambiguously.

**Safety**: session names are validated to contain no `:`/`.` (`validate.ValidateName`) and cannot start with `=` in practice, so the composition is injection-safe; `_rk-pin-<digits>` pin-session names and the `_rk-ctl` anchor are equally safe to wrap. Empirically verified against every touched command shape including **numeric** session names (`=0:`) and pin-session names.

**Call sites using the exact forms**: `ListWindows` (both the `list-windows -t` read AND the `list-panes -s -t` read), `buildCreateWindowArgs` / `CreateWindowWithOptions` (the `new-window -a -t` misroute vector), `KillSessionCtx`, `RenameSession`, `SetSessionColor` / `UnsetSessionColor` (`@rk_ses_color`), `MoveWindow` (its `list-windows` read, the `swap-window` src/dst chain via `exactWindowInSession(session, index)`, and the active-window-restore `select-window` via `exactWindowInSession(session, @id)`), `MoveWindowToSession` (dst `=session:`), `SelectWindowInSession` (`=session:@id`), and `board.go`'s `showSessionOption` / `setSessionOption`, all five `has-session` probes (`Pin`, `Unpin`, `killPinSessionIfPresent`, `Reorder`), the pin-session placeholder `list-windows`, and the recovery-path `set-option -u` calls. **`daemon` and `tmuxctl` carry their own `=` discipline** (e.g. the anchor's `=_rk-ctl` / daemon detection's `=rk-daemon:=serve` per § `_rk-ctl` Anchor Session); `internal/tmux` and `internal/riff` are the `ExactSessionTarget` consumers.

**Rule for new code**: any new code path that passes a session name to tmux MUST use `ExactSessionTarget` (or `exactWindowInSession` for session-qualified window targets). Extending the `=` hygiene to *session*-target commands too (`kill`/`rename`/`has-session`/options — not only the two proven *window*-target offenders) is defensive: it is behavior-identical when the named session exists (the exact match already wins there) and prevents prefix/fnmatch misroutes when it does not. `TestExactSessionTarget` unit-pins the `=name:` form (numeric and pin-session names included); `TestSessionWindowNameCollision` (real-tmux integration) reproduces the session/window name collision and asserts both the create routing and pane-join isolation.

**The same discipline holds outside Go**: the Playwright fixture `app/frontend/tests/e2e/_tmux.ts` composes `=${session}:` for its window-scoped commands (`new-window`, `list-windows`), so specs seeding real tmux state cannot hit the misroute either — the hazard is a property of tmux's target grammar, not of the Go layer. See `architecture.md` § Playwright E2E Tests.

**riff alignment**: `internal/riff`'s daemon-path spawn targets go through the same exact forms — it imports `rk/internal/tmux` (no import cycle; `tmux` imports only `validate`/stdlib). `sessionTarget(spec)` returns `tmux.ExactSessionTarget(spec.Session)` on the daemon path (the same `new-window -t` misroute vector `CreateWindow` guards against), `windowTarget(spec, name)` returns `=session:name` (the window-name part stays **non-exact** — riff uniquifies window names within the session pre-spawn, so the cross-session hazard is only the session part), and the `listWindowNames` collision probe targets `=session:`. The CLI path (empty `spec.Session`) is unscoped (`""`/bare `name`). See [rk-riff](/run-kit/rk-riff.md) § Server + Session Targeting Seam.

### Pane join keyed by window id (not index)

The `ListWindows` pane join is keyed by the **stable window ID**, not the window index (260717-hikh). `paneFormat` field 0 emits `#{window_id}`; `parsePanes` returns `map[string][]PaneInfo` keyed by window id, with a `ValidWindowID` guard dropping malformed first fields (non-`@N` lines are silently skipped — `TestParsePanes` covers this); `ListWindows` attaches `byWindow[w.WindowID]`. The panes come from a **separate** `list-panes` call than the windows (`list-windows`) they are joined to, so an index join would silently glue the wrong session's panes onto a window whenever the two calls' targets diverge (the bare-name collision above) or a concurrent reorder shifts indices between the calls. A window-id join can only attach a pane to the window that actually owns it — so a residual divergence degrades to a **visibly-empty pane list** instead of another session's cwd/branch/agent-state (silent data corruption — the live `ext` symptom).

## Server-Birth CWD Pin ($HOME, fallback `/`)

A tmux server process inherits the CWD of whichever process FIRST touches its socket, and never chdirs for the rest of its life. So every rk seam whose exec can birth (start) a tmux server pins that exec's working directory to the operator's home via the shared helper `tmux.ServerBirthDir()` (`internal/tmux/tmux.go`): `os.UserHomeDir()`, falling back to `/` when the home cannot be resolved (`/` always exists and can never dangle — mirroring tmux ≤ 3.6a's own child-side fallback chain target → `$HOME` → `/`). This makes the born server's CWD independent of wherever rk itself was launched (260720-ji0k-pin-server-birth-cwd-home).

**The failure it defends against — tmux 3.7 dead-server CWD.** When rk's own CWD is a deleted directory (e.g. `rk daemon start` was run inside a git worktree that is later removed), a server that inherited that CWD sits on a dead inode. tmux 3.7 restructured pane spawn so the **server process itself** chdirs to the pane's target before forking, guarded by `if (getcwd(...) != NULL)`; when the server's own CWD is gone that `getcwd` fails and the **entire chdir block is silently skipped** — every new pane is born on the dead inode **even when a valid `-c` path was passed**, filling with `shell-init: error retrieving current directory: getcwd: cannot access parent directories`. tmux ≤ 3.6a is immune (its chdir runs in the forked child with a target → `$HOME` → `/` fallback), but even there a dead server CWD makes `-c`-less sessions land in `/` and server-forked subprocesses run on the dead inode. A live server's CWD cannot be changed — only a restart clears it — so the pin is birth-time prevention, not remediation.

**Two mechanisms, five seams** — a server-birth `cmd.Dir` pin (seams 1–3 and 5, where the exec may START the server) vs. a session `-c` hygiene flag (seam 4, where the server already exists):

| Seam | Where | Mechanism |
|------|-------|-----------|
| `internal/daemon.startSession` | births the rk-daemon server (`socket rk-daemon`) | `cmd.Dir = ServerBirthDir()` via `runTmuxInDir` (see `architecture.md` § Daemon Lifecycle) — the **highest-leverage** pin: `rk serve` runs inside this session, and every LATER server birth inherits `rk serve`'s CWD |
| `internal/tmux.CreateSession` | births user-facing servers (create-server API / session-create on a dead socket) | `cmd.Dir = ServerBirthDir()` via `Run(ctx, full, RunOpts{Env: CleanEnvForServer(), Dir: ServerBirthDir()})` — the runner core carries both the env and dir overrides |
| `internal/tmuxctl.createAnchor` | births/resurrects the `_rk-ctl` anchor server | `cmd.Dir = ServerBirthDir()` **and** `cmd.Env = CleanEnvForServer()` via `anchorCommand` (see § `_rk-ctl` Anchor) |
| `internal/tmux/board.go` `Pin` | pin-session `_rk-pin-<id>` on a LIVE server | `-c <ServerBirthDir()>` on the `new-session` argv (see § Pin Sessions) — `session_path` hygiene, NOT a server birth (the server already exists) |
| `internal/tmux/layout.go` `CreateSessionForRestore` | births the target server on the first `rk snapshot restore` session create | `Run(ctx, full, RunOpts{Env: CleanEnvForServer(), Dir: ServerBirthDir()})` — same pins as `CreateSession`, since a restore onto a dead server is by definition a server birth (see [layout-snapshots](/run-kit/layout-snapshots.md)) |

**Daemon env is deliberately NOT sanitized** — only `createAnchor` gains `CleanEnvForServer` (env sanitization is env, distinct from the CWD pin). `startSession` pins only CWD, never env: the inner `rk serve` reads `RK_PORT`/`RK_HOST`/`RK_DAEMON_LOG` from the daemon session's env, so sanitizing there would break config (`config.Load` is env-only, so pinning the daemon session's start dir to home is safe for it — the exe path is absolute).

## Impact on Other Operations

All tmux functions accept a `server string` parameter:

- `ListSessions(server)` — queries only the specified server
- `ListWindows(session, server)` — lists windows for a session on the specified server
- `SelectWindowInSession(session, windowID, server)` — session-scoped select (`select-window -t =<session>:@N` via `exactWindowInSession`; the session part is exact-match (260717-hikh), see § Exact-Match Session Targets). Two production callers: the REST `/select` handler resolves the owning (home, non-pin) session via `ResolveWindowSession` then calls this, and the terminals-mux per-stream `attachStream` scopes the select to whichever session it will attach — the pin-session when the window is pinned (a no-op in effect, its sole window is already active) else the resolved home session (`api/terminals_ws.go` — pick-session → scoped select → direct attach)
- `ResolveWindowSession(ctx, server, windowID)` — returns the window's **HOME (non-pin) session** name for a window ID. Fast path is the targeted O(1) `display-message -t <windowID> -p '#{session_name}'` lookup (260609-enic-restore-display-message-resolve-window); under dual membership a pinned window is a member of two sessions and `display-message` may report EITHER link (tmux's pick across links is order-unspecified), so **when the naive result carries `PinSessionPrefix` this re-resolves deterministically to the non-pin owner** via `resolveHomeSession` (a `list-windows -a -F "#{session_name}\t#{window_id}"` enumeration that picks the session for `@N` that is not a `_rk-pin-*` name) (260718-co9z-link-based-board-pinning). A window whose ONLY link is its pin-session (home died while pinned, or a legacy move-based pin) **legitimately resolves to the pin-session**. The relay layers its own pin-session-first attach preference ABOVE this (§ Terminal Relay); this function's job is to name the home session for callers that need it. Used by `Pin` (to remember the home — pre-link, one link, unambiguous), the REST `/select` handler, `ProjectRoot`, and the terminals-mux per-stream attach fall-through when the window is NOT pinned (on the 5s open-time budget). A tmux non-zero exit whose stderr names a missing window, or an empty result, both mean "window not found" (`window %q not found`) — the stream emits a `closed` 4004 (a per-stream event; the socket stays open); genuine operational errors (dead server, deadline) propagate unchanged so callers distinguish "window gone" from "tmux unavailable". See [[resolve-window-session-on-relay-connect]] for the regression this path defends against
- `ListPinSessionNames(ctx, server)` — returns every `_rk-pin-*` session name via `list-sessions -F '#{session_name}'`; board-membership reads (`ListBoardEntries`) flow through it. Returns nil (no error) when no server runs. It is the only "raw session-name listing" helper, scoped to pin-sessions, not a general escape hatch
- `CreateSession(name, cwd, server)` — creates sessions on the specified server. Because its `new-session` may FIRST-touch the socket and birth the server, the exec runs `Run(ctx, full, RunOpts{Env: CleanEnvForServer(), Dir: ServerBirthDir()})` (the package's exported runner core — see `architecture.md` § tmux Runner Core) — pinning both halves of the "born as if started from $HOME" contract: `cmd.Env = CleanEnvForServer()` (env sanitization — see `architecture.md`) AND `cmd.Dir = ServerBirthDir()` (the server-birth CWD pin — see § Server-Birth CWD Pin below). An explicit `cwd` argument still lands as `-c <cwd>` on the session; the `cmd.Dir` pin anchors only the server process and the session's default start dir
- `CreateWindow(session, name, cwd, server)` — creates a session-scoped window; argv built by the pure `buildCreateWindowArgs(session, name, cwd)` — omits `-n` when `name == ""` (tmux auto-names to the folder basename via `automatic-rename-format`), includes `-n <name>` otherwise (pinned). No follow-up rename. See § Managed Window Creation (260707-j66b-unnamed-windows-autoname-folder)
- `ReloadConfig(server)` — hot-reloads config via `source-file` on the specified server
- `KillSession(session, server)` — kills the named session on the specified server (thin `context.Background()` wrapper around `KillSessionCtx`)
- `KillSessionCtx(ctx, server, session)` — ctx-accepting variant; `Pin`'s rollback/teardown passes `context.Background()` so the kill survives a near-deadline Pin ctx (an expired parent would make the kill a no-op and orphan the session)
- `SendKeys(windowID, keys, server)` — sends keys to the window identified by `windowID` on the specified server. **Window-targeted**; chat-send uses the PANE-targeted primitives below instead (a window `-t` target routes to the session's active pane, which in a split may not be the agent pane; `SendKeys` also appends an unconditional trailing `Enter` and interprets its argument as key names)
- **Pane-targeted chat-send primitives** (260714-jdyg-chat-send) — the injection path for `POST /api/windows/{windowId}/chat/send` (see [chat](chat.md) § Send Path + `architecture.md` chat/send endpoint row). Each targets the resolved **pane** (`%N`), not a window, and is a `*Ctx` variant bounded by the **caller's** context — the handler threads ONE shared deadline (`chatSendTotalBudget` ≈ 4s) through the whole sequence rather than granting each its own 10s timeout, so the route stays under the code-review 5s rule. All are argv slices (Constitution §I), surfaced on `api/router.go`'s `TmuxOps` (prod + mock) as `SetChatSendBuffer`/`PasteChatSendBuffer`/`SendEnterToPane` for handler testability:
  - `ChatSendBuffer = "rk-chat-send"` — the **named** tmux paste buffer (const). Named, not the anonymous top-of-stack, so loading a chat message never clobbers the user's buffer stack; the paste's `-d` deletes it afterwards.
  - `SetChatSendBufferCtx(ctx, text, server)` — `set-buffer -b rk-chat-send -- <text>`. Text is a DISCRETE argv element (no shell string, no stdin — `tmuxExecServer` has no stdin plumbing, so `set-buffer <text>` beats `load-buffer -`), stored verbatim incl. newlines / tmux key names / special chars. **The `--` option terminator is load-bearing**: without it a message that itself starts with a dash (e.g. `--force is broken`) is parsed as `set-buffer` flags and hard-fails (500); with `--`, leading-dash text stores verbatim (verified tmux 3.6a — `TestSetChatSendBuffer_LeadingDash`).
  - `PasteChatSendBufferCtx(ctx, paneID, server)` — `paste-buffer -d -p -b rk-chat-send -t <paneID>`. `-p` requests **bracketed paste** (the Claude Code TUI enables it, so a multiline / special-character message lands as one literal block with no per-line submission); `-d` deletes the buffer after pasting. The set→paste pair is the critical section serialized by a global mutex in the handler (the named buffer is a single server-wide resource; see [chat](chat.md) § Send Path).
  - `SendEnterToPaneCtx(ctx, paneID, server)` — `send-keys -t <paneID> Enter` (a single literal `Enter` to the PANE). Sent by the send handler ONLY after the novelty echo probe confirms the paste reached the live input buffer — never blindly.
  - `CapturePaneCtx(ctx, paneID, lines, server)` — the existing pane-capture helper (also surfaced on `TmuxOps` as `CapturePane`), reused by the send handler's baseline + probe captures. Not new to this change (already used by tile previews).
- `MoveWindow(windowID, dstIndex, server)` — reorders a window within its own session. Source addressed by stable `windowID`; destination remains positional. Resolves the source's current index from the ID exactly once (via `resolveWindowSessionIndex`, a bare-`@N` `display-message -t <windowID> -p '#{session_name}\t#{window_index}'` lookup), then emits the full adjacent-`swap-window` bubble sequence as a **single `\;`-chained tmux invocation** rather than one subprocess per step — so no other mutation can interleave mid-reorder (insert-before semantics) (260529-jad6). tmux preserves the window's ID across the swaps. **Active-window preservation** (260714-6pe6-preserve-active-window-reorder): tmux pins the session's active window to its *index slot* during `swap-window`, so without an explicit restore a DIFFERENT window ends up in the active slot after the shuffle (moving `w3` to index 0 in `[w0, w1*, w2, w3]` would leave `w0` active). `MoveWindow` therefore **captures the pre-shuffle active window ID** from the *same* `list-windows` call it already runs — the `-F` format is `#{window_index}\t#{window_active}\t#{window_id}`, so the active-window ID rides the existing subprocess (**no extra tmux call**) — and appends a final session-qualified `select-window -t =<session>:<activeWindowID>` (composed via `exactWindowInSession` — exact-match session part (260717-hikh); the `swap-window` src/dst indices in the same chain likewise compose through `exactWindowInSession(session, index)`) to the SAME `\;`-chained invocation as the swaps. The restore is atomic with the swaps (no SSE poll or concurrent mutation observes the intermediate active-window state), keeping the session's active window **invariant** across within-session reorders — sidebar drag-reorder and the palette `Window: Move Up/Down` actions all share the `moveWindow` API and must not yank the user's viewed terminal to a wrong window. The restore is appended **only on the swap-executing path** — the `srcIndex == dstIndex` and `srcPos == endPos` early returns perform no swaps and emit nothing; the active-window capture is a pure parse of the already-fetched output. Restoring by stable ID (not index) also covers the edge where the dragged window is itself the active one — `select-window -t =<session>:@N` restores it wherever it landed. The target is **session-qualified** (exact-match session part (260717-hikh)), not a bare `@N`, because a bare window-id select is ambiguous inside a session group (see `SelectWindowInSession` above). **Scoping caveat**: the session-qualified restore scopes to *the same session as the swaps*, but that session is whichever group member the bare-`@N` `resolveWindowSessionIndex` lookup resolves to — `MoveWindow` commits BOTH its swaps and its restore to that one resolved member and has no caller-supplied session. Cross-member disambiguation (preserving a *specific* named member's active window regardless of which member the id resolves to) would require changing the shared `resolveWindowSessionIndex` helper; empirically (tmux 3.6a, isolated socket) the bare-`@N` and session-qualified restores are equivalent *within* the resolved member, so the qualification is a minimal defensive fix, not a full group-safety guarantee
- `MoveWindowToSession(windowID, dstSession, server)` — moves a window from its current session to another on the specified server via `tmux move-window -s {windowID} -t =<dstSession>:` (window-ID source, exact-match session destination via `ExactSessionTarget` (260717-hikh)). Destination index is auto-assigned by tmux; the window's ID is preserved (tmux contract). Live caller: `POST /api/windows/{windowId}/move-to-session`. The board layer does not use it (Pin/Unpin are link-based (260718-co9z))
- `LinkWindowToSession(windowID, dstSession, server)` — **links** a window INTO another session via `tmux link-window -s <windowID> -t =<dstSession>:` (exact-match destination via `ExactSessionTarget`), leaving it a member of its original session(s) too (260718-co9z-link-based-board-pinning). Mirrors `MoveWindowToSession` (window-ID source, session destination, `withTimeout` + explicit argv, Constitution §I) but does NOT remove the window from its source — tmux destroys a window only when its LAST link dies. The window's ID is preserved (tmux contract). Two callers, both in `board.go`: `Pin` (link the target into its pin-session, keeping it home too) and `Unpin`'s last-link recovery (link a homeless window back into its still-live recorded `@rk_ses_pin_home` before killing the pin-session)
- `resolveHomeSession(ctx, server, windowID)` (unexported, `tmux.go`) — enumerates every session the window is linked into (`list-windows -a -F "#{session_name}\t#{window_id}"`) and returns the first non-`_rk-pin-*` session; `ok=false` when the window is linked ONLY into pin-session(s) (no live home). Read-only. Backs `ResolveWindowSession`'s pin→home re-resolve and `Unpin`'s live-home-membership branch

## Sort Windows Verb (`POST /api/sessions/{session}/sort-windows`)

A **one-shot, session-scoped reorder verb** — never a standing auto-sort (no watcher, no re-sort on state change). Body `{"by": [<key>, ...]}` — an ordered array of 1–3 **unique** keys from the closed set `status | created | name` (`validateSortKeys` enforces the allowlist: an empty array, >3 keys, a duplicate key, a non-array `by`, an unknown key, malformed JSON, or an invalid session name is a 400 with zero tmux mutations; the session path param validates via `validate.ValidateName`'s permissive existing-name tier, § Name-Validation Charset Contract). The first key is the primary sort; each later key breaks ties within equal earlier keys (spreadsheet semantics — a `created`-primary composite is degenerate since `@N` never ties, and is accepted without error). A session absent from the server's list is a 404. The handler (`handleSessionSortWindows`) and its pure helpers live in `app/backend/api/sortwindows.go` — the rank/plan functions consume the *enriched* `WindowInfo` (fab/PR/agent rollups) that only exists post-`FetchSessions`, so they sit in the `api` package beside their only consumer, not in `internal/tmux`. All windows sort within their own session; nothing crosses sessions.

- **`created`** — ascending numeric sort on the `@N` window ID's numeric part (`windowIDNum`; `@9` sorts before `@10` — numeric, not lexicographic; an unparseable ID sinks to the end). tmux assigns window IDs monotonically at creation and exposes no window-creation timestamp, so the numeric ID IS the creation order, derived with zero extra state (Constitution II).
- **`name`** — case-insensitive ascending window name (`strings.Compare` over `strings.ToLower` — simple Unicode lower-casing, no locale collation), meaningful as primary (duplicate folder-basename auto-names are routine) and as tie-break; equal names fall through to the next key / current order.
- **`status`** — attention-first rank (`statusRank`) linearizing the status pyramid's tier precedence into a total order; a window's rank is the MINIMUM (most attention-demanding) rank among its matched predicates, no match ⇒ rank 4. The pyramid's decision table (`docs/specs/status-pyramid.md` § Decision Table) defines which signal owns the sidebar DOT; this table defines tab ORDER from the same signals — the two rankings cross-reference each other (a code comment beside `statusRank` points at the spec):

| Rank | Tier | Predicates (on enriched `WindowInfo`) |
|------|------|----------------------------------------|
| 0 | attention | `AgentState == waiting` · open PR with `PrChecks == fail` or `PrReview == changes_requested` · `FabDisplayState == failed` |
| 1 | active work | `AgentState == active` · fab change in-flight (`active`/`ready`/`pending`) · open PR with `PrChecks == pending` |
| 2 | settled | `PrState == merged` · fab `done` · healthy open PR (checks pass/none, no changes requested) |
| 3 | idle agent | `AgentState == idle` |
| 4 | plain | everything else (a fab-`skipped` window with no other signal falls through to here, per the pyramid ladder) |

Ordering is ONE **stable sort** (`sortWindowsTarget`, copy-non-mutating) whose comparator (`compareWindowsBy`) walks the key list in order — not sequential re-sorts; windows equal under ALL keys keep current relative order (stability is the final tie-break), so re-running any composite is idempotent. Move planning is pure (`planSortMoves`): the set of index VALUES is invariant under `swap-window`, so the plan walks target positions over the session's sorted current index slots, simulating each insert-before move, and emits a `MoveWindow` ONLY for a window whose position actually changes — an already-sorted session yields an empty batch (no tmux mutation). Execution reuses the existing `tmux.MoveWindow` through `TmuxOps` (insert-before, active-window preservation — § Impact on Other Operations); on ≥1 applied move the handler wakes the SSE hub (`s.initSSEHub(); s.sseHub.wake(server)` — the option-write wake pattern) so the reorder repaints immediately rather than on the next derive tick. The response is 200 `{"order": ["@N", ...], "moved": N}` — the applied window-ID order; the UI updates via the normal SSE derive tick (no optimistic reorder — the sidebar's derive-over-store reconcile handles a new SSE order like any manual `tmux move-window`). Hidden/infra sessions are not offered the verb in the UI (pin-sessions are filtered by `parseSessions` ⇒ 404). Tests: `api/sortwindows_test.go` (rank tiers, composite key-walking with name tie-break, name case-insensitivity, stability/idempotence, `@9` vs `@10`, move-plan, handler 400 matrix/404/only-when-changed) and the real-tmux e2e `app/frontend/tests/e2e/sort-windows.spec.ts`. (260822-ga8z-sort-tabs-status-date)

## API Server Parameter

All API endpoints accept `?server=` query parameter via `serverFromRequest(r)` helper. The helper validates the server name using `validate.ValidateName` and defaults to `"default"` on invalid/missing input. The SSE hub polls per-server — only servers with active SSE clients are polled.

Server management endpoints:
- `GET /api/servers` — lists available servers via socket directory scan; each entry carries the `rank`/`ephemeral`/`protected`/`managed` class flags, each read per live server per request inside the fan-out (a read failure logs at warn and yields `false` for that flag, never a 5xx)
- `POST /api/servers` — creates a server (starts session "0" in $HOME)
- `POST /api/servers/kill` — kills a server via `tmux kill-server`
- `POST /api/servers/adopt` — converts an external (unmarked) server to managed: stamps `@rk_srv_managed` then sources the managed conf via `ReloadConfig`, best-effort unmarking on a failed reload; idempotent `200 {"status":"already-managed"}` on an already-managed target (including `rk-daemon` by derivation — deliberately unlike protect's 400, since adopt's bulk-migration role needs idempotency); wakes the SSE hub on success (§ Server-Scoped User Options → `@rk_srv_managed`)

### SSE Poll-Set Lifecycle — Connect to Reap (`api/sse.go`)

A server enters the poll set exactly once — when a browser subscribes to it: a `subscribe kind:"server"` frame on the `/ws/state` **state socket** (`stateSubscribe` → `addClient`) (260716-qf3j-state-socket). `addClient` adds the server to `h.clients` and re-spawns the poll goroutine if it had exited (the `!h.polling` guard). The hub's per-tick work-list is the snapshot of `h.clients` keys. A server leaves the poll set via **two** paths (260603-gs2t-reap-dead-tmux-servers-sse):

1. **Last-client-disconnect** — `removeClient` deletes the server from `h.clients` when its final subscriber drops (an `unsubscribe` frame, or the whole state-socket connection closing → `dropStateConn`); the next tick observing zero clients lets the goroutine stop.
2. **Dead-server reap** — when `FetchSessions` returns an error matching `tmux.IsServerGone`, the server is reaped even while clients are still connected. Killing a tmux server does NOT disconnect the browser's socket, so without this reap the dead socket would linger in `h.clients` and every ~2.5s tick re-shell `tmux -L <name> list-sessions` against the gone socket, logging a `slog.Warn("SSE poll error")` drumbeat forever (the `utils`/runaway-polling incident).

**Reap mechanics**: `sseHub.poll` collects gone servers into a **loop-local `deadServers` slice** during the per-server iteration — the fetch-error branch splits on `tmux.IsServerGone(err)`: a gone error logs at **Info** (`"SSE: tmux server gone, reaping from poll set"`) and appends to `deadServers`; a non-gone error logs `slog.Warn("SSE poll error", ...)` and is **not** reaped. Both `continue`. After the per-server loop completes (before the metrics broadcast), a single `h.mu.Lock()` block emits one **`gone` frame to each dead server's currently-registered clients**, then deletes the server from **every** per-server map so no stale state leaks into a later re-registration: `h.clients`, `h.cache`, `h.previousJSON`, `h.previousRealSessions`, `h.orderBootstrapAttempts`, `h.previousOrderJSON`, plus the loop-local `perServerGen` and `eventDrivenServers` maps. **The gone signal rides the state socket in-band** (260716-qf3j-state-socket): the reap fans a `hubEvent{gone: true, key: server}` (rendered as `{"op":"gone","kind":"server","key":"<server>","reason":"server-exited"}`).

**Concurrency contract** (load-bearing): the work-list is snapshotted under `RLock`, dead servers are collected into the loop-local slice *during* iteration, and all deletes happen *after* iteration under one write lock — never deleting from a map mid-range over its snapshot, never holding the write lock across `FetchSessions`. **Re-registration is free**: when the last client for a server is reaped, the next tick sees zero clients and the goroutine exits; a later `addClient` (e.g. the user restarts the server and navigates back — or a state-socket `subscribe` after the frontend's `onGone` releases and re-desires it) re-adds it and re-spawns the goroutine. The frontend reaction to `gone` is in `ui/routes-and-shell.md` § Server-Gone Reap (in-band `gone` event + connection-loss fallback) and `architecture.md` § State Socket (`onGone` releases the subscription's ref-count).

**Shared dead-server sentinel** (`tmux.IsServerGone`): the dead-server detection used by the reap is the **single** exported sentinel helper `tmux.IsServerGone(err error) bool` in `internal/tmux/tmux.go`, backed by the unexported `serverGoneText` slice (`"no server running"`, `"failed to connect"`, `"No such file or directory"`) via the string-accepting inner `containsServerGoneText`. This is the one definition of the bare dead-server sentinel set, shared across the `tmux` and `tmuxctl` layers (Constitution III): `tmuxctl.matchesServerDeadText` (used by `isServerDeadError`/`resolveBootstrap` — see § Anchor is join-only) delegates to `tmux.IsServerGone(errors.New(s))`. Note the *narrower* "absent-option-OR-dead" checks elsewhere in the `tmux` package (`ListKeys`/`KillServer`, `board.go`'s `isAbsentOption`) intentionally pair these phrasings with `"invalid option"`/`"unknown option"` and are a distinct set — out of scope for `IsServerGone`.

## Server-Scoped User Options

tmux distinguishes window-scoped (`-w`) options, server-scoped (`-s`) options, **pane-scoped (`-p`) options** (260705-dmex-generic-agent-state-tier), and session-scoped user options (the default — `set-option -t <session>`). We use all four: window-scoped for per-window state (`@rk_win_color`, `@rk_win_layout`, the indexed `@rk_win_web_<n>` family (`@rk_win_web_<n>`, `@rk_win_web_<n>_root`, `@rk_win_web_active`), `@rk_win_code_root`, `@rk_win_marker`, `@rk_win_flair`, `@rk_win_note`, `@rk_win_role`), server-scoped for state belonging to the tmux server as a whole (`@rk_srv_session_order`, `@rk_srv_rank`, `@rk_srv_origin`, `@rk_srv_ephemeral`, `@rk_srv_protected`, `@rk_srv_managed`), **pane-scoped for per-pane agent lifecycle and chat-identity state (`@rk_pane_agent_state`, `@rk_pane_chat`)**, and session-scoped for per-session color and row flair (`@rk_ses_color`, `@rk_ses_flair`) and, on `_rk-pin-*` pin-sessions, board membership (`@rk_ses_pin_board`/`@rk_ses_pin_home`/`@rk_ses_pin_order` (260602-qn62)). (Server-tier flair is NOT a tmux user option — it lives in `~/.config/run-kit/config.yaml` `server_flairs:`, applied to the group header and SERVER tile; see architecture.md § `internal/settings` and ui/sidebar.md § Row Flair.)

Every key sits in the `@rk_<scope>_<name>` namespace with its scope carried in the name (scope ∈ `srv` (`-s`) · `ses` · `win` (`-w`) · `pane` (`-p`)); a name is never reused at another scope, so hierarchical format lookup (`pane → window → session → global`) can never leak one scope's value into another (see § Legacy Option Migration for the pre-namespaced names). The rule of record lives in [fab/project/context.md](../../../fab/project/context.md) § Conventions — a new key gets the prefixed form from day one and a row in the registry below.

| Option | Scope | Set via | Read via | Owner | Legacy names |
|--------|-------|---------|----------|-------|--------|
| `@rk_srv_session_order` | server (`-s`) | `tmux.SetSessionOrder(ctx, server, order)` | `tmux.GetSessionOrder(ctx, server)` | sidebar reorder | `@rk_srv_session_order` |
| `@rk_srv_rank` | server (`-s`) | `tmux.SetServerRank(ctx, server, rank)` | `tmux.GetServerRank(ctx, server)` | server-tile display rank (260705-bpnr-server-tiles-drag-reorder) | `@rk_srv_rank` |
| `@rk_srv_origin` | server (`-s`) | the serve process's `tmuxctl.productionDial` stamp via `tmux.SetServerOrigin` (every dial/reconnect, non-fatal; gated on the injected origin being non-empty AND `tmux.ServerAllowed(socket)`) | `tmux.GetServerOrigin` server-side; the `cmd/rk` origin resolver (`rk url` / `rk notify`) reads it at request time via the pane's own `$TMUX` socket | the covering deployment's full origin string (`http://host:port`, exactly what `rk url` prints; const `tmux.OriginOption`). Excluded from snapshot capture/restore — the live daemon re-stamps a restored server on its next dial | `@rk_srv_origin` |
| `@rk_srv_managed` | server (`-s`) | rk's server-birth seams via the unexported `stampManagedOnBirth` (`CreateSession`/`CreateSessionForRestore` pre-probe birth detection, daemon `startSession` belt-and-braces) and the adopt verb (`POST /api/servers/adopt`, `rk mux adopt` — stamp→reload, best-effort unmark on a failed reload); NO write for external servers at detection time, and no user-facing unmark | `tmux.IsManagedServer` (combined: `productionDaemonServer` derivation ∨ option read — the `IsProtectedServer` taxonomy) by the three managed-conf apply paths (sweep/WS-attach/reload-config endpoint), the `GET /api/servers` fan-out (→ the `managed` payload field), and adopt idempotency | provenance mark: unmarked = external, and external servers receive no managed-conf pushes while staying fully first-class (const `tmux.ManagedOption`). Dies with the server | `@rk_srv_managed` |
| `@rk_srv_ephemeral` | server (`-s`) | the server's creator (agents, test runners, tooling — `set-option -s @rk_srv_ephemeral 1`; `-u` promotes back to durable); rk writes it too — `rk mux new --ephemeral` via `tmux.MarkServerEphemeral` ([agent-messaging](/run-kit/agent-messaging.md)), and the e2e creation sites (`scripts/test-e2e.sh`, the Playwright `_tmux.ts` `createSession`) mark their servers at birth | `tmux.IsEphemeralServer` — **dual-read** new-name-first (`@rk_srv_ephemeral`, then `@rk_ephemeral` when the new name is unset — § Legacy Option Migration) via the shared `readServerMarkDual` helper, by the snapshotter at due passes ([layout-snapshots](/run-kit/layout-snapshots.md)), by `rk mux reap --ephemeral` at enumeration time, and by the `GET /api/servers` fan-out per request (→ the `ephemeral` payload field, [architecture](/run-kit/architecture.md)); `rk doctor` enumerates via the exported `tmux.EphemeralServers` wrapper | ephemeral/scratch opt-out mark: no snapshot coverage + into the reap sweep + surfaced to the operator (UI `scratch` chip/grey/sort sink, doctor count) (const `tmux.EphemeralOption`). Dies with the server — `IsTestServerName` is the dead-socket fallback | `@rk_ephemeral` (`tmux.LegacyEphemeralOption`; dual-read) |
| `@rk_srv_protected` | server (`-s`) | any creator via `set-option -s @rk_srv_protected 1` / `-u`; the `POST /api/servers/protect` endpoint via `tmux.MarkServerProtected`/`tmux.UnmarkServerProtected` (rejects `rk-daemon` with 400 — its protection is derived; `UnmarkServerProtected` unsets **both** names so a demote fully disarms the guard through the legacy fallback read) | `tmux.IsProtectedServer` — **dual-read** new-name-first via the shared `readServerMarkDual` helper (§ Legacy Option Migration) — by the reaper's live enumeration (`enumerateMarkedServers`); `tmux.IsGuardedServer` by the kill guard (`POST /api/servers/kill`) and the `GET /api/servers` fan-out (→ the `protected` payload field) | kill-guard mark: 409 without `force` at the kill API, `rk mux kill` refusal without `--force`, unconditional `rk mux reap` skip (const `tmux.ProtectedOption`). Dies with the server | `@rk_protected` (`tmux.LegacyProtectedOption`; dual-read) |
| `@rk_ses_color` | session (`-t <session>:`) | `tmux.SetSessionColor`/`UnsetSessionColor` (via `POST /api/sessions/:session/color`) | `ListSessions` format string field 5 | per-session | `@session_color` |
| `@rk_ses_flair` | session-scoped on regular sessions (set via `set-option -t <session>`) | `tmux.SetSessionFlair` / `tmux.UnsetSessionFlair` (via `POST /api/sessions/{session}/flair`) | `ListSessions` format string field 7 (closed-set parse, unknown → empty) | per-session row flair (`""`/`rain`/`scan`/`nyan`/`naruto`/`onepiece`/`pacman`/`matrix`/`aquarium`/`roadrunner`/`invaders`/`cube`/`warp`/`spidey`/`ironman` — the same universal set as `@rk_win_flair`, decorative; const `tmux.SessionFlairOption`). Deliberately NOT `@rk_win_flair`: a shared name leaks across scopes in format expansion — flairless windows inherit the session value, and `list-sessions` resolves the current window's window option ahead of the session's own (the same hierarchical-lookup hazard `@rk_win_color`/`@rk_ses_color` avoids; 260814-2esh) (260819-lrm8) | `@rk_ses_flair` |
| `@rk_ses_pin_board` | session-scoped on each `_rk-pin-*` (set via `set-option -t <pinSession>`) | `tmux.Pin` / `tmux.Reorder` (re-stamp on wrong-board re-pin) | `tmux.ListBoardEntries(ctx, server)` (per-pin `show-options -v`) | board membership (which board; const `tmux.BoardOption`) | `@rk_ses_pin_board` |
| `@rk_ses_pin_home` | session-scoped on each `_rk-pin-*` | `tmux.Pin` (STAMP-BEFORE-LINK at pin time) | `tmux.Unpin` (last-link recovery target only) | board pin recovery-to-home when the pin link is the window's last link (const `tmux.HomeOption`) | `@rk_ses_pin_home` |
| `@rk_ses_pin_order` | session-scoped on each `_rk-pin-*` | `tmux.Pin` (append key) / `tmux.Reorder` | `tmux.ListBoardEntries` / `tmux.GetBoard` (sort) | board pin fractional order (const `tmux.BoardOrderOption`; via `ComputeOrderKey`) | `@rk_ses_pin_order` |
| `@rk_win_color` | window (`-w`) | `tmux.SetWindowOptions` (via `POST /options`), `tmux.SetWindowColor` | `ListWindows` format string field 8 | per-window | `@color` |
| `@rk_win_layout` | window (`-w`) | `tmux.SetWindowOptions` (via the unified `POST /options` allowlist; validated by `layoutspec.Parse`), the FRONTEND's layout verbs via that same endpoint (every surface toggle / tile verb / palette `View:`/`Tile:` pick POSTs the serialized layout — [lenses-and-layout](/run-kit/ui/lenses-and-layout.md) § Surface Layout), `POST /api/sessions/{session}/windows` (the `rkType:"iframe"` arm sets `single:web` via `CreateWindowWithOptions`), `rk present --window` (new window gets `single:web`), the `rk tab` CLI family — `rk tab new --layout` (the window is born with its layout), `rk tab layout` (set / `--add` / `--rm` / `--promote` / `--cycle` through the shared `internal/layoutspec` verbs), `rk tab web add --show` and `rk present` (ensure `web` in the layout — a full 3-tile layout yields its last slot), any external/agent `set-option -w` (the expansion mechanism — every mounted viewer repaints on the option tick) | `ListWindows` format string field 9 (→ `WindowInfo.Layout`), `layoutWindowFormat` (snapshot capture), `ReadWebTabFamily` (const `tmux.LayoutOption`) | per-window surface layout — `"<shape>:<surface>[,<surface>…]"` (`single`/`split-h`/`split-v`/`row`/`col`/`main-left`/`main-right`/`main-top`; surfaces `tty`/`web`/`chat`/`code`); unset renders `single:tty` | `@rk_win_lens` (migration row: `iframe` → `single:web`, value-mapped) |
| `@rk_win_web_<n>` (1..8) | window (`-w`) | `tmux.WebAdd`/`WebRemove`/`WebMove`/`WebSelect` (`internal/tmux/webtabs.go`, one chained `SetWindowOptions` per verb), `POST /options` (slot replace / `len+1` append; `null` routes through `WebRemove`) — including the frontend web tile's ADDRESS BAR, which writes the active slot (`n = webActive`, slot 1 while the pointer is unset) — the four `POST /api/windows/{id}/web*` verb routes including `POST …/web/{n}/move` — driven by draft materialization, the strip's select/close/drag/⌥⇧←/→ surfaces, and the `Web: … tab` palette entries — `rk tab web add|rm|mv|select` and `rk present` (the shell twins of the Web* ops), snapshot restore | `ListWindows` format string fields 10–17 — slots spelled out (one fixed format string cannot enumerate a family; const bound `tmux.MaxWebTabs = 8`), walked to the first empty slot (dense) → `WindowInfo.WebTabs`; `ReadWebTabFamily` reads slots AND roots in one `display-message` call; snapshot capture enumerates slots + roots | the window's web-tab family — per-slot URL (`/proxy/…`, absolute http(s), or for file/dir targets `/present/{server}/{12-hex-roothash}/{name}?v={epoch}` with an empty name for a directory; the one-release legacy `/present/{windowId}/{n}/{path}?server=` form remains readable and is rewritten on re-present); one web tile renders the whole family (one chrome + one URL-keyed iframe per declared tab, 260828-fykg, 9kip, s36e, 260901-ei4t) | — |
| `@rk_win_web_<n>_root` (1..8) | window (`-w`) | `tmux.WebAdd` (file/dir targets; cleared by a port/URL target replacing the slot), `tmux.WebRemove`/`WebMove` (shift or permute each root with its URL pair), `rk tab web add|rm|mv`, `rk present`, snapshot restore | `ReadWebTabFamily` (parallel to URL slots), `layoutWindowFormat` capture, and `ListDeclaredWebRoots` at content-keyed `/present` request time (one server-wide `list-windows -a -F` over all eight root slots); the legacy slot arm reads the addressed option directly and slot 1 dual-reads `@rk_win_present_root` | per-slot absolute serve root; content-keyed resolution succeeds while at least one live window on the server declares the matching root | `@rk_win_present_root` (slot 1 only — migration row) |
| `@rk_win_web_active` | window (`-w`) | `tmux.WebSelect` / `POST /options` (integer `1..len`; `null` only when no tabs) — including strip click/roving Enter and `Web: Next/Previous tab` through `POST …/web/{n}/select` — plus `tmux.WebMove` through `POST …/web/{n}/move`, strip drag/⌥⇧←/→, palette `Web: Move tab left/right`, and `rk tab web mv`; `rk tab web select` / `rk tab web add --show` / `rk present` are the CLI select paths. `WebAdd` sets `1` only when the family is empty ("add" is not "show" — draft materialization follows add with a select POST); `WebRemove` and `WebMove` repoint so the selected tab identity survives renumbering | `ListWindows` format string field 18 → `WindowInfo.WebActive` (clamped: non-numeric/out-of-range → 1 when tabs exist, 0 when none — degrade, never error; const `tmux.WebActiveOption`) | 1-based index of the tab the web surface shows; unset/0 = no tabs | — |
| `@rk_win_code_root` | window (`-w`) | `tmux.SetWindowOptions` (via the unified `POST /options` allowlist; `validate.ValidatePath` + tilde-expand, stored absolute) — the frontend writes through that endpoint: a SEED on the first code-tile render of a window with an empty root (from the derived `gitRoot`, one POST, in-flight-guarded) and a FOLLOW when code-server's own File > Open Folder navigation reports a folder ([lenses-and-layout](/run-kit/ui/lenses-and-layout.md) § The code root) — `rk tab code set` (absolute, must exist) — snapshot restore | `ListWindows` format string field 19 → `WindowInfo.CodeRoot` (const `tmux.CodeRootOption`), `tmux.GetWindowOption` by `rk code exec --tab` at host resolution (the option is the tab's folder source — see [code-bridge](/run-kit/code-bridge.md)) | absolute folder the code surface opens (shared per window; the frontend's availability/read path is `codeRootFor` = the option else the live `gitRoot` derivation) | — |
| `@rk_win_url` *(retired)* | window (`-w`) | **no rk writer**; still accepted on `POST /options` for one release, translated onto the family (`translateLegacyOptionKeys`) | `ListWindows` format string field 24 — **dual-read** as a `web_1` fallback when slot 1 is empty (never swept — external live writers still stamp it mid-session and the once-per-server sweep cannot see those writes; `ReadWebTabFamily` dual-reads it the same way, and `WebRemove` on the last tab clears it) | retired single-URL web option | `@rk_url` (migration row targets this retired name as its terminal convergence) |
| `@rk_win_present_root` *(retired)* | window (`-w`) | — | `/present/{windowId}/*` (slot 1 only) — **dual-read** as the serve root when `@rk_win_web_1_root` is empty (const `tmux.LegacyWinPresentRootOption`; external writers stamp it live, where the once-per-server sweep cannot see it) | retired serve-root name; migrates to `@rk_win_web_1_root` in one sweep | `@rk_present_root` (migration chain) |
| `@rk_win_lens` *(retired)* | window (`-w`) | **no rk writer**; still accepted on `POST /options` for one release (`"iframe"` → `@rk_win_layout=single:web` only when `_layout` unset; anything else no-op) | `ListWindows` format string field 25 — **dual-read**: with `@rk_win_layout` empty, a live-stamped `iframe` reads as the `single:web` layout; any other value is ignored | retired lens hint; migrates to `@rk_win_layout` in one sweep (value-mapped) | `@rk_type` (migration chain) |
| `@rk_win_marker` | window (`-w`) | `tmux.SetWindowOptions` (via the unified `POST /options` allowlist; empty string unsets) | `ListWindows` format string field 20 (`NormalizeMarker` maps stored flat tokens forward, current closed-set validation drops unknowns; const `tmux.MarkerOption`); snapshot capture and restore apply the same normalization | per-window display marker — one of the twelve `<mode>[:<stage>]` tokens (mode ∈ `manual`/`auto`/`blocked`, stage ∈ `1`/`2`/`3`, bare mode = stage 1); `""` unsets. All states are static user label conventions | `@rk_win_marker` |
| `@rk_win_role` | window (`-w`) | `tmux.SetWindowOptions` (via the unified `POST /options` allowlist), `rk role` | `ListWindows` format string field 21 (const `tmux.RoleOption`) | per-window orchestration role (`""`/`operator` — the server-scoped radio; § Operator Session) | `@rk_win_role` |
| `@rk_win_flair` | window (`-w`) | `tmux.SetWindowOptions` (via the unified `POST /options` allowlist; empty string unsets) | `ListWindows` format string field 22 (closed-set parse, unknown → empty; const `tmux.FlairOption`) | per-window row flair (`""`/`rain`/`scan`/`nyan`/`naruto`/`onepiece`/`pacman`/`matrix`/`aquarium`/`roadrunner`/`invaders`/`cube`/`warp`/`spidey`/`ironman` — one universal set of fourteen named states, decorative (260814-2esh) (260819-lrm8) (260819-9hh6)) | `@rk_win_flair` |
| `@rk_win_note` | window (`-w`) | `tmux.SetWindowOptions` (via the unified `POST /options` allowlist — clients send bare text, the handler stamps the `<unix-epoch>:` prefix server-side; empty string unsets), or agents/operator via plain `set-option -wt "$TMUX_PANE"` stamping their own epoch (the `@rk_pane_agent_state` no-server-dependency precedent) | `ListWindows` format string field 23 — a strict single field (write-side strips control chars), **dual-read** with the legacy free-text note LAST (field 26+, tail rejoined past the retired-`@rk_win_url`/`@rk_win_lens` fallback fields 24–25), new wins (const `tmux.NoteOption`) | per-window one-line status note (`<unix-epoch>:<text>`) — user/agent-authored annotation (the marker/flair user-preference class, not derived state) | `@rk_win_note` (dual-read) |
| `@rk_pane_agent_state` | **pane (`-p`)** | agent-harness hooks installed by `rk agent setup` — `rk agent hook` **dual-writes both names** at hook-fire time in one `;`-chained tmux exec (`set-option -pt <pane> <new> <v> ; set-option -pt <pane> <old> <v>`), NO rk/server dependency (260705-dmex-generic-agent-state-tier); gen-1 inline one-liners write the legacy name only | `paneFormat` field 9 — **dual-read** with the legacy field 6, new wins (`agentStateRaw` = field 9 if non-empty else field 6) → `parsePanes` (→ `PaneInfo.AgentState`/`AgentStateEpoch`); the single-pane read `PaneFactsCtx` likewise requests both names and prefers the new (§ Legacy Option Migration) | generic agent-lifecycle state (`active|waiting|idle:epoch`; const `tmux.AgentStateOption`). See [agent-state](agent-state.md) | `@rk_agent_state` (`tmux.LegacyAgentStateOption`; dual-read + dual-write, migration `CopyOnly`) |
| `@rk_pane_chat` | **pane (`-p`)** | agent-harness hooks installed by `rk agent setup` — the same `rk agent hook` **dual-write** as `@rk_pane_agent_state` (both names, one chained exec, the same no-server-dependency contract) | `paneFormat` field 10 — **dual-read** with the legacy field 7, new wins (`chatRaw` = field 10 if non-empty else field 7) → `parsePanes` (→ `PaneInfo.ChatProvider`/`ChatSessionRef`) | chat-session identity (`<provider>:<ref>`; const `tmux.ChatOption`). See [agent-state](agent-state.md) | `@rk_chat` (`tmux.LegacyChatOption`; dual-read + dual-write, migration `CopyOnly`) |

`@rk_win_note` (const `tmux.NoteOption`) carries a **free-text one-line status note** — the "why" behind a window's derived state ("blocked on flaky e2e"), where the marker/color axes say what the user thinks and the status dot says what derivation sees. The value schema is `<unix-epoch>:<text>` — the epoch prefix (the `@rk_pane_agent_state` staleness precedent) lets the UI age notes honestly instead of letting them lie; colons inside the text are safe (the split cuts at the first colon). In the tab-delimited `list-windows` format the note rides **two** fields (§ Deprecation Ledger): the new note is a **strict single field** (field 23 — write-side validation strips control characters, so one field is safe) and the legacy free-text note stays **last** (field 26+, tail rejoined via `strings.Join(parts[25:], "\t")`; fields 24–25 between them are the retired `@rk_win_url` / `@rk_win_lens` dual-read fallbacks) so tabs inside a legacy note cannot truncate sibling fields; `parseWindows` prefers the new field and falls back to the rejoined legacy tail (§ Legacy Option Migration). The read is **tolerant, never dropping**: a missing colon or a non-numeric/negative epoch prefix degrades to the whole value as text with epoch 0 (rendered without an age), parsed by `parseNoteValue` into `WindowInfo.Note` / `WindowInfo.NoteEpoch` (JSON `note,omitempty` / `noteEpoch,omitempty`); empty/absent option = no note (degrade-to-absent everywhere). Unlike marker/role/flair there is NO closed-set validation on read. Writers: the unified `/options` endpoint (the handler stamps the epoch — the server owns the clock, and its hub wake repaints immediately), agents and the operator's `annotate-tab` template via raw `set-option` with `$(date +%s)` (which ride the ~12s safety poll — user-option mutations emit no control-mode event), and layout-snapshot restore. The note surfaces on the window flyout card only ([status-signals](/run-kit/ui/status-signals.md) § Row-hover register flyout card) and rides layout snapshots verbatim, epoch included ([layout-snapshots](/run-kit/layout-snapshots.md)).

The window's web content is an **indexed, dense family** of window-level options — `@rk_win_web_1` … `@rk_win_web_8` (`tmux.MaxWebTabs` bounds the family; fixed tmux format strings spell out slots 1..8), each with an optional parallel root `@rk_win_web_<n>_root`, plus the 1-based `@rk_win_web_active` pointer. `tmux.ShowWindowOptions` reads every `@rk_win_*` option for `rk tab show`, while `ReadWebTabFamily` reads slots, roots, and layout in one `display-message` call. `tmux.WebAdd`/`WebRemove`/`WebMove`/`WebSelect` keep URL and root slots contiguous — remove shifts URL+root pairs; move removes slot `n`, inserts the pair at `to`, shifts intervening pairs, and repoints `_active` so the selected tab identity survives its index change (`n == to` writes nothing) — an emptied family unsets `_active`, direct `/options` writes replace an existing slot or append at `len+1`, and reads truncate at the first gap. `@rk_win_url` is a retired live-writer fallback for slot 1 in both hot read paths; removing the last tab clears it.

File and directory targets store `/present/{server}/{roothash}/{name}?v={epoch}` in `@rk_win_web_<n>`, where `roothash` is the first 12 lowercase hex characters of the sha256 of the absolute `@rk_win_web_<n>_root` and `name` is empty for a directory. Window id and slot are absent from this value, so dense-family shifts move the URL and root losslessly. The route accepts an 8–64-character hash prefix and resolves it against the roots declared by all live windows on that server. `ListDeclaredWebRoots` performs one `list-windows -a -F` over the eight root options and deduplicates them; a root remains resolvable while at least one live window declares it.

`WebAdd` treats same-form `/present/` identities as the form-specific tuple with `?v=` excluded: content-keyed identities contain server, root hash, remaining query, and path tail; legacy identities contain window id, remaining query, and path tail. A cross-form pair compares the path tail, with `WebAdd`'s existing stored-root equality as the decisive tie-breaker. Same-form hits refresh the stored `?v=` through `present.BumpVersion`; a legacy-to-content-keyed hit writes the incoming content-keyed value into the existing slot. The one-release legacy `/present/{windowId}/{n}/{path}?server=` arm continues to read the addressed root directly, including the slot-1 `@rk_win_present_root` fallback.

Attaching a tab leaves the window arrangement under `@rk_win_layout`; a web-first window carries `single:web`, while a tty-led layout remains tty-led. `POST /api/windows/{windowId}/options` enforces the web URL scheme allowlist: `/proxy/` and `/present/` root-relative paths or absolute `http:`/`https:` URLs with a host; other schemes and bare hosts return 400 before tmux. The frontend mirrors this for inline feedback. One web tile renders the family as one chrome with one URL-keyed iframe per declared tab ([lenses-and-layout](/run-kit/ui/lenses-and-layout.md) § Iframe Window → Web tab strip). Its `+`, an empty-space double-click, and palette `Web: New tab` open viewer-local drafts; a draft's Enter adds through `POST …/web` and selects the returned slot. Click/Enter and `Web: Next/Previous tab` select through `POST …/web/{n}/select`; `×`, middle-click and `Web: Close tab` remove through `POST …/web/{n}/remove`; drag, component-local ⌥⇧←/→, and `Web: Move tab left/right` reorder through `POST …/web/{n}/move`; the address bar writes the ACTIVE declared slot through `POST /options`. `rk tab web mv` is the daemon-independent CLI twin (source forms bare `n`, `web/n`, `@N/web/n`; destination a bare 1-based index; stdout prints the resulting address). The frontend remove and move transforms are display-only mirrors of server-authoritative `WebRemove`/`WebMove`; move POSTs serialize, and a rejected move cancels queued dependent moves whose indices came from the rejected optimistic family. (260813-becu-rk-present-attach-verb, 260819-v6y4-web-tile-browser-chrome, 9kip, 260901-ei4t-present-url-server-root-hash, s36e)

`@rk_srv_session_order` stores a JSON-encoded array of session names defining the user-preferred sidebar render order. Because the value is server-scoped, it is shared by every client connected to the same tmux server — laptop and phone hitting the same `tmux -L runkit` see the same order. Lifetime matches the tmux server (lost on server kill, NOT on rk-go restart per Constitution VI). Both wrapper functions wrap their context with `context.WithTimeout(ctx, TmuxTimeout)` (10s) and route through `tmuxExecRawServer` (which captures stderr in error messages so callers can pattern-match "invalid option" / "no server running" to distinguish operational empty-state from real failures).

The HTTP endpoints `GET /api/sessions/order` and `POST /api/sessions/order` (POST per §IX; see `architecture.md` § Endpoints) layer over these wrappers. The mutating POST triggers a synchronous SSE broadcast (`event: session-order`) so all connected clients on that server reorder live; the SSE hub also bootstraps the cache once per server on first poll so the order survives an rk-go restart that left tmux running.

`@rk_srv_rank` (const `tmux.ServerRankOption`) (260705-bpnr-server-tiles-drag-reorder) stores this tmux server's user-defined **display rank** — a single ascending integer — among the other tmux servers. It follows the SAME server-scoped mechanism as `@rk_srv_session_order` (`set-option -s` write / `show-option -sv` read via `tmuxExecRawServer` under `context.WithTimeout(ctx, TmuxTimeout)` (10s)) and the SAME error taxonomy: `GetServerRank(ctx, server)` returns `(nil, nil)` for the unset / dead-socket cases (stderr `invalid option`/`unknown option`, OR `tmux.IsServerGone` — the shared dead-server sentinel, covering `no server running`/`failed to connect`/`No such file or directory`), so a fresh or unranked server is a normal state, never an error; only a genuine subprocess failure OR a malformed (non-integer) stored value propagates as a wrapped `fmt.Errorf`. `SetServerRank(ctx, server, rank)` writes `strconv.Itoa(rank)`. **Rank data rides each server**, so a killed server takes only its own rank — there is no cross-server merge rule and no single "canonical" server holding the whole list (contrast `@rk_srv_session_order`, which is one JSON array on the server it orders). The order is *written* by `POST /api/servers/order` (rank `i` → the i-th listed server, best-effort) and *read back* onto each `serverInfo.rank` in the `GET /api/servers` fan-out; display ordering is composed frontend-side (see `architecture.md` § `/api/servers` rank + order endpoint and `ui/sidebar.md` § Server-tile drag reorder).

`@rk_srv_origin` (const `tmux.OriginOption`) stores the **full origin string of the run-kit deployment covering this tmux server** (e.g. `http://127.0.0.1:3001` — exactly what `rk url` prints). It answers the per-server question "which deployment covers this socket" — inherently per-tmux-server (prod on :3000 and an e2e deployment on its derived worktree port cover *different* sockets on the same box), and only the covering daemon can answer it authoritatively. The **writer** is the serve process: `cmd/rk/serve.go` injects its config-derived origin (`http://{cfg.Host}:{cfg.Port}`) into tmuxctl via `tmuxctl.SetStampOrigin` before `supervisor.Start`, and `productionDial` stamps it via `tmux.SetServerOrigin` on **every dial and every reconnect** (immediately beside the `SetExitEmptyOff` backstop, same non-fatal contract — a failed stamp logs at Debug and the dial proceeds). The stamp is skipped when the injected origin is empty (non-serve tmuxctl consumers) or when `tmux.ServerAllowed(socket)` is false — an allowlisted deployment (an e2e backend dials host sockets) must never overwrite a host server's origin. Coverage and healing fall out of the dial seams: fsnotify socket-create → dial stamps servers at birth, the synchronous `Start` enumeration stamps every pre-existing server at daemon startup (healing a port change across restarts — a daemon restarted on a new port overwrites the old value on its first pass), and reconnects re-stamp for free. The **reader** is the pane-side CLI: `rk url` and `rk notify` resolve their origin through the shared `cmd/rk` resolver with precedence explicit `RK_HOST`/`RK_PORT` env → validated `@rk_srv_origin` read from the pane's OWN server via the `$TMUX` socket path → the `127.0.0.1:3000` default (see `architecture.md` § CLI Subcommands). The value is validated before use (parseable `http`/`https` URL, non-empty host) — it is pane-writable same-user state used to build request/print targets. `GetServerOrigin` mirrors `GetServerRank`'s taxonomy: unset option and dead/absent socket return `("", nil)`, other failures propagate wrapped; the stored value is returned verbatim (validation is the consumer's job). The `srv` scope in the name distinguishes it from `@rk_win_url` — the present/iframe URL at window scope — so one name never rides two scopes in format expansion (the `@rk_win_flair`/`@rk_ses_flair` split precedent). Layout snapshots (`internal/snapshot`) deliberately do NOT capture/restore it: a restored server is re-stamped by the live daemon's dial within one pass, so persisting the value would only round-trip staleness.

`@rk_srv_ephemeral` (const `tmux.EphemeralOption`) marks a whole server **ephemeral** — scratch, creator-intent. Any creator may set it directly (`set-option -s @rk_srv_ephemeral 1`); rk's own write paths are `rk mux new --ephemeral` (via `tmux.MarkServerEphemeral`, mirroring `SetServerOrigin`, with kill-on-mark-failure — [agent-messaging](/run-kit/agent-messaging.md)) and the in-repo e2e creation sites (§ Unified Test-Socket Naming below). Presence with any non-empty trimmed value (`1` is the documented convention) opts the server out of layout-snapshot coverage ([layout-snapshots](/run-kit/layout-snapshots.md) § Snapshotter cadence) and into the `rk mux reap --ephemeral` sweep (§ `rk mux reap` below); unsetting it (`set-option -s -u @rk_srv_ephemeral`) promotes the server back to durable, snapshot coverage resuming on the next due pass with no other action. "This server is scratch" is genuinely underivable from tmux/filesystem/git, which is why it rides the user-option channel like the rest of the family (Constitution X's logic). The reader `IsEphemeralServer(ctx, server)` **dual-reads new-name-first** through the shared `readServerMarkDual` helper: the new name via `show-option -sv` (`tmuxExecRawServer` under `context.WithTimeout(ctx, TmuxTimeout)`), falling back to `@rk_ephemeral` (`tmux.LegacyEphemeralOption`) the same way only when the new name is unset — either name's non-empty trimmed value reads true. Otherwise it mirrors `GetServerOrigin`'s taxonomy exactly: an unset option on both names (`invalid option`/`unknown option` stderr) or a dead/absent socket (`IsServerGone`) reads as `(false, nil)` — liveness is the caller's concern — and other subprocess failures propagate wrapped. The option dies with the tmux server, so no consumer can read it post-mortem: `IsTestServerName` remains the dead-socket fallback (`IsTestServerName(name) ⇒ treated as ephemeral`), and the option extends that semantic to arbitrarily-named live servers. Beyond snapshot/reap, the mark feeds the operator-visibility surfaces: the `GET /api/servers` handler reads it per live server per request inside the existing fan-out (→ the `ephemeral` field; a read failure logs at warn and yields `ephemeral: false`, never a 5xx), the frontend sinks and badges marked servers from that field (ui/sidebar.md § Rank-aware comparator composition, § Infra-server de-emphasis), and `rk doctor` reports the live marked count via the exported `tmux.EphemeralServers(ctx)` wrapper over `enumerateMarkedServers` (same live-only, hard-skip semantics as the reap sweep — § `rk mux reap` below).

`@rk_srv_protected` (const `tmux.ProtectedOption`) marks a whole server **protected** — a kill-guard opt-in, the exact mirror of `@rk_srv_ephemeral` with inverted intent. Presence with any non-empty trimmed value (`1` is the documented convention) arms the guard; unsetting (`set-option -s -u @rk_srv_protected`) demotes — there is no tombstone value. The reader `IsProtectedServer(ctx, server)` **dual-reads new-name-first** through the same `readServerMarkDual` helper (`@rk_srv_protected`, then `@rk_protected` when the new name is unset), sharing `IsEphemeralServer`'s taxonomy exactly (`show-option -sv` via `tmuxExecRawServer` under `context.WithTimeout(ctx, TmuxTimeout)`): an unset option on both names (`invalid option`/`unknown option` stderr) or a dead/absent socket (`IsServerGone`) reads `(false, nil)` — liveness is the caller's concern — and other subprocess failures propagate wrapped. The writers `MarkServerProtected` (`set-option -s @rk_srv_protected 1`) / `UnmarkServerProtected` require a live server; `UnmarkServerProtected` unsets **both** names (`@rk_srv_protected` and `@rk_protected`) so a demote fully disarms the guard through the legacy fallback read. The kill-guard predicate is the combined `IsGuardedServer(ctx, name)`: true when `name == productionDaemonServer` (`"rk-daemon"` — the same string as `daemon.ServerSocket`, kept in `internal/tmux` to avoid an import cycle) OR `IsProtectedServer` reads true. The daemon check short-circuits BEFORE any tmux read, so the derived answer needs no live server and never errors; `rk-daemon` is protected by derivation, never by option — the derived state is not togglable, and the `POST /api/servers/protect` endpoint (see [architecture](/run-kit/architecture.md)) rejects `rk-daemon` with 400. Precedence: **protected beats ephemeral** when one server carries both marks. What the mark arms: `POST /api/servers/kill` refuses without `force` (409), `rk mux kill` refuses panes on the protected server without `--force`, and `rk mux reap` skips it unconditionally (§ `rk mux reap` below). The `GET /api/servers` fan-out reads `IsGuardedServer` per live server per request → the `protected` payload field (a read failure logs at warn and yields `protected: false`, never a 5xx — the same contract as `ephemeral`). (260824-xaw2-protected-server-class)

`@rk_srv_managed` (const `tmux.ManagedOption`) marks a whole server **rk-managed** — a provenance mark answering "did rk's managed tmux.conf apply at this server's birth". An **external** server is exactly an unmarked one (a server the user started outside run-kit — the `default` socket, a hand-started `-L` server); external stays fully first-class — attachable, editable, killable, colorable, renamable (guest mode) — and only **conf-pushing stops**: every managed-conf apply path ([configuration](/run-kit/configuration.md) § Managed tmux.conf — the daemon-start `RefreshSweep`, the WS-attach best-effort reload, and `POST /api/tmux/reload-config`) gates on the mark and skips external servers. Presence with any non-empty trimmed value (`"1"` is the documented convention) marks; unsetting (`set-option -s -u @rk_srv_managed`) is reserved for the adopt-failure rollback — there is deliberately no user-facing release verb. The writers `MarkServerManaged` (`set-option -s @rk_srv_managed 1`) / `UnmarkServerManaged` (`set-option -s -u @rk_srv_managed`) require a live server. The reader is the single exported combined predicate `IsManagedServer(ctx, name)`: true when `name == productionDaemonServer` (rk-daemon is managed **by derivation** — the `IsGuardedServer` pattern; the daemon-name check short-circuits BEFORE any tmux read so a daemon restart against a daemon server born before the stamp existed never strands rk's own server as external — Constitution VI) OR the option reads present with a non-empty trimmed value (the `IsProtectedServer` taxonomy verbatim: unset option or dead/absent socket reads `(false, nil)`, other subprocess failures propagate wrapped). There is no exported raw option reader — every consumer (the three conf gates, the `GET /api/servers` fan-out, adopt idempotency) wants the combined answer. **Birth stamping**: every rk path that births (starts) a tmux server stamps the mark on the newborn server — `CreateSession` and `CreateSessionForRestore` detect "this call births the server" via a `probeServerAlive` check immediately before the `new-session` exec and stamp on dead→create-success through the shared unexported `stampManagedOnBirth` helper (transitively covering `rk mux new`, the session/server create APIs, and riff — `internal/riff` holds no `new-session`), and daemon `startSession` stamps `rk-daemon` after birth as belt-and-braces beside the derivation. A stamp failure logs at warn and never fails the create — the server degrades to external and adopt is the recovery (a stale-conf window is recoverable, a killed user server is not). A session create on an already-live server writes no stamp (the pre-probe reads live). The stamp is truthful by construction: it records exactly when rk's `-f <managed conf>` actually applied. **Adopt** converts an external server to managed — `POST /api/servers/adopt` (§ API Server Parameter) and `rk mux adopt` ([agent-messaging](/run-kit/agent-messaging.md)) both stamp first, then source the managed conf via `ReloadConfig`, best-effort unmarking when the reload fails, so a stamped server whose conf never applied is never left behind. The `GET /api/servers` fan-out reads `IsManagedServer` per live server per request → the `managed` payload field (the same warn-and-false failure contract as `ephemeral`/`protected`); the frontend renders the external class only on `managed === false` — an old backend omitting the field renders no external treatment ([status-signals](/run-kit/ui/status-signals.md) § Server-Class Glyph Axis). (260826-lv87-external-server-provenance-adopt)

### Legacy Option Migration

The naming rule's carrier for pre-namespaced names is the table-driven sweep in `internal/tmux/legacy_options.go`. The table `legacyOptions []legacyOption{Old, New, Scope, CopyOnly, Transform}` holds one row per retired name — 24 rows today: the two color keys (`{"@color", ColorOption, scopeWindow}` and `{"@session_color", SessionColorOption, scopeSession}`), the 16 scope-prefix rows (15 copy-then-unset renames — the seven window keys `@rk_type`/`@rk_url`/`@rk_present_root`/`@rk_marker`/`@rk_flair`/`@rk_note`/`@rk_role` at `scopeWindow`, the session keys `@rk_session_flair` and the three `_rk-pin-*` keys at `scopeSession`, the four server keys `@rk_session_order`/`@rk_server_rank`/`@rk_origin`/`@rk_managed` at `scopeServer` — and `@rk_ctl_keepalive` as an unset-only row, `New == ""`), the two retired-web-name window rows, and the four externally-written-key rows — the server keys `@rk_ephemeral → @rk_srv_ephemeral` and `@rk_protected → @rk_srv_protected` at `scopeServer` (plain copy-then-unset rows), plus the pane keys `@rk_agent_state → @rk_pane_agent_state` and `@rk_chat → @rk_pane_chat` at `scopePane` as **`CopyOnly` rows**; a rename or retirement appends a row (`New == ""` is an unset-only row). The retired-web-name rows ride the optional per-row **`Transform func(string) (string, bool)`** hook, which maps the raw Old value onto the value copied to New (`ok=false` means no representation in the new scheme — no copy, but Old is still unset): `@rk_win_present_root → @rk_win_web_1_root` (plain copy) and `@rk_win_lens → @rk_win_layout` with `Transform: legacyLensToLayout` (`"iframe" → "single:web"`, any other value → no copy), copy only when New is unset. **`@rk_win_url` has NO sweep row** — external live writers can stamp it mid-session after the sweep, so it is dual-READ (see § Server-Scoped User Options registry row) and never unset; the `@rk_type → @rk_win_lens` / `@rk_url → @rk_win_url` / `@rk_present_root → @rk_win_present_root` scope-prefix rows stay ordered BEFORE the `@rk_win_*`-web rows so a doubly-legacy window converges in one sweep (`@rk_url → @rk_win_url` is the terminal convergence for the URL chain). The retired names live in **exported `Legacy*Option` constants** (`LegacyEphemeralOption`/`LegacyProtectedOption`/`LegacyAgentStateOption`/`LegacyChatOption` in `internal/tmux/tmux.go`) shared by the table, the dual-read format fields, the hook dual-write, and `rk agent setup`'s gen-1 recogniser, plus the unexported `legacyTypeOption`/`legacyURLOption`/`legacyNoteOption` and the unexported retired-window names `legacyWinLensOption`/`legacyWinURLOption`/`legacyWinPresentRootOption` shared by the table, the dual-read fields, and the `/options` compat translator.

**Dual-read for the externally-written keys.** The sweep converges existing carriers once per server per daemon lifetime, but keys written by anything outside the `rk` binary can be written under their legacy names again *after* the sweep by stale writers. Readers therefore accept both names — new wins — for exactly these keys: the window key `@rk_note` (agents/operator stamp it via `set-option -wt "$TMUX_PANE"`) rides the same `list-windows` / layout-capture format line with the parser preferring the new value; the retired `@rk_win_url` dual-reads as a `web_1` fallback in both `parseWindows` and `ReadWebTabFamily` (a compat for external live writers' mid-session writes — not a sweep row, since the sweep is once-per-server and mid-session writes would go unread); the server marks `@rk_ephemeral`/`@rk_protected` dual-read in the `IsEphemeralServer`/`IsProtectedServer` predicates via the shared `readServerMarkDual` helper (new first via `show-option -sv`, legacy on unset); and the pane keys `@rk_agent_state`/`@rk_chat` dual-read in `paneFormat` (the new names ride fields 9–10, appended after `#{alternate_on}`, the legacy names stay fields 6–7; `parsePanes` resolves field 9 if non-empty else 6, and field 10 if non-empty else 7 — lines with fewer than 11 fields are skipped) and in `PaneFactsCtx` (requests both names as fields 3–4, `parsePaneFacts` prefers the new). Writers: `MarkServerEphemeral`/`MarkServerProtected` write the new names only (`UnmarkServerProtected` unsets **both** names so a demote fully disarms the guard through the fallback read), and `rk agent hook` **dual-writes both pane names** in one `;`-chained tmux exec (`set-option -pt <pane> <new> <v> ; set-option -pt <pane> <old> <v>` — discrete argv elements, no shell; fab-kit reads only the legacy pane name until its new-then-old read ships). Every other renamed key is a hard rename with no fallback. The whole dual-read surface is transition-only — see § Deprecation Ledger for what the follow-up release deletes.

**Algorithm (per row; `MigrateLegacyOptions`).** (a) **Right-scope move** — for every carrier at `row.Scope` (server / sessions / windows / panes enumerated from the server), a `show-options -<flag> -t <target>` probe (without `-A`, so it reports only what the carrier's own table holds — a `-A`/format read cannot distinguish held from inherited) copies `Old` to `New` when `New` is unset there, then unsets `Old` — **unless `row.CopyOnly`**, in which case the copy still runs but the trailing `unsetOptionAt(Old)` is skipped entirely (a carrier already holding both names issues nothing): dual state (Old + New held) is the sanctioned deprecation-window state for the two pane rows. (b) **Wrong-scope purge** — every other scope, **including the global table (`show-options -g`, the row count's biggest leak vector)**, unsets `Old` where held regardless of `CopyOnly` (an `@rk_agent_state` found at window/session/server scope can only be a stray); a value is never copied forward from a wrong scope (a session-level `@color` was never a legitimate window color). Session targets compose through `ExactSessionTarget` (`=name:`; a bare `-t <session>` is a window target). Each set/unset logs `slog.Info` with `server`/`option`/`scope`/`target`; a per-carrier failure logs `slog.Warn` and continues; the first error is returned (daemon-path callers ignore it after logging). Every tmux call rides `tmuxExecServer`/`tmuxExecRawServer` under a per-call `TmuxTimeout` (a server sweep is O(carriers); several seams pass `context.Background()`, so the budget is per call, never absent). Idempotent — a second run issues zero `set-option` calls.

**Once-guard.** `MigrateLegacyOptionsOnce(ctx, server) (changed, err)` runs the sweep at most once per server per daemon process, marking on **attempt** (`MarkLegacyMigrationAttempt`, an in-memory `sync.Map`; a failing server is not re-swept on every attach — `rk mux adopt` and a daemon restart are the explicit retries). `MigrateLegacyOptionsReport` is the non-marking variant for calls that took the guard synchronously themselves (the async hook seams), and `ResetLegacyMigrationForTest` clears it.

**Hook seams (all behind the `@rk_srv_managed` gate — managed-only; an api seam var per seam on the `sweepReloadConfig`/`attachReloadConfig` test-substitution pattern).** The sweep rides wherever rk already reloads its managed conf onto a server: `tmux.RefreshSweep` at daemon start (`sweepMigrateLegacy`), the WS pre-attach reload (`attachMigrateLegacy`), the adopt handler (`adoptMigrateLegacy`), the reload-config endpoint (`reloadMigrateLegacy`), and `rk mux adopt` (`muxAdoptMigrateLegacyFn` — unconditional `MigrateLegacyOptionsReport` and prints `migrated legacy options on <name>` when changed; the explicit retry). The async seams (pre-attach, reload-config) run off the request path on their own goroutine — the sweep is O(carriers) round-trips and must not hold the route/attach budget past its budget — taking the once-guard synchronously (`terminals_ws.go`, `tmux_config.go`) and calling `MigrateLegacyOptionsReport` so the guard is never double-marked. An API sweep that changed something wakes the SSE hub (`s.initSSEHub(); s.sseHub.wake(server)`) so the browser repaints in one poll pass rather than waiting the 12s safety tick ([daemon-lifecycle](/run-kit/daemon-lifecycle.md) § Per-server wake seam); `RefreshSweep` runs at daemon start with no clients and does not wake.

**Diagnostic sibling.** `CountLegacyOptions(ctx, server)` shares the table and the scope walk with the migrator (no second enumeration implementation) and backs the `rk doctor` `legacy tmux options` row — always OK-shaped (informational, never flips the verdict; the attach/adopt-time sweep heals, doctor only reports), live servers only (probed via `ListServers` — a tmux command on a dead socket resurrects a server). A `CopyOnly` row's `Old` held at its **right scope** is NOT counted (the dual state is the sanctioned deprecation-window state — counting it would keep every hook-instrumented server permanently dirty), but the same name held at a wrong scope still counts. Note shapes: `none`; `N server(s) still carry legacy option names (@color/@session_color) — attach from the dashboard or run \`rk mux adopt <server>\` to sweep` (with external servers present appended `, of which M external — rk will not rewrite those`); an enumeration failure reports `skipped — enumeration failed: …` and stays OK. Seam `legacyOptionsScan` substitutes the scan in tests.

### Deprecation Ledger

The dual-read surface is transition-only. The follow-up "remove legacy reads" release deletes every item below — the ledger is its checklist. The externally-written-key items (server marks, pane keys, hook dual-write, `CopyOnly` rows) carry an extra gate beyond "a few releases": removal lands **no sooner than one release after fab-kit's new-then-old pane read ships** and the fleet's `rk doctor` `agent hooks` row reports clean for about a week of updates (the ledger's full provenance and the removal gate are reproduced in `docs/specs/agent-state.md` § Naming / Deprecation window).

- **Legacy dual-read format fields and parser fallbacks** — the `#{@rk_note}` fields in the `ListWindows` format and `layoutWindowFormat`, and the legacy-field fallback branches in `parseWindows`/`parseLayoutWindows`. They exist only to catch post-sweep legacy writes from stale external writers; after a few releases the sweep has converged every carrier and stragglers are unsupported. Delete the legacy fields from both formats and the fallback branches, leaving the new-name fields only.
- **Retired-web-name compat surface** — the `@rk_win_url` dual-read fallback field in the `ListWindows` format and in `ReadWebTabFamily` (plus the `WebRemove` last-tab clear), the `/options` `@rk_win_url`/`@rk_win_lens` compat translation (`translateLegacyOptionKeys`, `api/windows.go`), and the n-less `/present/{windowId}/*` compat arm (`api/present.go`) — all marked `// compat:`, one-release shims over the indexed web-tab family retained for the cleanup change. The derived `rkUrl`/`rkType` Window-JSON fields are GONE — the frontend reads `layout`/`webTabs`/`webActive`/`codeRoot` directly (iip5).
- **Server-mark dual-read** — the second `show-option` in `readServerMarkDual` (the `IsEphemeralServer`/`IsProtectedServer` legacy fallback). Once no writer still marks `@rk_ephemeral`/`@rk_protected`, the predicates read the new name only.
- **Pane-key dual-read fields and hook dual-write** — `paneFormat` fields 6–7 (`#{@rk_agent_state}`/`#{@rk_chat}`) and the field-9/field-6, field-10/field-7 preference branches in `parsePanes`, plus `PaneFactsCtx`'s legacy third field; and the second `set-option` in `writeAgentStateImpl`/`writeChatImpl`'s `;`-chained argv (leaving the new-name write only). Gated on fab-kit reading the new pane name.
- **`CopyOnly` pane rows** — once the hook dual-write stops, the two pane rows become unset-only for one further release, then are deleted with the rest of the table rows.
- **`rkHookMarker` (the gen-1 recogniser)** — `rk agent setup`'s first-generation inline-one-liner marker, bound to `tmux.LegacyAgentStateOption`; it is already scheduled for removal by the agent-setup indirection change, and its removal date is tied to this ledger.
- **Doctor `agent hooks` gen-1/gen-2 FAIL branches** — the row itself stays; its stale-generation FAIL branches can go once nothing reports them.
- **Registry "Legacy names" column entries for the 19 renamed keys** — the old→new pair per renamed row in the table above; once the legacy names are no longer read anywhere, the column entries for these rows are retired (the `@color`/`@session_color` entries may go in the same pass when their window also elapses).
- **`docs/site` "legacy names still read" notes** — the one-line transitional notes in `docs/site/skill.md` and `docs/site/skill/display.md`; delete the note lines with the legacy reads, leaving only the new names.
- **Legacy-name constants** — the exported `LegacyEphemeralOption`/`LegacyProtectedOption`/`LegacyAgentStateOption`/`LegacyChatOption` exist only as the single definition shared by the migration-table rows, the dual-read format fields, the hook dual-write, and `rkHookMarker`; once those consumers are gone, delete the constants with the rows. The unexported `legacyTypeOption`/`legacyURLOption`/`legacyNoteOption` (`app/backend/internal/tmux/tmux.go`) exist only as the single definition shared by the migration-table rows (and the note name additionally by the legacy-note dual-read format field); once those consumers are gone, inline the strings into their `legacyOptions` rows (or delete them with the rows) and remove the constants. The unexported retired-window names `legacyWinLensOption`/`legacyWinURLOption`/`legacyWinPresentRootOption` go with the retired-web-name compat surface above.
- **Dual-read and legacy-rejection tests** — `TestParseWindowsNoteDualRead` / `windowLineNoteDualRead` (`internal/tmux/tmux_test.go`), `TestParseLayoutWindowsNoteDualRead` (`internal/tmux/layout_test.go`), and `TestWindowOptionsLegacyKeysRejected` (`api/windows_test.go`) pin behavior that stops existing when the legacy reads are removed; delete the dual-read tests with the fallbacks (likewise the server-predicate and pane-format dual-read cases), and keep the API rejection test only if the allowlist still needs a negative case for arbitrary unknown keys (renamed accordingly).
- **e2e legacy pre-seed and sweep spec** — the legacy pre-seed block in `scripts/test-e2e.sh` (server-scope `@rk_origin`/`@rk_session_order`, window-scope `@rk_role`/`@rk_url`/`@rk_note` on the init window) and `app/frontend/tests/e2e/legacy-scope-sweep.spec.ts`, both marked "removed when the deprecation window closes"; delete them in the same release that turns the migration table unset-only (a purge-only table still needs one regression test — fold it into `legacy-color-sweep` if that spec survives).

### `@rk_ses_pin_board` — Board Membership (pin-session SESSION var)

`@rk_ses_pin_board` is a **session-scoped var on each `_rk-pin-*` pin-session** holding just the board name (260602-qn62-move-based-board-pin-sessions). Board membership is derived entirely from the set of pin-sessions and their vars — a **board is the set of pin-sessions sharing an `@rk_ses_pin_board` value**. There is no registry option; **empty boards cannot exist** (a board appears when its first pin is created and vanishes when its last pin is unpinned). Boards are **server-scoped** — `link-window`/`move-window` can't cross tmux servers, so a pin-session always lives on its window's server; the board *list* is summarized across reachable servers but no cross-server window union exists. The `BoardOption` constant is the `@rk_ses_pin_board` SESSION-var key.

**Validators**: board name `^[A-Za-z0-9_-]{1,32}$` (`ValidBoardName`), window id `^@\d+$` (`ValidWindowID`), order key `^[a-z]{1,16}$` (`ValidOrderKey`).

**The board API surface** (`internal/tmux/board.go`):

| Function | Purpose |
|----------|---------|
| `pinEntry(ctx, server, pinSession)` | derive one `BoardEntry` by reading the pin-session's `@rk_ses_pin_board`/`@rk_ses_pin_order` vars; a session with no/invalid `@rk_ses_pin_board` is defensively skipped (`ok=false`, no error) |
| `ListBoardEntries(ctx, server)` | per-server entries from `ListPinSessionNames` → `pinEntry` (no-server/absent-option ⇒ `([]BoardEntry{}, nil)`) |
| `ListBoards(ctx)` | iterate `ListServers`, group by `@rk_ses_pin_board`, alphabetical `[]BoardSummary` with per-board pin counts |
| `GetBoard(ctx, name)` | entries for one board across reachable servers, sorted by `OrderKey`, **no write-back** (membership is live; killed pins just disappear) |
| `Pin(ctx, server, windowID, board)` | STAMP-BEFORE-LINK 3 vars then `link-window` into `_rk-pin-<id>` (window stays in home too) + kill placeholder; idempotent (re-stamp `@rk_ses_pin_board` on wrong-board re-pin); rollback rooted in `context.Background()` (see § Pin Sessions) |
| `Unpin(ctx, server, windowID, board)` | board-match guard, then normal path = `kill-session` the pin-session (window survives in home via its home link); last-link recovery when the pin link is the only link (link into a live `@rk_ses_pin_home` / recreate a dead home via rename-session / collision-guarded `recovered<id>`); idempotent on missing pin-session (see § Pin Sessions) |
| `Reorder(ctx, server, windowID, board, newOrderKey)` | rewrite only that pin-session's `@rk_ses_pin_order`; errors if absent or `@rk_ses_pin_board` ≠ board |
| `ComputeOrderKey(before, after)` | fractional indexing helper |
| `nextAppendKey(entries)` | append key strictly greater than the max existing key, via `ComputeOrderKey` (thin wrapper) |

Reads route through `tmuxExecRawServer` + `context.WithTimeout(ctx, TmuxTimeout)`; `isAbsentOption` treats `invalid option`/`unknown option`/`no server running`/`failed to connect` as the empty case (mirrors `GetSessionOrder`). A pin-session whose var read fails is logged via `slog.Warn` and skipped — well-formed entries still return.

**Lexicographic / fractional order keys** (`ComputeOrderKey`, pure Go): returns a string strictly between `before` and `after` lexicographically; an empty neighbour means prepend/append. Examples: `(null,"b")→"a"`, `("c",null)→"d"`, `("b","c")→"bm"`, `("b","bm")→"bg"`. `initialAppendKey = "m"` is the first key on an empty board (midpoint letter leaves prepend headroom — there is no key `< "a"`). Inserts MUST NOT renumber siblings — a reorder rewrites exactly one `@rk_ses_pin_order` var. **The key is stored per pin-session** in `@rk_ses_pin_order`.

**No eager/lazy cleanup, no bootstrap**: there is no SSE poll-tick window-kill diff and no `RemoveAllByWindowID` consumer — a killed pinned window's pin-session simply drops out of the next `ListBoardEntries` read, picked up by the frontend's refetch on the next session-list change. There is no `@rk_ses_pin_board` first-poll bootstrap broadcast. Membership changes surface only via the explicit pin/unpin/reorder `board-changed` SSE events (see § SSE Board Events below and `architecture.md` § Boards Feature).

**SSE event** — `event: board-changed` rides the per-server SSE stream. Payload `{"board":"main","change":"pin"|"unpin"|"reorder","server":"runkit","windowId":"@1234","orderKey":"bm"}` (`orderKey` `omitempty`). Each pin/unpin/reorder handler emits its own event. See `architecture.md` § Boards Feature.

**Window ID stability across `link-window`/`move-window`** — pins rely on tmux's documented contract that both `link-window` and `move-window` preserve `#{window_id}` (`@N`). This is load-bearing for the link-based pin model: Pin (`link-window` into the pin-session) keeps the window's `@N` stable — the same id is a member of both sessions — so the URL/selection that addresses it by ID works on either surface, and `WindowIDFromPinSession` recovers the same id from the pin-session name. Unpin's normal path kills the pin-session (no window move at all; the window keeps its `@N` in home), and its last-link recovery either `link-window`s the window back into a live home or `rename-session`s the pin-session, both `@N`-preserving. `MoveWindow` (reorder via adjacent `swap-window`) and `MoveWindowToSession` (`move-window -s <windowID> -t =<dstSession>:`, exact-match dst (260717-hikh)) likewise preserve `@N`. This same ID-stability contract is what makes `MoveWindow`'s active-window restore correct (260714-6pe6-preserve-active-window-reorder): the pre-shuffle active window ID stays valid across the swaps, so the final chained `select-window -t =<session>:<activeWindowID>` re-selects the right window wherever it landed — including the edge where the active window is the one being dragged (see § Impact on Other Operations → `MoveWindow`).

## Frontend Server Routing Contract

Every API client function in `app/frontend/src/api/client.ts` that hits a server-scoped endpoint takes `server: string` as its **first positional argument** and forwards it to `withServer(url, server)` to build `?server=<server>`. There is **no module-level `_getServer` global, no `setServerGetter` export, and no ambient state** — `server` is always passed explicitly per call. This mirrors the backend `tmuxExecServer(ctx, server, args...)` shape so the routing parameter is visible in every signature on both sides of the wire.

Functions that take `server` (read + mutation):

| Category | Functions |
|----------|-----------|
| Read | `getSessions`, `getKeybindings` |
| Session mutation | `createSession`, `renameSession`, `killSession`, `setSessionOrder` |
| Window mutation | `createWindow` (session-scoped), `renameWindow`, `killWindow`, `moveWindow`, `moveWindowToSession`, `selectWindow`, `splitWindow`, `closePane` (all but `createWindow` take `windowId: string` as the 2nd positional arg and hit `/api/windows/{windowId}/...` (260529-chgz-window-id-routing)) |
| Window options | `setWindowOptions(server, windowId, options)` (the generic `POST /options` seam), `updateWindowUrl(server, windowId, url)` |
| Color | `setWindowColor`, `setSessionColor` |
| Server-scoped | `reloadTmuxConfig` |
| Session-scoped | `uploadFile` |

Signature shape:

```ts
function withServer(url: string, server: string): string {
  const sep = url.includes("?") ? "&" : "?";
  return `${url}${sep}server=${encodeURIComponent(server)}`;
}

export async function renameSession(server: string, session: string, name: string): Promise<{ ok: boolean }> {
  const res = await fetch(
    withServer(`/api/sessions/${encodeURIComponent(session)}/rename`, server),
    { method: "POST", headers: {...}, body: JSON.stringify({ name }) },
  );
  ...
}
```

**Functions that intentionally do NOT take `server`** (operate on the server itself or are global): `listServers`, `createServer`, `killServer`, theme settings (`getThemePreference`, `setThemePreference`), and server-color settings. `killServer` targets the server via request body (`{"name":"runkit"}`) rather than `?server=` query.

Neither muxed socket carries `?server=` in its URL — `/ws/state` names servers in-band via `subscribe` ops, and `/ws/terminals` carries the server in each `open` op (state socket (260716-qf3j); terminals mux (260717-803u)) — so the same "server is explicit, never ambient" property holds on the in-band control frame rather than the URL query.

### Why the explicit server contract exists (closure-race fix)

An ambient module-level getter (`let _getServer = () => "runkit"` installed once via `setServerGetter(() => serverRef.current)`) would dereference the current server at fetch time, so any switch of the server between user intent (open dialog, type new name) and fetch dispatch (Enter pressed) silently retargets the mutation at the **new** server. Reproducer: open rename for `foo` on server-A → Cmd+K → switch to server-B → return → Enter — the rename runs against server-B, hitting either the wrong `foo` or an error while the optimistic overlay still draws the rename on server-A until SSE reconciles. This is why there is no ambient getter.

With `server` threaded explicitly, the captured server is fixed at the moment the React handler runs (see `ui/dialogs-and-state.md` → "Optimistic UI & Mutation Feedback" → "Server Capture Convention" for the React idiom). The single source of truth for `server` is `useSessionContext()`; callers read it inside their event handler and pass it as the first arg.

### Verifying the contract

A single grep enforces the invariant — the symbols `_getServer` and `setServerGetter` MUST NOT exist anywhere under `app/frontend/src/`. The `useDialogState` regression test (`app/frontend/src/hooks/use-dialog-state.test.tsx`) flips `SessionProvider`'s `server` prop between `openRenameSessionDialog("foo")` and `handleRenameSession()` and asserts `renameSession` was called with the post-flip server (`"server-B"`) and never with the pre-flip server.

Window cross-session move endpoint:
- `POST /api/windows/{windowId}/move-to-session` — moves a window to another session. `{windowId}` validated via `validate.ValidateWindowID`. Request body: `{ "targetSession": "string" }`. Validates the window ID and target session name. Returns `200 { "ok": true }` on success. Handler in `api/windows.go`, `MoveWindowToSession(windowID, dstSession, server)` method on the `TmuxOps` interface in `router.go`. (260529-chgz-window-id-routing)

## `rk riff` Window Creation

`rk riff` creates N parallel worktree/window pairs (N defaults to 1) in the **user's current tmux server** (not the managed `runkit`/`default` server that `internal/tmux` targets). Each window may contain multiple panes — composed from argv-ordered `--skill`/`--cmd` occurrences or a preset's `panes:` list. The command lives at `app/backend/cmd/rk/riff.go` and bypasses `internal/tmux`'s execution helpers — it invokes `tmux` directly via `exec.CommandContext` so windows land where the user is attached.

Mechanics:

- **Preconditions** — `$TMUX` must be set (read via `tmux.OriginalTMUX`, captured before `internal/tmux` init() strips `$TMUX` from the process env) and `wt` must be on PATH. Fast-fail, exit 2. `--list-presets` short-circuits before preconditions.
- **Per-window pane sequence** — each window is built via:
  1. `tmux new-window -P -F '#{pane_id}' -n <resolvedName> -c <worktree-path> <pane-0-shell>` creates the window and prints the new pane's id (e.g. `%87`), captured by the orchestrator for the focus step below.
  2. `tmux split-window -t <resolvedName> -c <worktree-path> <pane-i-shell>` appends each additional pane (panes 1..N-1). The initial split direction is `-h` by default; the final arrangement is set by `select-layout`.
  3. `tmux select-layout -t <resolvedName> <canonical>` applies the resolved layout (skipped when pane count is 1 and layout is `auto`, or when `--layout` produces an empty canonical for the pane count).
  4. `tmux select-pane -t <captured-pane-id>` focuses the first pane. Targeting by pane id (rather than a hardcoded `<window>.0` index) is portable across tmux servers configured with `pane-base-index 1` — an index-based `<window>.0` target errors with `can't find pane: 0` on those servers.
- **Pane shell strings** — skill panes wrap `<launcher> '<escaped-skill>'` (or bare `<launcher>` when skill is empty) inside `${SHELL:-/bin/sh} -i -c '...'` then `shellWrap` (`; exec "${SHELL:-/bin/sh}"`). Cmd panes use the user's command directly + `shellWrap` (no interactive `sh -i -c` wrap — would alter argv semantics of user commands). Empty `--cmd` yields the bare `exec "${SHELL:-/bin/sh}"` form (drops to `$SHELL`). All handled by pure helpers `buildSkillShellString`, `buildCmdShellString`, `paneShellString`, and the argv builder `buildSpawnArgvs`.
- **Window naming** — `riff-<basename>` where `<basename>` is `filepath.Base(worktreePath)`. For `--count N` (N ≥ 2), each worktree gets its own name (assigned by `wt create`'s random adjective-noun generator; rk does NOT impose a `-1..-N` suffix scheme), so windows end up named `riff-swift-fox`, `riff-clever-crab`, etc. Collision resolution applies per-window via `resolveWindowName` (auto-suffix `-2`, `-3`, …).
- **`--count` orchestration** — `runCount` spawns N goroutines sharing one `context.Context`. Each goroutine runs `runWtCreate` + `spawnRiffReturningName`; first failure cancels the context (killing sibling subprocesses via `exec.CommandContext`) and triggers rollback. The pure `planFanOutRollback(results, failureIdx)` computes which worktrees + windows to clean up — excludes the failing goroutine's own artifacts — and `rollbackFanOut` invokes `wt delete --non-interactive <basename>` (positional arg, not the deprecated `--worktree-name`; `--non-interactive` so the prompt does not block on EOF) then `tmux kill-window -t <name>`. Rollback uses a fresh (non-cancelled) context so it runs to completion; rollback errors are logged but do not mask the primary error.
- **Child env** — `tmuxChildEnv()` restores `TMUX=<OriginalTMUX>` in every spawned tmux process so tmux targets the user's server. Mirrors `cmd/rk/context.go`.
- **Timeouts** — `exec.CommandContext` with 30s for `wt create` / `wt delete`, 10s per tmux call.
- **Signals** — SIGINT/SIGTERM once, at the top of `runRiff` via `signal.NotifyContext`; propagates to all goroutines and their subprocess calls.

The new windows never appear on the managed `runkit`/`default` servers unless the user's current `$TMUX` happens to point there. See `rk-riff.md` for flag surface (`--skill`, `--cmd`, `--layout`, `--count`/`-N`, `--preset`, `--list-presets`), exit codes, and preset schema.

`rk riff`'s bare `new-window` carries no active-window-highlight logic of its own: when it lands on a server run-kit observes, the resulting `%session-window-changed` is what drives the sidebar/URL to follow the new window (event-derived highlight — see § Terminal Relay → Active-window highlight). The derivation path is creator-agnostic, so `rk riff`, `wt open`, and raw external `new-window` all update the highlight correctly without any creator-local change (260530-v6hm-active-window-event-derivation).

## Managed Window Creation — Name-Optional `CreateWindow` + Folder Auto-Naming

A managed window (the plain sidebar/palette/board `+ New Window` paths — NOT `rk riff`, NOT iframe windows) is created **without a name**, so tmux auto-names it to its folder basename and live-updates that name as the pane `cd`s (260707-j66b-unnamed-windows-autoname-folder). The name is derived and single-sourced **in tmux** (Constitution II) — run-kit issues no rename and stores nothing; native tmux clients and the status line show the same folder name.

**Two halves make this work:**

1. **`automatic-rename-format` in the embedded configs.** All four `configs/tmux/*.conf` (`default.conf`, `simple.conf`, `poweruser.conf`, `byobu.conf`) set:

   ```tmux
   set -g automatic-rename-format '#{b:pane_current_path}'
   ```

   `#{b:...}` is tmux's basename format modifier (verified available on the host's tmux 3.6a). `automatic-rename` itself defaults to `on`, so any window not pinned with an explicit `-n` names itself to the basename of its active pane's current path — the folder basename, rather than tmux's default `#{pane_current_command}` ("zsh"/"node"/"vim"), which is near-useless identity in a worktree-per-change workflow. `byobu.conf` keeps its explicit `set -g automatic-rename on` line above the format line. The canonical `default.conf` is re-staged to the Go-embed copy `app/backend/build/tmux.conf` (staged by `just setup`), which carries the format line at ~L24. **Windows created with an explicit `-n` stay pinned** — tmux disables `automatic-rename` on an explicit name (this is desired for the deliberate-name paths below).

2. **`CreateWindow` omits `-n` when the name is empty.** `tmux.CreateWindow(session, name, cwd, server)` builds its argv via a pure helper `buildCreateWindowArgs(session, name, cwd string) []string` (mirrors the `buildNewWindowArgs`/`buildSpawnArgvs` pure-arg-builder pattern in `riff.go`, so the `-n`-conditional branch is unit-testable without a live tmux server — `TestBuildCreateWindowArgs`):

   ```
   name == ""   →  ["new-window", "-a", "-t", session,             "-c", cwd]
   name != ""   →  ["new-window", "-a", "-t", session, "-n", name, "-c", cwd]
   ```

   Because `-c cwd` is always passed, an unnamed create names itself to the folder basename **immediately** — there is **no follow-up `rename-window` round-trip** (single tmux invocation).

**Deliberate explicit-name paths pass `-n`**, keeping their names pinned: `rk riff` panes (`buildNewWindowArgs`), iframe/service windows via `CreateWindowWithOptions` (the `rkType`-present create branch), the `port-N` service windows, and explicit renames (`RenameWindow` — renaming pins the name, which is the desired tmux behavior). The rename API path **requires** a non-empty validated name.

### API contract — window name optional on CREATE only

`POST /api/sessions/{session}/windows` (`handleWindowCreate`, `api/windows.go`) makes `name` optional: `validate.ValidateNewName(body.Name, ...)` runs **only when `body.Name != ""`** (the tightened new-name rule — no spaces — since the name names a to-be-created window; see § Name-Validation Charset Contract). An omitted/empty name (with no `rkType`) is a valid request meaning "let tmux auto-name" → 201, calling `CreateWindow(session, "", cwd, server)`. A **non-empty** name that fails validation returns 400. **Guard**: when `body.RkType != ""` the handler requires a non-empty name (400 on empty/omitted) — the `rkType` path runs `CreateWindowWithOptions` with `-n <name>` and automatic-rename disabled, so an empty name there would pin the window to an empty name; the shipped UI always supplies one, and the 400 pins that API contract. The rename handler (`handleWindowRename`) requires a non-empty rename name validated via `ValidateNewName` (empty → 400). Contract documented in `docs/specs/api.md` § window create.

Windows pinned with an explicit `-n` (e.g. `-n zsh`) have `automatic-rename` off and stay pinned — there is no unpinning sweep, which would cost a per-window `set -w automatic-rename on` pass.

## Fab-Tier Derivation (native, per pane)

The fab tier (`FabChange`/`FabStage`/`FabDisplayState` on `WindowInfo`) is derived natively from the filesystem inside every `FetchSessions` (`internal/sessions/fabstate.go`) — no `fab` subprocess, no cache, fresh each fetch. Per pane: (1) from the pane's cwd, walk up to the nearest ancestor directory carrying `.fab-status.yaml` (bounded at the filesystem root; an empty/missing cwd is skipped); (2) `readlink` the symlink — its target is `fab/changes/{name}/.status.yaml`, so the change name is the target's parent-directory basename (relative targets resolve against the symlink's own directory); (3) parse the target's ordered `progress:` map (a `yaml.v3` node parse — order is load-bearing) and derive `(stage, displayState)` with fab's five-tier display-stage rule: first `active` → first `failed` (a parked failure outranks ready/done) → first `ready` → last `done`/`skipped` → first stage `pending`, mirrored from fab-kit `internal/status/status.go` `DisplayStage`. Freshness is per-fetch: a stage transition written to `.status.yaml` shows on the very next SSE tick. The only memo is per-call (`fabStateMemo`: cwd → located link, resolved link → derived state — many panes share one worktree); nothing persists across requests (Constitution II). Degradation is per-pane and fail-open: no symlink ancestor, a dangling symlink (archived change), unreadable/unparsable YAML, or an empty `progress:` map each yield empty fab fields for that pane only — never an error, never a failed fetch. The window rollup keeps the established selection semantics: among a window's panes a change-bound pane's derivation wins, else the first pane in pane order with any derivation (`fabStateMemo.windowState`). The `WindowInfo` JSON contract to the frontend is unchanged.

## Related Files

- `app/backend/internal/tmux/tmux.go` — `serverArgs()`, `tmuxExecServer()`, `ListSessions()`, `ListServers()` (delegates the raw socket-dir scan to `ScanSocketDir` and the probe to `probeServerAlive`; applies the env-gated `RK_SERVER_ALLOWLIST` filter post-probe), `ServerAllowlistEnv` const + `matchesServerAllowlist(name, allowlist)` pure prefix-match predicate (test-isolation filter; see § `RK_SERVER_ALLOWLIST`), `ScanSocketDir(ctx)`, `socketDirPath()`, `filterSocketEntries()`, `probeServerAlive(ctx, name)`, `IsTestServerName()` (single `HasPrefix("rk-test-")`; consumed only by the tmuxctl supervisor resurrection guard), `LockSocketSuffix`, `ListKeys()`, `KillServer()`, `CreateSession()`, `MarkServerEphemeral(ctx, server)` (`set-option -s @rk_srv_ephemeral 1`, mirrors `SetServerOrigin`), the protected-class pair `ProtectedOption` const (`@rk_srv_protected`) + `IsProtectedServer(ctx, server)` (exact mirror of `IsEphemeralServer`'s taxonomy — both predicates dual-read new-name-first through the shared unexported `readServerMarkDual(ctx, server, new, legacy)` helper, falling back to the exported `LegacyEphemeralOption`/`LegacyProtectedOption` constants when the new name is unset) + `MarkServerProtected(ctx, server)`/`UnmarkServerProtected(ctx, server)` (`set-option -s @rk_srv_protected 1`; the unmark unsets BOTH names; require a live server), and `IsGuardedServer(ctx, name)` (combined kill-guard: `productionDaemonServer` derivation ∨ `IsProtectedServer`; the daemon check short-circuits before any tmux read), `CreateWindow(session, name, cwd, server)` + the pure `buildCreateWindowArgs(session, name, cwd)` (omits `-n` for an empty name) (260707-j66b), `SelectWindowInSession()`, the managed-class set — `ManagedOption` const (`@rk_srv_managed`) + `MarkServerManaged(ctx, server)`/`UnmarkServerManaged(ctx, server)` (`set-option -s @rk_srv_managed 1` / `-u`; require a live server) + `IsManagedServer(ctx, name)` (combined provenance: `productionDaemonServer` derivation ∨ the `IsProtectedServer`-taxonomy option read; the daemon check short-circuits before any tmux read) + the unexported `stampManagedOnBirth(ctx, server, birthsServer)` shared by the `CreateSession`/`CreateSessionForRestore` seams (`probeServerAlive` pre-probe birth detection, warn-and-continue on stamp failure), `ResolveWindowSession()` (resolves the home non-pin session under dual membership; pin-session only when it is the sole link) + the unexported `resolveHomeSession()` it delegates the pin→home re-resolve to, `LinkWindowToSession(windowID, dstSession, server)` (`link-window`, dual membership) (260718-co9z), `HasSession(ctx, server, session)` (exact-match existence probe, consumed by the relay's pin-session-first attach and `Unpin`'s recovery), `resolveWindowSessionIndex()`, `MoveWindow(windowID, dstIndex, server)`, `MoveWindowToSession(windowID, dstSession, server)`, `ReloadConfig()`, `EnsureConfig()`, `ConfigPath()`, plus the pin-session helpers `PinSessionPrefix`/`PinSessionName(windowID)`/`WindowIDFromPinSession(name)`/`ListPinSessionNames(ctx, server)` (260602-qn62), and the operator-session helpers `OperatorSessionName`/`EnsureOperatorSession`/`MoveWindowIntoOperatorSession`/`DemoteWindowFromOperatorSession` (+`*OnServer` flavors, prefix-parameterized pure argv helpers `hasSessionArgv`/`newDetachedSessionArgv`/`demoteDestinationSession`; `ClearWindowRoleExcept` returns the cleared window IDs for displaced-carrier demotion — § Operator Session)
- `app/backend/internal/tmux/reaper.go` — brute-force-by-prefix reaper logic with the `--ephemeral` union dimension: pure `classifyReap(name, prefix, ephemeral, protected, serverLive) ReapAction` (enum `ReapActionSkip`/`ReapActionKill`/`ReapActionRemove`), `probeNeeded(name, prefix, ephemeral, protected)` (gates the kill-vs-remove subprocess probe), `enumerateMarkedServers(ctx, ephemeralEnabled)` (live-only `@rk_srv_ephemeral`/`@rk_srv_protected` enumeration via `ListServers` + `IsEphemeralServer`/`IsProtectedServer` (each dual-reading the legacy name through `readServerMarkDual`); the ephemeral set is nil when the dimension is disabled, the protected set always enumerated — the live-protected skip is unconditional; hard-skips skipped pre-query; per-server read failures isolate) — the exported sorted-names wrapper `EphemeralServers(ctx)` (consumed by `rk doctor`) rides it, the consts `productionDaemonServer = "rk-daemon"` + `minSafePrefixLen = 3`, `ReapResult`/`ReapPlanEntry`, the public `ReapTestServers(ctx, prefix, act, force, ephemeralOnly)` (applies the dangerous-prefix guard to the prefix dimension only), and the test seam `reapCandidates(ctx, dir, prefix, candidates, ephemeral, protected, probe, act)` (log-and-skip per entry via `slog`, aggregate error at end). Tested in `reaper_test.go`
- `app/backend/cmd/rk/reaper.go` — thin reap command built by the `newReapCmd(use, deprecated)` two-instance constructor (family member `rk mux reap` + hidden deprecated root alias `rk reaper`; per-instance closure-bound flag vars: `--prefix` default `rk-test`, `--ephemeral` union dimension, `--yes`/`--force` action gate, `--dry-run` explicit-alias for the default preview); `act := (yes||force) && !dryRun`; rejects an explicitly-set inherited `-L/--server` (usage, exit 2); calls `tmux.ReapTestServers(ctx, prefix, act, force, ephemeral)` and renders summary/dry-run (`renderReapSummary`/`renderDryRun`); `Long` help states the brute-force/no-liveness-protection/operating contract plus the `--ephemeral` union semantics and opt-in safety framing (naming the option via `tmux.EphemeralOption`); no scan/probe/remove/kill in `cmd/rk`
- `app/backend/internal/sessions/sessions.go` — `FetchSessions(server)` builds the dashboard view, `ProjectSession` has `Name` and `Windows` (no `Server` field) plus the content-conditional `Hidden` marker (`json:"hidden,omitempty"`) computed at the join via the pure `operatorSessionHidden` rule (§ Operator Session — `_rk-operator`, ≥1 window, every window `role == "operator"`); the per-window fab tier derives natively per pane (§ Fab-Tier Derivation) and rolls up change-bound-wins-else-first; `ProjectRoot(ctx, windowID, server)` resolves by window ID
- `app/backend/internal/sessions/fabstate.go` — the native fab-tier derivation: `locateFabStatusLink` (bounded cwd walk-up to the nearest `.fab-status.yaml`; Lstat, so a dangling symlink still counts and degrades at read time), `parseFabStatusLink` (symlink → change name from the target's parent basename → ordered `progress:` parse), `fabDisplayStage` (the five-tier display-stage rule), and the per-`FetchSessions`-call `fabStateMemo` (cwd → link, link → state; nothing persists across requests). Tested in `fabstate_test.go` (temp-dir symlink + `.status.yaml` fixtures: all five tiers, subdirectory walk-up, every degradation case)
- `app/backend/api/router.go` — `serverFromRequest()` helper, `TmuxOps` interface with server params, route registration
- `app/backend/api/windows.go` — window action handlers keyed by `/api/windows/{windowId}` (kill, move, move-to-session, rename, color, url/type PUT, keys, select, split, close-pane); `parseWindowID(r) (string, bool)` helper validates the path param; `handleWindowCreate` stays session-scoped and makes `name` **optional** (a non-empty name validated via `ValidateNewName`; empty ⇒ tmux auto-names), except the `rkType`-present branch which requires a non-empty name (400 otherwise) (260707-j66b). The rename handler validates the new name via `ValidateNewName` (non-empty, no spaces) while its `{windowId}` target is already ID-addressed — see § Name-Validation Charset Contract
- `app/backend/api/sortwindows.go` — the session sort-windows verb: `handleSessionSortWindows` + the `validateSortKeys` body allowlist and the pure `statusRank`/`windowIDNum`/`compareWindowsBy`/`sortWindowsTarget`/`planSortMoves` helpers (see § Sort Windows Verb). Named `sortwindows.go`, not `sort_windows.go` — Go's GOOS filename-suffix rule would silently exclude a `*_windows.go` file from Linux builds
- `app/backend/api/servers.go` — server list/create/kill handlers
- `app/backend/api/keybindings.go` — `GET /api/keybindings` handler (runs `list-keys`, filters via whitelist, returns JSON)
- `app/backend/api/sse.go` — per-server SSE polling hub
- `app/backend/api/terminals_ws.go` — muxed `/ws/terminals` WebSocket handler; per-stream `attachStream` picks the attach session (pin-session-first, else `tmux.ResolveWindowSession` home), session-scopes the select via `tmux.SelectWindowInSession`, and `attach-session -t <session>` via `creack/pty`. No ephemeral, no defer-kill (260717-803u-relay-mux)
- `app/backend/internal/tmux/board.go` — link-based board layer: `Pin` (STAMP-BEFORE-LINK + `LinkWindowToSession`) / `Unpin` (kill-session normal path + last-link recovery) / `Reorder`, `pinEntry`, `ListBoardEntries`/`ListBoards`/`GetBoard` (derived from `_rk-pin-*` session vars), the recovery helpers `resolveHomeSession`/`recoverPinToRenamedSession` (collision-guarded `recovered<id>` rename)/`clearMembershipOptions`/`killPinSessionIfPresent`, `ComputeOrderKey`/`nextAppendKey`/`initialAppendKey`, validators (`ValidBoardName`/`ValidWindowID`/`ValidOrderKey`), and the `BoardOption`/`HomeOption`/`BoardOrderOption` session-var key constants
- `app/backend/internal/tmuxctl/` — control-mode subscription package; `Client` opens `tmux -CC ... -t =<bootstrap> -r` per socket, **always** creates the `_rk-ctl` anchor floor (260602-a1wo-prevent-exit-empty-server-death), and sets `exit-empty off` via `tmux.SetExitEmptyOff` in `productionDial` before the anchor on every dial/reconnect. `productionDial` also stamps the covering deployment's origin onto `@rk_srv_origin` via `tmux.SetServerOrigin` (injected by `cmd/rk/serve.go` via `tmuxctl.SetStampOrigin`; skipped when no origin was injected or `tmux.ServerAllowed(socket)` is false; non-fatal, same contract as the `exit-empty` backstop it rides beside). See `architecture.md` § tmux Control-Mode Subscription
- `app/backend/internal/tmux/tmux.conf` — canonical tmux configuration (Go-embedded, written to `~/.run-kit/tmux.conf` on first run)
- `app/backend/cmd/rk/riff.go` — `rk riff` subcommand: N-pane `tmux new-window` + `split-window` + `select-layout` + `select-pane` sequence per window on the user's current tmux server (via `tmux.OriginalTMUX` restore in child env), with parallel fan-out + rollback on failure
- `app/backend/cmd/rk/pane_spec.go` — `paneFlag` pflag.Value + `rewritePaneSpaceForm` argv pre-processor supporting bare/space/equals forms for repeatable `--skill`/`--cmd`
- `app/backend/cmd/rk/layout.go` — `layoutAliases` table + `resolveLayout` + `autoLayout` helpers
- `app/backend/cmd/rk/layout_help.go` — `renderLayoutMocks` Unicode box-drawing mocks rendered inline in `rk riff -h`
- `app/backend/internal/fabconfig/fabconfig.go` — `ReadPresets`/`ReadPresetsOrdered` read the riff presets from `fab/project/config.yaml` (best-effort `yaml.v3`; return empty on any failure). Presets only — the launcher is resolved by `rk riff` via `fab agent --print`, not read here (260703-w884)
- `app/backend/internal/tmux/main_test.go`, `app/backend/api/main_test.go` — per-package `TestMain` dead-PID **POST-sweep** (`code := m.Run(); sweepDeadTestSockets(); os.Exit(code)`) (260530-cf3g): after tests, scans `/tmp/tmux-<uid>/` and `kill-server`s any `rk-test-*` socket whose embedded PID (the **second-to-last** hyphen field of `rk-test-<role>-<pid>-<ns>`, parsed by `parseTestSocketPID`) is dead per `testPIDAlive` — self-healing un-catchable SIGKILL/panic/OOM residue. PID-scoped (never a blanket wipe): live-PID sockets (a concurrent `go test` process) and names without a parseable PID are spared. Also host the shared `testSocketName(role)` helper and the `testSocketPrefix = "rk-test-"` const. Logic is duplicated across the two packages (Go `_test.go` symbols are package-private); kills use `exec.CommandContext` + 5s timeout (constitution I)
- `app/backend/internal/tmux/socketsweep_test.go` — `TestSweepDeadTestSockets_sparesLivePIDReapsDead` proves the post-sweep spares a live-PID `rk-test-*` socket while reaping a dead-PID orphan (hand-builds both names via `os.Getpid()` + a known-dead PID — the only sanctioned non-helper `rk-test-` literals); plus `TestParseTestSocketPID` / `TestTestPIDAlive`. `app/backend/api/socketsweep_test.go` carries the parse/liveness unit tests for the `api` copy
- `scripts/test-e2e.sh` (`cleanup`/`trap cleanup EXIT`), `app/frontend/tests/e2e/global-teardown.ts` — e2e teardown globs the worktree's own family: the trap iterates `"/tmp/tmux-$(id -u)/${E2E_TMUX_FAMILY}"*` (the `rk-test-e2e-<token>-` anchor, trailing hyphen included — hyphen-free tokens make a sibling worktree's family unmatchable by construction), and `global-teardown.ts` prefix-scans on `E2E_TMUX_FAMILY ?? E2E_TMUX_SERVER ?? "rk-test-e2e"`, best-effort, so PID-named secondaries (`…-multi-*`, `…-msb-*`, `…-scope-*`, `…-csw-*`) don't leak on crash/Ctrl-C. **Process-group teardown** (260530-cf3g): the dev server is launched into its OWN process group via `set -m` job control (`bash -c "… exec just dev" &` — portable, macOS has no `setsid`), and `cleanup` kills only `kill -- "-$DEV_PGID"` — never `kill 0` — after verifying via `ps` that the child's real PGID is not the script's own (a silent job-control failure would otherwise re-arm the grenade). A non-detached `kill 0` would signal the *caller's* process group, SIGTERMing live tmux servers / `-CC` control clients sharing it when the e2e run executes inline in an interactive/agent session (root cause of `kit`/`abbb`/`runWork` dying mid-session — a 16-server death burst with zero `audit=kill-server` lines; constitution VI). **Backend READ-path scoping** (260531-tmnm): the `bash -c "… exec just dev"` backend launch exports `RK_SERVER_ALLOWLIST=$E2E_TMUX_FAMILY` (the family anchor), scoping the backend's `ListServers` to this worktree's test servers for the run — distinct from `E2E_TMUX_SERVER`, which scopes the WRITE socket (see § `RK_SERVER_ALLOWLIST`)

## Design Decisions

### Upgrade legacy present values through ordinary re-presentation
**Decision**: `WebAdd` rewrites a matching legacy `/present/{windowId}/{n}/…?server=` slot to the incoming content-keyed `/present/{server}/{roothash}/…` value; same-form matches keep the `BumpVersion` refresh path.
**Why**: Normal use converges stored values without a migration sweep, while the compatibility arm keeps untouched values usable during its bounded window.
**Rejected**: Permanent dual-form identity handling, which retains two parsing contracts indefinitely; a one-shot stored-value sweep, which cannot catch legacy values written later by external live writers.
*Introduced by*: 260901-ei4t-present-url-server-root-hash

### Prefer `link-window` over `move-window` when a window must appear on two surfaces

**Decision**: A window that needs dual residence (here: home session in SESSIONS + pin-session on a board) is `link-window`'d into the second session rather than moved — tmux keeps it a member of both and destroys the window only when its LAST link is removed.
**Why**: This keeps both surfaces tmux-derived truth (Constitution II — no frontend ghost-row synthesis, no lost original index) and simplifies teardown: dropping the extra membership is a plain `kill-session` on the single-window session, and the window survives via its surviving link.
**Rejected**: `move-window` — the window leaves its home session, so the sidebar needs a synthesized ghost row and the original index is lost.
*Introduced by*: 260718-co9z-link-based-board-pinning

### Tighten new-name validation, keep existing-name lookups permissive
**Decision**: A tightened validator (`ValidateNewName` = `ValidateName` + no-spaces) guards only to-be-created / rename-target names; every existing-name lookup keeps the permissive `ValidateName`. The backend stays reject-only (no server-side name conversion) and keeps hyphens legal for both name kinds.
**Why**: The safe charset must be a real backend contract, not merely UI steering — but tightening the lookup paths too would strand pre-existing spacey names (created outside run-kit) as un-renamable and un-killable. Splitting the rule by new-vs-existing keeps the charset honest for created entities while leaving legacy names operable. Server-side conversion is rejected because silently rewriting a name would desynchronize the client's optimistic-update view of what got created (constitution §I — the backend is the security boundary, not a name-fixer).
**Rejected**: (a) tighten `ValidateName` itself — breaks rename/kill/upload of pre-existing spacey names and internal hyphenated sessions; (b) backend-side conversion — breaks WYSIWYG/optimistic-update parity; (c) reject hyphens for sessions to mirror the UI's session hyphen→`_` steering — would reject `_rk-pin-*`, `rk-test-*`, and group names, breaking run-kit itself.
*Introduced by*: 260722-ln4n-auto-safe-name-conversion

### Stamp-before-link: write derivation metadata before the entity crosses the boundary

**Decision**: When a mutation both creates a session and stamps the metadata that makes it derivable, stamp on the still-empty session BEFORE linking the payload window in.
**Why**: This removes the window-present-but-unstamped failure window entirely: a stamp failure strands nothing (the window has not moved), and a post-link failure can't exist because the metadata (`@rk_ses_pin_home`) is already durable — so the entity is always recoverable, with no double-fault rollback and no un-recoverable state.
*Introduced by*: 260718-co9z-link-based-board-pinning

### A last-link/recovery path must be collision-guarded and never strand

**Decision**: Any recovery that renames a session to a reconstructed name (`recovered<id>`, or a recreated `@rk_ses_pin_home`) probes with `has-session` first and, on collision, falls back (a bounded numeric-suffix probe for `recovered<id>`; the recovered-name rename for a raced-in `@rk_ses_pin_home`).
**Why**: The contract "a window is never left unrecoverable" outranks landing at the ideal name — a stale prior-recovery session must not make the operation error and leave the window invisible in a `_rk-pin-*` name.
*Introduced by*: 260718-co9z-link-based-board-pinning

### Retain and repurpose derivation metadata in place rather than re-encode it

**Decision**: When a data model changes shape (here: `@rk_ses_pin_board` from a bespoke server-option comma/colon encoding to a per-pin-session SESSION-var), keep the option-key constant (`BoardOption`) and the ordering helpers (`ComputeOrderKey`, `nextAppendKey`) and repurpose them — only the bespoke *encoding* helpers are dropped.
**Why**: Callers and validators stay stable.
*Introduced by*: 260602-qn62-move-based-board-pin-sessions

### An unconditional session floor beats a conditional one for `exit-empty` safety

**Decision**: Create the `_rk-ctl` anchor on *every* observed server, not only servers that start with zero sessions.
**Why**: A conditional floor leaves a server that had real sessions at attach time unprotected, so tmux's default `exit-empty on` reaps the whole server when its last real session later closes (a recurring Constitution VI violation, ≥3×).
**Rejected**: Conditional floor (anchor only on zero-session servers) — see Why.
*Introduced by*: 260602-a1wo-prevent-exit-empty-server-death

### Pin every server-birth-capable exec's CWD to a stable anchor, not rk's own CWD

**Decision**: Pin the birthing exec's `cmd.Dir` to `$HOME` (fallback `/`, which can never dangle) so the server's CWD is independent of where rk was launched.
**Why**: A tmux server keeps the CWD of the process that first touches its socket for its whole life; letting it inherit rk's CWD (often a git worktree later deleted) parks it on a dead inode, which on tmux 3.7 silently breaks every new pane's `-c` chdir. `$HOME` is what a login-shell tmux would give, the least-surprising anchor. The CWD pin is the symmetric other half of the env-sanitization "born as if started from $HOME" contract already applied at `CreateSession`.
**Rejected**: Inheriting rk's own CWD — often a git worktree later deleted, leaving the server on a dead inode.
*Introduced by*: 260720-ji0k-pin-server-birth-cwd-home

### Distinguish server-birth `cmd.Dir` from live-server `-c` hygiene

**Decision**: Only an exec that can START a server needs its `cmd.Dir` pinned (the server inherits that CWD permanently). When the server already exists, the same anchor is applied instead as a per-session `-c <home>` flag (`board.go`'s pin-session create). Pick the mechanism by whether the exec births the server, not by uniformity.
**Why**: The server's CWD is untouchable once it exists, so `-c` is the only lever and it keeps the new session's `session_path` from dangling.
*Introduced by*: 260720-ji0k-pin-server-birth-cwd-home

### Carry the deployment origin as a server-scoped tmux option, not re-injected env
**Decision**: rk-born tmux servers learn their covering deployment's origin from the server-scoped user option `@rk_srv_origin`, stamped by the covering daemon on every supervisor dial and read by pane-side CLI commands at request time — never from re-injected `RK_*` environment.
**Why**: `CleanEnvForServer()` strips all `RK_*` vars from every server rk births (the direnv-pollution fix), so a pane has no `RK_HOST`/`RK_PORT` and a bare `config.Load()` falls back to `127.0.0.1:3000` on non-default deployments. The origin is an ephemeral runtime fact only the covering daemon knows, and it exists nowhere derivable from tmux/filesystem/git within a pane — precisely the state Constitution X permits pushing, on the established `@rk_srv_rank`/`@rk_srv_session_order` channel. Options don't spray into child environments, and the every-dial stamp heals across daemon restarts and port changes.
**Rejected**: `-e` injection at server birth (server env is set once at birth — stale forever across daemon restarts since servers outlive daemons — and sprays into every child process, regressing the sanitizeEnv fix); `set-environment` post-birth (only new panes see updates; same inheritance spray); a state file under `$XDG_STATE_HOME/rk/` (a request-time read of a disk state store fails Constitution II, and one file can't disambiguate which deployment covers which tmux server); a port-owner probe (platform-divergent, slow, and ambiguous with multiple deployments on one box).
*Introduced by*: 260814-qb8z-server-url-tmux-option

### Gate the origin stamp on the deployment's own allowlist
**Decision**: `productionDial` stamps `@rk_srv_origin` only when the injected origin is non-empty AND `tmux.ServerAllowed(socket)` — the `RK_SERVER_ALLOWLIST` semantics `ListServers` already applies — admits the socket.
**Why**: The e2e backend (`RK_PORT` = the worktree's derived e2e port, `RK_SERVER_ALLOWLIST` = the `rk-test-e2e-<token>-` family anchor, per `scripts/test-e2e.sh`) runs a supervisor that dials the host's non-test sockets (the supervisor skips only `rk-test-*` names); ungated, it would overwrite host servers' origins with its own e2e origin. The allowlist is the deployment's own declaration of coverage, making "which servers do I stamp" the same question as "which servers do I enumerate".
**Rejected**: an ungated stamp (test-deployment corruption of host origins); a separate stamp-specific env knob (a second knob for the same coverage concept).
*Introduced by*: 260814-qb8z-server-url-tmux-option

### Path abbreviation is a frontend display heuristic
**Decision**: the backend sends the raw `#{session_path}`; the frontend abbreviates a leading `/home/{user}/`, `/Users/{user}/`, or `/root` prefix (segment-boundary-checked) to `~` at display time (`abbreviateHomePath`, `app/frontend/src/lib/format.ts` — the frontend's single home-substitution owner, also consumed by the PANE panel's `shortenPath`, `momh`).
**Why**: raw derived state over the wire (Constitution II posture); `~` is presentation. The frontend cannot know the server's `$HOME`, but the standard prefixes cover the real cases; unrecognized paths pass through honestly.
**Rejected**: backend abbreviation (bakes presentation into the API); shipping `$HOME` in the payload (a second field for a cosmetic win).
*Introduced by*: 260815-xb77-row-popup-identity-titlebars

### The fab tier is derived natively at request time, never via a choreography subprocess
**Decision**: `FetchSessions` reads `.fab-status.yaml` → `.status.yaml` itself (pure file I/O in `internal/sessions/fabstate.go`), fresh on every call, with only a per-call memo — no `fab` subprocess and no TTL cache on the sessions/SSE hot path.
**Why**: Constitution II names `.status.yaml`/`fab/current` as the source of fab state, and status-pyramid.md documents the L2 register source as `cwd → .fab-status.yaml → .status.yaml`. A subprocess behind a TTL cache made the StatusDot repaint lag stage transitions by the cache window plus subprocess runtime — a lag class that fresh per-fetch derivation eliminates outright; the per-call memo bounds the cost (many panes share one worktree).
**Rejected**: shelling to `fab pane map` without the cache (keeps the fab-binary dependency and per-tick subprocess cost); any cross-request cache (reintroduces the staleness window); dropping the fab fields (regresses the StatusDot L2 tier).
*Introduced by*: 260820-hol4-mux-panes-native-pane-map

### Shared move logic in internal/tmux, prefix-parameterized
**Decision**: Promote/demote/displacement move logic lives in `internal/tmux` beside `ClearWindowRoleExcept`, parameterized by the server-targeting argv prefix, invoked by both the HTTP options handler and `rk role`.
**Why**: There are two role-set seams (verified — the CLI does not ride the HTTP endpoint); the prefix-parameterized shape is exactly how the existing radio clear serves both. Handler-local logic would leave `rk role operator` (fab-operator's self-mark) cosmetic-only.
**Rejected**: Routing the CLI through the HTTP endpoint — `rk role` deliberately targets the pane's own `$TMUX` socket and hard-errors outside tmux; adding an HTTP dependency would break its contract and require origin resolution.
*Introduced by*: 260822-skcr-operator-session-physical-promotion

### Hidden marker at the FetchSessions join, not a parseSessions name-skip
**Decision**: `_rk-operator` stays in the sessions payload; a content-conditional `hidden` field is computed in `FetchSessions` after the per-session window fan-out; the frontend excludes hidden sessions at render.
**Why**: The payload nests windows under sessions and the operator window is moved (single membership) — a parse-level skip would erase the pinned row's data source. The content rule (all windows operator) needs window roles, known only post fan-out.
**Rejected**: parseSessions early-skip (the `_rk-pin-*` seam) — correct for linked pin-sessions whose windows keep home membership, data-destroying here. Also rejected: a second "operator windows" side-channel payload — more moving parts than one boolean field.
*Introduced by*: 260822-skcr-operator-session-physical-promotion

### Option-write-first sequencing
**Decision**: The role option write (set or unset, batched as today) completes successfully before any move; the move is the trailing step of the flow.
**Why**: A mid-sequence failure then degrades to the current cosmetic-only behavior (role set, window unmoved) — a known-good state the sidebar already handles — never a moved-but-roleless stray.
**Rejected**: Move-first — a crash would leave a roleless window inside `_rk-operator`; recoverable (session turns visible) but a worse resting state.
*Introduced by*: 260822-skcr-operator-session-physical-promotion

### Displaced carrier is demoted, not stranded
**Decision**: The radio clear surfaces the cleared window IDs, and any cleared window residing in `_rk-operator` is moved out per the demote destination rule within the same promote flow.
**Why**: A stranded roleless window flips the session visible with a confusing mixed population; demote-out preserves the "one operator, hidden home" invariant on every transfer.
**Rejected**: Leave-in-place (session becomes visible) — honest but confusing; the plan's collision-test item exists precisely for this transfer case.
*Introduced by*: 260822-skcr-operator-session-physical-promotion

### Attention-first total order derived from the pyramid's decision table
**Decision**: The sort-windows `status` key linearizes the status pyramid's tier-precedence model into 5 attention-first ranks (waiting/action-needed → active/in-flight → settled → idle → plain), taking the minimum matched rank per window.
**Why**: A tier-precedence dot model defines which signal *owns the dot*, not a total order; attention-first matches the verb's purpose (surface tabs needing the user). Cross-referencing `docs/specs/status-pyramid.md` from the code comment and this file keeps the two rankings of the same signals discoverable from each other.
**Rejected**: Reusing the frontend's dot decision table verbatim server-side — it answers a different question (rendering) and would couple sort semantics to presentation details like halo overlays.
*Introduced by*: 260822-ga8z-sort-tabs-status-date

### Selection-style move plan over invariant index slots
**Decision**: Sort moves are planned by walking target positions over the session's sorted current index values (invariant under `swap-window`), simulating each insert-before move, emitting a `MoveWindow` only for misplaced windows.
**Why**: `MoveWindow` re-resolves indexes per call, insert-before backward moves land exactly at the target slot, and the invariant slot set makes the plan pure and unit-testable; misplaced-count bounds the subprocess count.
**Rejected**: One `MoveWindow` per window unconditionally (wasteful, violates only-when-changed idempotence); a new chained multi-window tmux primitive (duplicates `MoveWindow`'s active-window-preservation logic — Constitution III posture, reuse the proven helper).
*Introduced by*: 260822-ga8z-sort-tabs-status-date

### Sort logic lives in the api package, in a GOOS-safe filename
**Decision**: The sort rank + plan functions sit beside the handler in `api/sortwindows.go`.
**Why**: They consume the *enriched* `WindowInfo` (fab/PR/agent rollups) that only exists post-`FetchSessions` — `internal/tmux` never sees those fields populated; the handler is their only consumer. The file is named `sortwindows.go`, not `sort_windows.go`: Go's GOOS filename-suffix rule treats a trailing `_windows` as a build constraint and would silently exclude the file from Linux builds.
**Rejected**: `internal/sessions` placement — plausible, but the functions are HTTP-verb-shaped (allowlist, 404 semantics) and the api package already hosts sibling pure helpers with handler tests.
*Introduced by*: 260822-ga8z-sort-tabs-status-date

### Born-general protected class
**Decision**: the kill-guard predicate is `rk-daemon` (derived) ∨ `@rk_srv_protected` from day one, shipped as one change.
**Why**: born-general beats build-then-generalize — the option class costs one const + one reader beside the daemon check, and deletes a stacked PR.
**Rejected**: daemon-only special-case first, generalize later — same guard seams touched twice.
*Introduced by*: 260824-xaw2-protected-server-class

### Daemon identity by derivation, never by option
**Decision**: rk-daemon's protected state derives from its constant socket name (`IsGuardedServer`); the protect endpoint rejects it (400) and the UI renders its toggle disabled.
**Why**: derivation beats options (Constitution X) — an option write could be unset, leaving the daemon killable.
**Rejected**: marking `@rk_srv_protected` on rk-daemon at daemon start — resurrectable/unsettable state for a fact the name already carries.
*Introduced by*: 260824-xaw2-protected-server-class

### Combined guard predicate lives in `internal/tmux`
**Decision**: `IsGuardedServer` keys the daemon check on `internal/tmux`'s existing `productionDaemonServer` const.
**Why**: `internal/daemon` imports `internal/tmux`, so the reverse import would cycle; the const already exists for the reaper's hard-skip.
**Rejected**: referencing `daemon.ServerSocket` from tmux (import cycle); a third shared package (overkill for one string).
*Introduced by*: 260824-xaw2-protected-server-class

### Birth detection by pre-probe inside the create seams
**Decision**: `CreateSession`/`CreateSessionForRestore` detect "this call births the server" via a `probeServerAlive` check immediately before the `new-session` exec, stamping `@rk_srv_managed` only on dead→create-success through the shared `stampManagedOnBirth` helper.
**Why**: puts the stamp at the single seam every rk birth flows through (`rk mux new`, the API create paths, riff-transitive, restore) instead of per-caller logic; the TOCTOU window degrades safely — a concurrent rk birth winning the race is still an rk birth, so the stamp stays truthful.
**Rejected**: per-caller stamping (misses future callers); unconditional post-create stamping (stamps pre-existing external servers on a mere session create — provenance lies).
*Introduced by*: 260826-lv87-external-server-provenance-adopt

### Stamp failure never fails the create
**Decision**: a failed `MarkServerManaged` after a successful birth logs at warn and the create succeeds; the server reads external until adopted.
**Why**: killing a user's just-created server over a bookkeeping mark is hostile, and the degraded state (conf applied once, no future reloads) is safe and recoverable via adopt.
**Rejected**: the `rk mux new --ephemeral` kill-on-mark-failure posture — right for ephemeral because an unmarked scratch server recreates the leak that flag exists to stop; here the failure mode is only a stale-conf window.
*Introduced by*: 260826-lv87-external-server-provenance-adopt

### One combined managed reader, daemon short-circuit first
**Decision**: a single exported `IsManagedServer` = daemon-name derivation ∨ option read; the raw option reader stays unexported.
**Why**: every consumer (the three conf gates, the servers fan-out, adopt idempotency) wants the combined answer; `rk-daemon` must read managed even against a daemon server born before the stamp existed (Constitution VI restart survival), and the short-circuit needs no live server — the `IsGuardedServer` shape verbatim.
**Rejected**: exporting the raw option reader too (no consumer; surface for drift).
*Introduced by*: 260826-lv87-external-server-provenance-adopt

### External = unmarked, and guest mode keeps full editing
**Decision**: no option write marks a server external; provenance is stamp-presence only, and external servers stay fully first-class (attach, edit, kill, color, rename) — only the managed-conf apply paths refuse them.
**Why**: on release day every pre-existing rk-born server lacked the stamp and read external — blocking edits would have stopped all work; conf regimes are the only thing that must not mix silently. `rk mux adopt` is the documented recovery for pre-feature rk-born servers (no automatic bulk stamping).
**Rejected**: a negative/external option write (a second write path for the absence of a fact); a read-only mode for external servers (breaks the guest-mode contract); automatic bulk stamping (a stamp rk cannot prove truthful — it records which conf applied at birth, unobservable retroactively).
*Introduced by*: 260826-lv87-external-server-provenance-adopt

### Adopt is one-way, non-interactive, and never auto-colors
**Decision**: adopt (stamp + source managed conf, unmark-on-failed-reload) has no release/un-adopt verb, prompts for nothing (invocation is consent), and assigns no server color.
**Why**: a release verb would need a truthful un-conf path tmux does not offer (the user's own config returns on server restart regardless); the bulk-migration role requires scriptability, so the CLI and endpoint are idempotent (`already managed` / `200 already-managed`, exit/200 — no 400 even for rk-daemon); color assignment stays its own action.
**Rejected**: a release verb ("3 not needed"); adopt auto-assigning a color (conflates two independent decisions); the protect endpoint's 400 for rk-daemon (breaks bulk-migration scripts against the idempotency contract).
*Introduced by*: 260826-lv87-external-server-provenance-adopt

### Sweep hooks at ReloadConfig seams, not a periodic loop
**Decision**: `MigrateLegacyOptionsOnce` runs wherever rk already reloads its managed conf onto a server (daemon start sweep, WS pre-attach, adopt API + `rk mux adopt`, reload-config endpoint).
**Why**: `RefreshSweep` only runs after a force-written stale conf; the pre-attach reload is the seam that touches every server a browser opens, so it is the de-facto cadence. Reusing the managed gate keeps rk from rewriting options on servers it did not birth.
**Rejected**: a ticker in the SSE hub — adds a background tmux write path with no managed gate of its own.
*Introduced by*: 260828-b71j-tmux-option-namespace-legacy-sweep

### Once-guard marks on attempt
**Decision**: the per-daemon once-guard records a server before the sweep runs (`MarkLegacyMigrationAttempt`), so concurrent attaches double-take nothing.
**Why**: a server whose sweep fails would otherwise be re-swept on every attach; `rk mux adopt` and a daemon restart are the explicit retry paths.
**Rejected**: mark on success — retry storms on a broken server.
*Introduced by*: 260828-b71j-tmux-option-namespace-legacy-sweep

### Append new pane-format fields rather than replace
**Decision**: `paneFormat` fields 9–10 carry the scope-named pane keys; the legacy unscoped names stay at fields 6–7, and `parsePanes` prefers the new field when non-empty.
**Why**: every existing parser index stays valid; the follow-up drops fields 6–7 — the same shape as the `@rk_win_note` dual-read.
**Rejected**: swap in place (touches every index and every fixture twice).
*Introduced by*: 260828-5jlp-tmux-option-dual-read-external-keys
