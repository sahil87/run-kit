## Backlog

- [ ] [63td] 2026-03-03: We would need buttons to delete this worktree and at the project level to delete all unused worktrees.
- [ ] [ar9l] 2026-03-03: Buttons to send Git PR and a way to keep checking its status.
- [ ] [6bdn] 2026-03-03: Buttons to control the 'wt-* workflow' - using wt-create to create a new worktree etc
- [ ] [oibl] 2026-03-14: unable to scroll terminal on mobile
- [ ] [rkx4] 2026-03-20: The text input dialog - show buttons at the bottom of that dialog in a section that represent 'most used commands' - Clicking on them enters that text in the input box - later parts of this list may also be exposed in the dashboard cards - a quick way send commands to different session. This could be used to send the text 'Create PR' or 'Merge PR' to sessions that are ready to do so
- [x] [29qz] 2026-04-03: Add CmdK options to move a the current window to the next or previous window (move window left or right). Also allow doing this by dragging the window on the left pane
- [x] [dq70] 2026-04-03: Add CmdK options to move a window to another session. Also allow this by dragging the window out of the current session into another session
- [ ] [sl01] 2026-04-10: Sync latency: Move window within session (drag-drop reorder) has no optimistic update — UI waits ~2.5s for SSE poll to reflect the new order. Add optimistic reorder to the sidebar window list on drop.
- [ ] [sl02] 2026-04-10: Sync latency: Move window to another session (cross-session drag) has no optimistic update — UI waits ~5s+ for SSE to reflect the move. Add optimistic removal from source session and insertion into target session on drop.
- [ ] [sl03] 2026-04-10: Sync latency: Create window via sidebar + button has no optimistic update — UI waits ~2.5s for SSE to show the new window. Add ghost window (similar to ghost session pattern used for create session) on window creation.
- [ ] [xao8] 2026-05-29: Root-cause the recurring window-sync bug class: the app routes and selects on session-name + window-index (/$server/$session/$window, select-window -t session:index), but tmux window indices are reused after kill/move while window names are ambiguous/renameable. Switch the routing + select layer to key on the stable tmux window id (@N, unique for a window's lifetime). The frontend already carries windowId in every snapshot (and now exposes data-window-id); the backend SelectWindow accepts @N as a target directly. PR #202 fixed the click->terminal and double-highlight symptoms but treats the symptom, not this root.
- [ ] [52jc] 2026-05-29: Fix the failing 'kill-then-create at same index' e2e test (sidebar-window-sync.spec.ts). After a ctrl+click optimistic kill, the killed window does not disappear from the sidebar within 5s (expect(...).not.toBeVisible() fails). Deterministic (3/3) and confirmed pre-existing — verified failing on the pre-#202 baseline, so it is not a #202 regression. Likely in the optimistic-removal / syncWindows reconciliation path.
- [x] [fww2] 2026-05-29: rk reaper: extend the existing sweepOrphanedRelaySessions (cmd/rk/serve_sweep.go) to also reap orphan tmux SERVERS matching IsGoTestServerName (rk-test-*, rk-relay-test-*, rk-verify-*, rk-tmuxctl-test, rk-daemon-test) at rk startup. Today these only leak — runtime filters in /api/servers (612f84f) and tmuxctl supervisor (7ba1a5d) hide them, but sockets and tmux daemon processes accumulate on disk. Also subsumes the manual one-time cleanup from PR #199 (.lock recursion orphans) by sweeping .lock sockets too. Be careful with rk-e2e-* sockets — those need to stay live during e2e test runs (don't reap by prefix; consider reaping only when the original go-test PID is dead, e.g. parse PID from rk-test-<pid>-<ns> naming convention and check kill(pid, 0)). [DONE as 260529-fww2-rk-reaper-command: shipped a standalone top-level `rk reaper` (operator-invoked, NOT startup) instead of extending the startup sweep, and NO PID-gate (operator invocation asserts safety) — both deliberate /fab-discuss overrides of the original text above.]
- [ ] [r8kp] 2026-05-30: Revisit #208's (260529-wtg4) Go-test socket cleanup timing. #208 added a TestMain PRE-sweep (runs before m.Run()) in internal/tmux and api that reaps dead-PID rk-test-*/rk-relay-test-* sockets — i.e. each `go test` cleans up the PREVIOUS run's residue. This is not-quite-right: residue lives on disk in the gap between runs, and if there's no next run it never gets cleaned. Preferred model: clean at test END. Existing per-test t.Cleanup(kill-server) already does this for the normal path; the only gap is the un-catchable case (SIGKILL/panic/OOM before t.Cleanup fires). Consider converting the TestMain pre-sweep to a POST-sweep (defer/after m.Run()) so each run reaps its OWN residue, and rely on the manual `rk reaper` (260529-fww2) for the genuinely un-catchable SIGKILL residue rather than the next run's pre-sweep. Evaluate the concurrent-go-test interaction (live-PID sparing must still hold for a post-sweep).
- [ ] [p4mx] 2026-05-30: Unify ALL test tmux-socket prefixes under a single `rk-test-*` umbrella so "is this a test artifact?" is one HasPrefix check instead of today's six-prefix / two-rule split. Currently IsGoTestServerName matches 5 reap-anytime Go-test prefixes (rk-test-, rk-relay-test-, rk-verify-, rk-tmuxctl-test, rk-daemon-test) while rk-e2e/rk-e2e-* (Playwright) is a SEPARATE never-reap exclusion — because e2e can be LIVE mid-run. All the .lock-inherits-base-server logic, the IsGoTestServerName-excludes-e2e comments, and the rk reaper exclusion dance exist only because e2e is a test artifact outside the test-naming convention. Proposed end state (Option A): rename to `rk-test-<role>-<pid>-<ns>` (e.g. rk-test-e2e-multi-…, rk-test-relay-…, rk-test-unit-…) AND replace the e2e NAME-exclusion with a uniform PID-LIVENESS gate (reap an rk-test-* only when its embedded PID is dead — exactly #208's parseTestSocketPID/kill(pid,0) discipline). A live e2e run has a live PID → spared automatically; no special-case. This collapses 6 prefixes + 2 rules + .lock-inheritance into 1 prefix + 1 PID rule. Touches: every test helper that names a socket, scripts/test-e2e.sh + global-teardown.ts, IsGoTestServerName, internal/tmuxctl supervisor filter, /api/servers filter, #208's parseTestSocketPID, and rk reaper. NOTE: this would reverse the deliberate no-PID-gate decision for the MANUAL rk reaper (260529-fww2) — decide whether the manual reaper adopts the PID gate or stays unconditional (only-manual). Coordinate with r8kp (both are #208-adjacent test-cleanup work).
- [ ] [mss7] 2026-06-02: Fix the multi-server sidebar group-expand async race: `multi-server-sidebar.spec.ts:70` ("clicking a session in the second server's group navigates") fails DETERMINISTICALLY (3/3) on the properly-isolated :3020 harness — expanding a NON-current server group never surfaces its session rows within 10s. NOT the expand toggle: a Vitest repro (inject sessions for a non-current server, click "Expand") proves `toggleServerSection` (sidebar/index.tsx:149-173) and the `isOpen`-gated row render are sound. The real cause is an async race in the attach→SSE→sessions-arrive path: clicking Expand calls `attachServer(B)` as an IMPURE side-effect INSIDE the `setServerSectionsOpen` updater (sidebar/index.tsx:168-169); under React 19 StrictMode (active in dev/e2e via main.tsx:8, which double-invokes updaters) this likely desyncs `attachedNonCurrent` (session-context.tsx:104) vs `serverSectionsOpen`, OR the EventSource pool never opens/keeps B's stream, so B's group opens but stays "(no sessions — + new)". Backend SSE for B is fast (~29ms first payload), so the gap is frontend. FIX DIRECTION: move `attachServer(server)` OUT of the state updater (compute `next`, then call setState and attachServer separately — updaters must be pure); re-verify under StrictMode and a production build (StrictMode off). Add a Vitest unit test for the async-attach path (index.test.tsx only covers the Server-Pane toggle + pure expand today) and consider switching the e2e to the `gotoServerReady` gate (tests/e2e/_ready.ts). REPRO: `just test-e2e "multi-server-sidebar.spec.ts:70" --trace on` — NOT `just pw` (poisoned by RK_PORT=3000 in some shells → hits the live :3000 operator instance instead of the isolated harness). Distinct from sl02 (that's the optimistic-update latency for cross-session drag; this is a non-current-group attach/render bug) and from the SSE-cadence cluster (fixed by #223).
- [ ] [a36m] 2026-06-02: Add a build-time 'help-dump' step that emits rk's CLI help tree as help/run-kit.json and PRs it into sahil87/shll.ai (the shll.ai landing site renders it as an expandable 'Command reference' on the run-kit tool page). CONTRACT (frozen — copy the reference sample at sahil87/shll.ai path help/wt.json): JSON shape is {tool, version, captured_at (ISO-8601 UTC), schema_version: 1, root: Node} where Node = {name, path (full invocation e.g. 'rk riff'), short (one-line desc), usage, text (the RAW -h output byte-for-byte, newlines preserved), commands: Node[] (recursive; empty array = leaf)}. NOTE the file is named help/run-kit.json (matches the tool's repo/site slug) even though the binary is 'rk' — set the top-level tool field to 'rk' (the invoked name). rk has genuine NESTED subcommands (e.g. rk riff ...), so the recursive commands[] is exercised here — capture the full depth. PRODUCER (rk is Cobra/Go, binary 'rk', module 'rk', main at app/backend/cmd/rk): walk the cobra command tree programmatically (rootCmd.Commands() recursively), NOT regex-parsing -h text; per node capture cmd.Name / cmd.CommandPath() / cmd.Short / cmd.UseLine() and cmd.UsageString() (or Long+UsageString) as 'text'. FILTER OUT cobra's auto-generated 'completion' and 'help' subcommands and any cmd.Hidden==true (rk has serve/internal-ish commands — audit which are user-facing). VERSION: read from the built binary (rootCmd.Version / embedded ldflags) — do NOT hardcode. PUSH: in CI after the Go build, run the dump, write help/run-kit.json, validate it parses, then open a PR into sahil87/shll.ai using the existing repo secret SHLLAI_TOKEN (contents + pull-request write) with auto-merge enabled (PR, not direct push to main, to avoid the multi-repo push race). This is rk's slice of a 7-tool rollout; the shll.ai site-side consumer (Astro loader + reference UI) is tracked separately in the shll.ai repo.
- [ ] [q7vn] 2026-05-30: Add a general-purpose manual kill mode to `rk reaper` via `--prefix <p>` — reap arbitrary-named tmux servers in bulk (operator escape hatch), DISTINCT from the bare-command test janitor (260529-fww2, which stays unchanged: curated IsGoTestServerName allowlist, acts by default). This is a separate, sharper tool than `tmux kill-server -L <name>` — its value is bulk + the curated exclusions still applying. Design (safety-first, all confirmed in discussion): (1) `--prefix` is MUTUALLY EXCLUSIVE with the default mode — bare `rk reaper` = test janitor unchanged; `rk reaper --prefix <p>` = general mode (never silently widen the safe path). (2) General mode DEFAULTS TO DRY-RUN — prints the kill list and touches nothing; requires explicit `--yes` (or `--force`) to actually reap, so a mistyped prefix can't destroy anything on first run (asymmetric with the bare janitor, intentionally — the arbitrary tool warrants it). (3) Hard exclusions STILL APPLY in --prefix mode: rk-e2e* (live Playwright), _rk-ctl (control anchor), and the live runkit/default PRODUCTION server stay protected unless the operator ALSO passes `--include-protected`. (4) REFUSE dangerous prefixes: empty prefix, or one so short it matches nearly everything (e.g. `rk-` or shorter), errors unless --force — a 1-2 char prefix is almost always a mistake. (5) Liveness: general mode reaps matches regardless of live/dead (operator chose the prefix), but the kill list shows (live)/(dead) per entry so the dry-run preview is the check. Tests: dangerous-prefix refusal, exclusions-still-apply-without-flag, dry-run-is-default (no --yes → no mutation), --include-protected override. Constitutional check: Minimal Surface Area — justified as the operator manual-cleanup escape hatch; keep the safe default obviously safe.
