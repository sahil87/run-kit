---
description: "Frontend UI patterns, split by surface: routes + shell, top bar, sidebar, status signals, lenses + surface layout, boards, compose strip, keyboard + palette, terminal frontend, visual design, dialogs + client state, updates + notifications."
---
# Ui Documentation

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [boards](boards.md) | Boards: link-based pinning, board page layout, pane cards, kill confirm, autofit, pane reorder, relay-connection suspension, mobile carousel, sidebar boards section, pin entry points, board/pin/view palette actions, frontend hooks. |
| [chat-view](chat-view.md) | The ?view=chat lens frontend: TS mirror of the rk-owned chat schema, pure derivation helpers (dedup/turn-group/tool-pair/pending), the useChatSubscription hook (one guarded compose for mount + reconnect), the read-only ChatView renderer (react-markdown + remark-gfm), the ChatSendForm input wired by AppShell, lens-machinery view state, center heading, connection dot = stream health, WaitingBadge deep-link, and the shared Enter-policy classifier. |
| [compose-and-bottom-bar](compose-and-bottom-bar.md) | Bottom bar (coarse-pointer-only) and docked compose strip: pointer-gated bar existence, send semantics and the Enter classifier, drafts + sent history, readline editing chords, file upload, iOS keyboard support, the card/compact compose layout with per-pointer morph triggers, edge-docked padding, the focus-memory recording seam, the stateful ⌘I/⇧Ctrl+E compose chord (show+focus / focus / hide). |
| [dialogs-and-state](dialogs-and-state.md) | Component conventions, dialogs (session name prompt, window-at-folder, spawn-agent, server kill confirm with the protected fork, server adopt confirm, tabbed settings with the registry-driven All-settings table, shell host add/edit form, width variants), clipboard utility, e2e host-global filesystem state, the Zustand window store, and optimistic UI + mutation feedback. |
| [focus-ownership](focus-ownership.md) | Per-window focus memory (tty/compose/code) and the code-server steal guard: real-focus-only recording asymmetry, the desktop restore router with its tty first-visit default, the guard arm/disarm lifecycle, the in-frame focusin grab detector, and the focus-seam consumers — the surface-chord family (recording only on the tty/compose arms), the web tile's IframeWindow reporting through the same seam shape without recording, and the tty ⌘F claim gating on the mirrored focused kind. |
| [keyboard-and-palette](keyboard-and-palette.md) | Keyboard shortcuts: tier system and the e.code rule, default bindings (incl. stateful surface chords), surface-restriction flags (ttyOnly/webOnly, the ⌘F find pair) and the kind-aware reclaim predicate, macros, override layer, claimed keys, the shortcuts panel (settings Shortcuts tab), education micro-copy; palette single mount, action registry (recovery verbs, per-server kill/protect/adopt, operator fix-name/annotate/compose/digest/color-tabs/update-annotations, session window-sort, web-zoom). |
| [lenses-and-layout](lenses-and-layout.md) | The window-view lens model (tty/web/chat/desktop), the surface layout manager (shapes × surfaces × ratios, ?layout= ladder, gap-seam tile chrome), tile/zen chords, rk present auto-expand, top-bar surface toggles + switch-to-tile, the iframe/code/chat surface renderers (web browser chrome + onboarding, shared FindBar, content zoom), the code-folder latch, tty progress line + chip, tty export menu, tile-header rk-slot anchors, and tile-renderer focus-memory + steal-guard seams. |
| [routes-and-shell](routes-and-shell.md) | Route surface and app shell: URL structure as resumable bookmark, server-gone flip, session tiles, shell grid layout, live safe-name conversion, session creation flows, instance display name, session-to-project mapping, activity status, the conditional host recovery zone, the HOST HEALTH run-kit system card (daemon line + service rows + View deep-links), and the server-class tile markers on the host TMUX SERVERS grid (ephemeral `scratch` chip, protected shield glyph, external ↗ glyph). |
| [sidebar](sidebar.md) | Sidebar: row anatomy, row identity hover tips, icon system and label zone, the three-tier coarse status rail, row flair overlays, render-performance constraints, keyboard nav + tree ARIA (incl. ⌘B), multi-select + broadcast, collapsible panels, section-visibility rail, host-page zones (health incl. the run-kit system card/boards/services), the server-class glyph axis (protected shield + external ↗ on server tile and tree header), kill controls, scroll-edge fade, operator pinned row, footer. |
| [status-signals](status-signals.md) | Status signaling: PR registers and glyphs, the compositional status dot, the shared server-class glyph axis (protected shield + external ↗, one system-owned leading slot), waiting/attention surfacing, the tty tile header's per-viewer progress chip, the three-tier row flyout card (window/session/server cards on one shared shell), the two-tier tooltip taxonomy, and the desktop status bar (window-mirror + host clusters, degradation ladder, the zen-only exit button). |
| [terminal](terminal.md) | Terminal frontend: RelayMux singleton + TerminalClient port, adaptive write batching, window-switch slide transition, xterm addons (incl. addon-search buffer find — theme-derived decorations, overview ruler) + the ⇩ export menu (serialize/transcript/copy/server history), keyboard Option-as-Meta handling, font size preference + bundling, device-split scrollback constants, unicode width handling, touch scroll, ctrl-wheel/pinch font-zoom gestures. |
| [top-bar](top-bar.md) | Top-bar chrome: persistent root mount, 3-column grid, center page heading + window rename, control cluster + overflow menu, surface-toggle group (desktop toggle mode + pinned mobile switch mode), Open split-button, refresh, breadcrumb dropdowns, host switcher, desktop-shell titlebar strip and badge reporter, and the shared control-glyph register (top-bar-icons.tsx — bar/menu + tile-verb + web URL-bar SVGs). |
| [updates-and-notifications](updates-and-notifications.md) | Web Push notification opt-in and the update-notification system: ambient + manual check feeds, reload guard, top-bar UpdateChip, palette actions, maintenance entries, steady-state version surfaces. |
| [visual-design](visual-design.md) | Visual design system: color tokens, border widths, theme system + switching, no-flicker init, PWA meta, popup elevation shadow (.rk-popup-elev), hover-animation vocabulary, logo ring + brand sweep, section/page headings, color tinting + instance accent, row textures + character flair overlays, sparkline/gauge renderers, mobile breakpoints/touch targets/safe-area. |
