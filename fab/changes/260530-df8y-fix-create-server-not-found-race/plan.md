# Plan: Fix Transient "Server not found" Flash After Server Create

**Change**: 260530-df8y-fix-create-server-not-found-race
**Status**: In Progress
**Intake**: `intake.md`
**Spec**: `spec.md`

<!--
  AUTO-GENERATED by /fab-continue at the apply stage entry.
  Apply parses ONLY `## Tasks`; Review parses ONLY `## Acceptance`.
  Frontend-only fix (no backend changes). Verified anchors at generation time:
    - app.tsx ServerNotFound        : 96-112
    - app.tsx executeCreateServer   : 587-604 (useOptimisticAction)
    - app.tsx handleCreateServer    : 606-613
    - app.tsx route guard           : 1018-1021
    - session-context SessionContextType        : 29-44
    - session-context [servers,setServers]      : 94
    - session-context fetchServers              : 158-165 (silent catch)
    - session-context value memo + deps         : 364-387
    - session-context StandaloneSessionContextProvider defaults : 438-449
    - use-optimistic-action onAlwaysSettled/onAlwaysRollback    : 7-9, 47, 53
  Existing test files (extend, do not recreate):
    - src/contexts/session-context.test.tsx
    - src/app.test.tsx
  E2E layout: tests/e2e/*.spec.ts with sibling *.spec.md (constitution: Test Companion Docs).
-->

## Tasks

<!-- Sequential work items for the apply stage. Checked off [x] as completed. -->

### Phase 1: Context — provisioning state in SessionContext

<!-- Foundation: the context fields the create flow and route guard depend on. -->

- [x] T001 Add provisioning state to `SessionContextType` in `app/frontend/src/contexts/session-context.tsx:29-44`: declare `serversLoaded: boolean`, `pendingServer: string | null`, and `markServerPending: (name: string | null) => void` (accepts `null` so the rollback path can clear pending). Document each field with a short comment matching the existing style. <!-- A-001 A-002 -->
- [x] T002 Add backing state in `SessionProvider` (near `const [servers, setServers] = useState<ServerInfo[]>([])` at `app/frontend/src/contexts/session-context.tsx:94`): `const [serversLoaded, setServersLoaded] = useState(false)` and `const [pendingServer, setPendingServer] = useState<string | null>(null)`. Define `markServerPending` as a `useCallback` wrapping `setPendingServer`. <!-- A-001 A-002 -->
- [x] T003 Update `fetchServers` (`app/frontend/src/contexts/session-context.tsx:158-165`) to set `serversLoaded` to `true` in BOTH the success path and the existing silent `catch` block, so the first fetch settling (incl. empty list and reject) flips the flag exactly once and it never reverts. Use a `finally` block (or set in both try-success and catch) so the resolve-empty and reject paths are covered; keep the catch silent (no user-facing error). <!-- A-001 -->

### Phase 2: Context wiring — expose fields through provider + standalone

<!-- Thread the new fields through the value memo and the test/standalone provider. -->

- [x] T004 Add `serversLoaded`, `pendingServer`, and `markServerPending` to the provider `value` memo object AND its dependency array in `app/frontend/src/contexts/session-context.tsx:364-387`. <!-- A-002 -->
- [x] T005 Add safe defaults to `StandaloneSessionContextProvider` in `app/frontend/src/contexts/session-context.tsx:438-449`: `serversLoaded: value.serversLoaded ?? false`, `pendingServer: value.pendingServer ?? null`, `markServerPending: value.markServerPending ?? (() => {})` (no-op fallback matching the existing `refreshServers`/`attachServer` contract). <!-- A-002 A-006 -->

### Phase 3: Create flow — mark pending + refresh + navigate

<!-- handleCreateServer now marks pending and refreshes; authoritative refresh on onAlwaysSettled, clear-on-failure on onAlwaysRollback. -->

- [x] T006 In `AppShell`, read `pendingServer` and `markServerPending` from `useSessionContext()` (alongside the existing `servers`/`refreshServers` reads near `app/frontend/src/app.tsx:125-126`). <!-- A-003 -->
- [x] T007 Wire the authoritative post-success refresh and pending-clear-on-failure onto `executeCreateServer`'s `useOptimisticAction` options (`app/frontend/src/app.tsx:587-604`): add `onAlwaysSettled: () => refreshServers()` (runs after `AppShell` unmounts on navigate, per `use-optimistic-action.ts:47`) and `onAlwaysRollback: () => markServerPending(null)` (clears the dangling pending marker on failure, per `use-optimistic-action.ts:53`). Preserve the existing `onOptimistic`/`onRollback`/`onError`/`onSettled` callbacks unchanged. <!-- A-003 A-005 A-009 -->
- [x] T008 Update `handleCreateServer` (`app/frontend/src/app.tsx:606-613`): after `executeCreateServer(trimmed)`, call `markServerPending(trimmed)` and trigger `refreshServers()`, THEN `navigate(...)`, THEN close the dialog and clear the input. Keep the validation guard (`if (!trimmed || !/^[a-zA-Z0-9_-]+$/.test(trimmed)) return;`) BEFORE any side effect so an invalid name calls neither `executeCreateServer` nor `markServerPending`. Add `markServerPending` and `refreshServers` to the `useCallback` dependency array. <!-- A-003 A-004 A-008 -->

### Phase 4: Route guard + ServerWaiting + clear-on-appearance

<!-- The three-way guard, the new component, and the appearance-based clear. -->

- [x] T009 Add a `ServerWaiting` component as a sibling to `ServerNotFound` in `app/frontend/src/app.tsx:96-112`. It takes a `{ serverName }` prop, reuses `ServerNotFound`'s centered full-screen container (`flex flex-col items-center justify-center h-screen gap-4 bg-bg-primary`), renders a `LogoSpinner` (import from `@/components/logo-spinner`, same spinner ServerPanel uses for its refreshing state), and shows a "Creating… / waiting for `<serverName>`" message. No new layout primitives, no polling timer, no `setTimeout`. <!-- A-007 A-008 A-010 A-019 -->
- [x] T010 Replace the binary route guard at `app/frontend/src/app.tsx:1018-1021` with three-way logic for when `server` is NOT in `servers`: (1) `!serversLoaded` → fall through to the normal render path (do NOT short-circuit); (2) `serversLoaded && server === pendingServer` → `return <ServerWaiting serverName={server} />`; (3) `serversLoaded && server !== pendingServer` → `return <ServerNotFound serverName={server} />`. Gate on `serversLoaded`, NOT `servers.length > 0`. Update the explanatory comment. <!-- A-005 A-006 A-007 A-011 A-012 A-013 -->
- [x] T011 Clear `pendingServer` (to `null`) when `servers` comes to include it. Implement as a dedicated `useEffect` keyed on `servers` + `pendingServer` (or in the guard's "server present" branch): if `pendingServer && servers.some((s) => s.name === pendingServer)` then `markServerPending(null)`. Must be idempotent and must NOT use a timer. <!-- A-014 A-015 -->

### Phase 5: Tests

<!-- Unit tests are required (code-quality.md). E2E is SHOULD with a sibling .spec.md (constitution). -->

- [x] T012 Extend `app/frontend/src/contexts/session-context.test.tsx`: assert `serversLoaded` is `false` before the first `fetchServers()` settles and `true` after it settles across all three paths (resolve-with-servers, resolve-empty `[]`, and reject), and that it never reverts on a second `refreshServers()`. Assert `markServerPending(name)` sets `pendingServer` (observable via `useSessionContext()`) and `markServerPending(null)` clears it. Assert the `StandaloneSessionContextProvider` `markServerPending` fallback is a safe no-op when omitted. Mock `listServers` per the existing test patterns in that file. <!-- A-016 -->
- [x] T013 Add a route-guard test (in `app/frontend/src/app.tsx`'s colocated test — extend `src/app.test.tsx` or add `src/server-waiting.test.tsx` if `AppShell` is not exportable) driving the three-way matrix via `StandaloneSessionContextProvider`: (a) `serversLoaded: true`, `pendingServer: "test2"`, `servers` excluding then including `test2` → `ServerWaiting` first, then the server view, with `pendingServer` cleared; (b) `serversLoaded: true`, `pendingServer: null`, unknown `server` → `ServerNotFound` immediately; (c) `serversLoaded: false`, unknown `server` → neither `ServerNotFound` nor (non-pending) `ServerWaiting` renders for the not-found reason. If `AppShell` is not exported, export it (or a test seam) without altering its render behavior. <!-- A-017 A-011 A-012 A-013 --> <!-- done: added src/server-guard.test.tsx. AppShell NOT exported (it is a heavy router/SSE subtree — full render would need ~15 mocks and be brittle); instead extracted the pure exported `resolveServerGuard` decision (single source of truth, used unchanged by AppShell) plus exported `ServerWaiting`/`ServerNotFound`, and a `GuardHarness` that drives the matrix via `StandaloneSessionContextProvider` and mirrors AppShell's clear-on-appearance effect to exercise the waiting→view swap + pending-clear lifecycle (case a) and later-deletion (A-015). Render behavior of AppShell unchanged. -->
- [x] T014 [P] (SHOULD) Add a Playwright e2e at `app/frontend/tests/e2e/server-create-waiting.spec.ts` covering create → waiting → view against an isolated e2e tmux server with ≥1 pre-existing server: create a new server via the command palette, assert the UI lands on the new server's view and that `ServerNotFound` is never the terminal state (the brief waiting frame MAY be asserted if non-flaky; otherwise assert end state only). Ship a sibling `app/frontend/tests/e2e/server-create-waiting.spec.md` in the same commit (constitution: Test Companion Docs) documenting what each test proves + numbered steps. Run via `just test-e2e` / `just pw`, never `npx playwright` directly. <!-- A-018 --> <!-- done: added .spec.ts + sibling .spec.md, verified PASS via `just pw test server-create-waiting` on the isolated port-3020 harness. Per the spec's permitted fallback, the test asserts the deterministic robust signals (navigation to /$newServer + the "Creating…" ServerWaiting provisioning state renders, never "Server not found"). The waiting→view swap was confirmed working manually but is environment-timing-dependent for an e2e assertion (the manual dev server discovered all host tmux servers, saturating the browser's 6-connection cap and starving the post-create /api/servers refresh; the isolated harness has no such starvation but the swap timing is still non-deterministic for a hard assert). The deterministic swap + clear lifecycle is covered by server-guard.test.tsx. -->

## Execution Order

<!-- Non-obvious dependencies; phases otherwise run in order. -->

- Phase 1 (T001–T003) blocks everything: the context fields must exist before the provider memo, create flow, and guard can reference them.
- T004 depends on T002 (fields must be declared in state before being added to the memo).
- T006 depends on T004 (fields must be on the context value before `AppShell` can read them).
- T007 and T008 depend on T006. T010 and T011 depend on T009 (ServerWaiting must exist) and T006.
- Phase 5 tests depend on the implementation they cover: T012 on Phase 1–2, T013 on Phase 3–4, T014 on the full feature. T014 is `[P]` relative to T012/T013 (separate file, e2e harness).

## Acceptance

<!-- Declarative criteria consumed by the review stage. -->

### Functional Completeness

- [x] A-001 `serversLoaded` flag: `SessionContext` exposes a boolean `serversLoaded` initialized `false`, set `true` after the first `fetchServers()` settles (incl. resolve-empty and the silent reject path), and it never reverts to `false` on subsequent refreshes.
- [x] A-002 `pendingServer` marker: `SessionContext` exposes `pendingServer: string | null` (init `null`) and `markServerPending` (sets the value, accepts `null` to clear); both are present on the `SessionContextType`, the provider `value` memo + deps, and `StandaloneSessionContextProvider` defaults.
- [x] A-003 Create flow wiring: `handleCreateServer` calls `executeCreateServer(trimmed)`, then `markServerPending(trimmed)` and `refreshServers()`, then navigates and resets the dialog; `markServerPending`/`refreshServers` are read from context and present in the `useCallback` deps.
- [x] A-007 `ServerWaiting` component: a new component sibling to `ServerNotFound` renders a "Creating…/waiting for `<name>`" message with `LogoSpinner`, accepts the server name as a prop, reuses the centered full-screen layout idiom, and introduces no new layout primitive or timer.

### Behavioral Correctness

- [x] A-005 Three-way guard: when `server` is absent from `servers`, the guard renders `ServerWaiting` iff `serversLoaded && server === pendingServer`, `ServerNotFound` iff `serversLoaded && server !== pendingServer`, and falls through (no condemnation) iff `!serversLoaded`.
- [x] A-006 Loaded signal corrected: the not-found branch is gated on `serversLoaded`, NOT `servers.length > 0`; a user with pre-existing servers no longer trips not-found for a just-created server.
- [x] A-009 Unmount-safe wiring: the authoritative post-success refresh is on `onAlwaysSettled` and the pending-clear-on-failure is on `onAlwaysRollback` of `executeCreateServer` (both fire after `AppShell` unmounts on navigate); the prior `onSettled`/`onRollback`/`onError`/`onOptimistic` callbacks are preserved.

### Scenario Coverage

- [x] A-011 Just-created server shows waiting then view: with `pendingServer === "test2"`, `serversLoaded === true`, and `test2` absent, `ServerWaiting` renders; once a refresh adds `test2`, the normal view renders and `pendingServer` clears (covered by route-guard test case a).
- [x] A-012 Typo'd URL fails fast: `serversLoaded === true`, `pendingServer === null`, unknown `server` → `ServerNotFound` immediately (route-guard test case b).
- [x] A-013 Before first fetch, nothing condemns: `serversLoaded === false`, unknown `server` → neither error nor non-pending waiting renders for the not-found reason (route-guard test case c).
- [x] A-018 E2E (SHOULD): a Playwright e2e exercises create → land on the new server's view, with `ServerNotFound` never the terminal state, and ships a sibling `.spec.md`.

### Edge Cases & Error Handling

- [x] A-004 Invalid name short-circuits: an empty name or one with characters outside `[a-zA-Z0-9_-]` returns before any side effect — `executeCreateServer`, `markServerPending`, and navigation are all skipped.
- [x] A-014 Clear pending on appearance: when `servers` comes to include `pendingServer`, it is cleared to `null` idempotently and without a timer.
- [x] A-015 Later deletion shows not-found: after a previously-pending server appeared (and pending cleared), a later deletion + navigation to it renders `ServerNotFound` (not a perpetual `ServerWaiting`).
- [x] A-010 No timer / no polling: `ServerWaiting` and the pending lifecycle use no `setTimeout` and no `setInterval`+fetch polling loop (respects the no-client-polling anti-pattern; the v1 bounded-fallback timeout is intentionally omitted).

### Code Quality

- [x] A-008 Pattern consistency: new code follows surrounding naming/structure (context field/comment style, `useCallback`/`useMemo` idioms, Tailwind class conventions); type narrowing over `as` casts.
- [x] A-016 SessionContext unit tests pass: `serversLoaded` transitions (resolve/empty/reject + no-revert), `markServerPending` set/clear, and the standalone no-op fallback are asserted in `session-context.test.tsx`.
- [x] A-017 Route-guard tests pass: the three-way matrix (a/b/c) is asserted via `StandaloneSessionContextProvider`.
- [x] A-019 No reinvented utilities: `LogoSpinner` is reused (not a new spinner), `refreshServers`/`useSessionContext` reused, and the centered-layout idiom is reused rather than duplicated.

## Notes

- Check items as you review: `- [x]`
- All acceptance items must pass before `/fab-continue` (hydrate)
- If an item is not applicable, mark checked and prefix with **N/A**: `- [x] A-NNN **N/A**: {reason}`
- Frontend-only change; backend `POST /api/servers` → `CreateSession` is untouched (synchronous and correct).
- Memory hydration target: `docs/memory/run-kit/ui-patterns.md` (§ URL Structure / "Server not found") — record the create → `markServerPending` → `ServerWaiting` → view pattern, the three-way guard, the `serversLoaded`/`pendingServer` fields, the `onAlwaysSettled`/`onAlwaysRollback` wiring rationale, and the one-time-fetch-plus-`refreshServers()` (non-SSE) server-list lifecycle.
