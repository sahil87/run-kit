---
description: "System architecture split by write-axis: overview (system, data model, SPA serving, security), repo layout, backend packages, the tmux runner, the PR-status collector, CLI subcommands, and testing layers."
---
# Architecture Documentation

> **Generated by `fab docs-index`** — do not hand-edit; only the manual block below is preserved.

<!-- fab docs-index:manual:start -->
<!-- Hand-managed rows (non-markdown descriptions, external links, groupings) — preserved verbatim. -->
<!-- fab docs-index:manual:end -->

| File | Description |
|------|-------------|
| [backend-packages](backend-packages.md) | The app/backend/internal Go package table, alphabetized — one row per package with its responsibility and exported surface (validate, config, daemon, sessions, settings, metrics, ports, push, updatecheck, selfpath, wt, transcript, desktop, codeserver, codebridge, codeworkspace, gui, mcp, remote, snapshot, fsatomic, present, layoutspec, tabaddr, inject, promptscan, shellq) — plus the external Go dependency table. The tmux runner and the PR-status collector have their own files. |
| [cli](cli.md) | The rk CLI — app/backend/cmd/rk on Cobra: root defaults and version wiring, the persistent --quiet/outputSink output convention (plus the shared `--json` envelope helper `JSONResult`/`JSONError`), and the alphabetized subcommand table (agent, board, code, code-server, daemon, desktop, doctor, gui, help-dump, mcp, mux, notify, operator, present, remote, riff, role, serve, shell-init, skill, status, tab, tutorial, update, url) with each verb's files and behavior. |
| [overview](overview.md) | System overview (Go backend + Vite SPA on one port, dev vs prod topology, the -L runkit tmux server, the URL hierarchy), the no-database data model (tmux-derived state, the @rk_* option inventory, fab-tier derivation, the SSE session cache), SPA static serving and its two-tier cache policy, embedded frontend assets, and the security posture. |
| [pr-status](pr-status.md) | The internal/prstatus collector — a periodically refreshed in-memory cache of the current user's PR statuses over gh: the network-isolated three-layer split, branch-derived PR links and gitRoot keying, first-sight registration wake, host-qualified origin identity, the startup seed cache that is never a source of truth, account-switch invalidation, and stateless merged-PR durability. |
| [repo-layout](repo-layout.md) | Repository layout at directory granularity — app/backend (cmd/rk, internal, api, frontend, build), app/frontend, app/code-bridge, app/desktop, config, scripts, .github, fab, docs — with pointers to the files that own the per-file detail. |
| [testing](testing.md) | Testing layers — Go unit tests and the internal/testutil scaffolding, frontend Vitest with MSW and the src/test-utils fixtures, Playwright e2e with the _tmux/_ready/_ports fixtures and the @perf tier, retire-with shim tags, the just test recipe chain, and the Proves/Steps intent-comment rule. |
| [tmux-runner](tmux-runner.md) | The internal/tmux package — every tmux operation over exec.CommandContext argv slices: the exported Run/RunOutput runner core (caller-owned timeouts, per-site socket targeting, TMUX/TMUX_PANE child-env scrub), server/session/window/pane read and write paths, user-option helpers, and the managed tmux.conf write path. |
