---
description: "Frontend UI patterns: routes; top-bar chrome; surface layout manager (shapes × surfaces × ratios, ?layout= ladder + legacy shim, framed tile chrome + verbs, rail toggles, layout chip, mobile slot-A); window-view lenses; right rail; boards + pinning; sidebar (perf, keyboard nav, multi-select, row icons); status dot, PR registers, waiting; row-flyout card; tooltips; tiles; dialogs; palette; hooks; keybindings; bottom bar; terminal relay/font; accent/PWA/titlebar/dock badge; optimistic UI."
type: memory
---
# run-kit UI Patterns

## URL Structure

| Route | View | Component Pattern |
|-------|------|-------------------|
| `/` | Host-page host-console home | Standalone page (`HostOverviewPage`) — `flex-col h-full`. The root layout owns viewport height and the shared TopBar mounts once at the persistent root layout above the outlet, NOT pinned by this page — `HostOverviewPage` registers nothing into the slot context (§ Chrome (Top Bar) → persistent root mount). Zones in order: HOST HEALTH → BOARDS → TMUX SERVERS → SERVICES (each a `[ LABEL▊ ]────` `SectionHeading`); the Host page's page identity is the solo `Host` heading in the top-bar center cell, plus the in-page `Host Overview` SectionHeading at the top of the scroll container (§ Page Overview Headings). No sidebar and no Shell grid. `/` carries live host data — the server-independent host-metrics subscription opens here (a `metrics` subscription on the shared state socket; no per-server subscription is attached). Being sidebar-less, `/` shows no connection dot and no sidebar-footer status row (§ Chrome (Top Bar) → connection dot). The tile grid + count/loading line render from SessionContext's `servers`/`serversLoaded` so the "+" create path shares the pending-marker → `ServerWaiting` → auto-swap lifecycle (§ Create-server lifecycle). See § HOST HEALTH Zone, § BOARDS Zone, § Chrome (Top Bar). |
| `/$server` | Session-tiles density view | `AppShell` layout with `SessionTiles` content. SSE connected to the specified server. See § Session Tiles. |
| `/$server/$window` | Terminal / Iframe / Chat | `AppShell` layout. The second segment is the **numeric part** of the stable tmux **window ID** (`@N` sans `@`, e.g. `/testServer/0`) — the window ID (`@N`) is the identity everywhere in code/params/API, restored from the segment by the route's idempotent parse. `$session` is not in the route (`@N` is server-global, so the session is redundant); the session name shown in chrome is derived from the active window's SSE snapshot, not a URL segment. The address bar carries `/{server}/{N}` — the `@` is dropped for display, NOT percent-encoded. Old bookmarked `/{server}/%40N` deep links still resolve: the decoded segment `@N` passes through the idempotent parse unchanged (never `@@N`). Rendering branch: the center renders the **resolved surface layout** — 1–3 tiles, each mounting `TerminalClient`, `IframeWindow` (web), `CodeSurface` (code, § Code Surface), or `ChatView` (chat) — via `SurfaceLayout` (see § Surface Layout); the tile-grid's `single:tty` case is the plain terminal + `BottomBar`. The route carries a validated `?layout=<shape>:<surface>[,…]` search param (per-viewer layout state); the legacy `?view=`/`?panel=` params remain accepted and are translated to an equivalent layout by the permanent shim (§ Surface Layout → resolution ladder + shim). SSE connected. 3-segment `/$server/$session/$window` URLs are a hard break (no redirect shim). |
| `/board/$name` | Pane board (cross-server) | `BoardPage` shares the `<Shell>` grid wrapper with AppShell — sidebar / `<TopBar mode="board">` / pane-row content / shared `<BottomBar>`. Owns no `<CommandPalette>` mount — it registers its board-scoped actions into the layout-level palette-actions slot and the single `AppLayout` palette renders them (§ Single palette mount + palette-actions slot). Peer to `/$server`, NOT under it — boards aggregate windows across multiple tmux servers. Invalid `name` (fails `^[A-Za-z0-9_-]{1,32}$`) renders `NotFoundPage`. See § Boards View. |

Two-tier URL model (`/$server/$window`) with server always in path (no `$session` middle segment). URLs are fully shareable — copying a URL and opening it elsewhere on the same host opens the same server and window (the session is re-derived from the snapshot). TanStack Router uses nested routes: `/$server` is a layout route (`serverLayoutRoute`) whose component (`ServerShell`) wraps `SessionProvider` + `AppShell`; the terminal child route is `path: "/$window"` and uses the paired `params: { parse, stringify }` API to serialize the single `window` param (no `session`) as its numeric part in the URL. The two directions are the exported pure helpers `windowIdToUrlSegment` (stringify — strips the leading `@`) and `urlSegmentToWindowId` (parse — idempotently prepends `@`) in `router.tsx`; parse restores the `@N` form so every `params.window` consumer keeps receiving `@N`. Child routes (server index and terminal) are matched by the router but rendered conditionally by `AppShell` based on whether the `window` param exists. (`260529-jad6`, `260703-8mpy`)

**Canonical page names** — the spoken/documentation vocabulary for referring to each route. Use these names in conversation, PRs, and docs instead of the raw path patterns. A mirror of this glossary is a comment beside the `routeTree` definition in `app/frontend/src/router.tsx`. (`260702-nuup`, `260715-zs1y`)

| Route path | Canonical name | Component |
|------------|----------------|-----------|
| `/` | **Host** | `HostOverviewPage` (global home / server list; also the host-console HOST HEALTH zone, PR #290) |
| `/$server` | **tmux Server** | `ServerShell` (a single server's view; the `resolveServerView` route guard runs here) |
| `/$server/$window` | **Terminal** | *(inherited layout — no dedicated component)* (a specific window `@N`) |
| `/board/$name` | **Board** | `BoardPage` (lazy) (cross-server pane board) |
| not-found fallback | **Not Found** | `NotFoundPage` (root `notFoundComponent` catch-all — not a registered path) |

The `$server` / `$window` / `$name` segments are **route params** (actual server names, window ids, board names), not literal path segments.

**Vocabulary split** — **every** route surfaces a `PageType: name` identity heading in the top-bar CENTER cell (`Window: <window>` — static in every lens, `Board: <board>`, `tmux Server: <server>`, solo `Host`) — the canonical page type is the two-tone prefix, the instance name is the subject (§ Chrome (Top Bar) center cell). Page identity lives in the chrome, not the page body. The split reads: **the left breadcrumb ends at the PARENT** (server / session — *the trail to here*), **the centered heading is the current-page leaf** naming both the page type and the instance (*what & where you are*), so a name never appears twice in one bar (move-don't-copy — § Chrome (Top Bar), § Section Heading). The bracket idiom names the in-page **section** labels (the tmux Server page's `Sessions`, the four Host-page zones) via the shared `SectionHeading` (§ Section Heading). (`260702-6m46`, `260704-pr0p`, `260714-uco1`, `260715-zs1y`)

**Window identity is the tmux window ID (`@N`), not the index**, and **`@N` is the *only* window identity carried by the URL** — the URL *segment* carries only `@N`'s numeric part (`@N` sans `@`), restored to `@N` by the route's idempotent parse (see the `/$server/$window` route-table row above). The `$window` route param holds a stable `@N` string; all URL/API/relay/tmux layers address a specific window by this ID, while the window *index* is retained only for positional reorder/move. The owning **session name is derived from the active window's SSE snapshot** (locating `@N` within `sessions[].windows[]`), not read from the URL — `app.tsx` computes `currentSession = sessions.find(s => s.windows.some(w => w.windowId === windowParam))` and `sessionName = currentSession?.name`. Index-based bookmarked URLs (`/$server/$session/3`) AND session-bearing URLs (`/$server/$session/@N`) are a **hard break** — they do not resolve (no redirect shim; constitution §II — ephemeral URLs, no persistent state). See `architecture.md` § API Layer and `tmux-sessions.md` § Window addressing identity for the cross-layer contract. (`260529-chgz`, `260529-jad6`, `260703-8mpy`)

Server not found: if the `$server` segment doesn't match any known tmux server, a "Server not found" page renders with a link to `/`. A 3-segment URL does not match the 2-segment terminal route and falls through to the `NotFoundPage` / server-dashboard fallback.

**Three-way route guard (view / waiting / not-found)** — the `$server` segment is resolved by a pure exported helper `resolveServerView(server, servers, pendingServer, serversLoaded)` in `app.tsx` returning `"view" | "waiting" | "not-found"`. The branches: (a) `server` IN `servers` → `"view"` (render the server view); (b) NOT in list AND `server === pendingServer` → `"waiting"` (render `ServerWaiting`, a sibling of `ServerNotFound`); (c) NOT in list AND `server !== pendingServer` AND `serversLoaded` → `"not-found"` (render `ServerNotFound` immediately, so a typo'd URL still fails fast); (d) otherwise (e.g. before the first fetch resolves) → `"view"`, falling through to the server view / loading rather than flashing not-found. The guard keys on the explicit `serversLoaded` flag, **NOT** the `servers.length > 0` proxy (§ Design Decisions → Route guard keys on `serversLoaded`). The `pendingServer` waiting state applies only to the one server the user just created. `ServerWaiting` reuses `ServerNotFound`'s centered idiom (`flex flex-col items-center justify-center h-full gap-4 bg-bg-primary` — `h-full`, so these edge pages fit the content region below the persistent TopBar without overflow) + the shared `LogoSpinner`, showing a brief "Creating server… / Waiting for `<name>`" message. (`260602-3i5d`)

**Create-server → pending-marker → waiting-then-view lifecycle** — `SessionContext` (`app/frontend/src/contexts/session-context.tsx`) carries the transient state for this: `pendingServer: string \| null`, `markServerPending(name)` (an empty-string argument is the clear sentinel → sets `null`), and `serversLoaded: boolean` (set `true` in `fetchServers()`'s `finally`, so it counts as loaded even on an empty list or a caught fetch error — a permanently-false flag would hang the not-found branch forever). All three are threaded through the context type, the live provider value, and the `StandaloneSessionContextProvider` test fallback as safe no-ops/defaults. **BOTH server-create entry points drive this identical flow** — the waiting state is entry-point-agnostic: (1) the in-app **AppShell** "Server: Create" dialog (`handleCreateServer` in `app.tsx`), and (2) the `/` home-page **`HostOverviewPage`** "+ New Server" (`handleCreate` in `host-overview-page.tsx`). Each: calls `markServerPending(trimmed)` then navigates to `/$server`; its create `useOptimisticAction` fires `refreshServers()` from `onAlwaysSettled` (unmount-safe — the create dialog unmounts on navigation but the hook-owning component's root-level `SessionContext` stays mounted; the refresh touches only that) and clears the pending marker via `onAlwaysRollback` (`markServerPending("")`) so a failed create never strands the UI on the waiting state. `HostOverviewPage`'s displayed list reads SessionContext's `servers`/`serversLoaded` (the same source the guard reads, so page-display and guard-check cannot diverge) while keeping its local `ghostServers` optimistic tiles and the Host-page HOST HEALTH zone (§ HOST HEALTH Zone). `pendingServer` is auto-cleared by an effect **in SessionContext** (not in `app.tsx`) once the refreshed `servers` list contains it — event-driven on the list changing, with **no timer and no polling** (§ Design Decisions → No bounded-fallback timer on the waiting state). (`260602-3i5d`, `260701-f4e5`)

**Server-list fetch lifecycle**: the server list (`servers`, from `listServers()` → `GET /api/servers`) is a **one-time fetch on `SessionProvider` mount + explicit `refreshServers()`** — it is **NOT** part of the per-server SSE stream (SSE streams per-server *sessions*/order/metrics/board-changed only, never the list of servers). So a newly-created server does not appear until `refreshServers()` is called; the create flow above is what triggers that refresh. The `server-gone` event (below) is the symmetric *removal* trigger — it likewise drives a `refreshServers()` re-query rather than carrying the list inline.

Kill/not-found redirects go to `/$server` (server dashboard), not `/` (server list). The user stays in their server context.

### Server-Gone Reap → not-found flip (in-band `gone` event + connection-loss fallback)

The backend signals a server whose tmux socket is gone right before reaping it from the poll set — see `tmux-sessions.md` § SSE Poll-Set Lifecycle. This rides the state socket as an in-band `gone` frame (`{"op":"gone","kind":"server","key":"<server>","reason":"server-exited"}`; the `key` names the server since one socket carries every server). There is **no new HTTP endpoint or component** (Constitution IX) — it reuses the one state socket and the existing `not-found` route guard. (`260603-gs2t`, `260716-qf3j`)

`SessionProvider` (`app/frontend/src/contexts/session-context.tsx`) routes it through the `StateSocket`'s `onGone(key)` callback. The handler: drop the server from `ackedServersRef` (its dot goes disconnected) and from `subscribedServersRef`, call `socketRef.current.unsubscribeServer(key)` to **release the StateSocket ref-count** (so the diff effect can re-subscribe cleanly if the server is later restored while still attached — see `architecture.md` § State Socket → `onGone`), delete the server's slice from `slicesByServer`, then call `fetchServers()` (the `useCallback` that `refreshServers` aliases). The now-dead server is already absent from `GET /api/servers` (enumeration is socket-file-based and self-healing), so the refreshed list **shrinks**, and if the user is currently viewing that server `resolveServerView(server, servers, pendingServer, serversLoaded)` flips to `"not-found"` (`serversLoaded` is already `true` post-mount), rendering the existing `ServerNotFound` view. This extends the three-way route guard (§ above): the same `not-found` branch that catches a typo'd URL also catches a reaped server.

**Connection-loss fallback** (belt-and-suspenders): a catastrophic socket death the backend can't signal (no poll tick lands, or the daemon is mid-restart) is covered by the socket-level disconnect debounce — the state socket's `onConnectionChange(false)` clears each acked server's `isConnected` after the 3s debounce, and the provider's server-list refresh path (a `fetchServers()` on reconnect / list change) still shrinks the list, so the guard-flip path fires even with no `gone` frame. The two paths are idempotent and **first-to-fire wins**: the `gone` frame is the fast path (sub-second), the connection-loss refresh is the guaranteed-eventual path. If the server is in fact still alive, the refreshed list still contains it and no flip occurs.

### URL as Resumable Bookmark

**tmux is the sole source of truth for "current window" per server.** The URL is a **resumable bookmark** — consulted on initial mount (and reload), then treated as derived state that follows tmux. Two contracts:

1. **Mount-time alignment (one-shot)**. On the first SSE payload after a route mounts, `app.tsx` compares the URL's `$window` window ID (`windowParam`, a `@N` string) to `currentSession.windows.find(w => w.isActiveWindow)?.windowId`. If they differ, it fires exactly one `selectWindow(server, urlWindowId)` to align tmux to the URL — `selectWindow` takes `(server, windowId)`, no session, since a window ID is a self-contained tmux target (and the backend `/select` handler derives the owning session for group disambiguation; see `architecture.md` § API Layer). Guarded by `hasAlignedToUrlRef: useRef(false)` keyed on the `${server}|${windowParam}` key (window-id-only — `$session` is not in the URL, so it is not part of the alignment key). Subsequent route changes to the same window never re-fire alignment. This supports reload (URL=active is a no-op) and deep-link (URL≠active aligns tmux, then yanks any other tabs on the same server per multi-client convergence). Since the compared identifier is the stable `@N`, a reorder/kill that shifts indices never spuriously triggers re-alignment.

2. **URL writeback (continuous)**. A separate effect watches the SSE-derived `activeWindow` and calls `navigate({ replace: true, to: "/$server/$window", params: { server, window: activeWindow.windowId } })` whenever `activeWindow.windowId !== windowParam`. The writeback writes the **window id only** — there is no `session` param to write. No debounce — tmux truth wins always. The effect skips when `dialogOpenRef.current === true` to avoid focus-stealing re-renders mid-dialog (this gates the URL writeback only, not the underlying SSE-derived selection). Comparing `windowId` (not `String(index)`) means an index shift from reorder produces no phantom writeback navigation to a different window. (`260529-chgz`, `260529-jad6`)

**Sidebar clicks navigate optimistically AND mutate.** `navigateToWindow(windowId)` in `app.tsx` runs a `runSwitch()` body that fires an optimistic `navigate({ to: "/$server/$window", params: { server, window: windowId }, search: {} })` *and then* calls `selectWindow(server, windowId)` to bring tmux into agreement. This navigate **pushes** — a user-initiated window switch IS a navigation, so every switch routed through `navigateToWindow` (sidebar click, window-switcher ▾, command palette, keyboard shortcut, spawn-agent same-server branch) creates a distinct browser-history entry the top-bar ◀ ▶ arrows can retrace (see § Chrome → Browser-history arrows). **No dedup on distinct hops** — `w1→w5→w1` pushes three entries and Back retraces every hop (explicit no-dedup requirement). One early-return guard is NOT a history dedup: `navigateToWindow` early-outs when the target is a **redundant no-op** — BOTH the URL window AND tmux's active window (`isRedundantSwitch(windowId, windowParamRef.current, activeWindowRef.current?.windowId)` — a mobile-drawer dismissal tap, the palette's `"(current)"` entry). Such a click can change nothing (tmux emits no bytes, the event-driven SSE snapshot never re-confirms), so arming the pending-switch machinery would guarantee a spurious spinner mask at 300ms plus a false failure toast at 5s over the very terminal the user is on; the early-out keeps only the ergonomic drawer close. A genuine `w1→w5→w1` still pushes duplicates (each hop IS a real switch); only the degenerate self-click is inert. The two tmux/preference-driven navigates that MUST stay `replace: true` are the SSE URL-writeback effect (contract 2 above — tmux corrections aren't user intent, pushing would fight the Back button) and `switchView` (§ Window-View Lens — per-viewer lens toggles must not step through Back); `runSwitch` alone pushes. The optimistic navigate is what makes the terminal render immediately (including the first click from the `SessionTiles` density view and cross-session clicks the SSE writeback alone can't express); `pendingClickRef` suppresses the writeback's bounce-back until the SSE snapshot confirms the switch (typically sub-500ms with the tmuxctl control-mode subscription; see `architecture.md` § tmux Control-Mode Subscription). **SSE confirm is the PRIMARY, event-driven clear** — a single per-switch `CONFIRMATION_WINDOW_MS` (5s) failure-bounce timer BACKSTOPS it: on an explicit `selectWindow` POST rejection OR no confirmation within the window, the intent is cleared, the URL/heading bounce to tmux truth, and a failure toast shows (§ Window-Switch Slide Transition → Confirmation-gated motion). This timer fires ONLY on non-confirmation to un-stick a silent-failure limbo — never to prefer the click over an external event. Server-side overrides during a click (another agent or hook calling `tmux select-window` concurrently) resolve to whichever mutation tmux processed last — the confirmation timer does not contest a genuine external switch (SSE confirming ANY active window clears the intent). On mobile, `runSwitch` additionally closes the overlay sidebar after a destination tap. **Timing caveat:** on an *instant/fallback* switch — no View Transitions support, reduced motion, no outgoing window, or an unresolvable direction — `runSwitch` executes synchronously at click time. On an *animated* same-server switch the body runs INSIDE `document.startViewTransition`'s async callback, i.e. **≥1 frame after the click** (the browser first captures the outgoing snapshot, then invokes the callback); "navigate at click time" is precise only on the instant path; see § Window-Switch Slide Transition. (`260703-l4nf`, `260715-m4xy`, `260715-38kg`)

**Keyboard switch path via the command palette.** The window-switch path is also surfaced as `Window: Switch to <session> › <name>` palette entries — the `windowSwitchActions` useMemo in `app.tsx`, one entry per window across **every** session (built from `flatWindows`, the current server's merged sessions — not cross-server). Each entry reuses `navigateToWindow(fw.window.windowId)`, so it inherits the same `selectWindow` + URL-writeback + mobile-close + `pendingClickRef` suppression as a sidebar click — no separate `selectWindow` plumbing. The URL-active window (`fw.window.windowId === windowParam`) gets a `" (current)"` suffix, mirroring `Server: Switch to <name> (current)`. The `›` separator is U+203A (single right-pointing angle quote) for `<session> › <name>` palette labels. The block composes last in `paletteActions` (see § Boards Command Palette). (`260613-o20f`)

**Click-intent and URL-match key on `{server, @N}` — never the session name.** `pendingClickRef` is a `PendingSwitchTarget = { server: string; windowId: string }` (`src/lib/window-transition.ts`). Every consumer compares BOTH fields via the pure exported `isSamePendingTarget(pending, server, windowId)`: the writeback's `urlMatchesPending` is `isSamePendingTarget(pending, server, windowParam)` and `sseConfirmed` is `isSamePendingTarget(pending, server, activeWindow.windowId)`; the mount-alignment skip and `bouncePendingSwitch`'s guards use it too. See § Design Decisions → Pending-switch identity is `{server, @N}` for why the key excludes the session name and includes the server. (`260529-jad6`, `260715-38kg`)

**Sidebar selection follows the URL window ID, falling back to `isActiveWindow`.** `WindowRow.isSelected` derives from `currentSessionName === session.name && (hasUrlWindow ? currentWindowId === win.windowId : (!ghost && win.isActiveWindow))` in `app/frontend/src/components/sidebar/index.tsx`, where `hasUrlWindow = currentWindowId != null` and `currentWindowId` is the URL's `@N` window ID. When the route carries a window ID (the normal terminal-route case), selection is the stable `currentWindowId === win.windowId` comparison. The `isActiveWindow` term (set by the backend `WindowInfo.IsActiveWindow` from tmux's `#{window_active}`) drives selection only when no URL window is present — e.g., the dashboard route, or before the first navigation resolves. Because the URL window ID is written back from `isActiveWindow` (see contract 2 above), the two stay in lockstep with `tmux select-window` (external switches, `rk riff` window creation) without an index↔ID seam. Ghost windows (mid-creation, before the SSE snapshot includes them) match only via the `!ghost && win.isActiveWindow` fallback path when no URL window applies.

### Multi-Client Convergence

All clients viewing the same server's window route converge on the same window — the per-server invariant. When the SSE `event: sessions` payload changes which window is `isActiveWindow` for the currently-viewed session, every browser tab on `?server=<same>` and viewing `/$server/$window` navigates to the new window's `windowId` via `navigate({ replace: true })` (the URL writeback effect above).

Clients viewing `/board/$name` SHALL NOT navigate — the board route does not subscribe its URL to `isActiveWindow`. The SSE payload still arrives and `BoardPane` rendering still receives the updated `isActiveWindow` data; the route stays put.

Clients viewing different `?server=` values are independent — each server's SSE stream drives its own clients. Cross-server tabs never yank each other.

**Stale-URL tab yanks other tabs on mount**: when a new tab opens at `/$server/@7` while existing tabs are on `/$server/@2` (tmux active is window `@2`), the new tab's mount-time alignment fires `selectWindow(server, "@7")`. The resulting SSE snapshot then yanks every existing tab on that server to window `@7` via the writeback effect. This is intentional ("yanking is OK" per the change intake) — the new tab represents user intent to view that window, and existing tabs collectively reflect "what tmux is now doing." A URL whose `@N` no longer exists on the server simply fails to resolve a current window (no nearest-index arithmetic) — see § Kill-redirect.

## Session Tiles

The component is `app/frontend/src/components/session-tiles/session-tiles.tsx` (`SessionTiles`). The literal string "Dashboard" does not appear in the chrome — the no-window top-bar leaf is the server-name `aria-current` leaf (§ Chrome (Top Bar)).

`SessionTiles` is the **multi-agent monitoring-DENSITY view** — "what is every agent doing right now" at a glance, the differentiator vs single-session tools. It renders in the terminal area when no `$window` param is present: the `windowParam ? <TerminalClient/> : <SessionTiles/>` branch in `app.tsx` (`windowParam` is the `@N` window ID; `sessionName` is derived from the active window's SSE snapshot, not a URL segment). The empty `serverIndexRoute` (`router.tsx`) is componentless — rendering is done conditionally by `AppShell` based on whether `$window` exists (no new route — Constitution IV). (`260701-70a0`)

**The load-bearing constraint** — window previews are **static text**, NOT a live xterm/relay per tile. N live relays for N tiles would re-trigger the documented HTTP/1.1 6-per-origin connection-pool starvation (see the board-route entries in this domain). A tile upgrades to a real live terminal ONLY on click (navigate to `/$server/$window`, where the single relay opens). The preview mechanism is a periodic `tmux capture-pane` snapshot delivered over the existing SSE hub as `event: preview` — see `architecture.md` § Session-Tile Previews (`event: preview` + preview-scope) for the backend.

**Layout**: Outer wrapper `flex-1 min-h-0 flex flex-col`. The tmux Server page's page identity is the `tmux Server: <server>` heading in the top-bar center cell (§ Chrome (Top Bar)). A single scrollable tile area (`flex-1 min-h-0 overflow-y-auto ...`) whose first child is a page-level `<SectionHeading label="tmux Server Overview" />`; the grid below is preceded by a `<SectionHeading label="Sessions" side={"{N} sessions, {M} windows"} />` — the server-view stats ride the `side` slot, right-aligned after the rule (`[ SESSIONS▊ ]──── {N} sessions, {M} windows`; § Section Heading, § Page Overview Headings). The `<h2>` + TypedLabel typed-sweep live INSIDE the brackets. Only the tile area scrolls. (`260704-pr0p`, `260715-zs1y`)

**Session tiles grid**: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3`. One tile per session (`bg-bg-card border border-border rounded`; `data-testid="session-tile-{name}"`), plus an always-visible dashed **New Session** tile at the end (calls `onCreateSession` → instant session creation, no dialog). A ghost/optimistic session tile gets `opacity-50 animate-pulse`.

**Session tile header**: A `<button>` toggling inline expansion (`aria-expanded`, `aria-label` "Expand/Collapse {name}"). Shows session name, window count, and activity summary (`{N} active, {M} idle`, derived from each window's `activity`). Chevron `▶`/`▼`. Multiple sessions may be expanded simultaneously. Expand state is local `useState<Record<string, boolean>>` keyed by session name.

**Window tiles** (inside an expanded session): each is a `<button>` (`rounded border border-border bg-bg-primary hover:border-text-secondary`; `data-testid="window-tile-{session}-{index}"`) that on click calls `onNavigate(win.windowId)` — the same `navigateToWindow` path a sidebar click uses (optimistic navigate + `selectWindow`; see § URL as Resumable Bookmark) — routing to the live-terminal route. NO preview relay is ever opened. A ghost window tile is `disabled` + pulsing. Each tile shows:
- A leading unified `<StatusDot win={win} />` (the SAME component as the sidebar row / pane panel / — see § Status Dot) + window name; a fab-stage badge (`bg-accent/10 text-accent`) when `win.fabStage` is set; and, for a window with an owned PR, a trailing rest-state PR glyph on the same header line (`data-testid="tile-pr-glyph"`, `aria-hidden`, never on ghost windows — `GitPullRequestIcon`, or the ✕ `GitPullRequestClosedIcon` when `prState === "closed"`) — same `prOwnsGlyph` gate + six-way `prGlyphColor` as the sidebar row's glyph (§ Rest-state PR glyph) (`aqo6`, `xuej`)
- The **pane text preview** as a static monospace `<pre>` (`h-24 overflow-hidden whitespace-pre text-[10px] font-mono bg-bg-inset`, `data-testid="window-tile-preview-{windowId}"`) — the `capture-pane` snapshot for `win.windowId`, read from the context `previewsByServer.get(server)` map. Empty (`""`) until the first `event: preview` arrives for the expanded set — the view degrades gracefully to an empty preview area before the first snapshot. Never an xterm instance.
- Fab change ID + slug when present (`parseFabChange`)

**New Window button**: dashed-border button inside each expanded session (`onCreateWindow(session.name)`).

**Preview subscription lifecycle** (bounded to expansions): `SessionTiles` consumes `previewsByServer` and `setPreviewScope` from `useSessionContext()`. A `useEffect` keyed on the sorted+joined expanded-session-name key POSTs the current expanded set via `setPreviewScope(server, expandedNames)` whenever it changes (expand/collapse) and on server change. It opens NO live relay per tile and does NOT `setInterval`+fetch — previews arrive on the single existing per-server SSE stream. On unmount / server change the declared scope lapses naturally (the connection drop clears the server-side per-connection set — see `architecture.md`).

**Touch targets**: session tile headers and window tiles use `coarse:min-h-[44px]`.

## PR Status

Live PR status is exposed two ways: a **full one-line detail** (the Pane panel `PR` L3 register) and a **single status dot** (the unified `StatusDot`, see § Status Dot below). The detail surface is:

1. **Pane panel `PR` (L3) register** (the per-*selected*-window detail view; see § Pane Panel / `status-panel.tsx`). This is the **only** full-line PR surface. The `SessionTiles` window tile carries the `StatusDot` plus the rest-state PR glyph (§ Rest-state PR glyph); there is no `PrStatusLine` component, though its module `pr-status-model.ts` remains as the single source of the shared PR color vocabulary + the StatusDot's exported model (§ Shared PR vocabulary). (`260701-70a0`, `260715-jykd`, `aqo6`)

The *dot* (a single colored dot, not the full line) reaches the remaining surfaces: the sidebar window-tree row and the `SessionTiles` window tile. The dense tree shows a single `StatusDot`, never the multi-part line. That single dot is the unified `StatusDot` driven by the compositional **two-family ladder** (cool fab blue-building → green-PR-ready, warm ad-hoc agent yellow, gray floor; core hue = phase, shape = status, additive yellow halo = `waiting`), reused on all its surfaces (sidebar row, SessionTiles window tile, pane-panel header). The dot tells the **local** story only — the branch's PR (the remote story) rides the separate rest-state glyph beside it. See § Status Dot. (`260610-obky`, `aqo6`)

### `PR` (L3) register — colored segments + open-first (`status-panel.tsx`)

> `WindowContent` renders the pyramid's **four orthogonal signal registers** (spec § Row Minimalism). See § Pane panel four-register view for the full L0/L1/L2/L3 model; this subsection covers the L3 `PR` register. (`260706-y1ar`)

The **`PR` (L3) register** shares the `tmx/cwd/git/fab` icon + value idiom but with an **open-first** interaction — it is the exception to the panel's "click row = copy" default. `getPrSegments(win)` returns `null` unless `win.prNumber` — the gate is **ungated from `fabChange`**: the L3 register shows the PR for ANY pane on a branch with a derived PR (universal derivation, Principle X; the dot renders no PR state — the row/tile glyph is the PR channel, see § Status Dot). Otherwise it returns an array of colored `{text, color}` segments — `#<n>` (`text-text-primary`), `<state>[ (draft)]` (via `PR_STATE_COLORS`), and, for an `open` PR only, `checks <prChecks>` (`PR_CHECKS_COLORS`) and `review: <prReview→spaces>` (`PR_REVIEW_COLORS`). `state` can be `open`/`merged`/`closed` — the collector queries `states: [OPEN, MERGED, CLOSED]`, so a recently-merged PR shows `merged` (checks/review suppressed for non-`open`), see `architecture.md` § `internal/prstatus`. The three color maps are imported from `pr-status-model.ts` (single source shared with the sidebar dot — the shared vocabulary home, § Shared PR vocabulary), so segment coloring follows GitHub state per-segment (no whole-value red swap) — the color story lives in § PR Status (draft-follows-health) above. The PR icon (``) is the Nerd Font git-pull-request glyph, matching the other rows' accent icons.

**Open-first split** — the row body's interaction is open-first (`↗`), not copy. The segment JSX is single-sourced via a shared `segmentSpans` const in `WindowContent` (consumed by both branches below, so the two can't drift), and the row branches on `win.prUrl`: (`260703-41ks`)

- **URL present** → the row body is a file-private `PrLinkRow` — a real `<a href={win.prUrl} target="_blank" rel="noopener noreferrer" title={win.prUrl} aria-label="Open PR #<n> in a new tab">` (NOT a `<button>` + `window.open`, so native middle-click / Ctrl-Cmd+click / right-click → "Copy link address" all work; mirrored the now-removed `PrStatusLine`'s anchor — the same anchor-not-button PR-link idiom). The anchor is a flex row: a `shrink-0` prefix span + PR icon span, then a `min-w-0 truncate` segment-text child, then an **always-visible** (not hover-gated) `shrink-0` inline `↗` sibling immediately after it — so the `↗` hugs the (possibly truncated) text end, is never eaten by truncation, and never floats to the far right of a wide row. The `↗` is the affordance that signals this row **opens** rather than copies; the existing row hover treatment (`hover:bg-bg-inset`, segment `group-hover:text-accent`) is kept as the row-level cue. Because the anchor is a flex container (whitespace-only `{" "}` text nodes between flex items are dropped and a trailing collapsible space trimmed), the inter-column gaps live as NBSPs INSIDE the prefix/icon spans — the at-rest prefix is `"pr"`+2×NBSP (4 monospace advances) matching `CopyableRow`'s `${prefix} ` (3 + gap = 4), and the `copied ✓`+NBSP feedback is 9 advances matching `CopyableRow`'s `copied ✓ ` — keeping the icon/content column-aligned with `tmx/cwd/git/fab` and the no-URL branch. **Copy is role-swapped, not dropped**: a sibling hover-revealed copy `<button aria-label="Copy PR URL">` (the `` clipboard glyph) sits in an absolutely-positioned right-side container reusing the **sidebar window-row icon-cluster gating** — the container is `pointer-events-none` at rest with `group-hover/pr:pointer-events-auto coarse:pointer-events-auto has-[:focus-visible]:pointer-events-auto`, and the button is `opacity-0 group-hover/pr:opacity-100 coarse:opacity-100 focus-visible:opacity-100` (revealed on hover / coarse pointers / keyboard focus; the anchor reserves `pr-6` so the icon doesn't overlap the inline `↗`/text). Its `onClick` does `e.preventDefault(); e.stopPropagation(); handleCopy("pr", win.prUrl)` — the button is a SIBLING of the anchor (not enclosed, so `<a>`-in-`<a>`/`<button>`-in-`<a>` nesting is avoided; `preventDefault` is belt-and-suspenders). Its color follows the window-row cluster precedent (`text-text-secondary hover:text-text-primary`), NOT `ICON_CLASS` (whose `text-accent-bright` would fight at equal specificity). The `copied ✓` feedback is unchanged — the copy still keeps the `copiedRow === "pr"` + `COPY_FEEDBACK_MS` mechanism, and since the row body is no longer a `CopyableRow`, `PrLinkRow` replicates the prefix swap (`pr` → `copied ✓`) inline. Both the anchor and the copy button are natively keyboard-focusable (Constitution V).
- **URL absent** (`prNumber` set, `prUrl` missing) → nothing to open, so the row stays a plain `CopyableRow` (prefix `"pr "`) copying the segment text (`prText`) on click — the row body IS the copy action, no anchor, no inline `↗`, no hover copy icon (unchanged from before).

`CopyableRow` has no `className` prop; its `title` prop is live (the `cwd` row uses it).

**Draft state-color follows health, not readiness.** `getPrSegments(win)` (the colored-segment variant used in the panel) does NOT force a draft PR's state-color to `text-text-secondary` — a draft's state segment follows `PR_STATE_COLORS[win.prState]` like any other (open → `text-accent-green`/green), under the "green = HEALTH, not merge-readiness" semantic. Checks/review are suppressed for `!open` (merged/closed) PRs, and `merged` is `text-signal-purple`. The color story is one system across the rest-state glyph and these status-panel segments (green = health, red = fail, yellow = checks running, purple = merged) driven by the shared `isFailish` predicate. The sidebar `StatusDot` renders no PR state at all — it keys off the lifecycle `PHASE_HUE`/shape model (§ Status Dot); the row's PR color channel is the glyph (`prGlyphColor`). (`260615-2olv`, `aqo6`)

The rest-state PR glyph (window row + session tile) is the **one** surface that colors draft differently — gray, below fail, open-gated (§ Rest-state PR glyph). Draft-follows-health governs these segments; the glyph carries the orthogonal *whose turn is it* read, and the `" (draft)"` state-segment suffix here stays the accessible signal either way. (`e30p`)

### Pane panel four-register view (`status-panel.tsx` `WindowContent`)

The pane panel's `WindowContent` renders the pyramid's **register view** — one line per signal layer, **orthogonal, never collapsed**, so the sidebar `StatusDot` is a pure function of what the panel shows and can be **mentally derived** from it. The four registers each render as absent when their layer is absent (a plain shell pane shows only `out`). All prefixes are the **fixed-width 3-char vocabulary** `out`/`agt`/`fab` (+ `tmx`/`cwd`/`git` identity rows), each followed by a single-space advance; only `PR` is 2-char + NBSP-padded to the same 4-advance column. (`260706-4h26`)

```
out  active · <command>   /   <command> — idle Xm since last output   (L0: activity + activityTimestamp)
agt  waiting 3m           /   active   /   idle 12m                    (L1: agentState + epoch duration)
fab  <id> <slug> · <stage>[ · <displayState>]                         (L2: fabChange · fabStage · fabDisplayState)
PR   #<n> · <state>[ · checks <c> · review: <r>]                      (L3: prNumber/state/checks/review, ungated)
```

- **L0 `out`** (`getOutputLine`) — ALWAYS rendered (the floor layer's precondition is "always"). Speaks about bytes, not intent. Its elapsed is **never muted** here — the register view is uncontested for space, so the **waiting-pierce rule is automatic** (the duration-mute rule that hides elapsed when output flows applies only to the tip's one-line summary, spec § Duration-Text Ladder). Icon: `BrailleSnake`.
- **L1 `agt`** (`getAgentLine`) — `null` when no `agentState`, else `${agentState} ${agentIdleDuration}` (or bare state when no duration). The rk-computed `agentIdleDuration` is present for `waiting`/`idle`, empty for `active`. Icon: `StarTwinkle`. This is where a waiting duration is always visible regardless of flowing output (the pierce rule).
- **L2 `fab`** (`fabLine`) — `<id> <slug> · <stage>[ · <displayState>]`; the `displayState` segment is appended when present (`fab pane map` may omit it on older binaries). Rendered as a `CopyableRow` (copies the change id). Icon: `ClockSpinner`.
- **L3 `PR`** (`getPrSegments`) — see § `PR` (L3) register above. **Ungated from `fabChange`** — shows for ANY pane with a derived `prNumber` (universal derivation, Principle X). The open-first `PrLinkRow` / `CopyableRow` branch on `win.prUrl` folds in here.

The identity rows (`tmx`/`cwd`/`git`) are pane metadata, orthogonal to these four signal registers, and render above them. The per-second `useNow()` tick lives in this leaf (`WindowContent`) — the bottom panel is a single instance, so its tick is negligible and never touches the memoized sidebar tree (§ Render Performance).

**The register view has TWO surfaces**: this PANE panel and the sidebar row-hover flyout card (§ Row-hover register flyout card). Both resolve their lines from the shared `sidebar/registers.ts` module (§ Register resolvers), so they cannot drift. The panel keeps the icon column (`BrailleSnake`/`StarTwinkle`/`ClockSpinner`), the identity rows, and the copy/open-first row interactions; the card is read-only glance content with only its PR + docs links interactive. (`93dy`)

### Register resolvers — `sidebar/registers.ts`

The four register lines are resolved by pure functions over the streamed `WindowInfo` in `sidebar/registers.ts` — **one source for both register surfaces** (the PANE panel's `WindowContent` and the row flyout card), no React, no duplication:

- `getOutputLine(win, nowSeconds)` → the L0 `out` string. Takes `nowSeconds` as a parameter rather than reading a clock, so each surface supplies its own leaf-scoped `useNow()` (§ Render Performance).
- `getAgentLine(win)` → the L1 `agt` string; `null` when no `agentState`.
- `getFabLine(win)` → the L2 `fab` string `<id> <slug> · <stage>[ · <displayState>]`; `null` without a parseable `fabChange` or without a stage.
- `getPrSegments(win)` → the L3 `PR` colored segments (+ the `PrSegment` type); `null` without `prNumber`. See § `PR` (L3) register for the segment vocabulary and gating.

Consumers own their own PRESENTATION — prefixes, icons, truncation, copy/open interactions — and share only the strings and segments. Covered by `registers.test.ts`; `status-panel.test.tsx` asserts the panel's output is unchanged by the extraction. (`93dy`)

### Shared PR vocabulary — `pr-status-model.ts`

The module `pr-status-model.ts` carries no `PrStatusLine` component — it is the single source of the shared PR **color vocabulary** AND the lifecycle **status-dot model** (below), imported by `status-dot.tsx`, `status-dot-label.ts`, `sidebar/registers.ts` (the shared register resolvers), `sidebar/window-row.tsx` and `session-tiles/session-tiles.tsx` (the rest-state PR glyph's gate + color on both surfaces), and `sidebar/row-flyout-card.tsx`, plus its own unit test (`pr-status-model.test.ts`). The PR text detail lives in the register resolvers, rendered by both the PANE panel's L3 `PR` register and the row flyout card (`getPrSegments`, § `PR` (L3) register). (`260715-jykd`, `93dy`)

**Module surface** (colocated in `pr-status-model.ts` — all `export`ed except the file-private `hasFreshAgent`, an internal helper to `statusDotState`):

- **PR color vocabulary**: `PR_STATE_COLORS`/`PR_CHECKS_COLORS`/`PR_REVIEW_COLORS` (the per-segment GitHub-state color maps, consumed by the L3 register resolver in `sidebar/registers.ts`) and `isFailish(win)` (`prChecks === "fail" || prReview === "changes_requested"` — the single fail predicate).
- **Row PR-glyph helpers**: `prOwnsGlyph(win)` (`!!prNumber && (prState === "open" || prState === "merged" || prState === "closed")` — a positive allowlist: an unknown/absent `prState`, the branch channel's unconfident `""` fallback, never owns) is **exported** — the gate for the rest-state PR glyph on the sidebar window row AND the session-tile window row (§ Window rows, § Session Tiles); it gates only the glyph, never any dot tier. `prGlyphColor(win)` returns the glyph's color token from the same vocabulary as a **six-way** first-match-wins chain: `text-text-secondary` for **closed** (a dead PR reads muted — its stale checks/review are historical noise, so closed sits above fail, mirroring merged-above-fail in `prDotState`), then `text-signal-red` when `prDotState(win) === "fail"`, then `text-text-secondary` for an **open draft** (`win.prState === "open" && win.prIsDraft`), then **`text-signal-yellow` for open with `prChecks === "pending"` (checks running)**, then `text-accent-green` for open otherwise (checks pass or no decisive signal), `text-signal-purple` for merged. **Fail sits above draft**: a draft with failing checks or changes requested reads red, the same `isFailish`-dominates rule `prDotState` encodes by ordering `fail` ahead of `healthy`. **Draft sits above pending**: drafts stay muted even while their checks run — pending would un-mute them. The draft branch is **gated on `prState === "open"`**, which makes the merged→purple path unreachable from it *by construction* rather than incidentally (a closed draft reads closed, since the closed branch matches first). Closed and open-draft share the inert gray token — the ✕ icon shape is what separates a closed PR from a draft (§ Rest-state PR glyph). No new color system, no new hex: `text-text-secondary` is the established inert/no-journey token (`PHASE_HUE.none`), and the pending yellow reuses `PR_CHECKS_COLORS.pending`'s token choice. **Draft, checks-pending, merged, and closed are glyph-only distinctions** — the dot renders no PR state at all (the local/remote split, § Status Dot). (`93dy`, `e30p`, `aqo6`, `xuej`)
- **Status-dot model**: `PrDotState` (the `"merged" | "fail" | "pending" | "healthy" | "neutral"` union) and `prDotState(win)` (the first-match-wins precedence function, reusing `isFailish` for its `fail` branch — the single source of the fail definition, shared with the Pane panel). The compositional dot model (`aqo6`): `DotShape` (`"ring" | "solid" | "failed"`), `DotPhase` (`"building" | "prReady" | "agent" | "none"`), `StatusDotState = { phase: DotPhase; shape: DotShape; waiting?: boolean }`, `fabPhase(stage)` (the stage-based two-stop split), `fabShape(displayState)`, `hasFreshAgent(win)` (file-private), `PHASE_HUE` (phase→hue token map), and the two-family `statusDotState(win)` — no PR branch (see § Status Dot for the ladder + the local/remote split).

The `StatusDot` component (`status-dot.tsx`, see § Status Dot) imports `statusDotState`/`PHASE_HUE`; `status-dot-label.ts` imports `DotPhase`/`DotShape`/`StatusDotState`; `dotLabel` lives in its own `status-dot-label.ts` module so both the dot and the row flyout card (which reuses the label as its header) import it without an import cycle (`260706-y1ar`). There are no `PR_DOT_COLOR`/`PR_DOT_LABEL` maps (`260615-0hsz`). `prDotState`/`PrDotState`/`isFailish` remain live — the L3 register segments use them and `prGlyphColor`'s `fail` branch reuses `prDotState`; the non-`fail` `PrDotState` states have no UI consumer and are exercised only by direct-call unit tests (a follow-up could collapse that branch to `isFailish` and retire `prDotState` — deletion candidate, `260810-aqo6`).

### Status Dot (`status-dot.tsx`) — compositional two-family ladder + waiting halo

> Spec: `docs/site/status-dot.md` (+ `docs/img/status-dot-reference.svg`) is the canonical reference — four small legend strips (hue / shape / glyph / halo), one per channel, replace precedence prose + a per-cell matrix. Design authority: `docs/specs/status-pyramid.md`.

> Detail surface: the dot is a **pure decoration** — no native HTML `title`, no `tabIndex`, no per-dot hover-card of its own. The hover/focus detail surface is the sidebar row's flyout card (§ Row-hover register flyout card). (`93dy`)

`StatusDot({ win }: { win: WindowInfo })` (`app/frontend/src/components/status-dot.tsx`) is the **one** dot reused on all three surfaces — the sidebar window row (leading position, the row's primary status signal beside the owned-PR rest glyph — see § Window rows / Row Minimalism), the `SessionTiles` window tile, and the pane-panel header. It is a **first-class consumer of `agentState`**, using **TWO orthogonal visual channels PLUS an additive attention overlay**: **core hue = phase** (which journey + position in it), **shape = status** (health — the SAME meaning in every hue, fully compositional), and **`waiting` → a ladder-exempt additive yellow halo** (see § Waiting halo below). (`260615-0hsz`, `260706-y1ar`, `aqo6`)

**The local/remote split** — the ownership rule the vocabulary rests on: the **dot tells the LOCAL story** (what runs in this pane: which journey, is it healthy, does it need me); the **glyph tells the REMOTE story** (the branch's PR on GitHub — § Rest-state PR glyph). The dot renders no PR state in any family — no purple, no orange, no PR shapes; `statusDotState` consults no PR field. A window's PR still shows everywhere derivation reaches — the rest-state glyph on the row and the session tile, the PANE panel's L3 register, and the flyout card's "Open PR" link (Principle X: derivation is universal) — just never on the dot. (`aqo6`)

**Ladder — two families joined at the top** (`statusDotState(win)` in `pr-status-model.ts`). First precondition wins for the core hue + shape; `waiting = win.agentState === "waiting"` is computed independently and attached as `state.waiting` on every branch:

- **`fabChange` present (and `fabDisplayState !== "skipped"`) → cool family (fab pipeline):** `{ phase: fabPhase(win.fabStage), shape: fabShape(win.fabDisplayState) }` — blue `building` for intake·apply·review, green `prReady` for everything else.
- **no fab ownership but a fresh `agentState` → warm family (ad-hoc agent):** `{ phase: "agent", shape: agentState === "idle" ? "ring" : "solid" }` (yellow — a `waiting`/`active` agent is mid-turn → solid, only a resting `idle` agent is a ring).
- **neither → the gray L0 floor:** `{ phase: "none", shape: win.activity === "active" ? "solid" : "ring" }`.

A **`skipped`** `fabDisplayState` makes the window NOT fab-owned — the change has left its journey, so the ladder falls through (fresh agent → yellow, else the gray floor) rather than rendering any fab hue (§ Design Decisions → Skipped display-state falls through the whole ladder).

The result type is `StatusDotState = { phase: DotPhase; shape: DotShape; waiting?: boolean }`. `hasFreshAgent(win)` is `agentState ∈ {active, waiting, idle}` — #314 already clears stale/shell-reconciled values server-side (the reconciler + rollup omit a plain-shell pane), so a non-empty rolled-up `agentState` on the window IS fresh; no client-side staleness heuristic (R4).

**Waiting halo — additive attention overlay, ladder-exempt** (R2): when `state.waiting`, `StatusDot` appends the `rk-waiting-halo` class to the dot element — a **pulsing yellow box-shadow ring** layered around it (the `rk-waiting-halo` keyframe in `globals.css`, driven by `var(--color-signal-yellow)` — the semantic is constant (yellow = "needs you now" in every theme) while the value re-themes: dark `#facc15`, light `#b07d02` gold, § Design Decisions → Signal hues are per-theme tokens). It is **ADDITIVE** — the core hue AND shape are untouched (a blue building dot keeps its blue core; a failed dot keeps its failed shape). NEVER a hue-flip — family identity must survive attention. The class rides the dot element itself (box-shadow renders outside the border-box, disturbing neither size nor hue). Under `prefers-reduced-motion` the halo is a **static yellow ring** (no pulse). The glance rule: **blue = still cooking, green = out the door, yellow core = my ad-hoc agents, gray = just a terminal, yellow HALO = needs me now.**

**Hue = phase** — four hues (spec § The Channel Model). `fabPhase(stage)` is the **two-stop split**: `intake`/`apply`/`review` → `building` (local work in progress); everything else — `ship`/`review-pr`/`done`, plus unknown/absent (`hydrate` lands here via the default arm) → `prReady` (local work complete, "PR is ready"). The split is **stage-based, never `prNumber`-based** — the dot must not consult PR fields; alignment with PR existence stays emergent, since `/git-pr` creates the PR mid-ship. `DotPhase = "building" | "prReady" | "agent" | "none"`. `PHASE_HUE: Record<DotPhase, string>` maps phase→core-hue token:

| Phase (`DotPhase`) | Family / source | Hue token |
|--------------------|-------------------|-----------|
| `building` | cool fab — intake · apply · review | `text-signal-blue` |
| `prReady` | cool fab — ship · review-pr · done (+ unknown/absent stage) | `text-accent-green` |
| `agent` | warm ad-hoc agent | `text-signal-yellow` |
| `none` | gray L0 floor (no journey) | `text-text-secondary` |

> **Why exactly two fab hues**: blue vs green answers "still cooking vs out the door" at a glance — the one pipeline distinction worth a hue on a 7px dot (the exact stage lives in the `fab` register on both register surfaces). Purple and orange are not dot hues — purple stays in the glyph/segment vocabularies. The maximal one-fab-hue reduction was rejected: it loses the building/PR-ready glance. (`aqo6`)

**Shape = status** — ONE vocabulary, three shapes, the same meaning in every hue, rendered by `StatusDot` from `{phase, shape}` with `color = PHASE_HUE[state.phase]` always (no per-shape color forcing); the halo is layered on top when `state.waiting`:

| Shape (`DotShape`) | Meaning | fab `fabDisplayState` | Rendering |
|--------------------|---------|------------------------|-----------|
| `solid` | running / live | `active` / `ready` / unknown (+ mid-turn agent, flowing floor) | filled circle (`backgroundColor: currentColor`, `border: none`) in phase hue |
| `ring` | at rest | `pending`, **`done` (parked)** (+ idle agent, quiet shell) | hollow circle, `1.8px solid currentColor` border in phase hue, transparent fill |
| `failed` | needs rescue | `failed` | **dotted ring** (`1.2px dotted currentColor`, transparent) in phase hue + a centered small **red** `bg-signal-red` dot (`w-[3px] h-[3px]`) |

Every shape EXCEPT `failed` renders at one uniform 7px footprint (`DOT_SIZE = "w-[7px] h-[7px]"`). The **`failed` dot is the lone size exception** — a slightly larger 9px footprint (`w-[9px] h-[9px]`): a CSS `dashed` border can't control its dash count and at 7px fits only ~4 dashes that read as flower petals, so it uses a `1.2px dotted` border at 9px, which renders as a delicate bead ring, with the `3px` red center sitting inside the 9px ring's ~6.6px hole.

**Shape mapping** — `fabShape(displayState)`: `pending`→`ring`; `failed`→`failed`; **`done`→`ring`** (a parked-done change is RESTING — journey complete; its green hue says "done", the merged glyph says how it ended); `active`/`ready`/unknown/absent→`solid` (a live fab window with a future/unrecognized state still reads as a live dot, not gone). `skipped` never reaches `fabShape` — the ladder fall-through handles it.

**Red — the honest split**: DOT-red appears in exactly one way — the small center dot of a `failed` dotted ring (a failure reads as a dotted ring **in its phase hue** with only the red center; no whole-dot red tint). The row GLYPH separately uses red for a failing PR — that is the glyph channel's vocabulary, not the dot's.

**Gray L0 floor** (`phase: "none"`, lowest precedence, no fab ownership + no fresh agent) is **monochrome gray** — color is reserved for the two journeys: `activity === "active"`→gray `solid` filled circle, else gray `ring`. No phase hue is ever applied here.

**Composed examples** (rows = phase/hue, cols = status/shape). The **waiting halo overlays ANY cell** — it is an overlay applicable to every tier, not a matrix row:

| Phase (hue) | ring (at rest) | solid (live) | failed |
|-------------|----------------|--------------|--------|
| building (blue) | blue ring (stage pending) | blue solid (intake/apply/review active·ready) | blue dotted-ring + red center (review failed) |
| PR-ready (green) | green ring (stage pending / **parked done**) | green solid (ship/review-pr active·ready) | green dotted-ring + red center (review-pr failed) |
| agent (yellow) | yellow ring (idle) | yellow solid (active/waiting mid-turn) | — |
| plain (gray floor) | gray ring (idle) | gray solid (active) | — |

**Accessibility** — every dot carries `role="img"` + `aria-label` composed from journey + status + **additive attention suffix**, never color alone (colorblind a11y + Constitution V). `role="img"`/`aria-label` are the accessible name and the dot's ONLY a11y surface: it carries **no `tabIndex`** — the row treeitem is the focus target, and focusing it opens the flyout card, which reuses the same `dotLabel` text as its header (§ Row-hover register flyout card). `dotLabel(win, state)` lives in its OWN module `status-dot-label.ts` (so both `status-dot.tsx` and the flyout card import it without an import cycle; `status-dot.tsx` re-exports it, keeping its public surface stable). It composes a **core label + additive `— agent waiting {dur}` suffix** (`coreLabel` + the `state.waiting` suffix): (`260706-y1ar`, `aqo6`)
- **fab tiers** (keyed on `phase ∈ {building, prReady}`, so a fab window whose `skipped` display-state fell through the ladder reads its floor/agent word — matching the dot it actually renders): the hue word (`FAB_PHASE_LABEL`: `building`→"building", `prReady`→"PR-ready") + a status word derived from `fabDisplayState` via `fabStatusWord` — NOT from the shape (`done` and `pending` both render the resting ring, but a parked-done change reads "parked", never "pending"): `pending`→"pending", `failed`→"failed", `done`→"parked", `ready`→"ready", `active`/unknown→"active" — e.g. `"building — active"`, `"PR-ready — parked"`, `"building — failed"`. NO PR words and no raw stage word — PR facts live in the glyph + register surfaces, the exact stage in the `fab` register.
- **warm agent tier** (`phase === "agent"`): agent-native words via `AGENT_SHAPE_LABEL` — the idle `ring` reads **"idle"** (NOT the fab-stage "pending") and `solid` reads "active" — e.g. `"agent — active"`, `"agent — idle"`. (`AGENT_SHAPE_LABEL.failed` is unreachable from the agent tier — retained only for `Record<DotShape, …>` totality, documented as such in the code comment.)
- **L0 floor**: the bare tmux activity word (`"active"`/`"idle"`).
- **Waiting suffix**: when `state.waiting`, append `" — agent waiting {dur}"` (duration from the rk-computed `agentIdleDuration`, present for `waiting`/`idle`) — so a review-failed waiting window reads `"building — failed — agent waiting 3m"` (R3).

**Impl symbols** — in `pr-status-model.ts` (colocated, see § PR Status exports): `DotShape` (`"ring" | "solid" | "failed"`), `DotPhase` (`"building" | "prReady" | "agent" | "none"`), `StatusDotState = {phase, shape, waiting?}`, `fabPhase`, `fabShape`, `hasFreshAgent` (file-private), `prOwnsGlyph`, `prGlyphColor`, `PHASE_HUE`, and the two-family `statusDotState`. In `status-dot-label.ts`: `dotLabel` + the file-private `coreLabel`/`fabStatusWord` + the `FAB_PHASE_LABEL`/`AGENT_SHAPE_LABEL` maps. In `status-dot.tsx`: `DOT_SIZE`, the three shape renderers returned directly by `StatusDot` (incl. the `rk-waiting-halo` class append), and a re-export of `dotLabel`; it imports `statusDotState`/`PHASE_HUE` from `pr-status-model.ts` and `dotLabel` from `status-dot-label.ts`. The component takes `{ win }` and returns one `<span>` per shape — no render-prop callback, no wrapper.

> **No PR maps on the dot.** The dot renders from `PHASE_HUE` + the label maps only. `prDotState`/`PrDotState`/`isFailish`/`PR_STATE_COLORS`/`PR_CHECKS_COLORS`/`PR_REVIEW_COLORS` serve the glyph color chain and the Pane-panel L3 `PR` register segments (see § Shared PR vocabulary); none of them is consulted by `statusDotState`.

**Why this shape** (design decisions): (1) two families let a glance separate "my fab pipeline" (cool blue→green) from "my ad-hoc agents" (warm yellow) from "just a terminal" (gray) — the user's own two-journey framing; the blue/green two-stop encodes the one pipeline distinction worth a hue (still cooking vs out the door). (2) shape meaning is uniform across every hue — fully compositional, so the doc needs four small legend strips instead of a per-cell matrix; a parked-done change is simply "green + at rest" (the resting ring), no dedicated square shape. (3) the additive yellow halo keeps family identity intact when attention is highest — a hue-flip (rejected) would destroy exactly the signal the user is scanning for; a self-colored halo (rejected) vanishes in the reduced-motion static form. (4) keeping PR off the dot avoids double-encoding with the row glyph (one fact, one channel — a merged PR reads purple once, on the glyph) and keeps new PR-adjacent states (draft, checks-pending) from pressuring the dot — see § Design Decisions → PR is evicted from the dot. (`260706-y1ar`, `aqo6`)

**Call sites** — the three surfaces are bare `<StatusDot win={win} />`: (1) sidebar `WindowRow` — ONE leading dot, wrapped in a `data-testid="status-dot-tap"` span that on COARSE pointers routes a tap to the row flyout card (§ Row-hover register flyout card) and is inert on fine pointers; (2) the `SessionTiles` window tile — leading dot before the window name (dot + name + fab-stage badge + rest-state PR glyph + text preview); (3) pane panel `WindowPanel` `headerRight` — `<StatusDot win={win} />` before `{win.name}`. Surfaces (2) and (3) carry the dot alone with no hover-card of their own — the PANE panel itself and the sidebar row flyout are the register-detail surfaces. The dot's inputs (`agentState`, `fabChange`/`fabStage`/`fabDisplayState`, tmux `activity`) all ride `WindowInfo` via SSE — no PR field among them; the halo keyframe is the only bespoke CSS (`globals.css` `rk-waiting-halo`). Tests: `status-dot.test.tsx` (the ladder across floor/agent/fab families × shapes — incl. the parked-done green ring, the skipped fall-through, and a merged-PR fab window rendering the fab arm's state with no purple/orange/square — plus the additive halo and `dotLabel` composition incl. the waiting suffix, and asserts the dot carries no native `title`); `pr-status-model.test.ts` covers the ladder, `prOwnsGlyph`, and `prGlyphColor` (the checks-pending yellow + draft-outranks-pending cases); the reduced-motion static-ring form is asserted via the `agent-next-waiting.spec.ts` e2e (jsdom can't evaluate `globals.css` media queries).

### Row-hover register flyout card (`sidebar/row-flyout-card.tsx`)

The sidebar window row's **tier-2 hover-card** and the app's one status-detail hover surface: a floating card built on `@floating-ui/react`, opening on **whole-ROW hover** at a **fixed x** (the sidebar's right edge). It is the single surface for all three triggers — fine-pointer row hover, keyboard row focus (the roving-tabindex treeitem, Constitution V), and coarse-pointer **dot-tap** — so status detail stays reachable by pointer, keyboard, and touch alike. The row is a large forgiving target where the 7px dot was a precise one, and the fixed x means the card never jitters under the pointer. (`93dy`)

**Wiring** — `useRowFlyout(win, { suppressed })` returns `{ setReference, referenceProps, card, openNow, close }`; `WindowRow` attaches `setReference` + spreads `referenceProps` on the **row root** (the floating reference is the ROW, not the mouse and not the dot) and renders `{flyout.card}` as its last child. `useFloating`: `placement: "right"`, middleware `offset(6)` / `flip()` / `shift({ padding: 8 })`, `whileElementsMounted: autoUpdate`, `FloatingPortal` to `document.body` (escaping the sidebar's `overflow:hidden` clip). Interactions: `useHover` (`mouseOnly: true` so touch never hover-opens, `move: false`, function-form `delay`, `handleClose: safePolygon()` — the bridge letting the pointer travel row → card to click a link), `useFocus`, `useDismiss` (Escape / outside press / blur). Deliberately **no** `useRole({ role: "tooltip" })` — the card holds real links (§ Design Decisions → No `role="tooltip"` on the status detail hover-card). Surface tokens are the shared ones (`bg-bg-primary border border-border rounded-md shadow-lg`, monospace, `w-max max-w-xs`) — no new color tokens.

**Delay + warm window + single-open** — `FLYOUT_OPEN_DELAY_MS = 350` cold; **0 while the cluster is warm**, where warm = another card is open right now OR one closed under `FLYOUT_WARM_WINDOW_MS = 500` ago. So sweeping the pointer down the tree retargets instantly instead of strobing. The cluster state is **module-scoped** (`activeFlyout` + `lastClosedAt`, plus a `resetFlyoutWarmState()` test seam), NOT React context — see § Design Decisions → Flyout warm window is module state, not a nested `FloatingDelayGroup`. `flyoutOpenDelay()` is handed to `useHover` in **function form**, evaluated at event time, so warmth set by another row is seen without a re-render. `activeFlyout` also enforces **single-open**: opening closes any other open card. `lastClosedAt` is stamped only when the closing flyout was the active one, so a no-op close (a drag-start before the delay elapsed, or a close racing another row's open) never falsely arms the warm window; an unmount-while-open releases the coordinator the same way.

**Suppression** — `suppressed` (the row passes `ghost || showPinPopover || showLabelPicker`) closes an open card and disables the hover/focus triggers, so the card never fights the row's other layers. The imperative `openNow()` honors the same gate with an early return. A drag start calls `flyout.close()` from the row's `onDragStart`.

**Focus management** — the card is wrapped in `FloatingFocusManager` with `modal={false}` and `order={["reference", "content"]}`, which is what makes `FloatingPortal` render its tab-order guards so **Tab walks row → card links** in tree order. Two non-default settings are load-bearing: `initialFocus={-1}` (the manager's default would move focus to `order[0]` on open — a hover-open must never steal focus from the terminal) and `returnFocus` gated on a `focusInsideCard` flag set by an `onFocusCapture` on the card (the default returns focus even when nothing inside was focused, which on a hover-sweep close yanks focus to the just-left row's button and breaks the warm retarget). The flag resets on each fresh open via an effect keyed on the real `open` state — not inside `onOpenChange`, because `useFocus` re-fires `onOpenChange(true)` for every `focusin` bubbling from the portalled card through the React tree, and a reset there would batch with the capture-phase set and negate it. Escape-dismiss and the tree's arrow-key nav are unaffected: focus stays on the row until the user tabs in.

**Content** — the full **four-register view** (`out` / `agt` / `fab` / `pr`) promoted from the PANE panel, resolved by the SHARED helpers in `sidebar/registers.ts` (§ Register resolvers), so the two surfaces cannot drift. Absent layers render as absent (a plain shell pane shows only `out`). Around them:

- **Header** — `dotLabel(win, state)`, the same text as the dot's `aria-label` (single source, imported from `status-dot-label.ts`), plus a top-right affordance cluster: the **fork link** (claude chats only — § Fork affordance below) then a quiet circled-(i) **docs link** (`STATUS_DOT_DOCS_URL` → the `docs/site/status-dot.md` GitHub blob; `docs/site` is not served locally, matching the `NOTIFICATIONS_HELP_URL` convention). Both `stopPropagation` on click.
- **Register lines** — `RegisterLine` renders a fixed-width prefix + read-only content, prefixes `"out "`/`"agt "`/`"fab "` and `"pr  "` (2-char key + 2 NBSPs = the same 4-advance column, written as escapes so a careless re-encoding can't mojibake them; codepoints pinned by a unit test). Each line carries `min-w-0 truncate`, matching the PANE panel rows they were promoted from, so long `fab`/`pr` text ellipsizes inside `max-w-xs` instead of painting outside the card. The `pr` line renders `getPrSegments` as `·`-joined colored segments. There is no separate `agent:` line — the `agt` register carries it.
- **Freshness line** — `checked Xs ago` via the `FreshnessLine` leaf (below).
- **PR link** — an `"Open PR #N ↗"` anchor whenever `prUrl` exists (`prNumber` is independently optional → `"Open PR ↗"` rather than `#undefined`), `target="_blank" rel="noopener noreferrer"`, `stopPropagation` so opening the PR never selects the underlying row. It is an anchor, not a button, so native middle/Ctrl-click work. **This is the PR click path** — the row's rest glyph is informational and swaps away under the pointer.

Registers are read-only text; the PR link, the docs icon and the fork button are the card's only interactive elements. The card carries no icon column and no copy interactions — those are PANE-panel idiom (§ Pane panel four-register view).

**Fork affordance (`ForkLink`)** — a `ForkIcon` inline-SVG button beside the ⓘ docs link that branches this window's agent conversation into a new window in the **same session and directory**. It is **double-gated**: `canForkWindow(win)` (an equality guard on `win.chatProvider === FORKABLE_CHAT_PROVIDER`, the module constant `"claude"` — the same `WindowInfo` field the chat lens gates on, so no new data plumbing; a `codex` window and a plain shell pane both fall through) **AND** a supplied optional `onFork` callback. A consumer wiring no handler — the board-route sidebar — renders no button and throws nothing, the optional-prop idiom `onSpawnAgent`/`onColorChange` already use. Tooltip and `aria-label` share one `FORK_TOOLTIP` constant naming the same-directory semantics explicitly (`"Fork conversation — new window, same directory"`) — that is what keeps a fork (branch this conversation *here*) legible against the spawn dialog (a fresh agent, usually in a new worktree), since nothing else guards the shared working tree. `stopPropagation` on click so forking never also selects the underlying row (the PR/docs links' idiom). The glyph is a hand-built inline SVG sized to match `InfoIcon` at 12px, per the card's stated convention (inline SVG renders without a patched terminal font and themes via `currentColor`). (`260806-s4av`)

**Fork in-flight guard** — `ForkLink` owns its own `busy` state, so the flag is **leaf-scoped**: only the button re-renders while a fork is in flight, honoring the card's render-performance discipline. The guard is load-bearing rather than cosmetic — `onFork` POSTs a mutating endpoint that CREATES a tmux window, so N clicks would create N fork windows. `disabled={busy}` (the spawn dialog's idiom) plus an early `if (busy) return` in the click handler is the belt-and-braces; the flag clears on settle for **both** outcomes, so a failed fork stays retryable. A `mountedRef` guards the settle `setState`, because the card unmounts when the flyout closes and a *successful* fork navigates away — settle-after-unmount is the normal case here, not an edge one. The handler chain (`app.tsx` `handleForkWindow` → `Sidebar` → `WindowRow` → `useRowFlyout`'s options) returns the **already-error-handled** settle promise, never rejecting, which is what lets the button simply await it.

**Fork navigation on success** — `handleForkWindow` (`app.tsx`) calls `forkWindow(server, windowId)` and on success routes through the **shared `navigateToSpawnedWindow(srv, windowId)`**: same-server → `navigateToWindow` (inheriting the window-switch slide transition), cross-server → `navigate({ to: "/$server/$window" })` + close the mobile sidebar. That helper was **extracted** from the spawn dialog's inline `onSpawned` block, so the fork and the spawn cannot drift on the routing rule (§ Spawn-Agent Dialog → Cross-server nav on success). A **falsy `windowId`** skips navigation entirely — the backend's id resolve is best-effort, and routing with an empty id would land on a junk `/$server/@` URL, so the SSE stream surfaces the new row instead. A rejected fork raises an **error toast** carrying the server's message (`404` no forkable claude chat, `400` a non-repo directory) rather than failing silently; there is no bespoke fork dialog (constitution §IV).

**Freshness line — "checked Xs ago"**: `prFetchedAtEpoch(win)` parses `prFetchedAt` (the collector-join `PrFetchedAt` timestamp, architecture.md § PR-Status SSE Join) to **epoch SECONDS**, returning `null` when absent OR unparseable (`Date.parse` → `NaN`) so the card omits the line rather than rendering "checked NaNs ago". `FreshnessLine` is its own leaf component (`data-testid="row-flyout-checked"`) owning a live `useNow()` clock and rendering `checked ${formatDuration(nowSeconds - fetchedAtEpoch)} ago` — reusing `formatDuration` (`lib/format.ts`) rather than adding a relative-time formatter. After a manual refresh the timestamp visibly resets — the ambient version of the trust signal the PANE refresh button's checkmark gives. (`260715-nwla`)

**Perf** — every hard constraint from § Render Performance holds: open/hover state is **row-local** (inside `useRowFlyout`, never lifted to `Sidebar`), the card body **mounts only while open**, and both live clocks (`RowFlyoutContent`'s `useNow` feeding the `out` register plus `FreshnessLine`'s own) are **leaf-scoped inside that open card** — the row itself never ticks and no `nowSeconds` prop is threaded into a memoized component. `WindowRow` stays `memo`'d. The two clocks are two `setInterval`s while one card is open; collapsing them onto a single hoisted clock is an available optimization, not a contract fix.

**Tests** — `row-flyout-card.test.tsx` covers card content (per-layer registers + absent-as-absent, the pinned `pr` prefix codepoints, colored PR segments, the PR anchor only with `prUrl`, the always-present docs link, header = `dotLabel`), the freshness line (present / absent / unparseable), the warm-delay helpers (350 cold, 0 within the warm window), the `suppressed` early-return in `openNow`, the `lastClosedAt`-only-when-active stamp, and the **fork gate in all four states** (claude+handler renders; no `chatProvider`, a `codex` provider, or no handler each render nothing) plus the click invoking the handler and the **in-flight guard** (while the handler's promise is pending the button is disabled and a second click does not re-invoke). E2E `tests/e2e/row-flyout.spec.ts` (+ sibling `.spec.md`) proves hover-open at the sidebar's right edge, warm retarget between rows, PR-link presence + row-select isolation, the rest glyph → hover swap to pin+✕, keyboard focus-open + Tab-reach + Escape dismiss, coarse-pointer hover suppression + dot-tap open, and the fork link's presence for a claude-chat row / absence for a plain shell row plus the **navigation contract** against a non-empty mocked `windowId` asserting the resulting `/$server/$window` URL.

## Tooltips — Two-Tier Taxonomy

Tooltips are a two-tier system with a **hard boundary and no middle species** (§ Design Decisions → Two-tier tooltip boundary):

- **Tier-1 — `Tip`** (`components/tip.tsx`): **names a control**. One-line plain-text label + optional keycap chip (`kbd`) + optional dim modifier note. Carries the W3C tooltip pattern — `role="tooltip"` on the floating element, `aria-describedby` on the anchor while open. NEVER interactive content. This is the app-wide replacement for native `title=` on interactive chrome controls.
- **Tier-2 — hover-cards** (the sidebar row flyout card, § Row-hover register flyout card): hold a second line of **state** and/or **interactive content** (real `<a>` links). Deliberately role-less (§ Design Decisions → No `role="tooltip"` on the status detail hover-card).

**Promotion rule**: a tooltip that needs a second line of state or anything clickable is tier-2, not a fatter tier-1. Consequence at the `title=` migration boundary: native titles whose value is *state or a content reveal* rather than a control name stay native `title=` — they are neither converted to `Tip` nor promoted to new hover-cards. Left native by this rule: the server-tile window-count summary (`sidebar/server-panel.tsx`), the PR-URL reveal and cwd reveal (`sidebar/status-panel.tsx`). (`260722-73al`)

### Tier-1 `Tip` (`components/tip.tsx`)

`Tip` wraps a **single child element** and clones it with the floating reference props/ref merged (`useMergeRefs`) — **no wrapper DOM node** is added, so layouts and the top-bar overflow fit's hidden width-measurement probe are unaffected (§ Design Decisions → Clone-child reference API). Built on `@floating-ui/react` (already a dependency — the tier-2 hover-card uses it too). A falsy `label` renders the child untouched (the conditional-tooltip idiom, e.g. "Copy version", "Create a server first").

**Visual shell — "quiet card"**: `bg-bg-card`, 1px `border-border`, 5px radius, soft shadow, 11px mono, `text-text-primary` label. The floating element is `pointer-events-none` (tier-1 holds nothing interactive, so it can never intercept a click) and `max-w-[40ch]` + `whitespace-nowrap` + truncate backstop the one-line ≤40ch cap. Optional slots: a `note` (dim `text-text-secondary` modifier, e.g. "⇧click: force") and a `kbd` keycap chip (`bg-bg-inset`, 1px border with a 2px bottom edge for the "key" read, 3px radius, 10px type). The `kbd` value is a plain string prop, but **for a registry-bound action the call site resolves the HOST-EFFECTIVE chord** rather than hard-coding a chord: `useKeybindings()` → `byAction.get(actionId)` → `formatCombo({ code, tier }, host.platform)` when the binding is `enabled`, else `undefined` so **no chip renders** — a tip advertising a dead chord would lie (§ Keyboard Shortcuts → Palette hints applies the same rule to palette hints). This makes every such chip reflect overrides and per-platform tiers. Five `Tip`-chip call sites resolve a chord this way: the top bar's Settings gear (§ Right cluster → Settings gear), the bottom bar's Compose `>_` and palette `⌘K` chips (§ Bottom Bar), and the Open and Split split-buttons' primary segments (§ Open split-button, § Split control); the chevron menu's Settings and Keyboard shortcuts rows apply the same derive-and-omit rule to their trailing `MENU_ROW_KBD_CLASS` keycaps (§ Right cluster → overflow chevron menu). The same derive-and-omit rule governs chord clauses in prose copy — placeholders and empty states (§ Education micro-copy). A **computed, non-registry** `kbd` string is correct only for a chord with **no registry binding** — the compose/chat submit + insert editing chords, which are focused-textarea keys the registry does not own. Those are still platform-correct without the registry: Send resolves through the shared `composeSubmitKeycap()` (`formatCombo({code:"Enter",tier:"cmd"}, detectPlatform())` → `⌘Enter` / `Ctrl+Enter`), while the Insert chips carry literals that differ per surface — the strip's reads `Enter` (its Insert follows Enter; the Alt+Enter raw-insert chord lives in the tip's label text), chat's reads `Alt+Enter`. The `tip.tsx` prop doc states this contract. (`260801-mqim`, `260802-lj98`)

**Behavior contract** (user-approved design session):

- **Open delay** — 300ms on hover (`TIP_OPEN_DELAY_MS`); **0ms while the cluster is "warm"** (a sibling tip in the same group closed <500ms ago, `TIP_WARM_WINDOW_MS`) — macOS-menu sweep behavior. Delays ride `useDelayGroup`; outside any group the group context is `delay: 0`, so `Tip` falls back to its standalone 300ms open.
- **Warm clusters** — `TipGroup` (exported; a `FloatingDelayGroup` preset with `delay={{open: 300, close: 0}}`, `timeoutMs={500}`) wraps one chrome region: the top-bar's three regions (left breadcrumb cluster, center heading cluster, right control cluster — the latter contains the overflow menu and every registry control's popover), the sidebar root, the iframe URL bar, and the chat/compose button rows. A single-tip region needs no provider.
- **Keyboard** — opens immediately on `:focus-visible` (`useFocus` `visibleOnly` default), never on mouse-down focus (Constitution V).
- **Dismiss** — pointer-leave, Escape (`useDismiss`), and activating the control (`referencePress: true` — the tip never sits over the click's result).
- **Touch** — suppressed under `pointer: coarse` (via `useCoarsePointer`). The child renders unchanged with no reference props, portal, or ARIA wiring — the control's `aria-label` carries the name (§ Design Decisions → Suppress-by-early-return on coarse pointers).
- **Placement** — per-site `placement` prop, default `bottom` (top-bar convention); bottom-of-screen strips pass `top`, sidebar rows pass `right`. `offset(6)` / `flip()` / `shift({padding: 8})` / `autoUpdate` — the shared hover-card middleware set — flip and shift at viewport edges.
- **Reduced motion** — trivially satisfied: no fade, instant show/hide.

Named constants carry every magic value: `TIP_OPEN_DELAY_MS`, `TIP_WARM_WINDOW_MS`, `TIP_OFFSET_PX`, `TIP_SHIFT_PADDING_PX`.

**Migration — no native `title=` on chrome controls**: native `title=` is **REMOVED wherever `Tip` lands** (never both — the OS bubble would double the styled tip); `aria-label`s stay untouched. `Tip` covers the interactive chrome controls across ~15 component files: `top-bar.tsx`, `top-bar-overflow-menu.tsx`, `breadcrumb-dropdown.tsx` (the `title` prop — a component prop like `Dialog title=`, so kept under that name — renders as an internal `Tip` around the trigger), `view-switcher.tsx`, `open-button.tsx`, `waiting-badge.tsx`, `sidebar/index.tsx` (scope chip, over-cap copy rewritten to ≤40ch action labels), `sidebar/status-panel.tsx`, `iframe-window.tsx`, `chat-view.tsx` + `compose-strip.tsx` (the natural `kbd`-slot users — Send + the submit keycap, Insert + `Alt+Enter` in chat / `Enter` on the strip), `host-overview-page.tsx`, `swatch-popover.tsx`, and the board-route twin `board/board-header.tsx` (the `/board/$name` route re-implements chrome and does not render AppShell — see [tmux-sessions](/run-kit/tmux-sessions.md); its one real native tooltip is the unpin button, migrated; its three other `title=` matches are `Dialog` props, untouched). **Untouched seams**: `Dialog title=` component props everywhere; `title="Proxied content"` on the proxied iframe (its accessible name, asserted by `top-bar-overflow.spec.ts` and `web-view-lens.spec.ts`); `CollapsiblePanel`-style heading `title` props; and the tier-2 hover-card (untouched by the tier-1 migration). (`260722-73al`)

**Tests** — `tip.test.tsx` covers label/note/kbd render, `role="tooltip"` + `aria-describedby` wiring on open, focus-open, no render / no `aria-describedby` under a mocked coarse pointer, and label-less pass-through (7 tests). E2e `tests/e2e/tooltips.spec.ts` (+ sibling `tooltips.spec.md`) proves keyboard-focus open, hover open, and coarse-pointer absence, plus the palette chip's **resolved** keycap reading `Ctrl+K` — Playwright's `devices["Desktop Chrome"]` pins a **Windows** UA, so `detectPlatform()` returns `other` regardless of the runner's own OS. Registry-resolved chips are covered per-site: `bottom-bar.test.tsx` (the palette chip's `Ctrl+K` tip beside the button face's `⌘K` glyph, the Compose chip's `Shift+Ctrl+E`, and the omit-when-disabled case seeded via a `{"compose-toggle": null}` override) and `sidebar/index.test.tsx` (the Settings gear's `Shift+Ctrl+,`). Per-site unit tests on migrated controls (`top-bar.test.tsx`, `top-bar-overflow-menu.test.tsx`, `update-chip.test.tsx`, `board/board-header.test.tsx`, `host-overview-page.test.tsx`) assert the native `title` is gone and the `aria-label` preserved — deep tooltip behavior lives once in `tip.test.tsx`.

## Attention Surfacing (`waiting` rollups + nav)

The per-window `waiting` overlay (agent blocked on a human) propagates UP the hierarchy as **count badges** and drives a **keyboard-first navigation** action and a **board-pane seam**. This is the UI half of #314's three-state `agentState` — see [agent-state](agent-state.md). All counts derive from the existing SSE session data already in the client — **no new endpoint, no `setInterval`/fetch** (Constitution II / code-quality anti-patterns).

**Shared rollup helpers** (`lib/waiting.ts`) — the single source of truth so every surface counts identically: `isWaiting(win)` (= `win.agentState === "waiting"`, keyed on the server-side window-level rollup that already applies `waiting > active > idle` across panes), `countWaitingWindows(windows)`, `countWaitingInSessions(sessions)`, and the instance-wide `countWaitingAcrossServers(sessionsByServer)` (which composes `countWaitingInSessions` over the streamed map). Pure functions over the streamed `WindowInfo`/`ProjectSession` shape.

**`WaitingBadge`** (`components/waiting-badge.tsx`) — a small count chip mounted unconditionally; **renders `null` when `count <= 0`** (attention badges are present only when they mean "something needs you"). Styling follows the existing count-chip vocabulary but in the **signal yellow** of the waiting overlay (`bg-signal-yellow/15 text-signal-yellow` — the *semantic* is constant: "yellow = an agent needs you now", the same glance language as the status-dot halo, in every theme; the *value* re-themes via `--color-signal-yellow`, § Design Decisions → Signal hues are per-theme tokens), plus a `⚠` glyph. It is TEXT, not motion (the pulse lives on the per-window dot/seam) — legible via color + count + `aria-label`, never color-only. Mounted on four surfaces:

- **Session row** (`sidebar/session-row.tsx`) — count of the session's waiting windows (`countWaitingWindows(session.windows)`), rendered after the session name button (R8).
- **Host-page TMUX SERVERS server tile** (`host-overview-page.tsx`) — per-server count summed over the server's sessions (`countWaitingInSessions(sessionsByServer.get(name) ?? [])`), absolutely positioned top-right on the tile. One glance at `/` answers "does anything need me" (R9). Only attached servers stream windows, so an unattached server's count is 0 (badge absent) — never a wrong count.
- **Board header** (top-bar `BoardModeInfo`, `top-bar.tsx`) — count of the board's waiting panes (`waitingPaneCount` prop), rendered in the board-mode left info between the pane/server counts and the `⌘[⌘] cycle` hint (R10).
- **Sidebar SERVER-panel server tile** (`sidebar/server-panel.tsx`) — the fourth server-level surface, per-server count over the server's sessions. Placement **differs** from the Host-page tile: it rides the **same flex row as the tile's "N sess" count, right-aligned** (`flex items-center justify-between`) rather than absolute top-right, because the sidebar tile already occupies its top-right with a hover-revealed palette/kill action cluster (`top-1 right-1 z-10`) — the Host-page tile's `absolute right-2 top-2` would collide. `ServerTile` takes a plain `waitingCount: number`; `ServerPanel` takes the optional `waitingCounts?: Map<string, number>` and forwards `waitingCounts?.get(name) ?? 0` per tile, keeping the presentational panel decoupled from session data. The Sidebar computes the map in `sidebar/index.tsx` via `useMemo(() => …, [sessionsByServer])` over the already-consumed `sessionsByServer` (same `countWaitingInSessions` helper). Attached-server-only by construction (an unattached server is absent from `sessionsByServer` → count 0 → badge absent), the same accepted caveat as the Host-page tile — no eager attach-all: lazy-attach is deliberate, so an unattached server shows no badge. (`260708-4li7`)

**Board-pane waiting seam** (`board/board-pane.tsx` + `board-page.tsx`) — a waiting board pane gets a **3px pulsing yellow seam** (border-width system; the `rk-waiting-seam` class + `border-[3px]`), the board-pane form of the status-dot halo. Border precedence: a `waiting` pane always shows the seam (attention is the highest-priority signal); a focused-AND-waiting pane layers the accent focus shadow on top so it reads both. Under `prefers-reduced-motion` the seam is static yellow (`globals.css`). `BoardEntry` is a thin shape with no `agentState`, so `BoardPageContent` **joins each pinned pane back to its live `WindowInfo`** via `(server, windowId)` against the streamed `sessionsByServer` (use-boards attaches every board server, so window data incl. `agentState` is flowing) — a `waitingWindowIds` set (`${server}:${windowId}` keys) drives both the per-pane seam and the header count; a pane whose window is not yet in the snapshot is simply not waiting (no wrong signal).

**OS-level escape hatch** — inside the Electron desktop shell a fifth consumer, the `ShellBadgeReporter`, mirrors `countWaitingAcrossServers(sessionsByServer)` to the dock/taskbar badge, so the attention tier is visible when the window is not (§ Desktop-Shell Waiting Badge Reporter). Waiting only, like every surface above; a browser tab physically cannot do this, which is what the badge buys over the in-page badges.

**`Agent: Next waiting` palette action** (`app.tsx` `agentActions`, R11 — keyboard-first, Constitution V): cycles focus through windows whose rolled-up `agentState` is `waiting`, **current server first** (in sidebar order, from `flatWindows`) then other **attached** servers (from a live `sessionsByServerRef.current` read — unattached servers' window data isn't streamed, a known constraint). No-op with a `"No agents waiting"` toast when none. The cycle arithmetic is the pure `nextWaitingTarget(ordered, currentServer, currentWindowId)` helper (`lib/palette-agent-nav.ts`, unit-tested): empty → `null`; current window not in the list → first target (jump in); current IS a target → the next one **with wraparound** (`(idx+1) % len`). This is a deliberate contrast with `palette-move.ts` (reorder arithmetic, which does NOT wrap) — do NOT extend that module. Same-server targets navigate via `navigateToWindow`; cross-server targets `navigate({ to: "/$server/$window" })` (and close the mobile drawer). The action is appended to `paletteActions` as `agentActions`.

## Iframe Window (the `web` lens renderer)

`app/frontend/src/components/iframe-window.tsx` — the renderer for the **`web` surface/lens** (§ Window Views (Lens Model)), mounted as a tile by `SurfaceLayout` whenever the resolved layout's order includes `web` (§ Surface Layout). The ladder's availability degradation bakes in the capability gate, so an iframe-typed window with no `rkUrl` never resolves a `web` tile. Bottom bar is NOT rendered for a web tile (no terminal to send keys to). The component is **presentational** — it takes `windowId`, `rkUrl`, and an `onSwitchToTty: () => void` callback and owns NO view/URL/localStorage logic (that lives in `surface-layout.ts` + `app.tsx`); it does not import `updateWindowType` (§ Window Views → decoupling). The web tile passes `onSwitchToTty` (applies `single:tty` through the shared mutation path), so the `>_` switch-to-terminal button renders. (`260714-t97o`, `260812-ab5v`)

**Layout**: Outer wrapper `flex flex-col flex-1 min-h-0` with two children: URL bar (`shrink-0`) and iframe (`flex-1`).

**URL Bar**: Thin toolbar above the iframe (`border-b border-border bg-bg-primary`). Four elements:
- **Refresh button** (↻ `&#x21bb;`) — forces iframe reload by clearing `src` to `"about:blank"` then re-setting it via `setTimeout(0)`. Styled: `w-7 h-7 rounded hover:bg-bg-card text-text-secondary`
- **URL input field** — shows current `rkUrl`, editable. On Enter, calls `updateWindowUrl(server, windowId, url)` which delegates to the unified `setWindowOptions(server, windowId, { "@rk_url": url })` → `POST /api/windows/{windowId}/options` (partial-merge — only `@rk_url` is touched). On API failure, reverts input to the SSE-confirmed `rkUrl`. Styled: `bg-bg-card text-text-primary text-sm px-2 py-1 rounded border border-border`. **This is the one POST the web lens fires** — editing the content ADDRESS is *substrate* state (shared, POSTed, everyone sees it — spec R7), distinct from switching which lens you look through (per-viewer, no POST). (`260529-jad6`)
- **Submit indicator** (⏎ `&#x23ce;`) — decorative visual affordance (`text-text-secondary text-xs`)
- **`>_` switch-to-terminal button** (`aria-label="Switch to terminal"`) — it calls the injected `onSwitchToTty()` prop (which runs `app.tsx`'s `switchView("tty")` → `applyLayout(single:tty)` — persist + URL mirror, § Surface Layout), **NOT** `updateWindowType(server, windowId, "")`. It is a per-viewer *layout switch*, so flipping to the terminal and back preserves the window (no `@rk_type` mutation, no global flip — the e2e asserts zero `/options` POSTs across a flip). (`260714-t97o`)

**SSE Sync**: A `useEffect` on `rkUrl` syncs both the URL bar text and iframe `src`. Uses a `currentSrcRef` to avoid re-setting iframe `src` when the URL hasn't actually changed (prevents unnecessary reloads on identical SSE ticks). When `rkUrl` changes externally (Claude or another process runs `tmux set-option`), the URL bar updates and iframe navigates automatically.

**Proxy URL conversion**: `toProxySrc(url)` converts localhost URLs to proxy paths: `http://localhost:8080/docs` -> `/proxy/8080/docs`. Non-localhost URLs pass through unchanged. Pattern: `^https?:\/\/(?:localhost|127\.0\.0\.1):(\d+)(\/.*)?$`.

**Iframe attributes**: `sandbox="allow-same-origin allow-scripts allow-forms allow-popups allow-modals allow-downloads"` (without `allow-downloads`, VS Code file downloads break — the same sandbox `CodeSurface` reuses, § Code Surface), `title="Proxied content"`, `border-0`.

**Window creation**: "Window: New Iframe Window" command palette action (id `create-iframe-window`) opens a `Dialog` with two inputs: window name (autofocused, Enter focuses URL input) and URL (Enter creates window). Create button disabled until both fields non-empty. Calls `createWindow(server, session, name, undefined, "iframe", url)` — the extended API client function passes `rkType` and `rkUrl` in the POST body. Backend uses `CreateWindowWithOptions` for atomic `\;`-chained tmux command. Only shown when a session is active. Synthetic iframe windows are created this way; `@rk_type=iframe` means "**default view = web**" (a creation-time hint, § Window Views → `defaultView`), not a locked identity. (`260714-t97o`)

## Code Surface (the `code` lens renderer)

`app/frontend/src/components/code-surface.tsx` — the renderer for the **`code` surface/lens** (§ Window Views (Lens Model)) — mounted as a tile by `SurfaceLayout` whenever the resolved layout includes `code` (§ Surface Layout): via the rail toggle, the `Layout: Add Code` palette entry, a `?layout=…code…` deep link, or the switcher's `View: Code` row (which applies `single:code` through the shim). It embeds **code-server** at the window's git root — a full editor (tabs, syntax highlighting, SCM view, side-by-side diffs) beside the live agent terminal with no editor functionality built in run-kit. It is a **NEW lean component, NOT `IframeWindow`**: the code-server URL is fully DERIVED, so the URL bar (`@rk_url` substrate chrome) is meaningless here — the component is exactly an iframe plus the not-running empty state (§ Design Decisions → The code surface is a lean `CodeSurface`). (`260811-k3vp`)

- **src** — `codeServerSrc(gitRoot)` returns the RELATIVE path `/code/?folder=<git root>` (folder value `encodeURIComponent`d), never a composed absolute origin and never a port — the same relative-path discipline `toProxySrc` follows, so the embed rides the same origin through any reverse proxy. The path is STABLE: code-server keys browser-side workspace state by the proxy pathname, and `/code/` is fixed by the backend route (the port behind it is a private implementation detail, resolved server-side). code-server restores per-folder state from the `?folder=` param.
- **Availability vs reachability** (spec right-panel.md § Surface Registry): AVAILABILITY = the window's `gitRoot` derived non-empty — the one STABLE capability signal, computed upstream in `hasCode` so the rail button / switcher segment never strobe (the port is always resolvable by convention, so the "configured" leg is always true). REACHABILITY (the host's TTL-cached probe, the state socket's `code-server` event) selects the surface's CONTENT only: reachable renders the iframe; unreachable renders the terse monospace empty state `code-server not running — check rk doctor` (`data-testid="code-surface-empty"`) instead of a dead iframe.
- **Sandbox** — the same token set as `IframeWindow`, including `allow-downloads` (without it VS Code file downloads break).
- **Chord reclaim** — focus inside the iframe swallows run-kit's chords (`⌘K` collides with VS Code's own), and same-origin makes an escape hatch possible: a capture-phase `keydown` listener attached to the iframe's `contentDocument` after every load intercepts events matching the INJECTED `shouldReclaimChord` predicate (built in `app.tsx` from the keybinding registry's pure `hasReclaimableMatch(e, bindings)` — `findMatches(...).some(b => !b.ttyOnly)`), `preventDefault` + `stopImmediatePropagation`s them before the embedded app's keybinding service, and re-dispatches a synthetic bubbling `KeyboardEvent` on the parent **document** (reaching both document-level listeners — the palette chord — and window-level ones — the dispatcher, the ⌘. view cycle). The reclaim carve-out: a keydown whose ONLY registry matches are `ttyOnly` bindings (the tmux split pair — a keydown arriving inside the iframe means the code tile owns focus) is NOT reclaimed, so ⌘D reaches code-server's own keybinding service while non-`ttyOnly` registry chords (⌘K, ⌘., ⇧⌘., …) reclaim exactly as before (`260812-wfic`). The effect is keyed on `reachable` (the iframe only mounts when reachable, so a reachability flip re-runs it against the fresh iframe), re-attaches on `load` (each navigation replaces the document), guards `contentDocument` access in try/catch (a cross-origin or pre-load frame silently skips the attach — click-out remains the escape), and removes the listener from the attached document on cleanup. Proven by the `code-surface.spec.ts` e2e "keyboard spike: a registry chord pressed INSIDE the iframe reaches the parent" (§ Design Decisions → Chord-reclaim via a capture-phase iframe listener).

## Window Views (Lens Model)

The model (`docs/specs/window-views.md`, rules R1–R7; arrangement generalized by `docs/specs/surface-layout.md`): **a window ROW is a substrate** (a supervised tmux pane); **a VIEW is a lens** over one derivable output of that pane (the tty is a lens; an iframe of the pane's HTTP service is a lens; the parsed agent transcript is a lens; later: desktop). **Which lenses a window offers is a derived capability set; what YOU look at is per-viewer client state** — the surface layout (§ Surface Layout) carried in the URL + localStorage, never a server-side `@rk_type` mutation, never part of the window's identity. A single-tile layout IS the exclusive-lens view: `single:chat` is "the chat lens", `single:tty` the default terminal. Frontend-only; no Go/API changes (`@rk_type`/`@rk_url`/`@rk_chat` plumbing and `POST /options` are untouched — the SSE payload already carries `rkType`/`rkUrl`/`chatProvider`). (`260714-t97o`, `260714-r7rq`)

Four lenses are implemented: `tty` (always available, `TerminalClient`), `web` (available when `rkUrl` is non-empty, `IframeWindow`), `code` (available when the window's `gitRoot` derived non-empty — the one stable capability signal; `CodeSurface`, § Code Surface), and `chat` (available when `chatProvider` is non-empty, `ChatView` — the read-only transcript renderer, § Chat View). Capabilities are orthogonal and STACK (spec R5): one window can offer `tty`+`web`+`code`+`chat` at once, and the layout can tile up to three of them at once (§ Surface Layout). The lens set IS the layout's tileable surface set (`SurfaceKind = ViewName`). The registry is open-ended — `desktop` adds a `ViewName` member, an availability clause, a `defaultView` hint, and a `ViewSwitcher` segment, NOT a new component/route/window-type.

### Pure view-model helper — `lib/window-view.ts`

The single source of lens CAPABILITY logic — pure, DOM-free, colocated `window-view.test.ts` (the same pattern as `window-transition.ts`/`navigation.ts`). `surface-layout.ts` builds its availability set on these predicates, so the rail, the layout, and the switcher share one capability source (no drift). Helpers take a minimal structural `{ rkType?, rkUrl?, chatProvider?, gitRoot? }` shape (assignable from `WindowInfo`):

- **`ViewName = "tty" | "web" | "chat" | "code"`** — the lens union; aliased as the layout's `SurfaceKind`.
- **`hasWebUrl(win)` / `hasChat(win)` / `hasCode(win)`** — the single-source availability predicates: `rkUrl` non-empty after `.trim()` (whitespace-only `@rk_url` is NOT web — no blank-src iframe); `chatProvider` non-empty (mirrors the backend `resolveWindowChat` gate); for `code`, the window's `gitRoot` non-empty — the one STABLE capability signal (the port is always resolvable by convention, so there is no configured-port leg); code-server REACHABILITY is deliberately NOT in this gate (it fluctuates and would strobe the rail/switcher; it selects the surface's content instead, § Code Surface).
- **`availableViews(win): ViewName[]`** (spec R1/R3) — `["tty"]`, plus `"web"` when `hasWebUrl`, plus `"code"` when `hasCode`, plus `"chat"` when `hasChat`. **Availability is decoupled from `@rk_type`**: an iframe-typed window with no URL offers only `tty` (matches the old render gate's AND-condition). Returned in the registry's fixed order via a `HINT_ORDER` (`[chat, code, web, tty]`) filter — so a fully-stacked window yields `["chat","code","web","tty"]`. The layout module's `availableTiles` re-derives the same set tty-first (§ Surface Layout).
- **`defaultView(win): ViewName`** (spec R5) — the window's default lens, applied ONLY as the layout ladder's hint rung (`hintLayout` wraps it as `single:<view>`): `"web"` when `rkType === "iframe"` AND `rkUrl` is set, else `"tty"`. `@rk_type=iframe` is **demoted from identity to a creation-time default-view hint** (no data migration). Structured as spec R5's ordered hint precedence `desktop > chat > code > web > tty` (module const `HINT_ORDER`, only `web`/`tty` wired) so later lenses add a hint clause, not a branch. **`chat` and `code` sit in `HINT_ORDER` for capability ORDERING only — they contribute NO default hint**: a chat- or code-capable window still defaults to `tty` unless the viewer chose otherwise (preserves `260714-r7rq`'s terminal-default).
- **`nextView(available, current): ViewName | null`** — the cycle successor (wraps); `null` when < 2 views or `current` is absent. Powers the `Cmd/Ctrl+.` chord, which applies `single:<next>` (pure so the order is unit-testable).
- Input gating for the lens chord is **not** a `window-view.ts` concern: the shared `shouldSuppressChord` predicate in `lib/keybindings.ts` gates every app chord (§ Keyboard Shortcuts → Dispatch seams).
- **localStorage helpers** — **value-bearing** key `runkit-window-view:{server}:{windowId}` storing the view NAME, including `"chat"` (absent = default). `windowViewStorageKey`/`readStoredView`/`writeStoredView`, try/catch-noop per `chrome-context.tsx`. These keys are LEGACY seed sources: the layout mutation path writes `rk-layout:` keys, and `runkit-window-view` is read only by the translation shim's seeding (§ Surface Layout) and by the switch-transition classification.
- **`resolveView` / `writeStoredView`** survive with no production callers — `resolveLayout` subsumes the resolution chain; both are Phase-3 retirement-sweep deletion candidates (`fab/changes/260812-ab5v-surface-layout-core/plan.md` § Deletion Candidates).

### `?layout=` search param + the translation shim — `lib/router-url.ts`

`layout` is the terminal route's one layout search param (spec surface-layout.md L1, Constitution IV: no new route); the legacy `view`/`panel` params remain ACCEPTED so old deep links never break. The exported pure `validateTerminalSearch(search): { view?: "web" | "chat" | "code"; panel?: "web" | "code"; layout?: string }` passes `view` ∈ {`web`, `chat`, `code`} and `panel` ∈ {`web`, `code`} through, forwards any non-empty string `layout` (shape/arity validation happens in `parseLayout`, § Surface Layout), and **drops any other/unknown/non-string value** (treated as absent, NEVER errored). It is wired as the route's `validateSearch` and unit-tested directly in `router-url.test.ts`. The `declare module "@tanstack/react-router"` `Register` block types the params, so `useSearch({ strict: false })` in `app.tsx` reads them **with no `as` cast** (code-quality type-narrowing — `tsc --noEmit` clean). At resolution, `translateLegacyParams(view, panel)` maps `?view=X` → `single:X` and `?view=X&panel=Y` → `split-h:X,Y` (X in slot A), and the route mirrors the resolved layout back as `?layout=` ONLY.

### `app.tsx` integration — resolution, `switchView`, the layout render

`app.tsx` (terminal route) owns ONE layout-state block (§ Surface Layout → `app.tsx` integration for the full ladder/mirror/mutation machinery): `searchLayout = search.layout ?? translateLegacyParams(search.view, search.panel)`, `storedLayout = readStoredLayout(server, windowParam)`, `layout = resolveLayout(searchLayout, storedLayout, currentWindow)` (memoized). The lens model's consumers key off **slot A** — `resolvedView: ViewName = layout.order[0]` (spec R12's shim: a multi-tile layout reads as its slot-A surface to the switcher, the `view-cycle` chord, and the palette `View:` actions). One stable `switchView(view)` callback collapses to a single-tile layout: `applyLayout({ shape: "single", order: [view] })` — `applyLayout` persists per-window localStorage AND mirrors the URL via `replace: true` navigation, so a lens pick is a full user mutation (never an `@rk_type` mutation, spec R7). The **render** is `<SurfaceLayout layout={layout} …/>` keyed `` `${server}:${windowId}` `` (a window switch remounts it) — one tile per surface in the resolved order (§ Surface Layout → tile renderer). There is no `@rk_type`-mutating "switch" path anywhere: switching lenses/surfaces is the rail/ViewSwitcher/palette's job, none of which call `updateWindowType` (spec R7).

### ViewSwitcher chip — `components/view-switcher.tsx`

The ONE switcher UX all lenses share (spec R4), retargeted to the layout model (surface-layout spec R12): selecting a view sets a **`single:<view>`** layout through the shared `applyLayout` mutation path (a user mutation — persisted + mirrored), and when the layout is multi-tile the switcher reflects **slot A**'s surface (`resolvedView = layout.order[0]`). It is the **first entry of the right-cluster overflow registry** (`view-switcher`, see § Right cluster → overflow chevron menu) and is **`menuOnly`** (§ Right cluster; the generic registry flag): the entry NEVER renders in-bar — its ONLY rendering is the per-view `View: …` rows (`ViewSwitcherMenuRows`) in the "More controls" chevron menu, at every width whenever the window offers more than one lens. Because `view-switcher` is the first registry entry, its menu rows are the FIRST rows in the menu. The pill itself is a generic segmented chip — props `{ views: ViewName[]; active: ViewName; onSelect }`, fed via the `useRegisterTopBarSlot` slot context (`availableViews`/`activeView`/`onSelectView` are the ONLY view fields on the slot) — but its `barRender` closure is unreachable under the flag; it survives in the codebase intact so a revert is one line (removing `menuOnly: true`). Renders `null` when `views.length <= 1` (both the chip and the menu-rows form gate on this, so a tty-only window offers no lens switcher at all). Presentational — owns no view logic. (`260717-6anu`, `260722-n2n4`)

- **Segment DISPLAY order is `[tty|web|code|chat]`** via a component-local `DISPLAY_ORDER = ["tty","web","code","chat"]`, **deliberately decoupled from `window-view.ts`'s `HINT_ORDER`** (`chat>code>web>tty`, which governs only default/capability precedence — the two orderings answer different questions). Any view not in `DISPLAY_ORDER` sorts to the END (defensive — a future lens still renders). The tty-first ordering is applied by a shared module-local helper `orderViews(views)` (listed-in-`DISPLAY_ORDER` first, unlisted appended) used by BOTH the in-bar pill AND the overflow `ViewSwitcherMenuRows`, so bar↔menu ordering can never drift. (`260717-6anu`)
- The pill (`ViewSwitcher`) is **kept but unreachable** under the `menuOnly` flag — its `barRender` closure never runs. Its structure: active segment **inverse-video** (`bg-accent-green text-bg-primary`); house `rk-glint` hover vocabulary (reduced-motion-safe — the glint is a CSS media-query no-op, no JS animation); `role="group" aria-label="Window view"` + `data-testid="view-toggle"` + per-segment `aria-pressed`/`aria-label` (`Terminal view`/`Web view`/`Code view`/`Chat view`); segment glyphs the **lowercase view names** `tty`/`web`/`code`/`chat` (spec R4's `[tty|chat]` style). The `view-toggle` testid therefore appears NOWHERE in the live DOM (not in the bar, not in the measurement probe — a `menuOnly` entry is excluded from both). The component carries **no `hidden sm:*` breakpoint gate** (`inline-flex`) — its space-driven-visibility structure stays intact so removing the flag restores the in-bar pill unchanged, and its segments track the shared `TOP_BAR_SEGMENT_H` height token (§ Right cluster → shared button-size token) so a revert lands already-aligned with the SplitControl / OpenButton chips. Being unreachable, the pill is a **deletion candidate** alongside the `260731-oiho` demotions.
- **Menu-rows form — `ViewSwitcherMenuRows`** (`260717-6anu`, exported from `view-switcher.tsx`, same `{views, active, onSelect}` props): the lens switcher's ONLY rendering (`menuOnly`, `260722-n2n4`) — **one `role="menuitemradio"` row per available view** in the shared `orderViews()` tty-first order, labeled `View: {VIEW_LABEL[view]}` (`View: Terminal` / `View: Web` / `View: Code` / `View: Chat` — the palette's `View:` vocabulary). The rows sit in a `role="menu"` container, so `menuitemradio` (single-select radio semantics) is the correct role and the **active** row carries **`aria-checked`** (NOT `aria-pressed` — that is a toggle-button state invalid on a `menuitemradio`); the active row is inverse-video accent-green (matching the retained pill's active segment), so the menu carries the lens-indicator role. Clicking a row calls the same `onSelect(view)` and the menu's role-keyed handler closes the panel. Row styling composes the shared `MENU_ROW_BASE` + `MENU_ROW_REST`/`MENU_ROW_ACTIVE` variants hosted in `top-bar-overflow-menu.tsx` (`tabIndex={-1}` per the menu's roving-focus model), so it stays in lockstep with every other overflow-menu row. Returns `null` for a single-view window (mirrors the pill). It is the multi-row `menuRender` case — one registry entry emitting several rows. No inline lens indicator exists — the marked row + the view content itself (chat bubbles vs. terminal) carry lens identity, and the heading stays the static `Window:` in every lens (§ Heading is a static `Window:`).

### Palette parity + keyboard shortcuts (Constitution V)

- **`View: Terminal` / `View: Web` / `View: Code` / `View: Chat` palette actions** (AppShell `viewActions` in `app.tsx`), each visible only when its view is **available AND not the current** (slot-A) view — each applies `single:<view>` through `applyLayout` via `switchView` (§ app.tsx integration). The gating + label + shortcut-hint composition live in a pure builder `buildViewActions(available, resolved, onSwitch)` in `lib/palette-view.ts` (mirrors `lib/palette-move.ts`/`palette-update.ts`, unit-tested in `palette-view.test.ts`). The per-entry `shortcut` hint tracks the binding that reaches it: `View: Chat` (and `View: Terminal` when LEAVING chat) show the chat toggle's combo; every other switch shows the lens cycle's. `buildViewActions` takes those two hint strings as a `ViewShortcutHints` parameter, and `app.tsx` passes the **effective** formatted combos from the keybinding registry, so an override moves the hints (an empty string means "no working chord" and renders no hint); the module defaults (`` Ctrl+` `` / `⌘.`) match the registry defaults for legacy callers and tests. One `buildViewActions` call feeds them; there is no `@rk_type`-mutating `toggle-iframe-terminal` action. The multi-tile affordances (add/close/promote/swap/zoom/shape) carry their own `Layout:` palette entries (§ Surface Layout → palette + chords).
- **The lens cycle chord** (`view-cycle`, default `⌘.` / `Ctrl+.`) is a registry binding on the `cmd` tier with `scope: "terminal"` (§ Keyboard Shortcuts), read through `bindingByAction` by a window-level `keydown` effect in `app.tsx` that `preventDefault()`s (so xterm doesn't also receive it), gates on the shared `shouldSuppressChord`, and calls `switchView(nextView(views, active))` — i.e. it cycles through **single-tile** lens layouts. No-op when < 2 views. The live view/window values and the effective binding are read via refs (`viewCycleRef`, `viewCycleBindingRef`) so the listener is stable across SSE ticks. It keeps its own listener rather than folding into the registry dispatcher because its enablement is lens-local state. `⌘.` is a free binding in the `⌘<punctuation>` family and `Ctrl+.` is inert in readline (unlike `Ctrl+/`→undo); per-device rebinding goes through the override layer.
- **`` Ctrl+` `` toggles tty↔chat** (`chat-toggle`, the `ctrl` tier, terminal scope — `use-chat-view-shortcut.ts`, § Chat View → shortcut): the VS-Code "toggle terminal" binding, speaking `ViewName` (`"tty"`, not `"terminal"`), enabled when `currentViews.includes("chat")`, toggling via `switchView`. Same registry read + shared suppression predicate.

### Heading is a static `Window:` in every lens (spec R4 reversal)

The top-bar center `PageType: name` heading on the terminal route reads a **static `Window: <window>` in every lens** (tty / web / code / chat) — it does NOT follow the active view (§ Window Heading). A single `WINDOW_PREFIX = "Window:"` constant (`top-bar.tsx`) is passed as a constant `prefix` into `WindowHeading` at the terminal-mode center cell; there is no lens-following `terminalHeadingPrefix()` helper and no `WEB_PREFIX`/`CHAT_PREFIX` constants. The heading identifies the WINDOW (the substrate); which lens you look through is shown by the `ViewSwitcher`'s `View:` menu rows in the chevron menu (the switcher is `menuOnly`, §ViewSwitcher chip), not the heading (§ Design Decisions → Heading identifies the window, not the lens). The name's anchor does not jump on a lens switch, since the prefix width does not change with the lens (§ Center-heading stable anchor + nav affordances). The click-to-rename affordance and boot-sweep treatment apply. (`260714-uco1`, `260717-6anu`)

### Internal navigation targets the bare route (spec R6 / surface-layout L4)

Navigating to a DIFFERENT window carries NO layout/view/panel param — each destination window resolves its own layout via the ladder (§ Surface Layout → resolution ladder). Client-side navigations (sidebar/palette/breadcrumb via `navigateToWindow`, plus the window-death redirect and SSE-writeback switch) omit/clear `search`, and TanStack Router's `final` search middleware returns `{}` when `dest.search` is absent, so an outgoing `?layout=` does not leak onto the target. (Per-window localStorage covers the intent; the web-view-lens e2e persistence case exercises this through a real sidebar-click navigation to guard against a future `retainSearchParams`/router-upgrade regression.) History gets "what you saw": layout tweaks `replaceState` (no entry per tweak) while window switches push entries, so back/forward restores the historical arrangement each entry carried (spec L4). The one exception is the `Agent: Next waiting` / WaitingBadge deep-link, which INTENTIONALLY sets `?view=chat` for a chat-capable target — the shim translates it to `single:chat` (§ Chat View → WaitingBadge).

### Window-switch transition classification (one rule)

The window-switch slide transition's `switchTransitionRef` carries a SINGLE `ungatedIds` set: window ids whose EFFECTIVE resolved layout is **not tty-led** — i.e. slot A of `resolveLayout(stored ?? legacy-seed, win)` is a non-`tty` surface, which renders `<IframeWindow>` (web), `<CodeSurface>` (code), or `<ChatView>` (chat), none of which has the terminal's xterm first-write receipt seam, so they use the transition's **ungated capture** (a first-write gate would never release for a non-xterm surface → stuck transition / blank pane). Computed at render time from `resolveLayout(readStoredLayout(server, id) ?? translateLegacyParams(readStoredView(…), readStoredPanel(…)), undefined, win).order[0] !== "tty"` (URL is unknown for a not-yet-navigated target, so only localStorage — the new `rk-layout:` key AND its legacy predecessors via the shim — plus the default hint decide) — the same "keyed on the resolved layout, not `@rk_type`" invariant as the render path (§ Window-Switch Slide Transition).

### e2e — `web-view-lens.spec.ts` / `chat-view.spec.ts` (+ `.spec.md`)

Both suites reach non-tty lenses **only** through the chevron menu's `View:` rows or legacy `?view=` deep links (never an in-bar pill — the switcher is `menuOnly`) and assert that the `view-toggle` testid appears NOWHERE in the DOM at any width; the deep links land through the translation shim, which mirrors the URL back as `?layout=single:<view>` (§ Surface Layout). A shared helper reads a lens row as `controlsMenu(page).getByRole("menuitemradio", { name: "View: {label}" })` and checks the active row's `aria-checked`. `web-view-lens.spec.ts` drives real tmux (own session, `window-heading.spec.ts` pattern), setting `@rk_url`/`@rk_type` via `tmux set-option -w`. Its `describe` defaults to a wide **1440px** desktop width used to clear the whole cluster — chosen so a distinguishing menu-only case is provable: the bar has room, yet the switcher is STILL in the menu. Covers: the `View:` rows appear only on web-capable windows (no `view-toggle` anywhere); flip to web then back via the `View: Web`/`View: Terminal` rows preserves the window and fires **zero `/options` POSTs** (asserted via `page.on("request")`); deep link `?view=web` cold-loads the iframe (lens proven by the iframe + the translated `?layout=single:web` URL + the `View: Web` row marked `aria-checked` — the heading stays the static `Window:` prefix); `?view=web` on a URL-less window falls back to the terminal; legacy `@rk_type=iframe`+url defaults to web with the `View: Web` row marked; last-layout persistence across a real client-side window switch away and back. The 375px test proves the same menu-only surface at phone width (long window name → `View:` rows in the menu, heading room, no horizontal overflow) AND re-asserts no `view-toggle` at desktop width. `chat-view.spec.ts` covers the chat lens through the SAME menu-only switcher: the `View: Chat` row gated on `chatProvider` (no in-bar pill ever), flip to chat via the `View: Chat` row (lens proven via the `chat-view` testid + the translated `?layout=single:chat` URL, not the heading prefix), the `` Ctrl+` `` toggle, `?view=chat` cold-load, the bubble/tool-card/pending renderer, reduced-motion — and the 375px "single-line top bar" test asserts the long-window-name case (`View:` rows in the menu, heading room, single line, no horizontal overflow). It drives chat over the state socket, NOT SSE: it uses the extended `_state-socket-mock.ts` (answers a `kind:"chat"` subscribe with an offset-bearing ack + optional `chat`/`chat-state`/`chat-reset` emits) plus a plain `GET /api/windows/*/chat*` backfill `page.route`; the terminal stub stays on `/ws/terminals` (memory `relay-mux-stale-ws-stub-class` — no `/relay/` or SSE stubs). `connection-budget.spec.ts` uses the any-route ≤2 WS + 0 SSE form with a chat-lens case (architecture.md § Testing). (`260714-uco1`, `260717-6anu`, `260717-vhvz`, `260722-n2n4`)

## Surface Layout (the center is a layout of tiles)

The terminal route's center is a **layout manager** (`docs/specs/surface-layout.md`): a resolved **layout** `(shape, order, ratios)` renders as 1–3 **tiles**, each mounting an existing surface renderer unchanged. This subsumes BOTH the legacy exclusive main-slot lens branch and the right panel's surface slot (`docs/specs/right-panel.md`'s P6 slot mechanics are superseded; the rail, availability derivation, and P4 attention carry forward — § Right Rail). A **surface** is a (substrate, lens) pair of the ROUTE window; the tileable set is `tty | web | chat | code` (`SurfaceKind = ViewName` — § Window Views (Lens Model)); `tty` is `(current window, tty)`, always available, listed first. (`260812-ab5v`) The rail lives in the Shell grid's full-height third column and collapses from a top-bar toggle chip (`260812-nm4p`, § Right Rail) — collapse hides the RAIL only: tiles are content-column state and survive it.

### The model — preset shapes × ordered surfaces × ratios

- **Shapes** (exact URL strings, `SHAPE_ARITY`): `single` (1), `split-h` / `split-v` (2), `row` / `col` / `main-left` / `main-right` / `main-top` (3). Slot A (first in `order`) is the main slot in `main-*` shapes. A layout carries exactly its shape's arity. Max 3 tiles (Constitution IV); presets, never free split trees.
- **Order** — 1–3 surface kinds; kinds MUST NOT repeat within a layout **except `tty`** (the muxed `/ws/terminals` relay supports N clients per pane, so duplicate tty tiles of one window are legal; only the FIRST tty tile receives the shared `wsRef`/`focusRef`).
- **Ratios** — divider positions, per-viewer localStorage ONLY, keyed `rk-layout-ratios:{server}:{windowId}:{shape}` (a ratio is meaningless across shapes), written on drag RELEASE; never in the URL. Clamped by `clampRatio` (`lib/right-panel.ts` — the `clampPanelWidth` approach generalized, `MIN_PANEL_WIDTH_PX` 280px floor).

### Pure layout model — `lib/surface-layout.ts`

New pure, DOM-free module with colocated tests (the `window-view.ts` pattern — § Design Decisions → Layout helpers live in a new pure module). Untrusted URL/localStorage strings are validated by guards on the parse path (no `as` casts):

- **`parseLayout` / `serializeLayout`** — `<shape>:<a>,<b>[,<c>]` round-trips byte-identically; rejects unknown shape/kind, arity mismatch, repeated non-tty kind (returns `null`). `ALL_SHAPES`, `shapesForArity(arity)` (the same-arity ring), `SURFACE_LABEL` / `SHAPE_LABEL` / `SURFACE_GLYPH` — shared copy for tile headers, rail tooltips/aria, palette `Layout:` entries, and the mobile sheet tabs so none drift.
- **`resolveLayout(searchLayout, storedLayout, win)`** — the ladder: (1) valid URL `?layout=` → (2) localStorage `rk-layout:{server}:{@N windowId}` → (3) default-view hint (`hintLayout` wraps `defaultView(win)` as `single:<view>` — a legacy `@rk_type=iframe` window hints `single:web`) → (4) `single:tty`.
- **Availability degradation (`degradeLayout`)** — an unavailable surface drops out TILE-BY-TILE: the remaining order renders in the matching smaller-arity shape (3→2 `split-h`, 2→1 `single`, slot A kept); an unknown shape or fully-invalid value falls through to the ladder's next rung. No broken iframe ever mounts.
- **The permanent translation shim** — `translateLegacyParams(view, panel)`: `?view=X` → `single:X`; `?view=X&panel=Y` → `split-h:X,Y`; bare `?panel=Y` → `split-h:tty,Y`. `seedLayoutFromLegacy(server, windowId)` migrates the localStorage predecessors: when no `rk-layout:` key exists, a stored `runkit-window-view` / `runkit-window-panel` value seeds the equivalent layout (legacy keys left in place — other tabs may be older).
- **Mutations** (all pure): `promote` (surface → slot A; order permutes, shape unchanged), `swapWithNext`, `closeSurface` (arity collapse, slot A kept; `null` when closing the last tile — disallowed), `addSurface` (1→2 `split-h`, 2→3 `main-left`; `null` at 3), `cycleShape` (next same-arity preset, order kept — tmux `next-layout` muscle memory), `setShape`.
- **Storage** — `rk-layout:{server}:{windowId}` (value-bearing, written on USER MUTATION only — spec L3) + the ratios key above; all reads/writes try/catch-noop.

### `app.tsx` integration — ladder, mirroring, write discipline

One layout-state block (§ Window Views → app.tsx integration): resolve via the ladder, then an effect MIRRORS the applied layout into the URL with `replace: true` navigation (`replaceState` — never `pushState` for a layout tweak; the window's DEFAULT layout — `hintLayout`, usually `single:tty` — mirrors as a CLEAN URL with the param DROPPED, the retired `?view=` tty-drops-param convention carried forward, and `applyLayout` applies the same rule on a mutation back to the default; window switches push entries as today, so back/forward restores the historical arrangement each entry carried, spec L4). **`applyLayout(next)` is the ONE mutation path** — `writeStoredLayout` + `replaceState` mirror; tile verbs, rail toggles, the ▦ chip, the `view-cycle` chord, and the ViewSwitcher all funnel through it (L3: localStorage is written on user mutation only, never on merely arriving via a carried `?layout=`). Internal navigation (sidebar, ▾ switcher, palette) targets the bare route (§ Window Views → Internal navigation targets the bare route); layout consumers read resolved state, never parse `location.search` pre-ladder. `togglePanel(surface)` is the rail/palette toggle: open surface → `closeSurface`, closed → `addSurface`, then `applyLayout` (a `null` mutation — closing the last tile — is a no-op). **Rail visibility**: `railOpen` is a ChromeContext boolean persisted to localStorage key `runkit-rail-open` (default `true`, the `sidebarOpen` lazy-read/write-through pattern, desktop-only); it gates the Shell column's `rightPanelVisible` prop — a display-level hide, NEVER an unmount (the column diverges deliberately from the sidebar aside's unmount-on-collapse). The top-bar rail toggle (`onToggleRail`, the same handler the `Panel: Toggle rail` palette entry invokes) flips the raw preference; the pre-layout derived `railOpen || panel open` visibility rule is RETIRED with the panel slot — tiles render in the content column regardless of rail visibility, so `?layout=` deep links, the `⇧⌘.` chord, and the `Layout: …` palette entries are never dead behind a collapse. `<RightPanel>` (rail-only) renders inside the Shell grid's full-height `rightpanel` column (the optional `rightPanelChildren` slot, § Shell Grid Layout), not inside the content `<main>`; the bottom bar + compose strip therefore scope to the content column. (`260812-nm4p`, `260812-ab5v`)

### Tile renderer — `components/surface-layout.tsx`

Presentational by contract (the view-switcher/right-panel precedent): (shape, order) state lives in `app.tsx` and arrives as the `layout` prop; the component owns only transient interaction state — zoom, the focused slot, the in-flight ratio drag, the mount-once bookkeeping. `app.tsx` keys it `` `${server}:${windowId}` `` (a window switch remounts).

- **Tiles** — shape → CSS grid; each tile mounts the existing renderer unchanged: `TerminalClient` (tty), `IframeWindow` (web), `ChatView` (chat — fed the AppShell-owned `kind:"chat"` subscription bundle, § Chat View), `CodeSurface` (code; host reachability selects CONTENT, never availability). On desktop the grid frames tiles as cards — the container carries `gap-[3px] bg-bg-inset` (a 3px inset gutter between tiles) and each tile `border border-border rounded`; divider drag mechanics are unchanged (the 6px hit zones span the gutter). (`260812-wfic`)
- **Tile chrome (header + verbs)** — each tile carries a 30px header (`h-[30px]`, 11px, `bg-bg-card` against the content's `bg-bg-primary`, keeping `border-b border-border`) with: a leading agent-state `StatusDot` on **tty** tiles only (`app.tsx` passes the current `WindowInfo` through a new optional `SurfaceLayout` prop — the sidebar row's dot vocabulary; non-tty kinds and a null window render no dot), the kind's `SURFACE_GLYPH` glyph (`>_` tty, `◫` web, `⌸` chat, `{}` code) before the surface label, and the meta text (git-root basename for code, `@rk_url` host for web) as a truncating inset chip (`bg-bg-inset rounded px-1.5`, 10px). Verb buttons are fixed-size boxed buttons (22×22, `coarse:` 26×26 on touch), **visible at rest** at ~65% opacity, hover giving `bg-bg-inset` + full opacity: **⛶ zoom**, **◧ promote**, **⇄ swap-with-next**, **✕ close** — ✕'s hover turns `text-signal-red`, separated from the safe verbs by a 1px hairline rule. `single` layouts render NO verbs (promote/swap are meaningless, closing the last tile is disallowed); mobile renders no header chrome at all. Verb clicks call parent callbacks that run the pure mutation + persistence/URL mirroring. (`260812-wfic`)
- **Focused tile (accent state)** — exactly one tile is the FOCUSED tile (the one that last received pointer/keyboard interaction): its border and kind glyph turn `accent-green` (the tmux active-pane metaphor), suppressed at arity 1. The focused SLOT is `SurfaceLayout` transient component state (default slot A; falls back to slot A when the focused tile closes or leaves the layout; per-window reset comes free from the `${server}:${windowId}` key — the zoom precedent), reported upward as the focused KIND via `onFocusedKindChange`; `app.tsx` mirrors the kind for the `ttyOnly` shortcut gate and the palette (§ Keyboard Shortcuts → Dispatch seams) and sets focus by kind through a registered `focusTileRef` seam (the `zoomToggleRef` pattern, first slot of that kind). Focus-assignment seams: `pointerdown` (capture) and `focusin` on the tile wrapper, plus `CodeSurface`'s optional `onInteract` callback fired from its contentDocument keydown/pointerdown listeners so editor interaction counts as focus; the seams report the kind SYNCHRONOUSLY (a chord pressed immediately after a click must see the new gate), with the reporting effect kept only for the mount default and the close fallback. (`260812-wfic`)
- **Zoom** — transient component state ONLY: one tile full-center, the others hidden at display level; NO URL/localStorage change; renders only when arity > 1. The palette's `Layout: Zoom`/`Unzoom` entries observe/trigger it through an app-level ref seam (state not lifted — the per-window key keeps the reset). The zoomed tile's header shows the ⛶ glyph in `accent-green` and hides its ◧/⇄ verbs (no-ops on a zoomed render); ✕ stays. (`260812-wfic`)
- **Hide-never-unmount (P3)** — a surface opened earlier this route visit stays mounted (`hidden` class) when closed or zoomed away, so iframe/terminal/chat state survives; bookkeeping is keyed by surface kind and resets per window.
- **Dividers** — drag mutates RATIOS only (never shape/order), persisted per (window, shape) on release; tiles stay LIVE mid-drag (no IntersectionObserver suspension unmount — the board pane-resize bug class, § Boards View → Desktop Relay-Connection Suspension); tile content gets `pointer-events: none` mid-drag so iframes cannot swallow `pointermove` (the RightPanel drag-handle pattern).
- **Mobile (R13)** — below `isMobileViewport()` only ONE slot renders (`mobileActiveSlot`, default slot A): no dividers, no verb chrome, no multi-tile grid (§ Mobile slot-A + sheet tabs below).

### ▦ Layout chip + palette + chords (Constitution V)

- **▦ chip** (`components/layout-chip.tsx`) — terminal-route L1 right-cluster entry (`id: "layout"`, `menuGroup: "view"`; § Chrome (Top Bar) → Right cluster): click opens a popover of the preset-shape glyphs valid for the CURRENT arity (`shapesForArity`), current shape marked (trailing ✓ + `aria-checked`, the macOS menu pattern); clicking a glyph jumps DIRECTLY via `setShape` → `applyLayout`. The popover follows the `SplitControl` direction-menu pattern (outside-mousedown closes, Escape closes + refocuses the trigger). Overflowed, its menu form is one `Layout: <Shape>` `menuitemradio` row per arity-valid shape (`LayoutMenuRows`, the `ViewSwitcherMenuRows` precedent).
- **Cycle chord** — `layout-cycle`, `⌘;` (`code: "Semicolon"`, `cmd` tier, terminal scope — free in every claimed set, not browser-reserved): the next same-arity preset, order kept. The handler reuses the `Layout: Cycle Shape` palette body (`fromPalette("layout-cycle")`), so the body is the Constitution V parity and `withShortcutHints` decorates the entry with the effective combo. The chip's Tip advertises the same registry-derived combo. Per-verb chords (zoom/promote/swap/close) are NOT direct-bound — they ship palette-reachable only (§ Design Decisions → Layout verbs are palette-reachable; only the shape cycle is a direct chord).
- **Palette** — the pure builder `buildLayoutActions(layout, availableSurfaces, opts)` (`lib/palette-layout.ts`, the `buildViewActions` precedent, colocated tests): `Layout: Add <Surface>` per available-not-open surface (omitted at 3 tiles), `Layout: Close <Surface>` per open kind (omitted on `single`), `Layout: Zoom`/`Unzoom` (label gated on the observed zoom state), `Layout: Promote <Surface>` / `Layout: Swap <Surface>` per open kind, per-shape jumps for the current arity, and `Layout: Cycle Shape`. `Layout: Focus <Surface>` entries (one per open NON-focused kind, desktop multi-tile only — hidden at arity 1 and on mobile) invoke a focus-by-kind callback that `app.tsx` routes through the `focusTileRef` seam (`260812-wfic`). The ⇧⌘. `panel-toggle` chord (toggles the first non-tty surface's tile, § Right Rail) is documented via the target surface's Add/Close entry hint.

### Mobile slot-A + sheet tabs (R13)

Below `isMobileViewport()` the layout manager renders only slot A full-width; the remaining resolved surfaces are reachable as TABS in a full-height sheet (`components/mobile-surface-sheet.tsx`) opened from a ▦ chip in the bottom bar (`bottom-bar.tsx`, present only on the mobile terminal route with a multi-tile layout). Every open surface is listed — the shown one included, marked pressed — so the sheet is also the way BACK to slot A (a remaining-only list would be a one-way door). Selecting a tab swaps which surface renders via TRANSIENT app-level state (`mobileActiveTile`) — never a layout mutation (no URL/localStorage write, the same discipline as zoom): the layout stays desktop's arrangement, and a phone arriving at a 3-tile `?layout=` URL shows slot A and offers the rest as tabs. The sheet is a backdrop + `role="dialog" aria-modal="true"` panel, focus-trapped (`useFocusTrap`), Escape and backdrop-tap close.

### e2e — `surface-layout.spec.ts` (+ `.spec.md`)

Real-tmux e2e on the isolated port-3020 server (the `web-view-lens.spec.ts` `_tmux.ts` pattern): legacy `?view=`/`?panel=` deep links resolve identically through the shim (URL rewritten to `?layout=`); the ladder (URL > localStorage > hint > `single:tty`) with `replaceState` mirroring; building a 3-tile layout via rail toggles + tile verbs only (no drag) and rearranging it (promote/swap/close outcomes); refresh restores; A→B→A window-switch round-trips restore each window's own layout; back/forward restores historical layouts; divider-ratio persistence keyed per (window, shape); the 375px mobile slot-A + sheet-tabs branch. **Perf budget (binding)**: the plaintext e2e origin is HTTP/1.1 with a 6-slot connection pool (spec § Performance note; the board-route postmortem class) — only ONE test mounts 3 tiles, every other flow stays at ≤2 tiles. The other panel/view suites (`right-panel.spec.ts`, `code-surface.spec.ts`, `web-view-lens.spec.ts`, `chat-view.spec.ts`, and companions) exercise the layout model (constitution: every `.spec.ts` ships its `.spec.md`).

## Right Rail (Open-Tile Toggles)

The right panel's surviving chrome is the **rail** — surfaces themselves render as layout tiles (§ Surface Layout). `components/right-panel.tsx` is rail-only (the file name and the `right-panel-rail` testid are kept — e2e depends on them): a fixed `w-[38px]` vertical strip on the terminal route's right edge, always rendered on desktop. Presentational by contract (unchanged): tile state lives in `app.tsx`; the component owns only the rail render. (`260812-ab5v`) The top-bar rail toggle (`260812-nm4p`) collapses the rail column at display level; e2e covers the toggle on a plain zero-surface window, collapse-grows-terminal with `runkit-rail-open` persistence, tile+`?layout=` survival across a collapse, the `⇧⌘.` chord while collapsed (tile opens, rail stays hidden), the full-height column (rail to shell bottom; bottom bar scoped to the content column), and a legacy `?panel=` deep link on a collapsed rail (`right-panel.spec.ts`; per-test top-frame-guarded pref reset — init scripts run for every frame and the web tile's same-origin `/proxy/` iframe shares the origin's localStorage).

- **Open-tile toggles** — one focusable `Tip`-wrapped button per AVAILABLE surface (`availableSurfaces`, `tty` first), LIT (`aria-pressed`, inverse-video `bg-accent-green text-bg-primary`) for every OPEN tile (`layout.order`). Clicking an unlit button ADDS the surface (`addSurface` — 1→2 `split-h`, 2→3 `main-left`); clicking a lit one CLOSES its tile (`closeSurface` — the arity collapse). The caller (`app.tsx`'s `togglePanel`) runs the pure mutation + persistence/URL mirroring; a disallowed mutation (closing the last tile) is a `null` no-op there. At 3 open tiles the remaining unlit buttons render DISABLED with a "Close a tile first" tooltip (the max-3 constraint must be visible, never a silent no-op); the `Tip` wraps a span so the disabled button's tooltip still fires (disabled buttons swallow pointer events).
- **Icon glyphs replace text labels** — `>_` tty, `◫` web, `⌸` chat, `{}` code (the shared `SURFACE_GLYPH` map in `surface-layout.ts`); the previous text labels moved into the `Tip` tooltips and the `<Label> tile` aria-labels (e2e hooks).
- **Availability + attention dots** — unchanged (right-panel P4): a collapsed or absent tile may hide content, never state that wants a human.
- **`tty` rail button** — the tty surface's toggle, present on every window (tty is always available).
- **`lib/right-panel.ts`** keeps the SHARED surface registry — `type SurfaceName = SurfaceKind` and `availableSurfaces(win)` (`tty` first, then `web`/`chat`/`code` per the `window-view.ts` capability predicates) — so rail, layout, and switcher read one availability source, plus `clampRatio`/`MIN_PANEL_WIDTH_PX` for the tile dividers. The panel-resolution helpers (`resolvePanel`, `writeStoredPanel`/`removeStoredPanel`, the `runkit-panel-width` machinery) have no production callers — `readStoredPanel` survives only as a legacy-seed source for the shim; all are Phase-3 retirement-sweep deletion candidates (`fab/changes/260812-ab5v-surface-layout-core/plan.md` § Deletion Candidates).
- **`⇧⌘.` (`panel-toggle`)** — toggles the first NON-TTY available surface's tile via `togglePanel` (gated on a desktop window route with ≥1 available non-tty surface; § Design Decisions → The panel toggle takes the shifted tier of `⌘.`).

### e2e — `right-panel.spec.ts` / `code-surface.spec.ts` (+ `.spec.md`)

Both suites exercise the layout model on the same real-tmux port-3020 rig: rail-button gating by capability, icon glyphs with label tooltips, toggle add/close (1→2 `split-h`, 2→3 `main-left`), disabled+tooltip at 3 tiles, legacy `?panel=web` deep links resolving through the shim, tile hide-never-unmount, divider drag with the same xterm instance surviving, and the 375px viewport rendering no rail. `code-surface.spec.ts` keeps the **stub HTTP server** on the preset port (`scripts/test-e2e.sh` exports `RK_CODE_SERVER_PORT`, default 3939, to both the dev backend and the playwright run — the preset wins over the `RK_PORT+2` convention, so the backend's `/code/*` forwards to the stub; code-server itself is not installable in the test env): the rail button + `View: Code` menu row appear only for a git-repo-cwd window (never for a `/tmp`-cwd window — `_tmux.ts`'s `newWindow` takes a `cwd` option for that); the code tile iframe's `src` is the stable relative `/code/?folder=<git root>` shape; the bare `/code` request 308-redirects to `/code/`; the not-running empty state renders when the stub is down; `?view=code` and `?panel=code` deep links both resolve through the shim; and the keyboard-spike case — a registry chord (Ctrl+K) pressed with focus INSIDE the same-origin iframe reaches the parent and opens the palette.

## Chat View

A **read-only HTML chat view over the SAME agent pane** — a second view over the tmux pane, never a substrate (the pane stays the agent's parent, Constitution VI; the view only renders the streamed transcript). It is the **`chat` lens** of the window-view model (§ Window Views (Lens Model)) — a `chat` TILE in the surface layout on the existing `/$server/$window` terminal route (Constitution IV, no new route; a legacy `?view=chat` deep link still works — the shim translates it to `single:chat`, § Surface Layout). **All view-state plumbing — the `?layout=` param + shim, availability gate, the ViewSwitcher (its `menuOnly` `View:` menu rows), `rk-layout:` localStorage persistence, palette parity, the `` Ctrl+` `` shortcut, and the switch-transition classification — is the UNIFIED machinery** described in § Window Views (Lens Model) and § Surface Layout; this section owns only what is chat-SPECIFIC: the `chatProvider` gate, the connection-dot semantics, the renderer/stream, and the WaitingBadge deep-link. (The heading is a static `Window:` in every lens, § Heading is a static `Window:`.) The renderer + data layer live in `architecture.md`/[chat](/run-kit/chat.md) § Chat View Frontend. The chat stream is a `kind:"chat"` subscription on the shared state socket (NOT an `EventSource`) — the owner hook is `use-chat-subscription.ts`, and the connection dot below derives from socket-connected AND chat-acked, not `EventSource` health. (`260714-uco1`, `260717-vhvz`)

**The `chatProvider` gate.** `WindowInfo` carries `chatProvider?: string` + `chatSessionRef?: string` (`types.ts`) — the window-level `@rk_chat` rollup the backend emits (typed, not newly parsed). **A non-empty `chatProvider` is the SOLE availability gate** for the chat lens (`hasChat(win)` in `window-view.ts`) — mirroring the backend's own `resolveWindowChat` gating — so it transitively gates every chat affordance: the ViewSwitcher `View: Chat` menu row, the `View: Chat` palette action, the `` Ctrl+` `` shortcut (enabled iff `currentViews.includes("chat")`), the chat tile's `<ChatView>` renderer, and the deep-link append. (The heading is NOT among them — it is a static `Window:` in every lens, § Heading is a static `Window:`.) There is NO separate `chatAvailable` variable — `resolveLayout`/`availableViews` fold the gate in (an unavailable `chat` in a layout degrades tile-by-tile, § Surface Layout).

### View state = the unified lens machinery (NOT a chat-specific mechanism)

The chat lens runs entirely on the unified lens + layout machinery (§ Window Views (Lens Model), § Surface Layout), with no chat-specific view-state module:
- Resolution is `resolveLayout` (URL `?layout=` → `rk-layout:` localStorage → default hint → `single:tty`); `lib/router-url.ts`'s single `validateTerminalSearch` still accepts `?view=chat`, which the shim translates to `single:chat`. There is no `lib/chat-view-resolve.ts`.
- Persistence is the value-bearing `rk-layout:{server}:{windowId}` key (the legacy `runkit-window-view:` key is only a shim seeding source). There is no `runkit:chat-view:` key-presence pref hook.
- The switcher is the generic `ViewSwitcher`, rendered as its `menuOnly` `View: Terminal`/`View: Chat` menu rows (§ ViewSwitcher chip) for a chat-only-capable window; flipping is `switchView("chat")`/`switchView("tty")` — i.e. applying `single:chat`/`single:tty`. There is no `SegmentedViewToggle`.
- `chatViewActive = layout.order.includes("chat")` is the render + stream gate (`resolveLayout` already gated on `chatProvider`) — true whenever a chat TILE is open, slot A or not.

### Center heading `Window: <window>` (static in the chat lens too)

The `chat` lens does NOT change the heading prefix — the top-bar heading is a static `Window: <window>` in every lens (§ Heading is a static `Window:`; § Window Heading for the boot-sweep/rename mechanics). The `<ChatView>` renderer preserves the inline-rename affordance (the heading IS the rename surface, especially on mobile) — the same parametrized `WindowHeading`, no fork. (`260714-uco1`)

### Renderer swap + chat stream (`app.tsx`)

In the `windowParam` render arm, `<ChatView>` mounts as the chat TILE inside `<SurfaceLayout>` (fed the AppShell-owned subscription bundle — the tile mount reads like the legacy lens branch, § Surface Layout → tile renderer). `AppShell` owns the single owner-hook call — **`useChatSubscription(server, windowId)`**, opened only when `chatViewActive` (else passed empty strings so the hook idles) — and passes `{events, pending, connected, error}` into the tile. ONE chat subscription on the shared state socket feeds both the renderer and the connection dot (below). The chat pane joins the switch-transition **ungated capture** through the unified `ungatedIds` classification (any non-`tty`-led resolved layout; § Window Views → Window-switch transition classification) — `<ChatView>` has NO xterm first-write seam, so a first-write gate would never release. (`260717-vhvz`)

### Connection dot = chat stream health in chat mode (R9)

"Dot = per-page live-data health" (§ Chrome (Top Bar) → connection dot). In chat view, the `isConnected` `AppShell` passes to `<Sidebar>` reflects the **chat subscription's** health instead of the per-server sessions slice: `dotConnected = chatViewActive ? chatStream.connected : isConnected`. **Since `260717-vhvz` `chatStream.connected` = (state socket connected) AND (this window's `kind:"chat"` subscription acked)**, keeping the established 3s disconnect debounce (from `use-chat-subscription`, was the `EventSource`-health form in `use-chat-stream`). In terminal/web view the value is unchanged.

### WaitingBadge click + `Agent: Next waiting` deep-link into `?view=chat`

`WaitingBadge` (`components/waiting-badge.tsx`) gained an **optional** `onClick` prop: when passed, the badge renders as a `<button>` (rk-glint hover, `stopPropagation` so it doesn't bubble to the parent row) that navigates to the next waiting window in its scope; when absent, it keeps today's **display-only** `<span>` (so the board-header / Host-page-tile / server-panel mount sites, which pass no onClick, are unchanged). It is currently wired ONLY at the **sidebar session row** (`session-row.tsx` → `sidebar/index.tsx` → `AppShell`'s `handleWaitingBadgeClick`) — the one surface with a navigable per-session scope.

`handleWaitingBadgeClick(srv, sess)` (`app.tsx`) builds the clicked session's ordered waiting windows and calls **`nextWaitingTarget(ordered, server, windowParam)`** (`lib/palette-agent-nav.ts` — REUSED, not re-implemented; the R12/A-024 review-rework fix: the first cut picked the session's FIRST waiting window via `windows.find(isWaiting)`, so clicking the badge while already ON its first waiting window was a no-op instead of advancing to the next). It appends `?view=chat` via the pure `chatSearchForTarget(hasChat)` helper (`lib/palette-agent-nav.ts`, unit-tested) — `{ view: "chat" }` iff the target window has a `chatProvider`, else empty search (the target resolves its own pref; the shim translates the param to `single:chat`, § Surface Layout). This is the ONE intentional `?view=` carry across a window switch (§ Window Views → Internal navigation targets the bare route). The existing `Agent: Next waiting` palette action gets the **same** `chatSearchForTarget` append rule for any target it navigates to.

### Connection dot = chat stream health in chat mode (R9)

"Dot = per-page live-data health" (§ Chrome (Top Bar) → connection dot). In chat view, the `isConnected` `AppShell` passes to `<Sidebar>` reflects the **chat subscription's** health instead of the per-server sessions slice: `dotConnected = chatViewActive ? chatStream.connected : isConnected`. **Since `260717-vhvz` `chatStream.connected` = (state socket connected) AND (this window's `kind:"chat"` subscription acked)**, keeping the established 3s disconnect debounce (from `use-chat-subscription`, was the `EventSource`-health form in `use-chat-stream`). In terminal/server view the value is unchanged.

### WaitingBadge click + `Agent: Next waiting` deep-link into `?view=chat`

`WaitingBadge` (`components/waiting-badge.tsx`) gained an **optional** `onClick` prop: when passed, the badge renders as a `<button>` (rk-glint hover, `stopPropagation` so it doesn't bubble to the parent row) that navigates to the next waiting window in its scope; when absent, it keeps today's **display-only** `<span>` (so the board-header / Host-page-tile / server-panel mount sites, which pass no onClick, are unchanged). It is currently wired ONLY at the **sidebar session row** (`session-row.tsx` → `sidebar/index.tsx` → `AppShell`'s `handleWaitingBadgeClick`) — the one surface with a navigable per-session scope.

`handleWaitingBadgeClick(srv, sess)` (`app.tsx`) builds the clicked session's ordered waiting windows and calls **`nextWaitingTarget(ordered, server, windowParam)`** (`lib/palette-agent-nav.ts` — REUSED, not re-implemented; the R12/A-024 review-rework fix: the first cut picked the session's FIRST waiting window via `windows.find(isWaiting)`, so clicking the badge while already ON its first waiting window was a no-op instead of advancing to the next). It appends `?view=chat` via the pure `chatSearchForTarget(hasChat)` helper (`lib/palette-agent-nav.ts`, unit-tested) — `{ view: "chat" }` iff the target window has a `chatProvider`, else empty search (the target resolves its own pref). The existing `Agent: Next waiting` palette action gets the **same** `chatSearchForTarget` append rule for any target it navigates to.

## Boards View

`/board/$name` renders a horizontal pane dashboard for windows pinned to a named board (see `tmux-sessions.md` § Pin Sessions / § `@rk_board` for storage and `architecture.md` § Boards Feature for the route placement rationale). The board view does NOT mount AppShell, but it shares the same root-mounted multi-server `SessionProvider`, the same unified `<Sidebar>`, and the same `<Shell>` grid wrapper — the sidebar's per-server session groups stay populated across the route switch because the provider lives at the root. (`260509-17m3`)

### SESSIONS + BOARDS dual presence (a pinned window is LINKED, not moved)

Pinning a window **links** it (`tmux link-window`) into a single-window pin-session (`_rk-pin-<id>`) while it stays a member of its home session, so a pinned window appears on **both** surfaces at once — in the SESSIONS sidebar at its real home index AND as a pane on the board. This is honestly tmux-derived (Constitution II): the SSE session snapshot lists the window under its home session (only the `_rk-pin-*` session name is filtered at the `parseSessions` chokepoint, not the window), so the sidebar shows it with no frontend synthesis. Two ambient dual-residence affordances make the second home legible: the board pane header shows a `{session} › {window}` home crumb (§ Board Tile Header) and the sidebar pin indicator surfaces a "Go to {board}" navigation row (§ Sidebar Pin Icon on Window Rows). The pin-icon filled state and the active-board accent (below) are the board-aware affordances on a SESSIONS row, which stays visible while pinned. (Legacy move-based pins do not appear in SESSIONS while pinned — the window isn't in its home session under the old model; they convert to dual presence on unpin + re-pin.) (`260718-co9z`)

### BoardPage Layout (`app/frontend/src/components/board/board-page.tsx`)

BoardPage uses the shared `<Shell>` wrapper. The Shell grid carries NO `topbar` row — the desktop grid is `grid-template-areas: "sidebar content" / "sidebar bottombar"` (rows `1fr auto`; collapses to single-column `"content" "bottombar"` on `< 640px` with the sidebar overlay); the TopBar is mounted once at the persistent root layout above the Shell, not as a grid child (see § Shell Grid Layout / § Chrome (Top Bar) → persistent root mount). Children are placed into named grid areas: (`260707-4vq2`)

- **Sidebar** (`gridArea: "sidebar"`) — the unified `<Sidebar currentServer={null}>` (same component as AppShell). No per-server group is marked current on board routes. Per-server session groups + Boards section + ServerPanel + bottom panels render as on AppShell. Mobile (`< 640px`) renders the sidebar as a Shell-level overlay whose grid child spans `gridRow: "1 / 3"` (content + bottombar; the Shell grid has no topbar row) — same overlay implementation as AppShell (`260707-4vq2`)
- **TopBar** — NOT a Shell grid child. `BoardPageContent` registers the board extras (`boardName: name`, `paneCount: entries.length`, `serverCount`, `waitingPaneCount`, `boards`, `focusedPane` (the focused-tile split target), `onRequestKill: requestKillFocused` (the top-bar menu's Kill row opens the kill confirm dialog; post-kill refetch rides `executeKillWindow`'s own `onSettled`, not a top-bar callback), `sidebarOpen`, `onToggleSidebar`) into the slot context via `useRegisterTopBarSlot`. Its `boardConnected` AND-over-attached-servers value goes to `<Sidebar isConnected={…}>` as a prop, not through the slot (§ Design Decisions → Connection state reaches the Sidebar as a prop). The persistent `RootTopBar` derives `mode="board"` + `boardName` from the route and renders the board bar fed by those extras (see § Board and § center cell for the current board rendering) (`260707-4vq2`, `260718-co9z`)
- **Content** (`gridArea: "content"`) — the `DesktopRow` (desktop) / `MobileCarousel` (mobile) horizontally-scrollable container of pane "cards" sorted by `orderKey`. Each card is a `BoardPane` with a `BoardHeader` (the `{session} › {window}` home crumb — see § Board Tile Header — + the pin-glyph unpin button) and an embedded `TerminalClient` streamed over the shared `/ws/terminals` mux keyed by `windowId` (one stream per pane) — **the same `TerminalClient`/relay path as the normal terminal route**. The relay attaches the window's `_rk-pin-*` pin-session server-side (the pin-session-first attach preference), so `BoardPane`/`board-page.tsx` need no structural change for the attach — `BoardEntry` shape unchanged. The horizontal-scroll viewport begins at the `content` grid area's left edge — flush with `sidebar.right` (or page.left when `sidebarOpen === false`); no left gutter for board-level chrome (`260717-803u`, `260718-co9z`)
- **BottomBar** (`gridArea: "bottombar"`) — the shared `<BottomBar>` (NEW on this route — board route had no BottomBar pre-rotation). Byte-identical to AppShell's invocation: same three callbacks (`onOpenCompose`, `onFocusTerminal`, `onScrollLockChange`). `onOpenCompose` is `toggleComposeStrip` (the `>_` chip toggles the docked compose strip preference — § Docked Compose Strip); `onFocusTerminal` invokes a ref-tracked `focusFocusedPaneRef.current()` that re-focuses the currently-focused board pane via its `paneRefs[focusedIndex].focus()`; `onScrollLockChange` plumbs through `DesktopRow`/`MobileCarousel` → `BoardPane` → `TerminalClient.scrollLocked`. Input target is the focused pane's wsRef (read from `FocusedTerminalContext.focused?.wsRef`). The docked compose strip renders in this footer above `<BottomBar>` when the preference is on (`{composeStripEnabled && <ComposeStrip />}`)
- **Palette via the slot, no own mount** — BoardPage publishes its board-scoped entries (Switch / Leave Board View / Cycle Pane Focus →/← / …) via `useRegisterPaletteActions(boardRouteActions)`; the single `AppLayout`-mounted `<CommandPalette>` renders them ahead of the global groups (§ Single palette mount + palette-actions slot)

Empty / non-existent board (`name` exists in URL but `getBoard` returns `[]`): shows `No panes pinned to this board yet — hover a sidebar window row and click its 📌, or {paletteChord} → Pin:` with a back link — `{paletteChord}` is the host-effective `command-palette` binding via `useKeybindings()` + `formatCombo`, the `, or {paletteChord} → Pin:` clause omitted when that binding is unbound/disabled (§ Education micro-copy).

### Pane Cards (Desktop)

- **Default width 480px**, drag-resizable between 280px (min) and viewport-minus-sidebar (max). Resize handle hidden on coarse-pointer devices
- **Persisted per-board** in `localStorage["runkit:board-widths:<name>"]` as `Record<windowId, number>`. `usePaneWidths(boardName)` encapsulates read/write/clamp; missing entries fall back to 480px; malformed JSON is ignored silently. Pane widths are intentionally browser-local view state — they do NOT cross devices (pin state in tmux does)
- **Click-to-focus** transfers focus to the pane's xterm via a `useImperativeHandle`-exposed `focus()` method on `BoardPaneHandle`. **Hover-to-focus is OFF in v1** (no hover handler attached)
- **Visual focus indicator** — focused pane has a distinct border/glow; unfocused panes are de-emphasized
- **Keyboard pane cycling** — `Cmd+]` / `Ctrl+]` next (wraps), `Cmd+[` / `Ctrl+[` previous (wraps). Bound via a `useEffect` keydown listener on the BoardPage component
- **Autofit override** — when the per-board autofit toggle is ON, desktop panes ignore the stored pixel width and become equal-share flex items, and the resize handle is hidden. See § Board Autofit Toggle. Autofit never mutates `runkit:board-widths:<name>`, so toggling off restores the exact hand-tuned widths above.

### Board Kill Confirm Dialog + Verb Discipline

The board surfaces two distinct destructive-vs-safe actions with disciplined verbs — **Unpin** (safe, reversible — removes the window from the board, it stays in its `{session}`) and **Kill** (destructive — closes the window everywhere, including its home session). Board surfaces (tooltips, aria labels, palette entries) do not use the "close"/"close pane" verb; sidebar and terminal-mode ✕ vocabulary/semantics are out of scope.

**Kill is confirm-gated** (`board-page.tsx`). The board top-bar's chevron-menu **Kill row** (via the slot's `onRequestKill`) and the `board-kill-focused` palette entry both call `requestKillFocused`, which captures the focused entry as `killTarget` (`{server, windowId, windowName, home}`) and opens a `Dialog` — they never fire immediately:

> **Kill `{window}`?** This closes it everywhere — including session `{home}`.
> `[Unpin instead]` `[Kill]` `[Cancel]`

- **`{home}` derivation + fallback**: `home` is resolved at open time from the `homeSessionByKey` snapshot join (§ Board Tile Header — the same `ctx.sessionsByServer` join the header crumb uses), read through a ref (`homeSessionByKeyRef`) so `requestKillFocused` (declared above the memo) sees the current map. When `home` is not derivable (legacy pin / home died — window absent from the snapshot) the copy falls back to window-only ("This closes the window everywhere.").
- **Default focus on the SAFE action**: `[Unpin instead]` is the FIRST focusable element, so `Dialog` auto-focuses it; the dialog is fully keyboard-operable (Escape/Tab/backdrop via the shared `Dialog`).
- **Confirmed Kill is a WINDOW-kill**: `[Kill]` calls `executeKillWindow` (`useOptimisticAction` over `killWindow` → `POST /api/windows/{id}/kill`), destroying the window entity everywhere (home + pin links) in one call. Its `onSettled` schedules the board refetch (the self-heal for the no-`board-changed`-on-kill gap).
- **`[Unpin instead]`** invokes the shared unpin path (`unpin(server, windowId, board)`), the same reversible action the tile-header pin-glyph and `Board: Unpin Focused Pane` use — no confirmation.

The tile-header pin-glyph unpin stays **unconfirmed** (unpin is cheap to reverse). See § Chrome (Top Bar) → Close pane / Kill for the menu row's `onRequestKill` routing and § Boards Command Palette for the `board-kill-focused` entry.

### Board Autofit Toggle

A per-board **desktop** toggle that stretches panes to fill the row instead of using explicit pixel widths — the minimal-surface answer (Constitution IV: one button + one palette action, no settings page) to boards chronically under-using screen width with 2–3 panes on a wide monitor. Desktop `DesktopRow` only — the `MobileCarousel` is already one full-width `w-full` pane, so autofit is a **no-op on mobile** (no branch there).

**Persistence — `useBoardAutofit` (`app/frontend/src/hooks/use-board-autofit.ts`)**, mirroring the guard discipline of `use-pane-widths.ts`:

- Key `runkit:board-autofit:<board>` (exported prefix `BOARD_AUTOFIT_LOCALSTORAGE_PREFIX = "runkit:board-autofit:"`), **per-board** — enabling autofit on board A does not affect board B, and switching `board` reloads state via a `useEffect`-on-`board` (the same reload seam `usePaneWidths` uses).
- **"on" sentinel with key removal on off**: the literal string `"on"` is the ONLY value that reads as on; anything else (absent, malformed) reads off (window-undefined bail + `try/catch`, malformed-tolerant like `use-pane-widths.ts`). Turning autofit OFF **removes the key** (`localStorage.removeItem`) rather than storing `"off"` — the read is `getItem(...) === "on"`, so there is no `"off"` state to persist.
- **Default off** (no key stored → current per-pane-width behavior); the key is untouched until the setter runs.
- API: `useBoardAutofit(board) → { autofit, toggleAutofit }`; `toggleAutofit` flips + persists and round-trips across a fresh hook instance / reload.

**Layout branch — `DesktopRow` / `BoardPane` (`board-page.tsx`, `board-pane.tsx`)**. `BoardPage` owns `useBoardAutofit(name)` and passes `autofit` down to `DesktopRow`, which forwards it to each `BoardPane` and passes `showResizeHandle={!autofit}`:

- **Autofit ON**: `DesktopRow` passes **no pixel `width`** (`width={autofit ? undefined : getWidth(entry.windowId)}`); each pane root becomes `flex: 1 1 0` with `min-width: max(BOARD_PANE_MIN_WIDTH, calc(25% - 3px))` (import the `BOARD_PANE_MIN_WIDTH` constant from `use-pane-widths` — 280px — never hard-code) inside the existing `overflow-x-auto flex gap-1 p-1` row. Classes drop `w-full`/`shrink-0`; the flex+min-width in `style` drives sizing. Result: ≤4 panes stretch to share the row equally with no horizontal scrollbar; >4 panes floor at the 25% arm and the row scrolls exactly as today. Pure CSS — **no ResizeObserver on the row** — so window resize reflows live for free.
  - **Gap-adjusted floor** (DD): the literal `max(280px, 25%)` from the backlog would force `4×25% + 3×4px (gap-1) > 100%` → a 12px scrollbar at exactly 4 panes, contradicting the "max 4 visible" intent. `calc(25% - 3px)` (3 gaps × 4px ÷ 4 panes) makes exactly 4 panes fit flush.
  - **Verified** the percentage `min-width` resolves against the flex container's content box (the scrollport), NOT the scrolled content width — the containing block of a flex item is its flex container's content box, and `overflow-x: auto` does not change that. The backlog's JS-measurement fallback is therefore unneeded; the e2e asserts the resulting widths as a safety net.
- **Autofit OFF**: **exactly** the prior per-pane-width behavior — explicit pixel widths from `usePaneWidths.getWidth`, `shrink-0`, resize handles present.
- **Non-destructive round-trip**: the stored `runkit:board-widths:<board>` map is **never touched** by autofit (the `width` prop is merely not passed while on; `getWidth`/`setWidth` are not called through the autofit branch), so toggling OFF restores the exact hand-tuned layout.
- **`autofit?: boolean` prop on `BoardPane`** (not a wrapper div — DD): the pane root is the row's direct flex child AND the `rootRef` IntersectionObserver target (§ Desktop Relay-Connection Suspension), so a wrapper would break both contracts; the flag follows the existing optional-`width` pattern.

**Controls (Constitution V parity)** — a top-bar button and a palette action flip the same state:

- **Top-bar `BoardAutofitToggle`** (`top-bar.tsx`): a **board-only** toggle and the L2 tier's only IN-BAR member, gated `mode === "board" && onToggleAutofit`. It is a per-board working control rather than a sticky per-device preference, which is why it stays in-bar while Aa / fixed-width / Kill are `menuOnly`. Mirrors the `FixedWidthToggle` vocabulary — `aria-pressed={autofit}`, `rk-glint` CRT-glint hover, the shared fixed-size button token. See § Chrome (Top Bar) → Right cluster (L2). Its menu counterpart is the `AutofitMenuRow` "Autofit panes" checkbox row when it overflows.
- **Palette action** `Board: Toggle Autofit (on/off)` (`id: "board-toggle-autofit"`, `boardRouteActions` in `board-page.tsx`, label reflects state) → `toggleAutofit`.
- **State plumbing** — `BoardPage` publishes `autofit` + `onToggleAutofit: toggleAutofit` into the top-bar slot context (`useRegisterTopBarSlot`, the same channel carrying the board `focusedPane` + `onRequestKill` kill-dialog seam); `top-bar-slot-context.tsx` carries two optional board-mode fields `autofit?` / `onToggleAutofit?`; `RootTopBar` (`app.tsx`) maps `slot?.autofit`/`slot?.onToggleAutofit` → `TopBar` props (tolerant-empty — absent ⇒ no toggle rendered). (`260718-co9z`)

**Alignments (no code change)**:

- **`MAX_LIVE_RELAY_PANES = 4`** (§ Desktop Relay-Connection Suspension) equals the 4-visible max the 25% floor produces, so on plaintext origins autofit never forces a visible-but-paused pane; the focused pane is exempt from the cap anyway (`selectLivePanes`). >4 panes still overflow horizontally and off-screen panes still suspend.
- **xterm refit rides the existing `terminal-client.tsx` ResizeObserver** (no autofit-specific wiring): a container `ResizeObserver` rAF-debounces `fitAddon.fit()` + a paired `{type:"resize",cols,rows}` WS message; flex reflow (window resize, pin/unpin, toggle flips) changes each visible pane's size and fires that observer per pane.

**Tests**: unit `use-board-autofit.test.ts` (default-off, "on"-sentinel round-trip + key removal, per-board isolation, board-switch reload, malformed tolerance); Playwright `tests/e2e/board-autofit.spec.ts` + companion `.spec.md` (equal-fill/no-scroll at 2–3 panes, ~25%-floor/scroll at 5+, per-board persistence across reload, handles-hidden-while-on, top-bar button + palette action parity) — run via `just test-e2e`/`just pw` only.

### Board Pane Reorder

Desktop board panes are drag-reorderable, with full Cmd+K keyboard parity (Constitution V). Panes render in server `orderKey` order (`GET /api/boards/{name}`); reorder writes a new fractional-index key via `POST /api/boards/{name}/reorder` (see `architecture.md` § Boards Feature). **`MobileCarousel`** — swipe is navigation there, not reorder.

**Adapts the § Server-tile drag reorder pattern** (`use-server-reorder.ts`) into a sibling hook `useBoardPaneReorder(entries, board, reorder)` (`app/frontend/src/hooks/use-board-pane-reorder.ts`), inheriting its two hard-won fixes (derive-over-store override, self-target dragover acceptance). Key differences from server/session reorder:

- **Custom MIME `application/x-board-pane-reorder`** (`BOARD_PANE_REORDER_MIME`) — must not collide with `application/x-server-reorder`, `application/x-session-reorder`, or the window-move default-JSON payload. Guarded FIRST, before any acceptance (the shared guard-order invariant — see § Server-tile drag reorder).
- **Composite `server:windowId` key** — a board spans servers, so `windowId` alone is ambiguous; the override array, the drag payload, and the DnD/palette bookkeeping all key by `server:windowId` (matching the `key={`${entry.server}:${entry.windowId}`}` in `DesktopRow`).
- **Split handle/drop props** — `getHandleProps(server, windowId)` returns `{ handle, drop }` separately: `handle` (`draggable` + `onDragStart`/`onDragEnd`) goes on the pane HEADER only (`BoardHeader`) so a live xterm terminal never hijacks the drag or becomes the drag image; `drop` (`onDragOver`/`onDrop`) goes on the pane ROOT (`BoardPane`) so the preview updates and a release commits anywhere over a pane, not only the ~24px header strip. The header unpin button (a pin-glyph, § Board Tile Header) opts out (`draggable={false}` + `stopPropagation`) and the resize handle lives on the pane root outside the header, so neither is shadowed by the drag handle.
- **ONE `reorderPin` POST per move, no debounce** — fractional indexing means one authoritative call per completed move (`before`/`after` = the moved pane's new neighbours' windowIds, `null` at edges), so the drop is the single commit point. This is the deliberate contrast with server/session reorder's 250ms debounced full-order writes + drop-flush (there is no full-order sweep to coalesce). The `board-changed` SSE echo (refetched by `useBoardEntries`, 50ms debounce) reconciles the override away.
- **Committed-drop flag drives cancel-revert** — `committedRef` is set true only when `onDrop` fires the POST. `onDragEnd` clears the override when uncommitted (Escape / release outside any pane = revert to authoritative order, no phantom unpersisted order) but keeps it when committed (no snap-back — the SSE echo reconciles it). This is the one place the board hook diverges from the no-drag-end-clear discipline of server/session reorder: with drop as the single commit point, native HTML5 cancel semantics = revert, so the no-snap-back rule applies only to committed drops and SSE reconciles.
- **Rollback on POST rejection** — `usePinActions().reorder` shows its toast AND **rethrows** (pin/unpin swallow — they have no client-side optimistic order to revert); the DnD hook `.catch()`es to clear the override and the palette move `.catch()`es to avoid an unhandled rejection, so a rejected reorder reverts the display to the authoritative order instead of leaving the failed order rendered.
- **`onDrop` derives neighbours inline from the final override order** (not from `computeReorderNeighbors` over the authoritative list) — the override is what the user sees and is robust to mid-drag entry churn where index arithmetic over the authoritative array is not. The pure `computeReorderNeighbors`/`computeMoveNeighbors` helpers (`src/lib/board-reorder.ts`, mirroring `palette-move.ts`) are the PALETTE path's arithmetic (`computeReorderNeighbors` has one live caller, `computeMoveNeighbors`, so it is kept with a doc comment, not dead).

**Palette parity** — `Board: Move Focused Pane Left/Right` (`boardRouteActions`, boundary-hidden per § Boards Command Palette) act on the focused pane, compute neighbours via `computeMoveNeighbors`, and fire the same single-POST path as DnD. No wraparound (hidden, not disabled, at the edges).

**Focus tracked by `server:windowId` key + a user-intent gate** (`board-page.tsx`, the load-bearing seam):

- Focus is tracked by the focused pane's **`server:windowId` key** (`focusedKeyRef`), NOT by positional index. A single focus-authority effect reconciles `focusedIndex` from `focusedKeyRef` on every order change (via `focusedIndexForKey`) so the focused pane's identity survives reorders — own palette move, own drop echo, AND a reorder arriving from another client (key-follow, not index-follow). The palette `moveFocusedPane` deliberately does NOT bump `focusedIndex`; the reconcile does it once the new order settles (§ Design Decisions → Board pane focus tracks by key, not index).
- **Imperative `.focus()` fires only on a TRUE user-intent flag** — this is the board-route form of the "SSE must not steal focus" invariant, mirroring the sidebar tree's `focusMovedRef` gate literally (see § Keyboard Navigation & Tree ARIA). `useBoardEntries` refetches a fresh `entries` array on EVERY `board-changed` SSE from ANY board on ANY server, so gating focus on *index change alone* is insufficient: a remote-driven reconcile (a `board-changed` from another client whose reorder/pin/unpin ahead of the focused pane shifts its index) changes the index without any local user intent and would steal DOM focus into the xterm while the user types in the docked compose strip / pin-popover / palette / another surface. The gate is `shouldFocusPane(focusIntentRef.current, prevFocusedIndexRef.current, focusedIndex)` (`focusIntentRef` = the user-intent flag beside `focusedKeyRef`): it fires `paneRefs.current[focusedIndex]?.focus()` only when a user-driven action set the flag AND the index genuinely changed. A passive `board-changed` refetch never sets the flag, so a remote reconcile reconciles the index but never moves DOM focus; board load never auto-focuses pane 0 (no intent, seed index unchanged).
- **Intent sites set the flag; same-index actions focus imperatively without it.** `focusIntentRef.current = true` is set at each user-intent site that will change `focusedIndex`: keyboard cycle (`Cmd/Ctrl + ]` / `[`), the two palette cycle actions (`Board: Cycle Pane Focus →` / `←`), and `handlePaneClick` on a *different* pane — each guarded so the flag is only set when the index will actually change (the single-pane modulo cycle is guarded by `entries.length >= 2`; clicking the *already-focused* pane focuses its terminal imperatively at the call site WITHOUT setting the flag, since no state change would consume it). The flag is cleared on the settled pass whenever it runs with the flag set (regardless of index change), so a set flag can never linger past its triggering render; the `orderChanged` key-reconcile early-return pass leaves the flag untouched so it survives to the re-entered settled pass.
- **The own-move follow is preserved through the async echo via `reorderWithFollow`** — a board-page-level callback wrapping `usePinActions().reorder` that sets `focusIntentRef.current = true` before delegating and clears it (`false`) in the `.catch` failure path (no echo will arrive). BOTH own-move paths route through this single seam: the palette `moveFocusedPane` and the `reorder` prop threaded to `DesktopRow` (DnD drop). An own move has no synchronous index change — the display reorders only when the `board-changed` echo arrives — so the flag is deliberately set *ahead* of the echo: the echo's reconcile bumps the index (early-return pass, flag untouched), the re-entered settled pass consumes the flag, and the moved pane is focused. The accepted residual is a one-echo-round-trip race window: a remote event interleaving before the own echo could consume the flag early, producing a single benign own-pane focus-follow (not a focus steal). `shouldFocusPane`/`focusedIndexForKey` are unit-tested in `board-reorder.test.ts`.

**e2e** (`app/frontend/tests/e2e/board-reorder.spec.ts` + `.spec.md`) mirrors `server-reorder.spec.ts`: it exercises the reorder ENDPOINT + `board-changed` SSE echo + a reload-free GET-order assertion against the live backend, NOT a native HTML5 drag simulation — synthetic Playwright DnD is unreliable (the session-reorder drag spec is `test.fixme`, never passed) and `page.reload()` does not commit under the SPA's long-lived SSE. The drag handlers, MIME guard, reconcile, cancel-revert, failure-rollback, and neighbour arithmetic are Vitest-covered (`use-board-pane-reorder.test.ts`, `board-reorder.test.ts`). Backend cross-server neighbour resolution is Go-tested (`router_test.go` `TestLookupNeighbourKeys_*`).

### Desktop Relay-Connection Suspension

On plaintext origins, `DesktopRow` suspends off-screen panes' terminal streams so the live count stays bounded — the desktop analogue of the mobile carousel's `paused={idx !== carouselIndex}` pause. The mechanism mirrors the mobile pause: a paused pane unmounts its `TerminalClient`; scroll-back re-mounts and re-opens, replaying terminal content. Suspension is NOT a closed WebSocket per pane — all panes share the ONE `/ws/terminals` socket, so a paused pane's `TerminalClient` unmount sends a `close` **control op** for its stream (not a socket teardown) and a resumed pane sends a fresh `open` op. This avoids the board pane-resize suspension-drop reconnect cost (memory `board-pane-resize-suspension-drop`). The mux + state socket already hold the tab to a fixed 2 WebSockets, so the visibility-pause + cap now bound how many PTY attaches (tmux clients) are live at once rather than the HTTP/1.1 pool. The unmount-on-pause seam is transport-agnostic. (`260531-rus8`, `260717-803u`)

- **Plaintext-only gate** — the entire feature is gated on `plaintext = window.location.protocol === "http:"` (`board-page.tsx`). On HTTPS/h2 (production via Tailscale) every desktop pane stays live (`paused={false}`), no `IntersectionObserver` is created, and no cap applies — behavior is byte-identical to before this change. The ~6-connection ceiling is a plaintext HTTP/1.1 artifact (h2 multiplexes; the relay WS limit is ~255), so the fix activates only where the problem exists (E2E/dev `http://localhost:3020`, raw-port `http://...` access)
- **Visibility via `IntersectionObserver`** — on plaintext origins, a single observer rooted on the `rowRef` horizontal-scroll container drives each pane's `paused` prop from viewport intersection. Panes are keyed to indices via a `data-paneIndex` attribute on each pane's root element (forwarded through a `rootRef` callback prop on `BoardPane`, kept distinct from the `paneRefs` imperative `BoardPaneHandle` so neither contract leaks into the other). The observer effect mirrors the existing wheel-handler effect's setup/cleanup discipline (`observer.disconnect()` on unmount) and re-subscribes when the pane count changes
- **Pre-warm margin** — the observer uses `RELAY_PREWARM_ROOT_MARGIN` = `` `0px ${BOARD_PANE_DEFAULT_WIDTH}px` `` (one pane-width of horizontal `rootMargin`, vertical 0) so a pane is kept live slightly before it enters and after it leaves the strict viewport, preventing pause/resume thrash (and the `[reconnecting...]` flicker) on a quick scroll-past. No debounce in v1 — add one only if thrash is observed during Playwright tuning
- **Live-pane cap** — simultaneously-live (unpaused) relay panes are capped at `MAX_LIVE_RELAY_PANES = 4` (named constant, not a magic number), backstopping the observer for the wide-monitor case where more panes fit on-screen than the budget allows (`1 SSE + 4 relay + headroom` stays under 6). When visibility alone would leave more than the cap live, the **least-recently-focused** visible panes are paused first. The selection logic is the pure `selectLivePanes` helper in `app/frontend/src/components/board/select-live-panes.ts` (colocated unit tests), consuming the visible-index set, the focused index, the most-recently-focused order, and the cap; `DesktopRow` passes `paused={!livePanes.has(idx)}` (and short-circuits to `paused={false}` when `livePanes === null`, i.e. on secure origins). The MRU order folds the current `focusedIndex` to the front during render (`useMemo` keyed on `focusedIndex`) so cap eviction never sees a stale focus
- **Focused pane always live** — `selectLivePanes` adds `focusedIndex` unconditionally before filling the remaining slots, so the focused pane is exempt from both visibility-pause and the cap. This preserves `Cmd+]`/`Cmd+[` cycling (cycling to an off-screen paused pane re-selects it → unpauses → re-mounts → imperative `focus()` via `paneRefs`) and BottomBar targeting of the focused terminal
- **Composes with the sibling change** — `260531-m3pl` removes the xterm chunk-fetch pressure from the runtime connection budget while this change bounds the persistent relay streams; together they fit the board route under the 6-connection cap on plaintext origins (the original board-route E2E hang root cause). `MobileCarousel` is untouched — it already suspended correctly

### Mobile Single-Pane Carousel

Below the `min-width: 640px` breakpoint (matching the existing project mobile convention), the BoardPage renders a single-pane swipe carousel:

- One pane fills the viewport width
- Touch swipe left/right cycles in `orderKey` order; threshold 40px on `touchstart` → `touchend` `clientX` delta
- **Off-screen panes pause** by unmounting their `TerminalClient` so the WebSocket closes; on swipe-in, the pane re-mounts and the terminal reattaches
- A pagination dot strip indicates the current pane index (no wrap on edges in v1)

### Sidebar Boards Section (`app/frontend/src/components/sidebar/boards-section.tsx`)

Renders **at the very top of the sidebar** — the section order is **Boards → Servers → Sessions**. Visible on every route that mounts `<Sidebar>` (`/$server/...` and `/board/$name`). The board route reuses the same `BoardsSection` component for board switching; there is no separate BoardPage listing. (`260509-17m3`)

- **Always visible**, regardless of `boards.length` or current route
- **Hint mode** — when `boards.length === 0`, the body shows a one-line hint `Pin a window to start a board` (`text-xs text-text-secondary`) verbatim from 4vuv's copy. Applies on every route, not only on `/board/<name>` — placing Boards at the top would cause a layout shift if the section hid/showed dynamically (Servers would jump up, then back down when the first board materializes)
- **First board materializes** — hint replaced by a single board row in place; no other section's vertical position shifts
- **Last board removed** — section reverts to hint mode; no layout shift to ServerPanel or Sessions

Each row: board name (left, truncate with ellipsis), pin count (right, muted), highlighted background when current route matches `/board/<name>`. Clicking the row navigates to `/board/<name>`. **Drag-reorderable** (`260708-a2qd`): rows render `orderedBoards` from the shared `useBoardListReorder` hook with `getTileProps(b.name)` spread onto each, and the dragged row dims via `opacity-50` — see § Board-list drag reorder.

Rows are also **drag-to-pin drop targets** (`260811-g0t1`): a sidebar window-row drag hovered over a board row is gated on the marker MIME `application/x-window-drag` (`WINDOW_DRAG_MIME`, exported from `boards-section.tsx` and shared with the drag source in `sidebar/index.tsx` — during `dragover` a target can only inspect `dataTransfer.types`, and `application/json` is too generic to gate on). A marker-carrying `onDragOver` `preventDefault()`s, sets `dropEffect = "copy"`, and rings the row with the session-row cross-move drop-target treatment (`boxShadow: inset 0 0 0 2px var(--color-accent)`), held in local `useState` and cleared on `dragleave` and on drop. `onDrop` parses the `application/json` payload and calls `usePinActions.pin(server, windowId, boardName)` unchanged — toast with View-board action, last-used persistence, SSE reconcile, no optimistic UI; a malformed payload is ignored silently. Cross-server drops work by construction (the payload carries `server`; boards are cross-server), and already-pinned windows get no client-side gating — the backend's re-stamp/no-op semantics decide, matching the popover path. The two drag species dispatch by MIME: a non-marker drag falls through to the `useBoardListReorder` handlers untouched, so a reorder drag never pins and a window drop never reorders.

**Dynamic default-open + header PinIcon**: the `CollapsiblePanel` uses `defaultOpen={boards.length > 0}` (storageKey `runkit-panel-boards`) so the panel opens live once the first board exists and a fresh pin surfaces where it landed, while an explicit user toggle always wins — this falls out of `useLocalStorageBoolean(storageKey, defaultValue)`, which consults `defaultValue` only when no stored key exists and resyncs its effect on `defaultValue` change (see § Sessions-pane scope → Sessions-scope hooks for the shared-hook family — `useLocalStorageBoolean` and its enum sibling). The panel `headerRight` renders the shared outline `PinIcon` (`components/pin-icon.tsx` — the same glyph the window-row pin button and board-tile-header unpin use) leading the `{boards.length}` count, visually linking the window-row pin affordance to where pins land; the icon stays present in zero-board hint mode (the count is dropped there, the icon is not). BOARDS keeps its name (the "Board" vocabulary is load-bearing — `/board/$name`, the `Board:` heading/palette, `/api/boards`); discoverability comes from default-open + the glyph, not a rename to "PINNED WINDOWS". (`260718-gxrq`)

### Sidebar Pin Icon on Window Rows

`window-row.tsx` gains a pin icon button that follows the existing icon-on-hover pattern in the sidebar:

- **Hover-revealed** (always visible on touch devices via `coarse:opacity-100`)
- **Filled** when the window is pinned to ANY board; **outline** when not pinned (computed via `useWindowPins(server, windowId)` which watches every board). The glyph is the shared thumbtack `PinIcon` (`components/pin-icon.tsx`) — the same component the board tile header renders in its `slashed` unpin variant (§ Board Tile Header). **Accent-colored** when the row is pinned to the currently-viewed board (`isPinnedToActiveBoard`), monochrome `text-text-secondary` otherwise — this glyph carries the active-board cue (there is no left border; § Row Anatomy, § Active-Board Highlight in Sessions Tree)
- **Click opens `PinPopover`** (`app/frontend/src/components/sidebar/pin-popover.tsx`) anchored to the icon: an optional **"Go to {board}" navigation row** at the top when the window is pinned, then the list of existing boards (each row pins or unpins on click, ordered last-used-first — see below), plus an inline "Pin to new board…" text input with an adjacent **Pin button** — the mouse submit path, mirroring Enter exactly (`handleSubmitNew`: non-empty input pins to that name, empty input pins to the ↵-hinted last-used board; disabled when Enter would be a no-op). Enter validates against the board-name regex and creates the board on first pin. Validation errors surface inline. Outside-click + Escape dismiss. Sizing is sidebar-compact: `min-w-[160px] max-w-[220px]`, `text-xs` rows at `min-h-[24px] coarse:min-h-[36px]` (matching BoardsSection row density). (`260718-co9z`)
- **Pinned-row → board navigation is a TWO-STEP** (indicator → popover → "Go to {board}" row): a pinned window's pin indicator is a path to the owning board, but navigation lives on a dedicated popover row rather than hijacking the indicator's click (§ Design Decisions → Pin-indicator navigation is a popover row). A window has exactly one pin-session → exactly one board, so the row names that single board. `PinPopover` takes a `pinnedBoard?: string` + `onNavigateToBoard?(board)` — `sidebar/index.tsx` derives the reverse `windowId → board` lookup by extending `useWindowPins` (the per-board pin-map aggregation) and wires `onNavigateToBoard` to navigate to `/board/{board}`. Keyboard/palette-reachable per Constitution V. (`260718-co9z`)

**Cold-start & last-used friction reduction** (`260718-gxrq`): the popover removes the invent-a-name-then-type friction of a cold start and makes bare Enter meaningful once boards exist.
  - **Cold start (`boards.length === 0`)** — the new-board input is pre-filled with the default board name `main` (`DEFAULT_BOARD_NAME`) and its text is selected on autofocus (`inputRef.current.select()`), so a bare **Enter** immediately pins to a new board `main` (the existing `handleSubmitNew` path — no new submit logic) while typing any character replaces the selection, preserving the invent-a-name path. `ValidBoardName` accepts `main`. When boards exist the input stays empty (placeholder unchanged).
  - **Existing-board ordering** — the board list renders via `orderBoardsLastUsedFirst(boards, readLastPinnedBoard())` (`lib/last-pinned-board.ts`): the last-used board moves to the front when it is still a live board; remaining boards keep their `boards`-prop order; a stale value (board gone) is ignored. `lastUsed` is read once via `useMemo` when the popover opens (stable for its lifetime).
  - **Empty-input Enter → last-used** — when boards exist, the input is empty, and a valid last-used board exists (`emptyEnterTarget`), Enter pins to it and closes the popover; otherwise it stays a no-op (current behavior). Unreachable on cold start (input pre-filled). The last-used row (rendered first) carries a small `↵` hint (`aria-label="press Enter to pin here"`) so Enter's target is visible; an already-pinned board additionally shows a `✓`.
  - **Last-used persistence** — written on every successful pin from any entry point by `usePinActions.pin` → `writeLastPinnedBoard(board)` (see § Pin last-used preference), NOT by the popover itself.

### Active-Board Highlight in Sessions Tree

When the current route is `/board/<name>`, the cue for a window pinned to **that specific board** is the **accent-colored pin glyph** — NOT a left border (the row carries no accent border since the axis split; see § Row Anatomy). `WindowRow` renders the persistent filled pin glyph accent-colored when `isPinnedToActiveBoard`, monochrome otherwise. Pins to other boards show a monochrome filled glyph. The pin icon's filled state (pinned to ANY board) is independent of the accent. On non-board routes, no window is pinned-to-active-board, so all pins render monochrome.

### Pin Entry Points

| Entry point | Location | Notes |
|-------------|----------|-------|
| Sidebar pin icon | `WindowRow` (every server's window rows in the unified sidebar — server routes and board routes alike) | Hover-revealed; popover with board picker + inline input (cold-start prefill + last-used-first ordering + empty-Enter-to-last-used) (`260718-gxrq`) |
| Command palette | `boardActions` block in `app.tsx` (AppShell mount) and BoardPage's own mount | Direct `Pin: Current Window to <board>` per unpinned board + `Pin: Current Window to new board…` (opens the popover) — the keyboard-first pin path (Constitution V). See § Boards Command Palette (`260718-gxrq`) |
| Board pane header | `BoardHeader` (only on `/board/<name>`) | Per-pane unpin button (a pin-glyph SVG, § Board Tile Header) — no confirmation (pin is cheap to restore) (`260715-6jwn`) |
| Drag window row onto board row | Sidebar window rows (drag source) → sidebar BOARDS panel rows (drop target) | Direct-manipulation pin — gated on the marker MIME `application/x-window-drag` with a copy cursor + drop-target ring; drop calls `usePinActions.pin` unchanged (§ Sidebar Boards Section) (`260811-g0t1`) |

There is no right-click context-menu pin entry — no context-menu pattern exists in the sidebar to extend, and the four entry points above cover all flows.

### Board Tile Header

`BoardHeader` (`components/board/board-header.tsx`) renders each board tile's header: a **home crumb** + an **unpin button**. The crumb is `{session} › {window}` — the top-bar crumb vocabulary (`›` separator) — where `{session}` is the pinned window's HOME session (dual presence keeps a pinned window linked into a visible session). `BoardHeader` takes a resolved `homeSession?: string` prop; `board-page.tsx` derives it at render time from the `homeSessionByKey` join (a `useMemo` over `ctx.sessionsByServer` mapping each `server:windowId` → its owning `ProjectSession.name`) and threads it through `board-pane.tsx`. When `homeSession` is absent (legacy pin / home died — window not in the snapshot) the header **falls back to window-only** (`{windowName} · {server}` / window name). The whole header is the drag handle for board pane reorder (`dragHandleProps`, § Board Pane Reorder); the unpin button opts out (`draggable={false}` + `e.stopPropagation()` on click) so a click there unpins rather than starting a drag, and does not refocus the pane. `aria-label={`Unpin ${windowName || windowId} from board`}`, `title="Unpin from board"`, no confirmation dialog (pin is cheap to restore — contrast the confirm-gated top-bar Kill, § Board Kill Confirm Dialog). Wired from `board-page.tsx`'s `onUnpin` per tile (`POST /api/boards/{name}/unpin`). (`260718-co9z`)

**Pin-glyph icon**: the button renders the shared **`PinIcon`** (`components/pin-icon.tsx`) in its **`slashed`** variant — the thumbtack outline crossed by a diagonal `line` = "remove the pin", not a text `×`. A `×` would read as a destructive close/kill, misleading since the top-bar's Kill affordance is a (confirm-gated) window-kill (§ Close pane / Kill · § Board Kill Confirm Dialog); the slashed pin truthfully signals the safe, reversible unpin. `PinIcon` is THE single pin shape for every board-pin affordance: the sidebar window-row pin button renders the same component (`filled` = pinned-to-any-board, outline = not pinned), so pin and unpin read as two states of one affordance. Hand-rolled inline SVG (16×16 viewBox rendered at 12px so strokes pixel-align, `stroke="currentColor"` `strokeWidth={1.5}`, round caps/joins) per the project's no-icon-library pattern (matching the top-bar `SplitControl` and the overflow menu's `HelpIcon`). The `slashed` variant is always unfilled — a currentColor slash over a currentColor fill would vanish in the overlap. Non-icon behavior: non-draggable, stopPropagation, aria/title, no-confirm. (`260715-6jwn`)

### Boards Command Palette (`Board:` prefix)

`boardActions: PaletteAction[]` is composed in a dedicated `useMemo` block in `app.tsx`, between `windowActions` and `selectionActions` — one group inside AppShell's route-scoped `paletteActions` list, which is registered into the palette-actions slot (§ Single palette mount + palette-actions slot):

```ts
const paletteActions = useMemo(
  () => withShortcutHints([...sessionActions, ...sessionsScopeActions, ...windowActions, ...boardActions, ...selectionActions, ...viewActions, ...openActions, ...themeActions, ...configActions, ...statusRefreshActions, ...serverActions, ...shellServerActions, ...pushActions, ...windowSwitchActions, ...agentActions, ...agentSpawnActions, ...macroPaletteActions], ...),
  [...]
);
```

(`pushActions` is the Web Push opt-in block — see § Notifications (Web Push opt-in); `statusRefreshActions` is `PR: Refresh Status` — see § Command Palette `PR: Refresh Status` action. The nav / terminal-font / refresh / help / shortcuts / settings / update / check / maintenance / version groups are NOT in this list — they are the layout-level global groups built by `use-global-palette-actions.ts` and merged in after the route list; `windowSwitchActions`/`agentActions`/`macroPaletteActions` compose last.) (`260715-ifco`)

| Entry | Visibility | Action |
|-------|------------|--------|
| `Board: Switch to <name>` (one per board, `(current)` on the active one) | Always | Navigate to `/board/<name>` |
| `Pin: Current Window to <board>` (one per board the window is NOT already pinned to, ordered last-used-first) (`260718-gxrq`) | Only when `sessionName && currentWindow && server` | Direct pin via `usePinActions.pin` — the keyboard-first pin path (Cmd+K → type → Enter; §2c success toast is the feedback). Built by the pure `buildPinActions` (`lib/palette-pin.ts`) — see § Pin palette actions |
| `Pin: Current Window to new board…` (`id: pin-current-new-board`) | Only when `sessionName && currentWindow && server` | Dispatches `pin-popover:open` to the matching `WindowRow` (free-text entry needs the popover — the palette has no value input); it carries the popover-opening role for pinning to a new board (`260718-gxrq`) |
| `Board: Unpin Current Window` | Only when current window is pinned to ≥1 board | Unpins from all boards the current window is pinned to (single-action — no per-board picker in v1) |
| `Board: Leave Board View` | Only on `/board/<name>` | Navigate to last viewed window route, or `/` if none |
| `Board: Cycle Pane Focus →` | Only on `/board/<name>` and ≥1 pane | Same as `Cmd+]` |
| `Board: Cycle Pane Focus ←` | Only on `/board/<name>` and ≥1 pane | Same as `Cmd+[` |
| `Board: Unpin Focused Pane` (`260704-9o7k`) | Only on `/board/<name>` and ≥1 pane | Unpins the focused entry via the shared `unpinFocused` handler (`onSelect: unpinFocused`) — non-destructive, NOT a kill. Keyboard parity for the **tile-header pin-glyph unpin**, NOT for the top-bar Kill row (which is a window-kill) (`260715-6jwn`) |
| `Board: Split Focused Pane Horizontal` (`260715-6jwn`) | Only on `/board/<name>` and ≥1 pane | `executeSplit(focusedPane.server, .windowId, true, .cwd)` — `splitWindow` with `horizontal: true` → tmux `split-window -h` → side-by-side. Listed FIRST (default-first, mirroring the top-bar chip and its menus) and named by the one direction vocabulary every surface shares (§ Split control). Keyboard parity for the board split control (Constitution V) |
| `Board: Split Focused Pane Vertical` (`260715-6jwn`) | Only on `/board/<name>` and ≥1 pane | `executeSplit(focusedPane.server, .windowId, false, .cwd)` — `horizontal: false` → `split-window -v` → stacked; mirror of Horizontal |
| `Board: Kill Focused Pane` (`id: board-kill-focused`) | Only on `/board/<name>` and ≥1 pane | `requestKillFocused` — opens the **kill confirm dialog** (§ Board Kill Confirm Dialog) rather than firing immediately; confirmed Kill is a window-kill (`killWindow` → `POST /api/windows/{id}/kill`) whose `onSettled` schedules `refetch()`. Keyboard parity for the board Kill menu row (Constitution V) (`260718-co9z`) |
| `Board: Move up` / `Board: Move down` (`260708-a2qd`) | Only on `/board/<name>`, boundary-hidden (not first / not last) | Reorder the CURRENT board in the board list — keyboard/touch parity for the board-list drag (Constitution V). Computes the full order via `computeMoveOrder` and POSTs `setBoardOrder`; see § Board-list drag reorder + § Command Palette Move up/down |
| `Board: Move Focused Pane Left` (`260708-rmiq`) | Only on `/board/<name>` when `focusedIndex > 0` (hidden at the left edge — not disabled) | Moves the focused pane one slot left — fires the same single `reorder(...)` POST as DnD, computing `before`/`after` via `computeMoveNeighbors` (§ Board Pane Reorder). No wraparound. Keyboard parity for the drag reorder (Constitution V) |
| `Board: Move Focused Pane Right` (`260708-rmiq`) | Only on `/board/<name>` when `focusedIndex < entries.length - 1` (hidden at the right edge) | Moves the focused pane one slot right — mirror of Move Left |
| `Board: Toggle Autofit (on/off)` (`260708-738w`, `id: board-toggle-autofit`) | Only on `/board/<name>` (label reflects the current state) | Flips the per-board desktop autofit preference (same state the top-bar `BoardAutofitToggle` reflects — Constitution V parity). Desktop layout only; a no-op on the mobile carousel. See § Board Autofit Toggle |

**Limits**: The right-click context menu pin entry is not implemented (use sidebar pin icon, command palette, or board pane header). Hover-to-focus is disabled. Pane reorder ships via DnD + the Move Focused Pane Left/Right palette entries above (§ Board Pane Reorder). (`260708-rmiq`)

The AppShell route list's `boardActions` group carries `Board: Switch to <name>` + the `Pin: Current Window to <board>` direct-pin set + `Pin: Current Window to new board…` + `Board: Unpin Current Window` (`260718-gxrq`). BoardPage registers `boardRouteActions` (a `useMemo` in `board-page.tsx`) into the palette-actions slot; it carries `Switch to <name>` + `Leave Board View` + the boundary-hidden `Board: Move up/down` (`260708-a2qd`) + the entries-gated `Cycle Pane Focus →/←` + `Unpin Focused Pane` (`260704-9o7k`) + the entries-gated `Board: Split Focused Pane Horizontal/Vertical` + `Board: Kill Focused Pane` (`260718-co9z`) + the boundary-gated `Move Focused Pane Left/Right` (`260708-rmiq`; Left pushed only when `focusedIndex > 0`, Right only when `focusedIndex < entries.length - 1`; the cycle/unpin/split/kill/move-pane entries all sit inside a single `entries.length > 0` guard) + the always-present `Board: Toggle Autofit (on/off)` (`260708-738w`, § Board Autofit Toggle — parity for the top-bar `BoardAutofitToggle`, outside the `entries.length > 0` guard) — board-scoped entries that only have meaning on a `/board/$name` route. The split/kill/unpin actions all consume shared `board-page.tsx` handlers rather than re-deriving state (parsimony): the `focusedPane` memo (`entries[focusedIndex]` → `{server, windowId, cwd}`, cwd from the active pane, hoisted above `boardRouteActions`) feeds the split executors (`executeSplit`, `useOptimisticAction`-wrapped `splitWindow` with `addToast` on error) and the kill opener `requestKillFocused` (which routes through the confirm dialog whose confirmed Kill runs `executeKillWindow` — `killWindow` — scheduling `refetch()` via `onSettled`, § Board Kill Confirm Dialog), and the shared `unpinFocused` callback backs both the palette unpin and (via the tile header) the header unpin. The registered board list stops there — the nav trio, terminal-font trio, refresh, help, shortcuts, settings, and update/check/maintenance/version groups are the layout-level GLOBAL groups built once by `use-global-palette-actions.ts` (§ Single palette mount + palette-actions slot), merged in after the active route's list. The board is the degraded-relay recovery scenario — N live relay WebSockets — and a top-bar RefreshButton also renders in board mode (L3 always-block), so the global refresh entry is the keyboard-parity form. Below `sm` the top-bar UpdateChip is hidden, so the palette is a phone user's only update surface on `/board/$name`. (`260703-p29w`, `260704-hmd0`, `260704-9o7k`, `260713-4zap`)

### Pin palette actions (`Pin:` prefix)

`buildPinActions` (`app/frontend/src/lib/palette-pin.ts`) is the pure, dependency-free builder for the command-palette pin actions, following the `lib/palette-move.ts` / `lib/palette-version.ts` / `lib/palette-view.ts` convention (label composition, exclusion, and ordering are unit-testable without mounting the shell). Signature `buildPinActions(boards, alreadyPinned, lastUsed, onPin, onOpenNewBoardPopover): PaletteAction[]`:

- One **direct-pin** action per board the current window is NOT already pinned to — `id: pin-current-<board>`, label `Pin: Current Window to <board>`, ordered last-used-first via `orderBoardsLastUsedFirst` (over the already-pinned-filtered set). `onSelect` → `onPin(board)`.
- The **new-board variant** last — `id: pin-current-new-board` (`PIN_NEW_BOARD_ACTION_ID`), label `Pin: Current Window to new board…`, `onSelect` → `onOpenNewBoardPopover()`. Always present (the only free-text entry seam).
- Edge cases: window pinned to every board, or zero boards → only the new-board variant.

**Wiring** (`app.tsx` `boardActions` useMemo): gated on `sessionName && currentWindow && server` so the actions are available on the terminal/server route for the current window. `onPin` calls `usePinActions.pin(server, windowId, board)`; `onOpenNewBoardPopover` dispatches the `pin-popover:open` CustomEvent (`{ detail: { server, windowId } }`) that the matching `WindowRow` handles. `alreadyPinned` = `currentWindowPinnedBoards` (derived from the cross-board pin map); ordering uses `readLastPinnedBoard()`. The per-board entries provide the direct keyboard pin (Constitution V); the new-board variant carries the popover-opening role. (`260718-gxrq`)

### Pin last-used preference (`lib/last-pinned-board.ts`)

Client-side "last board a window was pinned to" preference — per-client only (Constitution II, no backend persistence), `runkit-*` key convention (`LAST_PINNED_BOARD_KEY = "runkit-last-pinned-board"`, alongside `runkit-terminal-font-size`, `runkit-update-dismissed`). Reads/writes are best-effort try/catch-noop (mirroring `lib/window-view.ts`) so private mode / quota / SSR never throw.

- `readLastPinnedBoard(): string | null` — value NOT validated against the live boards list here; callers filter it (a board disappears when its last pin is removed).
- `writeLastPinnedBoard(name: string): void` — called at the **single** pin site inside `usePinActions.pin` (see § Hooks (Frontend)) on every successful pin, so all entry points (popover, palette) keep the preference consistent.
- `orderBoardsLastUsedFirst(boards, lastUsed): BoardSummary[]` — returns `boards` with a valid live last-used board moved to the front; a stale/absent `lastUsed` (board gone, or `null`) is ignored and input order preserved; the input array is never mutated (fresh array on reorder, same reference on no-op). Consumed by both the popover ordering and `buildPinActions`.

### Post-pin success feedback + toast optional action

`usePinActions.pin` surfaces success feedback (before this it toasted only on error). On a successful pin it fires `addToast("Pinned to <board>", "info", { label: "View board", onSelect: () => navigate to /board/<board> })` — placed in the hook (not per call site) so every pin entry point gets the same feedback; the hook runs inside router context so it obtains `useNavigate` directly. `unpin`/`reorder` stay error-only.

This required a minimal extension to the **toast system** (`components/toast.tsx`, see § Error toast system): `ToastEntry`/`addToast` gain an optional `action?: { label: string; onSelect: () => void }` as a **THIRD positional argument** — `addToast(message, variant?, action?)`, NOT an options object — which preserves all existing positional call sites with zero churn. The action renders as a keyboard-focusable `<button>` inside the toast body (the toast body is already `pointer-events-auto`); selecting it dismisses the toast then runs `onSelect`.

### Command Palette View actions (`View:` prefix)

`viewActions: PaletteAction[]` (`useMemo` in `app.tsx`) is the AppShell route list's `View:`-prefixed block, composed into `paletteActions` between `selectionActions` and `openActions` (see § Boards Command Palette). Its static entries are:

| Entry | Visibility | Action |
|-------|------------|--------|
| `View: Text Input` | Only when `sessionName` is set (a terminal is in view) | Toggles the docked compose strip (`toggleComposeStrip`) — palette parity for the `>_` chip (§ Docked Compose Strip) |
| `View: Terminal` / `View: Web` / `View: Code` / `View: Chat` | Each shown only when its lens is **available** (`availableViews(currentWindow)`, which folds in the `chatProvider`/`hasCode` gates) **AND not the current** (`resolvedView`) view; `shortcut` hint = the effective `chat-toggle` combo for `View: Chat` (and `View: Terminal` when LEAVING chat), else the effective `view-cycle` combo — passed in from the keybinding registry, so overrides move the hints (§ Keyboard Shortcuts → Palette hints) | `switchView(view)` — the per-viewer lens switch (no `@rk_type` mutation). Built by the ONE pure `buildViewActions(available, resolved, onSwitch)` (`lib/palette-view.ts`); see § Window Views (Lens Model) → Palette parity. There is no `@rk_type`-mutating `toggle-iframe-terminal` action (`260714-t97o`, `260714-r7rq`) |
| `View: Fixed Width (900px)` / `View: Full Width` | Always (label reflects `fixedWidth` state) | `toggleFixedWidth` |
| `Layout: Add <Surface>` / `Layout: Close <Surface>` | Window route (`windowParam`); Add per **available, not-open** surface (omitted at 3 tiles), Close per open surface kind (omitted on a `single` layout) | `addSurface` / `closeSurface` → `applyLayout` — palette parity for the rail's open-tile toggles (§ Right Rail). The ⇧⌘. `panel-toggle` chord (first non-tty surface's tile) is documented via the target surface's Add/Close hint |
| `Layout: Zoom` / `Layout: Unzoom` · `Layout: Promote <Surface>` · `Layout: Swap <Surface>` · `Layout: <Shape>` (per current arity) · `Layout: Cycle Shape` | Window route; Zoom gated on `!isMobile && order.length > 1`, label flips with the observed zoom state; `Layout: Cycle Shape`'s id IS the `layout-cycle` registry actionId, so `withShortcutHints` decorates it with the effective ⌘; chord (§ Surface Layout → ▦ Layout chip) | Tile verbs + shape jumps via `applyLayout`; Zoom drives the transient slot zoom through the app-level ref seam. All built by the ONE pure `buildLayoutActions(layout, surfaces, opts)` (`lib/palette-layout.ts`, the `buildViewActions` precedent) |

`View: Refresh Page` (`refresh-page`, `window.location.reload()` — a full page reload, `260703-p29w`) is NOT in this block: it is one of the layout-level global groups (§ Single palette mount + palette-actions slot), ungated and reachable on every route — unlike the terminal-only top-bar `RefreshButton` (§ Chrome (Top Bar)), it is the keyboard-reachable form of that recovery affordance everywhere (Constitution V).

### Command Palette `PR: Refresh Status` action

`PR: Refresh Status` is the keyboard-first parity (Constitution §V) for the PANE-header refresh button (§ Collapsible Panels → WindowPanel). It triggers the same `refreshStatus()` → `POST /api/status/refresh` best-effort kick of both PR pollers (fresh state via SSE).

It follows the established **pure-builder** convention (mirrors `lib/palette-view.ts` / `lib/palette-update.ts`): the label/id live in `buildStatusRefreshAction(onRefresh)` (`lib/palette-status-refresh.ts`, colocated `.test.ts`) — a single `{ id: "status-refresh", label: "PR: Refresh Status", onSelect: onRefresh }` action whose body is the caller-supplied `onSelect`. `app.tsx` memoizes it as a `statusRefreshActions: PaletteAction[]` group (`buildStatusRefreshAction(() => void refreshStatus().catch(() => {}))`, empty deps — the callback closes over the static import) and folds it into the AppShell `paletteActions` aggregation (between `configActions` and `serverActions`, in both the array spread and its dependency list). **AppShell route list only** (the board route registers board-scoped actions only) — the refresh is server-global but the affordance is labeled around the PANE panel, which the board route does not render; the top-bar/PANE surfaces cover the board user. The label is `PR:`-prefixed (scope honesty — it refreshes PR/status freshness, not "all pane stats"; the other PANE registers are already fresh within ~7.5s).

### Hooks (Frontend)

| Hook | File | Returns |
|------|------|---------|
| `useBoards()` | `hooks/use-boards.ts` | `{ boards, isLoading, error }`. Initial `listBoards()` on mount; subscribes to `board-changed` SSE on every server returned by `listServers()` (boards are server-scoped, but the board LIST is summarized across servers, so it attaches all to catch each one's pin/unpin/reorder events); 50ms debounce coalesces rapid events; preserves last good value on transient error (`260602-qn62`) |
| `useBoardEntries(name)` | `hooks/use-boards.ts` | `{ entries, isLoading, error, refetch }`. Initial `getBoard(name)`; subscribes on all known servers (the board list spans servers, so a pin/unpin on any server may affect this board); same debounce + error tolerance. `refetch` (= `fetchEntries`) forces a re-fetch bypassing the SSE debounce — the board-mode kill self-heal (a window-killing ✕ collapses a pin-session with no `board-changed` event) calls it to drop the now-dead tile (`260715-6jwn`) |
| `usePinActions(board?)` | `hooks/use-pin-actions.ts` | `{ pin, unpin, reorder }` stable callbacks; optimistic — SSE re-broadcast reconciles. `pin` on success writes last-used (`writeLastPinnedBoard`) and fires the `info` "Pinned to <board>" + "View board" toast (`260718-gxrq`, § Post-pin success feedback); `unpin`/`reorder` toast on error only (`reorder` rethrows so the caller can roll back its optimistic order) |
| `usePaneWidths(boardName, sidebarWidth)` | `hooks/use-pane-widths.ts` | `{ getWidth, setWidth }`; reads/writes `localStorage["runkit:board-widths:<name>"]`; clamps to `[280, viewport - sidebar]`; default 480px |
| `useBoardAutofit(board)` (`260708-738w`) | `hooks/use-board-autofit.ts` | `{ autofit, toggleAutofit }`; per-board desktop autofit preference in `localStorage["runkit:board-autofit:<board>"]` ("on" sentinel; OFF removes the key); default off; reloads on `board` change (mirrors `usePaneWidths`). Never touches `runkit:board-widths:<board>`. See § Board Autofit Toggle |
| `useMediaQuery(query)` | `hooks/use-media-query.ts` | `boolean`; the **shared matchMedia substrate** every media-query consumer routes through. One `MediaQueryList` per call, live via a `change` listener, effect keyed on `[query]` so a changed query resubscribes. Non-browser guard (`typeof window === "undefined" \|\| typeof window.matchMedia !== "function"`) returns `false` with no subscription; listener attach/detach is `typeof`-checked with a deprecated `addListener`/`removeListener` fallback (narrowing casts, no bare `as` widening); the handler re-reads `mql.matches` (not the event object) and runs once after subscribing to catch a change between state init and effect run. Sibling `evaluateMediaQuery(query): boolean` is the one-shot form for non-hook readers (state init, event handlers) — same guard, no subscription |
| `useIsMobile()` | `hooks/use-is-mobile.ts` | `boolean`; **narrow-width OR coarse-pointer** — the OR of **two separate** `useMediaQuery` subscriptions, `` `(max-width: ${MOBILE_BREAKPOINT_PX - 1}px)` `` (i.e. `max-width: 639px`) and `(pointer: coarse)`, deliberately not one combined query string. `MOBILE_BREAKPOINT_PX = 640` is exported from this module (matches the Tailwind `sm:` breakpoint) and is the single source of the width literal. Sibling `evaluateIsMobile()` is the exported one-shot form of the same rule over two `evaluateMediaQuery` calls — `chrome-context.tsx`'s `isMobileViewport()` delegates to it rather than mirroring the rule (§ Terminal Font Size) |
| `useCoarsePointer()` | `hooks/use-coarse-pointer.ts` | `boolean`; `useMediaQuery("(pointer: coarse)")` — pointer TYPE only, **deliberately not** `useIsMobile()`'s narrow-OR-coarse rule (§ Design Decisions → Two pointer policies stay unmerged). Drives the chat send form's coarse-pointer autofocus skip (§ Chat View) and the coarse-pointer tooltip suppression (§ Design Decisions → Suppress-by-early-return on coarse pointers). It does NOT drive Enter policy — that is pointer-independent (§ Docked Compose Strip → Send semantics) and `compose-strip.tsx` consumes the hook not at all |
| `useActiveBoardName()` | `hooks/use-active-board.ts` | active board name from `/board/<name>` route, else `null` |
| `useWindowPins(server, windowId)` | `hooks/use-window-pins.ts` | list of boards the window is pinned to; drives the pin-icon filled state |
| `useNow()` | `hooks/use-now.ts` | current epoch seconds (`Math.floor(Date.now()/1000)`); self-ticks every 1s via `setInterval` (cleared on unmount), re-rendering ONLY the calling leaf. Display-only clock — NOT data polling. Scope to duration leaves so the tick never re-renders ancestors; see § Render Performance (`260613-ect6`) |
| `useUpdateNotification()` (`260713-4zap`) | `contexts/session-context.tsx` (exported alongside the provider) | `{ qualifies, showChip, tools, key, manualOnly, singleRunKit, latest, current, updateNow, dismissUpdate, daemonVersion, brew, forceUpdateNow, restartNow }` — the DERIVED update-notification display shape read by the top-bar `UpdateChip` + the palette `buildUpdateActions`/`buildMaintenanceActions` callers (and the connection-dot title). Reads `SessionContext` via `useContext` DIRECTLY (provider-tolerant — never throws outside the provider; each field falls back). It is also THE two-feed merge point: the pure `deriveUpdateFeed` picks the ambient (SSE) feed over the tab-local manual-check feed, so `tools`/`key`/`manualOnly` describe whichever feed is effectively lit (`?? EMPTY_TOOLS` frozen module constant on both sides, so no per-render array churn). `qualifies = !isDev && tools.length > 0`; `showChip = qualifies && key !== updateDismissedKey` (composite-key dismissal); `manualOnly` drives `use-update-click`'s force-vs-scoped routing; `singleRunKit` gates the single-run-kit `⬆ v{latest}` wording, `latest`/`current` non-null only then. Self-contained-consumer pattern (mirrors `usePushSubscription`). See § Update Notification |
| `useGlobalPaletteActions({ onToggleShortcutsOverlay })` | `hooks/use-global-palette-actions.ts` | The layout-level GLOBAL palette groups — nav (`buildNavActions`, mode from the deepest-first route-param walk), terminal-font trio, `View: Refresh Page`, `Help: Documentation`, `Help: Keyboard Shortcuts`, `Settings: Open`, update/check/maintenance/version — built once for the single `AppLayout` palette mount and decorated with `withShortcutHints`. See § Single palette mount + palette-actions slot |
| `useRegisterPaletteActions(actions)` / `usePaletteActions()` / `usePaletteGlobals()` / `usePaletteActionsApi()` | `contexts/palette-actions-context.tsx` | The palette-actions slot (the `top-bar-slot-context` pattern): a route publishes its route-scoped, already-decorated list via `useRegisterPaletteActions` (last-writer-wins, clear-on-unmount); `usePaletteActions()` is the reactive merged `[...routeActions, ...globalActions]` list (layout-level leaves only); `usePaletteGlobals()` is the loop-safe globals-only channel routes resolve against; `usePaletteActionsApi().getAllActions()` reads the merged list imperatively at call time (chords, macro invocation). See § Single palette mount + palette-actions slot |
| `useServerDialogs()` | `contexts/server-dialogs-context.tsx` | `{ openCreateServer, requestKillServer, closeCreateServer, clearKillServerTarget, createServerOpen, killServerTarget }` — referentially-stable triggers plus open-state for the single `AppLayout`-mounted server create/kill dialogs (the `settings-dialog-context` pattern). See § Server create/kill dialogs |

## Notifications (Web Push opt-in)

Browser Web Push lets `rk notify` (and any process on the box) deliver an OS notification that reaches the user **even when the RunKit PWA tab is closed**. The end-to-end backend/delivery model lives in `architecture.md` § Web Push Notifications; this section covers the frontend opt-in surface. (`260615-xd9r`)

**Two surfaces, one hook, no bell.** The opt-in is a Cmd+K palette entry (the keyboard-first path, Constitution §V) plus the settings dialog's **Notifications** preference row (§ Settings Dialog); both read `state` and call the same `enable`/`sendTest` from one `usePushSubscription()`, so they cannot drift. There is **no bell chip in the top bar** — notifications are an app-global preference and live in the settings dialog (§ Chrome (Top Bar) → Right cluster; § Design Decisions → App-global actions live in the top bar). The palette label reflects subscription state and is **terminal-themed text**, never a bell icon:

| Palette label | When | Action |
|---------------|------|--------|
| `Notifications: Enable push` | Not yet subscribed | Runs the enable flow: `Notification.requestPermission()` → on `granted`, `getVapidPublicKey()` (`GET /api/push/vapid-public-key`) → `registration.pushManager.subscribe({userVisibleOnly: true, applicationServerKey})` → `subscribePush(sub)` (`POST /api/push/subscribe`) |
| `Notifications: Enabled ✓` | Already subscribed | Reflects state; the `✓` is a Unicode checkmark (terminal-themed glyph, not a bell) |

**Wiring**: `usePushSubscription()` (`app/frontend/src/hooks/use-push-subscription.ts`) returns `{ state, enable, sendTest, actions }` — the first three feed the settings-dialog row directly, and `actions` is the `pushActions: PaletteAction[]` block built from the same handlers; it is threaded into `app.tsx`'s `paletteActions` useMemo (spread just before `windowSwitchActions` — see § Boards Command Palette for the composition). The flow logic is the self-contained `app/frontend/src/lib/push.ts` module (`registerServiceWorker()`, `enablePushSubscription()`, `getPushState()`) plus the `getVapidPublicKey()`/`subscribePush()` client functions in `api/client.ts` — a separate module rather than inlining into the 1600-line, SSE-coupled `app.tsx`. The service worker (`public/sw.js`) is registered on app load from `main.tsx`.

**Fail-silent / secure-context**: every path is throw-free. `registerServiceWorker()` is guarded by `'serviceWorker' in navigator` and no-ops on unsupported / insecure-context environments. The enable flow aborts silently (a toast is acceptable) when permission is denied or push is unsupported / the context is insecure, sending no subscription. Web Push (service worker + `PushManager`) requires a **secure context — HTTPS or `localhost`**; the user typically hits run-kit on `localhost:3000` or behind a TLS proxy (both secure), but raw-IP plaintext access simply cannot subscribe (silently).

## Update Notification

Notifies the web-UI user when a newer release of ANY shll-toolkit tool (run-kit, shll, wt, idea, tu, hop, fab-kit) crosses its per-tool notify threshold, and offers a one-click upgrade that runs a SCOPED `shll update <matched…>` (fallback `rk update` self when shll is absent) in a managed `rk-jobs` job window — brew-upgrading the matched tools and restarting the daemon when run-kit is among them — with a `watch`-target affordance that jumps the user to the live job window (§ Top-bar L3 UpdateChip → click). **In-app ONLY — explicitly NO Web Push** (user-decided: Web Push is reserved for waiting agents; update notices must not buzz phones). Two surfaces — a top-bar L3 chip + command-palette actions — both self-contained consumers of one context hook, the same shape `usePushSubscription` uses for notifications (no props threaded through `RootTopBar`→`TopBar`). The backend origin (SSE `event: version`/`event: update-available`, `POST /api/update`, `internal/updatecheck`) is in [architecture.md](/run-kit/architecture.md) § SSE Hub / § API Layer; this section is the frontend surface.

**TWO FEEDS light those surfaces**, merged ambient-first inside `useUpdateNotification` (§ Context state → the merged two-feed derivation): the **ambient** feed — the durable, policy-driven, cross-tab SSE `update-available` verdict from the 6h released-manifest checker — and the **manual** feed, a tab-local, non-persisted hold of the latest on-demand ⟳ / palette check result (§ On-demand check commands). The manual feed exists because that check is a deliberate SIDE CHANNEL the daemon never caches (the github source carries no notify policy, and caching its verdict would wipe a legit released-manifest chip and starve the scoped `shll update` argv — `Snapshot().Matched`), so its finding has no server-side home: without the frontend hold, a check that reports "3 tools updatable" evaporates with its ~5s toast while the persistent surfaces stay dark. (`260807-s6zs`)

### Context state + `useUpdateNotification()` hook

`SessionContext` (`contexts/session-context.tsx`) owns the update state (it already owns the server-global SSE pool + `?metrics=1` stream, and `version`/`update-available` are server-global like `server-order`/`board-order`, so this reuses the exact fan-out — no second context). It exposes:

- `daemonVersion: string | null` — the running daemon version from `event: version` (no leading `v`), `null` until first seen.
- `updateAvailable: UpdateAvailable | null` — the current toolkit-update verdict from `event: update-available`, `null` when none. `UpdateAvailable = { tools: UpdateTool[], key: string, current: string, latest: string }` where `UpdateTool = { tool, current, latest, updateAvailable?, notable? }`: `tools` is the **FULL per-tool verdict list** — every tool with a pending update INCLUDING sub-threshold rows, each carrying the optional `updateAvailable?: boolean` (installed < latest) and `notable?: boolean` (crosses the notify threshold) flags parsed from the extended SSE payload; a MISSING flag is treated as `true` (an old-daemon payload listed only matched tools, so all its rows count as notable). Chip/palette consumers read the NOTABLE subset via `useUpdateNotification`'s filter. `key` = the composite dismissal key (sorted `tool@latest` pairs over the NOTABLE set, comma-joined); `current`/`latest` = the legacy run-kit-row fields, populated only when run-kit is in the notable set (transitional not-yet-reloaded-frontend compat).
- `manualCheck: ManualCheckResult | null` — the latest MANUAL check result held for this tab, `null` when none. `ManualCheckResult = { tools: UpdateTool[], source: string }`: `tools` is the **updatable subset** the check reported (reusing the context's own `UpdateTool` row type — `api/client.ts`'s `UpdateCheckTool` is structurally compatible), `source` the echoed report backend (`"released"`/`"github"`). Plain `useState` — **deliberately NOT persisted** to localStorage or anywhere else (§ Design Decisions → The manual check feed is tab-local). (`260807-s6zs`)
- `applyManualCheckResult(tools, source): void` — the setter `useUpdateCheck` calls after a check resolves (naming mirrors `applyVersion`/`applyUpdateAvailable`/`applyHostMetrics`). An **EMPTY `tools` array CLEARS** the held result — a fresh verdict supersedes a stale one, so a clean re-check must never leave a chip lit that the toast just contradicted. Referentially stable (`useCallback` with an empty dep list): it reads the current ambient key through a render-mirrored `ambientKeyRef` rather than taking `updateAvailable` as a dependency, because `use-update-check.ts` memoizes `runUpdateCheck` on it and `use-global-palette-actions.ts` memoizes the check-action array on THAT — an identity that churned with ambient state would churn the palette's global check group. (`260807-s6zs`)
- **Ambient-key-change clearing** — a `manualAmbientKeyRef` stamps the ambient composite key observed at manual-store time (`undefined` when nothing is held), and an effect keyed on `[ambientKey, manualCheck]` clears `manualCheck` when the current ambient key differs from that baseline — the update-consumed signal (including the cleared empty key, which surfaces as `null`). Baselining at store time rather than watching `updateAvailable.key` raw is load-bearing (§ Design Decisions → Ambient-key-change clearing baselines at store time). A daemon restart instead discards the state via the post-restart tab reload (§ Reload guard). (`260807-s6zs`)
- `updateDismissedKey: string | null` — the composite-key dismissal from localStorage `runkit-update-dismissed` (const `UPDATE_DISMISSED_KEY`), lazily read in a try/catch on mount. It is a **single slot serving BOTH feeds** — compared against whichever key is effectively displayed.
- `updateNow(): Promise<UpdateTriggerResult>` — thin wrapper over the `triggerUpdate()` client helper (`POST /api/update`; rejects on non-2xx). The resolved `UpdateTriggerResult` (`{status, watch?: UpdateWatchTarget}`) carries the job-window `watch` target the click handler turns into navigation (§ Top-bar L3 UpdateChip → click).
- `dismissUpdate(): void` — writes the **EFFECTIVE displayed key** to localStorage `runkit-update-dismissed` + sets `updateDismissedKey`; a no-op on an empty key. It re-derives the same ambient-first merge the display uses (calling the shared `deriveUpdateFeed` over the notable ambient set and the manual set, with `computeUpdateKey` for the manual side), so it writes the ambient key when the ambient feed is lit and the client-composed manual key when the manual feed is — which is what makes the chip `✕` and the palette `run-kit: Dismiss Update Notice` entry work unchanged against a manual-fed chip, with the same re-show semantics (a later manual check finding a different tool set composes a different key). **Known cross-feed interaction**: with one dismissal slot for two feeds, dismissing a manual-fed chip overwrites an already-dismissed ambient key and re-lights the ambient chip (accepted — a single slot is the pre-existing contract; no second key was added, per the no-new-localStorage-keys constraint). (`260807-s6zs`)
- **`brew: boolean`** — whether the daemon is a Homebrew install, from the `event: version` `brew` field. Tracked in React `useState` (`isBrew`, drives palette visibility → must re-render, unlike the reload-decision refs below), **default `false` until the first version event is observed** (so the brew-gated `run-kit: Update Now` palette entry stays hidden on the brief pre-connect window; mirrors the `qualifies` absent-data-hidden gating). Set inside `applyVersion`.
- **`forceUpdateNow(): Promise<UpdateTriggerResult>`** — thin wrapper over `triggerForceUpdate()` (`POST /api/update` body `{"force":true}`; rejects on non-2xx). Same shape as `updateNow`.
- **`restartNow(): Promise<UpdateTriggerResult>`** — thin wrapper over `triggerRestart()` (`POST /api/restart`; rejects on non-2xx).

The `version`/`update-available` listeners are added on **BOTH** stream kinds — the per-server pool AND the dedicated `?metrics=1` subscription — server-global, applied idempotently (no `data.server` filter), exactly like `server-order`/`board-order`. Both call the pure-ish `applyVersion` / `applyUpdateAvailable` callbacks; malformed events are skipped. **`applyVersion(version, boot, brew)`** parses the payload as `{ version?, boot?, brew? }` **tolerantly** — `boot` passed only when `typeof data.boot === "string"` (else `null`), `brew` as `data.brew === true` (defaults `false`) — so an older daemon's boot-less/brew-less payload never breaks parsing or flips brew true. The `update-available` parse maps each `tools[]` row's optional `updateAvailable`/`notable` booleans onto `UpdateTool` — carried ONLY when the daemon sends real booleans (an old daemon's flag-less payload leaves them `undefined`, which consumers treat as `true`). **`applyUpdateAvailable(next)`** stores `next` but **dedups by composite `key`** (a re-delivery of the same matched set does not churn consumers) — AND handles a **CLEARED payload** (empty `key`) by clearing the stored state so the chip hides. It MUST NOT early-return on an empty key: a siblings-only `shll update` never restarts the daemon, so the post-remediation re-check's cleared `update-available` is the ONLY signal that clears the consumed match; an early return would leave a consumed match advertised forever.

**Tolerant `useUpdateNotification()` hook** (exported from `session-context.tsx`) — reads the context via `useContext(SessionContext)` DIRECTLY (not `useSessionContext`, so it **never throws outside the provider**; every field falls back — `updateNow`/`forceUpdateNow`/`restartNow` → `() => Promise.resolve({ status: "" })`, `dismissUpdate` → `() => {}`, `brew` → `false`, `manualCheck` → `null`, so the manual fields degrade to the no-manual-feed defaults). It returns the DERIVED display shape the chip + palette consume, so the gating logic — and THE two-feed merge — lives in one place:

- `isDev = daemonVersion === DEV_VERSION` (`"dev"`).
- `ambientTools` = the **NOTABLE subset** of `updateAvailable?.tools`, computed in a `useMemo`: `all.filter((t) => t.notable !== false)` (a missing/undefined flag counts as notable — old-daemon compat), returning `all` unchanged (stable referential identity) when nothing filters out so a no-op filter never mints a fresh array. The payload's `tools` is the FULL verdict list (incl. sub-threshold rows the incl.-patches check surfaces), but the ambient chip/dismissal machinery reads only this notable subset — so the AMBIENT feed stays policy-driven (a patch-only ambient finding never lights the chip). The empty base is a **module-level frozen `EMPTY_TOOLS` constant** (`Object.freeze([])` cast through `unknown` to the mutable `UpdateTool[]` contract — frozen at runtime for a real no-mutation guarantee, typed mutable so the cast doesn't ripple through `updateChipToolSummary`/`buildUpdateActions` consumers) so `?? EMPTY_TOOLS` never mints a new array per render (a fresh `[]` would break referential memoization downstream).
- `ambientKey = updateAvailable?.key ?? null` — the server-computed composite key.
- `manualTools = manualCheck?.tools ?? EMPTY_TOOLS` (same frozen fallback, same referential-stability reason) and `manualKey` = `computeUpdateKey(manualTools)` in a `useMemo` over `manualTools`, `null` for an empty set — the manual feed has **no server-computed key**, so its key is composed client-side (§ Manual composite key).
- **The merge — `deriveUpdateFeed(ambientTools, ambientKey, manualTools, manualKey, isDev, dismissedKey): { tools, key, manualOnly }`** — a pure exported function, called once here and once by `dismissUpdate`, so the precedence rule exists in **exactly one place** and no surface can drift. **Ambient-first**: `ambientShows = !isDev && ambientTools.length > 0 && ambientKey !== dismissedKey`; when `ambientShows || manualTools.length === 0` it returns the ambient shape with `manualOnly: false` — so with no manual result held, or with the ambient chip live, every field is purely ambient-derived (the frozen `EMPTY_TOOLS` identity included) and the manual feed is inert. Only when the ambient feed does NOT show and a manual result holds tools does it return `{ tools: manualTools, key: manualKey, manualOnly: true }`. (`260807-s6zs`)
- `singleRunKit = tools.length === 1 && tools[0].tool === "run-kit"` (`RUN_KIT_TOOL`) — the one shape that keeps the historical single-version chip/palette wording. Computed over the EFFECTIVE `tools`, so a manual-fed single run-kit row gets the same `⬆ v{latest}` chip form as an ambient one.
- `latest = singleRunKit ? tools[0].latest : null` / `current = singleRunKit ? tools[0].current : null` — the run-kit-row versions, surfaced ONLY for the single-run-kit `⬆ v{latest}` chip label + its transition title. Null otherwise (a multi/non-run-kit chip uses the count form + per-tool detail). The palette carries no version-bearing entry — `run-kit: Update Now` is a static label.
- **`qualifies = !isDev && tools.length > 0`** — computed over the effective feed. The frontend trusts the server's `update-available` verdict for the per-tool threshold decision (the backend already applied it); it only additionally suppresses the `dev` daemon and requires a non-empty set. It does NOT re-run any semver compare. The `dev` sentinel suppresses a manual-fed chip exactly as it suppresses an ambient one.
- **`showChip = qualifies && key !== updateDismissedKey`** — the composite-key dismissal gate, over the effective `key`. A later verdict with a DIFFERENT key re-shows the chip: on the ambient side because the changed key re-broadcasts `update-available`, on the manual side because a check finding a different tool set composes a different `computeUpdateKey`.
- **`manualOnly: boolean`** — true exactly when the surfaced fields came from the manual feed (false when the ambient feed shows, and false when neither does). The feed indicator `use-update-click` branches on for click routing, so no consumer re-derives the merge. (`260807-s6zs`)
- Returns `{ qualifies, showChip, tools, key, manualOnly, singleRunKit, latest, current, updateNow, dismissUpdate, daemonVersion, brew, forceUpdateNow, restartNow }`. `daemonVersion` is surfaced raw because `buildMaintenanceActions` does its own `version === "dev"` gate.

**Manual composite key — `computeUpdateKey(tools)` (`lib/palette-update.ts`)**: a pure exported helper composing **sorted `tool@latest` pairs, comma-joined** (`""` for an empty set) — a verbatim client-side mirror of the backend's `computeKey` (`app/backend/internal/updatecheck/updatecheck.go`), which composes the `key` the ambient payload carries. It lives beside `updateChipToolSummary` in the context-free pure-helper module for this surface (colocated `palette-update.test.ts`), not inline in `session-context.tsx`, so the sort/join semantics are testable without mounting the provider. Composing the manual key in the backend's exact shape is what lets the ONE `runkit-update-dismissed` slot serve both feeds unchanged. (`260807-s6zs`)

### Reload guard — `shouldReloadOnVersion` (post-restart auto-reload, boot-aware)

After a one-click upgrade the daemon restarts, which drops the state socket; the reconnect replays a **different** `version` global event (a set-once cached slot delivered once after `hello` over `/ws/state` — see `architecture.md` § State Socket), and the open tab is stale (it embeds the old frontend assets baked into the old binary). The guard auto-reloads it exactly once. The `StateSocket`'s resubscribe-on-reconnect replays the global slots, so the `version` listener fires on every reconnect. The guard also fires on a same-version restart (a plain `POST /api/restart`, a config-change/wedge-recovery restart, or a restart initiated from SSH) via the per-process `boot` id — so a tab that reconnects to a **new process at the same version** is not left silently running stale in-memory state: (`260714-3law`, `260716-qf3j`)

- The pure exported predicate is **4-arg**: `shouldReloadOnVersion(firstVersion: string | null, firstBoot: string | null, nextVersion: string, nextBoot: string | null): boolean`. Logic, in order: (1) `firstVersion === null` → `false` (**never reload on first connect** — no reload loop); (2) `nextVersion !== firstVersion` → `true` (a version change means new assets); (3) **same version** → reload on a **boot change** UNLESS the version is `"dev"`: `nextVersion === DEV_VERSION` returns `false` (the **air-recompile storm guard** — under `just dev`, air recompiles the backend on every save, minting a fresh boot id each time; reloading every dev tab on every recompile would be a reload storm), else `nextBoot !== null && nextBoot !== firstBoot`. A **null `nextBoot`** (older daemon, mixed-version window) never triggers the boot branch. (`260713-4zap`, `260714-3law`)
- `applyVersion(version, boot, brew)` (the `version`-listener callback): sets `daemonVersion` + `isBrew`; if `shouldReloadOnVersion(firstVersionRef.current, firstBootRef.current, version, boot)` → `location.reload()` (and returns); else, **only on the first-ever version** (`firstVersionRef.current === null`), stamps BOTH `firstVersionRef.current = version` and `firstBootRef.current = boot`. Both `firstVersionRef` and `firstBootRef` (new in `3law`) are `useRef` (not state — the reload decision must not re-render) so `applyVersion` stays referentially stable.
- Coverage this buys: update (version change), plain restart (boot change, same version), every open tab (the version slot is replayed per SSE connect), and restarts initiated from SSH or crash-recovery (the signal is server-originated, not client-action-originated).

### Top-bar L3 UpdateChip (`UpdateChip` in `top-bar.tsx`)

A self-contained chip **leading the L3 right cluster** (the L3 order is UpdateChip → Refresh, with the exempt overflow chevron right-most). Reads `useUpdateNotification()`. It is a registry candidate: its bar/menu presence is 100% registry-driven (registry `hidden: !showChip` keeps it out of the candidate set when it wouldn't render, so it never reserves width or an empty slot). Below `sm` it does not vanish; under width pressure it overflows into the chevron menu, where its function merges into the fixed version row (§ Right cluster overflow / § Steady-state version surfaces → surface 5). The palette update surface provides keyboard-first parity; the menu also reaches the update surface on phones. (`260715-h1ck`) The chip IS the **promoted/single-action form** of the unified update button whose resting form is the overflow menu's version + ⟳ row; the split is verdict-derived — the chip renders in-bar when `showChip` and bar space allow, otherwise the update surface appears on the version row. It stays single-action (no ⟳ segment): checking is a resting-row affordance only, never on the promoted chip (`260720-ml7k`).

- **Hide gate**: `if (!showChip || tools.length === 0) return null` — the hide conditions (no qualifying update / dismissed-for-key / daemon is `dev`) are folded into `showChip` inside the hook.
- **Single/multi presentation** (R15): the visible label is `⬆ v{latest}` when the single matched tool is run-kit (`singleRunKit`), else a count form `⬆ updates (N)` for any other single tool OR multiple tools. The chip runs a SCOPED update of exactly the matched tools, so the `title`/`aria-label` must say WHICH tools move: `restLabel = singleRunKit && current ? `Update run-kit: v{current} → v{latest}` : `Update: ${updateChipToolSummary(tools)}``, where `updateChipToolSummary` composes the per-tool `tool v{a} → v{b}` transitions (a tool with no known current degrades to `tool → v{latest}`). Accent styling (`border-accent-green text-accent-green` + `rk-glint` CRT-glint hover — the button hover vocabulary, § Hover-Animation Vocabulary).
- **Click → one-click trigger** via the shared **`useUpdateClick()`** hook (`hooks/use-update-click.ts`), consumed by BOTH the chip and the overflow-menu version-row update surface so the updating-state + failure catch/toast is ONE implementation, never a mirrored copy. `triggerUpdate` early-returns if already `updating`, records the composite `key` at click time (`clickKeyRef`), sets `updating`, then `void run().then((result) => consumeUpdateWatchTarget(result, navigate, addToast)).catch(...)`. **The resolved result drives a watch affordance** (`consumeUpdateWatchTarget`, exported from the same file and shared with the palette's restart wrapper, `260812-z1ya-update-daemon-tmux-window`): `status: "already-running"` with a `watch` target → navigate STRAIGHT to the job window's terminal route `/$server/$window` (window param = the `@N` id verbatim — the route's `parse`/`stringify` already maps it; the second click IS the jump); a fresh spawn (`202`, `watch` present) → an info toast `<session>:<window> is running` whose **"Watch" action slot** navigates to the same route (auto-navigating on every click would be disruptive); `watch` absent (old daemon) → no-op (no toast action, no navigation). **The click routes by feed**: `run = manualOnly ? forceUpdateNow : updateNow` — a surface lit from the MANUAL feed fires the full-roster force update (`POST /api/update {"force":true}`), exactly what the check toast's own "Update Now" action slot already runs; the ambient feed keeps the SCOPED `updateNow()` path unchanged. The scoped path exists to move only the server's `Snapshot().Matched` set, which a side-channel manual verdict never populates — a scoped click on a manual-fed chip would find nothing to do (§ Design Decisions → Manual-fed clicks force). On a request FAILURE (409 daemon-down / not-brew / no-match / network) it clears `updating` + shows an error toast — unchanged in shape on both paths. (`260807-s6zs`) The busy state renders a `<LogoSpinner size={12}>` + `<span>{"updating…"}</span>` — the busy label is a JS string expression (a real `…` U+2026), NOT raw JSX text (JSX text does not process JS escape sequences, so a raw `…` would render six literal chars; `update-chip.test.tsx` asserts the exact visible text `updating…`).
- **Two `updating`-clearing paths** (R13): (1) run-kit IN the spawned scope → the daemon restarts, the state socket drops, and the reconnect's differing version reloads the tab (discarding this state); (2) SIBLINGS-ONLY scope → NO daemon restart, so no reload comes — the post-remediation re-check (R17, architecture § internal/updatecheck) broadcasts a cleared/changed `update-available` whose composite `key` differs from the click-time key, and a `useEffect` keyed on `key` clears `updating` when `key !== clickKeyRef.current` (including the cleared empty key). The effect keys on the hook's EFFECTIVE `key`, which for a manual-fed click is the manual composite key; the manual state itself clears on an ambient-key change, which flows through as an effective-key change. A FAILED brew upgrade leaves the key unchanged, so `updating` persists until a page reload / the palette force path — the accepted residual, and a manual-fed **siblings-only sub-threshold** update sits in the same envelope (no daemon restart, and the R17 re-check re-broadcasts an unchanged/empty ambient key, so `updating…` and the manual chip persist until reload; no completion machinery was added for it).
- **Dismiss `✕`**: a small sibling button (rendered only when `!updating`, `aria-label="Dismiss update notice"`) → `dismissUpdate()` (composite-key localStorage). Dismissal silences only the chip; the palette action remains (keyboard-first recoverability, §V).

### Palette parity — `run-kit:` actions via `buildUpdateActions` (`lib/palette-update.ts`)

Keyboard-first parity (§V): the pure helper `buildUpdateActions(qualifies, tools, onDismiss): UpdatePaletteAction[]` (in `lib/palette-update.ts`, unit-testable, mirroring `lib/palette-move.ts`) returns, when `qualifies && tools.length > 0`, the **single** entry:

- `{ id: "run-kit-dismiss-update", label: "run-kit: Dismiss Update Notice", onSelect: onDismiss }` — the keyboard mirror of the chip's `✕`; `onDismiss` writes the composite key.

and an empty array otherwise (signature: `(qualifies, tools, onDismiss)` — no update callback). There is **no version-bearing update entry here** — the update action is `run-kit: Update Now` in `buildMaintenanceActions` (§ Maintenance palette entries), THE single update action (see § Design Decisions → Single static `Update Now`). **Gated on `qualifies` ALONE — the palette deliberately IGNORES chip dismissal** (`qualifies`, NOT the chip's `showChip`): dismissal silences the ambient chip, but the palette is deliberate discovery. The per-tool `updateChipToolSummary` helper (consumed by the chip + overflow-menu aria) is the single exported source in this module, so the top bar and the menu can never drift (R15 / A-024).

**A layout-level global group** (§ Single palette mount + palette-actions slot): `buildUpdateActions` is folded into the global groups built once by `use-global-palette-actions.ts` (the `updateActions` memo over `useUpdateNotification()`'s `qualifies`/`tools`/`dismissUpdate`), so every route — boards and the Host page included — gets the entry from the single palette mount. Below `sm` the top-bar UpdateChip is hidden, so the palette is a phone user's only update surface. (`260714-3law`, `260720-n2ai`)

### On-demand check commands — `buildCheckActions` + `composeCheckToast` + `useUpdateCheck` (`260720-n2ai`; two-backend split `260720-wb3n`)

Two on-demand check commands make "is anything updatable right now?" reachable without waiting on the ambient 6h chip or firing a blind force-update — keyboard-first (§V), reporting via the existing toast system (§ Post-pin success feedback + toast optional action for the toast primitive). The pure builder `buildCheckActions(version, onCheck, onCheckIncludingPatches): UpdatePaletteAction[]` (`lib/palette-update.ts`, unit-tested) returns:

- `{ id: "run-kit-check-updates", label: "run-kit: Check for Updates", onSelect: onCheck }` — reports tools crossing their notify threshold (`notable`).
- `{ id: "run-kit-check-updates-patches", label: "run-kit: Check for Updates (incl. patches)", onSelect: onCheckIncludingPatches }` — reports every tool with any pending update, annotating sub-threshold rows.

Both POST the same `/api/updates/check` endpoint but select **TWO BACKENDS** (`260720-wb3n`): the default check runs shll's `released` manifest source (POST body `{}`), while incl.-patches requests the FRESH `github` release-tags source (`checkForUpdates("github")` → body `{"source":"github"}`) — a SIDE-CHANNEL query the daemon answers without touching the cached verdict/chip. The two commands thus become "two backends", not "same data, two filters": the default can report all-up-to-date while incl.-patches lists updates, purely from the shll.ai manifest's ~1-day lag behind a just-cut release (the github source is the workaround). **Hidden on the `dev` sentinel** (`version === "dev"` → `[]`, same gating pattern as `buildMaintenanceActions`; a `null` version — no version event yet — counts as non-dev). **No brew gate** — checking is harmless without a remediation path (unlike `Update Now`).

**`composeCheckToast(tools, includePatches, source = ""): { message, updatable }`** (pure, unit-tested) composes the result toast over the verdict list: the default view filters `notable`, the incl.-patches view filters `updateAvailable` with sub-threshold rows annotated `(patch — below notify threshold)` (e.g. `tu v0.9.1 → v0.9.2 (patch — below notify threshold)`), reusing `updateChipToolSummary`'s per-tool `tool v{a} → v{b}` shape; an empty filtered set composes `All tools up to date` (`updatable: false`). The `source` argument is the ECHOED report backend from the check response (`"released"`/`"github"`; optional, default `""` for old daemons — keeping today's behavior). **When `source === "github"` the sub-threshold annotation is SUPPRESSED**: that backend carries no notify policy, so every row lands `notable=false`, and a github minor bump (e.g. `run-kit 3.8.7 → 3.9.1`) would otherwise be mislabeled a sub-threshold patch. The `updateAvailable`/`notable` filtering is unchanged — only the annotation keys off the source.

**Shared `useUpdateCheck()` hook** (`hooks/use-update-check.ts`) wires the POST→toast flow ONCE for the palette's global check group AND the overflow-menu resting-row check affordance (the same anti-drift extraction as `use-update-click.ts`, § Design Decisions → Shared check-flow hook): it returns `{ runUpdateCheck, checking }`. `runUpdateCheck(includePatches)` maps `includePatches === true` → `checkForUpdates("github")` (the fresh github source) and the default check → `checkForUpdates()` (released), passing the response's echoed `result.source` into `composeCheckToast` so the sub-threshold annotation keys off what actually ran (`POST /api/updates/check`, tolerant parse, rejects on non-2xx with the server message), then shows ONE result toast (deliberately NO intermediate "checking…" toast — the endpoint responds synchronously ~1-2s). **`checking: boolean`** is the in-flight UI value (drives the overflow menu's ⟳ spinner/disabled form, § Steady-state version surfaces → surface 5). Single-flight is enforced by a **`useRef` flag checked synchronously** at the top of `runUpdateCheck` (early-return when set), with `checking` state purely driving the spinner — so a same-tick double-click can't slip past the not-yet-flushed state, and `runUpdateCheck` keeps a **stable referential identity** across the in-flight transition (`use-global-palette-actions.ts` memoizes the check-action array on it; adding `checking` to its deps would churn that array mid-check). Both the ref and the state clear in a `.finally` on success AND failure.

**The result is ALSO persisted onto the tab-local manual feed** so the finding survives the toast on the persistent update surfaces instead of evaporating after ~5s: in the same `.then`, `runUpdateCheck` calls `applyManualCheckResult(updatableSubset(result.tools, includePatches), result.source)`. The persisted subset is the SAME "updatable" filter `composeCheckToast` reports for that view (`includePatches` ⇒ rows with `updateAvailable`; default ⇒ rows with `updateAvailable && notable`) — a local `updatableSubset` helper mirroring `composeCheckToast`'s `relevant` predicate — so the chip and the toast can never disagree about what was found, and an all-up-to-date result persists an EMPTY set that clears any stale positive. The seam is read via `useContext(SessionContext)` DIRECTLY and called optionally (`applyManualCheckResult?.(…)`), the same provider-tolerant idiom `useUpdateNotification` uses, so the hook never throws in an isolated test mount without a `SessionProvider`. **The toast flow is untouched** — this is in addition to it, never a replacement. (`260807-s6zs`) The result toast:

- an **info** toast with the `composeCheckToast` summary;
- when something updatable was reported AND the daemon can actually update (**`brew && daemonVersion !== "dev"`** — the same gate as the `Update Now` palette entry), the toast's **action slot carries "Update Now"**, triggering the same `forceUpdateNow()` force-update flow (a `.catch` on it surfaces an `"error"` toast);
- on a failed check (502 shll-missing, 409 dev, network) an **error** toast surfaces the server's message — the manual check is fail-LOUD, unlike the fail-silent ambient loop.

The layout-level global builder consumes the hook: `use-global-palette-actions.ts` memoizes `checkActions = buildCheckActions(daemonVersion, () => runUpdateCheck(false), () => runUpdateCheck(true))`, folded into the global groups right after `updateActions`.

### Maintenance palette entries — `buildMaintenanceActions` (`lib/palette-update.ts`)

Two **palette-only** maintenance actions, independent of the qualifying-update gate that governs `buildUpdateActions` — they make **update** and **daemon restart** reachable from the web (remote/mobile over Tailscale, no SSH), per Constitution V (keyboard-first) and IV (no new chrome). There is **no chip, no confirmation dialog, and no dedicated toast** — both fire immediately on select, and the SSE drop followed by the boot/version-driven tab reload IS the success feedback (only a rejection surfaces a toast) — plus the shared watch affordance on the resolved result (`consumeUpdateWatchTarget`, below).

The pure builder `buildMaintenanceActions(brew: boolean, version: string | null, onForceUpdate, onRestart): UpdatePaletteAction[]` (co-located with `buildUpdateActions` in `lib/palette-update.ts`, same test surface) computes `isDev = version === "dev"` and returns:

- `{ id: "run-kit-force-update", label: "run-kit: Update Now", onSelect: onForceUpdate }` — **THE single update action** in the palette: a force update (POST `/api/update` `{"force":true}`, a full-roster `shll update`; idempotent, and it picks up patch-only bumps a scoped match set would skip). Included ONLY when **`brew && !isDev`**. NOT gated on a qualifying update, so it reaches patch releases and works before any check has run. Brew-gated because a non-brew install cannot self-upgrade (the backend brew 409 would reject it anyway).
- `{ id: "run-kit-restart", label: "run-kit: Restart Daemon", onSelect: onRestart }` — included whenever **`!isDev`** (**NO brew requirement** — restart works for any install method).

Neither appears on a `dev` daemon (both gated on `!isDev`). `Update Now` is the ONE update entry in the palette — a static label; version detail lives in the check-result toasts and the chip summary (see § Design Decisions → Single static `Update Now` for why there is no dynamic `Update to v{X}` entry).

**A layout-level global group** (§ Single palette mount + palette-actions slot) — palette-only is load-bearing here: below `sm` the top-bar chip cluster is hidden, and there is no chip surface for these two at all. `use-global-palette-actions.ts` memoizes `maintenanceActions` over `buildMaintenanceActions(brew, daemonVersion, …)` (reading `brew`/`daemonVersion`/`forceUpdateNow`/`restartNow` from `useUpdateNotification()`), folded into the global groups after `checkActions`. Each action wraps `forceUpdateNow`/`restartNow` in `.then((result) => consumeUpdateWatchTarget(result, navigate, addToast))` + a `.catch` toast-on-failure with **`"error"` severity** (messages `"Update failed"` / `"Restart failed"`) — the restart action shares the identical watch affordance with no special-casing (the post-restart reload discards its "Watch" toast harmlessly).

### Steady-state version surfaces

The `UpdateChip` above renders **only when a qualifying update is pending** and names only the TARGET version, so the running version is otherwise **invisible in the web UI** — a user filing a bug report or verifying a restart had to SSH in for `rk --version`. `260715-ifco` adds **four passive, chrome-level surfaces** that expose the *current* `daemonVersion` in steady state (frontend-only — the version already reaches every tab via `event: version`; no backend, no new routes/pages/buttons per Constitution IV, palette-first per V); `260715-h1ck` adds a **fifth**, the overflow menu's fixed version row (surface 5); `260724-6j1v` adds a **sixth**, the sidebar footer's resting version readout (surface 6). All **degrade gracefully on a null `daemonVersion`** (hide/omit, never a `vundefined` placeholder) and show the **`dev` sentinel as-is** (`run-kit dev`, not `vdev`) — pure display, unlike the dev-gated update/restart actions.

**Shared `displayVersion` helper + `lib/palette-version.ts`.** A new pure, dependency-free module `app/frontend/src/lib/palette-version.ts` (mirroring the `lib/palette-update.ts` pattern, colocated `palette-version.test.ts`) exports:

- **`displayVersion(version: string): string`** — the single frontend source of the v-prefix convention, mirroring the backend `displayVersion()` (`app/backend/cmd/rk/root.go:16-21`): prefixes `v` to a numeric version, leaves `dev` and any already-`v`-prefixed string bare (idempotent — never `vdev` / `vv0.6.2`). Reused by all three display surfaces below (avoids the magic-string conditional per-site). Chosen to live in the palette module rather than a standalone `lib/version.ts` — the palette builder already needs it and it is the new dependency-free home.
- **`buildVersionAction(version: string | null, onSelect: () => void): VersionPaletteAction[]`** — returns `[]` when `version` is null (omit, no placeholder), else a single `{ id: "run-kit-version", label: `run-kit: Version — ${displayVersion(version)}`, onSelect }`. The LABEL carries the version, so typing "version" in the palette answers "what am I on?" **without selecting anything**. `VersionPaletteAction` is a local type mirroring `palette-update.ts`'s shape.

**1. Command-palette version entry** (the primary discovery surface, §V). `buildVersionAction` is built once at the layout (`versionActions` in `use-global-palette-actions.ts`, folded into the global groups after `maintenanceActions` — § Single palette mount + palette-actions slot), for the same reach reason as the update actions: below `sm` the top-bar right cluster is hidden, so on a phone the palette is the ONLY version surface.
  - **Gated on non-null `daemonVersion` INCLUDING `dev`** — `buildVersionAction`'s only gate is the null check (pure display; the entry is present for a `dev` daemon).
  - **On select**: copies the **DISPLAYED form** (`v0.6.2` / `dev` — what-you-see-is-what-you-copy, matches the label; NOT the raw `daemonVersion`) via `lib/clipboard.ts` `copyToClipboard`, then toasts `addToast(ok ? "Version copied" : "Copy failed", ok ? "info" : "error")`.

**2. Connection-dot version title** — the sidebar-footer dot's hover Tip folds in `Connected — run-kit {displayVersion(daemonVersion)}`; full mechanics in § Sidebar footer status row.

**3. Host-page footer stamp** (`host-overview-page.tsx`) — a small passive BIOS/boot-style footer line `run-kit {displayVersion(daemonVersion)}` at the **bottom of the scroll container, after the SERVICES `</section>`** (out of the zone hierarchy — a POST-footer, on-brand with the CRT/terminal aesthetic). `text-xs text-text-secondary font-mono`, no border, no interaction, **no hover treatment** (passive means passive — the hover-animation vocabulary categories don't apply to inert text). Reads `daemonVersion` from `useSessionContext()` and renders **nothing when null** (`{daemonVersion && (…)}`, absence not a hidden placeholder). The Host page `/` is the only route with no top-bar version surface on phones, and its BIOS-footer aesthetic fits the stamp best.

**4. UpdateChip transition title** — the pending-update chip's `title`/`aria-label` show `v{current} → v{latest}`; mechanics in § Top-bar L3 UpdateChip and the `current` field in § Context state + `useUpdateNotification()` hook.

**5. Overflow menu's fixed version row** (`260715-h1ck`, `top-bar-overflow-menu.tsx`) — the top-bar overflow chevron's menu **always** contains a fixed version row as its last item (so the chevron renders even when nothing overflows; full mechanics in § Right cluster → overflow chevron menu). It reuses the same `displayVersion(daemonVersion)` + `copyToClipboard` machinery as the palette version action: renders `RunKit v{version}`, click copies the DISPLAYED form and toasts (`"Version copied"` / `"Copy failed"`); renders plain `RunKit` — no `v`, no `vundefined` — when `daemonVersion` is null. This is the sixth home of `displayVersion` and a chrome (not palette) version surface reachable on touch at any width.

The row has TWO forms, chosen by the **verdict-derived** `asUpdateSurface` computed in `TopBarOverflowMenu` from `useUpdateNotification`: `asUpdateSurface = tools.length > 0 && (updateOverflowed || (qualifies && !showChip))`. The `updateOverflowed` prop (`showChip && the update-chip entry is in the overflow set`, computed in `top-bar.tsx`) covers the undismissed-overflowed case; the `qualifies && !showChip` disjunct (`260720-ml7k`) covers the **dismissed-pending** case. Placement between this row and the in-bar `UpdateChip` (surface 4) is ALWAYS derivation — from `showChip` + overflow measurement — never imperatively moved (§ Design Decisions → Dismissed-pending shows the menu update surface).

- **Update-surface form** (the surface-4 UpdateChip's overflow twin): `asUpdateSurface` true → the row FLIPS from copy-mode to update-mode — `RunKit v{current} → v{latest} ⬆` for the single-run-kit match, else the count form `Toolkit updates (N) ⬆`; it is a `role="menuitem"` whose click triggers the update instead of copying, and no separate UpdateChip menu row is added (its function merges here). Both the chip and this row consume the shared `useUpdateClick()` hook (`hooks/use-update-click.ts`) — updating-state + failure catch/toast is ONE implementation (spinner + `updating…` while in flight), never a mirrored copy. The aria/summary reuses the shared `updateChipToolSummary` (single source, no bar↔menu drift).
- **Resting form** (the unified update button's resting version row, `260720-ml7k`): `asUpdateSurface` false → the version copy button PLUS a dev-gated ⟳ "Check for updates" affordance. The ⟳ renders whenever `!asUpdateSurface && daemonVersion !== DEV_VERSION` (a `null` version counts as non-dev — the same gate as the palette check entries). It is a **plain, non-`menuitem` control** (`tabIndex={-1}`) so the container's role-keyed close handler does not fire — the menu stays open across the ~1-2s check and the in-flight spinner/single-flight state stays visible (§ Design Decisions → Check affordance is a non-menuitem control). Arrow-nav still reaches it via the `button:not([disabled])` focusables selector. It uses the lucide `rotate-cw` SVG in the 24px/`coarse:30px` bordered-square step-button treatment (the refresh vocabulary, same glyph as `RefreshButton`). Click runs `runUpdateCheck(false)` — the plain notable check through the shared `useUpdateCheck` hook (info toast via `composeCheckToast`, error toast on failure; NO incl.-patches variant, that stays palette-only). While `checking` it shows a `LogoSpinner size={12}` and is disabled (single-flight; see surface's hook above).

**6. Sidebar-footer version readout** (`260724-6j1v`, `sidebar/index.tsx` `SidebarFooter`) — the **resting** version line in the footer's left readout segment, immediately right of the connection dot: bare `displayVersion(daemonVersion)` (`v0.9.3`) at 10px `text-text-secondary`, a click-to-copy `<button>` reusing the same `displayVersion` + `copyToClipboard` + `"Version copied"`/`"Copy failed"` toast machinery as the palette action and surface 5, with `aria-label` in the menu row's `RunKit v… (copy)` form and a `Tip label="Copy version" placement="top"`. It renders **nothing** when `daemonVersion` is null (`{versionText && …}`) — no `RunKit` fallback and no dead copy target, unlike surface 5's plain-`RunKit` form. It is a **passive readout only**: the overflow menu's version row remains the update surface with its ⟳ check affordance (§ Design Decisions → Footer version is a readout; the menu row stays the update surface).

**Chevron attention badge** is keyed on the **undismissed-overflowed case ONLY** — `showBadge = updateOverflowed && tools.length > 0`, unchanged from `h1ck`. It deliberately does NOT light for the dismissed-pending update surface: the surface itself appears (deliberate discovery, like the palette's `qualifies` gate) but the ambient badge stays silenced (dismissal silences ambient chrome).

**`copyToClipboard` success signal.** `lib/clipboard.ts` `copyToClipboard` returns `Promise<boolean>` — `true` on a successful copy (Clipboard API OR the `execCommand` non-secure-context fallback), `false` when both fail. Callers that `void`-ignore the return (`terminal-client.tsx`, `status-panel.tsx`) are unaffected; the palette-version `onSelect` consumes the boolean to toast confirmation vs. error (fail soft with an error toast in a non-secure context). The return-value cases (Clipboard-API success, `execCommand`-fallback success, total failure) are unit-tested in `terminal-client.test.ts` (the home of the `copyToClipboard` describe block).

## Shell Grid Layout

The `<Shell>` wrapper (`app/frontend/src/components/shell/shell.tsx`) is the shared layout for `AppShell` and `BoardPage` (the Host page `/` uses a plain `flex-col h-full` instead — no Shell). **The Shell grid carries NO `topbar` row** — the TopBar mounts once in the persistent root layout, full-width above the Shell (see § Chrome (Top Bar) → persistent root mount). The visual stack (full-width bar over sidebar+content) is preserved. (`260707-4vq2`)

The **desktop** (`≥ 640px`) grid is sidebar+content over bottombar:

```
gridTemplateAreas: "sidebar content" / "sidebar bottombar"   (rows: 1fr auto)
```

`gridTemplateColumns` stays `${sidebarWidth}px 1fr` when `sidebarOpen` else `0 1fr` (a `150ms ease-out` collapse transition, zero-width column when closed — no rail). The **mobile** (`< 640px`) branch is single-column `gridTemplateAreas: "content" "bottombar"` (rows `1fr auto`), with the sidebar rendered as an `absolute` overlay whose grid child spans `gridRow: "1 / 3"` (the content+bottombar rows; the Shell grid has no topbar row — see § Sidebar Mobile). The `sidebar` grid area is **Shell-owned** — `Shell` renders the desktop `<aside style={{ gridArea: "sidebar" }}>` itself from `sidebarChildren` (see § Sidebar Desktop); consumers place only the `content` and `bottombar` areas via `style={{ gridArea: "content" | "bottombar" }}`. (`260707-4vq2`, `260719-rwqf`)

When the terminal route fills the optional **`rightPanelChildren`** slot (the `sidebarChildren` pattern mirrored; board/host consumers pass nothing and get the two-column grid byte-identical), the desktop grid gains a full-height third column: `gridTemplateAreas: "sidebar content rightpanel" / "sidebar bottombar rightpanel"` with columns `${sidebarWidth}px 1fr auto`. The `rightpanel` area spans both rows, so the rail+panel run to the viewport bottom and the bottom bar + compose strip scope to the content column. Shell renders the `<aside style={{ gridArea: "rightpanel" }}>` itself and — diverging deliberately from the sidebar aside's unmount-on-collapse — hides it at display level (`hidden`, children stay mounted) when `rightPanelVisible` is false (right-panel spec P3: web/code iframes keep in-memory state; § Design Decisions → Collapse is Shell grid-column gating). Shell also publishes its grid element through `ShellGridRefContext` (`useShellGridRef`) — the measurement seam the panel's width basis reads (§ Right Panel). (`260812-nm4p-top-bar-rail-toggle`)

**Shell height is `100%`** — Shell fills the root layout's `flex-1` content region rather than sizing itself to the viewport. Shell does not call `useVisualViewport()` or consume `--app-height` directly (both live in `RootWrapper` — see § iOS Keyboard Support). `useSidebarKeyboardToggle` (Cmd+\ / Ctrl+\) is registered in Shell — the chord is live only on routes that mount a `<Shell>` (AppShell + BoardPage). (`260707-4vq2`)

## Chrome (Top Bar)

The full-width top bar is the app's primary chrome and doubles as the navbar. **It mounts ONCE at the persistent root layout, above the router `<Outlet>` — not once per page** (see § Persistent root mount below). `TopBar` (`app/frontend/src/components/top-bar.tsx`) accepts a `mode: "terminal" | "board" | "server" | "host"` prop (default `"terminal"`) selecting the left-nav content; the left nav is a breadcrumb bar. Everything below (per-mode breadcrumbs, center heading, right-cluster pyramid, connection dot) describes `TopBar`'s internal rendering. (`260702-6m46`, `260707-4vq2`, `260715-zs1y`)

### Persistent root mount (`RootTopBar` / `AppLayout`)

`TopBar` mounts once at the persistent root layout and re-renders in place across route changes (§ Design Decisions → TopBar mounts once at the root layout).

**Where it mounts.** A **pathless layout route** (`id: "app-layout"`, `router.tsx`) uniformly parents every page route (index, server, terminal, board), so every navigation keeps the match chain `[root, app-layout, <leaf>]`. Its component `AppLayout` (`app.tsx`) sits at a **stable match depth with a stable route id and is never remounted** across navigation; it renders a flex column — `<div className="app-root flex flex-col" style={{ height: "var(--app-height, 100vh)" }}>` — with `RootTopBar` in a `shrink-0` wrapper above a `flex-1 min-h-0` content region. The bar keeps a stable React identity and re-renders (props change, no unmount) as the URL flips. The pathless layout normalizes tree depth (§ Design Decisions → TopBar mounts once at the root layout). The `AppLayout` wrapper is a plain `div`, NOT `<header>` — `TopBar` already renders its own `<header>` banner landmark, so a second would nest two `role="banner"` landmarks.

**Suspense blast radius.** The `<Suspense fallback={null}>` boundary wraps ONLY the `<Outlet>` content region (inside `AppLayout`), not the header. So when the lazy board chunk loads, only the page body blanks — the bar stays painted.

**Prop delivery — two channels.** `TopBar`'s inputs split by how synchronously they must resolve:

- **Route-derived (synchronous, at root)**: `RootTopBar` (`app.tsx`) derives `mode` + `boardName` from `useMatches()` — a deepest-first walk over route params (`window` ⇒ `terminal`, `name` ⇒ `board` + `boardName`, `server` ⇒ `server`, none ⇒ `host`; param names are unique across the tree, the same walk `SessionContext` uses for `currentServer`). This flips the instant the URL changes, so the heading never waits on the incoming page's mount — critical for the lazy board (`Board: <name>` renders from the URL param while the chunk still loads). `mode`/`boardName` are deliberately NOT in the slot.
- **Page-registered (a slot context, `contexts/top-bar-slot-context.tsx`)**: pages publish their data/handler props — `sessions`, `currentSession`, `currentWindow`, `sessionName`, `windowName`, `sidebarOpen`, `server`, `onNavigate`, `onToggleSidebar`, `onCreateSession`, `onCreateWindow`, plus board extras (`paneCount`, `serverCount`, `waitingPaneCount`, `boards`, `focusedPane` — the focused-tile split target — `onRequestKill` — the board Kill row's kill-dialog opener — and `autofit`/`onToggleAutofit` for the board autofit toggle, § Board Autofit Toggle) — via `useRegisterTopBarSlot(...)` (an effect: publish on mount/update, **clear to `null` on unmount**, last-writer-wins). This follows the `FocusedTerminalProvider` precedent (referentially-stable dispatcher held in a `useRef`, `useMemo`'d context value) and keeps the heavy handlers where they live — `AppShell`'s `navigateToWindow` (View-Transitions gate) and `handleCreateSessionInstant` (optimistic ghosts) are published by reference; **no logic lives at root**. (`260708-738w`, `260715-6jwn`, `260718-co9z`)

**Tolerant-empty defaults.** When no page has registered yet (first frame after navigation, or a lazy chunk still loading), the slot value is `null` and `RootTopBar` renders the route-derived mode with the tolerant-empty prop shape every `TopBar` mode already supports (`sessions=[]`, `currentSession=null`, `currentWindow=null`, `sessionName/windowName/server=""`, `sidebarOpen=false`, no-op callbacks). No crash, no stale props from the prior page (the clear-on-unmount guarantees this).

**Not-found is a dedicated boolean channel.** `TopBarSlotContext` carries a `notFound` flag SEPARATE from the page-data `slot`. `NotFoundPage` (`router.tsx`) — the `app-layout` route's `notFoundComponent`, rendered inside the `<Outlet>` below the persistent bar — calls `useSignalTopBarNotFound()` (sets `true` on mount / `false` on unmount); `RootTopBar` reads `useTopBarNotFound()` and forces `mode="host"` (nulling `boardName`) BEFORE the route-param walk. This is required because TanStack Router's fuzzy not-found handling RETAINS the partially-matched params in `useMatches()` (e.g. `/board/x/y` keeps `name=x`), so the param walk alone would derive `board` mode ("Board: x") over the not-found body. A separate boolean keeps `NotFoundPage` — which owns no TopBar data — from publishing the full slot shape, and avoids coupling to a router-internal field + hardcoded route-id string. Because `RootTopBar` sits ABOVE the `<Outlet>`, the in-Outlet signal is inherently one render behind: a ~1-frame "Board: x" flash before the effect lands is unavoidable without reading router internals — not user-perceptible, and the settled state is unaffected. (The `/a/b/c` shape is a DIFFERENT arm: it renders `AppShell`'s `ServerNotFound` early-return, not `NotFoundPage`, so `notFound` stays false there and the `server`/`terminal` mode derivation is kept.)

**Per-page registration.** No page mounts `<TopBar>`; pages that own TopBar data register into the slot context instead: `AppShell` registers its terminal/server data (the `terminal` vs `server` distinction is derived at root); `BoardPage` registers the board extras (`boardName`, counts, `boards`, `focusedPane`, `onRequestKill: requestKillFocused` — the kill-dialog opener; it keeps the `HELP_URL` import for its palette action). `HostOverviewPage` registers **nothing** (wrapper `flex flex-col h-full`) — the connection state that was its only payload now reaches the Sidebar as a prop, and `/` has no sidebar (§ Design Decisions → App-global actions live in the top bar). Edge pages (`NotFoundPage`, `ServerNotFound`, `ServerWaiting`) center with `h-full` so they fit the content region below the persistent bar without overflow. (`260718-co9z`)

**Achievable-invariant note (e2e).** No component in the persistent chain (`RootWrapper`/`AppLayout`/`RootTopBar`/`TopBar`) remounts across navigation, so the bar stays continuously present with a per-route heading — this is what `tests/e2e/top-bar-persistence.spec.ts` asserts (mount-effect stability + per-route heading), NOT strict DOM-node identity. TanStack Router 1.168's per-match Suspense re-commit recreates the header host node on the `→ /` (index-route) transition; that is a router-internal reconciliation artifact that does NOT remount the component and does NOT blank the bar (the heading renders synchronously from route state), so it is not a user-visible flicker. The spec's brand-crumb hop is a RELOAD BOUNDARY — the brand is a raw `<a href="/">` TanStack Router does not intercept, a full document navigation — so only the client-side hops carry the no-remount claim.

**Non-goals.** Sidebar/BottomBar/Shell stay per-page (they still remount on `/$server` ↔ `/board/$name`); `TopBar`'s internal rendering across all four modes is unchanged; frontend-only (no backend/API/tmux change).

`app.tsx`, `board-page.tsx`, and `host-overview-page.tsx` (Host page `/`) register into the slot context rather than rendering `<TopBar>` directly (see § Persistent root mount above). The right-section control cluster is a **strict 3-level cumulative pyramid**: a stable always-block pinned right renders in every mode, with terminal+board extras and terminal-only extras growing leftward — so no shared button ever changes screen position between pages. Only which levels appear gates on mode; see § Right cluster below. (`260704-9o7k`)

**Layout** (`border-b-[3px] border-border` — a 3px structural seam, see § Border-Width System): a single line, a **3-column CSS grid `grid grid-cols-[1fr_auto_1fr] items-center gap-2 py-2`**. `1fr auto 1fr` makes the CENTER cell truly centered regardless of asymmetric left/right widths. LEFT cell = a flex wrapper holding the hamburger (first, outside the nav) + `<nav aria-label="Breadcrumb">` (the nav ends at the parent — see per-mode below); CENTER cell = the **universal `PageType: name` page heading, FILLED IN EVERY MODE** (terminal `WindowHeading` + ▾ window switcher / board display heading + ▾ board switcher / server display heading / solo `Host`; § Chrome (Top Bar) center cell); RIGHT cell = the control cluster (`justify-self-end`). No Line 2. The header itself carries `px-3` plus — outside the desktop shell — the `pt-[env(safe-area-inset-top)]` safe-area guard (§ Safe-Area Insets — 0 in browsers, non-zero only in standalone PWA mode where `viewport-fit=cover` exposes the status-bar strip; in-shell the class is `isShell()`-gated off, because the `ShellTitlebarStrip` already reserves the titlebar band and macOS hidden-titlebar windows can report it as `safe-area-inset-top` — padding on top would reserve the band twice); the center cell is `flex items-center justify-center min-w-0`, no flex `gap` — the boot sweep owns the prefix→name spacing via its own space cell. (`260703-5ilm`, `260704-pr0p`, `260724-2bmy`, `260805-9hn1`)

**Left cluster** — the `1fr` left grid cell is a flex wrapper (`flex items-center gap-1.5 min-w-0`) holding, in order, the **hamburger**, the **history ◀ ▶ arrows** (`HistoryNav`), then the **breadcrumb nav** — the macOS convention (sidebar toggle → back → forward → brand crumb). Both the hamburger and the arrow pair are siblings BEFORE the nav (outside the `nav[aria-label="Breadcrumb"]` landmark — neither a drawer toggle nor global history is a breadcrumb item); `min-w-0` lets the nav shrink below its content inside the `1fr` column. The center heading's true centering is untouched by the wrapper. (`260720-ap63`, `260731-oiho`)

**Hamburger** — the sidebar toggle (`aria-label="Toggle navigation"`, a Notion-style rounded-rect pictogram whose left column fills when `sidebarOpen`), the FIRST element of the left cluster (standard drawer-toggle position — VS Code / Slack), rendered before and outside the breadcrumb nav. Present on every mode that has a sidebar; `hasSidebar` is DERIVED as `mode !== "host"` (no separate prop) — the host mode is the only sidebar-less mode, so it gets no hamburger (the brand shifts left ~30px there; no ghost slot is reserved). It joins the top-bar button-control size vocabulary via the shared fixed-size token `TOP_BAR_BUTTON_BASE` (28px fine / 30px coarse, `shrink-0` included — the same token the HistoryNav arrows and the whole right-side cluster use, NOT the bottom-bar's 36px; § Right cluster → shared button-size token), composed with its own `text-text-primary` (the toggle carries page-level chrome weight). It is a **bordered chip** in the sibling vocabulary (`rounded border border-border hover:border-text-secondary` — the same treatment the HistoryNav arrows and `LINK_CRUMB_CLASS` crumbs carry), so hover is the full `rk-glint` treatment: green icon + sweep + border flip. Its `HamburgerIcon` renders at **16px** (a `18`-unit viewBox scaled down) — the ~13–14px optical scale of the icons inside neighboring chips; the same icon renders mirrored at the bar's far right as the rail toggle's glyph via its `side="right"` prop (§ Right cluster → rail toggle chip). (`260720-ap63`, `260724-2bmy`, `260812-nm4p`)

**Brand root crumb** (all modes, the nav's FIRST child): the logo icon (`/icon.svg`, always visible) + `hidden sm:inline` "RunKit" wordmark wrapped in a single `<a href="/">` (`aria-label="RunKit home"`). It is the breadcrumb's root crumb, so the `›` separator starts after it. This IS the home affordance — there is no separate "Host" crumb and no right-side "RunKit" anchor. The brand carries the `LINK_CRUMB_CLASS` bordered-chip treatment (see § Crumb Affordance Vocabulary) and `rk-brand-glitch` hover, plus `min-h-[28px] coarse:min-h-[30px]` on the anchor itself — the shared control height axis, so the brand, the hamburger, and the HistoryNav arrows all sit on ONE horizontal axis (same `y`, same height) instead of the brand riding 4px shorter on coarse pointers. The `min-h` lands on the brand anchor only, NOT inside `LINK_CRUMB_CLASS`: the server crumb — the class's other consumer — is an inline truncating link where `min-h` is inert without a flex display. Below `sm` it collapses to the bare icon so long crumbs still fit the single-line 375px topbar. (`260702-6m46`, `260720-ap63`, `260724-2bmy`)

**Separator** — `›` (U+203A, `aria-hidden`, `BreadcrumbSeparator`), the palette-label convention (`<session> › <name>`) between every crumb level. No `/` separator. (`260702-6m46`)

**Host** (`/`, `mode="host"`): brand crumb ONLY — no hamburger, no crumbs, no terminal-font control, no split/close buttons, and **no fixed-width button** (terminal-only; the 900px effect never applied to the Host page — `host-overview-page.tsx` never reads `fixedWidth`). Renders only the L3 always-block (UpdateChip when qualifying + Refresh + the Settings gear) plus the exempt overflow chevron. It is the one route with **no sidebar-footer status row** (no sidebar to hold one), so it shows no connection dot or resting version readout — but the app-global actions are all present on host mode like every other mode: the Settings gear in-bar and Help / Keyboard shortcuts / Theme… as the chevron menu's App-section rows (§ Right cluster → Settings gear; § Design Decisions → App-global actions live in the top bar). It registers nothing into the slot context — it owns no TopBar data. Session/server-dependent props are passed empty (`sessions=[]`, `currentSession=null`, `currentWindow=null`, `sessionName=""`, `server=""`, no-op callbacks) — the same tolerant-empty shape board mode uses. The Host page's page identity is the **solo `Host` heading in the top-bar center cell** (a `PageHeadingDisplay` with `prefix=""` + `name="Host"` + `solo` — primary-medium, visible at all breakpoints; § center cell), plus the in-page `Host Overview` SectionHeading (§ Page Overview Headings). A "RunKit Host" doubled word is avoided — it would duplicate the adjacent brand crumb. (`260704-9o7k`, `260704-pr0p`, `260715-zs1y`)

**tmux Server** (`/$server`, no `$window`, `mode="server"`): the left cluster is **hamburger + brand** — nothing more (the breadcrumb nav holds just the brand crumb). The server name is the **`tmux Server: <server>` heading in the CENTER cell** (a display-only `PageHeadingDisplay`, no rename), NOT a left-nav leaf (move-don't-copy: the current-page leaf is the centered heading; a name never appears twice in the bar). "Dashboard" appears nowhere. (`260702-6m46`, `260704-pr0p`)

**Terminal** (`/$server/$window`, `mode="terminal"`): the left cluster is hamburger + brand + `›` + server link + `›` + **session dropdown — and the breadcrumb ENDS THERE** (the hamburger precedes the brand, outside the nav) (the window identity lives in the centered `WindowHeading`, not the breadcrumb chain). The server crumb here is a plain `<a href="/$server">` (a link back to the tmux Server page, `LINK_CRUMB_CLASS` chip, `truncate max-w-[16ch]`) — NOT a dropdown (server switching stays in the command palette, `Server: Switch to …`). The session crumb is a `BreadcrumbDropdown` (session switch jumps to the session's first window; `+ New Session` action; session `max-w-[16ch] truncate` (matches the server crumb); the session name is snapshot-derived from the active `@N`, not a URL segment). **The window switcher lives in the center cell** — a label-less bare-`▾` `BreadcrumbDropdown` (`title="Window"`, `windowItems` + `+ New Window` action) sitting beside the `WindowHeading` (name-click = edit, `▾`-click = switch), so the window name is never duplicated in one bar. The centered `WindowHeading` carries a static prefix sibling span (§ Window Heading) — the two-tone `PageType: name` identity, `Window:` in every lens. See § Chrome (Top Bar) center cell / § Breadcrumb Dropdowns → Window switcher. No type-prefix words ("SERVER"/"tmux Server"/"session") — crumbs are instance names only; position conveys the level. The level IS named on hover via native `title` tooltips in the canonical vocabulary: brand `title="Host"`, server crumb `title="tmux Server"` (both link and leaf variants), session dropdown trigger `title="Session"` and the window switcher `title="Window"` (via BreadcrumbDropdown's optional `title` prop). (`260529-jad6`, `260703-5ilm`, `260704-pr0p`, `260714-uco1`)

**Center cell — the universal `PageType: name` page heading** (filled on every mode). The outer center `<div className="flex items-center justify-center min-w-0">` (no flex `gap` — the boot sweep's own `sp` space cell separates prefix from name) stays centered in the grid; it wraps an **inner anchor `<div className="flex items-center justify-start min-w-0 sm:min-w-[28ch]">`** — a `sm:`-gated min-width with LEFT-aligned content so the heading's left edge stops drifting as the instance name length changes (below `sm` the min-width is absent — space is scarce at 375px). The hierarchy ▾ lives INSIDE this anchored box (§ Center-heading stable anchor + nav affordances); the box carries **no history-arrow furniture** — `HistoryNav` is left-cluster chrome (§ Left cluster), so the box needs no width-compensation offset between its prefix and a neighbouring control. It renders per mode: (`260703-5ilm`, `260704-pr0p`, `260714-uco1`, `260731-oiho`)

- **terminal** (`mode === "terminal" && currentWindow`): `<HierarchyDropdown mode="terminal">` (left of the prefix) + `<WindowHeading>` (static `Window: <window>`, editable name — see § Window Heading) + the relocated bare-`▾` window switcher.
- **board** (`mode === "board" && boardName`): `<HierarchyDropdown mode="board">` + a `<PageHeadingDisplay prefix="Board:" name={boardName}>` (display-only — boards have no rename API) + the `<BoardSwitcher>` ▾ (moved here from the left `BoardModeBreadcrumb`).
- **server** (`mode === "server" && server`): `<HierarchyDropdown mode="server">` + `<PageHeadingDisplay prefix="tmux Server:" name={server}>` (display-only).
- **host**: `<PageHeadingDisplay prefix="" name="Host" solo>` — a solo type word, no instance name, primary-medium, visible at all breakpoints; NO hierarchy ▾ (the root has no ancestors).

The type-prefix constants live once in `top-bar.tsx`: `WINDOW_PREFIX = "Window:"`, `BOARD_PREFIX = "Board:"`, `TMUX_SERVER_PREFIX = "tmux Server:"`, `HOST_SOLO = "Host"` (title case). Because the cell lives in the `auto` middle grid column, it stays centered under the topbar regardless of the breadcrumb / control-cluster widths. The `WindowHeading` (and the display headings) are deliberately NOT keyed on the route identity (no remount `key`) so they persist across window/page switches — that is what lets the boot sweep replay on navigation and keeps an in-progress inline edit alive (an external windowId/server switch mid-edit CANCELS the stale edit instead, since committing the typed name onto the newly-navigated window would rename the wrong one). (`260714-uco1`, `260715-zs1y`)

### Center-heading stable anchor + nav affordances

Four navigation affordances around the top-bar page heading — the anchor, the hierarchy ▾, the history ◀ ▶ pair (left-cluster chrome, § Left cluster), and their `Go:` palette parity — no new routes and no backend surface: (`260714-uco1`, `260731-oiho`)

- **Stable left anchor.** The outer center cell stays `justify-center` in the grid; an INNER container `flex items-center justify-start min-w-0 sm:min-w-[28ch]` reserves a `sm:`-gated min-width with LEFT-aligned content, so the heading's left edge stops drifting as the instance name length changes (min-width over fixed-width+truncate — long names grow rightward, accepted). Applies uniformly across all four modes; below `sm` the min-width is absent (375px). The `28ch` value is monospace-exact, visually tuned. The inline-rename `<input>` is `text-left` so the name does not jump on edit entry (§ Window Heading → edit).
- **Hierarchy ▾ dropdown** (`HierarchyDropdown`, reuses `BreadcrumbDropdown`). A bare-`▾` sibling of the rename button, bound LEFT of the prefix (reads `▾ Window: name`), listing EXACTLY the current page's ANCESTOR chain — no lateral jumps. Terminal: `tmux Server: {server}` (→ `/{server}`) then `Host` (→ `/`); board & server: `Host` only; **solo Host: not rendered** (the root has no ancestors). `hidden sm:inline-flex` — below `sm` it rides with the hidden prefix span (the hamburger/sidebar covers mobile nav). Being a sibling of the rename button, clicking it never enters inline edit. Navigation routes through the typed `useNavigate()` (params validated). It is intentionally redundant with the left breadcrumb.
- **Browser-history ◀ ▶ arrows** (`HistoryNav`). Bordered `rk-glint` icon buttons on the shared fixed-size token (`TOP_BAR_BUTTON`, § Right cluster → shared button-size token), rendered in the **LEFT cluster** immediately right of the hamburger (before the brand crumb on Host, which has no hamburger) on ALL FOUR modes (history is global) — the macOS ordering, and the reason the center box needs no width-compensation furniture (§ Left cluster). The pair sits in its own `shrink-0` group so the arrows never collapse under breadcrumb pressure. **`hidden lg:flex`** — the pair is a rung of the left cluster's degradation ladder (§ Mid-width overlap degradation): below `lg` the rigid left cluster (toggle + arrows + the nav's `sm:min-w-[150px]` floor) exceeds its equal-`1fr` side track against a 28ch-capped long-name heading (worst case needs ~832px) and the overflowing nav would paint over the centered prefix — the exact overlap class the `260715-q8ey` invariant forbids (`top-bar-overlap.spec.ts`); browser back gestures plus the palette's `Go: Back` / `Go: Forward` carry coverage at narrow widths. They call TanStack Router's `useRouter().history.back()` / `.forward()` — BROWSER HISTORY, explicitly NOT sibling-window cycling. Forward is always-active (browser-chrome style — `canGoForward` is not reliably exposed, so a dim/disabled state is omitted; a no-forward click is a harmless no-op). **Within-server window hops retrace**: user-initiated window switches **push** history entries (`navigateToWindow` — see § URL as Resumable Bookmark → optimistic navigate), so Back/Forward step through every within-server hop, not only server boundaries. No dedup — `w1→w5→w1` yields three entries. History Back/Forward bypasses `navigateToWindow` (raw `router.history.back()/forward()`), so no slide transition plays on arrow navigation; the landed window's tmux is realigned for free by the deep-link intent effect (§ URL as Resumable Bookmark, contract 1 — its `hasAlignedToUrlRef` guard re-arms on every `windowParam` change). (`260715-m4xy`)
- **`Go:` palette parity** (Constitution V) via the pure `lib/palette-nav.ts` builder `buildNavActions(mode, server, handlers)` (+ colocated `palette-nav.test.ts`), mirroring `palette-move.ts`/`palette-view.ts`. Returns the two history actions (`Go: Back`/`Go: Forward`, ALWAYS) followed by the route-appropriate ancestor entries: `Go: tmux Server` when `mode === "terminal" && server`, then `Go: Host` **unconditionally** (the Host root is an ancestor of every palette-bearing mode — only the `terminal` extra differs). `NavMode` is `"terminal" | "board" | "server"` — the **palette-bearing subset of `TopBarMode`**: the root Host route carries no route params and resolves to the `"server"` arm, so there is no `host` mode (`TopBarMode` keeps its `"host"` member; only `NavMode` narrows). **Wired once at the layout level** (§ Single palette mount + palette-actions slot): `use-global-palette-actions.ts` derives the mode from the same deepest-first route-param walk `RootTopBar` does (a `name` param → `"board"`, a `window` param → `"terminal"`, anything else → `"server"`) and passes `router.history.back/forward` + `navigate(...)` bodies. No new app-level keybindings (browser-native Alt+←/→ already cover history). (`260714-uco1`, `260719-i996-nav-actions-board-wiring`)

**Board** (`/board/$name`, `mode="board"`): the left cluster is hamburger + brand + a **counts/hint span ONLY** (`BoardModeInfo` — `{N} pane[s] · {M} server[s] · ⌘[⌘] cycle`, no leading `›` because the hint is not a crumb). The board name + ▾ switcher live in the center heading (`Board: <board>` display + `<BoardSwitcher>`; § center cell); there is no left `Board ▸` home button (the brand crumb is the home affordance, and a left `Board` word would duplicate the centered type word — move-don't-copy). Right cluster: the L1 **merged split control** (wired to the focused tile's window via `focusedPane`) + the board-only **autofit toggle** (the one in-bar L2 survivor) + the L3 always-block. **Kill lives in the chevron menu** (`menuOnly` — § Right cluster): the menu's Kill row is a **confirm-gated window-kill** of the focused tile's window, opening the kill dialog with a default-focused `Unpin instead` escape (§ Board Kill Confirm Dialog); unpin lives on the tile-header pin-glyph + palette (§ Close pane / Kill / § Board Tile Header / § Board Autofit Toggle). The L3 block carries the connection dot, which shows an **AND over the board's attached servers** (green iff ≥1 entry AND every distinct attached server's `isConnectedByServer` slice is connected; `board-page.tsx` derives `boardConnected` and passes it). (`260702-6m46`, `260704-pr0p`, `260704-9o7k`, `260715-6jwn`, `260718-co9z`)

**Mobile (`< sm`)** — the brand collapses to the bare icon and intermediate crumbs (server link + session, plus their `›` separators, wrapped in `hidden sm:flex` spans) are hidden, leaving brand icon + the centered heading. The leaf is the **center-cell heading in EVERY mode**: Terminal → `WindowHeading` (the mobile leaf AND primary rename affordance, `coarse:min-h-[30px]` touch target), tmux Server → the `tmux Server: <server>` display heading, Board → the `Board: <board>` display heading, Host → the solo `Host`. **The two-tone `PageType:` prefix is hidden below `sm`** (`hidden sm:inline`) so mobile keeps just the name — EXCEPT the solo `Host` word (a name-equivalent, not a prefix), which stays visible at all breakpoints. The `▾` window/board switcher stays visible on mobile with its `+ New Window` / board-switch action. Long names truncate in the center cell (`max-w-[16ch]` on mobile, `sm:max-w-[28ch]`) — never wrap or squeeze neighbors. The topbar stays a single line at 375px (the documented invariant in `fab/project/context.md`). (`260704-pr0p`)

**Mid-width overlap degradation.** Between the `sm` breakpoint (640px) and ~900px (the common sidebars-open / half-screen browser-pane case), a deterministic **degradation ladder** (class-only in `top-bar.tsx`, no API/routing/backend change) keeps the left breadcrumb from painting over the centered heading: (`260715-q8ey`)

0. **History arrows hide below `lg`.** `HistoryNav` is `hidden lg:flex` — the ladder's widest rung, and the only one above the 640–900px band. The pair is rigid (`shrink-0` fixed-size buttons), so below `lg` the left cluster's floor (toggle + arrows + the nav's `sm:min-w-[150px]`) exceeds its equal-`1fr` side track against a 28ch-capped long-name heading — worst case needs ~832px — and the overflowing nav would paint over the centered prefix, the exact overlap this ladder exists to prevent. Coverage survives via browser back gestures and the palette's `Go: Back` / `Go: Forward`. (`260731-oiho`)
1. **Crumbs truncate first.** The two left-nav crumb wrapper spans (server-link crumb + session crumb) each gained `min-w-0` so the `truncate max-w-[16ch]` already present on their inner anchor / `BreadcrumbDropdown` trigger finally engages. **`min-w-0`-chain rule**: a nested flex item defaults to `min-width: auto`, which blocks `truncate` from shrinking the item until an explicit `min-w-0` is added at that level — the crumb text had been overflowing the nav box because the intervening wrapper span, not the anchor, was the blocked link in the chain. `BreadcrumbSeparator` and the brand chip stay `shrink-0` — the protected identity floor inside the nav; the hamburger sits outside the nav and carries its own `shrink-0`.
2. **Server crumb hides below `md`.** The server-link crumb wrapper is `hidden md:flex … min-w-0` — the first element to give way, because the hierarchy ▾ inside the heading prefix already lists `tmux Server: {server}` → `Host`, so the left server crumb is redundant navigation at cramped widths. The **session crumb stays at `sm+`** (it owns the session switcher + `+ New Session` action). (`260714-uco1`)
3. **Nav clips at its explicit floor.** The `<nav aria-label="Breadcrumb">` carries an explicit breakpoint-aware floor `min-w-[46px] sm:min-w-[150px]` plus `overflow-hidden`. The floor budgets only nav content — the hamburger sits outside the nav and carries its own `shrink-0` + min sizes: below `sm` the floor guarantees just the bare brand icon; at `sm+` also a session-crumb sliver. The left cell's TOTAL floor (hamburger + gap + nav floor) is what holds the left budget. **Clip-backstop rule**: `overflow-hidden` converts any residual overflow past the shrunk floor into a clean clip at the nav's right edge instead of paint-over overlap — overlap becomes structurally impossible, not merely unlikely. The `min-w-[46px] sm:min-w-[150px]` values hold across the full Playwright sweep (375 / 640 / 700 / 768 / 1024px) with the 30px coarse hamburger, no clip or wrap regression — the top-bar-overlap spec is the harness. (A clipped child keeps a layout box wider than its clipping parent, which is expected; the meaningful proofs are the no-intersection assertion + computed `overflow:hidden`, not a layout-box comparison.)

**Center protection.** The center `auto` grid column is protected by holding its bounded **content floor**, NOT by a magic pixel min-width. The center cell's OUTER wrapper (`flex items-center justify-center`) carries no `min-w-0`, so the `auto` column can not be squeezed below the heading's content. That floor is *bounded* because the heading name spans carry `max-w-[16ch] sm:max-w-[28ch] truncate` (WindowHeading button + PageHeadingDisplay) and the history ◀ ▶ / hierarchy ▾ / window & board ▾ controls are fixed-width `shrink-0`. The inner box's **`sm:min-w-[28ch]` stable anchor stays at `sm:` (NOT `md:`)** — the right cluster absorbs squeeze instead. Net effect: squeeze lands on the left (and right) `1fr` columns; the center heading is never compressed into overlap across the 640–900px band. (`260715-q8ey`)

**Right-cluster degradation.** The right cluster is registry-driven and its `1fr` grid track is squeezable via `min-w-0` on the cell, so under width pressure controls collapse into a chevron menu instead of clipping (full mechanics in § Right cluster → overflow chevron menu below). It uses an explicit fit computation + move-to-menu, NOT `overflow-hidden` — a partial `overflow-hidden` on the right cluster would clip the *wrong* end (flex overflow spills toward inline-end, dropping the L3 always-block / the chevron first). The cell carries `min-w-0` but deliberately NO `overflow-hidden` (the only content that can exceed a very-narrow track is the always-present exempt chevron, which must stay visible/clickable per R6). Regression coverage lives in `tests/e2e/top-bar-overlap.spec.ts` (+ `.spec.md`): at ~700×800 with long window + session names, nav-box ∩ heading-box is empty and crumbs are ellipsised; server crumb toggles across `md`; 375px stays single-line; 1024px+ keeps the anchor intact. The width-sweep spec `tests/e2e/top-bar-overflow.spec.ts` (+ `.spec.md`) is the right-cluster's regression proof (see § Right cluster). (`260715-q8ey`, `260715-h1ck`)

**Right cluster** (`flex items-center justify-end gap-3 min-w-0`) — a **strict 3-level cumulative pyramid**, growing LEFTWARD from a stable always-block pinned right, so no shared button ever changes screen position between pages. The pyramid is consumed by a priority+ overflow menu (§ Right cluster → overflow chevron menu below): the L1→L2→L3 order encodes DROP priority (L1 drops first, L3 last) and the whole cluster is registry-driven, not hardcoded JSX. The tiers: (`260704-9o7k`, `260715-h1ck`)

| Level | Gate | Buttons (left → right) |
|---|---|---|
| **L1** — Open terminal-only + the merged split control terminal+board + the ▦ layout chip terminal-only (`260715-6jwn`, `260722-6d0f`, `260731-oiho`, `260812-ab5v`) | Open: `mode === "terminal" && currentWindow && has open targets`; split: `currentWindow` (terminal) OR `mode === "board" && focusedPane` (board); layout: `mode === "terminal" && currentWindow && layout slot registered` | **OpenButton (Open ▾ — terminal-only, § Open split-button)** · **SplitControl (the ONE merged split chip — primary click = horizontal/side-by-side, ▾ = both directions, § Split control)** · **LayoutChip (▦ — preset-shape popover, arity-valid glyphs, current marked; § Surface Layout → ▦ Layout chip)** |
| **L2** — board-only survivor | `mode === "board" && onToggleAutofit` | **BoardAutofitToggle (board-only — the only in-bar L2 control; Aa, fixed-width, and ✕/Kill are `menuOnly`)** |
| **L3** — always (all four modes) | none | **UpdateChip (⬆ v… — leading, conditional: hidden unless a qualifying, un-dismissed, non-dev update is pending — `260713-4zap`)** · **RefreshButton (⟳)** · **Settings gear (app-global, the last fit candidate — § Settings gear below)** |

**Terminal-mode end state: Open · Split(▾) · Refresh · Gear · chevron** (+ UpdateChip when a qualifying update exists) — three sticky/destructive controls were demoted to chevron-menu rows in `260731-oiho` (§ `menuOnly` demotions).

**Shared button-size token.** Every top-bar icon control is a FIXED square — 28×28 on fine pointers, 30×30 on coarse — from one shared constant set hosted in `top-bar-overflow-menu.tsx` beside `MENU_ROW_*`. Decomposed like `MENU_ROW_*` so state-driven callsites compose around the geometry rather than re-declaring a drifted subset: `TOP_BAR_BUTTON_BASE` (geometry only — the fixed square, rounded border box, centering, `shrink-0`), `TOP_BAR_BUTTON_REST` (resting/hover colors), `TOP_BAR_BUTTON` (`rk-glint` + base + rest — the default for a plain icon button), plus two HEIGHT-only tokens for content-width chips that must share the axis without a fixed width: `TOP_BAR_BUTTON_H` (`h-[28px] coarse:h-[30px]`) for self-bordered chips (UpdateChip) and `TOP_BAR_SEGMENT_H` (`h-[26px] coarse:h-[28px]`) for segments INSIDE a bordered chip wrapper (SplitControl / OpenButton / ViewSwitcher segment groups — the wrapper's border adds 2px, so 26+2 = 28 fine and 28+2 = 30 coarse make the chip's TOTAL box identical to the squares). **A fixed size, not a floor** — a `min-w-*`/`min-h-*` floor lets a box stretch with its content, so sibling controls in one cluster render at visibly different sizes; the fixed square makes that drift structurally impossible rather than correcting it per callsite (§ Design Decisions → Shared top-bar button token). Consumers: the hamburger (§ Hamburger), the rail toggle chip (§ Right cluster → rail toggle chip), the HistoryNav arrows, SplitControl/OpenButton/ViewSwitcher segments, FixedWidthToggle, the TerminalFontControl trigger, ClosePaneButton, RefreshButton, BoardAutofitToggle, the `TerminalFontMenuRow` stepper buttons, the overflow chevron trigger, and the version row's check-⟳; the brand crumb normalizes to the same height axis (`min-h-[28px] coarse:min-h-[30px]`, § Brand root crumb). (`260731-oiho`)

Each control carries the shared `rk-glint` chip styling over that token. There is no per-control `hidden sm:flex` breakpoint cliff: controls do not vanish below `sm`; under width pressure they overflow into the chevron menu at any width (§ Right cluster → overflow chevron menu). L1's absence on non-terminal pages just widens the gap to the breadcrumb; the L3 always-block keeps its fixed right edge (the brand anchor lives in the left nav as the root crumb). Mode deltas: (`260702-6m46`, `260704-9o7k`, `260715-h1ck`)

- **The bar mixes page-scoped controls with the app-global action cluster.** Split/close/font/fixed-width/autofit/view-switcher/Open act on THIS page, so they sit beside the page heading. Settings (an in-bar gear chip, the last fit candidate) and Help / Keyboard shortcuts / Theme… (`menuOnly` App-section chevron-menu rows) are app-global and live in the bar because the top bar is the one chrome surface mounted on EVERY route — including the sidebar-less Host page and the mobile drawer-closed state. App-global READOUTS (the connection dot, the resting version readout) stay in the **sidebar footer** (§ Sidebar footer status row), and notifications live in the **settings dialog** (§ Settings Dialog → Notifications row). The overflow pyramid solves *space*; the bar/footer split is about *reach*. (`260724-6j1v`, `260812-d1at`)
- **L3 order**: the conditional **UpdateChip leads**, then **Refresh**, then the **Settings gear** — `UpdateChip → Refresh → Gear` — with the always-present **overflow chevron** pinned right-most in the trailing exempt block in every mode (on desktop terminal routes a rail-toggle chip sits AFTER the chevron as the bar's outermost element — see the rail-toggle bullet below; § Right cluster → overflow chevron menu). The chip renders only when a qualifying, un-dismissed, non-`dev` update is pending, so its absence just tightens the gap (§ Update Notification). (`260713-4zap`, `260724-6j1v`, `260812-nm4p`, `260812-d1at`)
- **RefreshButton** is L3 — renders in every mode (a reload is meaningful everywhere): plain click `window.location.reload()`, Shift+click `forceReload()`. See § Refresh button.
- **Settings gear** (`SettingsGearButton`) is the L3 tail — the LAST registry fit candidate, rendered on ALL four modes immediately before the exempt chevron (the right-rail toggle keeps the outermost corner). A plain `TOP_BAR_BUTTON` chip carrying `GearIcon` (imported from `sidebar/icons.tsx` — the gear changes corners, not species), `aria-label="Open settings"`, opening the single AppLayout-mounted settings dialog via `useSettingsDialog().openSettings()` (no new event, no prop threading). Its `Tip label="Settings"` carries the HOST-effective `settings-open` chord in its `kbd` slot via the shared `useActionChord()` derivation (`byAction.get("settings-open")` → `formatCombo(…, host.platform)`), omitted when unbound/disabled. Under width pressure it degrades to the App-section "Settings" menu row (`SettingsMenuRow` — same glyph, same chord as a trailing keycap) rather than shrinking or clipping. See § Settings Dialog → Triggers. (`260812-d1at`)
- **`fixed-width` is `menuOnly`** (terminal-gated, `hidden: !currentWindow`) — its "Fixed width" `menuitemcheckbox` row is the only reachable form. The 900px `maxWidth` wrapper lives in AppShell (`app.tsx`), which renders both `terminal` AND `server`, so **the tmux Server page keeps the constraint and the `View: Fixed Width` palette access** — the entry's row is simply absent there (the effect is a no-op on Board/Host, which never read `fixedWidth`). See § Command Palette View actions.
- **`terminal-font` (Aa) is `menuOnly`**, gated `mode === "terminal" || mode === "board"` (a terminal-bearing surface: the single window or a board pane) — reachable as the menu's inline `−`/`+` stepper row, the settings dialog, and the palette's `Increase/Decrease/Reset terminal font`.
- **`close-pane` (✕) is `menuOnly` and mode-branched** (one component): the terminal row is a real close-pane (`closePane`) of the current window's active pane; the board row reads **Kill** and routes through `onRequestKill` to open the **kill confirm dialog** whose confirmed action is a window-kill of the focused tile's window (with an `Unpin instead` escape), never a direct `closePane`. Unpin lives on the tile header + palette, not here. See § Close pane / Kill / § Board Kill Confirm Dialog. (`260715-6jwn`, `260718-co9z`, `260731-oiho`)
- **BoardAutofitToggle is board-only and stays IN-BAR** — the L2 tier's only in-bar member, gated `mode === "board" && onToggleAutofit`. It is a per-board *working* control, not a sticky per-device preference, which is why it was left out of the `menuOnly` demotion set. Mirrors the `FixedWidthToggle` vocabulary (`aria-pressed`, `rk-glint`, the shared size token) and flips the per-board desktop autofit preference. See § Board Autofit Toggle. (`260708-738w`, `260731-oiho`)

**`menuOnly` demotions — the reduced bar (`260731-oiho`).** Three entries carry `menuOnly: true` on top of the pre-existing `view-switcher`: **`fixed-width`** and **`terminal-font`** (sticky per-device preferences that don't earn permanent bar slots — both already live in the settings dialog and the palette) and **`close-pane`** (destructive, and it sat one slot from Refresh — a misclick trap). Their bar forms are excluded from the visible row, the measurement probe, and the fit budget; their menu rows always render. The **coverage invariant** (Constitution V) holds without new palette entries — the existing `Pane: Close`, `View: Fixed Width (900px)` / `View: Full Width`, and `Increase`/`Decrease`/`Reset terminal font` actions already cover all three, and board Kill keeps its `board-kill-focused` palette entry. Board mode demotes uniformly: the Kill row keeps the `onRequestKill` dialog path. `autofit` is deliberately exempt (above). Reverting any demotion is deleting one flag (the `260722-n2n4` mechanism).

**Overflow chevron menu (priority+ degradation).** The pyramid above is consumed by a browser-toolbar-style priority+ overflow menu: as the right `1fr` track shrinks, controls drop into a dropdown behind an always-visible down-chevron rather than clipping or vanishing. Registry + measurement live in `top-bar.tsx`; the chevron/menu component is `components/top-bar-overflow-menu.tsx`; the fit math is `lib/top-bar-overflow.ts` (+ colocated `.test.ts`). (`260715-h1ck`)

- **Ordered registry drives the cluster.** The right cluster is built from an ordered `RegistryEntry[]` (`{ id, tier — implicit in position, modes: TopBarMode[], hidden?, menuOnly?, menuGroup: MenuGroup, barRender: () => ReactNode, menuRender: () => ReactNode }`), one entry per control — **`view-switcher` LEADS the list** (terminal-only, `menuOnly`); then the pyramid in drop order: L1 open · split · **layout** (terminal-only, `menuGroup: "view"` — the ▦ layout chip, § Surface Layout); `fixed-width` · `terminal-font` (both `menuOnly`) · autofit; `close-pane` (`menuOnly`); L3 update-chip · refresh · settings; then the `menuOnly` app rows `help` · `keyboard` · `theme`. The `open` entry is the **first L1 fit candidate** (immediately after the `menuOnly` `view-switcher`, so first to drop) — earliest-drop placement keeps it out of the L1/L2/L3 pyramid-drop invariants the overflow e2e asserts (§ Open split-button). One ordered source drives BOTH the bar (first-that-fit render `barRender` as icon buttons) and the menu (overflowed + `menuOnly` entries render `menuRender` as labeled rows), so bar↔menu can never drift. Optional gate fields filter each render: (a) **`hidden`** (an entry with `hidden` true renders NOWHERE — not bar, not menu, not the measurement probe) — `view-switcher` `hidden: !(mode==="terminal" && currentWindow && onSelectView && availableViews && availableViews.length > 1)`; `open` `hidden` when not terminal mode, no `currentWindow`, or zero available open targets (a control with an empty menu is a dead control — § Open split-button); `split` `hidden: mode === "board" ? !focusedPane : !currentWindow` (one expression covering both wirings); `layout` `hidden: !(mode === "terminal" && currentWindow && layout && onApplyLayout)` (hidden until AppShell registers the layout slot); `fixed-width` `hidden: !currentWindow`; autofit `hidden: !(mode==="board" && onToggleAutofit)`; `close-pane` `hidden: mode === "terminal" && !currentWindow` (the board row stays, disabled, on an empty board); `update-chip` `hidden: !showChip` (kept out of the candidate set entirely so it never reserves an empty slot); (b) **`menuOnly`** (below) — the entry NEVER renders in-bar and its `menuRender()` rows ALWAYS render in the menu (subject to `hidden`). `hidden` keeps its "renders nowhere" priority over `menuOnly`. A third field, **`menuGroup`**, names the menu section the entry's rows render under (§ Menu density + section labels) — it is pure row-placement data and does not gate anything. Gating is 100% registry data — no per-item `hidden sm:flex` empty-flex-item gap hack. (`260717-6anu`, `260722-n2n4`, `260722-6d0f`, `260731-oiho`, `260812-d1at`, `260812-ab5v`)

- **`menuOnly` — never-in-bar placement (`260722-n2n4`).** An optional `menuOnly?: boolean` on `RegistryEntry`: a `menuOnly: true` entry is excluded from the visible in-bar row, the hidden measurement probe, AND the width-fit computation (it contributes ZERO pixels to the fit budget), while its `menuRender()` rows ALWAYS render in the chevron menu (when the entry passes its `modes`/`hidden` gates), in registry (pyramid) order alongside the overflowed fit candidates. It is a generic registry capability, not a view-switcher special case — any control can opt in (§ Design Decisions → `menuOnly` is a registry capability). **Seven entries set it**: `view-switcher` (the chat lens is not fully functional, so the `[tty|web|code|chat]` pill is kept out of the navbar entirely and reached only via its `View:` menu rows, `Cmd/Ctrl+.`, the palette, and `?view=` deep links) plus `fixed-width`, `terminal-font`, and `close-pane` (§ Right cluster → `menuOnly` demotions), plus the app-global `help` / `keyboard` / `theme` entries — those three exist ONLY as App-section menu rows by design (their chrome home is the menu; the settings gear beside them is a regular fit candidate whose menu row is the overflow fallback). Each demoted control's in-bar component stays intact but unreachable, so reverting any one of them is removing its flag. (`260722-n2n4`, `260731-oiho`)

- **Drop order = fit candidates consumed from the LEFT; surviving buttons never shift.** The fit candidates are the registry entries minus the `menuOnly` ones (`fitCandidates = candidates.filter((e) => !e.menuOnly)`). Overflow eats them leftmost first — **L1 (`open` first to yield when present, else `split`), then the L2 survivor (`autofit`, board only), then L3** — so the surviving in-bar set is a SUFFIX of the fit-candidate order and every survivor keeps its exact screen position (the pyramid invariant). Implementation: the fit function reverses the measured widths (L3-end first), `computeVisibleCount` greedily fits from the tail, and the returned N trailing fit candidates stay in-bar while the leading `fitCandidates.length − N` overflow into the menu. `visibleItems = fitCandidates.slice(splitAt)`; **`overflowItems = candidates.filter((e) => !visibleIds.has(e.id))`** — candidates-minus-visible, so the `menuOnly` `view-switcher` (never in `visibleItems`) interleaves at its registry position and the `View:` rows lead the menu, all in one expression that preserves registry order for free and cannot double-count (§ Design Decisions → Overflow list is candidates-minus-visible). (`260717-6anu`, `260722-n2n4`)

- **Exempt items never overflow**: the exempt set lives in the **trailing block** (`trailingRef`), distinct from the `menuOnly` entries, which are not exempt but simply out of the fit entirely (always in the menu, never fit candidates). The block holds the **overflow chevron** — present in every mode — plus, when AppShell registers `onToggleRail` (every desktop terminal route), the **rail-toggle chip** as the bar's outermost right element. The block's measured width is the only thing RESERVED before fit candidates are placed. So `reserved = trailing + RIGHT_GAP_PX` (the trailing block plus one `gap-3` joining it to the last fit candidate). No `menuOnly` entry contributes probe width, so the fit budget is unaffected by which lenses the window offers or by the demoted controls' bar forms. (`260717-6anu`, `260722-n2n4`, `260724-6j1v`, `260731-oiho`, `260812-nm4p`)

- **Rail toggle chip** (terminal mode) — the sidebar toggle's far-right mirror and the bar's outermost right element, sitting inside the trailing exempt block AFTER the overflow chevron (its width is measured and reserved by the fit, so like the chevron it never overflows). Same fixed-size chip treatment as the sidebar hamburger (`rk-glint` + `TOP_BAR_BUTTON_BASE` + `border-border` + `text-text-primary`), `aria-label="Toggle panel"`, rendering the `HamburgerIcon` mirrored via its `side="right"` prop (fill rect `x=11.5`, divider `x=11.5` vs the left icon's `2.5`/`6.5`) — the icon's right column fills while the right area is visible. The chip toggles the RAIL STRIP ITSELF, never a panel surface: collapse hides the whole right column (rail AND any open panel) so the terminal runs edge-to-edge, and collapsing with a panel open also closes the panel (`removeStoredPanel` + `?panel=` drop — § Right Panel). Rendered only when `onToggleRail` is provided — AppShell registers it on `windowParam && !isMobile`, i.e. every desktop terminal route, even with zero available surfaces. The `railOpen` prop carries the DERIVED `rightAreaVisible` (`railOpen || panel open`), so the fill also tracks an open panel while the rail is collapsed. Keyboard path: the `Panel: Toggle rail` palette entry (§ Right Panel → Keyboard + palette). (`260812-nm4p-top-bar-rail-toggle`)

- **Attention badge**: when an attention-bearing item is overflowed — today only the UpdateChip, i.e. `showChip && overflowItems` contains `update-chip` (`updateOverflowed`) — the chevron carries a small accent-green dot so the update signal isn't lost when the chip is hidden into the menu. No badge when the chip is in-bar or no qualifying update.

- **Always-present menu + fixed version row.** The chevron renders even when nothing overflows, because the menu ALWAYS ends with a fixed `RunKit v{version}` row (§ Steady-state version surfaces → surface 5): click copies the displayed version + toasts; plain `RunKit` when `daemonVersion` is null. When a qualifying update is pending AND the UpdateChip is overflowed, that row FLIPS to the update surface (`RunKit v{current} → v{latest} ⬆`, click = `updateNow()`) and no separate UpdateChip menu row is added (the `update-chip` entry's `menuRender` returns `null` by design). The chip and the version-row update surface share the `useUpdateClick()` hook so the updating-state + failure toast can't drift.

- **Menu-row representations** (`menuRender` closures): view-switcher → **one `View: {label}` `role="menuitemradio"` row per available view** (`View: Terminal`/`View: Web`/`View: Chat`) via `ViewSwitcherMenuRows` in tty-first `orderViews()` order, active row inverse-video accent-green + **`aria-checked`** (the single-select radio state — `aria-pressed` is invalid on a `menuitemradio`), click = `onSelectView(view)` + close (§ ViewSwitcher chip → menu-rows form); these are the switcher's ONLY rendering (`menuOnly`) and lead the menu as `view-switcher` is the first registry entry; **layout → one `Layout: <Shape>` `menuitemradio` row per arity-valid shape** (`LayoutMenuRows` — the `ViewSwitcherMenuRows` precedent, current shape marked; § Surface Layout → ▦ Layout chip); open → **one `Open: {label}` row per available target** via `OpenMenuRows` (mirrors `ViewSwitcherMenuRows`, § Open split-button); **`split` → TWO rows, "Split horizontal" then "Split vertical"** (the merged entry's `menuRender` emits both `SplitMenuRow`s in both the terminal and board branches, horizontal first to match the primary — menu rows stay one-action-per-row even though the bar consolidated them into one chip), each row carrying the same trailing right-aligned keycap as the SplitControl popover rows (§ Split control); fixed-width → "Fixed width" `role="menuitemcheckbox"` (checked=`fixedWidth`); terminal-font (Aa) → an inline `−`/`+` stepper `role="group"` row operating on `terminalFontSize` within `TERMINAL_FONT_BOUNDS` (does NOT reopen the Aa popover); autofit → "Autofit panes" checkbox row; close-pane → **"Close pane" on terminal, "Kill" on board** (the board row carries the `onRequestKill` dialog path, § Board Kill Confirm Dialog — verb discipline: the row never reads "Close pane" on a board), honoring the same disabled conditions; refresh → "Refresh page" row (Shift+click force-reload preserved; the "page" word disambiguates from the `Status: Refresh` palette action, `260715-jykd`); settings → a "Settings" row (`GearIcon` + the trailing `settings-open` chord keycap, `SettingsMenuRow`); help → a "Help — run-kit docs" external-link row over the shared `HELP_URL` — an `<a target="_blank" rel="noopener noreferrer">` with a ↗ affordance, NEVER a button (external navigation must not unload the live dashboard's terminals/socket); keyboard → a "Keyboard shortcuts" row dispatching the `shortcuts-overlay:open` document CustomEvent (the layout-mounted overlay toggles), trailing keycap the HOST-effective `shortcuts-overlay` chord when bound; theme → a "Theme…" row dispatching the `theme-selector:open` document CustomEvent, leading `ThemeModeIcon` + the current effective mode (`system` / `light` / `dark`) in the trailing slot. Help/Keyboard/Theme reuse the shared `global-chrome.tsx` definitions (`HELP_URL`, `HelpIcon`, `ThemeModeIcon`) so the menu can never drift from the palettes. (`260812-d1at`)

- **Leading-glyph parity — every mirrored row leads with its in-bar button's glyph** (§ Shared top-bar control glyphs). A menu row that mirrors an in-bar icon control carries that control's exact glyph as its FIRST child, riding the row's existing `flex items-center gap-2` (`MENU_ROW_CLASS` / `POPOVER_ROW_CLASS` are untouched — no row-class change was needed). This completes the "bar↔menu can never drift" contract *visually*, not just behaviorally, and teaches the icon↔action mapping in both directions. Coverage is the full mirrored set: the two `SplitControl` popover direction rows, `SplitMenuRow` ×2, `ClosePaneMenuRow` (✕), `RefreshMenuRow` (rotate-cw), `SettingsMenuRow` (gear), `FixedWidthMenuRow` (arrows), `AutofitMenuRow` (frame-with-columns), `TerminalFontMenuRow` (the "Aa" text glyph), alongside the pre-existing `OpenMenuRows` / `OpenTargetRow` glyphs (§ Open split-button → Row icons); the menu-only App rows lead with their glyph too — `HelpMenuRow` (`HelpIcon`), `KeyboardMenuRow` (`KeyboardIcon`), `ThemeMenuRow` (`ThemeModeIcon`). **`ViewSwitcherMenuRows` carries no glyph** — its in-bar form is a text pill, so there is nothing to mirror; the command palette, breadcrumb switcher dropdowns, the desktop titlebar host-switcher menu, and the bottom-bar Fn-keys menu are likewise outside this rule. **On a toggle row the leading icon is IDENTITY and the trailing ✓ is STATE** (the macOS menu pattern): `FixedWidthMenuRow` and `AutofitMenuRow` render the glyph's *static* identity variant — contract/inward arrows and the unfilled frame — which does not flip across ON/OFF, leaving the trailing ✓ as the sole state marker (their in-bar buttons keep their live state-driven variants). Every glyph is `aria-hidden`, so row accessible names are unchanged and the e2e specs selecting by role/name (`top-bar-overflow`, `top-bar-refresh`, `board-autofit`, `board-close-and-unpin`, `tooltips`) pass unmodified. (`260801-3q1z`)

- **Row density + View / Window / App section labels.** Menu rows run at the chrome's compact scale — `MENU_ROW_BASE` is `w-full text-left flex items-center gap-2 px-2.5 py-1.5 text-xs` — and every other row style in `top-bar-overflow-menu.tsx` matches it (the update-surface row, the version copy button, the `TerminalFontMenuRow` wrapper), as do the external `MENU_ROW_*` consumers (`ViewSwitcherMenuRows`, `OpenMenuRows`) which inherit the scale for free. Rows are **grouped under thin uppercase section labels** in the fixed order **View → Window → App**: each registry entry names its section via `menuGroup: "view" | "window" | "app"` (`OverflowMenuRow` carries it as `group`), and `TopBarOverflowMenu` partitions `MENU_SECTIONS` over the rows, rendering only non-empty sections — so the partition preserves registry (pyramid) order *within* each section with zero re-sorting and the `View:` switcher rows keep leading the menu. Membership: **View** = view-switcher, layout, fixed-width, terminal-font, autofit; **Window** = open, split, close-pane/Kill; **App** = update-chip, refresh, settings, help, keyboard, theme, and the always-present version row (which rides at the App section's tail, so the App section always renders). Each section past the first is preceded by the established divider (`border-t border-border my-1`) and its label is an `aria-hidden` decoration (`px-2.5 pt-1.5 pb-0.5 text-[10px] uppercase tracking-wider text-text-secondary select-none`) — the a11y tree keeps a flat `role="menu"` of rows rather than gaining group semantics. **No labels render when the menu holds only the version row** (nothing overflowed and no `menuOnly` rows — server/host modes at wide widths), keeping the minimal one-row form intact. A row whose action has a matching registry binding carries a trailing right-aligned keycap (`MENU_ROW_KBD_CLASS`, § Split control) — the two split rows, the Settings row, and the Keyboard shortcuts row; rows without a matching binding (close-pane/Kill, view lens rows, Open target rows, fixed-width, terminal-font, autofit, refresh, help, theme) render none. (`260731-oiho`, `260811-0f3d`, `260812-d1at`)

- **Menu a11y mirrors `BreadcrumbDropdown`**: `role="menu"` panel / `role="menuitem"`/`menuitemcheckbox` rows, `position: fixed` anchored to the trigger rect (top just below, right-aligned — no ancestor `overflow-hidden` clips it, no new dependency), Escape closes + refocuses the chevron, ArrowUp/ArrowDown move focus, outside `mousedown` closes, glued to a moving trigger via scroll/resize reflow. **Row-click close keys on the target's ROLE, not the row wrapper** (review S1): a click landing on a `role="menuitem"`/`menuitemcheckbox` closes the panel (single-shot action), but the font stepper's `−`/`+` are plain buttons inside a `role="group"`, so repeated stepping does NOT close the menu.

- **Measurement mechanism** (in `top-bar.tsx`, collapse-first, no hysteresis): the right cell is given `min-w-0` so its `1fr` grid track is squeezable (the left/center floors make the track width grid-determined — no feedback loop, no oscillation). **The cell must FILL the `1fr` track — `flex … justify-end … min-w-0`, NOT `justify-self-end`** (M1, load-bearing): `justify-self-end` content-sizes the box, which both deadlocks the fit (with collapse-first `visibleCount=0` the box measures only the exempt block → budget < 0 forever) and never fires the ResizeObserver on window resize (a content-sized box's width doesn't track the viewport); default grid stretch fills the track and `justify-end` right-aligns content inside it. A single `ResizeObserver` observes the cell + a hidden off-screen `inert`/`aria-hidden` **probe row** (renders every FIT candidate's `barRender` so real widths are always known — buttons vary by UpdateChip/`coarse:` sizing, nothing hardcoded; **the `menuOnly` ViewSwitcher is NOT in this probe row**, index-aligned with the widths array) + the trailing chevron block (the only reserved node), so reserved/candidate width changes (e.g. the UpdateChip's `⬆ v{latest}` label width changing) re-fit too even though they don't resize the outer cell (S2). The measure effect's dep key is the fit-candidate id list plus `updateKey`/`showChip` — it carries no view-lens deps, because the `menuOnly` pill is not probed and its segment/active-lens changes cannot affect the fit. The pure `computeVisibleCount(availableWidth, itemWidths, reservedWidth, gap)` (front-consuming greedy fit, `gap` charged between rendered items only, clamp `[0, len]`, 0 for a non-positive budget) is unit-tested in isolation. Collapse-first: `visibleCount` starts at 0 (`useState(0)`) and is set in a `useLayoutEffect` before paint, so no flash of overflowing buttons. The cell carries `min-w-0` but deliberately **NO `overflow-hidden`** — the only content that can exceed a very-narrow track is the always-present exempt chevron, which must stay visible/clickable (R6); `.app-shell`/`header` clip any horizontal page overflow. (`260717-6anu`)

- **Keyboard path (Constitution V)**: the menu is a pointer affordance; the command palette exposes every affected action (the menu is itself keyboard-operable per its a11y contract). Coverage: `lib/top-bar-overflow.test.ts` (fit edge cases), `top-bar.test.tsx` / `update-chip.test.tsx` (chevron presence, menu open, version-row states, a working menu action), `view-switcher.test.tsx` (the `ViewSwitcherMenuRows` per-view rows in `orderViews()` order, active marking + aria, click callback, single-view `null`), and the width-sweep e2e `tests/e2e/top-bar-overflow.spec.ts` (+ `.spec.md`) — 1280→…→375px asserting no overlap, monotonic drop over the FIT candidates (L1 `split` first to yield — L2 is empty on terminal now that Aa/fixed-width/✕ are `menuOnly`, and the mobile leaf keeps the L3 Refresh in-bar at 375px: the asserted contract is the pyramid ORDER, not an all-controls-overflow cliff), the never-in-bar `menuOnly` contract for the three demoted controls, menu = exactly the dropped controls + the always-present rows + the section labels + version row, exempt items always visible, a menu action firing, and (on a multi-view window) the `View:` `menuitemradio` rows present at every width — including wide — with no `view-toggle` in the DOM and a `View:` row activation switching the lens. The e2e is the regression proof for the M1 fit-wiring (jsdom's zero widths mean the Vitest layer can't catch fit defects). (`260717-6anu`, `260722-n2n4`)

**Connection dot — a sidebar-footer readout, not a bar element.** The dot lives in the sidebar footer (§ Sidebar footer status row) with its markup, aria, and version-folding `dotTitle` intact; the bar carries no `[role="status"]` element and `TopBarProps`/`TopBarSlot` carry no `isConnected`. (`260724-6j1v`)

**A dot reads Connected only when the state socket is connected AND the page's relevant subscription has ACKED.** A bare socket connection is not enough — the subscription must have received its `ack` (the fresh snapshot). Per-caller derivation, each passed straight into `<Sidebar isConnected={…}>`: (`260716-qf3j`)

- **Terminal / tmux Server**: `AppShell`'s `dotConnected` — the current server's slice `isConnected` (`isConnectedByServer.get(server)`), true iff the socket is up AND that server's `subscribe` acked (its slice flips Connected on `ack`, and back on the socket-level disconnect debounce); in the chat lens it is the chat subscription's health instead (§ Connection dot = chat stream health in chat mode). Reconnect nuance (nice-to-have, `A-011`): on socket re-open a previously-acked server flips Connected before its fresh re-ack lands (ms-scale window).
- **Board**: `boardConnected` — AND over the board's attached servers (green iff ≥1 entry AND every distinct attached server's slice is connected, i.e. subscribed-and-acked).
- **Host `/`**: no dot at all — the Host page mounts no sidebar, so the indicator is simply absent (§ Design Decisions → App-global actions live in the top bar). The same holds for any state that unmounts the sidebar: a collapsed desktop sidebar (`Cmd+\`) and a closed mobile drawer.

**Help — a chevron-menu row, palette-mirrored.** The help affordance is the overflow menu's "Help — run-kit docs" App-section row (§ Right cluster → overflow chevron menu) over the shared `HELP_URL` (`https://shll.ai/run-kit`, `components/global-chrome.tsx`) — an external-link anchor (`target="_blank"`, never a button, so it cannot unload the live dashboard), rendered on every top-bar mode. The palette counterpart is the `Help: Documentation` action (`id: "help-documentation"`), one of the layout-level global groups (`use-global-palette-actions.ts`, § Single palette mount + palette-actions slot) over that same constant — menu row and palette entry can never drift. (`260704-hmd0`, `260812-d1at`)

**Split control** (`SplitControl` in `top-bar.tsx`): ONE split-button chip carrying both directions — the L1 registry `split` entry. A bordered chip wrapper (`inline-flex items-stretch rounded border border-border overflow-hidden`) holds two `TOP_BAR_SEGMENT_H` segments, the `OpenButton` precedent so the pair reads as ONE control at cluster scale:

**One direction vocabulary across every surface** — the chip, its ▾ menu, the overflow rows, both command palettes (`Window: Split …` in `app.tsx`, `Board: Split Focused Pane …` in `board-page.tsx`), and the shortcuts overlay's tmux rows (`api/keybindings.go`'s whitelist labels) all name directions the way tmux's own flags do: **horizontal** = `horizontal: true` = `split-window -h` = new pane BESIDE (side-by-side, vertical divider); **vertical** = `horizontal: false` = `split-window -v` = new pane BELOW (stacked). Horizontal is the default and is listed first wherever both appear. (`260806-2x2h`)

- **Primary segment** — split HORIZONTAL, the default: `<SplitHorizontalGlyph />` (§ Shared top-bar control glyphs), `aria-label` / `Tip` label "Split horizontally", `execute(true)` → `splitWindow(server, windowId, true, cwd)`, spinner (`LogoSpinner`) while pending — the glyph shows only at rest. The Tip carries the host-effective `split-horizontal` keycap in its `kbd` slot — registry-derived via `useKeybindings().byAction` + `formatCombo(…, host.platform)`, omitted when the binding is unbound/disabled (§ Tier-1 `Tip`).
- **▾ segment** — `aria-haspopup="menu"` / `aria-expanded`, opening a small `role="menu" aria-label="Split direction"` dropdown listing `Split horizontal` FIRST and `Split vertical` second (the complete option set, split-button convention — not default-only), each row **leading with its direction glyph** (`SplitHorizontalGlyph` / `SplitVerticalGlyph` — the same definitions the bar renders; § Right cluster → leading-glyph parity) and **trailing a right-aligned keycap** (`MENU_ROW_KBD_CLASS` — `ml-auto`, palette-row kbd visual weight, `aria-hidden` so the row's accessible name stays its label) showing the row's effective registry chord, omitted when unbound. The popover sizes to its content — `w-max min-w-[170px]` — so rows never wrap with keycaps present on any platform tier (⇧⌘D vs Shift+Ctrl+\). The chevron segment's own Tip (`Split… (choose direction)`) stays keycap-free — it opens a menu, there is no chord. The rows show the plain direction glyph even while a split is pending — pending is carried by the row's `disabled` dim; only the primary segment swaps to a spinner. Outside `mousedown` and Escape close; Escape refocuses the ▾. The primary is a **fixed** horizontal split, not last-used.

Backed by `splitWindow(server, windowId, horizontal, cwd)` from `api/client.ts` → `POST /api/windows/{windowId}/split` with `{ "horizontal": bool }`, via `useOptimisticAction` with a toast on error. Custom square-split glyphs from the shared module (§ Shared top-bar control glyphs). Best-effort — tmux may reject if the pane is too small. **Rendered on terminal AND board mode.** Terminal: `server`/`windowId` come from `currentWindow` (`server` from `useSessionContext()`). Board: the same component wired to the slot's `focusedPane` `{server, windowId, cwd}` — behavior parity with the pre-merge per-direction buttons. Window IDs are server-unique, so targeting a pinned window's ID works even though it lives in a hidden `_rk-pin-*` session; the split lands relative to that window's active pane and renders live inside the tile. Hidden when the board has no `focusedPane` (and on terminal without a `currentWindow`) — no `hidden sm:*` cliff, the entry overflows into the chevron menu under width pressure like every other candidate. The board keybindings that call `executeSplit` directly (`board-page.tsx`) are independent of this control. The overflow menu keeps both directions as separate one-action rows, horizontal first (§ Right cluster → menu-row representations). (`260715-6jwn`, `260731-oiho`, `260806-2x2h`)

### Shared top-bar control glyphs (`top-bar-icons.tsx`)

**One glyph definition per mirrored top-bar control, consumed by BOTH the in-bar button form and its chevron-menu / SplitControl-popover rows** — so bar↔menu visual parity is structural rather than copy-paste. The module exports seven components: `SplitVerticalGlyph`, `SplitHorizontalGlyph`, `ClosePaneGlyph`, `RefreshGlyph`, `FixedWidthGlyph({expanded?})`, `AutofitGlyph({filled?})`, and `TerminalFontGlyph`. It is the sibling of `open-app-icons.tsx` (§ Open split-button → Row icons) and follows that precedent's conventions: **~14px rendered size, `currentColor` stroke/fill** (so the glyph rides the row hover treatments and `rk-glint` accent-green flips for free), **`aria-hidden="true"` decoration** (the row/button's accessible name stays its text label or `aria-label`), **`shrink-0`**, and a kebab-case **`data-icon` attribute as the test seam** (`split-vertical`, `split-horizontal`, `close-pane`, `refresh`, `fixed-width`, `autofit`, `terminal-font`) — monochrome paths are otherwise indistinguishable to queries. (`260801-3q1z`)

**A viewBox/strokeWidth/join-parameterized local wrapper, not `open-app-icons.tsx`'s fixed `Glyph`.** `ControlGlyph({name, viewBox, strokeWidth, join, children})` defaults to the 24-viewBox / strokeWidth-2 / round-caps-and-joins shape the split, close, and refresh originals carry, and each stateful-toggle glyph overrides it: fixed-width is 14-viewBox strokeWidth 1.5 with **round caps only** (`join={false}` omits `strokeLinejoin` entirely), autofit is 14-viewBox strokeWidth 1.5 with caps *and* joins. Preserving each original's exact attributes is what makes the in-bar refactor zero-visual-change (§ Design Decisions → A dedicated `top-bar-icons.tsx`, and per-glyph exact SVG attributes).

**Stateful toggles expose a variant prop so one definition serves both forms.** `FixedWidthGlyph` takes `expanded` (outward/expand arrows when true, inward/contract when false) and `AutofitGlyph` takes `filled` (three 0.35-opacity `currentColor` pane rects over the frame when true). **Both default to `false` — the static identity variant.** The in-bar buttons pass live state (`expanded={fixedWidth}` / `filled={autofit}`, preserving their pre-existing state-driven flip); the menu rows render the bare default, so the leading icon never flips and the trailing ✓ stays the sole state marker (§ Right cluster → leading-glyph parity).

**`TerminalFontGlyph` is a text span, not an SVG** — an `aria-hidden` `<span data-icon="terminal-font">Aa</span>` in a fixed `w-[14px] shrink-0 inline-flex items-center justify-center font-semibold leading-none` box, matching the in-bar `TerminalFontControl` trigger where "Aa" reads as "text size". Font size inherits from the host (`text-xs` on both the trigger and the menu rows), so the glyph matches whichever surface renders it. The fixed 14px box is a sub-pixel (~0.4px) squeeze on the natural monospace width of "Aa" at `text-xs` — centered and unclipped, the one place the refactor is not byte-identical to the original markup.

**In-bar consumers**: `SplitControl`'s primary segment, `ClosePaneButton`, `RefreshButton`, `FixedWidthToggle`, `BoardAutofitToggle`, and the `TerminalFontControl` trigger — all render the shared components with their inline SVGs deleted. **Menu/popover consumers**: the two `SplitControl` popover rows plus `SplitMenuRow` ×2, `ClosePaneMenuRow`, `RefreshMenuRow`, `FixedWidthMenuRow`, `AutofitMenuRow`, `TerminalFontMenuRow`. Two inline rotate-cw copies remain outside the module's reach and are **deletion candidates** for a follow-up: `top-bar-overflow-menu.tsx`'s "Check for updates" button (byte-identical to `RefreshGlyph`, and it lives in this very menu) and `sidebar/status-panel.tsx`'s pane-header refresh (a sidebar control outside the top-bar vocabulary — a shared home would need a module neither `top-bar-icons.tsx` nor `sidebar/icons.tsx` owns today).

**Tests.** `top-bar.test.tsx` asserts, via the `data-icon` seam: every terminal-mode menu row leads with its glyph while row accessible names stay unchanged; both SplitControl popover rows lead with direction glyphs and the primary segment shares the `split-horizontal` definition; the in-bar Refresh renders the shared refresh glyph; `FixedWidthMenuRow` keeps a **static** identity glyph across ON/OFF with the trailing ✓ as the sole state marker; and on board mode `Autofit panes` leads with the UNFILLED identity glyph even when autofit is ON while `Kill` leads with the close glyph. No new e2e spec — the five existing specs that reference these controls pass unmodified (glyphs are `aria-hidden`, so no `.spec.ts` edits and hence no `.spec.md` companion updates).

### Open split-button (`open-button.tsx`)

**A Conductor-style "Open ▾" split-button in the top-bar right cluster that opens the active pane's folder in an editor/app** — the FIRST L1 registry candidate (immediately after `view-switcher`; § Right cluster), **Terminal mode only in v1** (the folder is the active pane's cwd, already derived server-side; server/board routes have no unambiguous folder). `OpenButton` is a two-segment control: a **primary segment** re-runs the last-used target, and a **chevron** opens the full menu. `OpenMenuRows` is the overflow representation (one `Open: {label}` row per target, mirroring `ViewSwitcherMenuRows`; § Right cluster overflow menu-row representations). (`260722-6d0f-navbar-open-in-app`)

**Two mechanisms, chosen by local/remote branch** (pure `buildOpenTargets({hostname, sshHost, sshUser, hostApps, path})` in `lib/open-in-app.ts`, which derives `local` and the effective deeplink host internally):

- **Deeplink targets** — from the static `DEEPLINK_APPS` const (`lib/open-in-app.ts`): VS Code · Cursor · Windsurf, each a `(host, path) => "{scheme}://vscode-remote/ssh-remote+${host}${path}"` template (vscode/cursor/windsurf schemes over the shared `ssh-remote` grammar). No API, no detection — a plain frontend const (the templates live in run-kit's frontend only; architecture.md § Design Decisions → Open-in-app). Shown on **any remote** client (all three unconditionally — client installs are unknowable, a dead scheme no-ops on click); the section's visibility is not gated on `RK_SSH_HOST`. Activation is a plain user-gesture `window.location.href = url` navigation — the browser shows its own "Open <app>?" confirm.
- **Host targets** — from `GET /api/open-apps` (the `wt` registry). Shown as the sole section on a **local** client, and as an explicitly labeled **"on host"** escape hatch on a remote client. Activation calls `openInApp(server, path, app)` (`POST /api/open`) with an error toast on failure. Hidden when the registry is empty.

**Deeplink host resolution** (pure `resolveDeeplinkHost({sshHost, sshUser, hostname})`): (1) `RK_SSH_HOST` set → use it **verbatim, never `user@`-prefixed** (an alias carries user/port/key from the client's `~/.ssh/config`); (2) unset AND remote → derive `${sshUser}@${location.hostname}` (the name the browser reached the host by — SSH-reachable on a tailnet — plus the server-derived username from `GET /api/health`; the `user@` prefix is **omitted when `sshUser` is empty**, leaving the bare hostname, and the hostname is used as-given with no reachability guessing); (3) local → empty.

**Local/remote branch** keys on `isLocalHostname(location.hostname)` (∈ `localhost`/`127.0.0.1`/`[::1]`). **Section visibility composes to zero-target hiding**: the deeplink section shows for **any remote client** (no `sshHost` gate), the host section is hidden when the registry is empty — when no targets remain the whole control renders nothing (registry `hidden`; a control with an empty menu is a dead control). A local client with an empty registry still renders nothing.

**Split-button behavior + last-used preference.** The primary segment re-runs the **last-used target**, persisted to `localStorage["runkit-open-last-used"]` (`LAST_USED_OPEN_TARGET_KEY`, the `runkit-*` preference convention — same shape as `runkit-terminal-font-size` / `runkit-last-pinned-board`, best-effort try/catch-noop so private mode / quota never throw). With **no stored preference** (or a stored target that is no longer available), the primary click **opens the menu instead**. Clicking a menu target runs it AND persists its id as last-used, then closes the menu. Target ids are **kind-qualified** (`deeplink:vscode` / `host:vscode`) so deeplink and host entries for the same editor never collide.

**Primary segment last-used glyph.** When `lastUsed` resolves non-null (`resolveLastUsedTarget(targets, readLastUsedOpenTarget())`), the primary segment renders `<OpenTargetIcon target={lastUsed} />` (`open-app-icons.tsx`, § Row icons) before the kept "Open" text, so the segment shows WHICH app a primary click launches. The glyph is `aria-hidden` `currentColor` decoration — it rides the segment's `text-text-secondary hover:text-text-primary` flip for free (a small `gap` spaces it from the text) and stays out of the accessible name. **No glyph when `lastUsed` is null** (nothing stored, or the stored id no longer resolves against live targets — e.g. a cleared SSH host removed the deeplink section); the segment then looks text-only, exactly as the chevron-opens-instead case above. The accessible name is unchanged either way (`Open in {label}` when resolved, `Open in app` otherwise). Scope is the primary segment ONLY — the dropdown/overflow rows already carry their glyphs (§ Row icons). (`260723-l317`)

**Data ownership — an invalidatable external store, TopBar props untouched.** A `useOpenTargets(enabled)` hook (`hooks/use-open-targets.ts`) reads `OpenContext` `{sshHost, sshUser, hostApps}` from a small **module-level external store** consumed via React's `useSyncExternalStore` (a listener `Set` + `getSnapshot`), shared by every consumer. The bundle — `getHealth()` (`sshHost` + `sshUser`) + `getOpenApps()` — is fetched **once** and held; the context is then **static BETWEEN settings commits** (no client polling, no per-render refetch — the registry does not change on its own). The one runtime seam that changes it is the Settings dialog's successful SSH-host commit, which calls the exported **`invalidateOpenContext()`**: it bumps an epoch, drops the cache, and — when consumers are mounted — eagerly refetches the bundle and notifies subscribers so they re-render with the fresh `sshHost` **without a page reload**; with zero mounted subscribers it plain-drops the cache so the next `useOpenTargets(true)` mount fetches fresh (§ Design Decisions → Open control owns its data via an invalidatable external store; § Settings-commit invalidation seam). `enabled` gates the fetch; data still returns when another consumer already populated the store; both halves stay individually **fail-silent** (a failing health read ⇒ `sshHost`/`sshUser: ""`, deeplink section hidden, never a thrown error). `resetOpenTargetsCacheForTest()` resets the store cold between tests. The store feeds BOTH the TopBar registry entry (TopBar calls the hook itself) and the palette builder; no new TopBar props or slot-context fields are threaded through, keeping the e2e-asserted chrome surface stable. (`260723-l317`)

**Row icons** (`open-app-icons.tsx`, `OpenTargetIcon({target})`). Each Open menu row leads with a **monochrome `currentColor` inline SVG glyph** (~14px, `aria-hidden`, no image fetches, no icon dependency) — full-color brand marks would clash with the terminal aesthetic, and `currentColor` lets the existing row hover treatments (accent-green flips) apply to the glyph for free. Resolution: a `BRAND_ICONS` id map first (BOTH `vscode` — the deeplink id — and `code` — the wt host registry id — key to the VS Code glyph; plus `cursor`, `windsurf`), then a `KIND_ICONS` generic fallback by `kind` (editor → code-brackets, terminal → `>_` prompt, file-manager → folder — covers `ghostty_macos`/`terminal_app`/`finder` and any future entry), then a neutral open-in-app glyph for anything kindless. Deeplink targets resolve with implicit kind `editor`; host targets carry `appKind` from the wt registry `kind`. Each glyph carries a `data-icon` attribute naming the resolution (`vscode`/`cursor`/`windsurf`/`editor`/`terminal`/`file-manager`/`app`) — the test seam, since monochrome paths are otherwise indistinguishable to queries. Icons render on **both** row renderings (`OpenTargetRow` in the split-button dropdown AND `OpenMenuRows` in the overflow chevron menu); **command-palette rows stay text-only** (no icon affordance today).

**Keyboard parity (Constitution V).** Every available open target is a command-palette entry via the pure `buildOpenActions(targets, run)` (`lib/palette-open.ts`, the `lib/palette-*.ts` pattern), composed into `paletteActions` in `app.tsx`: `Open: <label>`, with host targets suffixed ` (on host)` ONLY on a remote client (disambiguating deeplink/host label collisions — a local view has no collision). The **per-target** entries carry no chord — the target set is data-driven and varies per deployment, so no static chord can name one; palette reachability satisfies §V, documented in the module's registration comment per the code-review rule.

**The last-used behavior does get a chord: ⇧⌘O / Shift+Ctrl+O (`open-last-used`).** Naming the *behavior* ("re-run the last-used target") rather than an app sidesteps the data-driven objection, giving the split-button's primary segment its keyboard twin. The builtin binding is `scope: "terminal"` and its handler is registered in `app.tsx` only when `windowParam` is set (the Open control is terminal-route-only; the board twin mounts no handler and the chord falls through). The handler resolves `resolveLastUsedTarget(openTargets, readLastUsedOpenTarget())` over the same live target set the palette uses and runs it through the shared `useRunOpenTarget().runTarget`, so chord, palette entry, and primary segment share one execution path and one last-used write. With nothing stored (or a stale id) the chord surfaces the info toast **"No last-used app yet — pick one from Open ▾ or the palette"** rather than trying to pop a mouse menu. The pure `buildOpenLastUsedAction(lastUsed, onRun)` adds the matching palette entry `Open: Last used (<label>)`, **hidden when no target resolves** (the dynamic suffix needs a target to name — the boundary-hidden convention of Move up/down); its id `open-last-used` doubles as the registry actionId, so `withShortcutHints` decorates it automatically. The primary segment's `Tip label={primaryLabel}` carries that chord in its `kbd` slot — derived via `useKeybindings().byAction.get("open-last-used")` + `formatCombo(..., host.platform)` and omitted when the binding is unbound/disabled (the § Tier-1 `Tip` registry-chord rule), so the split-button advertises its own keyboard twin; the chevron Tip (`Open in… (choose app)`) stays chord-less. (`260801-sm6g`, `260811-ke2s`)

**The `Open:` palette family carries one NON-target entry: `Open: PR #{n}`.** The same `openActions` memo in `app.tsx` spreads `buildOpenPrAction(prUrl, prNumber, onOpen)` (`lib/palette-open.ts`, same pure-builder pattern) **after** the `buildOpenActions` targets, so all `Open:`-prefixed rows group together. The builder returns `[]` unless `prUrl` is set and otherwise one `{ id: "open-pr", label: "Open: PR #{n}" }` — the real number from `currentWindow.prNumber`, falling back to a bare `Open: PR` when `prNumber` is absent (a `prNumber != null` guard, stricter than the truthiness check the row flyout card's PR anchor uses). The id is unqualified, so it cannot collide with the kind-qualified `deeplink:*`/`host:*` target ids. Visibility is exactly "the current window has a PR": `currentWindow` is null off the terminal route, and no `prUrl` → no entry (mirroring the sidebar `PrLinkRow`'s absence, § `PR` (L3) register). Selection is **client-side only** — `onSelect` delegates the url to the caller's `window.open(url, "_blank", "noopener,noreferrer")` (the `Help: Documentation` pattern), keeping `window.open` out of the pure lib. Because `prUrl` is a public GitHub URL, the behavior is identical for a local and a remote viewer; there is no host spoke (no `POST /api/open`, no exec) and hence no ` (on host)` label variant. It is deliberately **not an `OpenTarget`**: the palette↔menu mirror covers Open *targets*, so the top-bar Open split-button menu carries no PR row. Its keyboard path is the palette entry itself — no chord — documented in the registration comment per the code-review shortcut rule. This is the keyboard counterpart to the pointer-only sidebar `PrLinkRow` anchor. (`260727-w2d8-palette-open-pr`)

**Tests.** Vitest: `open-in-app.test.ts` (`resolveDeeplinkHost` chain — alias verbatim / derived `user@host` / empty-user bare host / localhost empty — the remote-implies-deeplinks gate, url grammar, last-used, cwd fallback, `appKind` on host targets), `open-button.test.tsx` (primary/last-used vs menu, deeplink navigation vs host POST + toast, per-row icons in both renderings via `data-icon`, `code` host id → VS Code glyph, generic fallback for an unknown id by kind, AND the primary-segment last-used glyph: present with `data-icon` when a stored target resolves / absent when localStorage is empty or the stored id is stale, accessible names unchanged), `palette-open.test.ts` (the `(on host)` suffix rule; `buildOpenLastUsedAction` — the `Open: Last used (<label>)` label + `open-last-used` id when a target resolves, `[]` when none does, and `onSelect` delegating to `onRun`; and for `buildOpenPrAction` — the numbered `Open: PR #123123` label, the bare `Open: PR` fallback when `prNumber` is absent, `[]` when `prUrl` is absent, and `onSelect` delegating the url to `onOpen`), `use-open-targets.test.tsx` (store invalidation — a mounted consumer receives a changed `/api/health` `sshHost` after `invalidateOpenContext()` without remount; zero-subscriber invalidation drops the cache so the next mount fetches fresh; `sshUser` carried; empty on health failure), `settings-dialog.test.tsx` (the SSH-host commit calls `invalidateOpenContext()` once on success / not at all on a rejected `setSSHHost`, the `@/hooks/use-open-targets` module mocked to observe the seam). **The remote-gate and resolution-chain coverage lives in Vitest, not e2e** — the e2e client is always `localhost`, so `location.hostname` cannot be faked against the test server. E2e `open-in-app.spec.ts` (+ sibling `.spec.md`) stubs `GET /api/open-apps` via `page.route` to assert stubbed-registry presence + menu + palette entries + icon presence (`data-icon`) in host rows; it asserts the primary segment by **accessible name** (`Open in app` / `Open in {label}`), so the `aria-hidden` primary glyph does not affect it. The **default localhost e2e environment yields zero targets** (localhost + empty registry), so the button is absent by default — the spec also asserts that absence, and the existing top-bar chrome specs (`top-bar-overflow`, `top-bar-overlap`, `window-heading`, `mobile-layout`) keep passing unmodified. (`260722-6d0f-navbar-open-in-app`, `260722-fc3b-ssh-host-fallback-open-logos`, `260723-l317`, `260727-w2d8-palette-open-pr`)

**Close pane / Kill** (`ClosePaneMenuRow` in `top-bar.tsx`): a **chevron-menu row**, not a bar chip — the `close-pane` registry entry is `menuOnly` (§ Right cluster → `menuOnly` demotions: a destructive control sitting one slot from Refresh was a misclick trap). **Mode-branched** between a terminal close-pane and a board window-kill. Props: `server?`, `windowId?`, `disabled?: boolean`, `onRequestKill?: () => void` (board routing), `label = "Close pane"`. **The label is coupled to the actual click behavior** — `effectiveLabel = onRequestKill ? label : "Close pane"` — so a row can never read "Kill" while firing a plain close-pane. (`260718-co9z`, `260731-oiho`)
- **Terminal mode**: the optimistic close-pane path — `closePane(server, windowId)` via `useOptimisticAction`, `disabled` while `isPending`. `POST /api/windows/{windowId}/close-pane` → `tmux.KillActivePane`. The row renders only when `currentWindow` exists; also "Pane: Close" in the palette.
- **Board mode**: when `onRequestKill` is present (registered by `BoardPage`) the row calls it INSTEAD of `closePane`, opening the **kill confirm dialog** (§ Board Kill Confirm Dialog) rather than firing immediately; the confirmed action is a window-kill (`killWindow`), not a close-pane, with an `Unpin instead` escape. Its label reads "Kill". `disabled={!focusedPane}` so the row is disabled on an empty board. The confirm gate + escalation exist because a board pane's blast radius — the whole window everywhere, including its home session — is otherwise illegible. (`260715-6jwn`, `260718-co9z`)
- **Self-heal seam**: post-kill board refetch rides `executeKillWindow`'s own `onSettled` in `board-page.tsx` (§ Board Kill Confirm Dialog), not a callback on this row. The self-heal covers the no-`board-changed`-on-kill gap: killing a window collapses its single-window pin-session with no `board-changed` SSE event — fired only by pin/unpin/reorder — so the refetch lets `getBoard` drop the vanished tile and an emptied board fall out of `GET /api/boards`.

The **in-bar ✕ chip** (`ClosePaneButton` — `<ClosePaneGlyph />` from the shared glyph module, the same definition `ClosePaneMenuRow` leads with, on the shared `TOP_BAR_BUTTON` token; `LogoSpinner` while pending) survives in the codebase intact but unreachable under the `menuOnly` flag, so a revert is deleting that flag (the `260722-n2n4` mechanism); it is a **deletion candidate** for a later change that confirms the demotion sticks. Unpin is not on the close/Kill affordance at all — it lives on the tile-header pin-glyph button (§ Board Tile Header) and the `Board: Unpin Focused Pane` palette action (§ Boards Command Palette), both driven by the shared `unpinFocused` handler in `board-page.tsx` (`unpin(entries[focusedIndex].server, .windowId, name)`, `POST /api/boards/{name}/unpin`). (`260715-h1ck`)

**Board autofit toggle** (`BoardAutofitToggle` in `top-bar.tsx`): the L2 tier's **board-only** toggle and its only IN-BAR member, gated on `mode === "board" && onToggleAutofit` (it never renders on the Terminal route). Props: `autofit: boolean`, `onToggle: () => void`. Mirrors the `FixedWidthToggle` vocabulary — `aria-label="Toggle board autofit"`, `aria-pressed={autofit}`, shared `rk-glint` glint hover over the `TOP_BAR_BUTTON_BASE` size token, `title` reflecting on/off ("Autofit on (panes fill the row)" / "Autofit off (fixed pane widths)"). It renders `<AutofitGlyph filled={autofit} />` (§ Shared top-bar control glyphs) — the frame-with-columns glyph gaining filled panes when on — the same definition `AutofitMenuRow` leads with, though the menu row renders the *unfilled* identity default so its trailing ✓ stays the sole state marker. Clicking it flips the per-board desktop autofit preference; the palette counterpart `Board: Toggle Autofit (on/off)` flips the SAME state (Constitution V parity). It is wired from `BoardPage` through the top-bar slot context (`autofit` + `onToggleAutofit`) → `RootTopBar` (`app.tsx`) → `TopBar` props (tolerant-empty: absent `onToggleAutofit` ⇒ no toggle). The behavior it controls (equal-share flex layout, hidden resize handles, non-destructive stored-width round-trip) lives in § Board Autofit Toggle. (`260708-738w`)

**Refresh button** (`RefreshButton` in `top-bar.tsx`): part of the **L3 always-block** — a reload is meaningful on every page, and it is the block's trailing fit candidate (after the conditional `UpdateChip`, before the exempt chevron). It is a registry candidate (`id: "refresh"`) — it renders in-bar in all four modes when it fits and overflows into the chevron menu as a "Refresh page" row otherwise. A **plain `<button type="button">`** whose `onClick` does a **full page reload** — plain click `window.location.reload()`; **Shift+click force-reloads** via the exported `forceReload()` helper (a `fetch(location.href, { cache: "reload" })` that overwrites the document's HTTP cache entry, then `reload()` in `.finally` so it fires even offline — Chrome Shift+reload semantics. The fetch races a 3s timeout (`Promise.race`, then `clearTimeout` in `.finally`) so a **stalled** socket that never resolves *or* rejects still reloads — `.catch` alone covers rejection but not an indefinite hang). `aria-label` "Refresh page", `title` "Refresh page (Shift+click: force reload)", carrying the shared fixed-size `TOP_BAR_BUTTON` token (§ Right cluster → shared button-size token) and `<RefreshGlyph />` from the shared glyph module (lucide `rotate-cw` — a circular arrow + top-right arrowhead, 14×14 `aria-hidden`, `stroke="currentColor" strokeWidth="2"`, round caps/joins) — the same definition `RefreshMenuRow` leads with, matching the sibling split/close glyphs (§ Shared top-bar control glyphs). It is the in-app recovery affordance for degraded terminal views (stale relay / wedged xterm / SSE drift) where browser chrome is out of reach (PWA/standalone/kiosk). Deliberately **NO spinner / `useOptimisticAction` / `isPending` / `LogoSpinner`, NO `disabled` state, and NO confirmation** (unlike Split / Close) — the page unloads synchronously so a spinner would never meaningfully render, and the reload is non-destructive by design (constitution II/VI: all client state re-derives on load; tmux sessions are unaffected). The same action is reachable from the command palette as "View: Refresh Page" (see § Boards Command Palette / § Command Palette View actions below). It is a full reload rather than a targeted relay-reconnect (§ Design Decisions → Refresh is a full page reload). (`260703-p29w`, `260704-9o7k`, `260715-h1ck`)

**Toolbar button color convention**: All toolbar buttons (top bar and bottom bar) use `text-text-secondary` as their default foreground color. Active toggle states (Ctrl/Alt modifiers when armed, FixedWidthToggle when active) use `text-accent` with accent background. Hover state uses `hover:border-text-secondary` (border highlight). This convention applies to: compose button, fixed-width toggle, the split control, the close/Kill affordance, the top-bar Settings gear, Esc, Tab, Ctrl, Alt, Fn trigger, arrow pad, and ⌘K.

### Window Heading (`WindowHeading` in `top-bar.tsx`)

The centered, highlighted, editable window heading — the single most important identity on the Terminal route (the tmux window name, e.g. `riff-gallant-jackal`). It gives the window name the prominence of a document title (mental model: browser-tab / editor title bar) and makes rename lowest-friction direct manipulation. Lives in the top bar's center grid cell (§ Chrome (Top Bar) center cell); renders only when `mode === "terminal" && currentWindow`. It is the terminal instance of the universal `PageType: name` heading — a static prefix span sits OUTSIDE the rename button, and the name is driven by the shared boot-sweep primitive (`useBootSweep`/`SweepCells`, § Hover-Animation Vocabulary → boot sweep). **The prefix is a static `Window:` in every lens** (§ Heading is a static `Window:`): `WindowHeading` takes a `prefix` prop, but the sole call site passes the CONSTANT `WINDOW_PREFIX` — the heading does not vary by lens (the `ViewSwitcher` shows the lens — its `menuOnly` `View:` menu rows in the chevron menu). (`260704-pr0p`, `260714-uco1`, `260717-6anu`, `260722-n2n4`)

**Two-tone prefix**: the `Window:` prefix (static in every lens) renders `text-text-secondary` as a **static sibling span outside the rename button/input** (`hidden sm:inline` — mobile keeps just the name) — clicking it never starts an edit, and the button's `aria-label` stays clean. The name keeps `font-semibold text-text-primary`. The prefix's frame states (dim ahead of cursor / typed behind) come from the boot sweep's `prefix` half. (`260704-pr0p`, `260714-uco1`)

**Three states** (one component):

- **display** — a `<button aria-label="Rename window {name}">` wrapped in `Tip label="Click to rename"` (the Tier-1 form — § Tier-1 `Tip`; the visible hover cue naming the rename action for sighted users, since the boot sweep signals interactivity without naming it and the `aria-label` covers screen readers) rendering the name (via `<SweepCells>` at rest → plain text) at `text-sm font-semibold text-text-primary` (weight-600 + primary color IS the "highlighted" rest state; the surrounding crumbs are secondary, so weight+color alone read as highlighted — no background). `max-w-[16ch] sm:max-w-[28ch] inline-flex items-center coarse:min-h-[30px]` (mobile-leaf touch target), with the name inside an inner `min-w-0 truncate` span — truncation MUST live on the span, not the button: `text-overflow` is inert on a flex container, and flex centering clipped long names on BOTH ends with no ellipsis (`riff-blustery-whale` → `iff-blustery-whal`). Left-anchored, so long names keep their head and cut at the tail; the button itself stays centered in the grid cell. Click enters edit.
- **boot sweep** — the JS sweep from `useBootSweep` (§ Hover-Animation Vocabulary → boot sweep). One inverse-video accent-green cursor at `DECODE_FRAME_MS = 28`ms/cell sweeps `Window:` + space + name; over the prefix it types (TypedLabel states), over the name it churns `DECODE_GLYPHS`. The heading container does NOT flip `text-accent-green` for the whole scramble — green lives ONLY on the per-cell churn/cursor spans inside `SweepCells`; each resolved cell settles to its own REST color (`restCellClass`: prefix → `text-secondary`, name → semibold `text-primary`), so the left-to-right two-tone reveal is visible instead of a uniform green flash. **Guards**: (a) a `DECODE_HOVER_INTENT_MS = 140`ms hover-intent delay before the sweep starts (cursor transit toward the right buttons doesn't replay it); (b) edit-start stops the sweep and the input binds to the real `name`/`draft` state, never the scrambled DOM text; (c) the sweep replays once whenever the **DISPLAYED name changes** — a `prevNameRef` **seeded `null`** effect keyed on the incoming `name` prop. The null seed plays the sweep ONCE on mount (first name resolution flips `prevNameRef` from `null`), so route navigation between page types animates — AND a committed rename, an SSE external rename, and window navigation all replay through the same name-change mechanism. This is why the instance is NOT remounted per window — a fresh mount would re-seed `prevNameRef` to `null` and replay every time / double-play. Skipped entirely under `prefers-reduced-motion` (`prefersReducedMotion()` reads `window.matchMedia`, guarded for jsdom — the rest state IS the reduced-motion state). (`260704-pr0p`)
- **edit** — an identically-styled inline `<input aria-label="Window name">` (monospace, `text-left font-semibold text-text-primary`, `border-b border-accent`; `text-left` so the name does not jump horizontally on edit entry given the left-anchored heading), width sized in `ch` and growing with content (`Math.max(draft.length + 1, 3)ch`), `autoFocus` + select-on-focus. **Enter commits, Escape cancels, blur commits**; empty/whitespace-only trim on commit = cancel (matches the sidebar/dialog trim guard); an unchanged name is also a no-op. Commit calls the shared `useWindowRename()` hook (§ Shared window rename below) — optimistic store rename + toast on error — then optimistically sets the display; the name-change effect replays the sweep when the store-driven `name` prop settles. (`260714-uco1`)

**Keyboard path (Constitution V)**: the command-palette `Window: Rename` action dispatches a `window-heading:rename` `CustomEvent` (mirroring `theme-selector:open`); a document-level listener in `WindowHeading` enters inline edit. There is no rename modal dialog — the inline edit (heading + sidebar) is the only window-rename surface; see § Keyboard Shortcuts (the palette rename action) and § Component Conventions (the shared `Dialog`). Only the SESSION rename dialog lives in `app.tsx`.

**External-switch cancel**: a `prevIdentityRef` effect keyed on `${server}:${windowId}` cancels an in-progress edit when the window identity changes out from under it (another client / tmux switching the route) — committing the typed name onto the newly-navigated window would rename the wrong window.

### Display-only page headings (`PageHeadingDisplay` in `top-bar.tsx`)

The board/server/host siblings of `WindowHeading` — a `PageHeadingDisplay({ prefix, name, solo?, ariaLabel })` that renders the same `PageType: name` two-tone identity via the SAME `useBootSweep`/`SweepCells` primitive, but with NO rename affordance (boards/servers have no rename API; the host mode has no instance). The static prefix span is `hidden sm:inline` (same mobile rule as terminal); the `solo` variant (host) renders the single word primary-medium and visible at all breakpoints. Hover replays the sweep behind the 140ms intent; mount + displayed-name-change replay via the same `null`-seeded `prevNameRef` effect; reduced motion skips. **A11y**: the identity carries a STABLE `aria-label` (the churn glyphs are `aria-hidden` inside `SweepCells`) placed on a `role="group"` wrapper — `aria-label` is prohibited on the implicit `role="generic"` of a bare `<div>`/`<span>`, so `role="group"` is what makes it a valid, screen-reader-stable accessible name. No `<h1>` is added to the top bar. (`260704-pr0p`)

**Tests**: `top-bar.test.tsx` covers the heading display/edit states (click → input, Enter commits via `renameWindow`, Escape/empty-trim cancel), the `window-heading:rename` CustomEvent entering edit, AND per-mode heading coverage (terminal `Window:` prefix sibling, board `Board:` + board switcher in the center, server `tmux Server:`, solo `Host`; board left nav shows only counts/hint — no `Board ▸`, no left board name; server left nav has no `aria-current` leaf). E2e `tests/e2e/window-heading.spec.ts` (+ sibling `.spec.md`, per the constitution's Test Companion Docs rule) covers, documented 1:1 in the `.spec.md`: the terminal heading renders the name once with a static `Window:` prefix sibling (NOT a trailing breadcrumb crumb); click → inline input → Enter commits (sidebar + heading reflect the new name); Escape cancels (window id unchanged); the palette `window-heading:rename` CustomEvent enters inline edit; the 375px bar stays single-line with no horizontal overflow; hover-treatment classes present and unchanged under `reducedMotion: "reduce"`; the board/server/host centered headings render with their prefixes; the `[ SESSIONS▊ ]` section-heading shape; a motion-opted-in **boot-sweep** e2e (hover the heading → an `.rk-typed-cursor` cell attaches → resolves to rest, since the units stub `matchMedia` to reduced-motion and never exercise the sweep behaviorally); and a reduced-context check that the sweep never starts (JS-gated). (`260704-pr0p`, `260714-uco1`)

### Shared window rename (`useWindowRename`, `hooks/use-window-rename.ts`)

`useWindowRename()` returns `execute(server, session, windowId, newName)` — the ONE optimistic window-rename action, used by BOTH the top-bar `WindowHeading` AND the sidebar inline rename (`sidebar/index.tsx:398`), so the two surfaces rename identically. It stamps the window store's `pendingName` (`renameWindow`) on `onOptimistic`, rolls back via `clearRename` on failure with a toast, and clears the pending marker on settle (SSE snapshot then becomes authoritative). The captured `(server, session, windowId)` is held in a `lastRenameRef` so rollback/settle target the right entry even if the caller re-renders between execute and resolution. Callers own their own trim/empty-name guards before calling (rename is unconditional inside the hook). Session rename is NOT part of this hook — it keeps its own dialog/inline path (§ Sidebar → Session rename). (`260703-5ilm`)

### Hover-Animation Vocabulary

A site-wide motion map where each treatment encodes **exactly one** category of element, so motion carries meaning instead of noise. The shared color cue: **the animated element turns `--color-accent-green` while it animates** (the brand chip on glitch-hover, the boot-sweep cursor + name-churn cells, both caret variants, the stepping brackets — user-requested — including the glint: the sweep strip is accent-green-tinted and the hovered chip's border + glyph flip green for the hover duration, the "green line"). **Scope note**: the boot-sweep green is PER-CELL — only the single cursor cell and the unresolved name-churn glyphs are green; the heading container does NOT tint green wholesale, so resolved cells settle to their own rest color and the two-tone reveal reads left-to-right (`260704-pr0p`). All CSS utilities live in `globals.css` (beside the `logo-chase` keyframes / `coarse` variant; Tailwind CSS 4). Under `@media (prefers-reduced-motion: reduce)` the contract is **animations zeroed, not styling removed**: the CSS gate zeroes every keyframe animation (classes stay in the DOM — that is the test seam), the JS treatments (decode, typed sweep, brand logo sweep) skip themselves by reading the same media query — a JS treatment therefore has NO reduced-motion line in the CSS gate, and its rest state IS its reduced-motion state — and static hover styling (e.g. the glint's green border/glyph flip) remains — a color change is not motion. No pixel assertions in tests — treatments are asserted via class-presence + reduced-motion emulation, plus one behavioral guard for the caret (below).

| Category | Treatment | Mechanism |
|----------|-----------|-----------|
| **Brand** | glitch (wordmark) + white glow sweep (logo ring) | `.rk-brand-glitch` on the top-left "RunKit" brand anchor — one-shot `@keyframes rk-glitch` RGB-split `text-shadow` (~300ms, `steps(2)`, ±2px split) on `:hover`, plus `color: var(--color-accent-green)` for the hover duration (the color snap is what makes the glitch read at 11.5px; the split is texture — the shadow alone was barely visible). Wordmark hidden below `sm`, so on mobile only the icon jitters (accepted). The logo ring **rides the glitch** with the JS-driven white glow "detach, orbit, land" sweep (§ Brand logo hover sweep) — the anchor's `onMouseEnter` drives it, so hovering the wordmark plays it too, and it is the one Brand-category treatment with no CSS rule of its own. |
| **Top-bar page heading** | boot sweep | JS in `top-bar.tsx` — the shared `useBootSweep` hook + `SweepCells` renderer (see § Window Heading / § Display-only page headings), NOT a CSS class. ONE inverse-video accent-green cursor at `DECODE_FRAME_MS` (28ms/cell) sweeps `PageType:` + space + `name`: over the PREFIX it types like TypedLabel (`rk-typed-off` ahead / `rk-typed-cursor` cursor cell / `text-secondary` resolved); over the NAME the cells right of the cursor churn `DECODE_GLYPHS` in accent-green from frame 0, each locked to its true char as the cursor arrives, resolved cells settling to semibold `text-primary` (spaces preserved). Reuses the `rk-typed-*` cell classes + `DECODE_*` constants — no class of its own. (`260703-5ilm`, `260704-pr0p`) |
| **Section headings** | brackets + caret | `.rk-bracket-group` scoped to `SectionHeading`'s inner bracket cluster ONLY (not the row/rule/`side`). On group `:hover` the `.rk-bracket-open`/`.rk-bracket-close` spans step outward `translateX(∓3px)` + turn `--color-accent-green` (transform only — no layout shift), and a `.rk-bracket-caret` (always-reserved `1ch` cell, accent-green, transparent at rest so the closing bracket never shifts) blinks `▊` (`@keyframes rk-caret-blink`, 1.06s `steps(1)`). Moved from the retired `PageHeading` to the in-page section labels by `260704-pr0p`. See § Section Heading. |
| **Section labels** | typed sweep | `TypedLabel` (`components/typed-label.tsx`) — on hover the label fades (`.rk-typed-off`, 26% opacity) and an inverse-video cursor (`.rk-typed-cursor`, accent-green cell OVER the character — a terminal block cursor sitting on text) sweeps from the FIRST cell, "typing" each character back to full brightness (`.rk-typed-on`); after the pass the label holds bright (`rk-typed-done`) until pointer-leave. Fixed ~350ms TOTAL pass regardless of length (per-cell = 350/len clamped 20–60ms) so all labels read as one gesture; width never changes (the cursor occupies its cell). JS-driven (the frame states are the `globals.css` `.rk-typed-*` classes); reduced motion skips the sweep entirely in JS. Applied to the sidebar panel titles (`collapsible-panel.tsx`) and the sidebar `Sessions` region heading (`sidebar/index.tsx`) directly, and — inside the `SectionHeading` brackets — on the tmux Server page's `Sessions` heading (`session-tiles.tsx`) and the four Host-page zone `h2`s (Host Health / Boards / Tmux Servers / Services, `host-overview-page.tsx`), where the typed sweep and the brackets+caret hover compose on one element (`260704-pr0p`). The sidebar labels are deliberately NOT bracket targets (typed-sweep only). Decorative — headings stay non-interactive-looking at rest. |
| **Buttons** | CRT glint + green line | `.rk-glint` — `position:relative; overflow:hidden` + an `::after` skewed accent-green highlight strip sweeping across the button face on `:hover` (`@keyframes rk-glint-sweep`, ~450ms one-shot), plus `.rk-glint:hover:not(:disabled)` flipping border + glyph to `--color-accent-green` (unlayered rule, so it beats the chips' Tailwind hover utilities without touching call sites; chosen over a fill/inverse cell so hover never reads as the armed state's solid accent fill). Applied to every top-bar chip — the hamburger, the rail toggle, the HistoryNav arrows, and the right cluster's SplitControl / OpenButton / BoardAutofitToggle / RefreshButton / SettingsGearButton / chevron trigger (plus the intact-but-unreachable `menuOnly` bar forms: ClosePaneButton, TerminalFontControl, FixedWidthToggle, the ViewSwitcher pill) — AND the bottom-bar `KBD_CLASS` buttons. `overflow:hidden` is verified safe — popovers (the terminal-font one, the Open menu) are SIBLINGS of their trigger buttons, not children, so clipping never clips a popover. |

**Typed-sweep note**: the section-label treatment is the `TypedLabel` typed sweep (no CSS-only hover caret); the e2e guard asserts the sweep behaviorally (cursor cell attaches → `rk-typed-done` lands → unhover resets). The `.rk-bracket-caret` is a separate, reserved `1ch` blinking caret cell inside the bracket group, carried by `SectionHeading`. The section labels compose BOTH vocabularies on one element: the typed sweep (TypedLabel, inside the brackets) AND the brackets+caret hover (the `rk-bracket-group` cluster around them). (`260704-pr0p`)

### Logo Ring — `LogoSpinner` (`components/logo-spinner.tsx`)

The shared brand mark, used both as the app-wide loading spinner (~9 call sites: dialogs, pending split/close buttons, the UpdateChip, the sidebar SERVER-panel refresh slot, the window-switch mask, `ServerWaiting`) and — at rest — as the top-bar brand crumb's inline logo. Its geometry is two static arrays: six `BORDER_SEGMENTS` (the ring, indexed 0–5 clockwise) and three `INNER_FACES` (the cube, never animated). Each border segment carries a `staticFill` — the lit trio `{5, 0, 1}` is `#b4b4b4` (`LIT_FILL`), the dark trio `{2, 3, 4}` is `#2a2a2a` (`DARK_FILL`) — and those static fills are the **single source of truth for the rest pattern**: the hover sweep derives lit-trio membership from them (`staticFill === LIT_FILL`) rather than restating the index set. Segments are tagged `data-ring-segment={i}` so JS can select the ring without touching the sibling `INNER_FACES` polygons. Props: `size`, `loading` (default `true`), and `svgRef` (the seam that lets JS reach the segments — used only by the brand crumb).

**Loading chase — negative stagger, steady state from frame one.** Under `loading` every segment runs `logo-chase 1.2s ease-in-out ${i * 0.2 - 1.2}s infinite`. The delays are **negative** (−1.2s … −0.2s), which starts each segment mid-cycle so the established six-phase chase is on screen at the first rendered frame; the `logo-chase` keyframes, the 1.2s duration, and the 0.2s stagger interval are unchanged by the sign. A positive stagger instead holds each segment at its unanimated opacity until its delay elapses, so the ring opens as one bright side that expands over the first lap — visible at every spinner mount, which is why the sign is load-bearing rather than cosmetic (§ Design Decisions → Chase stagger is negative). At rest (`loading={false}`) segments take their `staticFill` with an inline `transition: opacity 0.5s ease-out, fill 0.5s ease-out`.

### Brand Logo Hover Sweep (`sweepSegmentFill` + `useBrandLogoSweep`)

The brand crumb's ring treatment: a white glow blob **detaches** from the lit half, **orbits** the ring three laps decelerating over 900ms while the lit half dims, and **lands** centered on segment 0 — the lit trio's center — where it crossfades into the rest pattern. Motion is segment *illumination*, the same language as the loading chase, so the logo has one motion vocabulary across loading and hover; no geometry moves and there is no CSS rule for it (§ Design Decisions → Brand logo motion is state-based).

**Pure math — `sweepSegmentFill(i, p)`** (exported for unit tests) returns segment `i`'s fill at progress `p ∈ [0,1]`:

- Head position: ease-out cubic `ease = 1 − (1−p)³`, then `h = (ease · SWEEP_LAPS · 6) mod 6` — continuous/fractional, `SWEEP_LAPS = 3`.
- Settle envelope: `s = max(1 − smoothstep(p / 0.1), smoothstep((p − 0.78) / 0.22))` — `1` at both ends (rest pattern), `0` mid-flight (glow). `smoothstep(t) = clamp(t,0,1)² · (3 − 2·clamp(t,0,1))`; the constants are named `SWEEP_DETACH_END` / `SWEEP_LAND_START` / `SWEEP_LAND_SPAN`.
- Brightness: circular distance `d = min(|h − i|, 6 − |h − i|)`, `gauss = exp(−d² / (2·0.85²))` (`SWEEP_SIGMA = 0.85`), `rest_i = 1` for the lit trio else `0`, `B = gauss·(1−s) + rest_i·s`.
- Color: `bright = mix(WHITE, #b4b4b4, s)`, fill = `rgb(mix(#2a2a2a, bright, B))` — linear per-channel RGB interpolation, rounded. So the blob is **white** in flight and crossfades to the lit gray as it settles.

**Seamless at both ends.** The integer lap count parks `h` at exactly 0 when `p = 1`, and `s = 1` at both `p = 0` and `p = 1`, so those two frames compute to the literal rest fills — the landing and the rest state are the same event, with no restore snap. (`rest` at both ends is what makes an integer lap count a requirement, not a stylistic choice.)

**rAF driver — `useBrandLogoSweep()`** returns `{ svgRef, onMouseEnter }`; `top-bar.tsx` puts `svgRef` on the crumb's `<LogoSpinner size={20} loading={false}>` and `onMouseEnter` on the whole `.rk-brand-glitch` anchor (so the wordmark triggers it too, matching the glitch's scope — floating-ui's `getReferenceProps` composes the handler with `Tip`'s own rather than overwriting it). Per frame it walks `polygon[data-ring-segment]` and applies `sweepSegmentFill` via `setAttribute("fill", …)` — an attribute, matching how React renders the `fill` prop, so the completion restore leaves the DOM and React's recorded props consistent. Three behaviors the driver owns:

- **Re-trigger guard** — a `runningRef` flag; a mouseenter while a sweep is in flight is ignored outright (no restart, no queue).
- **Transition suspension** — the rest state's inline `transition: fill 0.5s` would smear the per-frame fills into crossfades and destroy the timing, so the driver captures each polygon's `style.transition`, sets it to `none` for the flight, and restores the captured values on landing.
- **Reduced motion** — `prefersReducedMotion()` (from `@/lib/motion`, the same helper `TypedLabel` reads) short-circuits `onMouseEnter` before any frame is scheduled; fills never change. The rest state IS the reduced-motion state, so there is nothing to gate in CSS.

On landing the driver parks the literal `staticFill` hex values and restores the transitions — a numerical safety net over an already-exact `p = 1` frame, not a corrective snap. Coverage is unit-level in `logo-spinner.test.tsx` (exact rest fills at `p = 0`/`p = 1`, mid-flight white glow near the head + dimmed lit trio, the negative per-segment delays, the guard, the reduced-motion skip, the completion restore) — animation frames are not asserted in e2e. (`260803-ufqr`)

### Theme System

> Full spec: [`docs/specs/themes.md`](../../specs/themes.md) — architecture, ANSI palette structure, tmux colour mapping, import script usage, persistence model.

Palette-based theme model: each theme defines a `ThemePalette` with 22 canonical terminal colors — `foreground`, `background`, `cursorColor`, `cursorText`, `selectionBackground`, `selectionForeground`, plus 16 ANSI colors (indices 0-15) as a fixed-length readonly tuple. The `Theme` type has shape `{ id, name, category, palette }` — no `colors` or `themeColor` properties (both replaced by derivation from palette).

20 built-in themes (14 dark + 6 light) defined in `app/frontend/src/themes.ts` with canonical ANSI palettes sourced from iTerm2-Color-Schemes / official theme repos. Three consumers derive from the same palette:

1. **Web UI CSS** — `deriveUIColors(palette, category)` produces 8 `UIColors` keys (`bgPrimary`, `bgCard`, `bgInset`, `textPrimary`, `textSecondary`, `border`, `accent`, `accentGreen`). Derivation: `bgPrimary` = background, `bgCard` = lighten/darken background, `bgInset` = darken background, `textPrimary` = foreground, `textSecondary` = ansi[8] (bright black), `border` = blend(fg, bg, 0.25), `accent` = ansi[4] (blue), `accentGreen` = ansi[2] (green). Color utility helpers (`hexToRgb`, `rgbToHex`, `lightenHex`, `darkenHex`, `blendHex`) are module-private. CSS custom properties (`--color-bg-primary`, etc.) applied via inline styles on `document.documentElement.style` overriding `globals.css` fallbacks.
2. **xterm.js canvas** — `deriveXtermTheme(palette)` produces an xterm.js `ITheme` with all 22 colors mapped (background, foreground, cursor, cursorAccent, selectionBackground, selectionForeground, and 16 named ANSI colors black through brightWhite). Terminal content (syntax highlighting, colored prompts, git diff output) matches the selected theme.
3. **tmux chrome** — `configs/tmux/default.conf` uses ANSI `colour{N}` indices (colour0-colour15) instead of hardcoded hex. Because tmux renders its chrome as escape sequences that xterm.js interprets, changing the xterm.js ANSI palette automatically themes tmux status bar, pane borders, and pane-border-format. No runtime `tmux set -g` calls needed — the tmux.conf is static.

`data-theme` attribute set to theme's `category` ("dark" or "light") for CSS branching. Theme preferences persisted to both backend API (`GET/PUT /api/settings/theme` writing `~/.rk/settings.yaml`) and localStorage as synchronous cache. Three settings: `theme` (mode: `"system"` or specific ID), `theme_dark` (preferred dark theme, default `"default-dark"`), `theme_light` (preferred light theme, default `"default-light"`). localStorage keys: `runkit-theme`, `runkit-theme-dark`, `runkit-theme-light`. On init, API is canonical source; localStorage is the fast fallback if API fails. Unrecognized `theme` values fall back to `"system"`, while unrecognized `theme_dark`/`theme_light` values fall back to `"default-dark"`/`"default-light"`. PUT accepts partial updates (load-then-merge).

**Theme menu row** (chevron menu App section, § Right cluster → overflow chevron menu): the "Theme…" row dispatches the `"theme-selector:open"` CustomEvent to open the theme selector; its leading `ThemeModeIcon` (monitor / sun / moon) and trailing slot reflect the effective mode (`preference === "system" ? "system" : resolved`). There is no click-to-cycle surface — the selector, the palette's quick-switch actions, and the settings dialog's theme-pair row (§ Settings Dialog) are the theme-switch surfaces.

**Theme Selector** (`app/frontend/src/components/theme-selector.tsx`): Modal overlay matching CommandPalette structure (fixed z-50, backdrop, max-w-lg at 20vh). Search input filters by name (case-insensitive). Themes grouped under "Dark" / "Light" category headers. Arrow key navigation wraps and skips headers. Mouse hover and arrow navigation trigger live preview via `previewTheme()` — CSS custom properties update in real-time. Enter confirms (persists to API + localStorage), Escape/outside-click reverts to original theme via `cancelPreview()`. Opens via `"theme-selector:open"` custom event (same pattern as `"palette:open"`). Theme rows display multi-color palette swatches showing background plus representative ANSI colors (red, green, yellow, blue, magenta, cyan) instead of a single-color swatch.

**Command palette**: "Theme: Select Theme" action dispatches `"theme-selector:open"`. Individual "Theme: System", "Theme: Light", "Theme: Dark" quick-switch actions retained.

**ThemeProvider** (`app/frontend/src/contexts/theme-context.tsx`): `useTheme()` returns `{ preference, resolved, theme, themeDark, themeLight }`. `useThemeActions()` returns `{ setTheme, previewTheme, cancelPreview }`. Preview applies colors to DOM without persistence. Cancel reverts to the last persisted theme. Uses stable `actionsRef` pattern for callback identity. On init: calls `getThemePreference()` from API (returns `{ theme, themeDark, themeLight }`), falls back to localStorage / defaults if API fails. Per-mode theme resolution: in `"system"` mode, resolves to the user's preferred dark or light theme based on OS `prefers-color-scheme`, falling back to `DEFAULT_DARK_THEME` / `DEFAULT_LIGHT_THEME` if the stored ID is invalid. `setTheme` with a specific theme ID saves it to the matching per-mode slot (by `category`), keeps preference as `"system"` (preserving OS auto-toggle), and persists all three values to API and localStorage. `setTheme("system")` resets to system mode without changing per-mode prefs. Backend is canonical source of truth; localStorage is the fast fallback.

### Breadcrumb Dropdowns

Session and window name text are the dropdown triggers. Clicking/tapping the name opens the respective dropdown. No split click-target pattern — the name itself is the trigger. The session name shown in the breadcrumb is the **snapshot-derived** owning session of the active `@N` (there is no `$session` in the URL), not a URL segment. (`260529-jad6`)

**Session dropdown**: Lists all tmux sessions. Current session highlighted with `text-accent`. Selecting navigates to the session's first window by its window ID (`/{server}/{windows[0].windowId}` — 2-segment). First item: `+ New Session` action — triggers instant session creation (no dialog).

**Window switcher**: Lists all windows in the current session. Current window highlighted (`w.windowId === currentWindow.windowId`). Selecting navigates to `/{server}/{windowId}` (2-segment). First item: `+ New Window` action (creates new window in current session). It renders as a **label-less bare-`▾` `BreadcrumbDropdown`** in the top bar's center cell, beside the centered `WindowHeading` (name-click = edit, `▾`-click = switch); the window name lives once in the heading. Carries `title="Window"` and stays visible on mobile. See § Chrome (Top Bar) center cell / § Window Heading. (`260529-jad6`, `260703-5ilm`)

**Board switcher** (`BoardSwitcher` in `top-bar.tsx`): the board mode's `▾` dropdown — lists the cross-server boards + a `← Sessions` navigate action — sits beside the centered `Board: <board>` display heading, mirroring the window switcher's center-cell placement. The board name is display-only (no rename API); the `▾` is the sole board affordance in the bar. The left nav keeps only the pane/server counts + `⌘[⌘] cycle` hint (`BoardModeInfo`). See § Chrome (Top Bar) center cell / Board. (`260704-pr0p`)

**Action items in dropdowns**: `BreadcrumbDropdown` accepts an optional `action` prop of type `{ label: string; onAction: () => void }`. When provided, the action item renders before the selection list, separated by a divider (`border-t border-border`). Action items use `text-text-primary` styling (not `text-accent`), close the dropdown on click, and are excluded from ArrowUp/ArrowDown keyboard navigation among selection items.

**Optional `secondaryAction`**: a second leading action of the same `{ label, onAction }` shape, rendered below `action` and above the divider — **only honored when `action` is also present**. Its sole call site is the terminal-mode window-switcher dropdown, which renders `+ New Window` (`action`) and `+ New Agent` (`secondaryAction`) as two stacked leading buttons. The two actions occupy dropdown indices `0..offset-1` (leading actions) with selection items following at `offset..N-1`; `actionRefs` is an array and the focus-index `offset` is the count of present leading actions — so the keyboard-nav index math generalizes to N leading actions while every **other** call site (session switcher, board switcher) that passes only `action` keeps its single-action behavior. This minimal second-prop shape is chosen over a generic `actions: []` array (only one call site needs two actions, and an array would churn every call site plus the focus-index logic). See § Spawn-Agent Dialog. (`260713-sbk1`)

**Dropdown component** (`app/frontend/src/components/breadcrumb-dropdown.tsx`): Reusable dropdown with outside-click dismiss, Escape dismiss, ArrowUp/ArrowDown keyboard navigation, ARIA `role="menu"`/`role="menuitem"`. Styled with `bg-bg-primary border-border shadow-2xl`, matching bottom-bar Fn key dropdown pattern. Name text serves as the trigger (44px on touch devices via `coarse:min-h-[44px]`), wrapped in a `min-w-0 truncate` span. When the trigger has a label (`icon` prop set) it renders a **persistent `▾` caret** after the label (`aria-hidden`, `ml-1 shrink-0 text-base` — 16px, user-tuned for visibility) — the always-visible "opens a menu" affordance distinguishing dropdown crumbs from link crumbs (`260702-6m46`; see § Crumb Affordance Vocabulary). A label-less trigger already IS a bare caret (`▾` fallback) and gets no second one. Long names truncated via `max-w-[240px]`.

Connection indicator: green/gray dot only (no text label), driven by `isConnected` from ChromeProvider (set by each page from `useSessions`).

**Open menu escapes any `overflow:hidden` ancestor via `position: fixed`.** The open `role="menu"` element renders **`position: fixed` anchored to the trigger's viewport rect** — it measures `buttonRef.getBoundingClientRect()` on open and positions its top-left at `rect.bottom + gap` / `rect.left`, recomputing (or closing) on `scroll`/`resize` (capture-phase, so a scrolling ancestor container is heard) so it never detaches from the moving trigger. **The reusable invariant — clip-backstop vs. contained popover**: when you add `overflow-hidden` (or `overflow-clip`) to a container that also *holds* a popover, a plain `absolute` popover inside it gets **both clipped AND focus-scroll-displaced** — `overflow:hidden` turns the container into a scroll box, and the menu's focus-on-open `scrollIntoView` then scrolls the whole container's content out of its own clip box. This is exactly the bug the `q8ey` nav clip-backstop (§ Center-heading … → degradation ladder step 3) introduced on the session-switcher menu (empirically: post-clip the menu landed off-viewport at y≈-75 with an empty hit-test; pre-clip it rendered visible at y≈37). The nav clip is a user-approved requirement, so the resolution is to let the menu **escape the clip context**, not weaken the clip — and `position: fixed` is viewport-relative, so no `overflow:hidden` ancestor can clip it. `overflow-clip` on the container would have fixed only the focus-scroll (a clip box is not a scroll container), not the clipping itself. Because the escape lives in the shared `BreadcrumbDropdown` component, it applies identically to **all four call sites** (session crumb, hierarchy ▾, window switcher, board switcher); it keeps `z-50`, the `min-w-[160px] max-w-[240px]` sizing, and the existing click-outside / Escape / Arrow-key focus behavior. **Rule for future clips**: adding `overflow-hidden` to any container that contains a popover requires that popover to escape via fixed/portal positioning, or the popover breaks. (`260715-q8ey`)

### Crumb Affordance Vocabulary

Breadcrumb crumbs advertise their interactivity WITHOUT hover — a two-affordance vocabulary where every remaining crumb is interactive (the current-page LEAF lives in the centered heading, not the left nav, § Chrome (Top Bar), so the "you are here" signal is the centered heading itself, not a no-affordance crumb). The cases: (`260704-pr0p`)

- **Link crumbs** (brand, server link) — carry the shared `LINK_CRUMB_CLASS` constant (`top-bar.tsx`): a **bordered chip** (`rounded border border-border hover:border-text-secondary px-1.5 py-0.5 text-text-secondary hover:text-text-primary transition-colors`), reusing the right cluster's "bordered = clickable" language. The user compared three candidates live (chip / accent color / dotted underline) and picked the chip; the unused variant definitions were removed at lock-in. *(Accent color was rejected — hue is reserved by the StatusDot phases; dotted underline was rejected — it left the mobile icon-only brand with no affordance, and text-decoration doesn't propagate into an anchor's flex children, so the wordmark needed a `[text-decoration:inherit]` patch that the chip doesn't.)*
- **Dropdown triggers** (session — and the window/board switchers) — carry the persistent `▾` caret inside `BreadcrumbDropdown` (see § Breadcrumb Dropdowns). Signals "opens a switcher", distinct from "navigates". The **window switcher** AND the **board switcher** are bare-`▾` `BreadcrumbDropdown`s in the top bar's center cell beside their headings (§ Chrome (Top Bar) center cell), using the same caret affordance. (`260703-5ilm`, `260704-pr0p`)
- **Current-page leaf** — there is NO leaf crumb in the left nav; the current page's identity is the **centered `PageType: name` heading** (the tmux Server page's identity is the centered `tmux Server: <server>`). The heading carries the "you are here" role. (`260704-pr0p`)

*Why not hover-only cues*: a `hover:text-text-primary` cue is invisible to first-time users and absent entirely on touch. (`260702-6m46`)

### Section Heading (`section-heading.tsx`)

A shared `SectionHeading({ label, side?, className? })` renders a one-line **bracketed BBS-menu-tag** SECTION heading — `[ SESSIONS▊ ]──────────── side`. The bracket idiom names in-page SECTIONS; page identity lives in the top-bar center heading (§ Chrome (Top Bar), § Canonical page names — Vocabulary Split). (`260704-pr0p`)

- **Structure**: an outer `<div className="flex items-center gap-3 min-w-0">` (a div, NOT a span — it contains an `<h2>`, and a span may not contain flow content). The inner `rk-bracket-group flex items-center gap-1.5 min-w-0 shrink-0 max-w-[60%]` cluster holds: the `[` open bracket, an `<h2 className="text-xs uppercase tracking-wide text-text-secondary">` wrapping `<TypedLabel text={label} />` (so the section-label typed-sweep lives INSIDE the brackets — § Hover-Animation Vocabulary), the always-reserved `▊` caret cell, then the `]` close bracket. A `flex-1 min-w-4 border-t border-border` fills the middle as the rule (a CSS border — responsive, no literal `─` glyphs to overflow at 375px); an optional right-aligned `side` slot (`text-sm text-text-secondary truncate shrink-0`) sits after the rule when provided.
- **Accessibility**: the `[`/`]` brackets, the `▊` caret cell, and the rule are `aria-hidden`; the `<h2>` is the accessible structure and its accessible name is the label text ONLY (uppercase styling). No `<h1>` anywhere (the top bar adds none either — § Display-only page headings).
- **Bracket hover treatment** (brackets+caret = section-heading vocabulary): the inner bracket cluster carries `rk-bracket-group`, scoping the hover treatment to THIS cluster ONLY — the rule and `side` slot are outside it and never trigger it. On group hover the `[`/`]` spans (`.rk-bracket .rk-bracket-open`/`.rk-bracket-close`) step outward `translateX(∓3px)` + turn accent (transform only, no layout shift), and the **always-reserved `.rk-bracket-caret` cell** (fixed `1ch`, transparent at rest, sitting AFTER the label and BEFORE the closing bracket → `[ SESSIONS▊ ]`) blinks its `▊`. All disabled under `prefers-reduced-motion`. See § Hover-Animation Vocabulary. (This reserved-caret is NOT the section-label typed sweep, which lives in the `TypedLabel` inside the brackets — the two vocabularies compose on this one element.)
- **tmux Server page** (`/$server`): a page-level `<SectionHeading label="tmux Server Overview" />` heads the scroll area (§ Page Overview Headings), then `<SectionHeading label="Sessions" side={"{N} sessions, {M} windows"} />` precedes the session-tile grid (`session-tiles.tsx`). The server-view stats ride the `Sessions` heading's `side` slot, right-aligned after the rule.
- **Host-page zones** (`/`): a page-level `<SectionHeading label="Host Overview" />` heads the scroll container (§ Page Overview Headings), above the four zone `<h2>`s — `Host Health` / `Boards` / `Tmux Servers` / `Services` — each `<SectionHeading label=... className="mb-2" />` (`host-overview-page.tsx`). The zone headings' `side` slots stay **empty** (reserved for the server-view stats relocation, plan assumption #4); each zone's existing inline metadata (live hostname / `{N} boards` / `{N} servers`) stays in the zone BODY at its `text-xs` sizing right below the heading — NOT in `side`. (`260704-pr0p`, `260715-zs1y`)
- **Tests**: `section-heading.test.tsx` asserts the `<h2>` accessible name equals the label (brackets/caret/rule `aria-hidden`), the optional `side` slot renders after the rule, and the `TypedLabel` is present inside the brackets. `host-overview-page.test.tsx` asserts no level-1 heading exists, the four zone `<h2>` labels render in order (Host Health → Boards → Tmux Servers → Services), and the `Host Overview` page heading renders.

### Page Overview Headings

Two page-level in-page `SectionHeading` headings sit at the TOP of their page's scroll container, above the page's first zone/section, reusing the `SectionHeading` component (§ Section Heading) — both inherit `SectionHeading`/`TypedLabel` `prefers-reduced-motion` behavior (static under reduced motion): (`260715-zs1y`)

- **"Host Overview"** — on `/` (`host-overview-page.tsx`), the first child inside the `overflow-y-auto` scroll container, ABOVE the HOST HEALTH zone (so the page reads `[ HOST OVERVIEW ]` then the four zones). Distinct from the top-bar center-cell solo `Host` identity heading — this is the in-page overview label, that is the chrome page-type identity.
- **"tmux Server Overview"** — on `/$server` (`session-tiles.tsx`, the no-window `SessionTiles` view), the first child of the scroll area, ABOVE the existing `[ SESSIONS ]` `SectionHeading`. It renders **only** in the no-window `/$server` view; navigating into a window (`/$server/$window`) does not mount `SessionTiles`, so the heading is absent there. Distinct from the top-bar `tmux Server: <server>` center-cell identity heading.

Coverage: `host-overview-page.test.tsx` asserts the "Host Overview" heading renders; `tests/e2e/window-heading.spec.ts` (+ `.spec.md`) asserts both "Host Overview" (`/`) and "tmux Server Overview" (`/$server`) in-page headings.

**Fixed width** — the `fixed-width` registry entry is **`menuOnly`** (gated `modes: ["terminal"]`, `hidden: !currentWindow` per the pyramid table above, which is authoritative), so its reachable form is the chevron menu's `FixedWidthMenuRow`: a "Fixed width" `role="menuitemcheckbox"` with `aria-checked={fixedWidth}`, **leading with the static `<FixedWidthGlyph />` identity variant** (the inward/contract arrows, which do not flip with state) and a trailing `✓` when on as the sole state marker (§ Right cluster → leading-glyph parity), calling `toggleFixedWidth` — reachable at every width, plus the palette's `View: Fixed Width (900px)` / `View: Full Width`. Fixed-width is a viewport preference, not a per-window setting, which is why it does not earn a permanent bar slot. The in-bar `FixedWidthToggle` component (self-contained, `useChromeState()`/`useChromeDispatch()`, on the shared `TOP_BAR_BUTTON_BASE` token, rendering `<FixedWidthGlyph expanded={fixedWidth} />` so the bar form keeps its state-driven arrow flip) stays intact but unreachable under the flag — a **deletion candidate** for a later change. (`260509-17m3`, `260704-9o7k`, `260715-h1ck`, `260731-oiho`)

**Terminal font (Aa)** — the `terminal-font` registry entry is **`menuOnly`** (gated `modes: ["terminal", "board"]`), so its reachable chrome form is the chevron menu's **`TerminalFontMenuRow`**: an inline `role="group" aria-label="Terminal font size"` row — the leading `<TerminalFontGlyph />` "Aa" (mirroring the in-bar trigger, § Right cluster → leading-glyph parity), a `Terminal font` label, `−`, the `{size}px` value (`tabular-nums`), `+` — operating on the same `ChromeContext.terminalFontSize` within `TERMINAL_FONT_BOUNDS` (`−` disabled at `min` 8, `+` at `max` 24), with the stepper buttons on the shared `TOP_BAR_BUTTON_BASE`/`_REST` token. Its `−`/`+` are **plain buttons inside a `role="group"`** (not `menuitem`s), so repeated stepping does not close the menu (§ Menu a11y). aria-labels `Decrease terminal font` / `Increase terminal font`. **The menu row has no Reset** — reset stays reachable via the palette's `Reset terminal font` and the settings dialog's `TerminalFontControl` (§ Settings Dialog). The in-bar `TerminalFontControl` popover in `top-bar.tsx` (the `[−] {size} [+]` + reset combo, self-contained over `useChromeState()`/`useChromeDispatch()`, its trigger rendering the same shared `<TerminalFontGlyph />`) stays intact but unreachable under the flag — a **deletion candidate** for a later change, and the only chrome home of the reset stepper button. (`260613-oo89`, `260731-oiho`)

### Sidebar Kill Controls

- **Session row ✕**: Always-visible ✕ button on session rows with red hover. Normal click opens confirmation dialog: "Kill session **{name}** and all {N} windows?" **Ctrl+Click / Cmd+Click** bypasses the confirmation dialog and kills immediately (best-effort `.catch(() => {})`).
- **Window row ✕**: Hover-reveal ✕ button on window rows (always visible on touch devices via `coarse:opacity-100`). Normal click opens confirmation dialog: "Kill window in **{session}**?" **Ctrl+Click / Cmd+Click** bypasses the confirmation dialog and kills immediately (best-effort `.catch(() => {})`).

The Ctrl+Click force-kill pattern follows the "modifier = power action" convention. Modifier detection uses `e.ctrlKey || e.metaKey` (Ctrl on Linux/Windows, Cmd on macOS).

### Command-palette per-server kill listing

The command palette's `serverActions` block (AppShell's route list) lists **one `Server: Kill {name}` entry per server**, the current server suffixed ` (current)` — following the `Server: Switch to {name}` enumeration pattern. Entry composition is a pure builder, `buildServerKillActions(serverNames, currentServer, onKill)` in `app/frontend/src/lib/palette-server-kill.ts` (mirroring `palette-pin.ts` / `palette-move.ts`, colocated unit test), each action id `kill-server-{name}` with a thin `onKill(name)` body; `app.tsx` wires `onKill` to the `server-dialogs-context` `requestKillServer` trigger, funneling every entry through the single layout-mounted kill-server confirm (`components/server-dialogs.tsx`, incl. the `DAEMON_SERVER` warning — § Kill guard for `rk-daemon`; § Server create/kill dialogs). This is the keyboard escape hatch (Constitution V) that keeps **every** server killable — including non-current servers, which have no SESSIONS-pane group header under the pane's `current` scope mode (§ Sessions-pane scope) and no hover kill affordance on the SERVER-panel tile (§ Sidebar `server-panel.tsx` — the tile carries no action cluster). Kill stays the existing POST flow (Constitution IX); the board route registers no server entries (its sidebar kill affordances reach the same dialog through the context).

### Server create/kill dialogs — single AppLayout mount

The create-server and kill-server confirm dialogs render **exactly once**, mounted in `AppLayout` alongside the one `SettingsDialog` mount (§ Design Decisions → One palette mount in AppLayout, routes register via the slot): `components/server-dialogs.tsx` owns the create-input local state and the submit/kill handlers (`useOptimisticAction` wrappers around `createServer`/`killServer` with `markServerPending`/`markKilled` + ghost add/remove), and `contexts/server-dialogs-context.tsx` (the `settings-dialog-context` pattern — small state + referentially-stable triggers) is the trigger channel: any route calls `openCreateServer()` / `requestKillServer(name)` and reads the open-state (`createServerOpen`, `killServerTarget`) for its modal-gating predicate (AppShell's `dialogOpenRef` gates chord handling on it). Both `Sidebar` mounts (AppShell and BoardPage) wire `onCreateServer`/`onKillServer` straight to the context triggers — the memoized server-group header cluster depends on their referential stability.

**Unified behavior is one superset on ALL routes** — there is no route-specific dialog variant:

- **Create**: `toSafeServerName` applies live on input change and `finalizeSafeName` on submit (§ Live Safe-Name Conversion); on submit the flow runs `markServerPending(name)` and navigates to `/$server` (route-agnostic — a board-route create lands on the new server), with `refreshServers()` on settle so the waiting state swaps to the view.
- **Kill**: the `DAEMON_SERVER` warning renders on every route when the target is the daemon server (§ Kill guard for `rk-daemon`). After a confirmed kill the app navigates to `/` **only when the killed server is `SessionContext`'s `currentServer`** (the deepest-first route-param walk) — `null` on board routes, so a board kill never navigates away.

## Sidebar

`app/frontend/src/components/sidebar/` — session/window tree navigation. The sidebar is decomposed into an orchestrator and seven sub-components:

- `index.tsx` — `Sidebar` orchestrator; owns all state (`collapsed`, `killTarget`, `editingWindow`, `editingSession`, `dragSource`, `dropTarget`, `sessionDropTarget`) and all `useOptimisticAction` hooks. Accepts a required `isConnected: boolean` (per-page live-data health, rendered by the footer dot — § Sidebar footer status row) and holds the file-private `SidebarFooter` + `BottomPanels` sub-components
- `session-row.tsx` — `SessionRow` (exported as `memo(SessionRowInner)`); renders the session header row (chevron, name) plus a **trailing hover-revealed icon cluster** in fixed order `[🎨 palette/color] [🤖 bot spawn] [+ create window] [✕ kill]` — the bot button (`gsmu`) sits immediately **LEFT of `+`** so `+`/`✕` keep their muscle-memory edge positions; handles cross-session drag-over styling; all event handlers passed as props (stable identity-arg `useCallback`s — see § Render Performance). **Session-row icon cluster** (the spawn-agent trigger): each icon button shares the same affordance classes as its palette sibling (`text-text-secondary hover:text-text-primary opacity-0 group-hover:opacity-100 coarse:opacity-100`) plus the cluster's **uniform geometry** — `px-0.5 min-w-[24px] coarse:min-w-[32px] min-h-[24px] coarse:min-h-[36px]` on all four buttons (§ Sidebar row icon system). The **bot button** (`onSpawnAgent && (…)`) renders **only when an `onSpawnAgent?: (server, session) => void` handler is present** (mirroring the optional `onColorChange` pattern), carries `aria-label={`Spawn agent in ${session.name}`}`, and on click does `e.stopPropagation()` then `onSpawnAgent(server, name)` — passing the ROW's explicit `{server, session}` (the spawn target). Its glyph is `BotIcon` from `sidebar/icons.tsx` — a lucide-style `bot` (robot head: antenna + rounded head with two eye dots + side ports) stroke SVG matching the `PaletteIcon` idiom (`stroke="currentColor"`, `strokeWidth={2}`, `aria-hidden`, 24-unit viewBox + 13px default size). **Prop threading** (`gsmu`): `onSpawnAgent` is optional on `SidebarProps` → threaded through `ServerGroupProps`/`ServerGroupInner` → `SessionRow` (mirroring `onColorChange`); the board-route sidebar passes NO handler, so the bot button is hidden there (Non-Goal — later wiring is cheap via the optional prop). See § Spawn-Agent Dialog for the dialog it opens.
- `window-row.tsx` — `WindowRow` (exported as `memo(WindowRowInner)`); renders a single window row (a leading unified `<StatusDot>` + name) plus the absolutely-positioned trailing cluster (`PinIcon` pin / `CloseIcon` kill on the shared uniform geometry — § Sidebar Row Icon System — with the rest-state PR glyph overlaying the kill slot, § Rest-state PR glyph); handles drag-and-drop and inline rename display; hosts the row-hover flyout card (the row root is the card's floating reference, § Row-hover register flyout card); all event handlers passed as props. **Row Minimalism**: no trailing status TEXT cluster (no fab stage word, no `WindowDuration`/`TickingDuration` duration leaves or their per-second `useNow()` tick) — status is the leading `<StatusDot>` plus the owned-PR rest glyph (§ Window rows / Row Minimalism). Its only local state is the hover-icon/popover state plus the row-local flyout state. The icon cluster is inert at rest (`pointer-events-none` + hover/coarse/focus restore). Renders no full PR line — the register detail lives in the flyout card and the PANE panel. (`260706-y1ar`, `260715-jykd`, `93dy`)
- `collapsible-panel.tsx` — `CollapsiblePanel`; reusable collapsible container with header (title + chevron) and localStorage open/closed state persistence via `storageKey` prop. Two modes: (a) legacy `max-height` CSS transition when `resizable` is absent/false (preserves existing Window/Host panel behaviour); (b) resizable mode (opt-in via `resizable` prop) — renders a 6px `ns-resize` drag handle at the bottom, persists user-set height to `localStorage[${storageKey}-height]`, and supports `defaultHeight`/`minHeight`/`maxHeight` props. `maxHeight` accepts a number or a `calc(100vh - Npx)` string form (parsed at drag time using `window.innerHeight`). On mobile (`useIsMobile()` — narrow-width OR coarse pointer, § Hooks (Frontend)) the drag handle is hidden and the content area is pinned to the `mobileHeight` prop (default 56px). All localStorage access wrapped in try/catch. **Header tint**: `tint` prop (`RowTint | null`) paints the header background. By default (legacy mode) the header uses `tint.base` with a `tint.base` ↔ `tint.hover` swap on hover. When `tintOnlyWhenCollapsed` is set, the tint is applied only while the panel is collapsed — and the shade switches to `tint.selected` with the hover swap disabled (stays flat), because in that mode the header is standing in for the selected item inside and a less-saturated hover would read as an inverted effect. `ServerPanel` is the only current consumer of `tintOnlyWhenCollapsed`; the legacy `base`/`hover` behavior is preserved for forward compatibility
- `status-panel.tsx` — `WindowPanel` (exported as both `WindowPanel` and deprecated `StatusPanel`); wraps the identity rows (`tmx`, `cwd`, `git`) plus the **four orthogonal signal registers** (`out`/`agt`/`fab`/`PR` — L0/L1/L2/L3, 3-char prefix vocabulary) in a `CollapsiblePanel` with copyable row interactions (via the file-private `CopyableRow`); see § Pane panel four-register view. The `PR` (L3) register is **ungated from `fabChange`** — shows for ANY pane with a derived `prNumber` (universal derivation); its interaction is **open-first** (the one row that isn't click-to-copy) — with a PR URL the row body is a real anchor (file-private `PrLinkRow`) opening the PR in a new tab (always-visible inline `↗`), and copy is role-swapped to a hover-revealed right-side icon (reusing the window-row icon-cluster gating); without a URL it stays a plain `CopyableRow` copying the segment text. See § PR Status. The panel's `headerRight` leads with a unified `<StatusDot win={win} />` before `{win.name}` — an at-a-glance status summary that survives panel collapse; the dot here is decoration only (the panel below IS the detail view) — see § Status Dot (`260703-41ks`, `260706-y1ar`)
- `host-panel.tsx` — `HostPanel`; hostname header + SSE dot inside a `CollapsiblePanel`, delegating the metric rows to the shared `HostMetrics` component (see § HostPanel). Keeps the `!metrics` "No metrics" branch
- `host-metrics.tsx` — `HostMetrics`; the shared layout-agnostic presentational component rendering the CPU sparkline / memory gauge / disk+uptime / load rows for a `MetricsSnapshot` via `lib/sparkline.ts` + `lib/gauge.ts`. Consumed by both `HostPanel` (sidebar) and the `/` HOST HEALTH zone (`260701-vshd`; see § HostPanel)
- `server-panel.tsx` — `ServerPanel`; swatch-style grid of server tiles (Mock A) inside a `CollapsiblePanel` with `title="Server"`, `storageKey="runkit-panel-server"`, `defaultOpen={true}`, `resizable={true}`, `defaultHeight={50}`, `minHeight={50}`, `mobileHeight={50}` — the panel is sized to one row of tightened tiles so the reclaimed height goes to the SESSIONS tree below. Its expansion state is purely the tile grid's own visibility — `runkit-panel-server` is `CollapsiblePanel`'s private persistence and no sidebar code path outside it reads that key; the sessions-pane scope is a separate, explicit state (§ Sessions-pane scope). The `headerRight` slot carries **only** the `LogoSpinner` when `refreshing` is true — no server name (the highlighted tile and the top-bar page heading already show it). The panel also passes `tint={activeTint}` + `tintOnlyWhenCollapsed` so the collapsed header background matches the selected server's `rowTints.get(activeColor).selected` shade (same body tint used by the active tile inside the panel) — collapsed and expanded readings agree, and the header hover stays flat at the selected shade. Desktop grid: `repeat(auto-fill, minmax(72px, 1fr))`, 6px gap — tiles expand to fill the sidebar width, multi-row, scrolls internally when overflowing the user-set height. **Tile anatomy** — each tile is a focusable `<button role="option">` (no sibling action wrapper) with a two-line body tightened to ≈38px total: a 2px top color stripe (`h-0.5`, ANSI tint via `rowTints.get(color).base`, neutral `--color-border` for tiles without an assigned color) — the top edge is the server signature/active marker (**top border = server, left border = window rows**; no left-border marker) — then a body div (`px-1.5 pt-0.5 pb-1.5`) holding an 11px truncated name line and a count row. **Count row** (`flex h-3.5 items-center justify-between mt-0.5`): a **bare window-count number** (10px `text-text-secondary`, `{windowCount}`, from the `windowCount` field on `ServerInfo`) at the left with the `WaitingBadge` numeric pill right-aligned on the same flex row. The `h-3.5` (14px) reserve is a **fixed height held unconditionally** whether or not a badge renders — the pill is taller than the count text, so without the reserve the tile (and its whole grid row) would jump every time an agent starts/stops waiting. The full count wording lives only in the button `title` tooltip: `` `${name} — ${N} window(s) across ${M} session(s)` `` (singular-aware via `windowCountTooltip(windowCount, sessionCount)` — `1 window across 1 session`), so the tile stays narrow enough for the 72px grid floor. Active tile: `aria-current="true"` + inset accent ring + `rowTints.get(color).selected` body tint (accent-subtle fallback for untinted active server). **No hover-revealed action cluster on server tiles** — server color and server kill live exclusively in the SESSIONS-pane server-group headers (§ Server-group header action cluster) and the command palette (`Server: Kill {name}`, § below); this makes the desktop tile consistent with mobile, which never carried the cluster. Mobile layout (`useIsMobile()` — narrow-width OR coarse pointer, § Hooks (Frontend)): single-row grid via `grid-auto-flow: column`, `grid-auto-columns: 72px`, `overflow-x: auto; overflow-y: hidden`, `scroll-snap-type: x mandatory` — swipe horizontally, tap to select. **Active-tile autoscroll (all layouts)**: a `useEffect` keyed on `[server]` calls `activeTileRef.current.scrollIntoView({ block: 'nearest', inline: 'nearest' })` on mount and whenever the active `server` changes — the ONE call serves both layouts (`block: 'nearest'` for the vertical desktop tile grid that scrolls internally inside the resizable `CollapsiblePanel`, `inline: 'nearest'` for the horizontal mobile single-row strip). It is un-gated by viewport (no `isMobile` read; deps are `[server]` only) and guarded by `typeof el.scrollIntoView === 'function'` for jsdom. `ServerInfo` shape (`{name, sessionCount, windowCount?, rank?}`) flows through from `/api/servers` — the tile renders `windowCount ?? 0`. **Drag-reorder** (`260705-bpnr`): regular server tiles are draggable via the shared `useServerReorder` hook (see § Server-tile drag reorder) — infra tiles are excluded (not draggable, not drop targets); the dragged tile dims via `opacity-50`. **Infra-server name de-emphasis** (`260705-uh8f-deemphasize-infra-servers`): the tile name span carries `text-text-secondary` (not the usual `text-text-primary`) when `isInfraServer(name)` (from `@/api/client`) — greyed, not disabled. Hover/click/active-selection are unchanged, so the infra tile stays fully attachable (useful for reading daemon logs) and never reads as dead/disconnected. See § Infra-server de-emphasis
- `server-selector.tsx` — `ServerSelector` (legacy); owns its own dropdown state (`serverDropdownOpen`, `refreshingServers`, `serverDropdownRef`); pinned-bottom server dropdown with outside-click dismiss. Retained for backwards compat — `ServerPanel` is the primary server-switcher in the current UI
- `kill-dialog.tsx` — `KillDialog`; stateless; renders the kill confirmation dialog for sessions and windows using `<Dialog>`

Consumers import `@/components/sidebar` as before — Vite resolves directory imports to `sidebar/index.tsx` automatically.

The tree's bulk multi-select state lives outside this directory, in `store/selection-store.ts` with its pure logic in `lib/selection.ts` and its palette family in `lib/palette-selection.ts` — see § Window-Row Multi-Select.

### Sidebar Row Icon System

**Every action icon in a session or window row's trailing cluster is a stroke SVG from `sidebar/icons.tsx` — ONE icon system, no text glyphs.** The file's icon idiom is fixed: `stroke="currentColor"`, `strokeWidth={2}`, `fill="none"`, round caps/joins, a 24-unit `viewBox`, a `size = 13` default, and `aria-hidden` (the accessible name lives on the wrapping button's `aria-label`). Members: `PaletteIcon` (color), `BotIcon` (spawn agent), `PlusIcon` (create window), `CloseIcon` (kill), `GearIcon` (the top-bar Settings gear), `GitPullRequestIcon` (the window row's rest-state PR glyph — § Rest-state PR glyph; a lucide `git-pull-request` silhouette: source circle + rail, an arc into the merge rail, target circle), `GitPullRequestClosedIcon` (the rest-state glyph for a **closed** PR — a lucide `git-pull-request-closed` silhouette in the same idiom: source circle + rail, an ✕ where the merge arc was, truncated target rail + target circle) — plus the shared 16-unit `PinIcon` (`components/pin-icon.tsx`, rendered at 12px), whose 1.5/16 stroke resolves to ≈1.125px against the 24-unit family's 2/24×13 ≈ 1.08px, so the cluster reads at one weight. The two PR glyphs are the members that are **not** action icons — they are `aria-hidden` decoration inside a non-interactive span — but share the cluster's idiom because they sit in the same slot row and must read at the same ink weight; deliberately NOT the Nerd Font `` (U+F407) glyph the PANE panel's L3 register uses (one icon system per row cluster). **Equal ink metrics is the whole point**: mixed systems (a 16px text `+` beside a 13px stroke SVG) read as *unevenly spaced* even when the measured center-to-center gaps are exactly equal, because glyph ink width and weight differ. Uniform gaps are a consequence of uniform ink, not of nudging positions. (`260724-2bmy`, `xuej`)

**Uniform button geometry + touch targets.** Every cluster button carries the same box: `px-0.5 min-w-[24px] coarse:min-w-[32px] min-h-[24px] coarse:min-h-[36px]` — 24px square on fine pointers, 32×36 on coarse. Both axes are required: a bare icon has no horizontal padding to grow into, so `min-h` alone yields a ~17px-wide touch target (§ Touch Targets → Both axes or neither). The coarse width is **32px, not the 36px** the bottom bar uses — 4×32 = 128px leaves the session name ~164px of a 319px row in a 375px drawer, where 4×36 = 144px would crush it (§ Touch Targets: `coarse:` is the mechanism, each surface sets its own target). (`260724-2bmy`)

**Optical centering over nominal centering.** `BotIcon`'s `viewBox` is `0 1.5 24 24` — a y-shift, not a wrapper transform, so the rendered box stays exactly 13px. The bot's antenna is top weight, which puts the *perceived* body (the head, spanning y=8..20) below the geometric center; the shifted view lifts the drawing ~0.8px so the head's center lands on the palette circle's center. The antenna dot keeps ~0.27px top clearance and is not clipped. (`260724-2bmy`)

**Scope**: the session row's four-button cluster and the window row's pin + kill pair (plus the rest-state PR glyph sharing the kill button's slot). The server-group header's `+`/`✕` (`sidebar/index.tsx`) are text glyphs, outside this system — a known inconsistency one level up in the same panel.

### Rest-state PR glyph (`window-row.tsx`)

A window row whose window has an **owned PR** shows a rest-state PR glyph at rest in the trailing cluster's LAST slot, right-edge-aligned with the hover ✕ — the icon is picked by state: `GitPullRequestIcon` for open/merged, the ✕ `GitPullRequestClosedIcon` for closed. It makes "which of my windows have PRs, and how are they doing" answerable by scanning the sidebar rather than by decoding a 7px dot's hue — the one status signal Row Minimalism admits beside the dot, and a glyph only (no status TEXT returns to the row). (`93dy`, `xuej`) The glyph is the row's ONLY PR channel — the dot renders no PR state (the local/remote split, § Status Dot) — and the `SessionTiles` window tile carries the same rest-state glyph on its header line (same `prOwnsGlyph` gate, same `prGlyphColor`, same state-picked icon, `aria-hidden`, `data-testid="tile-pr-glyph"`, never on ghosts); the pane panel deliberately carries none — its L3 register is the detail surface. (`aqo6`, `xuej`)

**Gate + color** — `prOwnsGlyph(win)` (`!!prNumber && (prState === "open" || prState === "merged" || prState === "closed")`: open, failing, merged, and closed all earn the glyph; an unknown/absent state never does — the backend's unconfident `""` branch fallback, MapBranchState), deliberately un-family-gated: any pane with an owned PR shows the glyph — even a plain floor pane whose dot stays gray (derivation is universal, Principle X; the dot never renders PR state, § Status Dot). Color is `prGlyphColor(win)` from the shared PR vocabulary — a **six-way** mapping: gray (`text-text-secondary`) for **closed** (a dead PR reads muted; its stale checks/review are historical noise, so the closed branch sits **above fail** — mirroring merged-above-fail in `prDotState`), red for a fail-ish PR, gray (`text-text-secondary`) for an open **draft**, **yellow (`text-signal-yellow`) for an open PR with checks pending (checks running)**, green for open otherwise, purple for merged. The draft branch sits **below fail** (a failing draft reads red — a problem first, a draft second), **above pending** (drafts stay muted even while their checks run), and is **gated on `prState === "open"`**, so the merged→purple path stays unreachable from it by construction (a closed draft reads closed — the closed branch matches first). Closed and open-draft share the inert gray token; the ✕ `GitPullRequestClosedIcon` shape is what separates a closed PR from a draft. Draft, checks-pending, merged, and closed are **glyph-only** distinctions: the dot carries no PR state, so the two channels answer different questions — the glyph *whose turn is it / how is the PR doing*, the dot *what runs here, is it healthy*. No JSX carries the token: the row already interpolates `${prGlyphColor(win)}` into the glyph span's `className`. Ghost rows render no glyph. (`93dy`, `e30p`, `aqo6`, `xuej`)

**Draft fidelity is dual-sourced — the branch channel is what covers a teammate's draft.** `prIsDraft` reaches the frontend from **two** backend channels: the author-agnostic branch refresher seeds it (`BranchPR.IsDraft` from `branchPRExec`'s `--json` field list → `enrichWindowPR`, `app/backend/internal/prstatus/prstatus_branch.go`), and the URL-keyed viewer-wide collector join overrides it on a hit. The branch seed is the load-bearing half for **any PR you did not open**: the collector's query is `viewer { pullRequests }` — the authenticated user's own PRs — so a teammate's draft never keys that join **at any `$limit`** (a categorical blind spot, not a top-`$limit` aging edge), and `api/sse.go`'s collector-only reset therefore leaves `PrIsDraft` intact, so a join miss preserves the branch-derived flag rather than falling back to `false`. The flag is dual-sourced in exactly the shape `prState` already is (`MapBranchState` seed + collector override), so a teammate's open draft renders gray on the same code path as your own. (`e30p`, `n927`)

The residual fidelity limit is **staleness, not absence**: the two channels tick at different cadences (branch refresher 30s, collector 90s) and on a URL hit the **staler collector wins**, so a draft just marked ready-for-review — or a PR just converted *to* draft — can read one generation behind for up to ~90s. Collector-wins-on-hit is deliberate: on the PRs the collector *can* see it stays the single authority for the whole PR row, and it mirrors `prState`'s precedence. The degradation stays graceful (it lands on exactly the other legitimate rendering, never on something wrong) and the glyph is `aria-hidden` decoration. The accessible draft signal — `getPrSegments`' `" (draft)"` state-segment suffix in the flyout card and the PANE panel's L3 register — reads the same field, so every draft surface moves together rather than disagreeing. (`e30p`, `n927`)

**Geometry — an absolute overlay on the last slot, not a third flex slot** (§ Design Decisions → Rest glyph as an absolute overlay). The glyph is an `absolute right-0` span at the icon-cluster's right edge, in the same 24px box the kill button occupies, so pinned rows read rest `[pin][PR]` → hover `[pin][✕]`: **the pin holds its slot; only the last slot swaps**, with zero layout shift. The row button's reserved right padding (`pr-[68px]` / `pr-11`) is unchanged.

**Swap + inertness** — the glyph hides via a **display swap** (`group-hover:hidden coarse:hidden group-has-[:focus-visible]/icons:hidden`, keyed on the cluster's `group/icons` name), never an opacity fade over reserved space: it disappears the moment the pointer enters the row, on coarse pointers (where the action cluster is always visible), and while keyboard focus is inside the cluster — so it can never occlude the revealed ✕. It is `pointer-events-none` and `aria-hidden` **informational decoration**: never focusable, never a click target. Its information is carried by the dot's `aria-label`, the flyout card, and the PANE panel; the PR **click** paths are the flyout card's "Open PR #N" anchor, the PANE panel's L3 `PrLinkRow`, and the palette's `Open: PR #n`. `data-testid="row-pr-glyph"`.

### Infra-server de-emphasis

**Infrastructure** tmux servers — the `rk-daemon` socket (hosting the run-kit daemon itself, backend `ServerSocket`, `app/backend/internal/daemon/daemon.go`) and any `rk-test-*` socket (the isolated test-socket umbrella, backend `IsTestServerName`, `app/backend/internal/tmux/tmux.go:1342`) — are de-emphasized (greyed + sorted-last) but NOT hidden (§ Design Decisions → Infra servers are greyed, not hidden). (`260705-uh8f`)

**Identification lives in one frontend home** (`app/frontend/src/api/client.ts`, near the `ServerInfo` type): an exported `DAEMON_SERVER = "rk-daemon"` constant, a module-private `TEST_SERVER_PREFIX = "rk-test-"` (the one frontend home of that literal, mirroring the backend `IsTestServerName`), an exported `isInfraServer(name)` (true for the exact daemon socket or any `rk-test-` prefix), and an exported `compareServers(a, b)`. Identification is name-based, not a backend `isDaemon` flag (§ Design Decisions → Infra servers are greyed, not hidden). Consumers import these; magic strings are never re-derived at call sites.

**`compareServers` ordering**: regular servers first (alphabetical), then infra servers (alphabetical within their class). Comparison is plain byte-order lexicographic (`a.name < b.name`), NOT `localeCompare` — this mirrors the backend's `sort.Strings` byte order so the regular-server segment renders byte-identical to before this change. `rk-test-*` sockets sort within the infra class.

**Single sort choke point** — `app/frontend/src/contexts/session-context.tsx` `fetchServers`: the fetched array is sorted once at the sole `setServers` ingestion point (`setServers(Array.isArray(data) ? [...data].sort(compareServersRanked) : [])` — note the `[...data]` copy: the sort is non-mutating, and the fresh reference each fetch matches production behavior, where `listServers` already returns a fresh array). Every consumer of `ctx.servers` inherits the effective ordering from this one place: the Sidebar Server-panel tile grid (`server-panel.tsx`), the Sessions-tree per-server `ServerGroup`s (`sidebar/index.tsx`), the Host-page TMUX SERVERS zone (`host-overview-page.tsx`), and the command-palette `Server: Switch to {name}` entries (`app.tsx`). **The backend does not sort** — `/api/servers` stays alphabetical (an asserted API contract, `servers_test.go:143`); display order is a frontend concern. `servers[0]` (the Host page's "Open in window" target, `host-overview-page.tsx`) prefers a real workspace over `rk-daemon` (see § SERVICES Zone on `/`). (`260705-bpnr`)

**Rank-aware comparator composition**: the choke-point sort key is **(infra-class, rank, name)** via `compareServersRanked(a, b)` (exported from `client.ts` beside `compareServers`), which **wraps** `compareServers` — the infra-last class pin and the byte-order intra-class ordering are preserved; rank is a secondary key inserted ONLY inside the regular class. Its logic: for a cross-class pair OR two infra servers it defers ENTIRELY to `compareServers` (infra ignore rank — their intra-class order stays byte-alphabetical); for two regular servers rank is primary — a ranked server sorts before any unranked one, two ranked sort by `rank` ascending, two unranked fall back to byte-order name. An **all-regular-unranked** list is byte-alphabetical, identical to `compareServers`. The rank on each `ServerInfo` comes from the `/api/servers` `rank` field (`@rk_server_rank`, see `architecture.md` / `tmux-sessions.md`). `session-context.tsx` imports `compareServersRanked`. (`260705-bpnr`)

**Live re-sort without refetch** — SessionContext listens for the server-global `event: server-order` on BOTH the per-server pool streams and the dedicated `?metrics=1` stream (identical payload on every stream, since the event is host-global). The `applyServerOrder(order)` handler stamps each named server's `rank` from its index in `order`, drops the rank of any server not listed (so it falls to the unranked tail), then re-sorts the held `servers` array with the same `compareServersRanked` — a state update, NO `/api/servers` refetch. Churn is bounded to explicit reorder events (not per-tick SSE), so no dedup guard is needed. See § Server-tile drag reorder below for the write path.

**Grey (de-emphasized, not disabled) treatment**: the tile **name** text renders `text-text-secondary` instead of `text-text-primary` when `isInfraServer(name)`, on two surfaces — the Sidebar Server tile (`ServerTile`, `server-panel.tsx`) and the Host-page TMUX SERVERS tile (`host-overview-page.tsx`). Applied **uniformly** to all infra servers (`rk-daemon` and `rk-test-*`). Hover, click, and active-selection are all unchanged — the tile stays fully attachable (reading daemon logs is legitimate) and MUST NOT read as dead/disconnected. The infra de-emphasis is a SERVER-tile treatment only — it does not touch the Sessions-tree `ServerGroup` header, which carries its own per-server tint treatment (§ Per-server `ServerGroup`s → Server-group header tint); an infra server's group header tints from its color (or the gray sentinel) like any other.

**Kill guard for `rk-daemon`**: the server-kill confirm Dialog is NOT `kill-dialog.tsx` (that stateless dialog handles sessions/windows only). The server-kill confirm is the `killServerTarget` Dialog in `app/frontend/src/components/server-dialogs.tsx` ("Kill tmux server?") — the single layout-mounted implementation (§ Server create/kill dialogs) and the single choke point for the kill entry paths: the SESSIONS-pane server-group-header ✕ on either route (`onKillServer` → the `requestKillServer` context trigger, § Server-group header action cluster) and the command-palette per-server `Server: Kill {name}` listing (§ Command-palette per-server kill listing). When `killServerTarget === DAEMON_SERVER`, the dialog renders an extra `text-signal-red` warning line ("**rk-daemon** hosts the run-kit daemon serving this dashboard — killing it takes the dashboard down") on every route; a regular target shows no warning. Every server (including `rk-daemon`) stays killable through both paths — killing/restarting the daemon server is a legitimate operator capability, so the guard is a dialog warning, not a removed affordance.

### Render Performance (memo tree + leaf-scoped `useNow`)

The sidebar tree does not re-render on every SSE session tick. The session SSE stream fires several times/sec; each tick produces a fresh `slicesByServer` Map in `session-context.tsx`, which churns the derived `sessionsByServer`/`sessionOrderByServer`/`isConnectedByServer` Maps (intentional fresh refs, documented in that file). Three coupled parts keep the whole tree (every `ServerGroup`/`SessionRow`/`WindowRow`) from re-rendering on each tick; **all three are load-bearing — undoing any one silently re-breaks the perf win with no test catching it.** (`260613-ect6`)

**1. Row components are `React.memo`'d.** `WindowRow` (`memo(WindowRowInner)`, `window-row.tsx:354`), `SessionRow` (`memo(SessionRowInner)`, `session-row.tsx:200`), and `ServerGroup` (`memo(ServerGroupInner)`, `index.tsx:1280`) are each wrapped in `memo` (bare named import from `react`). **These are the first — and currently only — `React.memo` usages in the frontend.**

**2. The per-second `now` tick lives in a leaf `useNow()` hook (`hooks/use-now.ts`), NOT a threaded prop.** `useNow()` returns `Math.floor(Date.now()/1000)` and self-ticks every 1s. Its consumers are leaves that are each a single live instance: `WindowContent` in `status-panel.tsx` (feeds the L0 `out` register's elapsed via `getOutputLine`), and inside the row flyout card `RowFlyoutContent` + `FreshnessLine` — both **mounted only while one card is open** (§ Row-hover register flyout card). The sidebar tree itself does not tick per-second — there is no `WindowDuration` leaf inside `WindowRow` and no `nowSeconds` prop threaded through `ServerGroup`→`WindowRow`/`WindowPanel` (a value that changes every render threaded into a memoized child defeats the memo). **Do NOT reintroduce a `nowSeconds` prop on these components, and do NOT re-add a per-second tick to `WindowRow`.** A clock inside a hover surface is acceptable only while that surface is open and mounted. The bottom panel (`WindowContent`) is a single instance, so its per-second re-render is negligible and does not touch the memoized `ServerGroup`/`SessionRow`/`WindowRow` tree. (`260706-y1ar`)

**3. Handler/array props are referentially stable, so the memos actually skip.** Per-row/per-group handlers are identity-arg `useCallback`s in `Sidebar` (taking `server`/`session`/`windowId` as arguments, following the existing `toggleSession` pattern — `handleSessionRowKill`/`handleWindowRowKill`/`handleSessionColorChange`/`handleWindowColorChange`/the drag handlers, `index.tsx:723-749` and the rename/drag block above), so a leaf calls a stable callback with its own identity rather than receiving a fresh closure. The Map/array context props (`rowTints`, `ansiPalette`, `allBoards`, `pinnedSet`, `pinnedToBoard`, `isPinnedToActiveBoardFor`) are verified stable across session ticks (sourced from `useMemo`/`useWindowPins`, changing only on theme/board events).

**THE SUBTLE INVARIANT (R6a — most regression-prone):** the three handler props `Sidebar` *receives from its parents* — `onSelectWindow`, `onCreateWindow`, `onCreateSession` — MUST be `useCallback`s **at their source in the parents**, because both parents (`AppShell` in `app.tsx`, `BoardPage` in `board-page.tsx`) consume `useSessionContext()` and therefore re-render on **every** SSE tick. An inline arrow re-added at either parent recreates these references each tick and silently defeats `ServerGroup`'s memo for **every** group (including the currently-viewed one), with **no test catching it** — the initial implementation memoized the rows but missed this caller-side leak, and review caught it as rework. The stable callbacks are `handleSidebarSelectWindow`/`handleSidebarCreateWindow`/`handleSidebarCreateSession` (`app.tsx:1116-1154`) and `handleSelectWindow` (`board-page.tsx:363`). **Root-cause fix:** `handleCreateSessionInstant` (`app.tsx:583`) and board-page's `handleCreateSession` (`board-page.tsx:145`) read churning values (`currentWindow`, `isSessionCreatePending`, `ctx.sessionsByServer`) via **render-time-mutated refs** (`currentWindowRef`/`isSessionCreatePendingRef`/`sessionsByServerRef`, set `ref.current = value` on each render) — the same `dialogOpenRef`/`orderOverrideRef` idiom used elsewhere — so the callbacks stay referentially stable across ticks instead of taking those churning values as `useCallback` deps.

**Why the memo skip is cross-server, not full per-row:** `updateSlice` (`session-context.tsx:216-227`) builds a `new Map(prev)` and replaces only the **changed** server's slice via `next.set(server, ...)`; every other server's slice object — and its `slice.sessions` array ref — carries over unchanged. So a tick on server B leaves server A's `sessions` prop referentially identical, and `ServerGroup`'s memo skips A's whole subtree. The currently-ticking server's own rows DO still re-render on its own data tick (correct — its `sessions` ref changed); the `useNow` leaf is what additionally isolates the pure clock tick from even that server's row bodies.

**Related invariants (cross-linked, not re-described here):** the derive-over-store session-order pattern (`orderOverrideRef ?? sseOrder` + render-time `arraysEqual` reconcile — see § Session drag-and-drop reorder; no whole-Map watcher effect); the triage signals (`fabDisplayState === "failed"` red stage *text* + `isFailish` PR-fail signal — see § Window rows; the failed-state *dot* is a phase-hue dashed ring + red center, § Status Dot); and single-source `isSelected` selection (URL window-id first, `isActiveWindow` fallback — see § URL as Resumable Bookmark). The intra-`WindowRow` `useMemo`s (`tint`/`uncoloredSelectedTint`/`borderColor`/`buttonStyle`/`buttonClass`) apply. (`260609-ebks`, `260615-0hsz`)

**Desktop** (`>= 640px`): `Shell` renders the sidebar `<aside style={{ gridArea: "sidebar" }} aria-label="Sidebar">` itself (rows 2–3, below the full-width topbar — see § Shell Grid Layout), gated `!isMobile && sidebarOpen && !!sidebarChildren` and wrapping `sidebarChildren` in a `flex-1 min-w-0 overflow-hidden` content wrapper. Consumers pass `sidebarChildren` (and, optionally, `sidebarResizeHandle`) rather than grid-area-placing their own aside. Drag-resizable panel, default 220px width. Width persisted to `localStorage` key `runkit-sidebar-width` via `persistSidebarWidth(width)` (called once at drag-end; `setSidebarWidth` updates in-memory state per-pointermove). Constraints: min 160px, max 400px. The drag handle is passed through Shell's `sidebarResizeHandle?: ReactNode` slot and rendered at the aside's right edge (after the content wrapper): a `w-[3px]` visible bar (the 3px structural seam — see § Border-Width System; `bg-border hover:bg-text-secondary`) with `col-resize` cursor, driven by pointer events (unified mouse/touch/pen). Its grabbable area is extended ~8px LEFTward into the sidebar via an invisible `before:` pseudo-element (`before:absolute before:inset-y-0 before:-left-2 before:right-0` — pointer events on a pseudo hit its element, so the drag/hover handlers fire unchanged) so the thin visible bar is still easy to grab; it cannot extend rightward over the terminal because the `<aside>` is `overflow-hidden`. The handle is hidden when `sidebarOpen === false` — the only re-open affordance is the hamburger at TopBar.left. Collapse animation: `grid-template-columns 150ms ease-out` (zero-width column when closed; no 48px rail). Drag-handle wiring is AppShell-only — AppShell passes the handle (with all its drag state/handlers) through the slot; BoardPage passes none, so its aside carries a `border-r border-border` seam instead (Shell applies the border only when no handle is present) and drag-resize is intentionally absent on the board route (the dimension lives in `ChromeContext` and is shared). (`260702-6m46`, `260719-rwqf`)

**Mobile** (`< 640px`, the project `sm:` Tailwind breakpoint / `useIsMobile()` hook): Sidebar renders as an overlay positioned via `gridRow: "2/4"` inside the `<Shell>` (NOT `fixed inset-0`) — backdrop and `<aside>` use `position: absolute` so the topbar stays visible during overlay open (matches the existing project convention recorded in `fab/project/context.md`). Backdrop classes include `absolute z-40 bg-black/50`; aside classes include `absolute left-0 z-50 w-[88%] max-w-[320px] bg-bg-primary shadow-2xl` plus `role="dialog" aria-modal="true"`. Open/close is driven by the single `sidebarOpen` boolean. Dismissal: tapping the backdrop, tapping a destination row (auto-closes after navigation), the explicit close affordance at TopBar.left (the hamburger), or **Escape**. (`260509-17m3`, `260613-o20f`)

**Shared modal focus trap** (`useFocusTrap`): `useFocusTrap(containerRef, active, onEscape)` (`app/frontend/src/hooks/use-focus-trap.ts`) is the single focus-cycle contract, consumed by **three** surfaces — the Shell mobile drawer, `Dialog` (`components/dialog.tsx`), and `CommandPalette` (`components/command-palette.tsx`). It focuses the container's first focusable on activation, calls `onEscape` on a **document-level** Escape (so Escape fires regardless of which element inside the container has focus), and contains Tab/Shift+Tab within the container (wrap at the boundaries, `preventDefault` only there). The `document` `keydown` listener attaches only while `active` and is removed on deactivate/unmount; `onEscape` is read through a stable `onEscapeRef` so a fresh closure each render still fires the latest. **No focus-return on close** across any consumer.

- **Shell mobile drawer**: `Shell` attaches a `drawerRef` to the `<aside>` (`role="dialog" aria-modal="true"`) and drives the hook with `active = isMobile && sidebarOpen && !!sidebarChildren` — the mobile overlay only; the desktop sidebar is a grid region and is NEVER trapped. Escape → `setSidebarOpen(false)` (additive to the backdrop/destination/hamburger dismissals above).
- **`Dialog`**: a leaf modal that only mounts while open, so it drives the trap with the constant `useFocusTrap(dialogRef, true, onClose)` — `dialogRef` on the `role="dialog" aria-modal="true"` div, Escape → `onClose`.
- **`CommandPalette`**: `useFocusTrap(paletteRef, open, () => setOpen(false))` — `paletteRef` on the `role="dialog" aria-modal="true"` div. The hook owns Escape (document-level — closes from any focused element inside), Tab containment, and initial focus; the `<input>` is the palette's **sole** focusable (option rows are non-focusable `role="option"` divs), so the hook's focus-first lands on it and Tab wraps to it. The palette's own input `onKeyDown` retains only ArrowUp/ArrowDown/Enter list navigation.

(`260613-o20f`, `260719-dsw9-focus-trap-adoption`)

**Nested-modal deference** (R10): while a nested **modal** dialog — a `[role="dialog"][aria-modal="true"]` **descendant** of the trap container — is open, `hasNestedDialog(container)` detects it and the trap stands down entirely: its `handleKeyDown` early-returns before both Escape and Tab. So a single Escape dismisses only the topmost modal, and the container-wide Tab wrap can't pull focus out of the nested dialog into the elements behind it. This applies to every `useFocusTrap` consumer; the live case is the drawer — `KillDialog`→non-portaled `Dialog` renders inside the Sidebar tree, hence inside the `<aside>` — while `Dialog` and `CommandPalette` are leaf modals today, so their stand-down is dormant (correct semantics if one ever nests). The container itself carries `role="dialog"` but is correctly excluded: `querySelector` returns descendants only, backed by an explicit `!== container` guard. **The `aria-modal="true"` part of the selector is load-bearing** (`260613-o20f` PR review): `PinPopover` carries `role="dialog"` but is NON-modal (no `aria-modal`, no internal Tab-trap) — matching it would have made the trap stand down for a layer that doesn't contain focus itself, letting Tab escape the container and regressing the `aria-modal` contract; restricting to modal dialogs keeps the trap live for `PinPopover`/`SwatchPopover`. (Those two popovers attach capture-phase `document` keydown listeners with `stopPropagation`, which independently suppress the trap's bubble-phase Escape listener — so there's no Escape collision with them even though they are not caught by `hasNestedDialog`.)

**Resize separator cursor convention**: Horizontal separator (server↔session panel divider, inside `CollapsiblePanel`) uses `cursor-row-resize`; vertical separator (sidebar↔terminal, in `app.tsx`) uses `cursor-col-resize`. Both are the double-arrow-with-middle-bar vocabulary (not `ns-resize`/`ew-resize`). Hover highlight on both uses full-opacity `hover:bg-text-secondary`.

**Document-body cursor override during drag**: Thin drag handles lose their `:hover` cursor once a drag starts because the pointer moves off the narrow strip. Both handlers work around this by writing `document.body.style.cursor = "<axis>-resize"` on drag start and `""` on drag end — so the cursor persists at document level across the whole gesture, regardless of where the pointer lands. Both handlers (horizontal `CollapsiblePanel` and vertical sidebar-width) drive this from pointer-event handlers; the vertical handler migrated to pointer events so the corner affordance's `preventDefault()` on pointerdown wouldn't suppress the mouse compatibility events the vertical drag would otherwise rely on. `CollapsiblePanel`'s unmount cleanup also clears the body cursor to prevent a leaked cursor if the component unmounts mid-drag (navigation, hot-reload). Future thin-handle drag affordances should follow this pattern rather than relying on element-level `:hover`.

**Multi-axis corner affordance**: Both sidebar drag handlers use independent document-level listeners (horizontal tracks `clientY` only, vertical tracks `clientX` only) with independent state refs, so they coexist without coordination. The corner element at the separator intersection exploits this: its pointerdown invokes the horizontal handler, then the vertical handler, then writes `document.body.style.cursor = "nwse-resize"` last so the diagonal cursor wins over the axis-specific writes. Corner visibility is coupled to the horizontal handle's `showDragHandle` (`resizable && isOpen && !isMobile`) — same guard, no separate source of truth. Wired via optional props: `app.tsx` → `Sidebar` (`onSidebarResizeStart`) → `ServerPanel` (`onSidebarResizeStart`) → `CollapsiblePanel` (`onCornerPointerDown`). All optional at every level so other `CollapsiblePanel` consumers (WindowPanel, HostPanel, mobile drawer) render the default single-handle path unchanged.

**Padding**: `px-3 sm:px-6` (matches top bar and bottom bar chrome padding).

**Sessions header**: The Sessions panel in `sidebar/index.tsx` is a plain always-open `<div>` (intentionally not a `CollapsiblePanel` — the session tree is a core always-visible nav surface). Its header row uses `text-text-secondary` as the baseline text color, with the "Sessions" label rendered via `TypedLabel`. The right edge of the header (an `ml-auto` cluster) carries, in order: the current-session name (rendered `truncate text-text-primary font-mono` when both `currentServer` and `currentSession` are non-null — mirroring the ServerPanel `headerRight` pattern) followed by the scope chip. No background tint on this header — per-server tints live on the `ServerGroup` headers below.

**Scope chip**: A keyboard-focusable `<button>` at the far right edge of the SESSIONS header (after the current-session name) that toggles the sessions-pane scope (`all` ⇄ `current`) via the `useSessionsScope` setter. It reads clearly at rest — a small monospace text chip showing `ALL` (scope `all`) or `CUR` (scope `current`) — because a `current`-filtered list with no visible indicator would look like servers vanished. Idiom: `font-mono text-[10px] tracking-wide`, a `border-border rounded-sm` box, `text-text-secondary hover:text-text-primary` (the standard header-button treatment). Its accessible name is the state-stable `aria-label="Toggle sessions scope"` (so tests/AT stay robust across flips); the `title` tooltip is state-descriptive ("Showing all servers — click to show current server only" / vice versa). No `CollapsiblePanel` refactor of the header — the chip slots directly into the hand-rolled div.

**Per-server `ServerGroup`s**: Below the Sessions header, the sidebar renders one `ServerGroup` per server in `servers` (the list returned by `/api/servers`). Each group is a `CollapsiblePanel`-style collapsible whose header carries the server name and a palette → plus → close action cluster (§ Server-group header action cluster; the `+` creates a session against that section's server regardless of `currentServer`); the body contains that server's session tree, fed by `sessionsByServer.get(server)` and ordered by `sessionOrderByServer.get(server)`. Per-server slice isolation is the rule — drag-and-drop, rename, kill, ghost reconciliation, and optimistic-action keys all carry the server they originated on so cross-server overlays do not leak.

**Default collapse + persistence**: By default the `currentServer` group is open and all other groups are collapsed. User toggles persist per-server in `localStorage["runkit-panel-sessions-{server}"]`. On board routes (`currentServer === null`) no group is the implicit default — collapse follows persisted state, falling back to all-collapsed. The legacy single-server `runkit-panel-sessions` key is migrated best-effort into the current server's namespaced key on first read when `currentServer` is non-null; if the user first lands on a board route, the migration is skipped (acceptable per the spec's "best-effort, no error if missing" rule). Expanding a non-current server's group also calls `attachServer(name)` on the provider so its slice starts populating from SSE. **`toggleServerSection`'s `setServerSectionsOpen` updater MUST be pure** — it MUST NOT `localStorage.setItem` or call `attachServer` inside the updater body. Under React 19 StrictMode (active via `main.tsx`, dev + e2e) updaters are double-invoked, so a `localStorage` write inside the updater is observed by the second pass (`readServerOpen` re-reads it), inverting the computed `next` and turning a single Expand click into a net no-op (the group never opens). Instead, snapshot `current = readServerOpen(server)` once, compute `next = !current`, run the persist + lazy `attachServer` side-effects ONCE outside the updater, then commit a pure functional update that derives `next` from `prev` (falling back to the pre-write `current` snapshot for an untouched group, never a fresh `readServerOpen`) for batch-safety. Established by `260602-mss7-fix-sidebar-group-expand`.

**Sessions-pane scope** (which servers' `ServerGroup`s the Sessions Pane lists) is an **explicit persisted state**, independent of the SERVER panel's expansion. It lives in localStorage key `runkit-panel-sessions-scope` with values `all | current`, default `all`; any unrecognized stored value reads as `all`. The state is owned by `useSessionsScope()` (`hooks/use-sessions-scope.ts`) — a thin wrapper over the generic `useLocalStorageEnum` (§ Sessions-scope hooks below) exporting `SESSIONS_SCOPE_KEY`, the `SessionsScope` type, and the `SESSIONS_SCOPES` tuple. The SESSIONS-header scope chip, the session-list render, and the command-palette entry are sibling subscribers of that one key, so a flip on any surface re-renders the others in the same tab.

**Scope behaviors**:

- **`all`** (default): the Sessions Pane iterates the full `servers` array; every server renders as a `ServerGroup` with per-server collapse state (`runkit-panel-sessions-{server}` via `readServerOpen`) applied as usual.
- **`current`** (resolvable): when scope is `current` AND `currentServer !== null` AND `currentServer` is present in the `servers` list, the pane renders exactly one `ServerGroup` (the entry whose `name === currentServer`), omitting all other groups from the DOM (absent, not CSS-hidden), and **force-opens** it (`isOpen={true}` passed regardless of `localStorage["runkit-panel-sessions-{currentServer}"]`) so the filtered view always shows content rather than a degenerate collapsed header. The force-open is **render-time only** — the persisted per-server key is never written by it, and chevron clicks still flow through `toggleServerSection(currentServer)` so the persisted state advances (taking visible effect when scope returns to `all`). Other servers' persisted collapse states sit dormant while filtered out — never cleared or overwritten.
- **`current` fallback-to-all**: when scope is `current` but no current server resolves — a board route (`currentServer === null`) OR a stale/deleted route param not in the `servers` list — the pane **falls back to rendering all servers**. There is no dead-end hint in this case — the `current` scope is never a dead-end or an empty pane.

The `currentOnly` gate in `index.tsx` is exactly `sessionsScope === "current" && currentServer !== null && servers.some((s) => s.name === currentServer)`; when true, `visibleServers` filters to the current server and each group's `isOpen` is forced true, otherwise `visibleServers` is the full list and `isOpen` reads `readServerOpen`. The transition between the all-servers tree and the current-only tree snaps in the next React commit — no group-list-level animation (only the per-group `CollapsiblePanel` body transitions remain). Empty `servers` still renders the "No servers" empty state regardless of scope.

**Scope chip** — see § Scope chip above. **Palette entry**: a single `sessionsScopeActions` block in `app.tsx` (composed into `paletteActions`) with one target-labeled toggle — `Sessions: Show current server only` when scope is `all`, `Sessions: Show all servers` when `current` — following the `Noun: Verb` idiom and satisfying Constitution V (keyboard reachability, palette as primary discovery).

**Sessions-scope hooks**: `useLocalStorageEnum<T extends string>(storageKey, defaultValue, allowedValues)` (`hooks/use-local-storage-enum.ts`) is the generic string-enum sibling of `useLocalStorageBoolean` — same in-module pub/sub keyed on the storage key (so same-tab siblings re-render on a write, which localStorage alone does not trigger), same `storage`-event subscription for free cross-tab parity, same try/catch guard around every localStorage access (SSR/privacy mode). Values outside `allowedValues` (including unset) resolve to `defaultValue`. `allowedValues` MUST be a stable reference (a module-level constant) — it is a `useEffect` dep and inline arrays would churn; all current consumers pass the `SESSIONS_SCOPES` constant. `useSessionsScope()` is the thin scope-specific wrapper over it. (`useLocalStorageBoolean` remains in use by `CollapsiblePanel`'s own open/closed persistence — including `ServerPanel`'s.)

**Server-group header tint (Variant D)**: Every `ServerGroup` header in the SESSIONS pane renders as a filled bar carrying that server's color — the tint is applied to the full-width header row container (the `group flex items-stretch w-full` div holding the flex-1 expand/collapse toggle button and the trailing action cluster — § Server-group header action cluster below), NOT just the current server. Fill and text resolve through the SAME dual-keyed descriptor maps the SERVER tiles use (§ Color Tinting) — no parallel resolution path, no hardcoded hexes: `rowTints.get(key)` for the fill, `rowBorders.get(key)` (contrast-guarded accent hex) for the text and top border. The lookup key is the raw stored `serverColor` descriptor when `rowTints` has an entry for it, else the `UNCOLORED_SELECTED_KEY` gray sentinel (`index.tsx:1523` — covers both an unassigned color and an unrecognized descriptor); the uncolored fallback carries the identical heavier treatment so colored and uncolored groups read as the same element class.

- **Current server** = deeper `headerTint.selected` (40%) fill + `text-text-primary` text, flat on hover (a lighter `hover` (22%) shade would read as an inverted effect — the same rule `CollapsiblePanel` applies to its `tintOnlyWhenCollapsed` selected-shade header, § Sidebar `collapsible-panel.tsx`).
- **Other servers** = `headerTint.base` (14%) fill + the guarded `rowBorders` accent hex as text (set inline, not a `text-*` class); hover deepens to `headerTint.hover` (22%) and restores to `base` on leave, applied imperatively via `onMouseEnter`/`onMouseLeave` background mutations (`index.tsx:1549` — the established dynamic-background pattern shared with window/session rows and `CollapsiblePanel`, avoiding CSS-specificity fights). Only non-current headers carry the hover swap.

**Heavier treatment**: the toggle button is `min-h-[26px]` on fine pointers / `coarse:min-h-[28px]` touch floor (the coarse floor must not shrink), `font-semibold`, `text-[10px] uppercase tracking-wider`; the header container carries a **1px top border in the resolved `rowBorders` accent** (`borderTop`, `index.tsx:1547`) separating consecutive groups. The `<section>`'s neutral `border-b border-border last:border-b-0` (`index.tsx:1532`) is left to carry the section separation, and the header bar has no separate background-only hover state (all shading rides the tint). Since every header is weight 600, the current-server distinction rides entirely on tint strength (`selected` vs `base`) and text (`text-text-primary` vs the guarded accent), not weight.

**Scope boundary — header surface only**: color appears ONLY on the header bar. The left gutter belongs to the window-marker vocabulary (§ Left-Edge Label Zone, `markerStripeStyle`) and stays untouched — this is why the rejected full-height group stripe was not chosen. No color wash on group body rows, and session rows, window rows, agent-activity tints, and the SERVER panel tiles are unchanged. Header semantics are preserved: `aria-labelledby`/`aria-expanded`, the `data-server`/`data-current-server`/`aria-current` attributes, the chevron disclosure, and the header's action cluster (§ below) all carry over. The tint tracks `currentServer` reactively (server-route switches re-paint without remounting the group); no tint is drawn distinctively when `currentServer === null` (board routes / index) — those headers simply render as non-current. (`260720-t1ca`)

**Server-group header action cluster** (palette → plus → close): the tinted header hosts a three-button cluster at its right end, in fixed DOM order palette, plus, close — the header both *displays* the server's color and *hosts* the server's actions, so a user working entirely in the SESSIONS pane (independent of the SERVER panel's expansion — § Sessions-pane scope) reaches server color and server kill on the element that shows the color. The cluster occupies the right-end slot; the toggle button keeps `flex-1 min-w-0` and stays the dominant click target.

- **Palette** (`PaletteIcon`, `aria-label` `Set color for server {name}`): hover-revealed with the touch fallback `opacity-0 group-hover:opacity-100 coarse:opacity-100` (the header container carries the plain `group` that drives it — no ancestor claims `group`). Toggles a per-group local `showColorPicker` boolean (not the SERVER panel's keyed `colorPickerFor` map — a header renders exactly one group) opening a **color-only** `SwatchPopover` (no `onSelectMarker`). The popover is portalled to `document.body` at `position: fixed` coordinates anchored at the palette button (right-aligned `right: innerWidth - rect.right`, flip-above via a `useLayoutEffect` with the ServerTile's ~100px height heuristic), escaping the session list's `overflow-y: auto` clip — the same portal treatment the SERVER tiles use. A swatch pick funnels through the shared `handleServerColorChange` (below) then closes.
- **Plus** (`aria-label` `New session on {name}`, unchanged behavior — `onCreateSession(server)` instant create): always visible.
- **Close** (`✕`, `aria-label` `Kill server {name}`, `hover:text-signal-red`): always visible; calls the `onKillServer(server)` prop, which both route shells wire to the `server-dialogs-context` `requestKillServer` trigger — inheriting the shared layout-mounted kill-confirmation dialog, daemon warning, navigation-after-kill, and `killServer` API (§ Server create/kill dialogs). No per-route dialog, handler, or endpoint; header-kill and tile-kill share one path.

Deliberate divergence from the SERVER tiles: the actions are NOT hidden on mobile (no `showActions = !isMobile` gate) — coarse pointers have no hover, so palette/plus/close all stay reachable on touch, mirroring the session-row `+ ✕` convention. Icon legibility on the tinted fill rides a cluster **wrapper** carrying the header's text treatment (inline contrast-guarded `headerAccent` for non-current headers / `text-text-primary` for the current one); buttons **inherit** that color at rest so their own Tailwind `hover:` classes win (an inline `style.color` on a button would beat any hover class). Known parity quirk carried over from the ServerTile picker: re-clicking the palette while the popover is open double-fires (outside-mousedown close, then toggle reopen), so the palette cannot self-close. (`260721-x4sf`)

**Single shared server-color write seam**: server color is written through ONE handler — `handleServerColorChange` (a `useCallback` in `Sidebar`) — passed to BOTH the SERVER-panel tiles and every SESSIONS-pane `ServerGroup` header, so the two color-picking surfaces funnel through the same code, not parallel paths. It updates `serverColors` optimistically (the header tint and the tile stripe repaint immediately — server user-option color mutations emit no control-mode event, so covered servers otherwise wait on the 12s safety poll, § tmux-sessions `row-color-safety-poll-latency`) then POSTs via `setServerColorApi`, toasting on failure. The picked family reaches it already mapped to its legacy descriptor through the `SwatchPopover` write seam (§ SwatchPopover — `familyToLegacy`).

**`ServerGroup` memo contract with the action-cluster props**: the header cluster added two props to the memoized `ServerGroup` (`React.memo(ServerGroupInner)`), both referentially stable so an SSE tick on one server does not re-render every other server's group. `onServerColorChange` is the shared `handleServerColorChange` `useCallback` (stable identity). `onKillServer` is the `Sidebar` prop passed through unchanged — and it is stable end-to-end because both `Sidebar` parents (`app.tsx` and `board-page.tsx`) pass the `server-dialogs-context` `requestKillServer` trigger straight through (referentially stable by construction, § Server create/kill dialogs); an inline arrow at the parent would churn a fresh identity every tick and defeat the memo skip for all groups. See § Render Performance for the group-memo tree.

**Cross-server window navigation**: Clicking a session name or window row in a non-current server's group navigates to that server's route (`/$server/$window`) via `navigate({ to: "/$server/$window", params: { server: thatServer, window } })`. The provider picks up the new `currentServer` via `useMatches()` on the next route match — no explicit `setCurrentServer` call. The previously-current server's group remains rendered (per default-collapse rules); its EventSource and slice persist so re-visiting is instant.

**Cross-server drag-and-drop is rejected**: Dragging a window from one server's group and dropping it on another server's session/group fires a toast `"Moving windows across tmux servers isn't supported yet"` and skips any move API call. tmux's `move-window` does not span servers, so cross-server move is a separate problem. Within-server drag-and-drop (window reorder within a session, cross-session window move within the same server, session reorder within the same server) is preserved verbatim.

**Session rows**: Chevron toggle (left, expands/collapses window list), session name (selects the session's first window via `onSelectWindow(server, session, windows[0].windowId)` → `selectWindow(server, windowId)`), + new window button (right), ✕ kill button (right, always visible). Click session name fires the select mutation for the first window's `@N`; the URL follows on the next SSE snapshot to `/$server/<windowId>` (2-segment); click chevron toggles expand/collapse (persisted — § Session collapse persistence). The session-level `+` button triggers instant session creation against the parent group's server (calls `onCreateSession` → `executeCreateSessionInstant`), not a dialog. The window-level `+` button triggers instant window creation (existing `executeCreateWindow` behavior, passes `activeWin?.worktreePath` as CWD).

**Session collapse persistence**: the per-session window-list collapse map (`collapsed` in `sidebar/index.tsx`, keyed `` `${server}:${session.name}` ``) survives a refresh, a fresh tab, and a dev-server reload. It persists in a SINGLE localStorage key `runkit-session-collapsed` (the exported `SESSION_COLLAPSED_STORAGE_KEY`) holding a JSON object of collapsed **exceptions** — `{"default:utils2": true, "fabKit1:relay-spike": true}`. Only `true` entries are stored: expanding a session **deletes** its key, and an emptied map removes the storage key outright rather than persisting `{}`, so "no exceptions" and "never used" are one state on the next read. A session with no entry renders expanded — the read sites keep their `collapsed[sessionRowKey] ?? false` default, so a new session and a cleared browser behave exactly as an untouched one. The state is seeded lazily at mount (`useState(readCollapsedSessions)`), so a collapsed session paints collapsed on the first frame with no post-hydration flip. This map key sits **alongside**, not in place of, the per-server section scalars `runkit-panel-sessions-{server}` (§ Default collapse + persistence) — a server's open/closed bit is one boolean per server; the per-session exceptions are one map for all servers, because per-session scalar keys would sprawl unboundedly across killed sessions and could not be enumerated for cleanup. (`260807-kddk`)

**Collapse read/write posture**: `readCollapsedSessions()` is tolerant — a throwing `localStorage` (privacy mode, sandboxed iframe), malformed JSON, a non-object root (an array included), and non-`true` entry values each degrade to `{}` (everything expanded); never a throw, never a blank sidebar. Normalizing to `true`-only entries on read is what keeps the map exceptions-only: a stored `false` is dropped, not honored. `writeCollapsedSessions()` is best-effort under the same try/catch silence (the file-wide localStorage convention). `toggleSession` derives `next` from a `collapsedRef` synchronous mirror, advances the ref, calls the writer, then commits the already-computed value via `setCollapsed(next)` — **the write stays OUTSIDE the state updater**, the same StrictMode purity constraint `toggleServerSection` carries (§ Default collapse + persistence): React 19 double-invokes updaters, so a `setItem` inside one is observed by the second pass and a single click nets to a no-op. The ref mirror covers the second hazard the snapshot alone does not — two toggles batched into one render would both derive from the same stale closure-captured state and persist a map disagreeing with what is committed.

**Two accepted gaps in the collapse map** (§ Design Decisions → Collapse exceptions are a single map key): (1) **no stale-entry pruning** — entries outlive the sessions they name, across kill and rename alike, so a recreated session that reuses a previously-collapsed name **inherits** that collapsed entry. Pruning would require the full session set across *non-attached* servers, which the client deliberately does not have; the orphans are single booleans and cost nothing but that surprise. (2) **no cross-tab sync** — there is no `storage` listener, and each tab snapshots the map at mount. Because a write replaces the **whole** map (the per-server scalars are independent keys, so a collision there is confined to one server), two tabs are last-write-wins across every server's exceptions: the later writer's snapshot wins and the other tab's collapses are dropped. A refresh picks up whatever was written last.

**Window rows** (**Row Minimalism**): Single line with a leading unified `StatusDot` + window name (left). There is **no trailing status TEXT cluster** — no fab stage-word span, no duration text — and the freed width goes to the window name. The row carries **two** status signals: the leading `StatusDot` (core hue = journey, shape = health, additive halo = `waiting`) and, for a window with an owned PR, a **rest-state PR glyph** in the trailing cluster's last slot (§ Rest-state PR glyph). The exact stage word + durations live in the row-hover flyout card and the PANE panel's register view (their recovery path). Hover-reveal action icons (pin / color / kill) are ACTIONS, not status. All rows have `border-l-2` (transparent when not selected to prevent layout shift). (`260706-y1ar`, `93dy`) Currently selected window highlighted with `bg-accent/10` + `border-accent` + `font-medium` + `rounded-r`. Selection compares the URL window ID against `win.windowId` (falling back to `isActiveWindow` when no URL window) — clicking a row fires `selectWindow(server, win.windowId)` (a pure tmux mutation; window ID is a self-contained tmux target, so no session arg); the selection state and URL update follow on the next SSE snapshot. See § URL as Resumable Bookmark for the full sidebar-click contract. The session row (parent) gains a `waiting`-count badge — see § Attention Surfacing.

**Current-row focus on mobile drawer open**: `Sidebar` attaches `navRef` to its `<nav aria-label="Sessions">` and runs a `useEffect` (reading `useIsMobile()` + chrome `sidebarOpen`) that, when the drawer is visible on mobile, queries `[data-window-id] [aria-current="page"]` then `scrollIntoView({ block: "nearest" })` + `focus()`es that row — landing the keyboard user on their current context. Deferred via `requestAnimationFrame` so it takes precedence over the focus trap's first-focus (committed in `Shell`'s effect). The selector is scoped to WINDOW rows: the active `BoardsSection` row also carries `aria-current="page"` but has no `[data-window-id]` ancestor, so it is excluded — board routes (no selected window) no-op and the trap's first-focusable focus stands. Mirrors the ServerPanel active-tile mount-scroll pattern (§ server-panel `ServerPanel`). This effect ALSO syncs the roving cursor: it `setRovingKey(key)`s the `[data-window-id]` row it focuses so the tab-stop (`tabIndex={0}`) and the DOM-focused row don't desync — see § Keyboard Navigation & Tree ARIA below. This is the **mobile drawer-open** effect: it scroll+focus+roving-syncs, a distinct contract from the desktop selection-keyed **scroll-only** effect below. (`260613-o20f`, `260613-wt1v`)

**Desktop selection-keyed autoscroll (scroll-only)**: a SEPARATE `useEffect` on `Sidebar` — alongside, NOT replacing, the mobile drawer-open effect above — brings the selected window row into view when the **selection identity** `selectionKey = ${currentServer}:${currentWindowId}` changes (a sidebar click, palette switch, or deep link). It queries the same scoped `navRef.current?.querySelector('[data-window-id] [aria-current="page"]')` selector (excludes the active `BoardsSection` row, which has no `[data-window-id]` ancestor) and calls `scrollIntoView({ block: "nearest" })` on the selected row's button inside the `role="tree"` scroll container. **Scroll-only**: it makes NO `focus()` call (focus-steal on desktop would break terminal typing — the mobile effect focuses only to beat the focus trap) and NO `rovingKey` write, so roving/focus state is untouched, which trivially preserves the Wave-2 #262 invariant (§ Keyboard Navigation & Tree ARIA → the load-bearing invariant). Guarded by `typeof row.scrollIntoView === "function"` for jsdom.

- **Deep-link retry via `pendingScrollKeyRef` + `rowsVersion`**: on a direct URL load the route resolves before SSE data lands, so the row may not exist when the effect first fires. A `pendingScrollKeyRef` arms when `selectionKey` changes (compared against `lastSelectionKeyRef`) and clears once the row is found and scrolled; the effect's deps are `[selectionKey, rowsVersion]`, so the existing `rowsVersion` counter (§ Keyboard Navigation & Tree ARIA — bumped ONLY on a group's visible-row SET signature change: add/remove, collapse/expand, rename; never on passive SSE activity ticks) re-runs the attempt when rows appear. Result: **exactly one scroll per selection change** — a passive SSE tick neither re-runs the effect nor re-arms the ref (Wave-2 #262 invariant, § Keyboard Navigation & Tree ARIA).
- **Null selection disarms**: when `currentServer` or `currentWindowId` is `null` (board route, or a `/$server` dashboard route before the URL carries a window segment) `selectionKey` is `null` and the effect performs no query-and-scroll work.
- **Collapsed group is a no-op with a deferred completion**: if the selected window's session/server group is collapsed, its row is not in the DOM, so the effect no-ops — it does NOT auto-expand the group (rejected: fighting the user's explicit collapse). The armed `pendingScrollKeyRef` persists, so a later `rowsVersion` bump that reveals the row (expand, or data arrival) completes the one deferred scroll — expand-reveal and data-arrival share the one retry mechanism.
- **Sidebar collapse/reopen does not re-scroll**: the effect keys on selection identity, not sidebar visibility, so collapsing and reopening the desktop sidebar for an unchanged selection re-runs nothing.

(`260723-nris`)

1. **Leading status dot (unified `StatusDot`)**: the row's single leading dot is `<StatusDot win={win} />` (`status-dot.tsx`, see § Status Dot), wrapped in a `data-testid="status-dot-tap"` span that on COARSE pointers routes a tap to the row flyout card (`stopPropagation`, so the tap does not select the row) and is inert on fine pointers. It is the compositional **two-family ladder**: a fab window shows its cool pipeline hue (blue `building` — intake·apply·review → green `PR-ready` — ship·review-pr·done) with a shape for health (ring at rest / solid live / dotted-ring+red-center failed); a plain window with a fresh `agentState` shows the warm ad-hoc-agent yellow; a plain window with neither shows the monochrome gray floor (solid = active, ring = idle). The dot renders no PR state — the trailing rest glyph is the row's PR channel (item 3). A `waiting` window of ANY tier gets an **additive signal-yellow halo** (core hue + shape untouched). A failed fab stage renders as a dotted ring **in its phase hue** with only a small red center dot (no whole-dot red tint). Pure CSS + the `rk-waiting-halo` keyframe. (`260615-0hsz`, `aqo6`)

2. **Row Minimalism — no trailing status TEXT cluster**: the row has no fab stage-word span and no duration text (`WindowDuration`/`TickingDuration` leaves + their per-second `useNow()` tick); the freed width goes to the window name. Status is the leading `StatusDot` plus the owned-PR rest glyph (item 3). The exact stage word + durations are the flyout card's + PANE panel's job (their recovery path — see § Row-hover register flyout card, § Pane panel four-register view). (`260706-y1ar`, `93dy`)

3. **PR status — the rest-state trailing glyph only; the dot carries none**. `statusDotState(win)`'s ladder has no PR branch — the dot tells the local story, the glyph the remote one (§ Status Dot). ANY window with an owned PR — including a plain floor pane — shows the rest-state PR glyph in the trailing cluster's last slot (§ Rest-state PR glyph; `GitPullRequestIcon`, or the ✕ `GitPullRequestClosedIcon` for closed), colored by the six-way `prGlyphColor` (gray closed / red failing / gray draft / yellow checks-running / green open / purple merged — closed and draft share the gray token; the ✕ icon shape separates them), keeping derivation universal (Principle X) without a mystifying floor-pane dot. **Still NOT the full PR line** — full PR detail lives in the register view (the flyout card + the PANE panel's L3 register). (`aqo6`, `xuej`)

4. **Trailing cluster** (pin / kill actions + the rest PR glyph) — absolutely-positioned container (`group/icons absolute right-2 top-1/2 -translate-y-1/2 flex items-center gap-1 z-10`) rendered as a sibling of the row button (avoids nested interactive elements); the row button reserves `pr-[68px]` when the pin is wired (`pr-11` otherwise) so labels don't run under the icons. **Inert at rest**: the container carries `pointer-events-none` plus `group-hover:pointer-events-auto coarse:pointer-events-auto has-[:focus-visible]:pointer-events-auto`, so on fine pointers a stray click near the row's right edge falls through to the row-select button instead of hitting an invisible kill/pin/swatch target (deliberate icon clicks are unaffected — any mouse interaction hovers the row first, restoring interactivity). Each hover-revealed `opacity-0` button (pin's not-pinned branch, color swatch, kill) carries `focus-visible:opacity-100` so keyboard focus never sits on an invisible control — the reveal is per-button because a container-level `opacity-100` cannot reveal children carrying their own `opacity-0` (element opacity is independent/multiplicative); the container-level `has-[:focus-visible]:pointer-events-auto` restores interactivity while a child is focused. The `pr-[68px]` reservation, `coarse:opacity-100` always-visible icons on touch, and the pin's permanent visibility when pinned-to-any apply. The container's `group/icons` name exists so the rest PR glyph — an absolute overlay on the kill button's slot — can hide on focus WITHIN the cluster (§ Rest-state PR glyph). Window metadata lives in the register surfaces (the flyout card + `status-panel.tsx`), not a per-row info popover. (`260612-epqk`, `93dy`)

**Empty state**: When a server group has no sessions (`sessions.length === 0`), it renders a full-width text button reading `(no sessions — + new, or {chord})` in the parenthesized terse style of the surrounding sidebar copy — `{chord}` is the host-effective `create-session` binding via `useKeybindings()` + `formatCombo`, the `, or {chord}` clause omitted when that binding is unbound/disabled (§ Education micro-copy). The button triggers instant session creation (same as the sidebar `+` button and breadcrumb dropdown `+ New Session` action). With no active window, `cwd` is omitted and the name falls back to `session`.

**Inline rename** (double-click — windows and sessions): Both window names and session names in the sidebar support double-click inline rename. The pattern is identical for both:

- Double-clicking a name `<span>` replaces it with a text `<input>` pre-filled with the current name, auto-focused with all text selected.
- Enter or blur commits the rename if the trimmed value is non-empty and differs from the original name. Empty or unchanged input dismisses the editor without an API call.
- Escape cancels editing. A `cancelledRef` / `sessionCancelledRef` prevents blur from committing after an Escape (or cross-cancel).
- Single-click behavior is preserved (navigate to window / navigate to session's first window) — only `onDoubleClick` triggers editing.

**Window rename**: the sidebar's inline window rename goes through the **shared `useWindowRename()` hook** (`hooks/use-window-rename.ts`) — the same hook the top-bar `WindowHeading` uses, so both surfaces rename identically. The hook's `execute(server, session, windowId, newName)` runs the optimistic store rename (`windowStore.renameWindow`), rolls back via `windowStore.clearRename` + toast on API failure, and clears the pending marker on settle; SSE reconciles the canonical updated name once the server event arrives. See § Shared window rename. (The sidebar owns its own trim/empty-name guard before calling `execute`.) (`260703-5ilm`)

**Session rename**: calls `renameSession(server, oldName, newName)` via `useOptimisticAction`. The UI updates immediately via `markRenamed("session", server, oldName, newName)`; on API failure it rolls back via `unmarkRenamed(server, oldName)` (`lastRenameSessionRef` snapshots `{ server, name }` together for cross-server-safe rollback). Toast error on failure. The dialog-based session rename in `app.tsx` is an alternate path — inline editing is an additional one.

**Cross-cancellation**: Only one inline edit (window or session) may be active at a time. Starting any new inline edit cancels the currently active one without committing it. `handleStartEditing` (window edit) sets `sessionCancelledRef.current = true` and clears `editingSession` before activating the window input; `handleStartSessionEditing` sets `cancelledRef.current = true` and clears `editingWindow` before activating the session input. This ensures blur on the cancelled input is a no-op.

**Window drag-and-drop reorder**: Window items in the sidebar are `draggable={true}` (ghost windows excluded). Uses native HTML5 drag-and-drop — no external library (constitution IV). Drag state managed via `dragSource` and `dropTarget` state in `Sidebar`. On `dragStart`, sets `dataTransfer` with JSON `{ server, session, index, windowId, name }`, a dedicated marker MIME `application/x-window-drag` (`WINDOW_DRAG_MIME`, value: the `windowId`) so foreign drop targets — the sidebar board rows, § Sidebar Boards Section — can gate on `dataTransfer.types` during `dragover`, and `effectAllowed: "copyMove"` so board rows can offer the copy (link) cursor while `"move"` stays permitted for the reorder/move targets (`260811-g0t1`). The `windowId` and `name` fields serve cross-session optimistic operations; within-session drops use only `session` and `index`. Within-session drops: drop indicator is a 2px accent-colored top border (`borderTop: 2px solid var(--color-accent)`) on the hovered window item when source and target differ. On `drop`, uses `useOptimisticAction` to immediately swap window indices in the Zustand store via `swapWindowOrder(session, srcIndex, dstIndex)`, then fires `moveWindow(server, srcWindowId, dstIndex)` API call in the background (server captured at drop-handler time; the move targets the source by its stable `windowId` and the destination by position). The optimistic action's arg tuple is `(server, session, srcWindowId, srcIndex, dstIndex)` — `session`/`srcIndex` retained for the store-side swap/rollback, while the API call uses only `srcWindowId` + `dstIndex`. `onSelectWindow` is called immediately (not deferred to API success). On API failure, `onAlwaysRollback` reverses the swap via `swapWindowOrder(session, dstIndex, srcIndex)` and shows a toast error. SSE reconciliation naturally clears the optimistic state when `setWindowsForSession` replaces all entries with server-confirmed data. Same-position drops are no-ops (source === target check). All drag visual state (drop indicators) cleared on `dragEnd` and `drop` — handles both successful drops and cancelled drags (Escape, drag outside sidebar).

**Cross-session drag-and-drop**: Dropping a window onto a different session's header moves it to that session. `handleDragOver` accepts drag events on session headers when the dragged window is from a different session. Visual feedback: the session header shows an accent border (`border-accent`) when a valid cross-session drop is hovering. The drag data payload includes `{ session, index, windowId, name }` — `windowId` and `name` serve optimistic store operations (within-session drop ignores the extra fields). On drop, `handleSessionDrop` calls `executeMoveToSession` (a `useOptimisticAction` instance) with all six arguments `(server, srcSession, srcIndex, windowId, windowName, dstSession)`. The optimistic lifecycle:

- **`onOptimistic`**: calls `killWindow(srcSession, windowId)` to hide the window from the source session, `addGhostWindow(dstSession, windowName)` to show it in the target (using the source window's display name, not a placeholder), and navigates to `/$server` immediately. The `optimisticId` is stored in a ref for rollback.
- **`action`**: calls `moveWindowToSession(server, srcSession, srcIndex, dstSession)` — the API client function with `server` as the first positional argument.
- **`onAlwaysRollback`** (API failure): calls `restoreWindow(srcSession, windowId)` to un-hide in source + `removeGhost(optimisticId)` to remove from target. Toast: "Failed to move window to session".
- **`onAlwaysSettled`** (success): clears the ref. SSE reconciliation handles final state — `setWindowsForSession` removes the entry from the source (not in incoming list), adds it to the target (new incoming entry), and reconciles the ghost (new `windowId` not in ghost's `snapshotWindowIds`).

Same-session header drops are ignored (not a valid cross-session target). Within-session window-to-window drag-and-drop is unchanged. The sidebar handles the move internally — no `onMoveWindowToSession` prop or `handleMoveWindowToSession` callback in `app.tsx` (the sidebar imports `moveWindowToSession` directly from `@/api/client`, matching the pattern of other sidebar API calls).

**Session drag-and-drop reorder**: Session header rows are themselves draggable (the row root, not the chevron / name button) — except optimistic ghost sessions, which set `draggable={false}` because they have no canonical name to persist yet. The reorder feature shares the SAME `<SessionRow>` `onDragOver` handler used by cross-session window drops; the two are disambiguated by `dataTransfer.types`: window drags use the default JSON payload, session reorder drags use a custom MIME `application/x-session-reorder` (so a window-drag never accidentally triggers a session reorder, and vice versa). Native HTML5 drag-and-drop, no external library (Constitution IV).

The persisted order lives server-side in tmux user-option `@rk_session_order` (see `tmux-sessions.md` § "Server-Scoped User Options"), broadcast to all connected clients via SSE event `session-order`. Frontend pipeline:

- **Source of truth**: `SessionContext.sessionOrder: string[]` populated from the SSE `session-order` event handler in `session-context.tsx` (`es.addEventListener("session-order", ...)`). The handler ignores events whose `server` field doesn't match the current `server` prop — defense-in-depth alongside the backend's per-server filtering.
- **Render-time order computation**: `Sidebar` derives `orderedSessions` via `useMemo` over `sessions` and an effective order. Sessions absent from the saved order render at the bottom in their natural sequence (`Number.POSITIVE_INFINITY` rank) so newly created sessions never disappear from the sidebar.
- **Snappy drag (derive-over-store)**: The transient drag override is held in a server-keyed `orderOverrideRef = useRef<Record<string, string[]>>({})` (NOT React state), and the displayed order is *derived at render* as `orderOverrideRef.current[server] ?? (sessionOrderByServer.get(server) ?? [])` — `handleSessionReorderStart`/`handleSessionReorderOver` compute the new order via `splice` and write it into the ref for instant visual feedback. Because writing a ref does not re-render, the handlers call a minimal `forceRender` nudge (`useReducer((x) => x + 1, 0)`). The override clears via a **per-server, render-time SSE-equality reconcile**: while computing each visible server's props, if `arraysEqual(orderOverrideRef.current[server], sseOrder)` the override is deleted and `localOrder` rendered as `null` in the same pass (no nudge needed — the displayed output is unchanged). The per-server equality check (NOT a whole-`sessionOrderByServer`-Map watcher `useEffect`, which would re-run on every unrelated SSE slice tick — sessions/metrics/connection fire a fresh Map reference several times a second) fires only on the relevant server's echo, so unrelated churn triggers no reconcile work; **do not reintroduce a whole-Map watcher.** No **data** snap-back: the override outlives the debounced PUT until that server's SSE order echoes the new arrangement, so the *order* never reverts. (`260609-ebks`)
- **`dragover` MUST accept the drop even on the self-target row**: `handleSessionReorderOver` MUST call `e.preventDefault()` + `dropEffect = "move"` for a valid same-server session-reorder drag *even when hovering the dragged row itself* (`sessionDragSource.name === targetName`), then bail with no reorder math. HTML5 DnD registers a release as an accepted drop only when the LAST `dragover` before mouse-up was `preventDefault()`ed; insert-before splicing lands the dragged row under the cursor, so the dragged row's OWN element is the **common terminal hover state** at release. Bailing early there (folding the self-name check into the leading guard) leaves the final `dragover` uncancelled → no `drop` fires → `dragend` sees `dropEffect: "none"` → the browser plays its native **cancelled-drag snap-back ghost** flying back to the drag origin, contradicting the visibly-succeeded reorder (the data stuck via the debounce). **Guard order (invariant across both this handler and `useServerReorder.onDragOver` — see § Server-tile drag reorder)**: source/scope guard (`!sessionDragSource || sessionDragSource.server !== server`, confines the drag to one server's group) + MIME guard (`application/x-session-reorder`) come BEFORE acceptance; the self-target no-op (`if (sessionDragSource.name === targetName) return;`) comes AFTER `preventDefault()`. Do NOT re-fold the self-name check back into the leading guard — that is exactly the snap-back regression. Session-reorder keeps **debounce-only persistence** — even though `drop` also fires on session rows, there is no session drop-flush (the row's own `onDrop={handleSessionDrop}` is the window-move handler and no-ops safely on a reorder payload: empty `application/json` → `JSON.parse` throws → `catch` → return). (`260705-i41e`)
- **Debounced PUT**: 250ms trailing debounce via `orderPutTimerRef` + `setTimeout`. Drag events fire on every cursor frame; coalescing avoids one HTTP call per frame. `handleSessionReorderEnd` (dragend) does NOT cancel the pending timer — a fast drag-and-release within 250ms still flushes via the timer, otherwise the user's edit is lost.
- **Drag visual**: Source row gets `opacity-50` while it is the drag source (driven by the `isDragSource` prop on `<SessionRow>`).

`<SessionRow>` exposes optional drag props (`draggable`, `isDragSource`, `onDragStart`, `onDragEnd`) — present for the new feature, absent for any future caller that doesn't want session reorder. The row root passes them straight through to the underlying `<div>`.

**Test ergonomics**: `session-context.tsx` exports a `StandaloneSessionContextProvider` test helper that accepts a partial multi-server shape (`sessionsByServer`, `sessionOrderByServer`, `isConnectedByServer`, `metricsByServer`, `currentServer`, etc.) and synthesizes the full context value without opening any network connection. Tests requiring SSE-driven behavior use the full `SessionProvider` with a stubbed `EventSource`; `MockEventSource` is keyed by URL so per-server streams can be driven independently (see `session-context.test.tsx` for the pattern).

**Drop-acceptance unit-test seam**: session-reorder drop acceptance is asserted through the `renderSidebar` harness in `index.test.tsx`. The idiom: render via `renderSidebar`, `fireEvent.dragStart` the `[data-session-row="{server}:{name}"]` row (seeds `sessionDragSource` and writes the MIME onto a shared mutable `dataTransfer` bag mirroring the hook test's `makeDragEvent`), then `fireEvent.dragOver(row, { dataTransfer })` on the SAME row. **`fireEvent.dragOver` returns `false` when the handler called `preventDefault()`** — that boolean is the drop-acceptance assertion (`expect(notPrevented).toBe(false)` + `dataTransfer.dropEffect === "move"`), no native-method stub needed. A source-guard negative case (`dragOver` with the MIME but NO prior `dragStart` → returns `true`, not accepted) proves the guard fires before acceptance. The window-move `handleSessionDragOver` no-ops when its `dragSource` is null, so a prevented default on a session-reorder dragover is attributable solely to `handleSessionReorderOver`. (`260705-i41e`)

**Server-tile drag reorder**: the two server-tile grids — the Sidebar `ServerPanel` (`server-panel.tsx`) and the Host-page TMUX SERVERS grid (`host-overview-page.tsx`) — are drag-reorderable, sharing ONE lifted hook `useServerReorder(servers, onError)` (`app/frontend/src/hooks/use-server-reorder.ts`) so neither grid copy-pastes the handler logic. (`260705-bpnr`) It mirrors the § Session drag-and-drop reorder pattern verbatim: native HTML5 DnD (no external library, Constitution IV), a custom MIME `application/x-server-reorder` (`SERVER_REORDER_MIME` — so a server drag never collides with the `application/x-session-reorder` session drag or the default-JSON window drag), insert-before splice semantics on `dragOver`, and the derive-over-store optimistic override.

- **Effective-order input**: the hook takes the already-`compareServersRanked`-sorted `ctx.servers` and returns `{ orderedServers, getTileProps(name), isDragging, draggingName }`. Consumers render `orderedServers` (the transient optimistic order during a drag, else the authoritative list) and spread `getTileProps(name)` onto each tile.
- **Infra excluded**: only REGULAR servers participate. `getTileProps` returns `{ draggable: false }` (no drop handlers) for `isInfraServer(name)` tiles — they are neither draggable nor valid drop targets, and the reorder POST carries only the regular-class order (rank `i` → i-th regular server; infra ignore rank server-side, so omitting them is correct and keeps them pinned last).
- **Derive-over-store optimistic override**: the transient dragged order lives in `overrideRef = useRef<string[] | null>` (of regular server NAMES), NOT React state — so writing it doesn't re-render; a minimal `forceRender` (`useReducer((x)=>x+1,0)`) drives the optimistic repaint (the same idiom as the sidebar session reorder). `onDragStart` seeds the override with the current regular order; `onDragOver` splices the dragged name to the target position. The override clears via a **render-time equality reconcile**: once the authoritative regular order element-wise equals the stored override (via `namesEqual`), the round-trip has landed and the ref is dropped (mutating a ref during render is safe — output unchanged, no nudge). **No data snap-back on drag-end** — the override outlives the debounced POST until the SSE `server-order` echo re-sorts `ctx.servers` to match, which triggers the reconcile, so the *order* never reverts.
- **`onDragOver` MUST accept the drop even on the self-target tile**: `onDragOver` MUST call `e.preventDefault()` + `dropEffect = "move"` for a valid same-hook server-reorder drag *even when hovering the dragged tile itself* (`dragName === targetName`), then bail with no reorder math. HTML5 DnD registers a release as an accepted drop only when the LAST `dragover` before mouse-up was `preventDefault()`ed; insert-before splicing lands the dragged tile under the cursor, so the dragged tile's OWN element is the **common terminal hover state** at release. Folding the self-target check into the leading guard leaves the release-over-self `dragover` uncancelled → no `drop` fires → `dragend` sees `dropEffect: "none"` → the browser plays its native **cancelled-drag snap-back ghost** flying the tile back to the drag origin, reading as failure even though the reorder visibly succeeded and persisted (the data stuck via the debounce). **Guard order (invariant, shared verbatim with the sidebar `handleSessionReorderOver` — see § Session drag-and-drop reorder)**: originating-instance guard (`!dragName` — the hook is instantiated by BOTH `ServerPanel` and the Host-page grid; a null-`dragNameRef` instance did not start the drag and must not accept it) → infra guard (`isInfraServer(targetName)`, infra tiles are non-targets) → MIME guard (`SERVER_REORDER_MIME`) — all BEFORE acceptance; then `preventDefault()` + `dropEffect = "move"`; then the self-target no-op (`if (dragName === targetName) return;`) AFTER acceptance. Do NOT re-fold the self-target check back into the leading `!dragName` guard — that is exactly the snap-back regression. (`260705-i41e`)
- **Debounce + drop-flush**: reorder writes go through a `SERVER_ORDER_DEBOUNCE_MS = 250` trailing debounce (`putTimerRef`), coalescing a rapid drag-over sweep into one `setServerOrder(order)` POST — mirroring the sidebar's `SESSION_ORDER_DEBOUNCE_MS`. `onDrop` flushes any pending debounced POST immediately so the write isn't lost if the SSE echo races the timer. The self-target drop acceptance (above) is what lets releasing over the source tile fire `onDrop`, which flushes and clears the timer (single POST, no double-post). A **mount-scoped unmount cleanup effect** (`useEffect(() => () => clearTimeout(putTimerRef.current), [])`) clears a pending timer so a drag ending right before the grid unmounts never fires a stray post-unmount write (mirrors `sidebar/index.tsx:258-262`). (`260705-i41e`)
- **Drag-source feedback**: both grids dim the dragged tile via `opacity-50`, driven by `isDragging && draggingName === name` (the session-reorder `isDragSource` treatment). Do NOT delete the `isDragging`/`draggingName` fields — they are the drag-affordance seam wired into both grids.
- **Non-drag paths**: the Sessions-tree per-server `ServerGroup`s and the palette `Server: Switch to {name}` entries FOLLOW the order (via the `compareServersRanked` choke point) but expose no server drag affordance. Touch devices get no drag (HTML5 DnD doesn't fire on touch — the palette `Server: Move up/down` are the touch/keyboard path; see § Command Palette Move up/down). Coverage: the hook's logic (MIME discrimination, insert-before splice, infra exclusion, override seed/reconcile, debounce/drop-flush, **self-target drop acceptance + drop-on-source flush**) is Vitest-covered in `use-server-reorder.test.ts`; the API+SSE slice is the `server-reorder.spec.ts` e2e (a live-drag e2e is infeasible under the single-infra-server e2e harness `RK_SERVER_ALLOWLIST=rk-test-e2e`, so it is descoped to Vitest, mirroring the `fixme`'d session-drag e2e). **The native snap-back ghost animation is browser-chrome-level and NOT observable via Playwright synthetic DnD** — the drop-acceptance (`preventDefault`/`dropEffect`) is the only testable seam, so it has unit coverage only, no e2e. (`260705-i41e`)

**Board-list drag reorder**: the BOARDS list itself is drag-reorderable on its two user-named surfaces — the Sidebar `BoardsSection` rows (`sidebar/boards-section.tsx`) and the Host-page BOARDS zone tiles (`host-overview-page.tsx`) — sharing ONE lifted hook `useBoardListReorder(boards, onError)` (`app/frontend/src/hooks/use-board-list-reorder.ts`). It is a near-verbatim mirror of § Server-tile drag reorder (`useServerReorder`), with the same derive-over-store optimistic-override discipline, and is DISTINCT from the within-board PIN reorder (`/api/boards/{name}/reorder`, § Boards View) — this reorders the boards, not the panes inside one. (`260708-a2qd`)

- **Effective-order input + return shape**: the hook takes the already-backend-sorted board list (the `GET /api/boards` response order via `useBoards`, which is itself the display order — see `architecture.md` `/api/boards`) and returns `{ orderedBoards, getTileProps(name), isDragging, draggingName }`. Consumers render `orderedBoards` (the transient optimistic order during a drag, else the authoritative list) and spread `getTileProps(b.name)` onto each row/tile.
- **Custom MIME**: `application/x-board-list-reorder` (`BOARD_LIST_REORDER_MIME`) — distinct from `application/x-server-reorder`, `application/x-session-reorder`, the window-drag marker `application/x-window-drag` (`WINDOW_DRAG_MIME` — the sidebar BoardsSection rows claim that MIME for drag-to-pin BEFORE the reorder fall-through, § Sidebar Boards Section), and the board-pane window-move payload, so a foreign-MIME drag over a board row/tile is ignored (MIMEs never cross-fire).
- **No infra exclusion**: unlike `useServerReorder`, EVERY board is draggable and a valid drop target — `getTileProps` returns the full handler set for every name (there is no board analog of the infra-server class).
- **Derive-over-store optimistic override**: the transient dragged order of board NAMES lives in `overrideRef = useRef<string[] | null>` (NOT React state); a minimal `forceRender` (`useReducer((x)=>x+1,0)`) drives the optimistic repaint. `onDragStart` seeds the override with the current order; `onDragOver` splices the dragged name to the target position (insert-before). The override clears via a **render-time name-equality reconcile** (`namesEqual`): once the authoritative `boards` order element-wise equals the stored override the round-trip has landed and the ref is dropped (mutating a ref during render is safe — output unchanged, no nudge). **No snap-back on drag-end** — the override outlives the debounced POST until the SSE `board-order` echo re-fetches `boards` to match (see propagation below), which triggers the reconcile.
- **`onDragOver` accepts the drop on the self-target tile BEFORE the self-target bail** — the same snap-back guard-order as the server hook: originating-instance (`!dragNameRef.current`) → MIME (`BOARD_LIST_REORDER_MIME`) BEFORE acceptance, then `preventDefault()` + `dropEffect = "move"`, then the self-target no-op (`if (dragName === targetName) return;`) AFTER acceptance. Bailing before `preventDefault()` on the release-over-self path would leave the drop uncancelled → native cancelled-drag snap-back ghost + a starved `onDrop` flush. Do NOT re-fold the self-target check into the leading guard. (`260705-i41e`)
- **Debounce + drop-flush**: reorder writes go through a `BOARD_ORDER_DEBOUNCE_MS = 250` trailing debounce (`putTimerRef`), coalescing a rapid drag-over sweep into one `setBoardOrder(order)` POST (mirroring `SERVER_ORDER_DEBOUNCE_MS`); `onDrop` flushes any pending POST immediately so the write isn't lost if the SSE echo races the timer. A mount-scoped unmount cleanup effect (`useEffect(() => () => clearTimeout(putTimerRef.current), [])`) clears a pending timer so a drag ending right before the list unmounts fires no stray post-unmount write.
- **Drag-source feedback + error toast**: both surfaces dim the dragged row/tile via `opacity-50` (`isDragging && draggingName === b.name`), and both pass `useToast().addToast` as the hook's `onError` handler so a failed reorder POST surfaces a toast — matching the server-panel / Host-page precedent.
- **Propagation (server-global re-sort)**: `api/boards.ts` has `setBoardOrder(order)` (POST `/api/boards/order`); `session-context.tsx` exposes a `subscribeBoardOrder` seam on BOTH the per-server pool streams AND the dedicated `?metrics=1` stream (so a zero-attached-server Host page still hears reorders, since the event is host-global); `useBoards` subscribes and reuses its `scheduleRefetch`/`REFETCH_DEBOUNCE_MS` machinery to re-fetch `/api/boards` on a `board-order` event — the backend-sorted response is the single display-order source, so a reorder on one client re-sorts every other client live (NO client-side order reducer). The top-bar BoardSwitcher dropdown and every other `useBoards` consumer inherit the order passively.
- **Non-drag / keyboard path**: the palette `Board: Move up/down` (see § Command Palette Move up/down) is the keyboard + touch path (HTML5 DnD doesn't fire on touch). Coverage: the hook logic is Vitest-covered in `use-board-list-reorder.test.ts` (override lifecycle, render-time reconcile, debounce/flush, self-target accept, foreign-MIME ignore) plus BoardsSection + Host-page wiring tests; the API + rank-aware-sort + server-global-SSE slice is the `board-list-reorder.spec.ts` e2e (endpoint/SSE-surface form, mirroring `server-reorder.spec.ts` — the live synthetic drag is Vitest's; see § E2E host-global filesystem state for its `~/.rk/settings.yaml` save/restore).

**Server selector footer** — pinned at the bottom of the sidebar below the scrollable session tree, separated by `border-t border-border`. Displays `Server: {name}` with a dropdown trigger. Clicking opens a dropdown listing all available tmux servers (from `GET /api/servers`); the current server is highlighted with `text-accent`. Selecting a different server calls `setServer(name)`, which updates localStorage (`runkit-server`), reconnects SSE, and navigates to `/`. The session tree area is `flex-1 min-h-0 overflow-y-auto` above the pinned footer.

### Keyboard Navigation & Tree ARIA

The session/window tree is a **W3C-APG two-level disclosure tree** with roving-tabindex arrow navigation, scoped entirely to the Sessions tree DOM (never `document`, never the terminal — no xterm.js key conflict). Builds on the Wave-2 memo tree (§ Render Performance) and preserves its SSE-tick invariant. Frontend-only — no backend/API/route/tmux changes.

**Tree ARIA roles** (two levels — decided; the server group stays structural, NOT a treeitem):
- The scrollable Sessions region (`<div role="tree" aria-label="Session tree">`, `index.tsx:1070`) carries `role="tree"`; the outer `<nav aria-label="Sessions">` stays the separate landmark, so the landmark and the tree widget don't collide on one element.
- **Session rows** (`SessionRow`, `session-row.tsx:113-128`) are `role="treeitem"` `aria-level={1}` with `aria-expanded={!isCollapsed}` (lifted onto the treeitem from the chevron), `aria-setsize`/`aria-posinset` (position among `orderedSessions`), and `data-session-row="${server}:${name}"` as the stable focus handle. `aria-controls={windowGroupId}` is emitted **ONLY while expanded** — the `role="group"` window list is mounted only when `!isCollapsed`, so a collapsed row pointing `aria-controls` at an unmounted id would be invalid ARIA (SF-5, enforced via `aria-controls={isCollapsed ? undefined : windowGroupId}`).
- **Window rows** (`WindowRow`, `window-row.tsx:204-217`) are `role="treeitem"` `aria-level={2}` LEAF nodes (no `aria-expanded`) with `aria-setsize`/`aria-posinset` (position among `session.windows`), reusing their existing `data-window-id` handle.
- The **window-list container** (`<div role="group" id={windowGroupId}>`, `index.tsx:1597`) is `role="group"`; that id — `windowGroupId` = `windows-${server}-${session.name}` (`index.tsx:1549`) — is threaded down for the parent session's `aria-controls`. The wrapper carries no left margin — window rows are full-bleed, and the 12px hierarchy indent lives inside each row's left padding (§ Left-Edge Label Zone). Server-group `<section>` headers stay structural wrappers — server collapse/expand remains mouse/Tab-button only for v1.

**Roving-tabindex arrow nav**: exactly one tree row carries `tabIndex={0}` (the roving row), the rest `-1` (`tabIndex={rovingKey === key ? 0 : -1}`, threaded as a single primitive into the memo'd rows so an arrow press flips `tabIndex` on only the two affected rows — the Wave-2 memo skip is preserved). The keydown handler is on the `role="tree"` container (`handleTreeKeyDown`) and early-returns when the target is an `<input>`/`<textarea>`/`isContentEditable` (so a row's rename input keeps Enter=commit / Escape=cancel and arrows move the caret). Bindings (APG-standard, each `preventDefault()`s):
- **ArrowDown/ArrowUp** — next/prev visible row; **stop at the ends (no wrap)**; flow **continuously across all open server groups** as one flat list.
- **ArrowRight** — collapsed session → expand (focus stays, via `toggleSession`); expanded session → first window child; window → no-op (leaf).
- **ArrowLeft** — expanded session → collapse; window → parent session row (scans upward for the nearest `aria-level="1"`); collapsed session → no-op (no parent treeitem — the server header is a wrapper).
- **Enter AND Space** — activate. Derives the target from a typed `RowIdentity` discriminated union (`identityForKey`) and calls `onSelectWindow(server, session, windowId)` / `onSelectWindow(server, session, firstWindowId)` **DIRECTLY** — NOT a synthesized `.click()` or a magic-string `aria-label` match (SF-2). Ghost/optimistic rows (`ghost` flag or empty `windowId`) are a no-op (SF-3), mirroring the drag-path `isGhostWindow` guard; an empty session (no first window) is likewise a no-op.
- **Home/End** — first/last visible row.

**THE LOAD-BEARING INVARIANT (most regression-prone)**: the roving cursor is a `rovingKey` STRING (the row's `data-window-id` / `data-session-row`), NOT a numeric index — so it survives expand/collapse and SSE add/remove without ever pointing at a stale or wrong row, and the "session, then descend into its windows, across groups" order falls out of DOM order for free (`getVisibleRows` reads `[role="treeitem"]` straight from the tree at keypress time). The key-normalization effect (resets `rovingKey` to the first visible row when it matches no rendered treeitem) is gated on `[rovingKey, rowsVersion]`, where `rowsVersion` (a `useReducer` counter) is bumped by `registerGroupRows` **ONLY when a server group's visible-row-SET signature changes** (the `|`-joined ordered row-key string — window add/remove, collapse/expand, rename), NOT on the several-per-second passive SSE activity ticks. This preserves the Wave-2 #262 invariant: **an SSE tick must NOT change roving state or steal focus.** A tick recomputes `rowSlice`/`rowSignature` but the signature STRING is unchanged → no bump → the normalize effect doesn't run (no full-tree `querySelectorAll`, no `setRovingKey`). The only `row.focus()` is gated by `focusMovedRef`, set `true` only on an actual keypress-driven move, so a passive re-render never pulls focus into the sidebar. DOM focus + `scrollIntoView({ block: "nearest" })` on a roving change reuse the CommandPalette/ThemeSelector "Keyboard-Navigable List Scroll Pattern" (§ Keyboard-Navigable List Scroll Pattern). Roving focus is INDEPENDENT of the URL-based `isSelected`/`aria-current="page"` selection (§ URL as Resumable Bookmark) — do not conflate them. The mobile-drawer focus effect (§ Window rows → Current-row focus on mobile drawer open) likewise `setRovingKey`s the row it focuses so its DOM focus and the tab-stop stay in sync (SF-4).

**APG caveats**: the inner per-row buttons (chevron, name, +, ✕, pin, swatch, kill) remain natively `Tab`-focusable rather than being subsumed under the single tree tab stop — a deliberate keyboard-first choice (every control stays reachable) over strict APG purity. The window row's status dot is NOT among them — it carries no `tabIndex`; focusing the row itself opens the flyout card, whose links are Tab-reachable from there (§ Row-hover register flyout card). `role="tree"` wraps the per-server `<section role="presentation">` wrappers — semantically transparent, so the accessibility tree reads tree → treeitem(level 1) → group → treeitem(level 2) with no interposed landmark. The wrappers carry no `aria-labelledby` (a global ARIA property would void the presentation role per WAI-ARIA presentational-conflict-resolution), and the header toggle button's `server-header-*` id is dropped as dead code — its only consumer was that attribute (exsp). The implementation reuses the scroll half of the named list-scroll pattern but deliberately diverges from its `selectedIndex` numeric-state model in favor of the `rovingKey` string + DOM enumeration (see § Keyboard-Navigable List Scroll Pattern's third-consumer note).

### Window-Row Multi-Select (APG multiselectable tree)

The tree is `aria-multiselectable="true"` and carries a **window-rows-only** bulk selection layered onto the roving-tabindex model above. Selection is orthogonal to both the roving cursor and the URL-based `aria-current="page"` selection — three independent notions of "current row" coexist on the same tree. Session and server rows are never selectable: each window is a worktree/change and PR status is per-window (§ PR Status). Its reason for existing is the cleanup chore — windows whose PRs merged accumulate in working sessions — served end-to-end by the two-command flow **`Selection: Select all merged` → `Selection: Close N windows`** (confirmed in the palette; moving the set to a parking session is the non-destructive variant of the same flow). **Three bulk actions** act on the set — move, close, and prompt broadcast — and all three are frontend-only: N sequential calls over per-window endpoints that already shipped for the single-window paths (`moveWindowToSession`, `killWindow`, `sendChatMessage`), no backend surface, no new endpoint, no new client fn. (`260807-nf9f`, `260808-ebgs`)

**Selection state — a dedicated zustand store** (`store/selection-store.ts`, following the `window-store.ts` shape): `selected: ReadonlySet<string>` keyed by the composite `${server}:${windowId}` handle (the same key `entryKey()` and the sidebar's roving `data-row-key` use — tmux `@N` ids are unique per server only), plus a nullable `anchor` for shift-range extension. Actions: `toggle` (flip one key, move the anchor onto it), `select` (add, anchor untouched), `selectOnly` (wholesale replace), `settleBatch(batchKeys, retainKeys)` (§ below), `clear`, and `prune(liveKeys)`. There is deliberately no standalone `setAnchor` (nor a `deselect`): every action that should move the anchor moves it as part of its own write, so no consumer ever needed one. A store rather than sidebar-local state because the two consumers are in different subtrees — the tree that paints and mutates the selection is `sidebar/index.tsx`, the palette that ACTS on it is composed in `app.tsx` (§ Design Decisions → Selection state lives in a dedicated store).

**Pure logic** lives dependency-free in `lib/selection.ts` (the `lib/palette-move.ts` discipline — unit-testable without mounting the tree): `selectionKey(server, windowId)`, `splitSelectionKey(key)` (splits at the FIRST `:`, so a windowId containing `:` round-trips), `rangeBetween(orderedKeys, anchorKey, targetKey)` (inclusive, direction-independent, `[]` when either endpoint is absent — the caller degrades to a plain toggle), `singleSelectedServer(selectedKeys)` (`null` when empty or cross-server — the bulk-move gate), and `pruneSelection(selected, liveKeys)` (returns the SAME set instance when nothing changed, so a caller no-ops without a state write).

**Gestures** (window rows only; ghost/optimistic rows — `ghost` flag or empty `windowId` — are never selectable, mirroring the SF-3 activation guard):
- **Cmd/Ctrl-click** toggles the row and does NOT navigate. **Shift-click** extends the inclusive range from the anchor in VISIBLE-row order (`getVisibleWindowKeys()`, the same `[role="treeitem"]` DOM walk roving nav uses) without navigating; with no anchor — or a stale one whose row has left the tree — it degrades to a plain toggle. The anchor stays put across a shift-click so a second one re-extends from the same fixed end. **Plain click** is unchanged: it navigates, and additionally clears a live selection. The seam is one stable identity-arg `onRowClick(server, session, windowId, {meta, ctrl, shift})` prop on `WindowRow` returning `true` when it CONSUMED the click (the row then `preventDefault()`s instead of navigating) — one reference for every row, so the memo contract holds (§ Render Performance).
- **`x` on the focused row** toggles its selection (`preventDefault()`), a case in the bubble-phase `handleTreeKeyDown`. Chorded `x` (⌘X/Ctrl+X/Alt+X — cut & friends) is explicitly passed through, session rows are a no-op, and the handler's existing `<input>`/`<textarea>`/`contentEditable` early-return keeps `x` typed into a rename input from being hijacked. `x` rather than Space, which is already bound to activation (§ Design Decisions → Keyboard selection-toggle key is `x`).
- **Escape clears** a non-empty selection — a **separate `onKeyDownCapture` handler** on the `role="tree"` container, not a case in the bubble-phase handler (§ Design Decisions → Escape-to-clear is a CAPTURE-phase handler). It consumes the event only when there is a selection AND the target is not editable, so the rename input's Escape-cancels and the flyout card's own Escape-dismiss both keep working.

**Row treatment**: a selected window row's `role="treeitem"` carries `aria-selected="true"` plus a **2px inset accent ring** (`boxShadow: inset 0 0 0 2px var(--color-accent)` — the session-row drop-target idiom), which reads as "member of a set" without competing with the URL selection's tint-depth + bold treatment and costs no layout (rows never shift when selected). The transient `isDragOver` indicator WINS over it while a drag is in flight. `aria-selected` is emitted ONLY when the row is wired for multi-select (`onRowClick` present) — the board-route and unit-test mounts otherwise announce a selection model they don't have.

**Selection-count indicator** (`SelectionIndicator`, file-private in `sidebar/index.tsx`): a non-interactive `role="status"` strip directly under the scrollable tree reading `N selected · ⇧click extends · x to toggle · <palette chord> → Selection: · Esc to clear` (`data-testid="selection-indicator"`). The `⇧click extends` clause renders on fine pointers only — keyboard-chord hints never render on coarse pointers (§ Education micro-copy). The chord is **derived, never literal** — the HOST-effective `command-palette` binding via `useKeybindings()` + `formatCombo` (the same derivation the top-bar Settings gear uses), so it tracks platform (⌘K vs Ctrl+K) and user rebinds; the whole `→ Selection:` clause is dropped when that binding is unbound/disabled or the mount has no `Selection:` actions (§ Selection palette commands). No buttons, no action strip — the palette is the sole action surface (Constitution IV + V). It renders nothing while the selection is empty, so it costs no vertical space at rest, and its hint text is half the `x` gesture's discovery surface (the other half is the palette entry's `shortcut` badge, § Selection palette commands).

**Prune — liveness is DATA-derived, and collapse is not departure.** Each `ServerGroup` registers TWO independent things with the sidebar: its VISIBLE-row identity slice + signature (`registerGroupRows` → `rowsVersion`, gating roving-key normalization) and its DATA window-key set + signature (`registerGroupDataKeys` → `dataKeysVersion`, gating the selection prune). The data set is composed from `orderedSessions` **ignoring `collapsed`/`isOpen`** (real windows only, ghosts excluded), so it holds every window the SSE snapshot knows for that server; its signature is a **sorted** `|`-joined key string, so a pure session/window reorder — which moves nothing in or out of the snapshot — does not bump the counter. The prune effect is gated on `dataKeysVersion` alone and prunes against `getLiveWindowKeys()` (the union over every rendered group's data keys), dropping a stale anchor with its key. **Collapsing or expanding a session changes no data key, so it bumps nothing and touches neither selection nor anchor** — and both signatures being strings over key sets means neither counter bumps on the several-per-second passive SSE activity ticks, preserving the load-bearing "an SSE tick must not change tree state" invariant on both paths. `unregisterGroupRows(server)` is the unmount counterpart to BOTH registrations (drops both slices, bumps both counters), wired from a `ServerGroup` cleanup effect keyed on `[server]` alone — a cleanup on either registration effect would fire on every signature change and double-bump. It covers the case no surviving group's signature does: a whole group leaving the tree (the sessions-scope ALL→CURRENT switch, a server dropping out of the SSE snapshot).

**Bulk execution — three executors over ONE sequential batch helper.** `executeSelectionBatch(keys, operation)` (`lib/palette-selection.ts`) is the shared control flow: it walks the composite keys **in order**, resolves each through `splitSelectionKey` (a malformed key fails locally as that batch item with the first error `malformed window key`, never a thrown exception), awaits `operation({server, windowId})` one at a time, and continues past a rejection, returning `{failedKeys, firstError}`. Its sibling `batchToast(copy, total, result)` composes the ONE aggregate report every batch emits — `<verb> N <noun><qualifier>` on full success, `<verb> X of N <noun><qualifier> — F failed: <first error>` on partial or total failure — so only the operation-specific verb/noun/qualifier copy lives at the call sites. The three `app.tsx` executors are thin over that pair and each ends with `settleBatch(keys, failedKeys)`:

- **`executeBulkMove(srv, keys, targetSession)`** — `moveWindowToSession(srv, windowId, targetSession)` per key. Move is **single-server by gate** (`singleSelectedServer` omits its entries otherwise), so it deliberately IGNORES each key's own server and moves every window on `srv`; only the control flow is shared. Toast `Moved N windows to <session>`.
- **`executeBulkClose(keys)`** — `killWindow(server, windowId)` per key, each against the key's OWN server. Toast `Closed N windows`.
- **`executeBulkSend(keys, text)`** — `sendChatMessage(server, windowId, text)` per key on the key's own server, at the endpoint's default `submit:true` ([chat](/run-kit/chat.md) § Send Path). A per-window structured `409` (novelty-probe failure — text pasted, Enter withheld) is that recipient's failure and does not stop the later ones. Toast `Sent prompt to N agents` / `Sent to X of N agents — F failed: <first error>`. It alone also **resolves the delivered count** (`keys.length - failedKeys.length`), which the compose strip consumes for draft retention (§ Design Decisions → A broadcast clears its draft on DELIVERY).

No bulk optimistic/ghost machinery on any of the three — rows repaint from the SSE stream; the single-window drag-and-drop optimistic `executeMoveToSession` path is untouched (§ Cross-Session Move: Compound Optimistic Update). The terminal `settleBatch(keys, failedKeys)` **subtracts only the keys this batch owned and gave up** from whatever the store now holds, dropping the anchor only when it pointed at one of those (§ Design Decisions → An async batch settles by RECONCILING). In the uncontended case that leaves exactly the failed windows selected as the retry affordance; in the contended one — the palette closes before the batch runs, so a slow batch races the user — a selection the user starts mid-batch survives.

**Tests**: `lib/selection.test.ts` + `store/selection-store.test.ts` + `lib/palette-selection.test.ts` (colocated Vitest units — builder eligibility/counts plus the batch helper's sequencing, malformed-key handling, first-error retention, and toast composition), `sidebar/index.test.tsx` (group-unmount pruning, collapse/expand survival), `command-palette.test.tsx` (the confirm sub-step's Enter/Escape paths), `compose-strip.test.tsx` (the selection-broadcast target, its Enter policy, and both delivery outcomes), and the e2e `tests/e2e/sidebar-multiselect.spec.ts` + its sibling `.spec.md` (cmd/shift-click, `x`, Escape, the count indicator, collapse survival, the real two-POST bulk move, a confirmed bulk close that removes both windows from tmux — with an Escape-cancel that issues no kill — and a broadcast whose frozen target survives a live-selection change and records two ordered chat-send POSTs at `maxInFlight === 1`), all against the isolated e2e tmux server.

### Collapsible Panels (Bottom-Aligned)

Two collapsible panels are pinned at the bottom of the sidebar below the scrollable session tree. Layout order top-to-bottom: SERVER panel -> Boards section -> per-server session groups (`flex-1 overflow-y-auto`) -> Window (PANE) panel -> Host panel -> the **footer status row** (§ Sidebar footer status row, the sidebar's last child). Combined panel height target ~140px when both are open.

**CollapsiblePanel** (`app/frontend/src/components/sidebar/collapsible-panel.tsx`) — reusable wrapper used by both Window and Host panels. Props: `title` (string), `storageKey` (string for localStorage persistence), `defaultOpen` (boolean, default `true`), `children` (ReactNode), `headerRight` (ReactNode rendered inside the toggle button, right-aligned — the StatusDot+name slot; clicking it toggles the panel), `headerAction` (ReactNode rendered OUTSIDE the toggle button at the header's right edge, whose clicks are **stopped from toggling the panel** — the purpose-built seam for an interactive control like the PANE-header refresh button, see WindowPanel below), and `titleAction` (ReactNode rendered OUTSIDE the toggle button immediately AFTER the title — when set, the header splits into `[toggle: chevron+title][titleAction][toggle click-region: headerRight]` so the trailing region keeps mouse click-to-toggle; the HOST panel's instance-color picker rides this seam, see § Instance Accent). Header is always visible: the title (`TypedLabel` — the section-label typed-sweep hover treatment, `260703-5ilm`, see § Hover-Animation Vocabulary) + chevron (`&#x25B8;` U+25B8) that rotates 90 degrees on toggle via CSS `transform: rotate()` with `transition-transform duration-150`. Content area uses `max-height` transition (`duration-150 ease-in-out`) for smooth expand/collapse. `overflow: hidden` during transition, `visible` when fully expanded (accessibility). Collapse state persisted to `localStorage[storageKey]` on every toggle. Each panel has `border-t-[3px] border-border first:border-t-0` (a 3px structural seam — see § Border-Width System — so panels partition cleanly; the resizable drag handle is transparent by default, letting the next panel's 3px top border be the sole visible inter-panel line).

**WindowPanel** (`app/frontend/src/components/sidebar/status-panel.tsx`) — collapsible panel with `title="Pane"`, `storageKey="runkit-panel-window"`, `defaultOpen={true}`. **PANE-header refresh button** (`PaneRefreshButton`, with an `idle | spinning | check` feedback state machine): passed as `CollapsiblePanel`'s `headerAction` (NOT `headerRight` — `headerAction` is the seam whose clicks are stopped from toggling the panel; `headerRight` is the StatusDot+name slot inside the toggle button). It kicks a server-side on-demand refresh of BOTH PR pollers via `refreshStatus()` → `POST /api/status/refresh` (see architecture.md § API Endpoints). **Feedback state machine**: a single `useState<"idle" | "spinning" | "check">` (discriminated over the three mutually-exclusive visuals; consumed by tests via a `data-state` attribute). The button **spins from CLICK until the server-global `status-refresh` SSE completion event, NOT until the POST settles** (the POST 202s in ~ms while the real `gh` work runs 1–10s detached). It branches on the tri-state 202 body: `started`/`coalesced` → `spinning` (no distinct coalesced visual — the in-flight pass's one completion event clears both); `throttled` → skip spinning and `flashCheck()` immediately ("already fresh"; no event will come). Completion is delivered via `useSessionContext().subscribeStatusRefresh` (subscribed ONLY while `spinning`, so an unrelated tab's completion doesn't flash an idle button); on fire it transitions `spinning → check` (a brief post-completion checkmark, `REFRESH_CHECK_MS = 1000`, auto-reverting to `idle`, mirroring the same-file `COPY_FEEDBACK_MS` cadence). A **15s UI fallback** (`REFRESH_FALLBACK_MS = 15000`, well under the 60s `statusRefreshTimeout` backend bound) clears a stuck spinner if the event is missed — **armed at click ENTRY, not inside `.then`** (a started/coalesced completion event can beat the POST settle, so arming in `.then` would restart a timer `flashCheck()` already cleared → a phantom checkmark ~15s later). A `refreshStatus()` rejection (non-2xx via the shared `throwOnError`) is swallowed back to `idle` (clearing the fallback) so a network failure never leaves a stuck spinner. All timers are cleared on every transition and on unmount. Renders `animate-spin` on the `lucide` rotate-cw SVG in `spinning`, a checkmark SVG (`data-testid="pane-refresh-check"`, `text-accent`) in `check`, the static rotate-cw icon in `idle`; 14×14 SVGs, 24/30px touch target (`coarse:`), `data-testid="pane-refresh"`, `data-state`, `aria-label`/`title="Refresh PR status"`. Follows the top-bar/board `RefreshButton` **CRT-glint** vocabulary (`rk-glint` — § Hover-Animation Vocabulary). It **renders whenever the PANE header renders, including with no window selected** — the refresh is server-global (both pollers are viewer-wide), so gating on window selection would add nothing. Best-effort/fire-and-forget: server-side coalescing + a min-interval throttle make it safe to over-fire (the fallback still clears any spinner). Scope-honest: labeled around PR/status freshness (the other PANE registers out/agt/fab are already fresh within ~7.5s). The `subscribeStatusRefresh` context seam it consumes is a `Set`-of-handlers ref + `fireStatusRefresh` fired from BOTH the per-server pool ES block AND the `?metrics=1` metrics-stream ES block in `session-context.tsx`, mirroring `subscribeBoardOrder`/`fireBoardOrder` (the event is host-global, so both streams carry it). Palette parity: the `PR: Refresh Status` action (§ Command Palette `PR: Refresh Status` action) is unchanged — a pure POST kick with no shared button state to hook. The `headerRight` slot renders `<StatusDot win={win} /> {win.name}` (in a `flex items-center gap-1.5 truncate text-text-secondary font-mono` span) — the unified status dot (see § Status Dot) leads the header so the window's status (compositional two-family ladder + waiting halo) stays glanceable even when the panel is collapsed; the dot carries no hover-card of its own here (the panel body is the detail view). Displays the identity rows `tmx` (pane index + ID), `cwd` (shortened path), `git` (branch), then the **four orthogonal signal registers** `out` (L0 activity + elapsed) / `agt` (L1 `agentState` + duration) / `fab` (L2 change · stage · displayState) / `PR` (L3 derived PR, ungated) — the 3-char register view (§ Pane panel four-register view). The L1 `agt` register (`getAgentLine`) is `null` when `!win.agentState`, else `${agentState} ${agentIdleDuration}`; a blocked pane shows `waiting <dur>` (the pierce rule: it shows independently of the flowing-output L0 register). No window selected -> "No window selected" in secondary text. `StatusPanel` is exported as a deprecated alias. (`260715-jykd`, `260715-nwla`, `260616-37ub`, `260706-y1ar`, `260706-4h26`)

**Board-route PANE fallback — follows the focused tile.** The window `WindowPanel` renders is derived in `BottomPanels` (`sidebar/index.tsx`), not passed by the route: the route-selected window (`currentServer`/`currentSessionName`/`currentWindowId`) wins when present; otherwise `BottomPanels` consumes `useFocusedPane()` (§ Focused-pane context) and resolves the board's focused tile. On `/board/$name` the route has no `server` param so `currentServer` is `null` and no route window exists — the focused-tile fallback is the only source. Resolution (pure helpers in `app/frontend/src/lib/focused-pane-window.ts`): `resolveFocusedWindow(sessions, windowId)` scans `ctx.sessionsByServer.get(focusedPane.server)` for a window whose `windowId` matches — a **hit** yields the fully-enriched home-session copy (out/agt/fab/PR registers all live), which works because board pins are LINK-based with dual home+pin membership so the pinned window's home-session copy flows through the sessions SSE stream (§ tmux-sessions link-based board pin-sessions). A **miss** is a pin-only window (home session died while the window stayed pinned — absent from the stream); `thinWindowFromFocusedPane(focused)` then synthesizes a thin `WindowInfo` from the published board-entry data (`windowName` + `panes`: paneId/paneIndex/cwd/command/isActive/gitBranch, active pane = `isActive` else first) with `activityTimestamp: 0` (`getOutputLine` guards on a truthy timestamp, so `out` shows the pane command with no fabricated idle duration) — the tmx/cwd/git rows and `out` register render, the enrichment-only agt/fab/PR registers are honestly absent. Both branches render through the unchanged pure `WindowPanel` (a `WindowInfo | null` component — no board types or fallback markup added to `status-panel.tsx`). An empty board publishes `null`, so the panel keeps its "No window selected" state. (`260720-zx4i`)

**Copyable rows**: The `tmx`, `cwd`, `git`, and `fab` rows are interactive `<button type="button">` elements that copy their underlying value to the clipboard on click or keyboard activation (Enter/Space). Copy values per row:

| Row | Copy value | Source |
|-----|------------|--------|
| `tmx` | Pane ID (e.g., `%5`) | `activePane.paneId` |
| `cwd` | Full unshortened path (e.g., `/home/sahil/code/run-kit`) | `activePane.cwd ?? win.worktreePath` |
| `git` | Branch name | `activePane.gitBranch` |
| `fab` | Change ID (e.g., `lc2q`) | `fabChange.id` (parsed from `win.fabChange`) |

Non-interactive rows: `run` (process-only, when no fab state) and `agt` remain plain text — no hover affordance, no focus ring, no copy behavior. Rows with empty values (`tmx` with empty pane ID, `git` when no branch) are also non-interactive.

**`cwd` deleted-directory marker**: when the active pane's `cwdMissing` flag is set (the backend `os.Stat` found the pane's cwd no longer exists — typically an archived change whose worktree was deleted out from under a still-live tmux window), the `cwd` row keeps the shortened stale path as a breadcrumb but recolors the whole value `text-signal-red` and appends a `(deleted)` tag (`data-testid="cwd-deleted"`); the row `title` reads `<full-path> (no longer exists)`. It uses **color over dimming** — a vanished cwd is more notable, not less, so it reuses the panel's failure-red token (the same `text-signal-red` the `pr` fail-ish row uses) rather than the dimmed `text-text-secondary` of a normal path. The row stays copyable (still copies the stale full path). The marker is **self-healing**: it only shows while tmux still reports the deleted path; once the shell's cwd recovers to a real directory the backend stat succeeds, `cwdMissing` clears, and the row reverts to its normal style. This is the minimal honest signal — run-kit does NOT close or rename the orphaned tmux window (Constitution §VI forbids tearing down tmux windows). Backend derivation: `architecture.md` § `internal/sessions` (`resolveCwdMissing`). (`260614-nj74`)

**Register icons** (the pane panel is the four-register view — § Pane panel four-register view; these inline spinner glyphs lead their register line, `aria-hidden`): `BrailleSnake` (`braille-snake.tsx`, `⣾⣽⣻⢿⡿⣟⣯⣷`, 80ms) → the `out` (L0) register; `StarTwinkle` (`star-twinkle.tsx`) → the `agt` (L1) register; `ClockSpinner` (`clock-spinner.tsx`) → the `fab` (L2) register; the L3 `PR` register uses the Nerd Font git-pull-request glyph (`ICON_CLASS`). Each follows the same pattern: `useState(0)` frame counter + `useEffect` with `setInterval` + cleanup on unmount, rendered as `<span aria-hidden="true">`. (`260706-y1ar`)

**Inline feedback**: After a successful copy, the row's prefix label swaps to `copied ✓` for 1000ms, then reverts. A single `copiedRow` state variable tracks which row was last copied — only one row shows feedback at a time. Clicking a different row immediately moves the indicator.

**Hover affordance**: Interactive rows render `cursor: pointer` and a subtle background tint (`bg-bg-inset` or equivalent) on hover.

**Keyboard accessibility**: Button elements have visible focus state (outline/ring) and are keyboard-activatable (Enter/Space). Styling is reset to preserve the panel's compact plain-text aesthetic — no default button chrome (padding, border, background removed in rest state).

**Text-selection guard**: The click handler checks `window.getSelection()?.toString()` — if the user has an active text selection (e.g., from drag-selecting text), the copy action is suppressed, preserving native text-selection UX.

**Clipboard utility**: Copy operations use `copyToClipboard()` from `app/frontend/src/lib/clipboard.ts` — see [Clipboard Utility](#clipboard-utility).

**HostPanel** (`app/frontend/src/components/sidebar/host-panel.tsx`) — the sidebar host-metrics view inside a `CollapsiblePanel` with `title="Host"`, `storageKey="runkit-panel-host"`, `defaultOpen={true}`. It takes no props: it owns only the chrome — the hostname header (truncated, on the left, accent-tinted per § HOST panel) plus the instance-color `titleAction` picker — and the `!metrics` "No metrics" branch; the metric ROWS are delegated to the shared `HostMetrics` component. **It carries no connection dot** — the one dot lives in the sidebar footer (§ Sidebar footer status row), so the panel never duplicates it.

**Metrics source — server-scoped with host-global fallback.** `HostPanel` reads `metrics = useMetrics() ?? useHostMetrics()`: the current-server-scoped `useMetrics()` wins when present, falling back to the host-global metrics broadcast (`useHostMetrics()`, available on EVERY route — the same source the `/` HOST HEALTH zone consumes). On `/board/$name` there is no `currentServer`, so `useMetrics()` is `null` by construction and the panel renders the host-global snapshot instead of "No metrics"; on a `/$server/...` route the two arrive on the same tick so the fallback is harmless. (`260720-zx4i`)

**Shared `HostMetrics` component** (`app/frontend/src/components/host-metrics.tsx`) — a single **layout-agnostic** presentational component `HostMetrics({ metrics }: { metrics: MetricsSnapshot })` renders the metric rows, so the two host views (sidebar `HostPanel` + the `/` HOST HEALTH zone) stay visually consistent (§ Design Decisions → HostMetrics is a shared component). It ALWAYS receives a non-null snapshot — the caller owns the surrounding layout chrome AND the `!metrics` empty state. It exports the `formatUptime`/`formatDisk` helpers and holds the `MemoryLine`/`LoadLine` sub-components. Rows (all `text-xs font-mono`), rendered via `lib/sparkline.ts` + `lib/gauge.ts`: (`260701-vshd`)

1. **CPU sparkline** — `cpu` label + braille sparkline (`text-accent`) + current percentage (`text-text-primary`). Sparkline rendered by `sparkline()` from `@/lib/sparkline.ts`
2. **Memory gauge** (`MemoryLine`) — `mem` label + filled/empty block gauge + `formatMemory` `used/total` text. Gauge color: green < 70%, yellow 70-90%, red > 90%. Uses `gaugeBar()`, `gaugeColor()`, `formatMemory()` from `@/lib/gauge.ts`. A `ResizeObserver` measures the container to size the gauge char-count responsively
3. **Disk + uptime** — `dsk` label + `formatDisk` `used/totalG` + ` · up ` + `formatUptime` (`Nd Nh` or `Nh Nm` if < 1 day). All `text-text-secondary`
4. **Load averages** (`LoadLine`) — `ld` label + three percentages (1/5/15 min) normalized as `(load / cpuCount) * 100`. Any percentage > 90% renders in `text-red-500`

See § Design Decisions → HostMetrics is a shared component.

### Sidebar footer status row (`SidebarFooter` in `sidebar/index.tsx`)

**The sidebar's last child is a passive status row** — readouts and hints only, no actions. The app-global ACTIONS live in the top bar: Settings as a persistent right-cluster gear chip, Help / Keyboard shortcuts / Theme… as `menuOnly` App-section rows in the chevron menu (§ Right cluster → Settings gear / → overflow chevron menu; § Design Decisions → App-global actions live in the top bar). The Sidebar renders on terminal, tmux Server, and board routes, so the row is present wherever there is a sidebar. (`260724-6j1v`, `260812-d1at`)

**Layout — `justify-between`** (`shrink-0 border-t border-border px-2 py-1 flex items-center justify-between`), two segments:

- **LEFT — passive readouts** (a status segment, `flex items-center gap-1.5 min-w-0`):
  - **Connection dot** — `role="status" aria-live="polite"` wrapper around a `block w-2 h-2 rounded-full` span, `bg-accent-green` connected / `bg-text-secondary` disconnected, `aria-label` `Connected`/`Disconnected`, a **non-focusable span** (a readout, not a control — no tab stop). Its `Tip` carries the computed `dotTitle`: `Disconnected` when `!isConnected`, else `Connected — run-kit {displayVersion(daemonVersion)}` when the version is known, else bare `Connected` (the version fragment is OMITTED until the first `version` event so the label is never `vundefined`). The **`aria-label` stays the concise `Connected`/`Disconnected`** — the version is hover-discovery detail, not screen-reader-essential state on the live region. Value semantics + per-caller derivation: § Chrome (Top Bar) → connection dot.
  - **Version readout** — the resting `v0.9.3` click-to-copy line; full mechanics in § Steady-state version surfaces → surface 6.
- **RIGHT — a status/hints slot**: a right-aligned, truncating, **non-interactive** span filling the remaining row width. Deliberately quiet content policy: when `useUpdateNotification()` reports a qualifying update it shows an accent-green hint — `v{latest} available` for a single run-kit update, `{N} updates available` for any other tool set — and otherwise renders NOTHING (no resting copy, Constitution IV). A readout, never a link/button: the overflow menu's fixed version row REMAINS the update surface with its ⟳ check affordance (§ Design Decisions → Footer version is a readout; the menu row stays the update surface).

**Single shared definitions, no drift.** `HELP_URL`, `NOTIFICATIONS_HELP_URL`, `HelpIcon`, and `ThemeModeIcon` all live in **`components/global-chrome.tsx`** — the chevron menu's App-section rows, the settings dialog's Notifications row, and both command palettes import from there, so no surface can drift from another (§ Design Decisions → Shared chrome definitions live in `global-chrome.tsx`).

**Data sources.** `isConnected` arrives as a required `SidebarProps` prop (§ Design Decisions → Connection state reaches the Sidebar as a prop); `daemonVersion` and the update-availability verdict (`qualifies` / `singleRunKit` / `latest` / `tools`) via `useUpdateNotification()`; the copy toast via `useToast()`. Tips use `placement="top"` because the row hugs the viewport bottom.

**Coverage.** Unit: `sidebar/index.test.tsx` (dot aria + both states, version copy + the null-version absence, the update hint rendering/omitting per `qualifies`, no action controls in the footer). E2e: `tests/e2e/sidebar-footer.spec.ts` (+ `.spec.md`) — footer dot left, clipboard equals the displayed version text, no action buttons. The four actions' coverage lives with their surfaces: `top-bar.test.tsx` + `tests/e2e/top-bar-overflow.spec.ts` (+ `.spec.md`) for the gear and the App-section rows, `tests/e2e/settings-dialog.spec.ts` for the gear trigger, and `tests/e2e/mobile-layout.spec.ts` for theme reachability through the chevron menu. (`260812-d1at`)

### Focused-pane context (`contexts/focused-pane-context.tsx`)

The publication channel that lets the sidebar PANE panel follow the board's focused tile (§ Collapsible Panels → Board-route PANE fallback). `FocusedPaneProvider` is mounted in `RootWrapper` (`app.tsx`) alongside `TopBarSlotProvider`, so the registered value survives navigation and is readable from both the AppShell and board sidebars. Shape mirrors `top-bar-slot-context`: a referentially-stable dispatcher (held in a `useRef`), a `useMemo`'d context value, `useFocusedPane()` (read hook; throws outside the provider) and `useRegisterFocusedPane(pane)` (register hook: an effect publishes the memoized value on mount/change and clears to `null` on unmount, last-writer-wins). The published `FocusedPane` value is `{ server, windowId, windowName, panes: BoardPaneInfo[] } | null`: the focused tile's identity PLUS the board entry's thin pane data (from the `getBoard` join), so the pin-only thin render (§ Collapsible Panels) has everything it needs at the consumer with no second data channel and no board-API knowledge in the sidebar. `BoardPage` registers `entries[focusedIndex]` (memoized so the effect re-runs only when the focused entry changes); an empty board registers `null`, and navigating away clears it. This is distinct from `FocusedTerminalContext` (which registers the focused xterm's imperative send/focus handle for the compose strip) — this context carries declarative pane identity/data, not a terminal handle — and from the top-bar slot's `focusedPane` field (the `{server, windowId, cwd}` split/kill target the top bar and palette act on), which stays a separate, thinner channel. (`260720-zx4i`)

### HOST HEALTH Zone on `/` (Host-page host-console home)

`HostOverviewPage` (`app/frontend/src/components/host-overview-page.tsx`, route `/`) renders a **HOST HEALTH** zone ABOVE the tmux-server tile grid, inside the scroll container (`div.flex-1.min-h-0.overflow-y-auto`, which carries `pt-6` so the gap below the pinned TopBar matches the `mb-6` inter-section rhythm). This makes `/` — the only route that is about the BOX, not a session — a machine console. The zones render in top-to-bottom order **HOST HEALTH → BOARDS → TMUX SERVERS → SERVICES** (each an uppercase-labelled `<section>`; the server-tile grid has a "Tmux Servers" heading + inline server count, SERVICES last after the tiles; see § BOARDS Zone, § SERVICES Zone on `/`). A page-level `Host Overview` `SectionHeading` heads the scroll container above these zones (§ Page Overview Headings). Each zone heading is a `[ LABEL▊ ]────` `SectionHeading` (§ Section Heading); the Host page's identity is the solo `Host` heading in the top-bar center cell. Read-only awareness view; nothing here mutates host state (Constitution IV). (`260701-vshd`, `260701-5d0z`, `260702-6m46`, `260704-pr0p`, `260715-zs1y`)

- The zone (`<section aria-label="Host health">`) shows a `<SectionHeading label="Host Health" className="mb-2" />` (§ Section Heading), then the live `hostname` (from `useHostMetrics()`) in the zone BODY as a `text-xs text-text-secondary font-mono truncate mb-2` line right below the heading (NOT in the SectionHeading `side` slot, which stays empty on Host-page zones), then the shared `HostMetrics` component (CPU sparkline+current, memory gauge, load 1/5/15 normalized, disk used/total, uptime). (`260702-6m46`, `260704-pr0p`)
- It consumes **`useHostMetrics()`** (the server-independent hook — see architecture.md § Server-independent host-metrics subscription), NOT `useMetrics()` (which is `null` on `/` — there is no `currentServer`). Metrics update on each `event: metrics` tick; on a bare `/` with zero servers attached, freshness is the fast ~2.5s (`legacyPollInterval`) cadence, because `safetyIntervalEffective` treats a sentinel-only slice as the exception and returns the fast interval rather than the 12s backstop (the on-connect cached snapshot renders immediately) — see architecture.md § API Layer `/api/sessions/stream`.
- **Empty state**: when `useHostMetrics()` is `null` (pre-first-tick, or a non-Linux zero-valued host), the zone renders a graceful `No metrics — waiting for the host's first report` line (never a blank/broken layout — the empty state names what fills it, § Education micro-copy), mirroring `HostPanel`'s `!metrics` branch.
- The server-tile grid below (create / open / count) and the `+ New Server` affordance function normally. **Infra-server name de-emphasis**: each tile's name `<div>` renders `text-text-secondary` (instead of `text-text-primary`) when `isInfraServer(name)` — the same greyed-not-disabled treatment as the Sidebar Server tile; the tile stays fully clickable/attachable. See § Infra-server de-emphasis. (`260705-uh8f`)

### BOARDS Zone on `/` (Host page)

`HostOverviewPage` renders a **BOARDS** zone (`<section aria-label="Boards">`) BETWEEN HOST HEALTH and TMUX SERVERS. A board aggregates windows across tmux servers, so the box-level Host page (not any single tmux Server page) is its list's natural home; it sits above TMUX SERVERS per the page's general→specific flow. (`260702-6m46`)

- **Data source**: the existing **`useBoards()`** hook (`plain /api/boards` fetch + the shared SSE pool — NO new API/endpoint; the same hook `board-page.tsx` and the sidebar consume). Returns `{ boards, isLoading }`.
- **Heading row**: a `<SectionHeading label="Boards" className="mb-2" />` (§ Section Heading). The board count / `loading…` is NOT in the SectionHeading `side` slot (reserved for the server-view stats) — it stays in the zone BODY, a `text-xs text-text-secondary font-mono mb-2` line right below the heading (`loading…` before the first fetch via `isLoading`, else `{N} board(s)`). (`260704-pr0p`)
- **Tiles**: one `<button>` per board (same tile idiom as the server tiles — `bg-bg-card border border-border rounded p-4 hover:border-text-secondary min-h-[60px]`) showing the board name + `{N} pin(s)`; clicking navigates to `/board/$name`. **Drag-reorderable**: tiles render `orderedBoards` from the shared `useBoardListReorder` hook (same instance seam as the sidebar) with `getTileProps(b.name)` spread onto each and the dragged tile dimmed `opacity-50` — see § Board-list drag reorder. (`260708-a2qd`)
- **Always visible**: the section renders even with zero boards — the empty state names the fill action, `No boards yet — hover a sidebar window row and click its 📌, or {paletteChord} → Pin:` with `{paletteChord}` the host-effective `command-palette` binding (clause omitted when unbound/disabled — § Education micro-copy; the sidebar `BoardsSection` keeps its own terser `Pin a window to start a board` hint) — instead of the section appearing/vanishing with the first/last board.
- **Not a shared component**: the tile markup is NOT shared with the sidebar `boards-section.tsx` (a parallel surface; the sidebar section drives the actual sidebar).

### SERVICES Zone on `/` (Host-page host-console home)

`HostOverviewPage` renders a **SERVICES** zone (`<section aria-label="Services">`) as the LAST zone, below the TMUX SERVERS tile grid, inside the same scroll container. It surfaces the host's listening TCP **ports** as tiles — a listening port is a HOST property (owned by no tmux window/session), so `/` (the box-level console) is its home, mirroring the HOST HEALTH rationale. The backend (`internal/ports`) passively enumerates and broadcasts **ALL** listening ports (procfs on Linux, lsof on darwin) with best-effort process attribution on BOTH platforms — no probing, no HTTP filter (see architecture.md § `internal/ports`). A tile is therefore **not guaranteed to speak HTTP**; the frontend does NO protocol guessing and applies no client-side denylist. Read-only awareness view; the only action opens a port's UI in a tmux window (Constitution IV — nothing mutates host state directly). (`260701-5d0z`, `260703-8y2a`, `260715-vfcz`)

- **Data source**: **`useHostServices()`** (its own `HostServicesContext`, separate from `HostMetricsContext` so the ~2.5s services stream doesn't cascade re-renders into metrics-only consumers). It rides the SAME server-independent broadcast as host metrics — the `event: services` frame arrives on every stream (the dedicated `?metrics=1` stream on a bare `/`, and each per-server stream's fan-out), deduped on the raw payload string (`applyHostServices`). Returns `[]` (never null) before the first tick, so consumers map over it unconditionally. See architecture.md § API Layer `/api/sessions/stream` (`event: services`) and § Server-independent host-metrics subscription.
- **Tiles**: each `Service` (from `@/types` — `{ port, process?, pid? }`) renders a row keyed by `svc.port` showing `:{port}` (mono) + the optional `process` name. **Process attribution is best-effort on BOTH platforms**: macOS populates `process`/`pid` from lsof; Linux joins lsof attribution onto its procfs port set and populates them where lsof can attribute the port, leaving them absent (bare tile) for ports lsof can't see (e.g. root-owned `sshd :22` under a non-root lsof) — see architecture.md § `internal/ports`. Empty state: `No services — listening TCP ports appear here automatically` when the list is empty (pre-first-tick, nothing listening, or an unsupported platform where the collector returns `[]` — Windows/BSD; macOS and Linux both enumerate) — the enumeration is passive and automatic, and the empty state says so (§ Education micro-copy). (`260715-vfcz`)
- **"Open in window" action** (`handleOpenInWindow`): opens the port's UI in an `@rk_type=iframe` tmux window via the existing `/proxy/{port}/` reverse proxy (`createWindow(server, session, "port-{port}", undefined, "iframe", "/proxy/{port}/")` — the `rkType`/`rkUrl` args become the `@rk_type`/`@rk_url` window options), then navigates to `/$server` (the new window surfaces via SSE). The window **name** is `port-{port}`, NOT `:{port}` — tmux rejects colons and periods in window names (`validate.ValidateName`); the tile still *displays* `:{port}`. Since `/` is server-less, it resolves a **target (server, session)**: `servers[0]` + that server's first session from the SSE cache (`sessionsByServer`), else an authoritative `getSessions()` fetch (the per-server stream is usually not attached yet on a fresh `/` load, so the cache can be empty even when sessions exist — fetching avoids a duplicate-name 500 on `new-session`), else an instant `"services"` session created on the fly (`createSession`). An `openingRef` guard debounces a double-click into a single create flow. `servers[0]` reflects the infra-last sort applied in `session-context.tsx` `fetchServers` (see § Infra-server de-emphasis), so a real workspace is preferred over `rk-daemon` as the "first-listed server" target. (`260705-uh8f`)
- **Button gated SOLELY on server existence**: the "Open in window" button is `disabled` with `title="Create a server first"` ONLY when `servers.length === 0` (there is no target to host the iframe window); otherwise it is enabled regardless of port number. The tile still renders in the disabled case.
- **Well-known-port de-emphasis**: with the SERVICES zone showing ALL listening ports, well-known/system ports (`port < 1024`, the reserved range — `sshd:22`, `smtp:25`, …) are visually de-emphasized, mirroring the `isInfraServer` server-tile treatment: their `:{port}` renders grey (`text-text-secondary` instead of `text-text-primary`) and they sort **last as a class**. `isWellKnownPort(port) = port < 1024`; the `orderedServices` `useMemo` partitions `hostServices` into regular vs. well-known, sorts each sub-group by port ascending, and concatenates (regular first) — so regular tiles keep their port-ascending order and well-known tiles trail. **Presentation-only, applied at render in `host-overview-page.tsx`**: the backend snapshot stays port-sorted; this is a frontend concern (parallel to the `session-context.tsx` infra-last server sort — see § Infra-server de-emphasis). Every tile, well-known or not, renders with its "Open in window" button. (`260715-vfcz`)
- **No HTTP gate anywhere**: there is no client-side HTTP-likely heuristic (no `NON_HTTP_PORTS` denylist, no `isLikelyHttpPort(port)`, no disabled branch, no `"Not a web service"` tooltip, no `handleOpenInWindow` guard) and no denylist or HTTP gate on the server; **do not re-add one**. The backend broadcasts ALL listening ports and does no probing, so a tile is not guaranteed to be a web service; **opening a non-HTTP port yields a user-initiated failed iframe — the iframe load IS the on-demand probe** (visible and harmless; there is no ambient probe). The empty state is `No services — listening TCP ports appear here automatically` (meaning "no listening ports"). `types.ts` `Service`/`ServicesSnapshot` carry no `http:` flag — there is no HTTP verdict. (`260703-8y2a`, `260715-vfcz`)

### Color Tinting — Owned Palette + Axis Split

Session rows, window rows, and server tiles support an optional color assignment that applies a full-width background tint. The palette is an **owned set of 10 fixed OKLCH hue families adapted to the active theme**: each family has a stable hue angle, and hue identity survives theme switches (a window labeled "orange" reads orange on every theme). The row's visual channels are split so labeling and selection never collide: **hue = label**, **tint depth = selection**, **left-edge marker = an independent label axis** (§ Left-Edge Label Zone). On window rows both label axes (color + marker) are set from the single left-edge Label picker; session rows and server tiles keep a right-side color affordance and have no marker.

**Owned hue families** (`themes.ts` `HUE_FAMILIES`): 10 families in stable display order, each `{ name, hue, legacy, neutral? }`. Hue angles are placed non-uniformly — tight through the discriminable red→amber region, the large gap parked in teal→blue where human hue discrimination is weakest: red 25°, orange 55°, amber 90°, olive 120°, green 150°, teal 185°, blue 250°, purple 290°, magenta 330°, slate 250° (`neutral`). `FAMILY_BY_NAME` / `FAMILY_BY_LEGACY` are the two lookup maps.

**Two-shade axis (normal + dark), 20 picker values**: each of the 10 families spans a **normal** and a **dark** shade — a second color axis that doubles the label space for organizing many parallel windows. The shade is a **render-time lightness offset carried on `PickerColor` (`{ family, shade }`), not a parallel vocabulary of pseudo-families** — hue identity, legacy mapping, and theme adaptation stay properties of the 10 families, so dark variants derive for free with zero new palette data. `PICKER_COLOR_VALUES` is the 20 display values in **paired order** (`red, red-dark, orange, orange-dark, … slate, slate-dark`). `parseColorValue` / `formatColorValue` / `resolveFamily` / `colorValueToHex` accept the `-dark`-suffixed names; `formatColorValue` emits `"{family}-dark"` for dark shades.

**Theme adaptation (full adoption) + dark derivation**: `themeColorStats(palette) → { L, C }` computes the mean OKLab lightness and mean chroma over `palette.ansi[1..6]`, with a chroma floor `THEME_CHROMA_FLOOR = 0.05` (near-monochrome themes stay distinguishable). A family's **normal** source hex = `oklchToHexInGamut(L_theme, C_theme, family.hue)` (slate uses a near-neutral chroma `min(C_theme × 0.2, 0.025)`); its **dark** source hex renders at `L_dark = L_theme − 0.14` with the **same hue and same chroma** (slate keeps its near-neutral chroma rule — an intentional gray ramp), gamut-reduced via the same `oklchToHexInGamut`. Because the normal shade is the existing mean-L rendering, **every existing stored color maps to the normal shade untouched**. `colorValueToHex(value, palette)` resolves any stored value (family name, `-dark` suffix, OR legacy alias) to its adopted hex, returning `null` when it maps to no family.

**OKLCH → in-gamut hex, chroma-reduction not channel-clamp**: `oklchToHex(L, C, hueDeg)` converts polar chroma/hue to OKLab (`a = C·cos h`, `b = C·sin h`) then reuses `oklabToHex`. `oklchInGamut(L, C, hueDeg)` reports whether the triple maps inside sRGB without any channel being clamped, reusing the shared `oklabToLinearRgb(c)` matrix helper (extracted so its 18 coefficients stay in lockstep with `oklabToHex` — both consume it). `oklchToHexInGamut(L, C, hueDeg)` brings an out-of-gamut triple in by **reducing chroma stepwise** (`GAMUT_REDUCE_FACTOR = 0.92`, `GAMUT_REDUCE_MAX_STEPS = 20`) — L and hue are preserved; sRGB channel-clamping is never used because it shifts hue and would defeat the stable-hue goal.

**Storage vocabulary — three coexisting forms, zero migration**: a color value is stored as one of (a) a **legacy numeric/blend descriptor** (`"4"` / `"1+3"`) — the form pre-existing colored rows carry, valid forever on read and write; (b) a plain **family name** (`"blue"`); or (c) a **`-dark` family-name value** (`"blue-dark"`) — the only form for dark shades, which have no legacy equivalent. The normal-shade 1:1 legacy map: red←`1`, orange←`1+3`, amber←`3`, olive←`1+2`, green←`2`, teal←`6`, blue←`4`, purple←`1+4`, magenta←`5`, slate←`3+4`. `resolveFamily(value)` accepts a family name, a `-dark` suffix, OR a legacy descriptor (legacy normalized via the file-private `normalizeLegacy`, mirroring the backend `NormalizeColorValue` rule) and returns the same family for all shades/forms. **Write seam** — `familyToLegacy(name)` maps a **normal**-shade pick to its legacy descriptor exactly as before (zero migration — existing values untouched), while **dark** picks have no legacy form and pass through unchanged, written verbatim as `"{family}-dark"`; the helper is idempotent — an already-legacy, `-dark`, or unrecognized input passes through unchanged.

**Pre-blended row tints, multi-keyed for all 20 values**: `computeRowTints(palette) → Map<string, RowTint>` pre-computes the three blended state hexes for every shade of every family, keyed under **every stored vocabulary form** — a normal shade under BOTH its family name AND its legacy descriptor (both point at the *same* entry), a dark shade under its `"{family}-dark"` key (no legacy form exists) — plus the `"8"` uncolored-selected sentinel (10×2 normal keys + 10 dark keys + sentinel = 31 entries). All forms are keyed because consumers (`window-row`/`session-row`/`server-panel`) look up the RAW stored value and the backend emits legacy forms for pre-existing rows, so a family-name-only map would miss every legacy-stored row. Each shade's adopted source hex (`colorValueToHex`, dark included) is saturated ×1.5 (`TINT_SATURATE_FACTOR`) before blending into the theme background at three ratios:

| State | Constant | Saturated-source ratio |
|-------|----------|------------------------|
| Base | `TINT_BASE_RATIO` | 14% |
| Hover | `TINT_HOVER_RATIO` | 22% |
| Selected | `TINT_SELECTED_RATIO` | 40% |

Selection is carried by tint **depth** alone (no left border — § Row Anatomy), so the selected ratio is 40%. The uncolored-selected gray sentinel (`"8"`, `UNCOLORED_SELECTED_ANSI`/`UNCOLORED_SELECTED_KEY`) is NOT saturated (gray is near-zero chroma) and uses the deeper `UNCOLORED_SELECTED_RATIO = 0.5` so it beats the `bg-card/50` hover. Each state gets its own concrete hex — no stacked transparent layers. The `RowTint` type (`{ base, hover, selected }`) is exported. Hover is applied imperatively via `onMouseEnter`/`onMouseLeave` style mutations to avoid CSS-specificity issues with dynamic backgrounds.

**Contrast-guardrailed borders** (`computeRowBorders(palette, category) → Map<string, string>`): each shade's adopted source hex passed through a WCAG contrast guardrail, multi-keyed (family name + legacy descriptor for normal shades, `"{family}-dark"` for dark shades, + `"8"` sentinel — 31 entries) exactly like `computeRowTints`. `adjustBorderForContrast(border, bg, isDark, BORDER_MIN_CONTRAST)` nudges **only the OKLab lightness L** (lighten on dark themes, darken on light) in `0.03` steps until the border clears `BORDER_MIN_CONTRAST = 3.0` against the background or hits a 24-step cap; **hue and chroma are preserved** so the color keeps its identity. Window rows do not use these for a selection border; **server tiles** use them for their stripe/edge, **the left-edge marker stripe uses them as its guarded family color** (`markerColor`, § Left-Edge Label Zone), and **the Label picker's marker preview cells use them as the guarded stripe color of the live row preview** (§ Variant-C paired-grid Label picker). `sidebar/index.tsx` computes `rowTints` + `rowBorders` once per theme via `useMemo` and threads them to `WindowRow`/`ServerPanel`.

**OKLab + WCAG color-math helpers** (`themes.ts`): `hexToOklab` / `oklabToHex` (Björn Ottosson 2020 coefficients; `oklabToHex` and `oklchInGamut` share the `oklabToLinearRgb` matrix helper), the sRGB↔linear `srgbToLinear` gamma step, `relativeLuminance` (WCAG 2.x) and `contrastRatio(hex1, hex2)` (1..21), and `adjustBorderForContrast`. OKLab is the chosen lightness/distinctness space; WCAG is the separate legibility standard.

**SwatchPopover** (`app/frontend/src/components/swatch-popover.tsx`): one shared component, **one universal square style** — no rounded variant, no `square` prop. The color section renders the **20 paired values** from `PICKER_COLOR_VALUES` laid out 4-wide (5 color rows, `normal | dark` adjacent) plus the removal row: `Clear` (cols 1–3) beside the ✕ close cell (col 4). Props are STRINGS: `selectedColor?: string`, `onSelect(color | null)`, `onClose()`, plus the marker extension `selectedMarker?`, `onSelectMarker?(marker)`. **Color write seam** — every color emit funnels through `familyToLegacy` (the file-private `emit` callback: `onSelect(familyToLegacy(value))`): a **normal**-shade pick is stored as its legacy descriptor (`"orange"` → `"1+3"`, zero migration), a **dark** pick passes through verbatim as `"{family}-dark"` (no legacy form exists), and `null` (Clear) passes through untouched — the backend now accepts all three vocabularies (see architecture.md § validate closed sets), so no write 400s. **Uniform solid swatches**: each swatch is a single 18×18px solid fill = the value's `computeRowTints` `selected` tint (falling back to `colorValueToHex` then `theme.palette.foreground`), with a ✓ glyph + bright ring on the selected swatch so the selection is unambiguous between the two adjacent same-family shades. Selection is `-dark`-aware — matched by the picked value's `{family, shade}` (via `parseColorValue`), so a legacy-stored value highlights the right family's normal shade. The listbox **autofocuses on mount** (`containerRef.current?.focus()` in a mount effect, mirroring pin-popover) so arrow-key nav works immediately after the `Window: Label` palette action — the sole keyboard path to the marker section. **Dismissal contract**: selection NEVER dismisses (live combo toggling); the popover closes only via its ✕ cell, an outside click, or Escape — callers pass pure write handlers and close only from `onClose`.

**Universal square style**: every caller gets the same vocabulary — zero border radius on the container and all cells (no `rounded-md`/`rounded-sm`), a hard offset block shadow `3px 3px 0 rgba(0,0,0,.35)` (not `shadow-lg`), 3px gaps (`gap-[3px]`), 18px cells (`w-[18px] h-[18px]`), and 1px selection/focus outlines (`ring-1 ring-text-secondary`). There is a single style literal per axis — no dual class-string branches, no `SQUARE_GRID_COLS`/`DEFAULT_GRID_COLS` split. Session/server rows and the palette color actions render square like the window-row picker.

**Variant-C paired-grid Label picker**: when `onSelectMarker` is supplied (`showMarkers`), the popover renders two side-by-side sections divided by a **vertical hairline** (`w-px bg-border mx-1.5 self-stretch`) — markers LEFT (a 1-column `flex flex-col gap-[3px]` of 6 cells), colors RIGHT (the 20-swatch 4-wide paired grid). The layout pairs the two axes row-for-row:

```
∅   │  [   Clear   ]  ✕          ← row 0: removal row + close cell
∷   │  red  red-dk  or  or-dk    ← row 1: dotted  + colors 1–4
┆   │  am   am-dk   ol  ol-dk    ← row 2: dashed  + colors 5–8
▐   │  gr   gr-dk   te  te-dk    ← row 3: solid   + colors 9–12
▐▓  │  bl   bl-dk   pu  pu-dk    ← row 4: double  + colors 13–16
▐█  │  ma   ma-dk   sl  sl-dk    ← row 5: thick   + colors 17–20
```

- **Row 0 is the removal row**: the `∅` no-marker cell (marker column) beside a `Clear` button spanning color columns 1–3 (`col-span-3`) and the **✕ close cell** in column 4 (`aria-label="Close picker"`, an 18px `role=option` cell that is never `aria-selected` — an option-as-command matching Clear's pattern, keeping the `role=listbox` container's children ARIA-valid per Copilot's #452 review).
- **Dismissal contract — selection never closes**: picking a color, a marker, or Clear leaves the popover OPEN so color + marker combos can be toggled and compared live against the row; it dismisses only via the ✕ cell, an outside click, or Escape. Closing on select is the component's contract, not the callers' — every call site (window-row Label picker, session/server rows, HOST panel, settings dialog, the palette Set Color overlay) passes a pure write handler and closes only from `onClose`.
- **Rows 1–5** pair the dotted / dashed / solid / double / thick marker cells with the 5 color rows (each `normal | dark` adjacent — row 1: red, red-dark, orange, orange-dark; …).
- **Marker cells are live row previews of the selected color** — each non-∅ cell renders as a miniature window row for the currently selected color: background = that value's `tint.base` (gray sentinel when uncolored), the stripe via the shared `markerStripeStyle` helper in the **guarded border color** with a **2px left inset** (so the marker doesn't kiss the cell edge and the cell reads as a mini row), plus the paired row texture — the static `.rk-hazard` wedge on the thick cell and the static `.rk-scanlines` wash on the double cell (with per-cell `--rk-marker-color`), reusing the row classes so the texture vocabulary lives in one place. Picking a different swatch **repaints the marker column immediately**. The empty ∅ cell shows a glyph; the current state carries `aria-selected` + a selection ring. Selecting a cell calls `onSelectMarker` **directly with the exact state** (no cycling — any state is one click; `""` clears).
- **Preview cells NEVER animate** — an explicit user decision that motion belongs to real rows only: no scanline crawl even when the double cell is selected, the hazard wedge is static as always, and the dashed preview never carries the data-rain overlay. No cell ever carries `rk-scanlines-crawl` or `rk-dash-rain`.
- Marker cell order mirrors `MARKER_STATES` (`["", "dotted", "dashed", "solid", "double", "thick"]`) via `MARKER_CELLS` — `∅` in row 0, the five non-empty states beside the five color rows.
- **One-period stripe tiles make the previews correct**: the marker cells are 18px tall, so the fixed one-period `markerStripeStyle` tiles (§ Left-Edge Label Zone) are what let the dotted/dashed rhythm render correctly inside them — the previously element-height-dependent tile broke at that size.
- **Documented 1:1 invariant**: the marker-cell ↔ grid-row pairing is a deliberate `GRID_ROWS === MARKER_CELLS.length` invariant (6 marker cells ↔ 6 grid rows: 5 color rows of 20 swatches 4-wide + the Clear row), documented as such in a code comment. The 20-swatch grid and the 6-cell marker set are sized to pair exactly — the two extensions fix each other's layout, leaving no dead cells.

**Color-only callers get the same paired grid, marker-less**: with the marker props absent (`onSelectMarker` undefined) the left marker column and vertical hairline are simply not rendered — the color section stands alone with its `Clear` + ✕ removal row over the 20 paired swatches (5 rows of 4). Same square style; no separate layout branch.

**Keyboard navigation — conceptual 5-column × 6-row grid, no dead cells**: focus is a `{row, col}` `GridPos` pair (not a linear item index). `col` 0 = the marker column (valid only when `showMarkers`), cols 1–4 = the color columns; `row` 0 = the removal row, rows 1–5 = the five color rows. The 20 colors fill the 5×4 color region exactly, so there are **no dead cells** to clamp around anymore. ArrowLeft/ArrowRight cross the hairline (col 1 ↔ col 0) and step within the color columns; ArrowUp/ArrowDown move within a column. The `Clear color` button is a **single spanning focus target** occupying cols 1–4 of row 0 (canonicalized to col 1; `focusOnClear = row === 0 && col >= 1`). Off-grid moves clamp to the nearest valid cell; focus stays valid, no crash. Enter/Space activates the focused cell (`activate`): col 0 → `onSelectMarker(MARKER_CELLS[row])`, row 0 → `emit(null)` (Clear), else → `emit(PICKER_COLOR_VALUES[colorIndexAt])`. **Initial focus FOLLOWS SELECTION**: the selected color swatch, or the `Clear color` cell (row 0 — already `aria-selected` in that state) when uncolored, never an arbitrary swatch; the marker column is reached via ArrowLeft. **Focus-visible semantics**: the focus ring (`ring-text-secondary`, visually quieter than the `ring-text-primary` selection ring) renders only after the first arrow key (`keyboardActive` flag) — the listbox autofocuses on mount, so an always-on ring showed mouse users a phantom highlight. The selected item is marked with the brighter `ring-text-primary` (colors additionally show a ✓; the uncolored state rings `Clear color`). Color-only mode is the same grid minus column 0.

**Window-row left-edge trigger**: the combined Label picker opens from the window row's left-edge label zone (§ Left-Edge Label Zone) or the `Window: Label` palette action — it is not a right-edge hover indicator. Session rows and server tiles still surface their color picker via a right-edge hover palette icon (visible on hover on desktop, always on touch via `coarse:opacity-100`).

**Command palette actions**: "Session: Set Color" opens the color-only SwatchPopover. The window **label** action is `window-label` / `Window: Label`, which dispatches an imperative `label-popover:open` CustomEvent (`{ detail: { server, windowId } }`) for the current window — the matching `WindowRow` listens and opens its combined picker (mirroring the `pin-popover:open` pattern used by "Board: Pin Current Window") to set both color and marker in one picker. The color-only `window-set-color` / `Window: Set Color` action (which opens the color-only SwatchPopover via `setShowColorPicker("window")`) is a separate registered action. Every color write path flows through the `familyToLegacy` write seam (normal picks → legacy descriptor, dark picks → `"{family}-dark"` verbatim); "Clear color" sends `null`, and clearing the marker sends `null` through `setWindowMarker`.

**Storage**: a stored color value is a **legacy descriptor** (`"4"` / `"1+3"`, the form pre-existing rows keep), a plain **family name** (`"blue"`), or a **`-dark` family-name value** (`"blue-dark"`, the only dark-shade form) — all three valid on read and write. Frontend wire types are `WindowInfo.color?: string` and `ProjectSession.sessionColor?: string` (`src/types.ts`). Window and session colors are ephemeral tmux user options (`@color` per window, `@session_color` per session — survive session lifetime, not tmux-server restarts), set via `setWindowColor`/`setSessionColor` in `client.ts`. Server colors persist in `~/.rk/settings.yaml`. **No color storage/API migration** — the shade axis widens the accepted vocabulary additively (legacy values untouched); the backend closed set was extended to accept the family-name forms. See architecture.md § Data Model and § validate closed sets for the backend storage/validation contract.

### Instance Accent ("host color")

A per-instance accent color makes multiple run-kit instances (laptop, Mac mini, GCP server) viewed as visually-identical installed Chrome PWA windows tell each other apart at a glance. It is an **axis orthogonal to the owned-palette color tinting above**: server/window/session colors saturate the sidebar and mean "which tmux server"; the instance accent means "which run-kit instance" and owns surfaces the sidebar colors never touch (the top bar and the HOST panel). The accent is a property of the **instance**, not the viewer — stored authoritatively on the instance's host (`~/.rk/settings.yaml` `instance_color`, see architecture.md § `internal/settings` and § `/api/settings/instance-color`), so every device viewing that instance sees the same accent. (`260721-1etw-instance-accent-host-color`)

**Resolution chain** (frontend, in the `InstanceAccentProvider`): (1) the explicit `instance_color` setting from `getInstanceColor()` — authoritative; (2) the localStorage echo (`runkit-instance-color`) — a **paint cache only, never authoritative**, used as the first-frame seed before the fetch lands; (3) **none** — there is **no derived default**: a fresh instance renders no accent until the user picks one (an earlier hostname-hash default was removed — its color could coincide with a just-cleared explicit pick, making "Clear color" look like a no-op). An `authoritative` flag (the explicit fetch resolved) gates clearing the echo/meta on a null accent, so the paint seed is never wiped before the real resolution replaces it.

**Provider + consumers** — `InstanceAccentProvider` / `useInstanceAccent()` (`contexts/instance-accent-context.tsx`) mounts once in `RootWrapper` inside `ThemeProvider` > `ToastProvider` and above `ChromeProvider`, so it can read `useTheme()` for the theme-derived hexes and `useToast()` for the write-failure toast. It fetches `getInstanceColor()` + `getHealth()` once on mount (StrictMode-guarded via a `didFetchRef`), seeds the first paint from the echo, resolves per the chain, derives `{stripeHex, washHex, titlebarHex}` from the active theme, keeps the echo + theme-color meta in sync on every accent/theme change, and exposes all three on the context value (`{color, isExplicit, stripeHex, washHex, titlebarHex, setColor}`). `titlebarHex` is nullable like its siblings and has two consumers: the theme-color meta bridge effect, and the desktop shell's titlebar strip, which renders from it directly rather than re-deriving the same blend (§ Desktop-Shell Titlebar Strip). `setColor(descriptor | null)` updates state optimistically then POSTs via `setInstanceColor`, toasting on failure. Every rendering surface reads from this ONE instance — one fetch, one state, so a pick in the HOST panel repaints the top-bar stripe without a reload. A test seam `InstanceAccentValueProvider` injects a fixed value (mirroring the session-context value-injection pattern).

**Theme-derived hexes** (`deriveAccentHexes(value, theme)` in `src/instance-accent.ts`, never hardcoded) — a three-hex split, each a distinct accent surface: `stripeHex` is the **contrast-guarded** family hex — `colorValueToHex(value, palette)` then `adjustBorderForContrast(src, bg, isDark, BORDER_MIN_CONTRAST)` (the same guarded-hex derivation server tiles/stripes use, § Color Tinting) — used for the 2px top-bar stripe and the HOST hostname tint. `washHex` is a subtle `blendHex(src, bg, INSTANCE_WASH_RATIO = 0.065)` (~6.5%) accent-into-background blend for the top-bar background wash. `titlebarHex` is the strongest of the three — `blendHex(src, bg, INSTANCE_TITLEBAR_RATIO = 0.35)` (~35%) accent-into-background — and paints **every titlebar-class surface**: the theme-color meta content (the installed-PWA titlebar tint) and the desktop shell's page-drawn titlebar strip, in both cases a tinted band with the full-brightness `stripeHex` 2px stripe visible directly below it. `INSTANCE_TITLEBAR_RATIO` sits beside `INSTANCE_WASH_RATIO` (a taste constant). Blend descriptors (`"1+3"`) render through the same `resolveFamily`-based owned-family mapping as single indices — no new blend scheme. All three recompute on a theme switch (light themes get a light-background blend by construction); nothing hardcoded survives. An unrecognized descriptor yields `null` (no partial object).

**Top-bar stripe + wash** (`AppLayout`, `app.tsx`) — the persistent top bar's `shrink-0` wrapper (above `RootTopBar`) carries a 2px accent stripe (an `aria-hidden` `<div style={{height:"2px", backgroundColor: stripeHex}}>`) plus the `washHex` as its `backgroundColor` (the `TopBar` header has no background of its own, so the wash on the wrapper shows through). Both render only when their hex is non-null (no accent resolved ⇒ neither renders). This is the "which instance" channel living where the sidebar colors do not.

**HOST panel — accent hostname tint + swatch picker** (`components/sidebar/host-panel.tsx`) — the hostname in the panel's `headerRight` slot renders in `stripeHex` (the contrast-guarded accent) when resolved, else the default `text-text-primary`. A palette swatch button rides the `CollapsiblePanel` **`titleAction` slot** (immediately right of the HOST title — a sibling of the toggle button, since a nested button would be invalid markup; with `titleAction` set the header splits into `[toggle: chevron+title][titleAction][toggle click-region: headerRight]`, the trailing region keeping mouse click-to-toggle while keyboard toggling stays on the title button), hover-revealed with the touch/keyboard fallbacks `opacity-0 group-hover/panel:opacity-100 coarse:opacity-100 focus-visible:opacity-100` (`CollapsiblePanel`'s header gained a `group/panel` class to drive it) and an `aria-label="Set instance color"`. Clicking it opens a **color-only** `SwatchPopover` (no `onSelectMarker`) portalled to `document.body` at fixed coordinates anchored at the button — left-aligned (the button sits beside the title at the sidebar's left edge), with a **flip-above** `useLayoutEffect` heuristic (the HOST panel sits at the sidebar bottom, so flip-above is the norm) — escaping the panel's overflow clip. This is the x4sf ServerGroup-header picker precedent (§ Server-group header action cluster). A pick calls `useInstanceAccent().setColor` (which POSTs and repaints every surface); `Clear color` sends `null`, removing the accent entirely (the no-color default). The HOST header carries **no connection dot**: the top-bar dot already reflects the same current-server subscription health (`setChromeConnected(slice.isConnected)` in session-context), and the removed dot's "SSE" title predated the `/ws/state` socket.

**PWA titlebar bridge — single theme-color meta writer** (`src/instance-accent.ts`) — the `<meta name="theme-color">` tag is accent-aware through ONE shared writer holding module-level state (`currentAccentHex` + `lastBackground`, content = accent hex when set, else the theme background): (a) the blocking pre-paint script in `index.html` (§ PWA Meta Tags & Theme Color) reads the echoed `hex` from `runkit-instance-color`, validates the `#rrggbb` shape, and applies it **verbatim** as the initial theme-color (falling back to the per-mode defaults) so an installed PWA window opens already tinted with no flash; (b) at runtime the provider calls `setAccentThemeColor(titlebarHex)` (the ~35% titlebar blend, NOT `stripeHex`) on accent/theme change, and writes the same `titlebarHex` into the echo's `hex` field — so cold start and runtime both paint the subtle tinted titlebar, and the full-brightness `stripeHex` 2px stripe stays visible directly below it; (c) `theme-context.tsx`'s `applyThemeToDOM` calls `applyThemeColorMeta(theme.palette.background)` instead of writing the meta directly, so a theme switch cannot clobber the accent tint. Desktop Chrome retints installed-PWA titlebars live on meta changes, and the **Electron shell listens to the same meta** — `did-change-theme-color` is what repaints its Windows/Linux window-controls overlay, so the one writer serves both hosts with no shell-specific color API ([desktop-shell](/run-kit/desktop-shell.md) § Hidden Titlebar & Accent Strip). `index.html` is unchanged — the pre-paint script applies the echoed `hex` verbatim, so swapping the echoed value retints cold start for free (an echo written by a pre-titlebar-blend build carries the full-hue hex and self-corrects when the runtime resolution rewrites both meta and echo — the same accepted self-correcting transient class as cross-mode loads).

**PWA install identity — Dock/Cmd-Tab icon + tab favicon** — the instance accent also tints the installed PWA's OS-level identity, so multiple instances are distinguishable in the Dock and Cmd-Tab, not only inside the window (`260722-eo8e-accent-dock-icon`). This is a **backend** concern: the Go server serves `/manifest.json`, the three `/generated-icons/icon-*.png`, and `/generated-icons/favicon.svg` dynamically, tinting the grayscale logo per the instance's `instance_color` (see architecture.md § PWA Identity Assets). No frontend component changes — `index.html`'s static `<link rel="manifest">` / `rel=icon` / `apple-touch-icon` hrefs are unchanged; the tint enters through the responses. Two behaviors matter to the UI surface:
- **Cache-bust contract**: the dynamic manifest appends `?c=<descriptor>` to each icon `src` (`+` encoded `%2B`) when an accent is owned; the icon PNG routes tint only on that owned `?c=`, serving stock bytes otherwise. The favicon route carries no `?c=` (its href is static) and resolves the tint from settings per request with `Cache-Control: no-cache`, so open browser tabs re-tint on their next load.
- **Chrome icon-refresh limitation** (accepted, documented — not engineered around): Chrome snapshots PWA icons at install and re-checks the manifest only on app launch with ~daily throttling, so an accent picked **before** install is immediate while a change **after** install updates the Dock icon eventually (reinstall forces it). The top-bar stripe/wash and PWA titlebar theme-color stay live regardless.

### Desktop-Shell Titlebar Strip (`components/shell-titlebar-strip.tsx` + `lib/shell-strip.ts`)

The instance accent's **third consumer surface**, and the only one that exists outside a browser: inside the Electron viewer shell the native titlebar is hidden, so the SPA draws the window's titlebar itself as a 28px full-width band above the top bar. The accent chain is therefore PWA titlebar meta → top-bar wash/stripe → **shell titlebar strip**, all three fed by the same `useInstanceAccent()` instance.

**Background is `titlebarHex`, read not re-derived.** The strip paints `titlebarHex ?? theme.palette.background` — the identical ~35% accent-into-background blend the installed-PWA titlebar carries, which is the point: the same window chrome should be the same color whether run-kit is installed as a PWA or as the desktop app. It consumes the context's exposed field rather than calling `deriveAccentHexes` again, so one derivation cannot drift into two (§ Design Decisions → `titlebarHex` is exposed, not re-derived). With no accent resolved the band falls back to the plain theme background and is still a drag surface.

**Drag region via a CSS utility class, with exactly one no-drag island.** The band carries `.rk-shell-drag` (`globals.css`: `-webkit-app-region: drag`); the host-switcher trigger and its open menu carry the sibling `.rk-shell-no-drag` (`-webkit-app-region: no-drag`) and are the band's **only** interactive island — everything else in the band drags the window. Both are classes because `WebkitAppRegion` is absent from React's `CSSProperties` and an inline style would need an `as` cast the code-quality rules forbid; `rk-*` is also where the utility vocabulary already lives. The classes are inert in browsers, so they cost nothing outside the shell (§ Design Decisions → Drag regions are a CSS utility class; → The strip's switcher trigger is the band's only no-drag island).

**The label names the window's host, and is the switcher trigger.** A centered, truncated `location.hostname`-fallback label showing the shell-registered **active** host's display name, fetched on mount via `listShellServers()` and reduced by the pure `activeShellHostName(servers)`. Text color is `stripLabelColor(bg)` — a `contrastRatio` pick between the two standard text hexes, the same WCAG helper the border guard uses. Horizontal insets keep it clear of the OS controls compositing over the band: a fixed symmetric 80px on darwin (traffic lights sit left; symmetry keeps the label visually centered) and `titlebar-area-*` env expressions elsewhere, which degrade to no inset where window-controls-overlay is absent. `stripLabelColor`, `stripInsets`, `activeShellHostName`, and the shared `SHELL_STRIP_HEIGHT_PX` / `SHELL_STRIP_MARKER_CLASS` constants live in `lib/shell-strip.ts` with a colocated vitest suite.

**Host-switcher dropdown — the mouse-secondary switch path.** When the bridge answers a **non-empty** host list (`stripSwitcherEnabled(servers)`, a type guard) the label renders as a trigger button — `<active-host-name> ▾`, `aria-haspopup="menu"` / `aria-expanded`, `aria-label="Switch host"`, with a subtle currentColor-tinted hover pill (`hover:bg-current/15`, which reads on any accent-blended strip background since the label color is already contrast-derived against it) — reusing the top bar's `▾` switcher vocabulary (§ Crumb Affordance Vocabulary). It is the mouse-secondary companion to the ⌥⌘1–9 / ⇧Ctrl+1–9 accelerators and the palette's `Server: Switch to` block, which stay the primary paths (Constitution V). Below the gate — an older shell exposing only `version`/`platform`, a denial, or an empty store — the label is a plain static span with no chevron, no pill, and no click behavior; the band still mounts, because its gate is `isShell()` alone (§ Design Decisions → Shell chrome mounts on `isShell()`).

**Row anatomy** (`shellHostMenuRows(servers, platform)` in `lib/shell-strip.ts`, list order preserved because that IS the native menu's binding order): a fixed-width ✓ marker column so names align, the active row in `text-accent`; the display name; the **dimmed origin** (`new URL(url).origin`, falling back to the raw string on a parse failure) — host display names are not unique because the shell's store never dedupes, so the origin disambiguates; and a trailing accelerator hint `⌥⌘{n}` on darwin / `⇧Ctrl+{n}` elsewhere, `null` past `MAX_SHELL_SWITCHER_HINTS = 9` (mirroring the native menu's `MAX_SWITCHER_ACCELERATORS` — a host with no binding gets no hint). Platform comes from the bridge's `shellInfo()?.platform`. Rows need no bridge change: `servers:list` already projects `{id, name, url, active}`.

**Refetch on open, guarded by a monotonic sequence.** Every open issues a fresh `listShellServers()` — the native `Hosts → Remove "<name>"…` menu mutates the store without a page reload, so a mount-time-only list goes stale. One `listSeqRef` counter guards *both* fetches: a resolution whose sequence is not the latest issued is dropped, so out-of-order resolutions can never leave a stale list rendered, and the mount effect's cleanup bump doubles as cancellation (the open-time fetch has no effect cleanup to cancel it). A `null` resolution — the never-throw wrapper's denial signal — keeps the last known list rather than blanking an open menu.

**Menu semantics copy `BreadcrumbDropdown`'s contract** (§ Breadcrumb Dropdowns): `role="menu"` with `role="menuitem"` rows, capture-phase keydown for Escape (close + refocus trigger) and ArrowDown/ArrowUp (wrapping roving tabindex; Enter needs no handler because each row is a native `<button>`), outside-`mousedown` close, and focus landing on the active row on open. The menu is a plain `absolute left-1/2 top-full` anchor — the strip has no `overflow: hidden` ancestor clipping below it, so no fixed-position machinery is needed — and it paints in theme surface colors (`bg-bg-primary`/`border-border`) rather than the strip's accent blend, because it overlays SPA content below the band. Three guards make an open-time refetch that changes the row count safe: the seed effect keys on the **open transition only** (re-running on a refetched set would yank focus mid-arrowing); a separate clamp effect keyed on `[open, rows.length]` re-seats the roving tabindex when a refetch *shrinks* the set below the current seat (an in-bounds seat is left alone, so an unchanged or growing list never moves focus); and the arrow branch's emptied-list guard reads the live committed row count from `hostCountRef` — a ref a `useLayoutEffect` keyed on `rows.length` writes at commit time — returning before `preventDefault` when it is 0, so even a stale capture-phase subscription still attached in the commit→passive-effect-flush window observes the zero and releases the key instead of swallowing arrows app-wide (a passive-effect ref write would flush in the same phase it is meant to beat); the wraparound modulus derives from the same live read, so a shrunk-but-non-empty refetched list also cycles over the committed count (§ Design Decisions → The emptied-list arrow guard reads the live row count from a commit-synced ref). A close-on-empty effect then releases `open` so the menu's effects tear down with it.

**Selection hands off to the shell, no optimistic UI.** Picking a row closes the menu and calls `switchShellServer(id)`; the shell's single `switchToHost` seam swaps the displayed host, so there is nothing to update optimistically and no SPA-side path handling exists. The swap is a detach/attach flip between per-host renderer views ([desktop-shell](/run-kit/desktop-shell.md) § Host Views), so this component instance simply stops being displayed — it keeps running, with its state intact, and is shown again unchanged when the user switches back. A `false` resolution surfaces an error toast (the palette's precedent), and selecting the already-active row is a harmless no-op — the same behavior the palette action and the native menu radios have, so all three switch paths behave identically.

**Marker class + mount gating.** While mounted the component sets `rk-shell-strip` on `<html>` (a `useLayoutEffect`, removed on unmount) — the shell's version-skew fallback CSS is keyed on `html:not(.rk-shell-strip)` and self-disables against it ([desktop-shell](/run-kit/desktop-shell.md) § Hidden Titlebar & Accent Strip). `AppLayout` mounts the strip gated on `isShell()`, as a **sibling above** the top-bar wrapper — the top bar itself is untouched, which is the explicit contrast with a titlebar merge. `isShell()` is false in every browser and in Playwright, so **no e2e sees the strip or its dropdown**: the whole surface is covered by two colocated vitest suites — `lib/shell-strip.test.ts` for the pure helpers and `shell-titlebar-strip.test.tsx` for the component (rendered inside `ToastProvider`, asserting the gate branch, the open-time refetch call count, row anatomy, shrunk/emptied-list focus behavior, out-of-order resolutions, selection, and Escape/Arrow keys) — plus manual verification in the real shell. The band persists in macOS fullscreen (traffic lights auto-hide over it; no fullscreen bridge flag exists).

### Desktop-Shell Waiting Badge Reporter (`components/shell-badge-reporter.tsx`)

A null-rendering subscriber, mounted in `AppLayout` beside the strip and gated on `isShell()`, that mirrors the waiting-agent count to the OS dock/taskbar badge — the attention tier escaping the page (§ Attention Surfacing). It is the fifth `lib/waiting.ts` consumer and the only one whose output leaves the browser.

The count is `countWaitingAcrossServers(sessionsByServer)` — a new helper composing the existing `countWaitingInSessions` over every entry in the streamed map, so the badge counts identically to the sidebar and tile badges. It derives from SSE state already in the client: no polling, no new endpoint (Constitution II). **Scope caveat**, inherited from `sessionsByServer` itself: only **attached** servers stream windows, so an unattached server contributes 0 — the same accepted constraint the Host-page and sidebar server tiles carry.

Reporting is change-only: the derived count is the effect dependency and a ref guards re-mount duplicates, so a 2 → 2 SSE tick makes no bridge call while 2 → 0 reports `0` explicitly (clears must propagate). The call goes through `setShellBadge(count)`, which never throws — a plain browser or an older shell without the `badge` bridge group simply resolves `false`.

### Row Anatomy — Selection & Board-Pin Cues (`window-row.tsx`)

The window row's visual channels are orthogonal: **hue = label** (family tint), **tint depth = selection**, **left-edge zone = the independent marker axis + the Label-picker trigger** (§ Left-Edge Label Zone). Selection and the board-pin cue are borderless — the sidebar window row carries no left accent border.

**Full-bleed row box**: the row `<button>` (`window-row.tsx:238`) spans the sidebar **edge-to-edge** — every box-anchored visual layer (family tint via `buttonStyle`, hover fill, 40% selection tint, the `isDragOver` box-shadow, and the `inset-0` scanline overlay) reaches the physical sidebar left edge with no gutter. The 12px hierarchy indent lives inside the button's left padding (`pl-[30px]` = 12px indent + 18px content padding), not as a group margin on the window-list wrapper — so the status dot and window name sit at a fixed 30px from the sidebar edge (dot leading edge) while the box itself extends to the seam. The wider scanline band on selected+double rows follows from the box reaching the edge and is intended.

**Selection = tint depth + typography, borderless**: a selected colored row deepens to the 40% family tint (`rowTints.get(color).selected`); an uncolored selected row uses the gray sentinel's deeper 0.5 tint. The name goes `font-medium` + `text-text-primary`. There is **no** left accent border on selection.

**Board-pin active-board cue on the pin glyph** (not a border): the persistent filled pin glyph — already `opacity-100` + filled whenever the window is pinned to any board (`isPinnedToAny`) — renders **accent-colored** when the row is pinned to the currently-viewed board (`isPinnedToActiveBoard`), and monochrome `text-text-secondary` otherwise. `isPinnedToActiveBoard` implies `isPinnedToAny`, so the accent branch is always the persistent (never hover-gated) glyph. This is load-bearing because link-based pinning keeps pinned rows visible in SESSIONS: the cue distinguishes "pinned to the board you're viewing" from "pinned to some board" with zero new elements, leaving the left-edge zone for the marker + Label picker. See § Active-Board Highlight in Sessions Tree, § Sidebar Pin Icon on Window Rows. (`260718-co9z`)

### Left-Edge Label Zone (`window-row.tsx` `LabelZone`)

The whole 26px to the left of the status dot on each window row is ONE interactive target that opens the combined **Label picker** (colors + marker). It consolidates both of the row's label axes on the left edge — hue and the independent 5-state marker — leaving the right hover cluster for actions (pin + kill). The marker is an abstract label axis, orthogonal to hue and selection; its states are deliberately unnamed in the UI (todo/doing/done for one user, priority for another), though `dashed` = "working" and `thick` = "completed" are the suggested conventions — **label conventions only, with no functional wiring** to `@rk_agent_state` or the status pyramid.

**One 26px target, opens the picker — never cycles, never selects**: clicking anywhere in the zone opens the combined Label picker anchored at the row's bottom-left (an `absolute left-0 top-full z-50` wrapper; the row root stays overflow-free so the `top-full` popover is not clipped even on a selected+double row). The click `stopPropagation`s so it does not select the row, and coexists with drag-reorder. Cursor is `pointer` (menu-opener semantics). The zone is enabled on non-ghost rows wired with both the color and marker write seams plus a `server` (`labelZoneEnabled`).

**Zone geometry**: an absolute `z-20` sibling positioned `left-0` spanning `LABEL_ZONE_WIDTH = 26`px (full row height). Because the row box is full-bleed (§ Row Anatomy), `left-0` IS the physical sidebar edge — the zone needs no negative offset, so the coordinate expression is a plain `left-0` overlay. The zone's absolute span (sidebar x = 0…26) is 26px of the true sidebar edge. The palette-icon home is a 12px band (`ICON_ZONE_WIDTH = 12`) inset `ICON_EDGE_INSET = 12`px off the physical edge (sidebar x = 12…24) — past the widest (double, 4+6=10px) marker stripe, so the hover icon sits beside the stripe rather than over it. The button content is padded clear with `pl-[30px]` (12px hierarchy indent + 18px content padding) so the leading StatusDot + name start 4px past the zone's inner edge — the dot keeps its click-to-select (and its coarse-pointer flyout tap), and the dot + name keep their exact x-positions (no content shift). Active on coarse pointers — touch gets direct label access (no `coarse:pointer-events-none`).

**Hover affordance — palette icon + two-stage zone glow**: at rest the zone is transparent with no icon. On ROW hover (`group-hover`) the shipped `PaletteIcon` (~11px, in the 12px icon zone at `left: ICON_EDGE_INSET = 12`px, beside the edge-anchored stripe rather than over it) fades to ~65% and the whole zone glows ~12% family color; when the ZONE ITSELF is hovered the icon reaches full opacity and the glow deepens to ~24% (`color-mix`). The icon is drawn in the row's guarded family color (`markerColor`) on colored rows and inherited monochrome on uncolored rows. The icon container keeps an explicit `z-10` (the stripe is a DOM sibling rendered before it) as a guard so any residual overlap layers icon-over-stripe deterministically inside the zone's own `z-20` stacking context, not by incidental sibling order. `aria-label="Set window label"` names the zone for pointer AT users and test selection.

**Marker vocabulary — 5 states, one display order**: `themes.ts` owns `MARKER_STATES = ["", "dotted", "dashed", "solid", "double", "thick"]` (empty first, then the display order dotted → dashed → solid → double → thick) and the `markerStripeStyle(state, color)` helper, shared by this stripe and the Label picker's marker cells so the stripe vocabulary lives in exactly one place. Rendering per state, all in the guarded family color (gray uncolored): `dotted` = 3px-wide column of 3px dashes / 3px gaps; `dashed` = 3px-wide column of 8px dashes / 4px gaps (12px period); `solid` = `border-left: 3px solid`; `double` = `border-left: 6px double`; `thick` = `border-left: 6px solid` (a continuous bar). Markers are available on all rows including uncolored ones.

**Marker semantics (user-owned, deliberately distinct from the status dot)**: markers and row colors are USER PREFERENCES — what the user thinks about a task — whereas status-dot states are DERIVED (activity/PR/agent signals, § Status Dot); the two axes never mix, by design. Three markers carry agreed meanings: `dashed` = **working** (an agent is actively working here — the data-rain pairing), `thick` = **completed** (done, taped off — the hazard-wedge pairing), `double` = **different** (this window IS different — what "different" means is the user's call; the scanline wash makes it the showcase mark). The remaining states — none, `dotted`, `solid` — are entirely free for per-user conventions (e.g. dotted = "still deciding whether to start an agent", solid = "made up my mind what to do"). None of this is wired to `@rk_agent_state` or any derived signal; the semantics are hand-set conventions only.

**Dashed data rain (always-on row overlay)**: a `dashed`-marked window row carries `.rk-dash-rain` — two sparse 1px dash tracks streaming LEFT→RIGHT across the row's full width in the guarded marker color: the ambient "agent is working" cue. Mounted on `isDashed` alone (`window-row.tsx`), in every state — "working" is inherently live, and the thinned two-lane rain proved quiet enough to run ambiently (a deliberate user call after watching the selection-gated version); the gutter stripe itself stays STATIC in every state. Same overlay discipline as scanlines/hazard: a dedicated clipped inner element (`absolute inset-0 z-[5] overflow-hidden pointer-events-none`), never the row root, reading `--rk-marker-color` from the root (set for dashed rows too). Each track has its own rhythm (7px dash/36px period at y 8px; 5px dash/28px period at y 16px) so the lanes drift at different speeds and read as rain, not a marquee. **Seamless loop rule**: each layer's tile is ONE FIXED PERIOD wide (`background-size: 36px 1px, 28px 1px` — never `100%`, which makes the tile element-width-dependent and snaps at the loop boundary), and the keyframes displace each layer by an integer multiple of its own period (72/56px over 2.8s), so the cycle point is invisible. `background-position` only, never transform (the drag-ghost rule). Under `prefers-reduced-motion` the rain hides entirely; the static label cue is the dashed stripe itself. The picker's marker preview cells never carry it.

**Marker stripe is display-only**: the marker renders as a left-edge stripe via `markerStripeStyle`, anchored near-flush at the sidebar's physical left edge — `left: STRIPE_EDGE_INSET = 4`px (`window-row.tsx`), `right: 0`. It is purely a display of current state — clicking it opens the picker; it does not cycle.

**One-period fixed stripe tiles**: the gradient cases (`dotted`, `dashed`) use a plain `linear-gradient` of exactly one period with `backgroundRepeat: "repeat-y"` — dotted `backgroundSize: "3px 6px"` (`${color} 0 3px, transparent 3px 6px`), dashed `backgroundSize: "3px 12px"` (`${color} 0 8px, transparent 8px 12px`) — NOT `border-left: dotted` (a dotted border distributes dots per-element and restarts at every row, so stacked dotted rows would show seams while solid/double merge) and NOT an element-height-dependent `repeating-linear-gradient` + `3px 100%` + `no-repeat` tile. Because each tile is exactly one period, the rhythm is correct at ANY element height (e.g. the 18px picker preview cells), not only at heights that happen to be period multiples; row rendering is pixel-identical because the 6px/12px periods divide the 24px (36px coarse) row heights exactly, so the rhythm carries across adjacent stacked rows.

**Marker/color plumbing**: `WindowInfo.marker?: string` (`src/types.ts`) carries the state from the `@rk_marker` window option (architecture.md § `@rk_marker`). `setWindowMarker(server, windowId, marker | null)` in `client.ts` delegates to `setWindowOptions({"@rk_marker": ...})` (empty/`null` clears). `sidebar/index.tsx` owns `onColorChange` + `onMarkerChange` handlers, prop-drilled through `ServerGroup` to `WindowRow`; the Label picker passes the **exact** picked color and marker state straight to these (no cycling — any state is one click). The `Window: Label` palette action opens this same picker via an imperative `label-popover:open` CustomEvent that the matching `WindowRow` listens for (`{ detail: { server, windowId } }`, mirroring the `pin-popover:open` pattern); the row registers both listeners in one `useEffect`. **Scope: window rows only** — session rows and server tiles get the owned-palette color affordance but no marker and no left-edge label zone.

### Easter Egg — Double-Marker Scanlines (`globals.css` `.rk-scanlines`)

A window row whose marker is `double` gets a static CRT scanline overlay across its full width, making `double` the one marker state readable from across the sidebar. Selecting such a row brings the scanlines alive.

**Static scanlines**: `.rk-scanlines::before` paints a `repeating-linear-gradient(to bottom, {marker color}@24% 0 1px, transparent 1px 3px)` reading the family color from the `--rk-marker-color` custom property (set on the row when `double`). The overlay is a **dedicated absolutely-positioned inner element** (`absolute inset-0 z-[5] overflow-hidden pointer-events-none`), NOT the row root — `overflow-hidden` on the root would clip the row's `top-full` pin/color popovers, so clipping lives on the overlay and the root stays free to overflow. The overlay sits above the tint/button background (`z-5`), below the `z-10` icon cluster and `z-20` gutter. Pure static CSS — it does not touch the status pyramid's animation channel (the waiting halo stays unambiguous).

**Selected + double retro animation**: when a double-marker row is ALSO selected, the class `rk-scanlines-crawl` is added, animating a slow downward crawl of the gradient period (`@keyframes rk-scanline-crawl`, `background-position-y` 0 → 3px, 2.4s linear infinite) plus an occasional CRT refresh band — a soft ~12px gradient band in the marker color rolling top→bottom via `::after` (`@keyframes rk-scanline-band`, 7s cycle with a rest phase then a ~2.8s sweep, `pointer-events: none`). Static again when deselected. **Both animations move `background-position-y`, never `transform`** — an animated translateY range grows the compositor layer beyond the row box (ancestor clips ignored), and the HTML5 drag-ghost snapshot of a selected+double row then carries a sliver of the neighboring row along with it; a background sweep cannot paint outside the element's own box. The band is a `no-repeat` `background-size: 100% 12px` gradient on an `inset-0` `::after`, parked at `background-position-y: -12px` (outside the box paints nothing) and swept to `calc(100% + 12px)` (band top at box bottom — fully exited).

**Reduced motion**: inside the existing `@media (prefers-reduced-motion: reduce)` block, `.rk-scanlines-crawl::before { animation: none }` and `.rk-scanlines-crawl::after { animation: none; display: none }` — the crawl and band are fully zeroed (static scanlines only, band hidden). The base rules PRECEDE the reduced-motion block so its equal-specificity overrides win by source order, mirroring the `rk-waiting-halo`/`rk-waiting-seam` discipline.

### Hazard-Wedge Row Texture — paired with `thick` (`globals.css` `.rk-hazard`)

A window row whose marker is `thick` gets a static "hazard wedge" background — the completed / "taped-off" cue that pairs with the thick marker exactly as the scanlines pair with `double`. It is **NEVER animated in any state** (rest, hover, selected) — an explicit user decision after animated variants were tried and rejected as distracting.

**Static hazard wedge**: `.rk-hazard::before` paints a 45° weave in the guarded marker color (read from the same `--rk-marker-color` custom property the scanlines use, set on the row when `thick`) at 18% alpha via `color-mix`, on a **12px square tile** (`background-size: 12px 12px`) so adjacent thick rows weld seamlessly. A `mask-image: linear-gradient(to right, #000 22%, transparent 55%)` (with its `-webkit-` twin) confines the weave to a **left-edge wedge** — full strength for the first ~22% of the row, fading out by ~55% — a taped-off corner, not full-row wallpaper (full-width was tried and rejected as louder than the selected row; an earlier 13% fade-from-zero-to-38% version tapered so aggressively it was hard to make out and was re-tuned by user request).

**Overlay-owns-clip, mirroring scanlines exactly**: the wedge is a **dedicated absolutely-positioned inner element** (`absolute inset-0 z-[5] overflow-hidden pointer-events-none`), NEVER `overflow-hidden` on the row root (which would clip the row's `top-full` pin/color popovers — the same must-fix the scanlines documented). It sits above the tint/button background (`z-5`), below the `z-10` icon cluster and `z-20` gutter. `window-row.tsx` sets `--rk-marker-color` inline for `thick` rows the same way it already does for `double`. No `prefers-reduced-motion` gate is needed (it is static), but it stays listed in the reduced-motion audit block beside the adjacent scanlines block.

### Braille Sparkline Renderer

`app/frontend/src/lib/sparkline.ts` — converts an array of float values (0-100 range) into a Unicode braille sparkline string. Uses 8 vertical levels from the U+2800-U+28FF braille range filling bottom-to-top: `⣀⣄⣤⣦⣶⣷⣾⣿` (level 0 = `⣀`, level 7 = `⣿`). Values linearly interpolated across 8 levels. Zero-filled buffer renders as repeated `⣀`. Exported as `sparkline(samples: number[]): string`.

### Memory Gauge Renderer

`app/frontend/src/lib/gauge.ts` — utilities for the memory gauge visualization:
- `gaugeBar(ratio: number): string` — builds a filled/empty block string (`█` filled, `░` empty) from a 0-1 ratio. Fixed width of 10 characters
- `gaugeColor(percent: number): string` — returns a Tailwind color class: `text-green-500` (< 70%), `text-yellow-500` (70-90%), `text-red-500` (> 90%)
- `formatBytes(bytes: number): string` — compact human-readable size (`3.1G`, `512M`, `128K`)
- `formatMemory(used: number, total: number): string` — compact `used/total` string (e.g., `3.1G/8G`)

### Clipboard Utility

`app/frontend/src/lib/clipboard.ts` — shared `copyToClipboard(text: string): Promise<boolean>` function. Primary path uses `navigator.clipboard.writeText()`; fallback uses `document.execCommand('copy')` for non-secure contexts (HTTP). All callers (terminal copy, Pane panel row copy) import from this module (§ Steady-state version surfaces for the boolean success signal).

CWD display (line 1) uses `shortenPath()` to shorten the active pane's `cwd` (falls back to `worktreePath`):
- Home substitution: `/home/<user>/…` → `~/…`, `/Users/<user>/…` → `~/…`, `/root/…` → `~/…` (exact home dir → `~`). Handles Linux and macOS conventions.
- Truncation: if the path (after home substitution) has more than 2 non-empty segments, it is truncated to `…/<second-to-last>/<last>`. Paths with ≤ 2 segments are not truncated.
- Examples: `/home/sahil/code/org/repo/src` → `…/repo/src`; `/home/sahil/code/org` → `~/code/org`; `/var/log/nginx` → `…/log/nginx`.
- The `title` attribute on the CWD element always contains the original unmodified `activePaneCwd` — hover to see the full path.

## Live Safe-Name Conversion

Every naming entry point converts a user-typed name to its safe/canonical form **live, in the input's `onChange`** — the user watches "My problem" become "My_problem" as they type (WYSIWYG), so the optimistic-update name is identical to the committed name. The conversion is **per name kind**, sourced from the shared pure module `app/frontend/src/lib/names.ts` (the established `src/lib/*.ts` + colocated `*.test.ts` home for shared pure logic), which exports one transform per kind plus a commit finisher:

| Export | Rule |
|--------|------|
| `toSafeSessionName(raw)` | spaces, **hyphens**, colons, periods, and the backend forbidden set (`; & | ` backtick ` $ ( ) { } [ ] < > ! # * ?` + control chars) → `_`; collapse `_` runs; strip leading `_`; **preserve case**; cap 128 (`MaxNameLength`). Hyphen→`_` is session-specific (session-group collision avoidance). |
| `toSafeWindowName(raw)` | same as session but **keeps hyphens** — `riff-*` windows use them legitimately and the group-collision rationale does not apply. |
| `toSafeServerName(raw)` | anything outside `[a-zA-Z0-9_-]` → `_`; collapse; strip leading `_`; cap 64 (`MaxServerNameLength`). Matches backend `ValidateServerName`. |
| `toSafeWorktreeName(raw)` | the window rule plus `/`→`_` and a leading `-` dropped (matching `ValidateWorktreeName`). |
| `finalizeSafeName(name)` | commit-time finisher — strips leading **and trailing** `_` runs. |
| `deriveNameFromPath(p)` | path-derived session-name suggestion — extracts the last path segment, composes the session transform + `finalizeSafeName` (both ends trimmed, matching the old `toTmuxSafeName`). |

Case is preserved ("My problem" → "My_problem", never lowercased). Chars outside a kind's unsafe set (e.g. `@`, `%`, `+` for session/window names) pass through unchanged — this is conversion, not stripping.

**Live-typing edges** (§ Design Decisions → Live transform + commit finalizer split): the live `onChange` transform strips **leading** unsafe chars as typed (a space pressed in an empty field produces nothing) and collapses `_` runs live, but a **trailing** `_` stays visible while typing — trimming it live would delete the separator the user just typed ("My " + "p" would become "Myp"). Commit/submit sites run `finalizeSafeName` to trim the trailing `_`. An input that is empty after conversion falls through to the existing empty-name submit guards — no new error surface. There is no caret-position management beyond React-controlled-input defaults; a mid-string length-changing edit may move the caret to the end (an accepted edge).

**Wired entry points** (each applies the kind's transform in `onChange` and `finalizeSafeName` at commit):

| Surface | File | Kind |
|---------|------|------|
| Create-session dialog typed-name field | `create-session-dialog.tsx` | session |
| Session rename dialog input | `app.tsx` (state in `use-dialog-state.ts`) | session |
| Sidebar inline session rename | `sidebar/session-row.tsx` | session |
| Top-bar `WindowHeading` inline rename (also the palette's "Window: Rename" CustomEvent path — § Window Heading) | `top-bar.tsx` | window |
| Sidebar inline window rename (commits via shared `use-window-rename.ts`) | `sidebar/window-row.tsx` | window |
| "New iframe window" name field | `app.tsx` (`handleCreateIframeWindow`) | window |
| Host Overview create-server dialog | `host-overview-page.tsx` | server |
| Create-server dialog (the single layout-mounted `ServerDialogs`) | `components/server-dialogs.tsx` | server |
| Spawn/riff worktree-name field | `spawn-agent-dialog.tsx` | worktree |

The create dialog's **window mode has no name input** (windows are created unnamed and tmux auto-names them — § Unnamed `+ New Window`), so there is nothing to wire there.

The backend enforcement side of this contract (the tightened `ValidateNewName` for created/renamed-to names, permissive lookups for existing names) lives in [tmux-sessions](/run-kit/tmux-sessions.md) § Name-Validation Charset Contract and [architecture](/run-kit/architecture.md) § API Layer.

## Session Creation Pattern

### Instant Creation (Primary)

All primary session creation entry points create a session immediately without a dialog. Implemented by `executeCreateSessionInstant` in `app.tsx`.

**Algorithm**:
1. Derive a name from the active window's `worktreePath` using `deriveNameFromPath(worktreePath)` (from the shared `@/lib/names` module — § Live Safe-Name Conversion). If the result is empty (CWD is `/`, `~`, or `worktreePath` is undefined), the name is `session`.
2. Deduplicate against `sessions`: if the name is taken, try `{name}-2`, `{name}-3`, … up to `{name}-10`. If all are taken, use `{name}-11` (best-effort).
3. Call `createSession(server, derivedName, worktreePath)`. If no active window exists, call `createSession(server, "session")` (no `cwd` — tmux defaults to server CWD).
4. The session appears in the sidebar via the existing optimistic/ghost mechanism.

**Entry points** (all call `onCreateSession` → `executeCreateSessionInstant`):
- Sidebar `+` button (session level)
- Sidebar empty-state `+ New Session` button
- `SessionTiles` "+ New Session" dashed-border tile (the density view on `/$server`)
- Top-bar breadcrumb session dropdown `+ New Session` item
- Cmd+K "Session: Create" action

**Name derivation utilities**: `deriveNameFromPath` and the per-kind safe-name transforms live in the shared `app/frontend/src/lib/names.ts` module (§ Live Safe-Name Conversion), imported by `app.tsx` and every naming surface. Name conversion is split per kind (`toSafeSessionName`/`toSafeWindowName`/`toSafeServerName`/`toSafeWorktreeName`) — there is no single `toTmuxSafeName`.

### Folder-Prompted Creation (Secondary, via Cmd+K)

Two secondary entry points open `CreateSessionDialog` for users who want to specify a starting directory:

- **"Session: Create at Folder"** — opens `CreateSessionDialog` (mode `"session"`, default). The dialog's path input is pre-filled with `currentWindow.worktreePath` via the `defaultPath?: string` prop added to `CreateSessionDialogProps`. If no active window, the field starts empty.
- **"Window: Create at Folder"** — opens `CreateSessionDialog` with `mode="window"` and `session={currentSession}`. In window mode: title changes to "Create window at folder", session name input hidden, confirming calls `createWindow(server, session, undefined, cwd)` — **no name**, so tmux auto-names the window to the chosen folder's basename via `automatic-rename-format` (see [tmux-sessions](/run-kit/tmux-sessions.md) § Managed Window Creation). This flow sets no optimistic ghost, so there is no ghost label to derive. (`260707-j66b`)

`CreateSessionDialog` gains three optional backward-compatible props:
- `defaultPath?: string` — pre-fills the path input
- `mode?: "session" | "window"` — controls dialog behavior (default `"session"`)
- `session?: string` — required in window mode to pass to `createWindow`

### Deprecated: Dialog-First Flow

There is no `showCreateDialog` / `openCreateDialog` / `closeCreateDialog` API in `use-dialog-state.ts`; `CreateSessionDialog` is not opened by the sidebar `+` button or the primary "Session: Create" palette action. Use "Session: Create at Folder" in Cmd+K for folder-prompted session creation.

### Unnamed `+ New Window` — Folder Auto-Naming

The `+ New Window` action does **not** pin the window name to `"zsh"` — it calls `createWindow` with **no name**, so tmux auto-names the window to its folder basename and live-updates it (the tmux-layer convention lives in [tmux-sessions](/run-kit/tmux-sessions.md) § Managed Window Creation). A hardcoded name would leave a sidebar full of windows as indistinguishable "zsh" rows. (`260707-j66b`)

**All three unnamed-window call sites:**

1. **Sidebar `+ New Window`** (`app.tsx`, the `create-window` "Window: Create" palette action) — `createWindow(srv, session, undefined, activeWin?.worktreePath)`. Its optimistic-ghost label becomes the **raw** (unsanitized) basename of the creation cwd via the local `rawBasename(cwd)` helper (falls back to `"window"` when no cwd). This is deliberately **NOT** `deriveNameFromPath` — that runs the session safe-name transform (`.`→`_` etc.), but tmux's `#{b:pane_current_path}` uses the unsanitized basename, so the ghost must match the raw form tmux will actually display.
2. **Palette "Window: Create at Folder"** (`create-session-dialog.tsx`, `mode="window"`) — `createWindow(server, session, undefined, cwd)`; this flow sets **no** optimistic ghost, so no label is derived.
3. **Board-route `+ New Window`** (`board-page.tsx`) — `createWindowApi(srv, sess)` (no name, no cwd — the backend resolves a default; this action registers no ghost). Identical behavior to the same button on `/$server`.

**Client contract**: `createWindow(server, session, name?, cwd?, rkType?, rkUrl?)` in `api/client.ts` makes `name` optional and **omits it from the JSON body when absent/empty** (matching the existing omit-when-absent handling for `cwd`/`rkType`/`rkUrl`). Explicit names are still sent — the deliberate-name paths are unchanged: **iframe/service windows** (`Window: New Iframe Window`, `createWindow(..., name, undefined, "iframe", url)`), the `port-N` "Open in window" service windows (`server-list-page.tsx`), and `rk riff`.

**E2E** (`app/frontend/tests/e2e/new-window-unnamed.spec.ts` + sibling `.spec.md`): asserts, via route interception on `POST /api/sessions/*/windows`, that the `+ New Window` request body carries **no `name` key** — a deterministic frontend→API seam. (The visual auto-rename is NOT asserted: the e2e tmux server is created with a bare `new-session` and `CreateWindow` never sources the config there, so `automatic-rename-format` isn't reliably applied on that server; the tmux-native naming is covered by the config + Go arg-builder tests instead.) (`260707-j66b`)

**Ghost TTL backstop** (`GHOST_WINDOW_TTL_MS = 15_000`, exported from `src/store/window-store.ts`): an optimistic ghost row is **claimed** only when a NEW windowId (one not in the ghost's `snapshotWindowIds`) arrives for its `(server, session)` via SSE (`setWindowsForSession`, oldest-ghost-first). Ordinary creates confirm within one SSE tick (~1-2s), and a *failed* create's caller rollback calls `removeGhost`. But if a create **succeeds somewhere else** — e.g. a tmux target ambiguity routes the window into a different session (exact-match session targets prevent this at the backend, see [tmux-sessions](/run-kit/tmux-sessions.md) § Exact-Match Session Targets) — no rollback fires and no claim ever arrives, stranding a permanently greyed, pulsing, unclickable row. `addGhostWindow` therefore arms a `setTimeout(() => get().removeGhost(optimisticId), GHOST_WINDOW_TTL_MS)`: the unclaimed ghost self-clears after 15s (comfortably past any legitimate claim). `removeGhost` is **idempotent** (filters by `optimisticId`), so a timer firing after the ghost was already claimed or rolled back is a no-op. This is a generic frontend backstop for the "no rollback, no claim" strand, independent of the backend root-cause fix. Two fake-timer unit tests cover TTL expiry and the claimed-ghost no-op. (`260717-hikh`)

## Bottom Bar (Shell-level, Shared Across Routes)

Single row of `<kbd>` styled buttons, rendered at shell level via `<Shell>`'s `bottombar` grid area on every route that mounts `<Shell>` — single-terminal, board, and tmux Server (no-window) alike. Width tracks the right grid column (page width minus sidebar). Styled with `border-t-[3px] border-border` (the 3px structural seam — see § Border-Width System) and `px-1.5 h-[48px]`. Layout: `Tab Ctrl Alt Fn▴ ArrowPad | ⌘K >_ ⌨`. The hostname is shown in the sidebar Host panel, not the bottom bar; Escape lives in the Function key dropdown's extended-keys section. The `>_` button (conditionally rendered when `onOpenCompose` is provided) is an `aria-pressed` accent toggle for the docked compose strip (§ Docked Compose Strip), not a modal trigger. The compose chip is **rightmost of the fine-pointer chip run** — the palette `⌘K` chip renders before it, putting the higher-touch control at the end-of-row position. (`260509-17m3`, `260702-6m46`, `260811-0f3d`)

`BottomBarProps` contains no `wsRef`. Input handlers read `focused?.wsRef` from `FocusedTerminalContext` and send ANSI escape sequences through it. The component is byte-identical across single-terminal and board routes (same JSX, same callbacks: `onOpenCompose`, `onFocusTerminal`, `onScrollLockChange`) — board panes are terminals, so the input toolbar applies identically. When `focused === null` (Dashboard route, no active terminal), input handlers natural-no-op via the existing `wsRef.current?.readyState !== OPEN` guard — no error, no toast.

**Modifier toggles** (Ctrl, Alt): Sticky armed state with visual indicator (`accent` bg). Click to arm, auto-clears after next key is sent. Click again while armed to disarm. Multiple modifiers can be armed simultaneously. No Cmd (`⌘`) toggle — on desktop users hold the real Cmd key; on mobile Cmd combos aren't used in terminal workflows.

**Armed modifier bridging**: When modifiers are armed, a capture-phase `keydown` listener intercepts physical keypresses and translates them to terminal escape sequences (Ctrl+letter → control characters, Alt → ESC prefix). Sends via WebSocket, preventing xterm from receiving the unmodified key. Ignores real Cmd/Ctrl/Alt held by the OS.

**ArrowPad** (`arrow-pad.tsx`): Combined directional pad replacing individual arrow buttons. Sends ANSI escape sequences (`[A/B/C/D`). With modifiers, use xterm parameter encoding (`[1;{mod}X`). Modifier parameter: 1 + (alt?2:0) + (ctrl?4:0).

**Function key dropdown** (F▴): Opens a combined popup above the button. Top section: F1-F12 in a 4-column grid. Divider (`border-t border-border`). Bottom section: Esc, PgUp, PgDn, Home, End, Ins, Del in a 3-column grid (3x3, 7 items). Escape uses `sendSpecial` (preserves Ctrl re-arm semantics); other extended keys use `sendWithMods`. Closes after each selection, on outside click, or on Escape.

**Special keys** (Tab in bottom bar, Esc in Fn menu): Direct send via `sendSpecial`. Ctrl is not consumed for Esc/Tab (Esc IS Ctrl+[, Tab IS Ctrl+I in terminal semantics) — Ctrl stays armed for the next key. Alt prefix with ESC (Meta convention).

**All buttons**: 33px min-height / 35px min-width on fine pointers (`min-h-[33px] min-w-[35px]` — reduced from 36×36 to give the row air), full 36×36 touch target on coarse pointers (`coarse:min-h-[36px] coarse:min-w-[36px]`). Row gap is `gap-1.5` (6px) on fine pointers, `coarse:gap-1` (4px) on touch — so the 375px single-row budget is byte-identical to before. `text-xs`, `<kbd>` element styling. The row's vertical padding is asymmetric — `pt-1.5` with the bottom arm reading `max(var(--bottom-bar-floor,0.375rem),env(safe-area-inset-bottom))`: 6px everywhere except coarse pointers with the keyboard collapsed, where the `globals.css` floor rises to 1rem to clear the phone's corner-arc/home-indicator zone (§ Mobile Responsive → Safe-Area Insets).

**Chip tooltip chords** (`260801-mqim`): the `>_` Compose and `⌘K` palette chips carry **registry-resolved** `kbd` chips in their tips — `BottomBar` consumes `useKeybindings()` once and a local `chordFor(actionId)` helper applies the shared enabled-else-`undefined` derivation (§ Tier-1 `Tip`) for `compose-toggle` and `command-palette`. So the palette tip reads ⌘K on a mac and `Ctrl+K` on Windows/Linux, and both reflect overrides. **The ⌘K chip's own button FACE is the literal `⌘K` glyph on every platform** — the glyph is the chip's identity, not a chord claim; only the tooltip resolves. The remaining non-registry `kbd` strings in this area are the compose/chat submit (`composeSubmitKeycap()` → `⌘Enter` / `Ctrl+Enter`) and the per-surface Insert chips (`Enter` on the strip, `Alt+Enter` in chat), editing chords the registry does not own.

**Compose education hint** (`260811-0f3d`): the dead space right of the chip pair (before the `ml-auto` coarse cluster) carries a dimmed, non-interactive (`aria-hidden`, `select-none`, `opacity-60`) line reading `>_ compose — type here, send to the pane`, with the effective `compose-toggle` chord appended as a keycap (the same `chordFor` derivation as the chips; the keycap is omitted when the binding is unbound while the text stays — § Education micro-copy's clause-omission rule). Gates, ALL required: `onOpenCompose` present, a live compose target (`focused !== null`), the compose strip OFF (`!composeStripEnabled` — educate-toward, not a label: once the strip is open the feature has been found), fine pointer (`!useCoarsePointer()`), and viewport ≥ `lg` (`hidden lg:flex` — the 375px single-row budget is hard, § Mobile Responsive Design). The **target gate carries the copy's promise**: `app.tsx` wires `onOpenCompose` unconditionally, so the prop alone would let the line advertise "send to the pane" on routes with no pane to send to (the strip itself degrades to `No focused terminal — click a pane to target it` there). A selection-broadcast target needs no clause — starting one always turns the strip ON, and the hint is strip-off only. The hint never intercepts input and names the same facts the chip's tip does, so it adds no new state to maintain.

**Focus preservation**: All bottom bar buttons that send terminal input or toggle modifier state have `onMouseDown={(e) => e.preventDefault()}` via a shared `preventFocusSteal` handler. This prevents the browser from shifting focus away from xterm.js's hidden textarea when buttons are tapped, keeping the on-screen keyboard visible on iOS/touch devices. The CmdK button is excluded (it intentionally opens a dialog that takes focus). The ArrowPad handles focus preservation independently via its own `onMouseDown` handler.

### Docked Compose Strip

`ComposeStrip` (`components/compose-strip.tsx`) is a single **global**, sticky text-input surface docked at the bottom of the terminal area, immediately **above** the bottom-bar keys. It is a plain `<div>` in the shell footer — no backdrop, no `role="dialog"`/`aria-modal`, no focus trap, no Escape-closes, no close-on-send, no terminal dimming. A real `<textarea>` gives IME/dictation (xterm is a `<canvas>`, so the OS has no text input to attach to) and a stable home for pasting large text blocks over a laggy relay. Autocorrect, autocapitalize, and spellcheck are explicitly OFF on it — the text goes to a shell, where rewriting a path or a flag is a bug.

**Two-row stack.** The input surface below the `→ {target}` header row is a `flex flex-col gap-1.5` **stack**, not a single button-flanked row: **row 1** is the `<textarea>` alone at `w-full`, **row 2** is a `flex items-center gap-1.5` button row holding 📎 at the left and an `ml-auto` group carrying Insert + Send at the right. The textarea's default is **`rows={2}`** on every device (desktop included), so the strip opens at two lines. The bounded auto-grow (`MAX_TEXTAREA_ROWS = 6`, scrollHeight-based `resize()`) treats that box as its **floor**: the `height = "auto"` measurement resolves to the `rows` attribute, so growing past two lines and deleting back settles at two rows, never one. The right group is an `ml-auto` container rather than a spacer element, which leaves the Tip-wrapped Insert/Send buttons untouched. (`260724-2bmy`)

**Two distinct send paths** (do not conflate): the compose strip rides the **relay WebSocket as raw bytes** — a submit sends `text + "\r"`, an insert-line sends `text + "\n"`, a raw insert sends bare `text` (no trailing byte), over the focused terminal's `wsRef` (the muxed RelayMux stream, same path as BottomBar keystrokes), no backend endpoint. The **chat-send** path is entirely separate — `POST /api/windows/{id}/chat/send` with named-buffer paste + NOVELTY echo probe + probe-and-submit-gated Enter + per-`(server,paneID)` lock (see [chat](/run-kit/chat.md) § send path). Compose does NOT use that machinery. The two surfaces DO share the keydown layer — one surface-parameterized `classifyComposeEnter` Enter policy plus one `handleReadlineKey` editing layer — but not the delivery mechanism, and not plain Enter's meaning (§ Send semantics).

**Toggle + persistence.** The strip's enablement is the `composeStripEnabled` chrome preference (ChromeContext, `localStorage["runkit-compose-strip"]`, `"true"` sentinel — the `terminalFontSize`/`fixedWidth` pattern; see § Terminal Font Size). Four parity affordances flip it via the SAME `toggleComposeStrip()` action: the ⇧⌘E / Shift+Ctrl+E `compose-toggle` chord (§ Keyboard Shortcuts — `ignoreInputs`, so it also closes the strip from inside the strip's own textarea; mounted on both AppShell and the board twin), the bottom-bar `>_` chip (`aria-pressed={composeStripEnabled}` + the ctrl/alt pressed-state vocabulary `bg-accent/20 border-accent text-accent` when on, `text-text-secondary` off; `preventFocusSteal` preserved), the `View: Text Input` palette action, and an **on-strip × close button** in the strip's own header row (`data-testid="compose-strip-close"`, `aria-label="Close compose strip"`). The × is a **pointer convenience** only — the chip and palette already satisfy Constitution V, so it adds no new keyboard shortcut. It consumes `toggleComposeStrip` from `useChromeDispatch()` *inside* `ComposeStrip` (not threaded as a per-route prop), so both footer mounts inherit the close affordance with zero per-route work. It sits at the **far right** of the `→ {target}` header row, grouped with the conditional "Uploading…" status in a single `ml-auto` flex container so the × stays right-aligned whether or not the upload status renders (uploading immediately left of it). It follows the strip's secondary-button vocabulary (`rk-glint`, bordered, `text-text-secondary`, hover `border-text-secondary`, compact `text-xs`/`px-1.5 py-0.5 leading-none`), carries `onMouseDown={preventFocusSteal}` like every other strip button, and takes the coarse-pointer touch treatment on **both** axes (`coarse:min-h-[36px] coarse:min-w-[36px]` — a bare × has no horizontal padding, so it needs `min-w` too, unlike the wider padded buttons that use only `min-h`; NOT the tiny 16px attachment-remove `×` pattern). Closing needs **no confirmation** — it is lossless because the draft (text + attachments) lives in the module store under its target's key and survives toggle-off/on (see **Drafts are per-target and survive refresh**). The caller gates the mount (`{composeStripEnabled && <ComposeStrip />}` in `app.tsx` and `board-page.tsx` footers). Toggling grows/shrinks the footer grid row by the strip's height, which the terminal container's existing `ResizeObserver` picks up → `fitAndSync` (FitAddon `fit()` + tmux resize) — the same reflow class as a font-size change; no dedicated refit effect.

**Live target.** The strip sends to the **currently-focused** pane's `wsRef` from `FocusedTerminalContext`, read live at send time — never a target snapshotted at open. A persistent surface that outlives many focus changes would, with a frozen target, silently send to a stale/closed pane. The wrong-pane risk is instead mitigated by an always-visible `→ {window}` target label, resolved in layers: window-store name by `server:windowId` (live — tracks renames) → the `windowName` the registrant carried into `FocusedTerminalContext` (board entries have a server-derived `windowName`; the store only covers servers whose sidebar group has delivered sessions, so board panes from other servers would otherwise label as `@N`) → raw windowId. With `focused === null` (e.g. the `/$server` tiles route) the strip renders **disabled** with a "no target" label and never sends; its placeholder reads `No focused terminal — click a pane to target it` (both pointer types — the clause names no chord, § Education micro-copy). The terminal-target placeholder is itself an education surface: `Compose text — Enter inserts · {submitKeycap} sends · ↑ history` on fine pointers (the sole surfacing of ↑ sent-history recall anywhere in the UI) and the short `Compose text…` on coarse — `{submitKeycap}` via the shared `composeSubmitKeycap()` seam (the fixed Enter-policy chords are classifier-owned, not registry bindings).

**Selection-broadcast target — the one FROZEN mode.** The optional `selectionTarget?: {keys, onSend}` prop (`ComposeSelectionTarget`) puts the strip in a second, explicit target mode, fed by `Selection: Send prompt to N agents` (§ Selection palette commands). While `keys` is non-empty the strip labels itself `→ N selected` instead of the focused window, keys its draft to the frozen recipient set, and submits through `onSend(text)` rather than any `wsRef` — the only mode where the target is snapshotted rather than read live (§ Design Decisions → Broadcast recipients are frozen). It is **submit-only**: `send()` early-returns for every non-submit mode, so the Insert chip is disabled (`canInsert` is false in selection mode) and Alt+Enter's raw insert is left NATIVE rather than consumed into a silent no-op; the 📎 upload button and the drag-drop/paste attach path are disabled too (`canUpload` gates them separately from `hasTarget`, since cross-server recipients share neither a websocket nor a worktree). Enter policy follows suit: the keydown calls `classifyComposeEnter(…, "chat")` in selection mode, so **plain Enter is a local newline**, Cmd/Ctrl+Enter and the Send button are the only submits, and `enterKeyHint` reads `"enter"` — the surface parameter is what diverges, never a fork of the classifier's policy at this call site. Send is gated on non-empty text (`canSubmit`) and on a `selectionSending` in-flight flag that also relabels the chip `Sending…`, so a second Enter cannot double-broadcast. The `aria-label` becomes `Compose prompt to send to selection` and the placeholder `Compose prompt — {submitKeycap} sends to all selected` on fine pointers (the short `Compose prompt…` on coarse — § Education micro-copy; broadcast takes the chat Enter policy, so the placeholder names `{submitKeycap}` as the sole submit). Clearing is the one behavior that is **not** unconditional here: `onSend` resolves with the DELIVERED recipient count, and only a non-zero count pushes sent history and clears the draft — a `0` (nobody received it) keeps the composed prompt, and an unexpected rejection escaping the executor is caught and likewise preserves it (§ Design Decisions → A broadcast clears its draft on DELIVERY). (`260808-ebgs`)

**Send semantics — Enter transmits a line, Cmd/Ctrl+Enter submits.** The keydown routes through the shared pure `classifyComposeEnter(key, surface)` (`lib/compose-keys.ts`) — the SAME classifier the chat send form uses, called here with `surface: "strip"`. One classifier remains the single authority for both surfaces' Enter policy, but the two **deliberately diverge on plain Enter**, and the divergence is declared inside the classifier per surface, never forked at a call site (see § Design Decisions → Enter transmits a line in the strip, composes in chat, and [chat](/run-kit/chat.md) § Send-form input box). The classifier takes key state + surface alone — Enter policy is pointer-independent, so it reads no pointer hook — and stays text-agnostic (empty-text policy lives at the call sites). `send(mode)` takes `Exclude<ComposeEnterAction, "default">` and delivers over the focused terminal's `wsRef`, guarded on `draftKey !== null` and `wsRef.readyState === WebSocket.OPEN`:
- **Enter = insert line** — `ws.send(text + "\n")` and clear that target's draft. The strip overlays the VISIBLE terminal, so the transmitted line lands in the pane's own composer where the user can see it (Claude Code treats a raw `"\n"` as newline-insert), and consecutive Enters stage sentence-per-line exactly like typing into the pane. On an **empty or whitespace-only** textarea Enter is a **full no-op**: the keydown is consumed (no local newline appears) and nothing is sent.
- **Shift+Enter = newline** — the ONLY local multi-line compose in the strip.
- **Cmd/Ctrl+Enter = submit** — `ws.send(text + "\r")`; on an **empty or whitespace-only** textarea a bare `ws.send("\r")` ("press Enter in the pane"), which completes the stage-then-submit loop from the keyboard. Whitespace-only counts as empty: the whitespace is discarded, never transmitted, and the draft still clears.
- **Alt+Enter = raw insert** — chord-only. `ws.send(text)` with **NO trailing byte** (byte-exact, for completing a partial line already in the agent's input); empty/whitespace-only is a no-op.
- Precedence (first match wins): non-Enter/IME-composing → default; meta/ctrl → submit; alt → insert; shift → default; plain Enter → insert-line (strip) / default (chat).

Enter is guarded against IME composition (`e.nativeEvent.isComposing`). A `"default"` action falls through to the textarea; every other action gets `preventDefault()` + `stopPropagation()` and is handed to `send(action)` as-is. Clear-on-delivery is uniform across modes — a delivered send clears that target's text + attachments (and revokes its preview URLs) and the strip stays open, including a delivered whitespace-only submit. A **guard-blocked** send (no target / stream not open) early-returns WITHOUT clearing, so the draft is preserved. Terminal-conventional `"\n"` semantics are **accepted and documented in the send-path code comment, not guarded**: on a plain shell pane the transmitted `"\n"` EXECUTES the line — which is exactly what Enter does in a terminal — and an embedded `\n` inside a raw insert executes per line there too, so raw insert is only truly Enter-free for single-line text on non-TUI panes.

**Readline editing chords** (`lib/readline-keys.ts` — shared with the chat send form, the consistency mirror of the shared Enter classifier). The textarea keydown routes through `handleReadlineKey(e.nativeEvent, e.currentTarget)` **before** Enter classification (after the strip's Escape branch), intercepting exactly five chords that browsers/macOS leave unbound: **Ctrl+U** (kill from cursor to line start — readline unix-line-discard, not a whole-line wipe), **Ctrl+W** (delete word backward), **Alt+B / Alt+F** (move cursor word backward / forward), **Alt+D** (delete word forward). Matching is on `KeyboardEvent.code` (`KeyU`/`KeyW`/`KeyB`/`KeyF`/`KeyD`), never `key` — on macOS Alt+B/F/D compose `∫`/`ƒ`/`∂` into `key`, so key-based matching would miss exactly the chords the layer exists for. Modifier matching is exact (Meta or Shift anywhere → unhandled; Ctrl chords reject Alt and vice versa) and IME-composing keydowns are never intercepted, so the natively-bound Cocoa chords (Ctrl+A/E/B/F/P/N/D/H/K/T/O/Y, Opt+arrows, Opt+Delete) pass through untouched. Word boundaries are whitespace-delimited (the readline default), crossing newlines; `lineStart` keys on the previous `\n`. Motions use `setSelectionRange`; deletions go through `document.execCommand("delete")` after selecting the range so the native undo stack (Cmd+Z) survives, with a controlled-component-safe fallback (prototype value setter + a bubbled `input` event) when `execCommand` is missing or returns false (jsdom, future removals) — that fallback keeps React state in sync at the cost of undo for that edit. A handled chord calls `preventDefault()` + `stopPropagation()` (so it never reaches global chord listeners) and returns `true`; an **empty** edit range (Ctrl+U at line start, Ctrl+W at position 0) still consumes the chord but performs no edit, because a collapsed-selection `execCommand("delete")` would eat a character backward. Conflict safety: app chords are suppressed while a textarea has focus (`shouldSuppressChord`), the `ignoreInputs` punch-through set (⌘K, ⇧⌘E, ⇧⌘/, ⇧⌘,) does not collide, and Alt chords are excluded from every registry tier (§ Keyboard Shortcuts). The one near-miss is `KeyW`: `kill-window` claims it on the `cmd` (⌘W) and `shifted` (⇧Ctrl+W) tiers — never plain Ctrl+W — so the exact-modifier rule (Meta or Shift → unhandled) is what keeps the two from overlapping rather than luck. **Platform caveat**: on Windows/Linux *browsers* Ctrl+W is browser-reserved (closes the tab before the page sees it) — that binding works on macOS and in the desktop shell only; win/linux web users keep native Ctrl+Backspace. Ctrl+U (view-source) IS interceptable and works everywhere. The pure classification + cursor math (`classifyReadlineKey`, `wordLeft`/`wordRight`/`lineStart`, `readlineMotionTarget`, `readlineDeleteRange`) are exported for mount-free unit tests (the `palette-move.ts` extraction pattern).

**Buttons + truthful `enterkeyhint`.** The two chips mirror their chords, so their enable conditions **split** rather than sharing one flag. A secondary **Insert** house-chip (border-styled, `data-testid="compose-strip-insert"`, `aria-label="Insert line"`, `preventFocusSteal`, `coarse:min-h-[36px]`) renders left of Send and fires `send("insert-line")` — it follows Enter, so it is disabled when there is no target or the text is empty/whitespace-only (`canInsert`), matching Enter's empty no-op. **Send** fires `send("submit")` and is enabled whenever a target exists (`hasTarget`) — an empty click sends the same bare `"\r"` its chord does, because a button that advertises a chord and then diverges from it on empty would be a lying affordance. Tips: Insert reads `label="Insert line (Alt+Enter: raw insert)"` with `kbd="Enter"` — the chord-only raw insert stays discoverable in the label text, with no native `title` (titles are retired for Tip chips); Send keeps the platform-formatted submit chord from the shared `composeSubmitKeycap()` (`⌘Enter` on mac, `Ctrl+Enter` elsewhere, via `formatCombo({code:"Enter",tier:"cmd"}, detectPlatform())`), so both surfaces render the identical chip. None of these chords are registered in the command palette (focused-textarea editing chords like the unregistered Shift+Enter; the palette steals focus from the textarea it would act on), and Constitution V's keyboard reach is met by the chords themselves. The textarea's `enterKeyHint` is **`"send"`** unconditionally — the truthful hint, since Enter transmits the text to the pane and clears the draft. `compose-strip.tsx` consumes no pointer hook at all. (`260802-lj98`)

**Drafts are per-target and survive refresh.** Every draft belongs to its **addressee**, not to the user: the store (`lib/compose-draft-store.ts`) is a map keyed by the send target's `entryKey(server, windowId)` — the same key the strip's `focusedKey()` computes from the live focused target each render. Switching targets (route change or board pane-focus cycling) swaps the visible draft; each window keeps its own unsent text + attachments, and a delivered send clears only that target's draft. A **selection-broadcast** draft is the one exception to live keying: it keys on `selectionDraftKey(keys)` — `selection:` plus the JSON of the SORTED recipient set — which cannot collide with any `server:@N` window key, so composing a broadcast never overwrites the focused pane's draft. With no target the key is `null`, the store yields a single frozen empty draft, and the key-bound setters no-op (the strip is disabled anyway). The store lives at module scope — not component state — because the mount is conditional (toggle-off unmounts it) AND per-route (the AppShell and BoardPage footers are distinct React subtrees), so component-local `useState` would destroy unsent drafts on toggle-off and on terminal↔board route changes. It is exposed through the same module-slot + listener-set + stable-snapshot pattern as the window-switch pending mask (`window-transition.ts`), read as `useSyncExternalStore(subscribeComposeDraft, () => getComposeDraft(draftKey))`: one **global** listener set (any commit notifies — at most one strip is mounted) over **per-key** stable snapshots, so `useSyncExternalStore` sees an identical object reference while a key's draft is unchanged. Commits notify only on an actual change. (`260718-dhdj`, `260801-cyth`)

**Text persistence — one localStorage key, pruned on every write.** Draft **text** survives a page refresh via `localStorage["runkit-compose-drafts"]`, a JSON map `{[entryKey]: {text, updatedAt}}` written through **synchronously** on every commit and hydrated at module load by the exported `hydrateComposeDrafts()` (also the test-reset seam: `localStorage.clear()` + re-hydrate). The read is tolerant in the `parseOverrides` posture of `keybindings.ts` — malformed JSON, non-object/array roots, and wrong-typed entries degrade to empty/skipped via an `isStoredDraft` type guard rather than an `as` cast — and every localStorage access is `try`/`catch` best-effort, never throwing. One shared `pruneDraftEntries()` helper runs on **both** the write and the hydrate path (no duplicated pipeline): empty-text entries are never stored, entries outside the `MAX_DRAFT_AGE_MS` (7-day) window are dropped — the check is two-sided (`Math.abs`) so a future-dated `updatedAt` from clock skew or corrupted storage ages out instead of sorting first and squatting the cap forever — and only the `MAX_PERSISTED_DRAFTS` (30) newest by `updatedAt` are kept. `pruneDraftEntries` is itself a thin wrapper over the generic `pruneByAgeAndCap(entries, isEmpty, cap)` that both persisted maps share (the sibling sent-history map is the other caller); the wrapper stays because it pins the draft-specific `text === ""` emptiness predicate and the `MAX_PERSISTED_DRAFTS` cap so neither draft call site restates them. Pruning is not optional hygiene: tmux window IDs are **reused after a server restart**, so a stale draft can otherwise point at a stranger. An all-empty map removes the key rather than storing `{}`. Multi-tab is last-write-wins — no `storage`-event sync. (`260801-cyth`, `260806-kadm`)

**Sent history — every transmitted text is recorded per target, one keystroke from recall.** Delivery is unverifiable at all three hops (`ws.send()` only queues bytes and an OPEN socket can be half-dead; the backend's `ptmx.Write` discards its error; the receiving TUI can silently swallow the bytes at a permission prompt or an open autocomplete), so the strip does **not** gate its clear on confirmation — the clear stays unconditional and the send semantics are byte-untouched. Instead the same module (`lib/compose-draft-store.ts`) carries a **second, independent per-target map** under the same `entryKey(server, windowId)`, exposed as `pushComposeSentHistory(key, text)` / `getComposeSentHistory(key)` / `hydrateComposeSentHistory()` (the test seam mirroring `hydrateComposeDrafts()`). Recovery over verification. Newest-first, capped at `MAX_SENT_HISTORY_PER_TARGET` (10) with the oldest evicted. Two push guards: **whitespace-only text pushes nothing** (so the empty-submit's bare `\r` never enters history) and **text identical to the current newest entry pushes nothing** (adjacent-duplicate collapse — the natural retry after a swallowed send must not burn a slot). The **stored** value is the exact untrimmed text — recall must reproduce what was transmitted, so only the guard trims. `getComposeSentHistory` is a plain read returning the live internal array as `readonly string[]` (never a defensive copy — every mutation replaces the array rather than mutating in place, and recall reads at keydown time, so history is deliberately NOT part of the `useSyncExternalStore` seam and a push notifies no subscriber); null/absent keys yield one frozen empty-array identity. (`260806-kadm`)

**History persistence rides a SIBLING localStorage key.** `runkit-compose-sent-history` (`COMPOSE_SENT_HISTORY_STORAGE_KEY`), schema `{[entryKey]: {entries: string[], updatedAt: number}}` — never folded into `runkit-compose-drafts`, so the shipped draft schema, its `isStoredDraft` guard, and its prune pipeline stay byte-compatible and neither surface's corruption can take the other down. Same discipline as drafts otherwise: write-through on every push (best-effort `try`/`catch`, `removeItem` when nothing remains), tolerant-parse hydration at module load through an `isStoredSentHistory` type guard (malformed JSON, non-object/array roots, and wrong-typed entries degrade to empty/skipped — `entries` validated as an array of strings, `updatedAt` as a finite number; no `as` casts on parsed storage), and the **same `pruneByAgeAndCap` helper** on both write and hydrate — targets with empty `entries` dropped, targets outside the shared `MAX_DRAFT_AGE_MS` (7-day, two-sided) window dropped, and the newest `MAX_PERSISTED_SENT_HISTORIES` (30) targets by `updatedAt` kept. The cap is a **sibling constant** of `MAX_PERSISTED_DRAFTS` rather than a reuse, so the two surfaces can diverge later without a rename. Hydration also re-caps each restored target's entries to 10. Window IDs are reused after a tmux server restart, so stale history must not resurrect against a stranger window. Multi-tab is last-write-wins, matching drafts. (`260806-kadm`)

**↑/↓ recall walk — a keydown layer between readline and the Enter classifier.** In the textarea's `onKeyDown`, recall sits after the Escape branch and the `handleReadlineKey` call (which owns no arrow keys — Ctrl+U/W, Alt+B/F/D only — so the composition is clean) and before `classifyComposeEnter`. **Only a BARE, non-composing arrow can recall**: an `isComposing` ↑/↓ navigates the IME candidate list (intercepting it would break the very surface the strip exists to provide, since xterm.js has no IME) and a modified arrow is a native editing motion (Shift extends the selection, Alt/Opt and Cmd are macOS paragraph/document jumps, Ctrl is bound by desktop environments) — the same exact-modifier discipline `classifyComposeEnter` and `classifyReadlineKey` already declare. **↑ is intercepted only when it cannot mean cursor movement**: the textarea is **empty**, or a walk is already in progress (where the visible text is recalled, not composed). A non-empty textarea outside a walk keeps native ↑ untouched — hijacking it would destroy in-progress composition. With no history for the target, ↑ starts no walk and changes nothing. Repeated ↑ steps toward older entries and **pins at the oldest** (no wrap). **↓ is intercepted only during a walk**, stepping toward newer; stepping **past the newest** restores the stashed pre-walk text and ends the walk. **Mid-walk the arrows follow the REPL line-boundary discipline**: a recalled entry can be multi-line, so ↑ steps older only with a collapsed caret on the entry's **first line** and ↓ steps newer only with a collapsed caret on the **last line** — everywhere else (an inner line, or any non-collapsed selection, whose bare arrow is the native collapse motion) the arrow moves the caret natively *within* the recalled text and the walk stays alive, since caret motion is not an edit. A recall step lands the caret at the entry's end (native value-set behavior), so single-line entries — first line == last line — walk straight through on repeated ↑, and the boundary gate is only observable on multi-line entries. An intercepted arrow is consumed (`preventDefault` + `stopPropagation`, mirroring the Enter-action handling so it never reaches global chords); a non-intercepted one falls through untouched. Recalled text is written through `setComposeText(draftKey, …)` — the textarea is store-controlled, so recall persists as the draft and auto-grows exactly as if typed. Recall restores **text only**: the `File` objects were revoked at send and are unpersistable, but their path lines ride the recalled text. (`260806-kadm`)

**Walk state lives in refs; four exits.** The walk is a `recallIndexRef` (steps back from live — `-1` means no walk, `0` selects the newest entry) plus a `recallStashRef` holding the textarea text captured at walk start. Both are `useRef`s, not state: no render reads them (the visible text is store-owned), so an arrow press costs zero re-renders. The walk ends via a shared `endRecall()` on **four** events: stepping past newest via ↓; **any text mutation that is not a recall step** — a user keystroke (`onChange`), an upload's appended path lines (`handleUpload`), or an attachment removal's spliced-out path line (`removeFile`), each of which calls `endRecall()` because leaving the walk armed would orphan the attachment (the next arrow overwrites the text, dropping the path line, while the `File` and its preview chip stay mounted pointing at a path the agent can never resolve); **any send** (so the next ↑ starts fresh from the newest entry, which is the text just sent); and a **target switch**, which fires **eagerly from a `draftKey` effect** rather than lazily at the next keydown — index and stash are meaningless against a stranger target, so a keydown-lazy check would leave an A→B→A round-trip resuming A's stale walk and let B's edits tear down A's stash. Because the teardown is eager, `handleRecallKey` needs no stale-target check: an in-progress walk always belongs to the currently-focused target. Escape keeps its blur-only semantics (the focus contract is unchanged). (`260806-kadm`)

**Push wiring is one line in `send()`.** After the `wsRef.readyState === WebSocket.OPEN` guard passes and immediately **before** `clearComposeDraft(draftKey)`, `pushComposeSentHistory(draftKey, text)` runs for **all three modes** (`submit`, `insert`, `insert-line`), recording the **pre-trailing-byte** text — the transmitted bytes, the clearing behavior, the focus contract, and `classifyComposeEnter` are all unchanged. A **guard-blocked** send early-returns above the push, so nothing is recorded and nothing was cleared (the draft is already preserved). An empty/whitespace-only submit reaches the push but the store's own guard makes it a no-op. Coverage is the two colocated unit-test siblings (`compose-draft-store.test.ts` for the store surface, persistence round-trip, tolerant parse, and pruning; `compose-strip.test.tsx` for the walk, its exits, and the per-mode push) — no Playwright spec, matching how the Enter matrix is tested. (`260806-kadm`)

**Attachments are in-memory per key; blob URLs are per-mount.** `File` objects cannot reach localStorage, so pending attachments live only in the in-memory map under their target's key and a hydrated draft carries an empty attachments array. The loss is cosmetic: uploads are **eager** (already on the target worktree's disk at attach time) and their path lines live in the persisted **text**, so after a refresh the previews and × remove-chips are gone but the path lines and uploaded files remain and sending still works. Blob-URL previews stay component-local (a fresh per-mount `Map<File, string>` ref, recreated lazily via `getBlobUrl` from the retained `File` objects). They are reclaimed at three seams: on unmount, on a delivered send (scoped to the cleared draft's files only — other targets' previews recreate lazily when their draft is shown again), and by a `[files]` effect that revokes URLs for any file no longer in the **visible** draft, so a departed target's URLs cannot strand in the map while the strip stays mounted. (`260801-cyth`)

**Focus routing — focus on OPEN, never otherwise.** Opening the strip focuses its textarea; everything else leaves focus where the user put it. The seam is a module-level **focus-on-open flag** in `lib/compose-strip-events.ts` (`markComposeStripFocusOnOpen` / `consumeComposeStripFocusOnOpen`, the same module-slot shape as the attach queue and focus registry beside it): `toggleComposeStrip()` marks it on the **off→on transition only**, and ComposeStrip's mount effect consumes-and-clears it and focuses. Because every open path funnels through that one toggle — the `>_` chip, the `View: Text Input` palette entry, the ⇧⌘E chord, the terminal's drag-drop/paste auto-enable, and the board twin — one seam covers them all with no per-affordance wiring. The consume **always clears**, even when the effect declines to focus (the disabled "no target" state), so a stale flag can never grab focus on a later remount. A plain route remount with the strip already enabled never calls the toggle, so it never sets the flag and never steals focus. The rest of the no-steal contract is intact: **after-send never focuses**, and Escape in the textarea blurs it back to the terminal (**never closes the strip** — the × close button does not alter this; only the explicit toggle affordances close it). On mobile, opening summons the IME — desired, matching the ⌨ chip. `preventFocusSteal` (`onMouseDown` preventDefault) is on the Send/📎/Insert/× buttons. On touch devices the ⌨ keyboard button focuses the strip's real textarea when the preference is on, via the **focus registry** in the same module (`registerComposeStripFocuser`/`focusComposeStrip` — a module registry, not a `document.querySelector('[data-testid]')`, since test ids are test-only here; the focuser returns `false` in the disabled "no target" case so `bottom-bar.tsx` falls back to `onFocusTerminal`). (`260801-sm6g`)

**Uploads — eager and worktree-scoped; an attachment never faces a foreign worktree.** The strip calls `useFileUpload(focused.session, focused.windowId, focused.server)` scoped to the **live** focused target, so uploads land in that window's worktree `.uploads/` (backend `upload.go` unchanged — see § File Upload). Because the draft — attachments included — is keyed by that same target, an attachment can only ever be shown alongside the worktree it was uploaded to: changing focus changes the whole draft, so there is **no re-upload, no path rewriting, and no error surface** on a target change (the strip carries no `role="alert"` error state at all). The retained `File` objects back previews only. Terminal drag-drop / clipboard-paste gestures (`terminal-client.tsx`) hand raw files to the strip via a document `CustomEvent` bridge (`COMPOSE_STRIP_ATTACH_EVENT` + a module hand-off queue in `compose-strip-events.ts`, drained on both the event and mount), enabling the preference first if off — no prop-drilling of upload results. The `File[]` overload on `uploadFiles` (`hooks/use-file-upload.ts`) exists for that queue. (`260801-cyth`)

**Board-pane focus cleanup.** `BoardPane` registers itself as the focused terminal while `isFocused`, and clears `FocusedTerminalContext` on **unmount** iff it is still the registered pane (`focusedRef.current?.wsRef === wsRef`, unmount-only `[]` deps, ref-guarded so a sibling's newer registration during a focus cycle is not clobbered). Without it, leaving a board for `/$server` would strand a stale non-null target — the enabled strip would render active with a stale label and upload into the stale worktree. With it, the strip reverts to the disabled "no target" state.

### File Upload

Entry points, all on the terminal page:
- **Clipboard paste** (`Cmd+V` / `Ctrl+V`) — document-level paste listener; files in `clipboardData.files` trigger upload, text-only paste passes through to xterm
- **Drag-and-drop** — drop files onto the terminal area; `ring-2 ring-accent` border highlight during drag-over; non-file drag content ignored
- **Compose strip upload button** (📎) — the left-most chip on the docked compose strip's button row (§ Docked Compose Strip → Two-row stack); opens native file picker via hidden `<input type="file">`

The paste/drop gestures live in `terminal-client.tsx` and hand raw files to the docked compose strip (enabling the `composeStripEnabled` preference first if off) via the `COMPOSE_STRIP_ATTACH_EVENT` document bridge; the strip uploads them to the **focused** target's worktree and inserts one path per line in its textarea (see § Docked Compose Strip). Server writes to `.uploads/{YYMMDD-HHmmss}-{sanitized-name}` in the target window's worktree. 50MB size limit. `.uploads/` auto-added to `.gitignore` on first use.

### iOS Keyboard Support

`useVisualViewport` hook (`app/frontend/src/hooks/use-visual-viewport.ts`) manages all viewport-related CSS side effects: adds the `fullbleed` class to `<html>` on mount (removed on cleanup), and listens to both `resize` and `scroll` events on `window.visualViewport`, setting `--app-height` (and `--app-offset-top`) CSS custom properties from `visualViewport.height`. The `scroll` listener catches iOS Safari viewport panning that doesn't trigger `resize`. **The hook is called once in `RootWrapper`** so the `--app-height` var is maintained on EVERY route — including the Host page and edge pages that mount no `Shell` — and owning the single idempotent effect at the root avoids the double-mount cleanup race a second copy in `Shell` would create. (`260707-4vq2`)

**Keyboard-open signal — `html.kb-open`.** The same rAF-coalesced `apply()` pass also derives a boolean keyboard-open signal and publishes it as the `kb-open` class on `<html>` (`syncKeyboardSignal`, called from `apply()` and from the initial sync — the hook's only two call sites). It registers **no additional listeners**: the derivation rides the existing `resize`/`scroll` pair, and `width` joins `height`/`offsetTop` in the change guard so a rotation reaches it. Removal is paired with `fullbleed`/`--app-height` in the existing cleanup, and the whole machinery is skipped when `window.visualViewport` is absent (jsdom, old browsers) — no class, no throw. The signal's sole consumer today is the bottom bar's raised coarse-pointer safe floor (§ Mobile Responsive → Safe-Area Insets); it is app-wide because the hook mounts at the root, so both bottom-bar render sites (app shell and the board twin) see it for free.

The heuristic: **baseline = the maximum `visualViewport.height` observed since the last `visualViewport.width` change**; the signal is on when `height < baseline − KEYBOARD_DELTA_PX`, with `KEYBOARD_DELTA_PX = 150` (exported constant, `use-visual-viewport.ts`). A keyboard shrinks height without touching width, whereas rotations and window resizes change width — so a width change is the baseline-reset trigger. 150px sits above iOS URL-bar chrome deltas (~50–114px) and below any real keyboard (~260px+). Two accepted limitations, both self-healing: **rotating or split-screen-resizing while the keyboard is open** re-baselines to the keyboarded height and clears `kb-open` until the next keyboard close/open cycle; **mounting with the keyboard already up** likewise reads as closed until the keyboard first closes (which grows the baseline). Both degrade to the 6px base floor, never to a wrong-direction pad. Unit coverage is `use-visual-viewport.test.ts` (mocked `visualViewport` + controllable rAF): baseline-off, keyboard-on, restore-off, sub-threshold chrome delta does not fire, rotation resets the baseline, cleanup removes the class, missing-`visualViewport` mount/unmount is safe, and the listener set stays exactly `resize` + `scroll`. (`260805-fi9m`)

In fullbleed mode, `globals.css` pins the **`.app-root` root layout container** (`position: fixed; inset: 0; height: var(--app-height, 100vh)`) to the visual viewport regardless of document scroll — the pin lives on `.app-root` (not the per-route `.app-shell` beneath it) because the persistent TopBar lives inside `.app-root` above the content region, so pinning that container keeps the header above the content. The per-route `.app-shell` is `position: relative; height: 100%` — it fills the root layout's `flex-1` region (its `overflow: hidden` contains xterm's canvas). (`260707-4vq2`) When the iOS keyboard appears, the bottom bar stays pinned above it, the terminal shrinks, and xterm refits via the existing `ResizeObserver`. The `fullbleed` class is also present in `index.html` as a static default (FOUC prevention); the hook takes over lifecycle management at runtime.

**Keyboard toggle** (`⌨` U+2328): Right-aligned button in the bottom bar, visible only on touch devices (`hidden coarse:inline-flex`). Bidirectional toggle: when terminal is focused (detected via `document.activeElement.closest(".xterm")`), tapping blurs to dismiss the keyboard; when not focused, tapping calls `onFocusTerminal` callback which chains through `app.tsx` → `TerminalClient.focusRef` → `xtermRef.current.focus()` to summon the keyboard. Dynamic `aria-label`: "Hide keyboard" / "Show keyboard". Uses `preventFocusSteal` to avoid stealing focus on the dismiss path.

**Scroll-lock mode**: Long-press (>= 500ms) on the keyboard toggle button activates scroll-lock — a mode that prevents the soft keyboard from appearing when the terminal area is tapped, allowing uninterrupted reading and scrolling. State is a `scrollLocked` boolean in `BottomBar` (default `false`), exposed to the parent via `onScrollLockChange` callback and passed down to `TerminalClient` as a `scrollLocked` prop. When locked, a capture-phase `focusin` listener on the terminal container immediately blurs any `.xterm` element that gains focus, preventing the keyboard from appearing. Touch scroll gestures (SGR mouse sequences) are unaffected — only focus is prevented. Activating scroll-lock while the keyboard is visible auto-dismisses it (`document.activeElement?.blur()`). Optional haptic feedback via `navigator.vibrate?.(50)` on toggle (graceful no-op if unavailable).

- **Long-press detection**: `touchstart`/`touchend`/`touchmove` handlers with a 500ms timer (`LONG_PRESS_MS` constant). Touch move > 10px (`LONG_PRESS_MOVE_THRESHOLD`) cancels the long-press. On timer expiry, scroll-lock toggles and subsequent `touchend`/`click` are suppressed via a ref flag. Desktop click behavior is unaffected (touch events only).
- **Tap in locked mode**: Tapping the keyboard button (< 500ms) when `scrollLocked` is `true` unlocks AND summons the keyboard in one action — matches user intent without requiring a double-tap.
- **Visual indicator**: When locked, the button shows `bg-accent/20 border-accent text-accent` (same armed-state pattern as Ctrl/Alt modifier toggles) and the icon changes from `⌨` (U+2328) to `🔒` (U+1F512). `aria-label` updates to "Scroll lock on — tap to unlock".
- **State lifecycle**: Session-scoped (component-local React state, not persisted). Resets on navigation (component unmount/remount). The docked compose strip is unaffected — it has its own input field outside the terminal container.

### Terminal Touch Scroll

The terminal container div has `touch-pan-y` (CSS `touch-action: pan-y`) — allows vertical swipe gestures for scrollback access on mobile/touch devices while blocking horizontal panning (prevents page-level overflow from tmux's ~80 column minimum). The xterm.js `.xterm-viewport` has `overflow-y: scroll` and `overscroll-behavior: none` (in `globals.css`), so the browser delegates vertical touch scroll to xterm natively without page bounce. In the single-view model, fullbleed is always active — `overflow: hidden` and `overscroll-behavior: none` are applied to both `html` and `body` (via `globals.css`), preventing iOS Safari elastic bounce scrolling. The docked compose strip and bottom bar are siblings of the terminal container, not children, so their touch behavior is preserved.

## Mobile Responsive

### Breakpoints & Container Width

All zones use `px-3 sm:px-6` — reduced horizontal padding on screens < 640px. No `max-w-4xl` constraint — terminal, top bar, and bottom bar all span full width. The mobile/desktop breakpoint for the `<Shell>` topology is **640px** (matches the existing `sm:` Tailwind breakpoint and `useIsMobile()` hook): `>= 640px` uses the 2-column grid with sidebar full-height; `< 640px` uses the single-column grid with sidebar overlay. Sidebar is drag-resizable on desktop (default 220px, min 160, max 400); terminal fills remaining space via the right grid column. Terminal container has `py-0.5 px-1` padding for breathing room against border lines. The bottom-bar toolbar row uses `pt-1.5` with a `--bottom-bar-floor`-driven bottom arm — 6px, raised to 1rem on coarse pointers while the keyboard is collapsed (§ Safe-Area Insets).

### Touch Targets

A custom Tailwind variant `coarse:` is defined in `globals.css` via `@custom-variant coarse (@media (pointer: coarse))`. On touch devices, interactive elements are upsized per surface — `coarse:` is the mechanism, not a universal size (there is no single 44px minimum; each surface documents its own target). Upsized elements include:
- Top-bar icon controls — the shared fixed-size token's `coarse:w-[30px] coarse:h-[30px]` arm (`TOP_BAR_BUTTON*`, § Right cluster → shared button-size token); content-width chips/segments share the height axis via `TOP_BAR_BUTTON_H` / `TOP_BAR_SEGMENT_H`
- Sidebar session/window row action buttons — `coarse:min-w-[32px] coarse:min-h-[36px]`, both axes (§ Sidebar Row Icon System explains why 32 and not 36 on the width axis)
- Breadcrumb name dropdown triggers
- `⋯` command palette trigger
- Hamburger icon (sidebar toggle — single state covers desktop column collapse and mobile overlay) (`260509-17m3`)

**Both axes or neither.** An icon-only control needs `coarse:min-w-*` as well as `coarse:min-h-*`: a bare glyph has no horizontal padding to grow into, so a `min-h`-only rule yields a tall-but-thin target that measures ~17px wide on touch. Wider padded chips (text buttons) can get away with `min-h` alone.

Bottom bar buttons use the full `coarse:min-h-[36px] coarse:min-w-[36px]` touch target on touch devices, `min-h-[33px] min-w-[35px]` on fine pointers (with a 6px fine / 4px coarse row gap). The row's own bottom padding also lifts on coarse pointers — via the `--bottom-bar-floor` custom property rather than a `coarse:` utility class, because the lift is additionally gated on the keyboard being collapsed (§ Safe-Area Insets).

### Viewport Zoom Prevention

The viewport meta tag in `app/frontend/index.html` includes `maximum-scale=1.0` and `user-scalable=no` to prevent iOS Safari from auto-zooming when text inputs (command palette, docked compose strip) receive focus. Without these directives, iOS zooms in on inputs with `font-size < 16px`, displacing the entire interface. Pinch-to-zoom is also disabled — acceptable tradeoff for a keyboard-first tool dashboard where zoom doesn't improve terminal readability. The `interactive-widget=resizes-content` directive (keyboard layout resizing) and `viewport-fit=cover` (§ Safe-Area Insets) ride the same meta tag and are unrelated to zoom.

### Safe-Area Insets

**The app is `viewport-fit=cover` and every edge-docked pad is a `max(<floor>, env(safe-area-inset-<edge>))` expression — but on this app `env()` is 0 in in-browser Safari, so the floor is what actually ships the clearance.** The viewport meta in `app/frontend/index.html` carries `viewport-fit=cover`, which is the precondition for `env(safe-area-inset-*)` resolving to anything but 0 on iOS. In practice it resolves to **0 in in-browser iOS Safari** for this app on **both** edges: the page is `position: fixed` and non-scrollable (`html.fullbleed .app-root` pinned to `--app-height`, § iOS Keyboard Support), so Safari never enters the chrome states where it reports insets. Non-zero values appear only in standalone PWA mode. Device-confirmed on the bottom edge 2026-08-05 (photo evidence: the bottom-bar chips clipped by the corner arc). The `max()` shape is retained everywhere so genuine inset reporting — standalone PWA, future Safari behavior — still wins where it exists. Two consumers: (`260724-2bmy`, `260805-fi9m`)

- **Bottom bar toolbar row** (`bottom-bar.tsx`): `pt-1.5 pb-[max(var(--bottom-bar-floor,0.375rem),env(safe-area-inset-bottom))]` — asymmetric padding whose bottom arm reads a floor custom property. `--bottom-bar-floor` is owned by `globals.css` beside the `html.fullbleed` block: `0.375rem` (6px) at `:root`, raised to **`1rem` (16px)** under `@media (pointer: coarse)` on `html:not(.kb-open)`. That raised coarse floor is what clears the corner-arc/home-indicator zone for the extreme chips (`⇥` left, `⌨` right) on iPhone-class devices — `env()` contributes nothing in-browser. Fine-pointer and desktop-shell layouts stay byte-identical at 6px (the `@media (pointer: coarse)` scope is the gate; the raised floor must not leak into contexts that don't need it — the same rule as the shell's `isShell()` top gate below). Accepted cost: flat-screen touch devices spend ~10px of terminal height they didn't need.
- **Top-bar `<header>`** (`top-bar.tsx`): `pt-[env(safe-area-inset-top)]` — a guard, because `viewport-fit=cover` in standalone PWA mode also extends the page under the status bar, which would tuck the bar beneath the clock. The class is `isShell()`-gated OFF inside the desktop shell (a conditional template literal — browser/PWA output stays byte-identical): the `ShellTitlebarStrip` already reserves the titlebar band in the SPA layout (§ Desktop-Shell Titlebar Strip), and macOS hidden-titlebar windows can report that band as `safe-area-inset-top`, so keeping the padding would reserve the band twice. `isShell()` is read once per render — the preload injects `window.runkitShell` before any SPA script runs, so the value is stable for the page's lifetime. (`260805-9hn1`)

**The raised bottom floor is gated by an explicit JS keyboard signal.** A static raised floor would waste vertical space on an already-short viewport when the on-screen keyboard is up and the bar rides above it on flat screen area. `useVisualViewport` therefore toggles **`kb-open` on `<html>`** (§ iOS Keyboard Support), and `html:not(.kb-open)` is what admits the 1rem floor — with the keyboard open, coarse pointers fall back to the same 6px floor fine pointers always use. **Do NOT rely on `env()` collapsing, or on any layout-viewport side effect of `interactive-widget=resizes-content`, to produce this behavior**: iOS support for that directive is unreliable, and on this app `env()` is already 0 in both keyboard states, so it carries no signal at all. JS toggles the class; the padding math stays entirely in CSS.

**Playwright/Chromium reports every `env()` as 0**, so e2e can honestly assert only the floor values and the class flip (`bottom-bar-safe-floor.spec.ts`: 16px coarse-emulated via `hasTouch: true`, 6px after adding `kb-open`, 6px on fine pointers). The `max()` arm itself is verifiable only from the compiled class, never from a computed style — no test may pretend to exercise a real inset.

**New chrome docked to a screen edge SHOULD use this convention** rather than a hardcoded pad: `max(<the pad you wanted>, env(safe-area-inset-<edge>))`. Treat `env()` as a bonus that fires only in standalone PWA mode — pick the floor as if it will never fire, and scope any coarse-pointer-only lift with `@media (pointer: coarse)`.

**Interaction rule: edge-docked safe-area padding and shell-reserved chrome must not stack.** Where the desktop shell already reserves an edge with its own chrome (today: the titlebar strip at the top edge), the safe-area pad for that edge is gated off with `isShell()` rather than layered on top — the OS-reported inset covers the same band the shell chrome occupies, and applying both reserves it twice. (`260805-9hn1`)

### Terminal Addons

Addons loaded in `init()` from **static top-of-file imports**, after `terminal.open()`, before `ResizeObserver` setup. Order: FitAddon (existing) → fit() → ClipboardAddon → WebLinksAddon → UnicodeGraphemesAddon → WebglAddon. The Unicode addon MUST precede WebGL so the renderer measures cell widths against the active Unicode 15 table on first paint.

**Static import bundling**: all six xterm-family value imports (`@xterm/xterm` `Terminal`, `addon-fit`, `addon-clipboard`, `addon-web-links`, `addon-unicode-graphemes`, `addon-webgl`) are static top-of-file `import` statements in `terminal-client.tsx`, NOT runtime `await import()`. (`260531-m3pl`) Because `terminal-client.tsx` is already router-lazy, the xterm family bundles into the already-deferred terminal-route chunk and loads **once when that chunk loads** — never as a per-pane-mount runtime module-graph fetch. This removes all six chunk fetches from the browser's per-origin connection budget at terminal-init time, fixing the board-route E2E hang: on the plaintext dev/test origin (no HTTP/2 — only negotiated over TLS), the browser caps ~6 persistent connections per origin, and long-lived streams (Vite HMR socket, pooled SSE `EventSource`, one `/relay/<wid>` WS per visible pane) already saturate the slots, so a 7th xterm chunk fetch hung pending forever → `setTerminalReady(true)` never ran → blank pane → E2E poll timeout. Masked in production over Tailscale HTTPS (h2 multiplexes). The CSS side-effect import (`@xterm/xterm/css/xterm.css`) and type-only `import("@xterm/...")` annotations are static too.

| Addon | Purpose | Notes |
|-------|---------|-------|
| `@xterm/addon-fit` | Auto-resize columns/rows | Existing — loaded first, `fit()` called immediately |
| `@xterm/addon-clipboard` | OSC 52 clipboard sequences | Custom `ClipboardProvider` accepts both `""` (empty/default, tmux's format) and `"c"` (explicit) selection targets. Rejects `"p"`, `"s"`, `"0"`–`"7"`. Provider exported as `clipboardProvider` for testability |
| `@xterm/addon-web-links` | Clickable URLs in terminal output | Constructed **with an explicit activation handler** — `new WebLinksAddon((_event, uri) => window.open(uri, "_blank", "noopener,noreferrer"))`. The handler-less default is not viable (§ WebLinksAddon Explicit Handler) |
| `@xterm/addon-unicode-graphemes` | Unicode 15 + grapheme-cluster width tables | Requires `allowProposedApi: true` on the Terminal constructor; `terminal.unicode.activeVersion = "15-graphemes"` set after `loadAddon()`. Must load before the WebGL addon |
| `@xterm/addon-webgl` | GPU-accelerated rendering | The `new WebglAddon()` / `loadAddon(...)` construction is wrapped in try/catch — silently falls back to canvas renderer on GPU/WebGL-context failure. The *module load* is static (resolved at chunk load, cannot fail at `init()` time), but context creation can still throw at runtime, so the runtime guard is required (`260531-m3pl`) |

#### WebLinksAddon Explicit Handler

`WebLinksAddon` is constructed with an explicit activation handler that calls `window.open(uri, "_blank", "noopener,noreferrer")` — the codebase's universal external-open idiom (`app.tsx`, `board-page.tsx`, `buildOpenPrAction`). **The handler is load-bearing for the desktop shell, not a stylistic preference.**

The addon's default handler opens a window with **no URL** (to get a blank window whose `.opener` it can clear), then assigns `location.href = uri` on it. In a browser that works — `window.open()` succeeds on the click gesture. In the Electron shell it cannot: the main process's `setWindowOpenHandler` sees `url: "about:blank"`, which is not http(s), so the intent is denied (see [desktop-shell](/run-kit/desktop-shell.md) § Security Wiring); `window.open()` returns `null` and the addon bails with a console warning ("Opening link blocked as opener could not be cleared"). The failure mode is a dead link with nothing but that console warning as evidence, and the shell cannot fix it from its side: `about:blank` carries no URL by the time main sees it.

Passing the URI through `window.open` directly makes the two environments agree: identical browser behavior (new tab, opener severed by `noopener,noreferrer` rather than the addon's manual `.opener = null` dance), and the shell's window-open policy receives the real URL and routes it to the system browser. Covered by a unit test in `terminal-client.test.tsx` asserting the addon is constructed *with* a function and that invoking it calls `window.open` with the three-arg idiom; there is no e2e — xterm renders links on a canvas, so link-region clicks are not reliably automatable. (`260730-e9lz`)

### Terminal Font Size (user-controllable global preference)

Terminal font size is a **global** user preference owned by `ChromeContext`, not a per-pane or per-window value (`260613-oo89`). The context exposes the **effective** size as `terminalFontSize: number` (what `TerminalClient` reads and the top-bar control displays); the internal preference is stored as `number | null` (null = unset) and persisted to `localStorage["runkit-terminal-font-size"]`. Effective = preference ?? device default — 11px mobile / 13px desktop via the shared `isMobileViewport()` rule (narrow-width OR coarse-pointer, NOT a CSS media query). Bounds 8–24px, step 1, exported as `TERMINAL_FONT_BOUNDS` (`{ min: 8, max: 24, step: 1 }`); all stored values clamped into `[8, 24]` on read. Helpers `clampTerminalFont`/`deviceDefaultFontSize`/`readTerminalFontSize` mirror the `fixedWidth`/`SIDEBAR_WIDTH_BOUNDS` shape.

**Mutators** (`ChromeDispatch`): `increaseTerminalFont` / `decreaseTerminalFont` step `effective ± step`, clamp, persist (try/catch noop); `resetTerminalFont` = **forget preference** (`localStorage.removeItem` → preference back to `null` → effective reverts to the device default), NOT a fixed value. First step from the unset (`null`) state operates on the device default and persists a concrete adjacent value (desktop 13 → 14).

**Applied to xterm** (`terminal-client.tsx`): `terminalFontSize` is passed as the `fontSize` option on construction (and to the `document.fonts.load` preloads); an effect keyed on it sets `term.options.fontSize = N` + calls `fitAddonRef.current?.fit()` on change (guarded against an uninitialized terminal). `fit()` recomputes rows×cols and **resizes the PTY** (the correct xterm resize path — reflows running TUIs, not a bitmap-scaled canvas). Because the value is global, **every** live `TerminalClient` reacts on change — including all mounted board-route panes. The mobile touch-scroll `LINE_HEIGHT` fallback reads `options.fontSize` live, so it tracks runtime changes.

**Control surfaces**: (1) the top-bar chevron menu's `−`/`+` stepper row plus the settings dialog's `[−] {size} [+]` + reset combo (see § Terminal font (Aa)), and (2) three command-palette actions — `terminal-font-increase` ("Increase terminal font"), `terminal-font-decrease` ("Decrease terminal font"), `terminal-font-reset` ("Reset terminal font"), no `shortcut`, wired to the dispatch fns. The palette actions are **layout-level globals** (`use-global-palette-actions.ts`, § Single palette mount + palette-actions slot) — the setting is global (the board's panes are live terminals too) and Constitution V requires the palette reachable on every route.

**Deliberate non-decision**: `Cmd +/-` is **NOT** intercepted — browser-native whole-page zoom is retained (no global keybinding registered), avoiding the brittleness of hijacking `Cmd +/-` / `Cmd 0`.

### Terminal Font Bundling

The frontend bundles JetBrainsMono Nerd Font (patched single-file variant) as a webfont so terminal rendering is deterministic across all viewers, independent of which monospace fonts the browser happens to have installed. Without bundling, per-glyph font fallback for Nerd Font private-use-area codepoints produces visible baseline wobble within a single terminal row.

**Asset layout**: Three `.woff2` weights served from `/fonts/` — `JetBrainsMonoNerdFont-Regular.woff2` (400 normal), `-Bold.woff2` (700 normal), `-Italic.woff2` (400 italic). All three `@font-face` rules in `app/frontend/src/globals.css` expose the same `font-family: "JetBrainsMono Nerd Font"` name and MUST declare `font-display: block`. `swap` or `fallback` would let xterm measure cells against system-font metrics that persist as misalignment — xterm measures the character cell grid exactly once at `terminal.open()` and does not re-measure when a deferred font arrives. `index.html` includes a `<link rel="preload" as="font" type="font/woff2" crossorigin href="/fonts/JetBrainsMonoNerdFont-Regular.woff2" />` to overlap the Regular download with JS parsing (only Regular is preloaded — Bold/Italic are a smaller fraction of the initial paint and don't justify the extra critical-path bytes).

**Load convention before `terminal.open()`**: The init routine in `app/frontend/src/components/terminal-client.tsx` awaits a concurrent `Promise.all([document.fonts.load(...), document.fonts.load(...), document.fonts.load(...)])` for all three weights at the exact `fontPx` (`isMobile ? 11 : 13`) the Terminal will use, BEFORE `new Terminal(...)` / `terminal.open()` / `fitAddon.fit()`. Three explicit `document.fonts.load(size, family)` calls (not `document.fonts.ready`) scope the await to exactly the weights xterm will request. A fresh `if (cancelled || !terminalRef.current) return;` guard MUST follow the await. With all six xterm imports static, this font-load `await Promise.all([...])` is the **only** async boundary in `init()`, so it is the sole place a post-await unmount guard is required before `new Terminal(...)`; one post-construction dispose guard sits before `setTerminalReady(true)`. (`260531-m3pl`)

**Primary `fontFamily`**: `'"JetBrainsMono Nerd Font", ui-monospace, monospace'` — bundled webfont first, `ui-monospace` as the system-default monospace, generic `monospace` as final guard against total load failure. The older long tail (`JetBrains Mono`, `Fira Code`, `SF Mono`, `Menlo`, `Monaco`, `Consolas`) is dead code once `font-display: block` plus a successful load makes the webfont always win; do not reintroduce it. Non-terminal monospace surfaces pick up the same font automatically via Tailwind's `--font-mono` custom property (webfont-first). (`260417-hyrl`)

**Test caveat**: jsdom does not implement the FontFaceSet API. `src/test-setup.ts` stubs a minimal `document.fonts.load()` / `document.fonts.ready` surface (same pattern as the existing `ResizeObserver` stub) so unit tests that mount `TerminalClient` do not hang on the await.

### Terminal Unicode Width Handling

xterm.js defaults to Unicode 6 width tables, which classify many modern glyphs (most emojis, several Misc Symbols codepoints) as 1 cell. Modern tmux lays out its buffer using wcwidth with a newer Unicode table (typically 14/15), treating the same glyphs as 2 cells. Without alignment, subsequent characters in a row drift between tmux's intended column and xterm's rendered column, producing visible ghost/overlap artifacts — especially with the WebGL renderer.

**Resolution** (`app/frontend/src/components/terminal-client.tsx`):
1. Construct the Terminal with `allowProposedApi: true` — required to access `terminal.unicode` (a proposed-API surface in xterm v6).
2. After `terminal.open()`, `loadAddon(new UnicodeGraphemesAddon())` (statically imported) and set `terminal.unicode.activeVersion = "15-graphemes"`.
3. Load order MUST precede the WebGL addon so the renderer initialises against the Unicode 15 table on first measure.

The `addon-unicode-graphemes` package (v6-era) covers Unicode 15 and grapheme clusters (ZWJ sequences, flag emoji, skin-tone modifiers). The `unicodeVersion` Terminal constructor option is a no-op past `"6"` without the addon — the addon is what registers the newer width table. (`260418-xgl2`)

### Command Palette Mobile Trigger

The `CommandPalette` component listens for a `palette:open` CustomEvent on `document` (in addition to `⌘K`). The `⋯` button in Line 1 dispatches this event on mobile. This is the mobile equivalent of `⌘K` — physical keyboards aren't available on phones.

### Keyboard-Navigable List Scroll Pattern

Both `CommandPalette` and `ThemeSelector` use the same scroll-into-view pattern for arrow key navigation: a `listRef` on the listbox container plus a `useEffect` on `[selectedIndex, open]` that queries `[aria-selected="true"]` and calls `scrollIntoView({ block: "nearest" })`. This ensures the selected item stays visible when navigating past the `max-h-64` scroll boundary. New keyboard-navigable list components SHOULD follow this pattern.

The sidebar session/window tree is a **third consumer** (see § Sidebar → Keyboard Navigation & Tree ARIA): a `treeRef` on the `role="tree"` container plus a `useEffect` on `[rovingKey]` that resolves the roving row (`[data-window-id]`/`[data-session-row]`) and calls `scrollIntoView({ block: "nearest" })` + `focus()`. It reuses the scroll-into-view half but deliberately diverges from the `selectedIndex` numeric-state model — the roving cursor is a row-key STRING (robust to the variable-length tree's expand/collapse and SSE row churn, where a numeric index can desync); the focus call is gated by `focusMovedRef` so passive SSE re-renders never scroll/steal focus.

## Keyboard Shortcuts

Every app chord is **data in one declarative registry** — `app/frontend/src/lib/keybindings.ts` (pure, DOM-light, colocated `keybindings.test.ts`, the `window-view.ts`/`palette-*.ts` convention). The module owns the tier definitions, `DEFAULT_BINDINGS` with its per-platform default refinements, the tier-stamped claimed-key map, the per-device override layer, chord matching (incl. scope precedence and the terminal-seam refusal predicate), conflict detection, capture, per-platform formatting, the shared suppression predicate, and the palette-hint decorator. React integration is two hooks: `hooks/use-keybindings.ts` (the reactive effective map) and `hooks/use-keybinding-dispatch.ts` (ONE window-level `keydown` listener per route shell). (`260730-g40a`)

### Tiers and the `e.code` rule

Three tiers, each a modifier predicate in `matchesCombo` — **Alt is excluded from every tier** (macOS composes characters with it):

| Tier | Chord | Used by |
|------|-------|---------|
| `shifted` | `Shift + (Meta or Ctrl)` — ⇧⌘ on macOS, ⇧Ctrl on Windows/Linux | the Windows/Linux action tier in full, and the mac actions that cannot demote (H/L/A) |
| `cmd` | `(Meta or Ctrl)` without Shift | the legacy punctuation chords ⌘K ⌘\ ⌘., the board ⌘[/⌘] pair, and the macOS-demoted action defaults (§ Per-platform default tiers) |
| `ctrl` | plain `Ctrl` without Meta/Shift | the `` Ctrl+` `` chat toggle (⌘` is macOS window cycling and must never be bound) |

Matching is on **`KeyboardEvent.code`, never `e.key`** — layout-independent (`"KeyN"`, `"BracketLeft"`), and it sidesteps the shifted-accelerator character-resolution flakiness the shell docs flag ([desktop-shell](/run-kit/desktop-shell.md) § Keyboard-Tier Menu Seam). **The letters are constant across platforms; only the modifier varies.** The shifted tier is what makes N/T/W/H/L coexist on Windows/Linux at all: **plain Ctrl must always reach the pane** there (Ctrl+W delete-word, Ctrl+L clear, Ctrl+T fzf, Ctrl+N/P history), and legacy TTY encoding cannot distinguish Ctrl+Shift+letter from Ctrl+letter, so claiming the shifted tier costs terminal apps nothing (the same tier every cross-platform terminal — Windows Terminal, GNOME Terminal, Konsole, kitty — uses for app actions). That pane-ownership constraint is **Windows/Linux-only**, and browser key reservation is **host-specific** — so macOS defaults demote to the unshifted ⌘ tier wherever nothing immovable blocks them (§ Per-platform default tiers).

### The default binding set

Fourteen starter actions on the letter/punctuation tier — `actionId` doubles as the palette action id wherever one exists. The letter is invariant — with one deliberate exception, the split pair's mac `macCode` refinement (§ Per-platform default tiers) — and the modifier is per platform and per host. All are `scope: "global"` except `open-last-used` and the two split actions, which are `terminal`:

| Key | actionId | Action | macOS shell | macOS browser | Win/Linux |
|-----|----------|--------|-------------|---------------|-----------|
| N | `create-session` | New session | ⌘N | palette only (browser-reserved even shifted) | ⇧Ctrl+N |
| T | `create-window` | New window (tab-analog in the current session) | ⌘T | palette only | ⇧Ctrl+T |
| W | `kill-window` | Close window (the confirm flow) | ⌘W | palette only | ⇧Ctrl+W |
| E | `compose-toggle` | Toggle the docked compose strip (`ignoreInputs`) | ⇧⌘E | ⇧⌘E | ⇧Ctrl+E |
| O | `open-last-used` | Re-run the last-used Open target (`scope: "terminal"`) | ⇧⌘O | ⇧⌘O | ⇧Ctrl+O |
| \ (D on mac) | `split-horizontal` | Split the pane side-by-side, tmux `-h` (`scope: "terminal"`) | ⌘D | ⌘D | ⇧Ctrl+\ |
| − (D on mac) | `split-vertical` | Split the pane stacked (`scope: "terminal"`) | ⇧⌘D | ⇧⌘D | ⇧Ctrl+- |
| H | `window-prev` | Previous window | ⇧⌘H | ⇧⌘H | ⇧Ctrl+H |
| L | `window-next` | Next window | ⇧⌘L | ⇧⌘L | ⇧Ctrl+L |
| [ | `go-back` | Router history back | ⌘[ | ⌘[ | ⇧Ctrl+[ |
| ] | `go-forward` | Router history forward | ⌘] | ⌘] | ⇧Ctrl+] |
| A | `agent-next-waiting` | Next waiting agent (§ Attention Surfacing) | ⇧⌘A | ⇧⌘A | ⇧Ctrl+A |
| / | `shortcuts-overlay` | Toggle the cheatsheet overlay (`ignoreInputs`) | ⌘/ | ⌘/ | ⇧Ctrl+/ |
| , | `settings-open` | Open the settings dialog (`ignoreInputs`) | ⌘, | ⇧⌘, (⌘, is browser Preferences) | ⇧Ctrl+, |

**`settings-open` is the one comma chord** (`260801-mqim`): a chrome-level *opener*, not a toggle — a re-fire while the dialog is open is a React state bailout, matching the macOS ⌘, convention where Preferences never closes itself. It is the third `macShellOnly` demotion alongside N/T/W, but for a different reason: ⌘, is the **browser's own Preferences accelerator** on macOS (claimed data, § Claimed keys), so the browser default stays shifted while the shell — which binds no ⌘, accelerator — promotes it to the OS-conventional ⌘,. `ignoreInputs: true` mirrors `shortcuts-overlay`/`compose-toggle`, the other chrome-level chords. `mapLabel: "settings"` is carried for parity but unrendered: `Comma` has no keycap cell in the overlay's tier-map grids, the `Period`/`Backslash`/`Backquote` precedent.

**E and O stay shifted on every platform** — no `macTier` demotion. ⌘E is macOS browser "use selection for find" territory, and the letter choice for the compose toggle is what the surrounding tier left free: C is the win/linux terminal copy/paste claim, T is `create-window`, I is win/linux DevTools. `compose-toggle` carries `ignoreInputs: true` so the chord also fires from *inside* the strip's own textarea (the one place the strip is most likely to be closed from) — the same field ⌘K and the overlay toggle use. `open-last-used` names the **behavior**, not an app, which is why it can carry a static chord at all while the per-target `Open:` palette entries cannot (§ Open-in-App palette actions). (`260801-sm6g`)

**The split pair is the one per-platform-CODE default** (`260807-rbx5`): `split-horizontal`/`split-vertical` bind the existing `Window: Split Horizontal` / `Window: Split Vertical` palette bodies, direction semantics inherited from the top-bar chip (horizontal = side-by-side = tmux `-h`; § Horizontal is the default split direction). Both are `scope: "terminal"` — the `view-cycle`/`open-last-used` precedent, since the palette bodies exist only on window routes, so `fromPalette` yields no handler elsewhere and the chord falls through untouched. The base (Win/Linux) codes are keycap-as-divider mnemonics: ⇧Ctrl+\ side-by-side (shift+\ types `|`, the divider the split creates — the VS Code split-editor keycap) and ⇧Ctrl+- stacked. macOS refines both to the D keycap (`macCode: "KeyD"`) for the iTerm2/Warp/Ghostty pair: `macTier: "cmd"` also demotes horizontal to ⌘D in **both** mac hosts (⌘D is the mac-browser bookmark accelerator, page-interceptable like ⌘[/⌘]/⌘/ — no `macShellOnly`, and no `browser`-owner claim entry), while vertical keeps the shifted tier as ⇧⌘D — tier-disjoint on the one code, so the mac pair is conflict-free. The mac pair cannot ship with constant codes: plain Ctrl+D is EOF and belongs to the pane on Win/Linux, and two rows cannot share ⇧Ctrl+D because equal-scope same-combo defaults would break the conflict-free invariant (§ Per-platform default tiers). Horizontal takes the plain ⌘D because it is the primary/default split. `mapLabel`s `split h` / `split v` annotate whichever keycap each is bound to on its map layer.

Both split rows also carry **`ttyOnly: true`** (`260812-wfic`) — an optional `KeyBinding` data flag meaning "this chord targets the tmux pane; only meaningful when the tty tile owns focus", set on exactly these two rows and consulted by gate sites rather than hardcoded actionId lists: `app.tsx`'s handler map drops a `ttyOnly` handler when the focused tile kind is not `tty` (§ Dispatch seams), and the code-surface reclaim predicate refuses to reclaim a keydown whose only matches are `ttyOnly` (§ Code Surface → Chord reclaim).

Seven further bindings cover the punctuation chords — same registry, so they are per-device rebindable like everything else: `command-palette` ⌘K (`cmd`, global, `ignoreInputs`), `sidebar-toggle` ⌘\ (`cmd`, global), `view-cycle` ⌘. (`cmd`, terminal), `panel-toggle` ⇧⌘. (`shifted`, terminal — the shifted tier of the view-cycle keycap; § Right Rail), `layout-cycle` ⌘; (`cmd`, terminal — the next same-arity layout preset, order kept; § Surface Layout), `chat-toggle` `` Ctrl+` `` (`ctrl`, terminal), and the board pane-cycle pair `board-cycle-prev`/`board-cycle-next` ⌘[/⌘] (`cmd`, board). `scope` (`global | terminal | board | sidebar`) is descriptive — it drives overlay grouping and conflict scoping; actual applicability is handler presence at each dispatcher mount. `sidebar` is schema-reserved with no binding. `kind` is `"builtin" | "macro"` — the builtins are the shipped registry actions above; `macro` bindings are user-defined and enter resolution from the macro store (see § Macro bindings). (`260730-hbyh`)

`Cmd+C`/`Ctrl+C` in a focused terminal is **not** a registry binding — it is a special case at the head of `attachCustomKeyEventHandler` (copy selection when one exists, else fall through to xterm's SIGINT), using `navigator.clipboard.writeText()` with a `document.execCommand('copy')` fallback for non-secure contexts and clearing the selection via `.finally()`.

No single-key shortcuts (`j`/`k`/`c`/`r`) or `Esc Esc` — these conflicted with xterm.js terminal input. Every action is also reachable via `Cmd+K` or top-bar buttons.

### Per-platform default tiers (`macTier` / `macCode` / `macShellOnly` / `defaultComboFor`)

The per-key table above is **data on the binding**, not a resolver branch. `KeyBinding` carries the base (Windows/Linux) `tier` plus three optional refinements — all of which touch the SHIPPED DEFAULT only, never the stored override:

- **`macTier?: BindingTier`** — the tier this binding's default combo uses on mac hosts. Shipped on `go-back`/`go-forward`/`shortcuts-overlay`/`split-horizontal`.
- **`macCode?: string`** — the KEY CODE the default combo uses on mac hosts, replacing `code`. The one deliberate exception to 260730-n789's letters-constant rule, for chords whose mac convention has no legal cross-platform letter. Shipped on both split rows (`macCode: "KeyD"`): mac gets the iTerm2/Warp/Ghostty ⌘D/⇧⌘D pair while Win/Linux keeps the divider-mnemonic `\`/`−` base codes. Composes with `macTier` under one host gate. (`260807-rbx5`)
- **`macShellOnly?: boolean`** — restricts the mac refinements (`macTier`/`macCode`) to `isShell()` hosts. Shipped alongside `macTier: "cmd"` on `create-session`/`create-window`/`kill-window`/`settings-open`.

Every other binding (including the legacy chords and macro projections) carries none of the three and resolves identically everywhere.

`defaultComboFor(def, host)` is the **single seam** where platform and shell are consulted for defaults — it returns `{ code: macCode ?? code, tier: macTier ?? tier }` when `host.platform === "mac"` and either mac refinement is set and (`!macShellOnly || host.shell`), else `{ code, tier }`. Both `resolveBindings` (the no-override fallback *and* the `isDefault` comparison) and `applyCapture` (own-default detection, which therefore takes a required `host`) read defaults through it, so an effective map is always host-coherent: on a mac shell host ⌘[ *is* `go-back`'s default and shows no modified dot (and ⇧⌘D *is* `split-vertical`'s default, so re-capturing it is a diff-dropping no-op), while the same combos on Win/Linux are user overrides.

The **stored override shape is untouched** — `{ [actionId]: { code, tier } | null }`, byte-identical. Overrides are per-device by construction (localStorage), so per-platform storage is inherent and no migration exists. Win/Linux resolution is byte-identical to the pre-demotion map.

Which keys demote is constrained by what the environment refuses to give up:

- **⌘[ / ⌘] / ⌘/ demote on macOS in both hosts** — all three are interceptable in a mac browser (the board pane-cycle already ran on the cmd tier), and ⌘[/⌘] match native browser back/forward convention.
- **The split pair refines to the D keycap on macOS in both hosts (`macCode`)** — `split-horizontal` also demotes to ⌘D (mac-browser ⌘D is the bookmark accelerator, `preventDefault`-interceptable like the ⌘[/⌘]/⌘/ set rather than reserved like N/T/W, so no `macShellOnly` and no `browser` claim row) while `split-vertical` keeps the shifted tier as ⇧⌘D — tier-disjoint on the one code, so the mac pair is conflict-free. Win/Linux never sees the D codes: the pair lives there on its divider-mnemonic base codes ⇧Ctrl+\ and ⇧Ctrl+- (§ The default binding set), because plain Ctrl+D is the pane's EOF and two rows cannot share ⇧Ctrl+D (equal scope → `findConflicts`).
- **⌘N / ⌘T / ⌘W demote in the macOS shell only** — a browser reserves them unshifted *and* shifted (incognito / reopen-tab / close-window), so a mac browser keeps the shifted default and resolves it `reserved` (palette-only). The shell deliberately leaves the whole unshifted tier unbound, which is exactly what these spend ([desktop-shell](/run-kit/desktop-shell.md) § Keyboard-Tier Menu Seam; ⌘W is unbound-by-design there for this).
- **⌘, demotes in the macOS shell only, for a narrower reason** — the browser claims only the **unshifted** ⌘, (Preferences), not the shifted tier, so `settings-open`'s ⇧⌘, browser default is live rather than `reserved` (unlike N/T/W, which are palette-only in a mac browser). The demotion is therefore about reaching the OS-conventional ⌘, where the accelerator is free, not about escaping a shifted-tier reservation.
- **H / L / A / E / O stay shifted on every platform** — ⌘H is macOS Hide (OS/app-menu level, dead in both hosts; the shell's App menu binds `role: hide`) and ⌘A is browser select-all plus the shell's Edit-menu `selectAll` role. H and L stay shifted **as a pair**: demoting L alone would split the H/L navigation pair across two tiers. ⌘E is mac-browser "use selection for find" territory.

### Claimed keys

Claimed keys are registry data (`claimedKeys(platform, shell)`), each carrying a **`tier`** (a claim occupies one chord tier — the mac ⌘ page tier has its own claimed set alongside the shifted tier) and an `owner` that drives presentation and effect:

- **`shell`** — menu accelerators. Shifted tier: ⇧CmdOrCtrl+R (force reload, everywhere), plus ⇧Ctrl+1–9 (server switcher) and ⇧Ctrl+I (DevTools) on **win/linux only** — `SHELL_SWITCHER_DIGITS` carries `platform: "other"`. The mac switcher sits on ⌥⌘1–9, which no tier can express (Alt is excluded from every tier), so on mac the shell owns no shifted digits at all; that unrepresentability is the point of the mac accelerator choice, not a gap in the mirror ([desktop-shell](/run-kit/desktop-shell.md) § Keyboard-Tier Menu Seam). Mac `cmd` tier, present only inside the shell (`MAC_SHELL_CMD_CLAIMS`, mirroring the exhaustive mac bound set in `app/desktop/src/menu.ts`): ⌘Q quit, ⌘H hide, ⌘M minimize, ⌘R reload, the Edit roles ⌘Z/X/C/V/A, and zoom ⌘0/⌘+/⌘− (`Digit0`/`Equal`/`Minus`). Display + capture-warning data; the menu accelerator consumes the key before the page sees it anyway.
- **`system`** — shifted ⇧⌘Q (macOS logout), the mac ⇧⌘3/4/5 screenshot trio (`MAC_SCREENSHOT_CLAIMS`), and ⇧Ctrl+C/V (the win/linux terminal copy/paste convention), plus mac-browser `cmd`-tier ⌘Q/⌘H/⌘M (OS-level). Display-only. The mac rows carry `platform: "mac"` with no `shell` restriction — like logout, screenshots are system-wide, so they claim in both hosts. Mac ⇧⌘1/2/6–9 carry **no** claim: unclaimed page real estate.
- **`browser`** — the one owner that changes resolution. Shifted N/T/W (incognito / reopen-tab / close-window) outside the shell on every platform; on a mac browser also the `cmd` tier's ⌘N/⌘T/⌘W (new window / new tab / close tab), ⌘L (address bar), ⌘, (Preferences — the reason `settings-open`'s browser default stays shifted, `260801-mqim`), and tab digits ⌘1–9. Reservation is **tier-aware** — `resolveBindings` keys its reserved set on `` `${tier}:${code}` `` — so a combo resolves `enabled: false` with `disabledReason: "reserved"` only when a browser claim matches *both* its tier and its code. In a plain browser `create-session`/`create-window`/`kill-window` are therefore inert chords that stay palette-reachable, and an override onto ⌘L or ⌘, is dead on a mac browser but live in the mac shell (⌘, being the mac shell's shipped `settings-open` default there). Inside the shell every action is live.

Win/Linux claim sets are unchanged: the unshifted Ctrl tier there belongs to the pane, so it carries no claims data.

### Override layer

`localStorage["runkit-keybindings"]` stores **diffs only** — `{ [actionId]: { code, tier } | null }`, `null` meaning unbound/disabled (the same persistence shape as `runkit-terminal-font-size`; no backend, no config file, Constitution II). `resolveBindings(defaults, overrides, host)` merges them into the effective map — defaults resolved host-aware through `defaultComboFor` (§ Per-platform default tiers) — marking each binding `enabled` / `isDefault` / `disabledReason`. `parseOverrides` is tolerant — malformed JSON, a non-object root, or garbage entries degrade to "no override" — and both storage wrappers are try/catch, so an unavailable localStorage falls back to defaults without throwing.

This is also the **only** combo store: a macro's key lives here as an ordinary diff entry keyed by its actionId, never inside the macro definition. Macros therefore enter resolution as **keyless defaults** — `resolveBindings` resolves an effective combo whose `code` is `""` to `enabled: false, isDefault: false, disabledReason: "user"`, the same state a steal victim lands in, so the overlay's `unbound` affordance covers both. Builtins always carry a code and are unaffected. (`260730-hbyh`)

Rebinding is **steal-with-warning** in a single capture press: `applyCapture(bindings, overrides, actionId, combo, host, defaults?)` gives the combo to the new owner, sets the previous owner's override to `null`, and reports it as `stolenFrom` so the overlay flags it until it is rebound or reset. Re-capturing an action's own **host** default (`defaultComboFor(def, host)` — so ⌘[ counts for `go-back` on a mac) is a **no-op that short-circuits before the victim search**: it deletes the diff entry, keeping the blob diffs-only, and steals from nobody. The ordering is load-bearing — the mac defaults ship global↔board shadow pairs on ⌘[/⌘], so a victim search running first would let a no-op re-capture of either partner unbind the other. The optional `defaults` parameter is what the own-default check reads, so callers holding a **macro-extended** default set (`use-keybindings.ts`, which also passes its `host`) get the same detection as the `DEFAULT_BINDINGS` fallback. Stealing is kind-blind: a builtin capture can unbind a macro and a macro capture can unbind a builtin, one mechanism in both directions. Reactivity is an in-module pub/sub in `use-keybindings.ts` (the `use-local-storage-enum.ts` pattern — the native `storage` event fires only across tabs, which covers cross-tab for free); every write funnels through one `persist()`, and `setBinding` recomputes against freshly-read storage — **both** stores, so a macro-owned combo is steal-detected — meaning rapid successive captures compose.

`useKeybindings` composes `useMacros` and resolves over `DEFAULT_BINDINGS ∪ macros.map(macroToBinding)`. Because macros ride that one effective map, the dispatcher, the terminal seam (`attachCustomKeyEventHandler`), palette hints (`withShortcutHints`), and the overlay key map (`custom` keycap state) pick them up with **zero** consumer changes. A macro-store change also re-reads override storage, since `removeMacro` drops the deleted macro's diff without a keybinding-store notification and the two stores must not drift in-memory. `reset all` writes `{}`, which unbinds every macro combo while leaving the definitions intact — rows return to `unbound`. (`260730-hbyh`)

### Macro bindings

A **macro** is a user-named keyboard route to an action that already exists — never a shell string. `app/frontend/src/lib/macros.ts` (pure, DOM-light, colocated `macros.test.ts`) owns the model; `hooks/use-macros.ts` is its React integration. (`260730-hbyh`)

```ts
type MacroTarget =
  | { type: "riff"; preset: string }              // → POST /api/riff
  | { type: "palette"; paletteActionId: string }; // → the palette action body
type MacroAction = { actionId: string; kind: "macro"; label: string; target: MacroTarget };
```

- **Two target kinds only.** A riff target carries the preset **NAME** alone; the pane commands and arguments live in the preset definition inside `fab/project/config.yaml` — the committed-config trust boundary ([rk-riff](/run-kit/rk-riff.md) § Security / Trust Boundary). Riff targets carry **no `args` passthrough**: `POST /api/riff` has no args field and its `task` REPLACES preset panes (`composePanes`), so per-macro dynamic args would need a backend extension. `isMacroTarget` narrows structurally, so a `{type:"shell",cmd}` blob planted in localStorage is dropped on parse.
- **Definitions in `localStorage["runkit-macros"]`** — a JSON array with the tolerant-parse posture of `parseOverrides` (malformed JSON, a non-array root, and invalid entries degrade to dropped/empty; valid entries are re-projected so unknown keys are stripped and `kind` is pinned) and try/catch storage wrappers. An empty list removes the key. The combo lives in `runkit-keybindings` (§ Override layer).
- **`actionId` is `"macro:<label-slug>"`** — lowercase, non-alphanumeric runs collapse to `-`, `-2`/`-3`… suffixes on collision (the riff `resolveWindowName` convention), empty slug falls back to `macro`. It doubles as the palette action id, so the `withShortcutHints` join is free. `isMacroActionId` is the guard that keeps macros out of target lists and out of palette-target resolution — no macro→macro recursion.
- **`macroToBinding(macro)`** projects a macro into a `KeyBinding`: `kind: "macro"`, `scope: "global"`, `tier: "shifted"`, `code: ""` (keyless — the unbound resolution in § Override layer), `description`/`mapLabel` from `macroCommandPreview(target)`, which renders `rk riff --preset discuss` / `palette: {actionId}`.
- **`useMacros()`** returns `{ macros, addMacro(label, target) → actionId, removeMacro(actionId) }` with the same in-module pub/sub + native `storage` reactivity as `use-keybindings.ts`; `addMacro` uniquifies against freshly-read storage so rapid adds compose. `removeMacro` also drops the actionId's `runkit-keybindings` entry, so a later same-slug macro can never inherit a ghost combo.

**Execution** is `executeMacro` in `app.tsx`, mounted on AppShell only. A `palette` target resolves its action body by id through the stable `getAllActions()` API channel **at call time** (the merged route + layout-globals list — the § Design Decisions "Chord handlers are the palette action bodies" convention; § Single palette mount + palette-actions slot) and invokes it in place; an absent action means no handler, so the chord falls through untouched. A `riff` target is **session-gated** (the `Agent: Spawn` precedent) and calls the existing `spawnRiff(server, session, { preset })` client: success surfaces an info toast and navigates via `navigateToWindow` when `windowId` is truthy (the spawn dialog's falsy-id guard preserved); failure — including a 400 for a preset gone from fabconfig — surfaces the error text as a toast. No new endpoint, no new exec surface, no fire-and-forget. `keybindingHandlers` spreads the macro handlers **first** so a theoretical id collision resolves builtin-first. The board route mounts no macro handlers, so macro chords fall through there.

Macros are also palette-reachable without their key: one `Macro: {label}` entry per macro (id = the macro's actionId) folded into `paletteActions`, riff macros filtered out with no session. `withShortcutHints` decorates them automatically; an unbound macro renders no hint.

`findConflicts` is a pure function over the effective map — two **enabled** bindings conflict when their codes match, their tiers collide (`tiersCollide`: equal, or the overlapping `cmd`/`ctrl` pair), and their scopes are **EQUAL**. A same-combo global↔scoped pair is a **shadow, not a conflict** (§ Scope precedence). It is the **test-only invariant guard** asserting the shipped defaults are conflict-free in every host — including the mac hosts, whose defaults ship exactly one such shadow shape; the capture UI takes its steal warning from `applyCapture`'s `stolenFrom` instead, and that search stays `scopesOverlap`-wide (`global` overlaps everything, equal scopes overlap, `terminal`/`board` are disjoint since those routes never co-mount).

### Dispatch seams

- **`useKeybindingDispatch(handlers)`** — one window-level bubble-phase `keydown` per route shell, keyed by `actionId`. Rules in order: skip `defaultPrevented` events (the overlay's capture-phase rebind listener claims its own); walk the **enabled** matches in `findMatches` order (§ Scope precedence) and fire the **first one that has a handler at this mount**; when **no** match has a handler the chord falls through untouched (no `preventDefault`), so a route without context for an action leaves browser/pane behavior alone; then `shouldSuppressChord` gates real text inputs unless the binding sets `ignoreInputs`. Bindings and handlers live in refs so the listener registers once per mount and SSE ticks never churn it.
- **`shouldSuppressChord(target)`** — the single shared input-gating predicate (`lib/keybindings.ts`): suppress only when a real text input owns focus, with two carve-outs — anything inside `.xterm` (the hidden helper textarea is the terminal's normal focus state) and `.rk-chat-input` (the chat lens's analog, which the `` Ctrl+` `` toggle must escape). jsdom-safe (`contentEditable` attribute fallback for the missing `isContentEditable` getter). ⌘K and the overlay toggle carry `ignoreInputs` and skip it entirely.
- **AppShell (`app.tsx`)** mounts the dispatcher with handlers derived from the **palette action bodies** — `fromPalette(id)` looks each up by id over the **merged route+globals list** (`[...paletteActions, ...paletteGlobals]`, both this-render fresh — the globals-only channel keeps the memo loop-safe, § Single palette mount + palette-actions slot), so chords bound to layout-global ids resolve the same way as route-scoped ones: `create-session`, `create-window`, `kill-window`, `agent-next-waiting`, `go-back`, `go-forward`, `settings-open` (the `Settings: Open` global entry's body), `panel-toggle` (the surface-tile toggle — desktop window route with ≥1 available NON-TTY surface, toggling that surface's tile via `togglePanel` → `addSurface`/`closeSurface` — § Right Rail) — plus `layout-cycle` (reuses the `Layout: Cycle Shape` palette body — the next same-arity preset, order kept; § Surface Layout), `window-prev`/`window-next` (a modulo cycle over the current session's windows in sidebar order, wrapping, through the rich `navigateToWindow` path; gated by `canCycle`, so the chord no-ops off a window route), the `shortcuts-overlay` toggle (the global entry's layout-owned `onSelect`), `compose-toggle` (chrome-context's `toggleComposeStrip`, the same body the `>_` chip and `View: Text Input` run), `open-last-used` (registered only when `windowParam` is set — § Open-in-App palette actions), `split-horizontal`/`split-vertical` (the two `Window: Split …` palette bodies, whose block is gated on a current session + window, so off a window route `fromPalette` yields `undefined` and the chord falls through — and which additionally carry the registry's `ttyOnly: true` flag, so `app.tsx` treats their handlers as ABSENT unless the focused tile kind is `tty`: with a non-tty tile focused the split chord falls through untouched, no `preventDefault`, while the palette's `Window: Split …` rows stay invocable regardless of focus — the gate wraps only the chord handler map, § Surface Layout → Focused tile; `260812-wfic`), and one handler per stored macro (§ Macro bindings).
- **BoardPage (`board-page.tsx`)** mounts its own dispatcher with `board-cycle-next`/`board-cycle-prev` (both handlers are absent when the board has no panes, so the chords fall through untouched), `go-back`/`go-forward`, `shortcuts-overlay` (resolved from the layout global list via `usePaletteGlobals` — the overlay state is layout-owned, § Single palette mount + palette-actions slot), `compose-toggle` (the same chrome-context body — the compose strip has a board footer mount too), and `settings-open` (`useSettingsDialog().openSettings` lifted above the handler memo for the dep). `open-last-used` and the `split-horizontal`/`split-vertical` pair are deliberately unhandled here: the Open control and the split palette bodies are terminal-route-only, so those chords fall through. The two route shells never co-mount.
- **Four chords keep their own component-local listeners** rather than joining the dispatcher, because their enablement is local state: ⌘K in `command-palette.tsx` (palette open-state), ⌘\ in `shell.tsx`'s `useSidebarKeyboardToggle`, ⌘. in `app.tsx`'s view-cycle effect (lens availability), and `` Ctrl+` `` in `use-chat-view-shortcut.ts` (its chat-enablement gate). Each reads its binding through `byAction` and matches with `matchesCombo` + `shouldSuppressChord` — definition and suppression are the registry's, enablement is the seam's.
- **Terminal seam** — `terminal-client.tsx`'s `attachCustomKeyEventHandler` keeps the ⌘C copy case first, then delegates the refusal decision to the pure predicate `shouldRefuseTerminalChord(e, bindings, platform)`, reading both the bindings and the host platform through render-updated refs (the handler attaches once at terminal init, but overrides can change any time). Returning `false` means xterm neither handles nor transmits the key, and xterm does not `preventDefault` a refused key, so the event bubbles to the window dispatcher and the action fires while the terminal owns focus. Two refusal rules:
  - **Any enabled shifted-tier match, on every platform** — legacy TTY encoding cannot distinguish Ctrl+Shift+letter from Ctrl+letter, so xterm would emit the Ctrl-char; refusing costs the pane nothing.
  - **On macOS only, an enabled `cmd`-tier match pressed with `metaKey`** — ⌘ chords never reach the pane as control bytes, so refusal is loss-free and the demoted ⌘[/⌘]/⌘/ (plus shell-host ⌘N/T/W) fire under terminal focus. The `metaKey` gate is load-bearing: `matchesCombo`'s cmd tier also accepts plain Ctrl, and mac Ctrl+[ is ESC.
  **Plain-Ctrl chords are never refused on any platform** (Ctrl+W delete-word, Ctrl+L clear, Ctrl+T fzf, Ctrl+[ ESC). On Win/Linux the cmd rule never applies at all — cmd-tier combos *are* plain-Ctrl chords there — so the seam is byte-identical to its pre-demotion behavior.

The chords are SPA-renderer bindings, **not Electron menu accelerators** — the shell's documented seam is accelerator *avoidance*, so the letters work identically in shell and browser hosts; only which modifier is bound varies (§ Per-platform default tiers).

### Scope precedence — scoped beats global

`findMatches(e, bindings)` returns **every** enabled matching binding, ordered non-`global` scopes first (registry order within each class). This exists because the mac demotion makes two shipped defaults share a combo by design: the board pane-cycle pair `board-cycle-prev`/`board-cycle-next` (`scope: "board"`) and the global `go-back`/`go-forward` both sit on ⌘[/⌘] on mac hosts.

Resolution is **precedence, not conflict**: the board route keeps pane-cycle on ⌘[/⌘] (back/forward stays palette-reachable there), every other route gets history navigation, and a *paneless* board — where the cycle handlers are absent — falls through to the global back/forward at the same mount, because the dispatcher's walk skips matches with no handler. `findConflicts` therefore flags only **equal**-scope pairs, while `applyCapture`'s steal search deliberately stays `scopesOverlap`-wide (§ Override layer, § Design Decisions).

### Shortcuts overlay (`components/shortcuts-overlay.tsx`)

The cheatsheet is **THE single shortcuts surface** — app chords, user macros, and tmux keybindings in one sheet. It is a focus-trapped **dialog, not a route** (Constitution IV fixes the route set), opened by three affordances that all TOGGLE it: the `shortcuts-overlay` chord (⌘/ on mac hosts, ⇧Ctrl+/ on Win/Linux), the `Help: Keyboard Shortcuts` palette action, and the chevron menu's "Keyboard shortcuts" App-section row (via the `shortcuts-overlay:open` document CustomEvent — § Right cluster → overflow chevron menu). It mounts **once in `AppLayout`** (`LayoutShortcutsOverlay`) with layout-owned open state — the chord and the palette entry both resolve the layout-global `Help: Keyboard Shortcuts` entry's `onSelect` through the merged palette list (§ Single palette mount + palette-actions slot). It uses `useFocusTrap`, the bracket-heading vocabulary, `rk-glint` buttons, accent-green states, and theme tokens; `data-testid="shortcuts-overlay"`. Rows, keycap states, and capture all read the **host-effective** map, so the per-platform tiers surface without per-consumer branching.

**No tabs — one scroll, one filter.** The core query is "what's the key for X?", and the user should not have to know which system owns X before asking. So a single filter input spans app rows + macro rows + tmux rows, and everything lives in one scroll container. Navigation is a **sticky jump-nav chip row** under the header instead (`<nav aria-label="Shortcut sections">`, `data-testid="shortcuts-jump-nav"`): `key map · global · terminal · board · custom · tmux`, each chip a `scrollIntoView` anchor onto a section ref. While a filter is active each chip additionally renders its **live match count** and dims (`opacity-40`) at zero hits, so matches below the fold stay discoverable. The `custom` chip renders only when that section does; the `key map` chip carries no count and dims while filtering (the map hides then). Contents in document order:

- **ONE keycap grid with a modifier picker**, **foldable** via a `▾ collapse map` / `▸ expand map` toggle and **auto-hidden entirely while a filter is active** (a keycap grid cannot answer a text query, and the reclaimed vertical space keeps row hits above the fold on short viewports). The **tables are the authority** for effective chords; the map is a **discovery + rebind tool** — which is why one grid answers "what does my keyboard do while I hold these modifiers" rather than two grids re-answering per-action chords the rows already carry. The grid is three rows of keycaps where a key renders `bound` (accent-green, host default combo), `custom` (accent, overridden), `claimed` (amber, dimmed — claimed wins over bound so a browser-reserved N reads claimed) or `free`, annotated with the binding's `mapLabel`, all via one shared `keyCell(states, code, idx)` over a per-layer state map. The map stays **app-layers-only**: tmux prefix chords are sequences, not combos, and do not fit the model. The digit run collapses into a **decorative `…` ellipsis cell** standing for Digit3–Digit8, which takes its claimed styling from **`Digit3`** — the one digit carrying a claim on *both* displays (switcher-claimed on win/linux, screenshot-claimed on mac). Keying it off the run's first member is what keeps mid-run claims visible on the mac display, where `Digit1`/`Digit2`/`Digit9` have their own unclaimed cells.
  - **The picker IS the map label**: the header's left slot reads **"Holding"** followed by a segmented control (`role="group"`, `aria-label="Keyboard map modifier"`, `aria-pressed` per segment, the platform-toggle visual idiom) whose selected segment supplies the modifier caps — `⇧ ⌘` | `⌘`. Selecting a segment swaps which layer the single grid renders, feeding `tierKeyStates(activeMapTier)`.
  - **The `⌘` option is a macOS-display affordance only** — the Win/Linux unshifted layer is plain Ctrl and belongs to the pane, so that display renders a static **"Holding Shift Ctrl"** label instead (a one-option segmented control is dead chrome). Default selection is `⇧⌘`. The active layer is **derived** (`displayPlatform === "mac" ? mapTier : "shifted"`), so switching the display away from macOS falls back to the shifted layer while the `⌘` selection survives and reapplies on return.
  - **Session-scoped view state**, like the fold: `mapTier` and `mapFolded` are both deliberately excluded from the on-close reset effect — a chosen layer and a folded map are reading preferences, unlike the transient filter/capture state the close effect clears.
  - The map header also carries the **"plain Ctrl always reaches the pane"** note (it explains why there is no Ctrl layer to pick) and the fold toggle, all on one row.
  - **Legend** (four states): `bound` · `custom` · `claimed — taken by the OS / browser / app menu (the desktop app frees the browser ones)` · `free`. The claimed entry is where the browser-vs-desktop story lives in the map — one line, no per-map captions.
- **Platform display toggle** (macOS ↔ Win·Linux) — a **view** control initialized from the detected host: it restyles keycaps and gates the `⌘` picker option, never what this host must actually press. Capture and the header chord always read the physical `host.platform`; claim lookups combine the *display* platform with the *physical* host's `shell` flag.
- **Header chord hint** — `{chord} toggles this sheet` derives from the effective map (`byAction.get("shortcuts-overlay")`) formatted for the physical host, so it reads ⌘/ on a mac and reflects overrides; it is **hidden entirely** when that binding is unbound or disabled rather than advertising a dead chord.
- **Filter input** over label + description, and a per-scope **grouped row list** (GLOBAL / TERMINAL / BOARD) with `ScopeBadge` pills on non-global rows.
- **Shell-owned locked rows are a subgroup at the END of GLOBAL** (a `SubHead` reading "Shell-owned — accelerators live in the desktop shell menu"), not a top-level section — three flavors of locked top-level section was one too many once tmux arrived. Same 🔒 non-interactive idiom (accelerators are edited in the shell menu; the DevTools row is win/linux only), and the rows still count toward the `global` jump chip. Locked rows are **free-form caps arrays**, not tier-constrained, which is what lets the `Switch to server 1–9` row render `⌥ ⌘ 1…9` on the mac display — diverging from the shared `tierCaps` helper that still gives `Force reload` ⇧⌘R — while win/linux keeps `Shift Ctrl 1…9` on both rows. No registry or tier change backs the divergence, because the mac switcher accelerator lives outside every tier.
- **Click-to-rebind capture** — arming a row installs a capture-phase window listener that `preventDefault`+`stopPropagation`s (keeping the chord away from the focus trap, the dispatcher, and the legacy listeners); Esc cancels without persisting; a modifier-only or tier-less press keeps capturing. After a successful capture the row shows a steal warning naming the victim, or a claimed-key warning when the combo is spoken for — the claim lookup is **tier-aware** (`c.tier === combo.tier && c.code === combo.code`), so capturing ⌘R inside the mac shell warns that the shell owns it while ⇧⌘R warns separately.
- **Host-divergent rows carry the fact themselves** — a `desktop` pill (ScopeBadge/reserved-pill idiom) plus the OTHER host's chord as secondary text: `in browser: ⇧⌘T` inside the shell, `in desktop app: ⌘T` in a mac browser (there alongside the amber `browser` reserved pill). The gate is `host.platform === "mac" && b.isDefault && b.macTier != null && b.macShellOnly === true`, so exactly the `create-session`/`create-window`/`kill-window` trio qualifies — the only `macTier`+`macShellOnly` set in `DEFAULT_BINDINGS`. It is a **physical-host** fact, never the display toggle, and only at the host default: an override applies verbatim on both hosts and an unbound row has no chord, so either collapses the divergence and drops the badge. Host-invariant rows and win/linux hosts never render it. Most of the ⌘ layer (⌘K, ⌘., ⌘[/⌘], ⌘/) works in a browser tab too, so the layer as a whole is **not** labelled desktop-only. Derivation reuses existing seams: `macTier`/`macShellOnly` ride onto `EffectiveBinding` through `resolveBindings`'s spread, and the other-host chord is `defaultComboFor(def, { platform: "mac", shell: !host.shell })` over the base def read from `DEFAULT_BINDINGS` (the effective row's `tier` is overwritten by resolution, so the base def is the correct input). No registry schema change.
- **Per-row modified dot + ↺ reset**, an `unbound` button on user-disabled rows, a `browser` badge on reserved rows, and a footer with the localStorage note + **reset all**.
- **`[ CUSTOM ]` section** (`data-testid="macro-section"`) — the editable macro tier (§ Macro bindings). One row per macro: the label, a monospace `macroCommandPreview` chip, the same click-to-rebind combo button as a builtin row (an unbound macro shows the amber `unbound` button; a browser-reserved combo shows the `browser` badge), and a `✕` delete that drops the definition **and** its combo diff. A riff macro whose preset is absent from the fetched preset list renders a red `missing preset` badge — no silent fallback. The grouped GLOBAL/TERMINAL/BOARD lists filter macros out (`b.kind !== "macro"`), so each macro renders exactly once; the filter input searches macro labels and previews too.
- **`+ bind a key to a palette action or riff preset…`** — an inline add flow: a searchable target list (riff presets + the mount's palette actions, macros excluded), a name input pre-filled from the picked target's label, and `add + capture key`, which creates the macro and immediately arms capture on the fresh row — naming and binding in one gesture. Target data arrives via optional props `paletteTargets` / `riffPresetNames`, fed by the single layout mount (`LayoutShortcutsOverlay`): `paletteTargets` is the merged palette list minus the macro entries themselves (no macro→macro chains), and **`paletteTargets == null` gates the whole add flow off** — the layout mount passes none unless the route has a server + session, so on board/host routes the CUSTOM rows render read/rebind/delete-only. `riffPresetNames == null` means "preset list unknown" — no badges, no riff targets offered.

- **`[ TMUX ]` section** (last, `data-testid="tmux-section"`) — the pane's own keybindings, **read-only**. tmux keys are pressed inside the pane and configured in `configs/tmux/default.conf`; run-kit only documents them, so every row is the same 🔒 locked, non-interactive idiom as the shell-owned subgroup (no capture, no reset, no rebind). Data is the existing curated `getKeybindings(currentServer)` client API (`app/backend/api/keybindings.go`'s whitelist — a read-only dependency, unchanged). Two `SubHead` groups: **Direct** for root-table rows (F2/F3/F4 new/prev/next window, Shift+F3/F4 prev/next pane, Shift+F7 copy mode) and **Prefix — Ctrl+S, then key** for prefix-table rows, rendered as a keycap **sequence** (`Ctrl` `S` *then* `\`) rather than a simultaneous combo. Both subheads hide while filtering (the group labels are navigation, not matches). Key-name formatting is `S-` → `Shift+`, `C-` → `Ctrl+`.

  The server comes from `useSessionContext().currentServer` read **inside** the overlay, and the fetch runs only while the overlay is open, cancel-flagged on close/server change. `null` bindings means loading; `[]` means no current server (board/host routes), an empty response, or a failed fetch — all three collapse to the one-line **"No tmux server running"** empty state, so there is no spinner deadlock and no crash path.

`riffPresetNames` comes from a best-effort `getRiffPresets(server, session)` fetch inside `LayoutShortcutsOverlay` (`app.tsx`) that runs only **while the overlay is open on a route with a session**, mapped to names; a failed fetch leaves it `null`. Detection is therefore advisory — `POST /api/riff` validates authoritatively at execution time either way (§ Macro bindings), so a macro is never silently inert.

Export/import of overrides is not offered.

### Palette hints

`withShortcutHints(actions, byAction, platform)` (pure, in `lib/keybindings.ts`) decorates a palette action array with each action's **effective** combo as its `shortcut` hint — `actionId` doubles as the palette id, so the join is free. Each route applies it to the list it registers into the slot (AppShell's `paletteActions`, the board's `boardRouteActions`), and the layout applies it to the global groups inside `use-global-palette-actions.ts` (§ Single palette mount + palette-actions slot). Formatting is per platform via `formatCombo`/`comboParts`: mac symbol tiers join bare (`⇧⌘N`, `⌘.`), other platforms join with `+` (`Shift+Ctrl+N`), and the `ctrl` tier keeps the `` Ctrl+` `` spelling on both. Hints reflect overrides. A **disabled** binding (user-disabled or browser-reserved) contributes NO hint — advertising a dead chord would lie — while the palette entry itself stays reachable. Actions with no registered binding pass through untouched, so hand-set hints survive; `buildViewActions` (`lib/palette-view.ts`) takes the effective cycle/chat hint strings as parameters rather than hardcoding them.

The **page tier** — the unshifted `⌘<key>` tier on macOS, unshifted `Ctrl+<key>` on Windows/Linux — is left entirely unbound by the Electron desktop shell on every platform it packages for ([desktop-shell](/run-kit/desktop-shell.md) § Keyboard-Tier Menu Seam). The SPA spends part of that budget on **macOS**: ⌘[/⌘]/⌘/ in both mac hosts and ⌘N/⌘T/⌘W inside the mac shell (§ Per-platform default tiers), alongside the legacy ⌘K/⌘\/⌘. chords that always lived there. Host-conditional demotion is the `isShell()` gate in `defaultComboFor` (`lib/shell.ts`, the `window.runkitShell` detection seam). Whatever the SPA does not take stays the browser's or the OS's: mac-browser ⌘N/T/W/L and ⌘1–9 are claimed data, and on **Windows/Linux the page tier remains untouched by the SPA** — plain Ctrl is the pane's.

### Education micro-copy (placeholders, empty states, in-situ hints)

Discoverability copy lives **at the point of use**, never in a dedicated surface — placeholders, empty states, and existing Tips teach the hidden feature they sit on, each string computed where the mode is known so it cannot lie about it (§ Design Decisions → Placeholder over inline legend; → Hints render from current state alone). No new routes, dialogs, or persistent state — the whole apparatus is copy plus existing Tip `kbd` slots and menu-row keycap chips. (`260811-ke2s`)

- **Mode-aware placeholders.** The compose strip's textarea is the exemplar (§ Docked Compose Strip): terminal target `Compose text — Enter inserts · {submitKeycap} sends · ↑ history` (the sole surfacing of ↑ sent-history recall anywhere in the UI), selection broadcast `Compose prompt — {submitKeycap} sends to all selected`, no target `No focused terminal — click a pane to target it`. The chat send box reads `Message the agent — Enter for newline · {submitKeycap} sends` — chat's plain Enter deliberately diverges from the strip (newline, not insert-line), and the placeholder is where the divergence stops surprising ([chat](/run-kit/chat.md) § Send-form input box). The command-palette input reads `Type a command — try Board: Pin: View: Window:` and its no-results row `No results — try a prefix: Board:, Pin:, View:, Window:` — typed-namespace education, not a chord, so neither branches on pointer type (the confirming step's `Confirm action...` is untouched).
- **Empty states name their fill action.** No bare labels: the board page and host BOARDS zone name the sidebar row 📌 + `Pin:` palette route; SERVICES reads `No services — listening TCP ports appear here automatically`; HOST HEALTH `No metrics — waiting for the host's first report`; a session-less server group reads `(no sessions — + new, or {chord})`; every `{chord}` here is fine-pointer-only (see the coarse-pointer bullet below); the shortcuts overlay's no-match line scopes its own filter (`… · the filter spans app, custom & tmux keys`).
- **Platform-aware keycaps, never hardcoded lies.** Fixed Enter-policy chords (owned by `classifyComposeEnter`, not the registry) use the `composeSubmitKeycap()` seam. Chords for **rebindable** actions (`create-session`, `command-palette`, `open-last-used`) are read from `useKeybindings().byAction` + `formatCombo(…, host.platform)` and the clause is **omitted when the binding is unbound/disabled** — the `sheetChord`/palette-hints rule (§ Palette hints, § Tier-1 `Tip`) extended to prose copy. The derivation stays inline at each call site (no shared helper — readability over cleverness).
- **Menu-row keycaps.** A menu row whose action has a registry binding carries a trailing right-aligned `kbd` chip (the shared `MENU_ROW_KBD_CLASS`, palette-row visual weight, `ml-auto`, `aria-hidden` so the chord stays out of the accessible name), derived inline by the same `byAction` + `formatCombo` enabled-else-`undefined` rule — the SplitControl direction popover rows, the overflow chevron menu's split rows (§ Split control), and the App-section Settings / Keyboard shortcuts rows. A menu is itself a discovery surface, so every open doubles as a shortcut lesson; rows without a matching binding stay keycap-free. (`260811-0f3d`)
- **Coarse pointers keep the short copy.** Keyboard-chord hints never render on touch — the Tip suppression rule (§ Design Decisions → Suppress-by-early-return on coarse pointers) applied to strings via `useCoarsePointer()`. It binds **every** chord clause, not just the placeholders: placeholders fall back to their short forms (`Compose text…` / `Compose prompt…` / `Message the agent…`); the SelectionIndicator drops both its `⇧click extends` clause and its `{chord} → Selection:` clause; and the empty states drop their chord tail, leaving the pointer path alone (`(no sessions — + new)`, `… click its 📌` on the board page and the host BOARDS zone). Each site folds the pointer test into the same expression that derives the chord (`!coarse && binding?.enabled ? formatCombo(…) : undefined`), so an unbound binding and a touch pointer collapse to one `undefined` and the copy has a single fallback branch.
- **Stateless.** Every hint renders from current state alone (selection active, binding bound, pointer type) — no first-run tours, dismissal flags, or rotating tip carousels.

### e2e — `shortcut-registry.spec.ts` (+ `.spec.md`)

A mocked-backend spec covering ⇧Ctrl+H/L window cycling, ⇧Ctrl+[ back navigation, ⇧Ctrl+/ overlay open/filter/Esc-close, the `Help: Keyboard Shortcuts` palette entry, click-to-capture rebind persisting the localStorage diff, palette hint rendering, and browser-reserved N inertness. Two cases cover the merged surface: the jump-nav chips + TMUX locked rows (fed by a mocked `GET /api/keybindings*`, prefix rows asserted on their `then` sequence separator) with one filter driving live per-chip counts across app and tmux rows, and the absence of a `Help: tmux Keybindings` palette entry. A **mac block** spoofs the platform with an init-script getter override on `Navigator.prototype.platform` (`"MacIntel"`) so `detectPlatform()` resolves `mac` and the demoted defaults apply: ⌘[/⌘] retrace history (with ⇧Ctrl+[ proven inert), ⌘/ toggles the overlay and the map's modifier picker offers the ⌘ layer (the `Keyboard map modifier` group; ⌘ starts `aria-pressed="false"` — ⇧⌘ is the default — and clicking it selects the layer, asserted by the mac-browser `address bar` claimed cell), and both ⌘N and ⇧⌘N stay inert in a mac *browser* host. Three cases cover the **split chords** (`260807-rbx5`), fed by a per-test body-capturing `**/api/windows/*/split*` route mock: ⇧Ctrl+\ then ⇧Ctrl+- on the terminal route posting `{horizontal: true, cwd: "/tmp/win-one"}` then `{horizontal: false, cwd: "/tmp/win-one"}` to `POST /api/windows/@1/split` (pressed while the xterm textarea owns focus, so the shifted-tier seam refusal is exercised too); the palette hints — both `Window: Split …` entries rendering their divider-pair chords `Shift+Ctrl+\` and `Shift+Ctrl+-` exactly once each; and inside the mac block, ⌘D then ⇧⌘D posting the same two bodies from the one D keycap on the two tiers (the `macCode` refinement). Deep mac paths — shell-host N/T/W demotion, the claimed sets, the refusal predicate — are unit territory in `lib/keybindings.test.ts`, since e2e runs on Linux.

### e2e — `macro-riff-bindings.spec.ts` (+ `.spec.md`)

A second mocked-backend spec for the macro tier (`260730-hbyh`): the overlay add-flow end-to-end (pick `riff: discuss` from the mocked `GET /api/riff/presets*` preflight → name → `add + capture key` → ⇧Ctrl+D) asserting both localStorage stores and the preview chip, then the chord POSTing `{session, preset}` — **preset name only** — to a mocked `POST /api/riff*` with a toast + navigation to the spawned window; the `Macro: {label}` palette entry rendering its `Shift+Ctrl+D` hint and executing the same path; and a seeded macro targeting a preset the preflight does not define showing the `missing preset` badge with the spawn's 400 surfacing as an error toast and no navigation. The spawn window `@9` sits in the static `/ws/state` snapshot from the start so post-spawn navigation confirms rather than tripping the switch-confirm watchdog. Both route globs carry the trailing `*` the `?server=`/`?session=` queries need, and the spawn glob `**/api/riff?*` cannot match `/api/riff/presets` (glob `?`/`*` never match `/`). (⇧Ctrl+D is a clean capture here: e2e runs on Linux, where the split defaults live on `\`/`−` and the D codes exist only behind the mac `macCode` refinement.)

### Single palette mount + palette-actions slot

Exactly one (lazy) `CommandPalette` mounts in `AppLayout` (`LayoutCommandPalette` in `app.tsx`) and renders the merged list `[...routeActions, ...globalActions]` — the active route's registered actions first, then the layout-level global groups, both already shortcut-decorated. Because the mount lives above the router outlet it exists on EVERY route, including the Host page `/` (where no route registers, so the palette shows the global groups only — never a previous route's stale entries; registration clears on unmount).

- **The slot** — `contexts/palette-actions-context.tsx` (the `top-bar-slot-context` register-into-a-root-mount pattern): routes publish their route-scoped, already-shortcut-decorated lists via `useRegisterPaletteActions(actions)` (referentially-stable dispatcher, last-writer-wins, clear-on-unmount). AppShell registers its `paletteActions` (session/window/board/selection/view/open/theme/config/status-refresh/server/shell-server/push/window-switch/agent/spawn groups plus the macro entries); BoardPage registers `boardRouteActions` (§ Boards Command Palette).
- **The global groups** — `hooks/use-global-palette-actions.ts` builds them ONCE at the layout level from the shared `lib/palette-*` builders: nav (`buildNavActions`, mode from the same deepest-first route-param walk `RootTopBar` uses), the terminal-font trio, `View: Refresh Page`, `Help: Documentation` (shared `HELP_URL`), `Help: Keyboard Shortcuts`, `Settings: Open` (`useSettingsDialog`), and update/check/maintenance/version over `useUpdateNotification` + `useUpdateCheck` — decorated with `withShortcutHints` and handed to the provider as its `globalActions` prop.
- **The merged list flows back to routes** for id-based resolution, over deliberately split channels (§ Design Decisions → The palette slot splits state from a stable API channel): AppShell's `fromPalette` chord dispatch resolves over `[...paletteActions, ...usePaletteGlobals()]` (zero-lag, this-render fresh); macro target invocation resolves via the imperative `getAllActions()` at call time; the layout leaves that RENDER the list (the palette itself, `LayoutShortcutsOverlay`'s macro-target candidates) subscribe to the reactive `usePaletteActions()` channel. No chord or macro target loses its handler when an id's ownership moves between route and global groups.

### Command Palette Actions

**Shell server-switch palette entries** (`shellServerActions`, the first shell-gated palette block): one `Server: Switch to "<name>"` action per **shell**-registered rk server, active one suffixed ` (current)`, ids `shell-switch-server-<uuid>`. The **quoted** name is what distinguishes these from the unquoted tmux `Server: Switch to {name}` entries — a shell server is a whole rk instance addressed by URL, a tmux server is a socket on the current host. Composition is the pure builder `buildShellServerActions(servers, onSwitch)` (`lib/palette-shell.ts` + colocated vitest, following the `palette-view.ts`/`palette-pin.ts` convention); `app.tsx` feeds it `useShellServers()` (`hooks/use-shell-servers.ts` — one `listShellServers()` fetch per mount, resolving `[]` in a plain browser so no caller needs an `isShell()` pre-check) and folds the memo into `paletteActions` right after `serverActions`. Selecting an entry hands off to `switchShellServer(id)`: the **shell** then loads that server's URL — a full page swap, so no SPA-side navigation follows — and a denied or failed bridge call surfaces as an error toast. AppShell route list only (the board route registers board-scoped actions only), mirroring where the tmux `serverActions` live. The mount-time snapshot has one known staleness window: removing a non-active server in the shell's native menu leaves its palette entry until the next page load. Scope is **switch-only** — Add/Remove Server stay in the shell's native menu + welcome flow. The keyboard paths to the same operation are the shell-side switcher accelerators (⌥⌘1–9 on mac, ⇧Ctrl+1–9 on win/linux) and these palette entries (Constitution V). (`260730-9lez`)

Command palette actions include: create/rename/kill session, create/rename/kill window, move up/down for the current server/session/window (`260705-bpnr` — see § Command Palette Move up/down), theme switching, "Reload tmux config" (targets the active server via `?server=` param), "Create tmux server" (opens name dialog, creates session "0" in $HOME), "Kill tmux server" (confirmation dialog, kills active server, switches to next available — the dialog carries a daemon-specific warning when the target is `rk-daemon`; see § Infra-server de-emphasis → kill guard), "Switch tmux server: {name}" (one entry per available server, current marked), `Server: Switch to "{name}"` (one **quoted** entry per desktop-shell-registered rk server, active marked — present only inside the shell; see § Shell server-switch palette entries above), `Help: Keyboard Shortcuts` (id `shortcuts-overlay` — toggles the registry-driven cheatsheet overlay, THE single shortcuts surface incl. its read-only TMUX section; a layout-level global entry — § Single palette mount + palette-actions slot), "Copy: tmux Commands" (opens tmux commands dialog — only visible on terminal route when `currentWindow` is available), "Notifications: Enable push" / "Notifications: Enabled ✓" (the Web Push opt-in gesture — terminal-themed label, NOT a bell; see § Notifications (Web Push opt-in)), `Macro: {label}` (one per user-defined macro, id = the macro's actionId, hint = its effective combo; riff macros session-gated — see § Keyboard Shortcuts → Macro bindings), and terminal navigation (jump to any session/window).

**Session/window creation actions in the palette**:
| Action ID | Label | Behavior |
|-----------|-------|----------|
| `create-session` | "Session: Create" | Instant creation — no dialog (see Instant Session Creation) |
| `create-session-at-folder` | "Session: Create at Folder" | Opens `CreateSessionDialog` pre-filled with `currentWindow.worktreePath`; empty if no active window |
| `create-window` | "Window: Create" | Instant window creation — the sidebar `+ New Window` path. Creates an **unnamed** window (`createWindow` called with no name), so tmux auto-names it to the active window's folder basename; the optimistic ghost is labeled with the **raw** cwd basename (see § Unnamed `+ New Window`) (`260707-j66b`) |
| `create-window-at-folder` | "Window: Create at Folder" | Opens `CreateSessionDialog` in `mode="window"` (dialog title changes, session name input hidden, confirms via `createWindow(server, session, undefined, cwd)` — no name, so tmux auto-names to the folder basename); only shown when a session is active (`260707-j66b`) |
| `create-iframe-window` | "Window: New Iframe Window" | Opens dialog with name + URL inputs; creates iframe window via `createWindow(server, session, name, undefined, "iframe", url)`; only shown when a session is active. `@rk_type=iframe` seeds "default view = web" (§ Window Views (Lens Model)) (`260714-t97o`) |

**Window rename action**: the `rename-window` action ("Window: Rename") dispatches a `window-heading:rename` `CustomEvent` (guarded on `currentWindow`), which the centered top-bar `WindowHeading` listens for and enters inline edit (mirrors `theme-selector:open`), rather than opening a modal dialog. The action stays registered (Constitution V keyboard path); there is no window rename `<Dialog>` (the SESSION rename dialog is separate). See § Window Heading. (`260703-5ilm`)

**Window move actions**: "Window: Move up" (id `window-move-up`) and "Window: Move down" (id `window-move-down`) in the `windowActions` group — the sole window-move pair, matching the up/down vocabulary of the Session / Server / Board move actions (windows render as vertical sidebar rows). Only shown when `currentWindow` exists. "Move up" excluded when the current window is at the minimum index in the session; "Move down" excluded when at the maximum index (boundary exclusion, not disabled state). On select, calls `moveWindow(server, currentWindow.windowId, targetIndex)` (source by stable window ID, destination by position) then navigates to the **same** `currentWindow.windowId` with `search: (prev) => prev` — the window keeps its ID across the reorder (only its index changes) and the current view is preserved (`260714-r7rq`), so the user stays on their window and view after the swap. The target index is computed via the shared pure helper `computeWindowMoveTarget(index, delta, minIndex, maxIndex)` (returns `null` at a boundary — no wraparound). (`260705-bpnr`, `260719-p8pv-window-move-palette-consolidation`)

**Command Palette Move up/down (server / session / window)**: a keyboard/touch reorder path parallel to the two drag surfaces, giving all THREE hierarchy levels a keyboard-reachable reorder (Constitution V). Six `PaletteAction`s, each acting on the **current** route-context entity (the `Session: Move` / `Window: Move` current-entity semantics), grouped with their existing families in `app.tsx`, all with boundary = **hidden** (the entry is filtered out at the top/bottom of its list — no wraparound, no disabled state): (`260705-bpnr`)

- **`Server: Move up` / `Server: Move down`** (`server-move-up`/`-down`, `serverActions`) — move the current server one position within the REGULAR class (infra excluded). The new full regular-class order is computed and POSTed via `setServerOrder` (server-global `event: server-order` echo re-sorts every client). Hidden when the current server is infra (`currentRegularIdx === -1`) or at a class boundary.
- **`Session: Move up` / `Session: Move down`** (`session-move-up`/`-down`, `sessionActions`) — move the current session one position in the **effective** session order (`deriveEffectiveSessionOrder(liveNames, sseOrder)` — persisted `@rk_session_order` filtered to live names, un-ordered live sessions appended in natural order; the sidebar's transient drag override is component-local and NOT visible to the palette). Persisted via the EXISTING `setSessionOrder(server, order)` — the same primitive the sidebar session drag uses; no new backend.
- **`Window: Move up` / `Window: Move down`** (`window-move-up`/`-down`, `windowActions`) — move the current window ±1 index via `moveWindow(server, windowId, targetIndex)`, then navigate to the same stable window ID with `search: (prev) => prev` (view-preserving). The sole window-move pair. See § Window move actions above.

**`Board: Move up` / `Board: Move down`** (`board-move-up`/`-down`) — the same boundary-hidden / no-wraparound keyboard+touch path for the board list, registered in `board-page.tsx`'s `boardRouteActions` (NOT `app.tsx`), because "current board" only has meaning on a `/board/$name` route (the board registers its own route-scoped list — § Single palette mount + palette-actions slot). Each acts on the CURRENT board (the `/board/$name` param), computes the full new order via `computeMoveOrder(boardNames, currentBoardIdx, delta)` over the backend-sorted `boards`, and POSTs it via `setBoardOrder` (server-global `event: board-order` echo re-sorts every client — see § Board-list drag reorder). Hidden when the current board is first (`currentBoardIdx === 0` → only Move down) or last (only Move up); a `null` from `computeMoveOrder` is a defensive no-op. `onError` surfaces `useToast().addToast`. (`260708-a2qd`)

**Pure order-computation helpers** — the boundary gating, effective-order derivation, and swap arithmetic are extracted into `app/frontend/src/lib/palette-move.ts` (Vitest-covered `palette-move.test.ts`) so they are testable without mounting the shell; the palette action bodies are thin wrappers that call these then invoke the client mutation. Exports: `deriveEffectiveSessionOrder(liveNames, sseOrder)` (SSE-order-∪-natural), `computeMoveOrder(order, currentIdx, delta)` (insert-before splice, `null` on boundary/out-of-range — shared by Server, Session, AND Board Move), and `computeWindowMoveTarget(index, delta, minIndex, maxIndex)` (target index, `null` on boundary — used by the Window up/down pair). `MoveDelta = -1 | 1`.

**Selection palette commands (`Selection:` prefix)** — the action surface for the sidebar's window-row multi-select (§ Window-Row Multi-Select). Composed by the pure builders in `lib/palette-selection.ts` (+ colocated `palette-selection.test.ts`, the `palette-pin.ts`/`palette-move.ts` convention) with thin `onSelectAll(keys)` / `onClose(keys)` / `onCompose(keys)` / `onMove(targetSession)` bodies wired in `app.tsx`'s `selectionActions` memo. Four entry kinds, every one **omitted rather than disabled** when ineligible (the family's convention) — close and send need only a non-empty selection, so they are the two that stay eligible across a **cross-server** one (§ Design Decisions → Close and send are cross-server): (`260807-nf9f`, `260808-ebgs`)

- **`Selection: Select all merged (N)`** (id `selection-select-all-merged`) — replaces the selection with exactly the current server's merged-PR windows via `selectOnly`. Merged comes from the PR knowledge the frontend already has: `win.prState === "merged"` read directly (`mergedWindowKeys`), the same field `prDotState` reads for its tier-1 merged state (§ Shared PR vocabulary). Scoped to the current server — a cross-server selection would dead-end the single-server bulk move below. Omitted with no current-server context or no merged windows. **It also carries the feature's discovery surface**: `SELECTION_GESTURE_HINT` (`x · ⌘-click · ⇧-click`) rides its `shortcut` badge, which the palette renders as a `<kbd>`. That is deliberate — `x` is a bare key handled inside the tree's own keydown and is NOT in `DEFAULT_BINDINGS` (§ Design Decisions → The `x` toggle is documented at the palette registration), so it appears on no chord map and in no shortcuts overlay.
- **`Selection: Close N window(s)`** (id `selection-close`, `buildSelectionCloseAction`) — the destructive one, and the only palette entry in the app that **confirms in the palette itself**. It carries a `confirmLabel` (`Close N window(s) — Enter to confirm`), an optional field on `PaletteAction` that `CommandPalette` interprets as a **one-row confirmation sub-step**: selecting the entry does NOT run `onSelect` — it stashes the action in `confirming` state, replaces the whole result list with the single derived `${id}-confirm` row, resets query + index, and makes the search input `readOnly` with a `Confirm action…` placeholder (so typing cannot escape the step). Enter on that row runs the action once and closes; Escape (the shared `useFocusTrap` document handler), a backdrop click, and the ⌘K toggle all cancel through the same `closePalette` / `setConfirming(null)` seam, leaving the windows live and the selection intact. The mechanism is generic — any action can opt in by setting `confirmLabel`; the confirmed row itself carries `confirmLabel: undefined`, so a step can never recurse.
- **`Selection: Send prompt to N agent(s)`** (id `selection-send-prompt`, `buildSelectionSendPromptAction`) — hands a **snapshot** of the selected keys to `app.tsx`, which stores it as `selectionBroadcastKeys` and routes text entry to the docked compose strip in its selection-broadcast target mode (§ Docked Compose Strip → Selection-broadcast target). The action does not send: it opens the strip if the `composeStripEnabled` preference is off (`toggleComposeStrip()`, whose off→on focus-on-open flag then focuses the textarea) or, when it is already mounted, focuses it via `queueMicrotask(() => focusComposeStrip())` — one microtask later, so React has swapped the disabled focused-terminal target for the selection target first. Toggling the strip off cancels the target (an effect clearing `selectionBroadcastKeys`); per-window drafts in the module store are untouched.
- **`Selection: Move N window(s) to <session>`** (ids `selection-move-to-<session>`) — one entry per eligible existing session, with the live count and correct singular/plural. **The palette's fuzzy filter IS the session picker**: no dialog, no create-if-missing, no new backend surface. A target that would be a COMPLETE no-op (every selected window already lives in it) is excluded; a selection with unresolved keys excludes nothing, since the move is still meaningful.

**The move family's server is the SELECTION's, never the route's.** With sessions scope `all` the sidebar paints every server's groups, so a user can select rows on server A while routed to server B — and tmux `@N` ids are unique per server only, so feeding the route server's sessions would list the wrong targets AND move the wrong windows. `app.tsx` derives `selectionServer = singleSelectedServer(selectedWindows)` and passes THAT server's sessions (the merged `useMergedSessions` list when it is the route server — it carries the ghost/rename overlays the sidebar paints — else the raw `ctx.sessionsByServer.get(srv)`), omitting the entries entirely when that server's sessions are `undefined` (not loaded yet, which is not the same answer as "no eligible targets"). `buildSelectionMoveActions` additionally takes an explicit `sessionsServer` first parameter and returns `[]` on a mismatch, so the contract is enforceable at the builder seam rather than trusted at the call site. `singleSelectedServer` returning `null` for an empty or cross-server selection omits the whole family (§ Design Decisions → The move builder owns the server-match guard).

The entries are folded into `paletteActions` in AppShell only. The **board route registers board-scoped actions only** (§ Design Decisions → One palette mount in AppLayout, routes register via the slot) and does not include them, while `BoardPage` mounts the sidebar with `currentServer={null}`. So a board-route user can still build a selection, but the indicator **suppresses its `→ Selection:` hint** there — `SelectionIndicator` takes `hasSelectionActions={currentServer !== null}`, keyed on the same non-null-server condition the `Selection:` family itself is scoped to — rather than advertising a palette route to entries that route cannot reach.

**Cross-session move actions**: Dynamically generated "Window: Move to {sessionName}" actions (id `move-window-to-session-{sessionName}`) — one per session other than the current one. Only shown when `currentWindow` exists AND there are at least 2 sessions. On select, calls `moveWindowToSession(server, currentWindow.windowId, targetSession)` then navigates to `/$server` (server dashboard) because tmux auto-assigns the window index in the destination session and no `/$server/$session` route exists. Flat action list (not a sub-picker) — works well for typical session counts (2-5) and requires zero changes to the command palette component.

### Tmux Commands Dialog

`app/frontend/src/components/tmux-commands-dialog.tsx` — opened via command palette "Copy: tmux Commands" action (id `copy-tmux-attach`). Only available on terminal pages when `currentWindow` exists. Opens a `Dialog` with title "tmux commands" showing three copyable tmux command rows:

| Label | Command |
|-------|---------|
| Attach | `tmux [-L {server}] attach-session -t {session}:{window}` |
| New window | `tmux [-L {server}] new-window -t {session}` |
| Detach | `tmux [-L {server}] detach-client -t {session}` |

**Server-aware command generation**: Commands include the `-L {server}` flag only when the server is not `"default"`. When the server is `"default"`, the flag is omitted. This matches the `tmuxExecServer` convention in the backend (see `tmux-sessions.md`).

Each row has a label (`text-text-secondary text-[11px]`), a monospace code block (`bg-bg-inset border border-border rounded px-2 py-1.5 font-mono text-[11px] select-all`), and a copy button. Clicking the copy button writes the command to the clipboard via `navigator.clipboard.writeText` and swaps the copy icon to a checkmark for 1.5 seconds before reverting. Clipboard failure is silently caught.

Dialog state is a `showTmuxCommands` boolean in `app.tsx` (a route-local dialog state, unlike the layout-mounted server dialogs). Props: `server`, `session`, `window`, `onClose`.

## Visual Design

Three theme modes: **system** (follows OS), **light**, **dark**. Default: system. Linear/Raycast aesthetic.

Theme is applied via `data-theme` attribute on `<html>` (`"dark"` or `"light"`). CSS custom properties in `globals.css` switch values per `html[data-theme="dark"]` and `html[data-theme="light"]` selectors. The `@theme` block registers token names for Tailwind CSS 4 with dark palette as initial values.

### Border-Width System

A three-tier border-width vocabulary, all in `--color-border`: (`260702-6m46`)

- **1px hairlines** (`border` / `border-t` / `border-b`) — the default: card borders, tile borders, in-panel dividers, dropdown separators, the `SectionHeading` rule.
- **3px structural seams** (`border-t-[3px]` / `border-b-[3px]` / `w-[3px]` / `border-[3px]`) — the major chrome boundaries that partition the app frame: the topbar's bottom edge (`top-bar.tsx`), both bottombars (`app.tsx` + `board-page.tsx`), the sidebar's inter-panel separators + Sessions-section top (`collapsible-panel.tsx` + `sidebar/index.tsx`), the sidebar resize handle bar (`app.tsx`; see § Sidebar → Desktop), and the **waiting board-pane seam** (`border-[3px]` + `rk-waiting-seam`; a pulsing yellow attention border, static under reduced-motion — see § Attention Surfacing). The intent: seams read as deliberate structure, hairlines as content detail. (`260706-y1ar`)
- **8px selection accent** (`borderLeft: 8px solid`) — the contrast-guardrailed colored-row accent border (`computeRowBorders`; see § Contrast-guardrailed borders). The thickest tier — a selection/identity signal, not a structural seam. The **sidebar window row does not use it** (selection = 40% tint depth, active-board pin = accent glyph; § Row Anatomy); it is used on **server tiles** (stripe/edge).

### Color Tokens

| Token | Dark | Light | Usage |
|-------|------|-------|-------|
| `--color-bg-primary` | `#0f1117` | `#f8f9fb` | Page background |
| `--color-bg-card` | `#171b24` | `#ffffff` | Card backgrounds |
| `--color-bg-inset` | `#0a0c12` | `#e8eaef` | Fixed-width outer background |
| `--color-text-primary` | `#e8eaf0` | `#1a1d24` | Primary text |
| `--color-text-secondary` | `#7a8394` | `#6b7280` | Secondary text, labels |
| `--color-border` | `#454d66` | `#d1d5db` | Borders, dividers |
| `--color-accent` | `#5b8af0` | `#4a7ae8` | Active states, focus rings |
| `--color-accent-green` | `#22c55e` | `#16a34a` | Activity indicators |
| `--font-mono` | JetBrains Mono, etc. | (same) | Everywhere |

### Theme Switching

Preference persisted to backend API (`PUT /api/settings/theme` → `~/.rk/settings.yaml`) with localStorage key `runkit-theme` as synchronous cache (values: any theme ID or `"system"`). On init, ThemeProvider calls `getThemePreference()` from API; falls back to localStorage / `"system"` if API fails. `setTheme` writes localStorage immediately and calls `setThemePreference(id)` fire-and-forget. Three switching surfaces: (1) command palette (`Cmd+K` → "Theme: System/Light/Dark", current indicated with "(current)" suffix), (2) the settings dialog's theme-pair row (§ Settings Dialog), and (3) the Theme Selector modal (opened by the chevron menu's "Theme…" App-section row dispatching `theme-selector:open`, or command palette "Theme: Select Theme" — the selector modal mounts in AppShell, so the event has a listener wherever the Shell renders).

### No-Flicker Initialization

A blocking inline `<script>` in `index.html` `<head>` reads `localStorage("runkit-theme")`, resolves system preference via `matchMedia`, and sets `data-theme` on `<html>` before first paint. Static fallback: `data-theme="dark"` on the `<html>` tag. The same script also seeds the `theme-color` meta from the echoed instance-accent hex (§ PWA Meta Tags & Theme Color / § Instance Accent).

### PWA Meta Tags & Theme Color

`app/frontend/index.html` includes PWA-related tags in `<head>`:
- `<meta name="theme-color" content="#0f1117" />` — initial value matching dark theme background
- `<meta name="apple-mobile-web-app-capable" content="yes" />` — enables standalone mode on iOS
- `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />` — content renders behind the status bar
- `<link rel="apple-touch-icon" href="/generated-icons/icon-192.png" />` — homescreen icon for iOS
- `<link rel="manifest" href="/manifest.json" />` — a static link to the manifest route (the Go backend serves `/manifest.json` dynamically, accent-tinting the icon `src`s per instance — see § Instance Accent → PWA install identity and architecture.md § PWA Identity Assets)
- `<link rel="icon" type="image/svg+xml" href="/generated-icons/favicon.svg" />` — the tab favicon, likewise served dynamically and accent-tinted per request

**Theme-color synchronization**: The `theme-color` meta tag value is kept in sync via a **single shared writer** (`src/instance-accent.ts`, § Instance Accent) that resolves the content to the instance-accent hex when an accent is resolved, else the active theme background:
1. **Initial load** — the blocking inline script in `index.html` sets `theme-color` alongside `data-theme` before first paint; it first tries the echoed instance-accent hex from `runkit-instance-color` (validated `#rrggbb`), falling back to the per-mode default (`#0f1117` dark / `#f8f9fb` light) so an installed PWA window opens already tinted with no flash
2. **Runtime switch** — `applyThemeToDOM` in `ThemeProvider` calls `applyThemeColorMeta(theme.palette.background)` (NOT a direct `setAttribute`), and the `InstanceAccentProvider` calls `setAccentThemeColor(hex)`; both funnel through the one writer's module state so a theme switch cannot clobber the accent tint (§ Instance Accent → PWA titlebar bridge)

Theme color is per-theme (derived from `palette.background`) when no accent is set, else the accent's `titlebarHex` (the ~35% accent-into-background titlebar blend, § Instance Accent → Theme-derived hexes) — not a fixed dark/light pair.

**Icon set**: Canonical mark at `app/frontend/public/icon.svg` (grayscale hexagonal cube, transparent — its five gray fills are what the instance-accent tint replaces). Generated variants in `app/frontend/public/generated-icons/`:
- `favicon.svg` — copy of `icon.svg` (transparent, used as browser favicon)
- `icon-192.png` — 192x192, solid `#0f1117` background, ~20% padding (homescreen icon)
- `icon-512.png` — 512x512, solid `#0f1117` background, ~20% padding (splash screen)
- `icon-512-maskable.png` — 512x512, solid `#0f1117` background, ~40% padding (maskable, safe zone for adaptive icon shapes)

Generated by `scripts/generate-icons.sh` (Node + sharp). Run via `just icons`.

**Standalone display mode**: When installed via "Add to Home Screen" (Android or iOS), the app runs without browser chrome (no address bar, no toolbar). The `display: "standalone"` manifest property and `apple-mobile-web-app-capable` meta tag enable this on their respective platforms.

### ThemeProvider Context

`app/frontend/src/contexts/theme-context.tsx` — split context (ThemeStateContext + ThemeActionsContext) following ChromeContext pattern. Provides `useTheme()` (preference + resolved + theme object) and `useThemeActions()` (setTheme, previewTheme, cancelPreview). Listens to `matchMedia("(prefers-color-scheme: dark)")` change events when preference is "system" for real-time OS theme tracking. On init: calls `getThemePreference()` from API, falls back to localStorage / `"system"` if API fails. `setTheme` writes localStorage immediately and calls `setThemePreference(id)` fire-and-forget. `applyThemeToDOM` computes CSS values via `deriveUIColors(theme.palette, theme.category)` and writes the `theme-color` meta tag through the shared `applyThemeColorMeta(theme.palette.background)` writer (§ Instance Accent → PWA titlebar bridge — the instance accent wins over the bare background when resolved).

Provider order: `ThemeProvider > ToastProvider > InstanceAccentProvider > ChromeProvider > SessionProvider > AppShell` (`InstanceAccentProvider` inside the theme so it can read `useTheme()`; § Instance Accent).

### xterm Terminal Theme

`terminal-client.tsx` uses `useTheme()` to get the active `Theme` object. Initial theme set at Terminal construction via `deriveXtermTheme(activeTheme.palette)` — all 22 colors (background, foreground, cursor, cursorAccent, selectionBackground, selectionForeground, and 16 named ANSI colors). Live updates via `xtermRef.current.options.theme = deriveXtermTheme(theme.palette)` in a `useEffect` — no terminal recreation needed. There is no `XTERM_THEMES` constant.

### Terminal Relay (frontend) — `RelayMux` singleton + `TerminalClient` port

The browser opens no `/relay/{windowId}` WebSocket per pane. A **`RelayMux` singleton** (`src/lib/relay-mux.ts`, mirroring `state-socket.ts`) owns the ONE `/ws/terminals` socket per tab; each `TerminalClient` consumes a per-stream handle. This mirrors the state-socket transport — same URL-builder shape (`terminalsSocketURL()` vs `stateSocketURL()`), same backoff-reconnect + re-open-live-streams lifecycle, same singleton-per-tab ownership — but carries bulk binary terminal I/O on a SEPARATE socket (see `architecture.md` § Terminal Relay). (`260716-qf3j`, `260717-803u`)

- **`openStream({server, windowId, cols, rows}) → { send, resize, close, onData, onOpened, onClosed, setWindowId }`** — allocates a fresh client-side u32 stream id and issues an `open` control op (once the socket is open, else re-issued in `ws.onopen`). `send` emits a binary `[u32 BE streamId][payload]` data frame (strings UTF-8-encoded); `resize`/`close` emit control ops; `onData` fires on inbound binary frames for that id; `onOpened` on the `opened` control event; `onClosed` on `closed` (carrying 4004/4001/1000). `onOpened` is the re-open signal seams 2/4 (below) need.
- **Backoff reconnect + re-open of live streams** — on a SOCKET-level drop the mux reconnects with exponential backoff (`RECONNECT_BASE_MS = 1000` → `RECONNECT_CAP_MS = 30000`) and, in `ws.onopen`, **re-issues `open` for every still-live stream** — the server re-attaches (fresh PTY) and each pane's deferred reset (below) repaints on the incoming first data frame, so a reconnect is flicker-free with NO per-pane reconnect loop. An idle tab with zero live streams lets the socket stay closed. Each live stream **REUSES its SAME stream id on the new connection** (ids are unique per-connection, so reusing them across a reconnect is valid); a closed stream's id is retired.
- **`setWindowId(windowId)` — the fresh-target seam** — a same-session windowId ride updates the stream's `opts.windowId` WITHOUT a wire op (the live PTY already tracks the new active window). This keeps the stream's re-open target CURRENT: without it, a transient socket drop would re-`open` the STALE open-time window and `SelectWindowInSession` would yank the pane back. `cols`/`rows` stay fresh the same way via `resize()`, so `s.opts` is the single current source the mux re-issues `open` from.
- **A stream-level `closed` retires the stream client-side** (does NOT re-open — unlike a socket drop): `onClosed` fires and the stream is deleted from the mux map.

**`TerminalClient` port — the four delicate seams.** `terminal-client.tsx:813`'s `new WebSocket('/relay/...')` is the ONLY socket-creation site. The stream handle is surfaced through the `wsRef` seam via a **WebSocket-shaped adapter** (`{ readyState, send, close }`) so the shell consumers (`BottomBar`, the docked compose strip, touch-scroll, `fitAndSync`) that read `wsRef.current.{send,readyState,close}` are UNTOUCHED (a `{type:"resize"}` JSON `send` routes to the stream's `resize` op, everything else to a binary data frame; the `adapter as unknown as WebSocket` double-cast is the documented tradeoff — a structural `wsRef` type would remove it). The four seams:

1. **Confirmation-gated window-switch receipt = the stream's first DATA frame** — `notifyFirstWrite()` fires at the top of the stream's `onData` handler (`handleInbound`), before the write/coalesce decision. The gate state machine is pure. See § Window-Switch Slide Transition. (`260715-38kg`)
2. **Connect-select alignment epoch anchors on `open`→`opened`** — the deferred reset is armed and the grid re-fit/re-synced in `stream.onOpened` (which fires on the initial open AND every transparent re-open). Because the server enqueues `opened` on the stream's own queue ahead of any data (`architecture.md` § Terminal Relay, M2), the epoch is soundly ordered in-band, closing the `onmessage`-races-the-select-POST hazard (memory `relay-connect-select-alignment`).
3. **IntersectionObserver pane suspension = stream `close`/`open`** (no socket churn) — see § Desktop Relay-Connection Suspension.
4. **Deferred per-stream reset** — the deferred reset (below) is per-stream, re-armed in `stream.onOpened`.

**Non-4004 `closed` self-heal.** RelayMux re-opens only on a SOCKET drop, not a stream-level `closed`, so the client owns per-stream recovery: a `closed` with code 4004 prints `[session not found]` and fires `onSessionNotFound` (redirect); a non-4004 close (4001 attach-failed, 1000 PTY-EOF/graceful) prints `[reconnecting…]` and **probes exactly ONE fresh re-open of the current window** (bounded by a `probedReopen` flag). A genuinely gone window then 4004s the probe → redirect; a transient one re-attaches. A second non-4004 close prints `[disconnected]` and stops (no spin on a hard-failing window). This is a per-pane self-heal without a per-pane reconnect loop.

**Tab budget.** The tab holds a fixed **2 WebSockets** (state + terminals) and zero SSE — the e2e connection guard (`connection-budget.spec.ts`) asserts exactly this. See `architecture.md` § Terminal Relay for the backend/protocol. (`260716-qf3j`)

### Terminal Write Batching (Adaptive Flush + Deferred Reset)

Inbound terminal data in `terminal-client.tsx` — a `RelayMux` stream's inbound binary frames — flows through an **adaptive flush** (PR #244, with WebGL-renderer confirmation + context-loss survival in PR #245): (`260327-cnav`, `260717-803u`)

- **Immediate path** — a chunk that is small (≤ `IMMEDIATE_WRITE_MAX_BYTES` = 64 **UTF-8 bytes**), arrives while **idle** (nothing buffered, no flush pending), and is the **first immediate write this frame** is written synchronously so a keystroke echo paints on the same tick (latency attribution showed the rAF tail dominated perceived input latency ~3:1 over the network hop; keystroke→echo went 40→10ms). The threshold is measured in UTF-8 bytes via `textByteLength` — `String.length` UTF-16 units would under-count multibyte input and weaken the flood guard; the helper only pays for a `TextEncoder` encode in the ambiguous middle band, keeping the tiny-ASCII hot path allocation-free. The one-immediate-write-per-frame guard is `wroteImmediatelyThisFrame`, set by `markImmediateWrite()` and cleared by a one-shot rAF — a program emitting one byte at a time still coalesces after its first byte of the frame.
- **Coalesced path** — everything else (large chunk, already buffering, or a second small chunk in the same frame) accumulates in buffers (string concatenation for text, `Uint8Array[]` for binary) and flushes once per `requestAnimationFrame` (`flushToTerminal`), batching floods (builds, log tailing, `cat largefile`) into one xterm render pass per frame.
- **Ordering guarantee** — terminal bytes are order-sensitive: the moment anything is buffered, every subsequent chunk buffers until the buffer drains; an immediate write only happens when the buffer is empty AND no flush is pending, so a synchronous write can never jump ahead of buffered bytes.
- **Close-time drain** — the stream's `onClosed` handler cancels pending rAFs and calls `flushToTerminal()` (try/catch for a disposed terminal) **before** the `cancelled` check, so a transient-drop reconnect never loses tail data; `cancelAnimationFrame()` also runs on effect cleanup. Under the mux, a socket-level drop is transparent — RelayMux re-opens and the pane repaints on the first data frame — so `onClosed` fires only for stream-level closes; the close-time drain runs before the non-4004 probe re-open (above).

**Deferred per-stream reset**: each (re)open arms an effect-scoped `pendingReset` flag — armed in the stream's `onOpened`; `consumePendingReset()` (check + clear + `terminal.reset()`) runs **exactly once per stream open, immediately before that open's FIRST chunk write** — in the same tick on the immediate path, at the top of the same rAF callback (same frame) on the coalesced path — so clear + repaint land in **one presented frame**, with no black-frame flicker (the old content persists until the new redraw paints). This holds on every re-open path (sidebar click, tmux status-bar click, palette, board pane churn, transient socket-drop re-open, non-4004 probe re-open). **Ordering dependency**: arming on `onOpened` is only sound because the server enqueues `opened` ahead of the first data frame (`architecture.md` § Terminal Relay, M2) — a data-before-`opened` frame would fire the reset AFTER the first repaint and wipe it. (`260610-qf25`, `260717-803u`)

**`onOpened` also wipes the carried-over flush state.** Alongside arming `pendingReset`, the `onOpened` handler clears `binaryBuffers` and cancels/nulls any pending `flushRafId` — the adaptive-flush state belongs to the connection that just died, and it must never replay into the fresh one. **Why it is load-bearing**: on a backgrounded tab rAF never fires, so inbound bytes pile up unflushed; RelayMux then re-opens on wake and the server re-attaches. Writing that stale backlog into the new stream repaints dead-connection content, and any terminal query sitting in it would be answered by xterm.js into the CURRENT tmux client — which already got its answer, so the extra reply is typed into the active pane as junk. The relay strips tmux's DA identification queries before they ever reach the browser (`tmux-sessions.md` § Terminal Relay), so this wipe is defense-in-depth against that specific symptom while independently closing the whole replay-dead-connection-bytes class. Discarding the backlog costs nothing — the deferred reset wipes the repaint anyway. Safe w.r.t. the handoff below — an empty flush neither consumes nor executes the freshly armed `pendingReset`. The per-frame immediate-write pacing state (`wroteImmediatelyThisFrame`/`frameResetRafId`) is deliberately left untouched: it holds no bytes and self-resets on the next rAF tick. (`260801-f715-relay-device-attributes-responder`)

**Handoff semantics**: an **empty flush neither consumes nor executes a pending reset** — `flushToTerminal` early-returns when both buffers are empty, so a zero-data stream's close-time drain is a no-op (resetting with nothing to repaint would recreate the flicker); the next open simply re-arms in `onOpened`, idempotently. The **effect cleanup additionally neutralizes the closure's pending write state** (`pendingReset = false; binaryBuffers = []`): a late-delivered `onClosed`/`onData` — which can arrive AFTER cleanup, and whose drain runs before the `cancelled` check so same-effect transient-drop tail drains keep working — can therefore neither reset the shared terminal nor write stale old-window content after a successor effect (e.g. a `windowId` change forcing a fresh stream) has taken over. Reset-ordering unit tests live in `terminal-client.test.tsx` (no receipt-time reset; exactly once per stream open; string + binary first chunks; re-armed on re-open; zero-data close; post-consumption tail drain; connection A's buffered-but-unflushed bytes never written after connection B's `onOpened` — asserting against the mocked `RelayMux`/stream handle).

**Relay connection identity is (server, owning session), NOT windowId**: the stream-open effect's deps are `[terminalReady, server, wsRef, connectionEpoch]` — `sessionName` and `windowId` are deliberately excluded (the `eslint-disable react-hooks/exhaustive-deps` comment explains why). A same-session windowId switch (sidebar click, palette, URL writeback after a tmux-status-bar click) **rides the existing stream**: tmux has already switched the active window in place (or will, via the REST `selectWindow` already fired by `navigateToWindow` and mount-time alignment in app.tsx) and the directly-attached PTY redraws by itself — no stream close/open, no `terminal.reset()`, and **xterm scrollback is preserved** across same-session switches (native `tmux attach` behavior). A dedicated `windowId`-keyed effect calls **`streamRef.current.setWindowId(windowId)`** on every ride so the mux's re-open target stays current. Any GENUINE reconnect — session change, server change, transient socket drop — attaches to the latest window: the epoch-bumping paths re-open a fresh stream reading `windowIdRef.current`, and a socket drop's re-`open` reads the stream's `setWindowId`-kept-fresh `opts.windowId`. (`260602-qn62`, `260610-9umy`, `260717-803u`)

Identity is tracked by `connectedSessionRef` (session the live connection serves; `""` = resolved server-side only) + `connectedServerRef` + a `connectionEpoch` state bumped by a **session-identity watcher** effect on `[sessionName, server]`. The watcher is declared BEFORE the connect effect — ordering is load-bearing: on a same-commit server+session change it must read the PRE-change server from `connectedServerRef` (the connect effect overwrites it) to skip its bump, since the connect effect already re-runs via its own `server` dep — bumping too would tear down and reconnect twice. Watcher direction semantics:

- **`""` → resolved: absorption only** (record, never bump). On a cold deep-link the SSE-derived `sessionName` prop is `""` until the first snapshot; the client connects immediately by windowId and the relay resolves the owning session server-side (`ResolveWindowSession`) — by construction the live connection is already attached to that window's owning session, so the resolution transition costs no reconnect.
- **resolved → resolved: bump** — cross-session navigation, or a window moved to another session. A session RENAME also lands here and reconnects — accepted tradeoff: the SSE snapshot carries no stable session id to tell a rename from a genuine session change, renames are rare, and the deferred reset keeps any reconnect flicker-free.
- **resolved → `""`: LOSS-OF-IDENTITY signal** — record `""` AND bump for a probe reconnect. `sessionName` is derived by locating the URL's `@N` in the SSE snapshot, so it goes `""` and STAYS `""` when the viewed window left the snapshot: killed externally, pinned to a filtered `_rk-pin-*` session (pin transitions present as resolved → `""`, never resolved → resolved), or a dead deep link. The probe (by `windowIdRef.current`) either re-resolves the owning session server-side (an X → `""` → X ghost gap during navigation costs one flicker-free reconnect) or the relay closes 4004 → the `onSessionNotFound` redirect fires. This is the ONLY reachable recovery path in that state — `computeKillRedirect` and the URL writeback are both gated on a non-empty `sessionName` — so the bump is load-bearing (review finding M1, fixed in rework cycle 1: the original "treat `""` as inert in both directions" wedged the UI on kill-while-viewing).
- **`""` → `""`: no-op** (cold mount, still unresolved).

Deferred-reset and adaptive-flush semantics apply on every stream open that IS established (cross-session switch, server change, transient drop re-open, non-4004 probe re-open). Connection-lifecycle unit tests live in the "TerminalClient connection identity" block of `terminal-client.test.tsx` (same-session switch keeps the stream + pushes `setWindowId`; cross-session switch closes old + opens exactly one new with the deferred reset before its first write; `""` → resolved no-reconnect with later genuine change reconnecting; server change reconnects exactly once — no watcher double-bump; resolved → `""` probe; 4004 recovery after an external kill; the non-4004 feedback + one-probe path). The **transient-drop-latest-windowId** assertion is split across the seam: `terminal-client.test.tsx` pins the same-session ride pushing `setWindowId("@5")` into the live stream, and `relay-mux.test.ts` pins the socket-drop reconnect re-issuing `open` with the ridden-to windowId (`relay-mux.test.ts` also unit-tests framing, id-demuxed data in/out, and `opened`/`closed` dispatch against a stubbed WebSocket).

### Window-Switch Slide Transition

A same-server window switch animates as a **vertical slide** of the terminal surface via the browser's [View Transitions API](https://developer.mozilla.org/en-US/docs/Web/API/View_Transitions_API), keyed off the two windows' positions in the flattened sidebar order (macOS-Spaces-like, but vertical because the sidebar arranges windows vertically). It is **fire-and-forget** (a play-on-switch animation), NOT a gesture-scrubbed interactive drag. It is pure UX polish and layers ON TOP of the deferred-reset / relay-identity machinery above — a same-session switch still rides the existing socket with no reconnect; the slide just animates the in-place repaint. Progressive enhancement: any gate failure falls through to the instant switch. (`260703-l4nf`)

> **The slide is an EARNED signal.** The mechanics below (the one seam, the gate, direction, CSS, the first-paint gate) are the foundation; the slide plays ONLY on confirmed-fast arrival, with a pending spinner mask + a failure bounce-back layered on (§ Confirmation-gated motion below). `waitForFirstWrite()` resolves a `GateSettleReason` (`"first-write" | "timeout" | "superseded"`), and the wrapper `skipTransition()`s on every non-`"first-write"` settle rather than always playing the slide. (`260715-38kg`)

**The one seam — `navigateToWindow`.** The wrapper lives on `navigateToWindow` (`app.tsx`) alone — the single same-server switch seam shared by sidebar clicks, command-palette `Window: Switch to …` entries (keyboard path, constitution V), TopBar breadcrumb navigation, and SessionTiles clicks — so the animation applies identically to keyboard- and mouse-driven switches. The instant-switch body is factored into a `runSwitch()` closure (optimistic `navigate` + `selectWindow` POST + mobile sidebar close); the wrapper either calls it directly (fallback) or runs it inside `document.startViewTransition`'s async callback (see the timing caveat in § URL as Resumable Bookmark — on the animated path `runSwitch` runs ≥1 frame after the click). `runSwitch` **returns the `selectWindow` POST** so the gate can wait until tmux has been told to switch. **Direct `startViewTransition`, NOT TanStack Router's `viewTransition: true` option** — the polished first-paint gate (below) needs the async-callback seam to await a custom signal, which the router option does not expose.

**Gate (`shouldAnimateWindowSwitch`).** The switch animates only when ALL hold: View Transitions supported (`viewTransitionSupported()` — a plain-`boolean` `typeof document.startViewTransition === "function"` check, NOT a type predicate; the TS 5.7+ `dom` lib already declares the API, so no ambient augmentation and no `as` cast — code-quality type-narrowing), `prefers-reduced-motion: reduce` NOT set (checked via a **guarded** `matchMedia` — `typeof window.matchMedia === "function"`, for jsdom/older-WebView envs), an outgoing terminal window is in view (`windowParam`/`currentWindowId` non-empty — a first switch from SessionTiles with no outgoing surface skips), and the slide direction is non-null. Any failure → `runSwitch()` directly (instant switch, unchanged). Cross-server navigation takes the separate `navigate` branch in `handleSidebarSelectWindow`, never `navigateToWindow`, so it falls out with no transition and no code change; board routes and the Host page are untouched.

**Direction — flattened SSE sidebar order → root data attribute.** `windowSwitchDirection(order, currentId, targetId)` (pure, exported, in `src/lib/window-transition.ts`, colocated `.test.ts` — the same pure-helper pattern as `resolveServerView`/`computeKillRedirect`) returns `"up"` when the target sits BELOW current (new content enters from the bottom), `"down"` when ABOVE, and `null` when either id is missing from the order or the ids are equal (→ skip). `order` is `flatWindows.map((fw) => fw.window.windowId)` read from a render-synced `switchTransitionRef` (so the click handler reads current order without re-creating the callback). The resolved direction is written to `document.documentElement.dataset.windowSwitchDirection = "up" | "down"` immediately before `startViewTransition` (the `data-theme` root-attribute pattern); the CSS pseudo-elements key off `:root[data-window-switch-direction=…]`. **Accepted divergence:** `flatWindows` is raw SSE order, while the sidebar's drag-reorder override (`orderOverrideRef`) is component-local, so during a transient drag-reorder window the slide direction may briefly disagree with the visual sidebar order until SSE confirms — rare and self-healing.

**Direction-attribute latest-wins guard.** The attribute is cleared in `transition.finished.finally()`. But `finished` **FULFILLS on skip** (a rapid second switch supersedes this transition) — it does not reject — so a superseded transition's `finally()` would otherwise delete the attribute its successor already set, dropping the successor's direction CSS. Guard: each switch mints a monotonic token via `nextDirectionToken()` and clears the attribute only when `isLatestDirectionToken(token)` — only the LATEST switch's cleanup may clear it (mirrors the gate's still-points-at-itself pattern; pure module state, no DOM).

**CSS (`globals.css`) — pure transform + containment + full UA-animation neutralization.** `view-transition-name: terminal-surface` is on the terminal content container `<div>` (the shared wrapper around the `TerminalClient`/`IframeWindow` branch, merged with the fixed-width style) so **only the terminal region slides** — sidebar, top bar, bottom bar stay static. `::view-transition-old/new(terminal-surface)` animate via pure `translateY` keyframes, **180ms ease-out**, up/down variants keyed off the root attribute. Transform-only means **no layout change** — the container's `ResizeObserver`/`fitAndSync` path never fires, so tmux sees NO resize churn during the slide (hard constraint). Two non-obvious rules, both probe-verified:
- **Containment:** `::view-transition-image-pair(terminal-surface) { overflow: clip; }` — without it the translating snapshots escape the terminal group and paint over the top bar / bottom bar / sidebar (the UA does no clipping on the pseudo-tree by default).
- **Full UA-default neutralization for LIFETIME, not just visuals:** `::view-transition-old/new(root)` AND `::view-transition-old/new(terminal-surface)` AND `::view-transition-group(root), ::view-transition-group(terminal-surface)` ALL get `animation: none`. The group rule is the load-bearing one: `animation: none` on old/new alone does NOT shorten the transition — the UA's default **~250ms `::view-transition-group(*)` animations** hold the whole pseudo-tree (and thus the pointer-dead window and `transition.finished`) alive ~250–290ms past the 180ms slide. Neutralizing the group animations makes the 180ms slide govern the transition's whole lifetime.

**First-paint gate (`SwitchGate` in `window-transition.ts`) — the polished variant.** The new-state snapshot is gated on the incoming window's first inbound bytes so the surface slides in painted, not blank, with a **~300ms budget measured from callback entry**. Mechanics, in one module:
- `beginWindowSwitchGate()` opens a per-transition gate (a distinct token) and **FIRES any prior still-pending gate on supersession** — so rapid switches never serialize: the VT spec queues a second `startViewTransition` callback behind the first's returned promise, and the first callback holds that promise open while awaiting its gate, so firing the prior gate lets the queued second callback run at once. This call is load-bearing on BOTH the terminal AND iframe paths (a terminal→iframe switch must fire an in-flight terminal gate), so it runs before branching on iframe-vs-terminal.
- The callback does NOT `await` the `selectWindow` POST — it **chains `openForNotify()` off it** (`void posted.then(() => gate.openForNotify()).catch(() => {})`) and awaits only `gate.waitForFirstWrite()`, whose timeout clock starts at callback entry. This hard-caps the callback (and thus document-wide render suppression) at ~300ms **regardless of the POST's fate** — a stalled `selectWindow` (no client fetch timeout) can no longer freeze the document up to Chromium's ~4s VT deadline, and a rapid second switch never queues behind a stalled POST. `openForNotify` is the identity filter: only writes arriving AFTER the POST resolves count, so a busy OUTGOING window's still-streaming bytes (a same-session switch rides the existing socket) can't release the gate before tmux has run `select-window`.
- `settleGate` resolves the pending promise, clears the timer, and relinquishes the module slot **only when it still points at that gate** (still-points-at-itself) — a late timeout from a superseded gate can neither resolve nor null a newer gate.
- **Release is at message-RECEIPT time — `notifyFirstWrite()` at the TOP of the stream's `onData` handler (`handleInbound`) in `terminal-client.tsx`, before the write/coalesce decision — NOT at write time.** The receipt source is the stream's first DATA frame (inbound frames are always binary under the mux, so there is one `onData` handler). This is the load-bearing subtlety: `startViewTransition` suppresses rendering while its callback runs, and **rAF callbacks do not fire during suppression**. The coalesced flush (`flushToTerminal`) is reachable only via `scheduleFlush`'s `requestAnimationFrame`, and a tmux redraw's first chunk (~4KB) always exceeds `IMMEDIATE_WRITE_MAX_BYTES` (64) — so a write-time signal is structurally unreleasable on the common path, and every animated switch would degenerate into the full 300ms freeze. `onData` is a macrotask and DOES run during suppression; releasing there is safe for capture correctness because the pending rAF flush runs at the first rendering opportunity after the callback resolves — the same rendering update in which the new state is captured, so the received bytes are painted into the captured frame. **Distinguish this receipt-time NOTIFY (the transition first-paint gate) from the deferred per-stream RESET (`consumePendingReset`, above)** — different mechanisms at different seams: the reset fires once per stream open before its first chunk write, the notify fires at receipt time. (`260717-803u`)
- **iframe targets** (`switchTransitionRef.iframeIds`) have no terminal receipt seam, so they use the **ungated capture** (skip the wait entirely). The predicate is the **effective resolved layout** (surface-layout spec L2), not raw `rkType && rkUrl`: a target is an iframe target iff slot A of `resolveLayout(readStoredLayout(server, id) ?? translateLegacyParams(readStoredView(…), readStoredPanel(…)), undefined, win)` is non-`tty` (the URL `?layout=` param is unknown for a not-yet-navigated target, so it resolves from localStorage — the `rk-layout:` key AND its legacy predecessors via the shim — plus the window's default hint only). This keeps a non-tty-led target on the ungated path AND — critically — keeps an **iframe-typed window whose stored layout is tty-led** (or one with no `rkUrl`) on the **gated terminal path**, exactly where it renders a `TerminalClient`. Getting this wrong reintroduces the blank-pane/stuck-transition class of bugs this section documents — hence its own R12 task + e2e switch coverage. Because both the render branch and this classification call the same `resolveLayout`, the two can't drift. (`260714-t97o`)

**Accepted trade-offs.** (1) During the ~180ms slide the VT snapshot pseudo-element hit-tests to `<html>`, so a click landing on the terminal region mid-transition is lost — a brief **whole-document pointer-input dead window**. Keyboard input is unaffected (it targets the focused element, not a hit-tested point), so keyboard-first flows (constitution V) never lose input. (2) The direction-vs-sidebar-drag divergence noted above.

**Testing.** `playwright.config.ts` sets reduced-motion globally so window-switch e2e specs run against instant switches (the product's own fallback). **Trap worth recording:** `reducedMotion` is NOT a top-level `use` fixture in Playwright 1.59.1 — it is **silently ignored** there and only reaches the browser context via `contextOptions`, so the config uses `use: { contextOptions: { reducedMotion: "reduce" } }` (the top-level form is a no-op AND tsc-unchecked, since the config sits outside the tsconfig include). One dedicated animated-path spec `app/frontend/tests/e2e/window-switch-transition.spec.ts` (+ companion `.spec.md` per constitution Test Companion Docs) opts back in via `test.use({ contextOptions: { reducedMotion: "no-preference" } })`, performs a same-session switch, and asserts the incoming window's content paints AND `data-window-switch-direction` clears within ~1s — guarding against a regression that re-introduces a systematic gate-timeout freeze. Colocated Vitest unit tests cover `windowSwitchDirection` (up/down/missing/equal cases), `shouldAnimateWindowSwitch` (each gate branch), `viewTransitionSupported`, the full gate API (open-for-notify filtering, timeout, supersession-fires-pending, stale-timer isolation, stalled-POST bounded wait), and the direction-token latest-wins guard.

#### Confirmation-gated motion — the slide is an EARNED signal

Each visual state is an **honest, distinct signal** — the slide plays only on confirmed-fast arrival, so it never animates unconfirmed content (which would show the OLD window's bytes on a slow/failed switch), and a silent `selectWindow` failure can no longer park the URL/heading in an indefinite limbo with keystrokes routed to the OLD window: (`260715-38kg`)

| Signal | Meaning |
|--------|---------|
| slide | confirmed arrival (bytes within the 300ms budget) |
| spinner mask | in transit — don't type |
| spinner → content cut | arrived late |
| spinner → bounce + toast | switch failed |

Optimistic navigation is retained (heading/URL flip at click = acknowledged intent; sidebar highlight stays SSE-derived = confirmation); pessimistic navigation and a dimmed overlay are both rejected (§ Design Decisions → Window-switch feedback stays optimistic). Frontend-only — the `notifyFirstWrite()` call is the ONE arrival signal. The gate machine is pure; the receipt SOURCE is the RelayMux stream's first `onData` frame (see the first-paint-gate release bullet above). (`260715-38kg`, `260717-803u`)

**Gate settle reasons (`window-transition.ts`).** `waitForFirstWrite()` resolves a `GateSettleReason = "first-write" | "timeout" | "superseded"`. `settleGate(gate, reason)` carries the reason from its three callers: `notifyFirstWrite` → `"first-write"`, the gate timer → `"timeout"`, supersession by a newer `beginWindowSwitchGate`/`armGraceMask` → `"superseded"`. `FIRST_WRITE_TIMEOUT_MS = 300` is forced by VT render suppression — must not be extended. Three concurrency guarantees hold: supersession fires the prior pending gate immediately; only the INCOMING window's post-POST bytes release the gate; the still-points-at-itself guard means a stale timer never clobbers a newer gate or touches its mask. (`260715-38kg`)

**`skipTransition()` on EVERY non-`first-write` settle.** In `navigateToWindow`'s VT callback the wrapper awaits the reason and calls `transition.skipTransition()` whenever `reason !== "first-write"` (`app.tsx`). This covers `"timeout"` (cut to the masked new state — no motion into unconfirmed content) AND `"superseded"`, which has TWO cases: a rapid successor VT (the VT spec already skips this transition, so the explicit call is a harmless no-op) AND an **abandoned/bounced** switch — `abandonSwitchFeedback()` settles a still-pending gate `"superseded"` with NO successor VT, so WITHOUT the explicit skip the failed switch would animate alongside its own failure feedback (slide = confirmed arrival, so a failed switch must never slide). The `"first-write"` fast path is byte-identical to `l4nf`. Note SSE-confirm deliberately settles `"first-write"` (via `confirmSwitchArrived`, below): an authoritative arrival IS a confirmed arrival, so its slide is correct.

**The LogoSpinner pending mask — pure module state machine + a `useSyncExternalStore` seam.** The mask is expressed as pure, DOM-free, unit-testable module logic in `window-transition.ts` (NOT React `useState` in the wrapper — that would duplicate arm/lift/teardown and reintroduce gate↔mask drift): a `MaskState = "idle" | "masked"` with `getMaskState()` + `subscribeMaskState(listener)` (the `useSyncExternalStore` contract — snapshot stable while unchanged, listeners fire only on an actual transition). `app.tsx` reads it via `useSyncExternalStore(subscribeMaskState, getMaskState)` and, when `"masked"`, renders an absolutely-positioned full-cover `<div className="rk-window-switch-mask" role="status">` with a centered `<LogoSpinner size={48}>` over the `terminal-surface` container (which gained `position: relative`). It FULLY hides the stale bytes (a full mask, never a dimmed overlay) and sits at `z-index: 40` (below the `z-50` toast container).
- **Armed ONLY at the 300ms gate-timeout decision — NEVER at click time.** The mask arms as a side effect of `settleGate` when `reason === "timeout"` AND the gate was created `gated` (tty). Arming there, not in the wrapper after the await, makes the "never at click time" invariant structural (during the VT callback the screen is frozen on the old snapshot anyway, so early masking gains nothing). `beginWindowSwitchGate({ gated })` carries the tty flag; the wrapper passes `{ gated: !targetUngated }` (the `ungatedIds` classification, § Window Views → Window-switch transition classification — web/chat targets never mask and never reach a timeout settle).
- **POST-filtered lift via epoch + `liftAccepting`.** The SAME `notifyFirstWrite` receipt that releases the gate ALSO lifts the mask on late arrival ("one signal drives everything") — but the lift is FILTERED exactly like the gate release: a byte counts only once the switch's `selectWindow` POST has resolved. A module-level `liftAccepting` flag is `false` from switch start until the switch opens it (`openForNotify()` on the gate path chains off the POST; `openForLift()` on the grace path — see below), guarded by a monotonic `switchEpoch` minted per switch so a STALE switch's late POST resolution can never enable lifts for a NEWER switch's mask. Without the filter, a busy OUTGOING window's still-streaming bytes (a same-session switch rides the same socket) would un-mask stale content before tmux ever ran `select-window` — the exact hazard being fixed.
- **Input block while masked.** The overlay's `pointer-events: auto` swallows clicks on the old window; keyboard is blocked by a capture-phase `onKeyDownCapture` on the `terminal-surface` container that `preventDefault()`+`stopPropagation()`s keystrokes (dropped, NOT buffered/replayed — replay into the new window would be its own surprise). **Global chords are EXEMPT** via the pure `isMaskExemptKey(e)` predicate (constitution V — Cmd+K is the primary discovery mechanism, and the swallow could otherwise kill it for up to the 5s confirmation window): `Escape`; meta (Cmd) chords EXCEPT `Cmd+V` (xterm doesn't forward meta-modified keys to the pty, but the browser's default paste DOES land in xterm's focused textarea → the old pty, so paste stays swallowed); and the specific Ctrl-bound globals `Ctrl+K`/`.`/`\`/`` ` `` with `!altKey` required (AltGr reports `ctrlKey:true` and AltGr+char is typed INPUT, not a chord — same modifier discipline as `use-chat-view-shortcut.ts`). Plain typing and terminal control bytes (Ctrl+C) stay dropped.
- **Lift is a CUT (a fast fade-in only), never a second slide.** `.rk-window-switch-mask` has a 120ms `rk-window-switch-mask-in` fade-IN; the lift is a plain unmount. Under `prefers-reduced-motion: reduce` the mask STILL shows (input safety is never motion-only) but the fade is dropped (`animation: none`). **Ordering matters (rework G4):** the base rule + keyframes sit BEFORE the reduced-motion block, so the equal-specificity `animation: none` override wins by later source order (the same discipline as `.rk-waiting-halo`).
- **`abandonSwitchFeedback()` teardown — settles the gate, not just the mask.** The failure/bounce and route-leave/unmount paths call `abandonSwitchFeedback()` (NOT a bare `tearDownMask()`): it settles a still-pending gate `"superseded"` AND tears the mask down. A bare unmask would leave the pending gate armed — a `selectWindow` POST that rejects INSIDE the 300ms budget bounces first, then the gate's timer fires `settleGate(gate, "timeout")` and re-arms a mask over the bounced-back window with NO lift path (`liftAccepting` stays false after a rejected POST), a permanently stuck input-blocking mask (rework G2). `confirmSwitchArrived()` is the sibling authority-confirmed teardown: it settles a still-pending gate `"first-write"` (so its slide is correct) and tears down any mask — closing the real gap where a same-session tmux redraw completes BEFORE `openForNotify` (those bytes filtered as outgoing), leaving no later write to lift and letting the gate arm a mask over a switch that DID land. The bare `tearDownMask()` primitive has ONE sanctioned direct production caller — `beginPendingSwitch`'s fresh-switch teardown (§ below) — which clears a leftover mask WITHOUT settling a gate; the abandonment (`abandonSwitchFeedback`) and arrival (`confirmSwitchArrived`) wrappers cover every other intent and both delegate here.

**Failure bounce-back — un-sticking the silent-failure limbo.** `CONFIRMATION_WINDOW_MS = 5000` (named tunable) arms a SINGLE `setTimeout` per pending switch (NOT a poll — SSE stays the confirmation source). The bounce fires on an explicit `selectWindow` POST rejection OR the confirmation-window timeout — **NEVER** merely because SSE still reports the old window mid-switch. `bouncePendingSwitch(target)` clears `pendingClickRef`, calls `abandonSwitchFeedback()`, explicitly `navigate`s (replace) to tmux's actual active window (a stale `activeWindow` won't retrigger the writeback effect), and `addToast("Window switch didn't confirm — back to the active window", "error")`.
- **Server-scoped identity at all FOUR seams (rework H1, cycle 3).** `pendingClickRef` is a `PendingSwitchTarget = { server, windowId }` compared via the pure `isSamePendingTarget(pending, server, windowId)` at: (1) the mount-alignment skip, (2) the writeback's `urlMatchesPending`, (3) the writeback's `sseConfirmed`, and (4) `bouncePendingSwitch`'s guard. tmux `@N` ids are only unique per server and `AppShell` persists across `$server` changes, so a window-id-alone match false-positives on a cross-server collision (serverA `@5` vs serverB `@5`) — see § Click-intent and URL-match above. `bouncePendingSwitch` additionally clears SILENTLY (no toast, no navigation) when the recorded server no longer matches the live route (`serverRef.current !== target.server`), and its navigate uses `target.server` only after verifying it equals the live route server — a stale-server navigation is structurally inexpressible.
- **Three teardown effects, each a distinct route topology.** (a) **Server-change** (`$server` param change) — declared BEFORE the alignment effect (effects run in declaration order within a commit) so the skip sees a cleared intent and serverB's alignment fires; prev-ref keyed so it no-ops on mount. (b) **Route-leave** (`!windowParam` — windowless `/$server` SessionTiles, where the writeback can early-return) so a straggler timer can't navigate the user back with a spurious toast. (c) **Unmount** (Host `/` or a board route unmounts AppShell, but the setTimeout closure + module mask state outlive the instance). All three call `abandonSwitchFeedback()`. These are NOT subsumed by one another.
- **`beginPendingSwitch(target, { posted, graceMask })`** is the one entry point that records the intent, supersedes prior tracking, **proactively tears down a leftover mask/grace timer** (a fresh switch owns ALL feedback), arms the confirmation timer, and (instant path only) arms the grace mask + wires `openForLift` off the POST. The proactive teardown is a **bare `tearDownMask()`** at the top of the callback (right after `clearPendingSwitchTracking()`): `clearPendingSwitchTracking` cancels only the tracked entry's own timer/handle, NOT the module mask, so without it a prior TIMED-OUT switch's armed mask survives into the new route until SSE confirmation. It is deliberately the BARE primitive, NOT `abandonSwitchFeedback` — on the animated path `beginPendingSwitch` runs (via `beginWindowSwitchGate → startViewTransition → runSwitch`) while the switch's OWN just-opened gate is current, and settling it would skip the earned slide (`260715-38kg` R8); it is idempotent on the gated paths (which already tore down via `armGraceMask`/`beginWindowSwitchGate`). This closes the gateless-path residual: the two gateless call sites — the deep-link mount-alignment effect (no grace mask — a cold deep-link mounts a fresh connecting pane, not stale bytes) and `navigateToWaitingTarget`'s same-server branch (no grace mask, `?view=chat`-preserving) — otherwise cleared nothing, carrying a stale input-blocking mask into a window the user just navigated to. Its explicit-rejection handler is keyed on the **tracked object identity** (`pendingSwitchRef.current === tracked`, the still-points-at-itself pattern), NOT the windowId — so a superseded POST's late rejection can't bounce a newer healthy switch (rework SF8). It is also called from the click path (`runSwitch`). The mount-alignment effect SKIPS re-tracking when `isSamePendingTarget(pendingClickRef.current, server, windowParam)` — a click already recorded the intent, and re-tracking would fire a DUPLICATE `selectWindow` POST and (via `beginPendingSwitch`'s supersession) cancel the click's just-armed grace mask, making the instant-path mask dead in the live app (rework G1, the cycle-2 must-fix that made the instant path actually work).
- **Accepted non-goal — a prior switch's still-PENDING gate re-masking after a gateless switch begins.** The bare teardown clears an already-*armed* leftover mask, not a still-pending prior gate: an animated switch to A followed within ~300ms by a gateless nav to B lets A's gate timer fire and re-arm the mask over B. Discriminating that needs gate supersession `beginPendingSwitch` cannot safely perform (own-gate ambiguity — it runs with the animated path's own gate current), so it stays a rare self-healing corner, settled on the next SSE confirm or by the ungated instant path's own `abandonSwitchFeedback()`.

**Non-VT / reduced-motion grace-timer parity (`armGraceMask`).** Browsers without View Transitions support or with `prefers-reduced-motion: reduce` take the instant-switch path (no render-freeze phase, so no gate to time out). `armGraceMask(timeoutMs = 300)` returns a `GraceMaskHandle { openForLift(), cancel() }` and arms `maskState → "masked"` after the same 300ms threshold UNLESS an early first write cancels it or a late one lifts it — same threshold as the gate, different mechanism, same POST-filtered lift (`openForLift` mirrors the gate's `openForNotify`; epoch-guarded) and same `abandonSwitchFeedback` teardown. It supersedes any prior grace timer AND any still-pending gate (a rapid animated→instant sequence must not let a stale gate's timeout mask the newer switch) and clears any leftover mask — one feedback machinery at a time. On the instant path `runSwitch(graceMask)` receives `!ungatedIds.has(windowId)` (a gated tty target masks; web/chat stay mask-less, matching the gated path); the animated path passes `false` (its gate's timeout arms the mask instead).

**Tests.** `window-transition.test.ts` (colocated Vitest) covers the three settle reasons, the mask state machine (arm-at-timeout gated-only, lift-on-late-write via `notifyFirstWrite`, teardown on supersession / via `abandonSwitchFeedback` / via `confirmSwitchArrived`, gated-`false` does NOT arm), the `subscribeMaskState` seam (notify-on-change, unsubscribe stops), grace-timer parity (arm-after-threshold / cancel-on-early / lift-on-late), the post-POST lift filter, the SF8 stale-rejection identity guard, `isMaskExemptKey` (Cmd+V NOT exempt, AltGr NOT exempt), `isRedundantSwitch`, and `isSamePendingTarget` INCLUDING the cross-server `@N` collision — plus all preserved `l4nf` gate/direction-token assertions. The e2e spec keeps the fast-path animated-completion guard and adds a deterministic **mask-not-stuck** poll (`toHaveCount(0)` within budget); a strict "never-flashes-on-fast-path" assert and the >300ms/failure paths are documented in the `.spec.md` as unit-covered (a >300ms first write can't be deterministically forced on localhost).

### E2E host-global filesystem state (`~/.rk/settings.yaml` snapshot/restore)

`scripts/test-e2e.sh` isolates the **tmux server** (dedicated `-L rk-test-e2e` socket) and the **port**, but NOT `$HOME` — so any e2e that exercises a feature persisting to `~/.rk/` (settings, VAPID keys, push subscriptions) POSTs against the **developer's REAL host file**. `board-list-reorder.spec.ts` therefore snapshots the developer's `~/.rk/settings.yaml` and restores it around the suite so a curated board order (or any other setting) is never clobbered by test residue:

- **`beforeAll`** raw-byte-reads `settings.yaml` (`readFileSync(path, "utf8")`). Absent detection is **ENOENT-only**: a `code === "ENOENT"` read error means "no file to restore" (afterAll then deletes any residue); ANY other read error (EACCES/EIO — the file EXISTS but couldn't be snapshotted) is **rethrown**, so afterAll never `rmSync`-deletes real settings on a failed snapshot.
- **`afterAll`** (always runs, even on test failure) restores VERBATIM: write the original bytes back if the file existed, else `rmSync({ force: true })` to delete residue — a byte-identical round-trip. Teardown errors are swallowed so they never mask a test failure.

This is the general pattern for **any** host-global filesystem state an e2e touches: explicit save/restore in the spec, because the harness scopes tmux+port but not the filesystem. The companion `.spec.md` documents the save/restore in its Shared setup section (constitution Test Companion Docs).

## Component Conventions

- **All components are client-side** — pure React SPA, no Server Components. Data fetched via typed API client (`app/frontend/src/api/client.ts`) and SSE context
- **No loading spinners** — SSE keeps data fresh, the view renders with whatever data is available
- **Data fetching via context** — `SessionProvider` at layout level owns the `EventSource` connection and provides session data via `useSessions()` hook
- **SSE via `useSessions` hook** — thin wrapper over `SessionProvider` context. Single `EventSource` at layout level. SSE handler diffs incoming `e.data` JSON string against a `useRef<string>` before parsing — if identical, skips `setSessions()` entirely (eliminates ~90% of redundant re-renders). When data has changed, `setSessions()` is wrapped in `startTransition()` to keep user input responsive. Auto-reconnects via `EventSource` built-in. Server-side SSE uses a module-level goroutine hub that deduplicates polling across browser tabs
- **ChromeProvider context** (`app/frontend/src/contexts/chrome-context.tsx`) — split into state/dispatch contexts. Three hooks: `useChromeState()` (state only), `useChromeDispatch()` (dispatch only), `useChrome()` (convenience alias for both). Components that only read state (e.g., `AppShell`, `FixedWidthToggle`) use `useChromeState()` to avoid subscribing to dispatch identity changes. Manages current session:window selection, sidebar open/collapsed state, drawer state (mobile), `isConnected`, `fixedWidth`, and `terminalFontSize` (the effective global terminal font preference — see § Terminal Font Size). Chrome derives content from the selection — no slot injection
- **SessionProvider context** (`app/frontend/src/contexts/session-context.tsx`) — layout-level provider owning the single `EventSource`. Session data consumed via `useSessions()` hook. Connection status forwarded to ChromeProvider internally.
- **Shared `Dialog` component** (`app/frontend/src/components/dialog.tsx`) — reusable modal with title, backdrop, close-on-click, and an optional `size` width variant (§ Settings Dialog → Dialog width variant). Used for create, kill, **session**-rename, and settings dialogs. Window rename is the centered `WindowHeading`'s inline edit, not a dialog (§ Window Heading). (`260703-5ilm`, `260724-6j1v`)

## Create Session Dialog

The "Create session" dialog (breadcrumb `+ New Session` action, sidebar empty state button, or command palette) has three sections:

1. **Quick picks ("Recent:")** — Deduplicated project root paths from existing tmux sessions (window 0's `pane_current_path`). Tappable list items with 44px min height for mobile. Selecting fills path + auto-derives session name.

2. **Path input with autocomplete** — Text input that calls `GET /api/directories?prefix=...` with ~300ms debounce. Results appear as a dropdown below the input. Selecting a result fills the path and triggers a new autocomplete for children. Hidden directories (`.`-prefixed) are excluded from results.

3. **Session name** — Auto-derived from the last segment of the selected path (e.g., `~/code/sahil87/run-kit` yields `run_kit`). Editable — auto-derivation is a convenience, not a lock. The typed name field converts **live** as the user types via `toSafeSessionName` (§ Live Safe-Name Conversion) — a typed space appears as `_`. When the name field is left empty at submit time, the name is derived from the path automatically via `deriveNameFromPath()`. The Create button is enabled when either a name or a path is provided.

On submit, the dialog calls `createSession(server, name, cwd)` (the typed name run through `finalizeSafeName` first — trailing `_` trimmed, § Live Safe-Name Conversion) which sends `POST /api/sessions?server={server}` with `{ name, cwd }`. If the name field is empty but a path is set, the name is derived from the path's last segment via `deriveNameFromPath` (the session transform: hyphens→underscores, spaces and colons/periods and the forbidden set → underscores). Collision with existing session names is checked on the finalized/derived name and shows an error. The `cwd` field is omitted when no path is selected, preserving the original name-only behavior. Accessible from breadcrumb `+ New Session` dropdown action, sidebar empty state button, and command palette.

## Spawn-Agent Dialog

`app/frontend/src/components/spawn-agent-dialog.tsx` — a compact `Dialog` (create-session-dialog styling: `text-xs` field labels, disabled-submit styling) surfacing `rk riff` as a one-action web spawn. It is the frontend of the web-UI agent-spawn flow; the backend is `POST /api/riff` (see [architecture](/run-kit/architecture.md) § API Layer and the engine in [rk-riff](/run-kit/rk-riff.md)). **Title** carries the target session: `Spawn agent in {session}` (substring-compatible with the e2e's `getByRole("dialog", { name: "Spawn agent" })`, which does case-insensitive substring matching).

**Fields — in order Task → Preset → Where → Worktree → Agent**:

1. **Task** — free text, optional, autofocused; empty spawns a blank agent session.
2. **Preset** — a dropdown fetched on open (see preflight below), shown **only when the repo defines presets** (`presets.length > 0`); each option renders `name — layout (N panes)`.
3. **Where** — a `role="radiogroup"` with two options: **new worktree** (default, `where="worktree"`) and **this checkout** (`where="checkout"`, roots the window in the existing checkout — no worktree created). State is the typed `RiffWhere = "worktree" | "checkout"` union (no `as` casts).
4. **Worktree** — a text input, blank by default, placeholder `auto-named (e.g. swift-fox)`; **hidden when `where === "checkout"`** (it has no meaning there). Converts **live** via `toSafeWorktreeName` as the user types (§ Live Safe-Name Conversion) — spaces → `_`, `/` → `_`, a leading `-` dropped (matching `ValidateWorktreeName`), hyphens kept. Blank = wt auto-names; a typed name rides `worktreeName` through to `wt create --worktree-name`. No pre-filled suggestion — `wt` exposes no name-suggest seam and constitution §III forbids reimplementing its generator, so the placeholder is the honest fallback.
5. **Agent** — a tier dropdown populated from the preflight's `tiers` (built-ins ∪ the repo's `agent.tiers`, `default` first), defaulting to the `DEFAULT_TIER = "default"` constant. Displays tier **names only** (no model IDs — brittle to parse out of command strings). **FAB-GATED**: the field renders **only when `tiers.length > 0`**. The backend fab-gates the presets response (`tiers: []` for a non-fab repo — see [architecture](/run-kit/architecture.md) § API Layer `/api/riff/presets`), so an empty list HIDES the field entirely — no label, no hint text, no disabled control — because in a non-fab repo every tier resolves to the same `DefaultLauncher` (inert noise). When hidden, `tier` is omitted from the spawn body (see § Defaults). Non-fab-repo hiding does NOT enumerate system agents (claude/codex/gemini) — tiers remain fab's abstraction (constitution §III). (`gsmu`)

**Enter submits from any field** (`handleKeyDown` wired on all inputs incl. the radios and both selects). **Preflight fetch** (`getRiffPresets(server, session)`) is **best-effort**, with two branches: (`gsmu`)

- **Success** — sets `presets` and mirrors the response `tiers` **verbatim** (`setTiers(data.tiers)`). A fab project returns a populated list (field shown); a non-fab repo returns `[]` (field hidden). This verbatim mirror is what makes the frontend gate follow the backend gate.
- **Failure** (e.g. non-repo cwd, network error) — leaves the preset dropdown hidden and **keeps the Agent dropdown at the built-in `[DEFAULT_TIER]` fallback (field SHOWN)**. On a rejected fetch the repo's fab-ness is UNKNOWN, so the conservative status quo is to show the inert default rather than hide it — still allowing a task-only spawn.

A `cancelled` ref guards the async setState; a `mountedRef` guards the submit path against a stale setState after the dialog closes.

**Defaults omit unset fields**: `spawnRiff` omits each field when unset/default — `where` when `"worktree"`, `worktreeName` when blank (and always in checkout mode — the client drops it since the backend rejects the pairing), `tier` when `"default"` — so leaving the fields untouched sends a two-field body. The submit also drops `tier` entirely when the Agent Tier field is hidden (`tier: tiers.length > 0 ? tier : undefined`): a non-fab repo never sends an inert tier, matching the gate. The `tiers` array is a submit dependency of the `handleSubmit` `useCallback`. (`gsmu`)

**Submit → in-flight → outcome**: on submit it sets a `busy` flag (disables all fields + the button, guarding double-submit) and shows an **indeterminate** busy pipeline label — `LogoSpinner` + "Spawning: worktree → window → agent…" — with **no per-step progression**, because the synchronous endpoint emits no per-step events (intake assumption; streaming progress would need a new SSE/long-poll seam, out of proportion for v1). On success it closes and calls `onSpawned(windowId)` (wired to `app.tsx`'s `navigateToWindow`, inheriting the window-switch slide transition). **Falsy-windowId guard**: the backend `windowId` is best-effort (`""` when its `display-message` window-id resolve fails), so the dialog only navigates when `res.windowId` is truthy — otherwise it closes without navigating and lets the SSE stream surface the new sidebar row (navigating with an empty id would land on a junk `/$server/@` URL). A `400`/`500` renders its message in-dialog (`text-signal-red`; nothing was created on a `400`) and keeps the dialog open for correction.

**Entry points — THREE** (the first two are terminal-route only, the third is the sidebar). The two terminal-route entries open the dialog for the CURRENT window's session; the sidebar entry targets ANY listed session on ANY server (cross-server spawn): (`gsmu`)

1. **Cmd+K `Agent: Spawn`** — a `PaletteAction` in `app.tsx`'s `agentSpawnActions` block (folded into `paletteActions`), gated on `sessionName` (mirrors `Window: Create`). Passes the CURRENT `{server, sessionName}` (`onSelect: () => handleOpenSpawnAgent(server, sessionName)`; `server` added to the memo deps). Constitution V palette parity; the shortcut/registration is documented at the registration site per `code-review.md` ("New keyboard shortcuts must be documented in the command palette registration").
2. **`+ New Agent`** — the window-switcher dropdown's `secondaryAction` beside `+ New Window` (see § Breadcrumb Dropdowns). Threaded via an optional `onSpawnAgent?(session)` on the top-bar slot context (`top-bar-slot-context.tsx`) → `TopBar` prop → `BreadcrumbDropdown.secondaryAction`; absent (e.g. before `AppShell` registers, or off-terminal routes) → the dropdown renders no `+ New Agent`. The one-arg slot signature is kept via a thin `handleSlotSpawnAgent = (sess) => handleOpenSpawnAgent(server, sess)` binding the CURRENT server (this entry is not cross-server).
3. **Sidebar session-row bot button (`gsmu`)** — a per-row 🤖 button in the session-row trailing icon cluster (see § Sidebar → session-row icon cluster). It passes the ROW's explicit `{server, session}`, so it works from any route the sidebar is mounted on (Host / tmux Server / Terminal — NOT the board-route sidebar, which passes no handler) and targets any server → **cross-server spawn**. Wired `app.tsx` → `<Sidebar onSpawnAgent={handleOpenSpawnAgent} />`.

All three entry points call `AppShell`'s `handleOpenSpawnAgent(server, session)`, which sets the explicit target state `spawnAgentTarget: {server, session} | null`. The dialog renders iff `spawnAgentTarget != null` and is lazy-imported + `Suspense`-wrapped (like `CreateSessionDialog`), receiving `server={spawnAgentTarget.server}` and `session={spawnAgentTarget.session}` (an explicit target, not the current server from `useSessionContext`). `spawnAgentTarget != null` is folded into `dialogOpenRef` so the active-window effect treats it like every other open dialog. (`gsmu`)

**Cross-server nav on success** (`gsmu`): `onSpawned(windowId)` branches on whether the target IS the current route server. Same-server → reuse `navigateToWindow` (inherits the window-switch slide transition). Cross-server → `navigate({ to: "/$server/$window", params: { server: target, window: windowId } })` (+ close the mobile sidebar), mirroring `handleSidebarSelectWindow`. The falsy-`windowId` guard is preserved on both branches. The branch itself lives in the **shared `navigateToSpawnedWindow(srv, windowId)`** helper in `app.tsx`, which the row-flyout fork also calls (§ Row-hover register flyout card → Fork navigation on success) — one routing rule for every riff-shaped result, so the two call sites cannot drift.

## Settings Dialog

A VS Code-style settings **dialog** (not a routed page — constitution §IV keeps "no settings pages") gathers the instance's scattered preferences into one keyboard-first surface: `app/frontend/src/components/settings-dialog.tsx` on the shared `Dialog` shell (focus trap, `role="dialog"`, Escape closes) at its wide `size="lg"` variant. It is the single edit surface for the host-scoped settings (SSH host, instance display name) and for **notifications**, alongside second surfaces for controls that also live in-context (theme pair, instance accent color, terminal font size).

**Mounted once at `AppLayout`, not `AppShell`** — `AppShell` is server-scoped (assumes a non-null `currentServer` throughout `app.tsx`), while `AppLayout` is the true every-page layer (the persistent `RootTopBar` mounts there; `/board/$name` renders inside it without `AppShell`). So the dialog, its state, and its logic exist exactly once and it is available on every route including boards. `SettingsDialog` is lazy-imported and rendered inside a `SettingsDialogProvider` at the `AppLayout` level; the body (`SettingsDialogBody`) mounts only while open, so the per-open SSH-host fetch runs on mount with no reopen-staleness bookkeeping.

**`SettingsDialogContext` (`contexts/settings-dialog-context.tsx`)** — a deliberately small open/close context `{ isOpen, openSettings, closeSettings }` provided at `AppLayout` so any descendant (palette actions, sidebar gear) calls `openSettings()` while the dialog renders once. Instance data (display name, accent) lives in its own contexts, not here.

**Two labeled sections make the persistence scope visible** (a device-local value not syncing across devices reads as designed, not broken):

- **This host** ("stored on this instance, shared by every device") — persisted to `~/.rk/settings.yaml`: **instance display name**, **SSH host**, **instance accent color**, **theme pair**.
- **This device** ("stored in this browser only") — browser-local ergonomics: **terminal font size** and **notifications** (the Web Push subscription is per-browser, so the scope is semantically exact).

**Desktop preference-pane layout, one responsive code path** (`260724-6j1v`):

- **Wide dialog** — `size="lg"` on the shared `Dialog` (`max-w-2xl`, ≈672px) instead of the phone-card `max-w-sm` every other dialog keeps (§ Dialog width variant below).
- **`PreferenceRow`** — each setting is a `grid grid-cols-1 min-[480px]:grid-cols-[190px_1fr] gap-x-6 gap-y-1.5 py-2.5 items-start` row: label column left (the label plus an optional small `text-text-secondary` **sublabel** hint underneath — the former below-input hint moved here), control column right, so every control's left edge lands on ONE vertical rule. `htmlFor` renders the label as a real `<label>` bound to the row's input; rows whose control carries its own labeled elements (theme selects, steppers) pass none. Hairline separators come from `divide-y divide-border/40` on the wrapping section.
- **`ScopeHeading`** — each scope heading is a full-width underlined rule (`border-b border-border`): the uppercase scope name left, the storage hint right-aligned on the SAME baseline.
- **Input cap** — text inputs stop at `max-w-[320px]` so they don't stretch edge to edge.
- **Responsive collapse, no second dialog** — the `min-[480px]:` variant is the ONLY breakpoint: below 480px the same markup falls back to `grid-cols-1`, stacking label above control (the phone layout). One code path, no mobile fork.
- **Scroll path** — the `Dialog` panel carries `max-h-[calc(100vh-2rem)] overflow-y-auto` and the backdrop container `p-4` (both sizes), so a tall pane scrolls inside a short viewport instead of clipping content off-screen unreachably.

**Controls reuse existing models, never rebuilt** (the second-surface rule):

- **Instance display name** — a `TextSetting` (Enter/blur commits, Escape cancels the edit only; a second Escape closes the dialog — the window-rename vocabulary) reading `useInstanceName().instanceName`, committing via the context's optimistic `setInstanceName` (empty clears). Placeholder is the real hostname.
- **SSH host** — a single free-form `TextSetting` used verbatim (alias or `user@host`, never split into username/hostname fields — preserves the `open-in-app.ts` verbatim-alias contract). It reads the stored **setting** via `getSSHHost()` (NOT the effective health value, which may be an env fallback), commits via `setSSHHost` (empty clears), and surfaces a backend `400` as an inline `role="alert"` error without clobbering the stored value.
- **Instance accent color** (`AccentColorControl`) — reuses the HOST-panel `SwatchPopover` (color-only) + `useInstanceAccent().setColor` descriptor model ("4" / "1+3"; NOT a free RGB picker — the color model is descriptor-based end-to-end, see § Instance Accent). A pick POSTs and repaints the top-bar stripe without reload; the popover's Clear row clears.
- **Theme pair** (`ThemePairControl`) — a second surface reusing `useTheme()`/`useThemeActions()` (`/api/settings/theme` partial-merge POST): a System/Light/Dark mode control plus preferred dark-theme and light-theme `<select>`s. The top-bar `ThemeSelector` stays; the dialog is additive.
- **Terminal font size** (`TerminalFontControl`, under This device) — the shared `ChromeContext.terminalFontSize` control: a `[−] {size}px [+]` stepper + Reset wired to `increaseTerminalFont`/`decreaseTerminalFont`/`resetTerminalFont` (localStorage `runkit-terminal-font-size`, clamped by `TERMINAL_FONT_BOUNDS`). No new persistence (see § Terminal Font Size).
- **Notifications** (`NotificationsControl`, under This device, sublabel "Web Push to this browser") — the Web Push opt-in surface in chrome, backed by the same `usePushSubscription()` `{state, enable, sendTest}` the palette actions use, so the two surfaces cannot drift (§ Notifications (Web Push opt-in)). Contents: a status line (a 1.5px `role="status"` dot + `Subscribed on this device` / `Blocked in browser settings` / `Not subscribed`), an **Enable notifications** button whenever `!subscribed` (so it stays offered under `denied`, where re-allowing is a browser-settings action), a **Send test notification** button `disabled` until subscribed (Tip: "Send a local test notification" / "Enable notifications first"), the denied re-allow note, and a "Setup & troubleshooting guide ↗" link over the shared `NOTIFICATIONS_HELP_URL` in a new tab. **No Disable/unsubscribe action** — `lib/push.ts` has no unsubscribe path. When `state === "unsupported"` (insecure context / no service worker) the row **stays present** and renders a short "Not supported in this browser" note with no action buttons — a settings pane explains absence rather than vanishing (§ Design Decisions → The settings Notifications row explains an unsupported browser). (`260724-6j1v`)

### Dialog width variant (`size` on `components/dialog.tsx`)

`Dialog` takes an optional `size?: "sm" | "lg"` defaulting to `"sm"`: `sm` → `max-w-sm` (the phone-card width every other dialog uses — spawn, kill, create-session, board kill confirm, …), `lg` → `max-w-2xl` (≈672px), the settings dialog's desktop preference-pane width and its only consumer. Every other call site passes no `size` and keeps `max-w-sm` byte-identically. Both variants also carry the panel scroll path (`max-h-[calc(100vh-2rem)] overflow-y-auto` on the panel, `p-4` on the backdrop container — the `calc` offset matches that padding so the panel never touches the viewport edge), which is safe for `sm` dialogs and is what keeps a tall pane reachable in a short viewport. Asserted in `dialog.test.tsx` (default vs `lg` max-width, and the scroll classes on both). (`260724-6j1v`)

**Triggers — a registry chord, a palette action in the layout-level global group, and a top-bar gear chip** (all three reach the same `useSettingsDialog().openSettings`):

- **The `settings-open` chord** — ⌘, in the macOS desktop shell, ⇧⌘, in a mac browser, ⇧Ctrl+, on Windows/Linux (§ Keyboard Shortcuts → The default binding set). It is a registry builtin like every other app chord, so it is rebindable per device and its combo surfaces automatically in the shortcuts overlay, the palette hint, and the gear's tooltip chip. **Both route shells wire the handler** — `keybindingHandlers` in `app.tsx` and `boardKeyHandlers` in `board-page.tsx` (§ Dispatch seams). The unshifted ⌘, cannot be the *browser* default because it is the browser's own Preferences accelerator (claimed data, § Claimed keys). (`260801-mqim`)
- **Command palette** — a `Settings: Open` action among the layout-level global groups (`use-global-palette-actions.ts`, § Single palette mount + palette-actions slot), a one-liner calling `openSettings()`. Its id is the `settings-open` **registry actionId**, so palette entry and chord share one identity and `withShortcutHints` decorates the entry for free. The dialog itself mounts once in `AppLayout` (§ Design Decisions → Settings dialog mounts at `AppLayout`, not `AppShell`; → One palette mount in AppLayout, routes register via the slot).
- **Top-bar gear chip** — `SettingsGearButton` in `top-bar.tsx` (`GearIcon` from `sidebar/icons.tsx`), the L3-tail fit candidate in the right cluster on ALL four modes, sitting immediately before the exempt overflow chevron (the right-rail toggle stays outermost), consuming `useSettingsDialog()`; under width pressure it degrades to the chevron menu's App-section "Settings" row (`SettingsMenuRow`) (§ Right cluster → Settings gear). Per the tier-1 tooltip system (§ Tier-1 `Tip`), the gear is named via a `Tip label="Settings"` carrying the **host-effective `settings-open` chord** in its `kbd` slot, omitted when unbound/disabled — with an `aria-label="Open settings"` retained and never a native `title=`. The same Tip-not-title rule applies to icon-only controls inside the dialog (the accent-picker button, the font steppers). (`260812-d1at`)

**Instance display name has THREE display consumers, delivered via a root context** — see § Instance Display Name below.

## Instance Display Name

An optional per-instance display-name override lets a user label a run-kit instance ("dev mini") instead of showing its raw hostname. Stored host-side (`~/.rk/settings.yaml` `instance_name`, see architecture.md § `internal/settings` and § `/api/settings/instance-name`); empty means "derive from the hostname". Delivered frontend-side by **`InstanceNameProvider` / `useInstanceName()`** (`contexts/instance-name-context.tsx`), mounted once in `RootWrapper` beside `InstanceAccentProvider`.

The provider fetches `getHealth()` once on mount (StrictMode-guarded via a `didFetchRef`; `deduplicatedFetch` coalesces it with other same-tick `/api/health` consumers), owns the `{hostname, instanceName}` pair plus the optimistic `setInstanceName` write seam, and exposes `{ hostname, instanceName, displayName, setInstanceName }` where `displayName = instanceName ?? hostname`. `setInstanceName(name | null)` updates state optimistically then POSTs via `setInstanceName` client, toasting on write failure — so the settings-dialog edit repaints every consumer live and clearing the field reverts them to the hostname. This mirrors `InstanceAccentProvider` (fetch once, optimistic set, toast on failure).

**Three display surfaces prefer the display name** over the health-reported hostname: the browser tab title (`use-browser-title.ts` — `AppShell`'s local hostname fetch was replaced by `useInstanceName().displayName`), the HOST panel hostname line (`sidebar/host-panel.tsx` — `instanceName ?? metrics.hostname`), and the host-overview HOST HEALTH hostname line (`host-overview-page.tsx` — `instanceName ?? hostMetrics.hostname`).

**Two surfaces deliberately keep the REAL hostname**, NOT the display name, and are NOT consumers of this provider: the instance-accent hash fallback (`instance-accent.ts` — renaming the instance must not silently change its color) and the SSH deeplink derivation (`open-in-app.ts` `resolveDeeplinkHost` — deeplinks need the reachable hostname, not a vanity label).

## Session-to-Project Mapping

Every tmux session is a project — derived from tmux, no config file needed. Project root derived from window 0's `pane_current_path`.

## Activity Status

Windows are `"active"` (last tmux activity within 10 seconds) or `"idle"`. No "exited" state.

## Zustand Window Store

Window optimistic state is managed by a Zustand store at `app/frontend/src/store/window-store.ts`. This is the single source of truth for what windows are visible and what their display names are during the period between a user action and its SSE confirmation.

**Store location**: `app/frontend/src/store/window-store.ts`

**Store shape:**

```ts
// Flat entry type (not WindowInfo & {...} — stores only the fields needed for display)
type WindowEntry = {
  session: string;
  windowId: string;
  index: number;
  name: string;
  pendingName?: string;    // non-undefined = optimistic rename, pending SSE confirmation
  killed: boolean;         // true = optimistically hidden, pending SSE confirmation
};

type GhostWindow = {
  optimisticId: string;    // client-generated unique key for React rendering / rollback
  session: string;
  name: string;
  createdAt: number;
  snapshotWindowIds: Set<string>; // windowIds present in session at creation time
};

type WindowStore = {
  entries: ReadonlyMap<string, WindowEntry>;  // keyed by windowId (@N)
  ghosts: GhostWindow[];
  // actions (the only ways to mutate window state):
  setWindowsForSession(session, incoming): void;
  addGhostWindow(session, name, currentWindowIds?: Iterable<string>): string;  // returns optimisticId
  removeGhost(optimisticId): void;
  killWindow(session, windowId): void;
  restoreWindow(session, windowId): void;
  renameWindow(session, windowId, newName): void;
  clearRename(session, windowId): void;
  clearSession(session): void;
};
```

**Key identifier**: `windowId` is the tmux `@N` value (e.g., `"@3"`). It is globally unique per tmux server, assigned at window creation, and never renumbered. It is used as the store key — not the mutable numeric index.

**`MergedWindow` type**: defined in and exported from `app/frontend/src/store/window-store.ts`. Includes `windowId: string` as a required non-optional field.

**Action surface (minimal by design)**:

| Action | Effect |
|--------|--------|
| `setWindowsForSession(session, incoming)` | SSE reconciliation — merges by `windowId`, preserves `killed`/`pendingName`, removes absent windows, reconciles ghosts |
| `addGhostWindow(session, name, currentWindowIds?)` | Creates a ghost entry; returns `optimisticId` for rollback |
| `removeGhost(optimisticId)` | Removes a ghost by ID (API failure rollback) |
| `killWindow(session, windowId)` | Sets `killed: true` |
| `restoreWindow(session, windowId)` | Sets `killed: false` (API failure rollback or always-settled cleanup) |
| `renameWindow(session, windowId, newName)` | Sets `pendingName` |
| `clearRename(session, windowId)` | Clears `pendingName` (settled or rollback) |
| `swapWindowOrder(session, srcIndex, dstIndex)` | Swaps index values of two entries (optimistic reorder); no-op if either missing |
| `clearSession(session)` | Removes all windows and ghosts for the session |

**SSE sync**: `AppShell` (in `app.tsx`) calls `setWindowsForSession(s.name, s.windows)` for each session in a `useEffect` on `rawSessions`. This keeps the store in sync with the SSE ground truth.

**Ghost reconciliation**: When `setWindowsForSession` is called, it computes `newIds = incomingIds − priorKnownIds`. For each ghost (oldest first) whose `snapshotWindowIds` does not contain any element of `newIds`, the ghost is removed. This set-difference approach is more reliable than count-based reconciliation — it handles concurrent creates/deletes without false positives.

**useMergedSessions**: `useMergedSessions` in `optimistic-context.tsx` derives window data from the Zustand store rather than from raw `session.windows`. For each session: filters `killed: true` entries, applies `pendingName ?? name` for display, sorts by `index`, then appends ghosts.

**Consumers use the store via `useWindowStore()` hook**:
```ts
const { killWindow, restoreWindow, renameWindow, clearRename, swapWindowOrder } = useWindowStore();
```

**Session/server state** (ghost sessions, ghost servers, session kill/rename) remains in `OptimisticContext` — these use name-based keys and are not subject to index-collision bugs.

## Optimistic UI & Mutation Feedback

All mutating API calls use the `useOptimisticAction` hook (`app/frontend/src/hooks/use-optimistic-action.ts`) which provides `{ execute, isPending }`. The hook calls `onOptimistic` synchronously before the async API call, tracks `isPending`, and calls `onRollback`/`onError` on failure and `onSettled` on success. An unmount guard (`mountedRef`) prevents state-after-unmount warnings.

**Callback contract** — four optional result callbacks with distinct mount-safety guarantees:

| Callback | Called on | Mount guard | Use for |
|----------|-----------|-------------|---------|
| `onAlwaysSettled` | success | none — always fires | Root-level context cleanup (e.g., `unmarkKilled`) |
| `onAlwaysRollback` | failure | none — always fires | Root-level context cleanup (e.g., `unmarkKilled`) |
| `onSettled` | success | behind `mountedRef` | Local component state updates |
| `onRollback` | failure | behind `mountedRef` | Local component state updates |

`onAlwaysSettled`/`onAlwaysRollback` MUST be safe to call after the initiating component unmounts — i.e., they may only interact with root-level stores/contexts like `OptimisticContext` or the Zustand window store (both always available for the lifetime of the app). Using local component state or `setState` in these callbacks will cause state-after-unmount warnings. Use `onSettled`/`onRollback` for anything that touches local component state.

`onError` is also behind the `mountedRef` guard (safe to call `addToast` — `ToastProvider` is root-level, but error display is only meaningful when the user can see it).

**Three feedback patterns:**

1. **Ghost entries** (CRUD operations): Creating a session/window/server immediately inserts a ghost entry with `opacity-50 animate-pulse` styling. SSE reconciliation auto-clears ghosts when real data arrives. Failure removes the ghost and shows an error toast. Kill operations immediately hide the entry; failure restores it. Rename operations immediately update the displayed name; failure reverts. **Window** ghost/kill/rename state is managed by the Zustand window store (`app/frontend/src/store/window-store.ts`); **session and server** ghost/kill/rename state remains in `OptimisticProvider` context (`app/frontend/src/contexts/optimistic-context.tsx`). Both feed into `useMergedSessions(realSessions, currentServer)` which filters session-level overlays by `currentServer` (so cross-server ghosts/kills/renames don't leak — see "Server Capture Convention" below) and merges with SSE data.

2. **Button loading states** (fire-and-forget): Split pane and close pane top-bar buttons show a spinner SVG (`animate-spin`) and `disabled` attribute during `isPending`. Command palette equivalents use the same hook for error toast feedback (palette closes, so spinner not visible).

3. **Inline progress** (async data): File upload shows an "Uploading..." badge in the terminal area. Directory autocomplete shows a spinner in the path input trailing slot. Server list refresh shows a spinner on the dropdown trigger.

**Error toast system**: `ToastProvider` + `Toast` component (`app/frontend/src/components/toast.tsx`). Fixed bottom-right, auto-dismiss after 4 seconds, stacked vertically. Error variant has `var(--color-ansi-1)` (red) left accent border; info variant uses `var(--color-ansi-4)` (blue). Theme-aware via CSS custom properties. Despite the "error" name it is the general toast surface (the `info` variant carries success/neutral messages). `addToast(message, variant?, action?)` takes an optional THIRD positional `action?: { label, onSelect }` (`260718-gxrq`) rendered as a keyboard-focusable `<button>` inside the toast body — selecting it dismisses the toast then runs `onSelect`; the positional shape keeps every existing two-arg call site unchanged. Used for the post-pin "Pinned to <board>" + "View board" toast (§ Post-pin success feedback + toast optional action).

**Type guard**: `isGhostWindow(win)` exported from `optimistic-context.tsx` — narrows `WindowInfo | MergedWindow` to `MergedWindow & { optimistic: true }`. Used in the sidebar and the `SessionTiles` density view instead of `as` casts. `MergedWindow` type is defined in and exported from `app/frontend/src/store/window-store.ts`; it includes `windowId: string` as a required non-optional field.

### Window Kill: Zustand Store Handles Kill Cleanup

Window kill state is tracked in the Zustand window store by `windowId` (the immutable tmux `@N` identifier), not by mutable index. This eliminates the index-collision bug where killing window N would cause tmux's renumbering to suppress the next window at that index.

**Kill flow** (`useOptimisticAction` pattern):
- `onOptimistic`: calls `windowStore.killWindow(session, windowId)` — sets `killed: true` in the store
- `onAlwaysRollback` (API failure): calls `windowStore.restoreWindow(session, windowId)` — clears `killed`
- `onAlwaysSettled` (API success): calls `windowStore.restoreWindow(session, windowId)` — clears `killed` (SSE absence will remove the entry once tmux confirms)

When the next SSE update arrives without the `windowId`, `setWindowsForSession` removes the entry from the store entirely — regardless of whether `killed` is set. No explicit `confirmKill` action is needed.

**Three `useOptimisticAction` instances** use this pattern:

| Instance | File | Kill path |
|----------|------|-----------|
| `executeKillWindow` | `app/frontend/src/components/sidebar.tsx` | Ctrl+Click direct kill |
| `executeKillFromDialog` | `app/frontend/src/components/sidebar.tsx` | Confirmation dialog kill |
| `executeKillWindow` | `app/frontend/src/hooks/use-dialog-state.ts` | Command palette kill |

**Session kills are unaffected**: Session names are stable across kills (tmux never renumbers sessions). Session kill/restore remain in `OptimisticContext`.

### Cross-Session Move: Compound Optimistic Update

The `executeMoveToSession` hook in `sidebar/index.tsx` combines two store actions (`killWindow` + `addGhostWindow`) for a single optimistic update. This is the only `useOptimisticAction` instance that performs a compound optimistic mutation (hiding in one session while inserting a ghost in another). The ref-based `lastMoveToSessionRef` stores `{ srcSession, windowId, optimisticId }` so `onAlwaysRollback` can reverse both operations even after the component navigates away.

### Server Capture Convention (Optimistic Actions)

The `server` argument that scopes a mutation to a tmux server is **always captured at user-event time**, never read from an ambient module-level global, never frozen at component mount. This is enforced both by the API client signature (every server-scoped function takes `server: string` as its first arg — see `tmux-sessions.md` → "Frontend Server Routing Contract") and by the React handler shape on every call site.

#### The two compliant capture shapes

**Shape A — explicit capture inside `useCallback`**: read `server` from `useSessionContext()` at component scope, list it in the callback's deps array, and pass it as the first argument to the action when the user-event handler fires:

```tsx
const { server } = useSessionContext();
const handleRenameSession = useCallback(() => {
  if (!renameSessionName.trim() || !sessionName) return;
  executeRenameSession(server, sessionName, renameSessionName.trim());
  setShowRenameSessionDialog(false);
}, [renameSessionName, sessionName, server, executeRenameSession]);
```

**Shape B — `server` threaded through the `useOptimisticAction` argument tuple**: extend the tuple's first slot to `string` and forward it inside `action`. This is the standard shape for hooks like `executeRenameSession`, `executeKillFromDialog`, `executeMoveToSession`, etc.:

```tsx
const { execute: executeRenameSession } = useOptimisticAction<[string, string, string]>({
  action: (srv, oldName, newName) => renameSession(srv, oldName, newName),
  onOptimistic: (srv, oldName, newName) => {
    lastRenameSessionRef.current = { server: srv, name: oldName };
    markRenamed("session", srv, oldName, newName);
  },
  onRollback: () => {
    const last = lastRenameSessionRef.current;
    if (last) unmarkRenamed(last.server, last.name);
  },
  ...
});
```

**Refs that bridge async callbacks** (e.g., `lastKillSessionRef`, `lastRenameSessionRef`, `killDialogServerRef`) snapshot `{ server, name }` together inside `onOptimistic`, so `onAlwaysRollback`/`onAlwaysSettled` can target the originating server even if the user has switched servers by the time the API resolves. Snapshotting the name without the server is a bug — rollback would invalidate the wrong server's overlay.

#### Optimistic overlays carry `server` (session-level)

`OptimisticContext` (`app/frontend/src/contexts/optimistic-context.tsx`) stores session-level entries with their originating `server` and filters by `(server, name)` at render time. The discriminated-union types reflect this:

```ts
type GhostEntry =
  | { optimisticId: string; type: "session"; name: string; server: string }
  | { optimisticId: string; type: "server"; name: string };

type KilledEntry =
  | { type: "session"; identifier: string; server: string }
  | { type: "server"; identifier: string };

type RenamedEntry = { type: "session"; identifier: string; newName: string; server: string };
```

API surface (session-level entries take `server` first; server-level entries are global):

| Method | Signature | Notes |
|--------|-----------|-------|
| `addGhostSession` | `(server, name) => optimisticId` | Session ghost |
| `addGhostServer` | `(name) => optimisticId` | Server ghost — global, no `server` arg |
| `markKilled("session", server, name)` | overload | Session kill |
| `markKilled("server", name)` | overload | Server kill — global |
| `unmarkKilled("session", server, name)` | overload | Mirror of `markKilled` |
| `unmarkKilled("server", name)` | overload | Mirror of `markKilled` |
| `markRenamed("session", server, name, newName)` | required `server` | |
| `unmarkRenamed(server, name)` | required `server` | |
| `useMergedSessions(real, currentServer)` | filter | Drops session-level overlays whose `server !== currentServer` |

`useMergedSessions` filters ghosts/kills/renames by `currentServer` before applying them. SSE reconciliation only inspects ghosts whose `server === currentServer` so the other server's pending state is left intact when the user switches servers and back.

**Window-store entries are NOT keyed by server** — windows cannot migrate across tmux servers (`MoveWindowToSession` operates within a single server, and there is no cross-server move API). The `windowId` (tmux `@N`) is unique per server, and `setWindowsForSession` is only ever called with data for the active server. Adding `server` to the window-store key would be defensive bookkeeping with no failure mode to defend against.

#### Why this convention exists

The convention exists to prevent an ambient-state retargeting bug: holding `server` in a module-level closure (`_getServer`) wired to `serverRef.current` dereferences live state at fetch time, so any switch between user intent and fetch dispatch silently retargets the request — most commonly via Cmd+K's near-instant server switcher between opening a rename dialog and pressing Enter. The optimistic overlay makes such a bug invisible until SSE reconciles (~2–5 s later), manifesting as random renames/kills landing on the wrong server with a flicker on rollback.

#### General rule: don't introduce ambient state for request parameters

Any value that scopes an HTTP request to a particular backend resource (server, project, account, tenant) MUST be passed as an explicit argument to the API call, captured at user-event time. Module-level mutable getters, refs read at fetch time, or context reads inside the action callback (rather than the handler) all create the same closure-race shape that this change retired. If a value travels with a mutation, it travels in the call signature — period.

The regression test in `app/frontend/src/hooks/use-dialog-state.test.tsx` flips `SessionProvider`'s `server` prop between `openRenameSessionDialog("foo")` and `handleRenameSession()` and asserts the API call uses the post-flip server (`server-B`), proving the capture point is the handler invocation, not the dialog open.


## Design Decisions

### Server tree wrappers are presentational sections, not groups
**Decision**: The per-server `<section>` wrapper inside the sidebar `role="tree"` carries `role="presentation"` and no `aria-labelledby`; the element stays `<section>` (no swap to `<div>`), and the header toggle button keeps its own `aria-expanded` + `aria-label` naming.
**Why**: A `<section>` with an accessible name maps to the implicit `region` landmark role, which is not a valid intervening node between a tree and its treeitems; `role="presentation"` makes the wrapper semantically transparent, so the accessibility tree reads tree → treeitem(level 1) → group → treeitem(level 2). The `aria-labelledby` removal is spec-required, not optional: WAI-ARIA presentational-conflict-resolution voids the presentation role when the element carries a global ARIA property, so keeping it would silently leave the section exposed as a named `region` and make the fix a no-op.
**Rejected**: (a) `role="group"` per server — the server header row's non-treeitem buttons live inside the same wrapper, so a group would still interpose non-treeitem interactive content between the tree and its level-1 treeitems (backlog exsp also prescribes `presentation`); (b) swapping `<section>` for `<div>` — implicit semantics are neutralized either way, so the swap is pure churn; (c) keeping `aria-labelledby` alongside `role="presentation"` — spec-voided, change becomes a no-op.
*Introduced by*: 260811-exsp-sidebar-tree-aria-presentation

### Shell servers reach the palette by empty-list gating, not an `isShell()` branch
**Decision**: `useShellServers()` resolves `[]` outside the desktop shell (the bridge wrappers swallow every absent/malformed/denied case), so `buildShellServerActions` yields no entries and `app.tsx` registers the group unconditionally — no `isShell()` conditional at the registration site. The list is a mount-time snapshot with no refresh seam, quoted labels distinguish shell servers from tmux servers, and v1 is switch-only.
**Why**: One code path for both environments — an `isShell()` branch would gate on a *second* signal that can disagree with whether the `servers` group actually answers (an older shell has `version`/`platform` but no bridge group), so the list's own emptiness is the more truthful gate. A snapshot suffices because every list mutation the shell offers (switch, add) loads a different server URL, which remounts the SPA anyway; only removing a non-active server leaves a stale entry until reload.
**Rejected**: An explicit `isShell()` conditional around the registration (a second, less accurate signal); a live refresh subscription or poll (a new seam for a list that changes only via flows that already remount the SPA); unquoted labels matching the tmux entries (two different kinds of "server" reading identically in one palette); including Add/Remove Server (Constitution IV — the native menu + welcome flow already own those, and Remove needs a confirm dialog the palette has no home for).
*Introduced by*: 260730-9lez-shell-keyboard-tier-symmetry

### Live safe-name transform + commit finalizer split
**Decision**: The live `onChange` transform strips leading separators but keeps a trailing `_` visible; a separate `finalizeSafeName` trims leading+trailing `_` at commit. Name conversion happens live in the input (not silently at submit), per name kind, in one shared `src/lib/names.ts` module.
**Why**: Converting live gives WYSIWYG — the user sees exactly the name that will exist, so the optimistic-update name equals the committed name. Keeping the trailing `_` visible while typing is required so "My " + "p" does not collapse to "Myp" (trimming the trailing separator live would delete the separator the user just typed and break mid-word entry); commit-trim is the one minimal WYSIWYG deviation. One transform per kind in `src/lib/` (the shared-pure-logic home) keeps every entry point on one canonical rule with colocated Vitest coverage.
**Rejected**: (a) backend-side conversion — breaks WYSIWYG and optimistic-update parity (the backend stays reject-only, constitution §I); (b) submit-time silent conversion — the user commits a name they never saw; (c) keeping reject-only frontend errors (the old server-name UX) — explicitly the experience this moves away from; (d) strict-WYSIWYG commit (trailing `_` included) — leaves awkward trailing separators for no benefit.
*Introduced by*: 260722-ln4n-auto-safe-name-conversion

### Shade carried on PickerColor, not a parallel vocabulary
**Decision**: Extend `PickerColor` to `{ family, shade }` with `formatColorValue` emitting `"{family}-dark"`, rather than minting 10 new pseudo-families for the dark shade.
**Why**: Hue identity, legacy mapping, and theme adaptation stay properties of the 10 `HUE_FAMILIES`; the shade is a render-time L offset (mean-L − 0.14), so there is zero new palette data and every family-keyed code path (tints, borders, accent) derives dark variants for free.
**Rejected**: 20 `HueFamily` entries — duplicates hue/legacy data and breaks the 1:1 legacy map (dark variants have no legacy form). Also rejected a light shade variant, replaced by normal+dark so every existing stored color maps to the normal shade untouched.
*Introduced by*: 260723-wwoi-label-picker-markers-shades

### Marker preview color held as picker-local state
**Decision**: The Label picker's marker-preview color derives from `selectedColor`, but a swatch pick also updates picker-local preview state before emitting.
**Why**: The requirement is "picking a different swatch repaints the marker column immediately"; the window-row caller closes the popover on pick, so prop-driven repaint alone would make the requirement vacuously untestable — local state makes it true for any caller.
**Rejected**: Prop-only derivation — repaint would depend on the caller keeping the popover open and re-rendering.
*Introduced by*: 260723-wwoi-label-picker-markers-shades

### Marker preview cells and the hazard wedge are never animated; the dashed data rain is the lone rest-state motion
**Decision**: The `thick` hazard wedge is static in every state (rest, hover, selected), and the Label picker's marker preview cells never animate — no scanline crawl even when the double cell is selected, no data rain on the dashed preview. On real rows, exactly one treatment moves at rest: the dashed data rain (§ Dashed data rain), always-on. The dashed gutter STRIPE itself stays static in every state (a revival that scrolled the stripe was corrected — the intended motion was the row rain). The rain's gating flip-flopped deliberately: always-on → selection-only → always-on, settling on always-on once the thinned two-lane version proved quiet enough (the user's call after watching both).
**Why**: Motion belongs to real rows only, and rest-state motion is rationed to the one marker whose semantic ("working") is inherently live. The other animated variants tried during the design session (scrolling the dashed stripe, progress seam, barber-pole thick, animated hazard weave, phosphor bleed) stayed rejected as distracting or reading like a loading state. Double's scanline crawl + CRT band remains selection-gated.
**Rejected**: Scrolling the dashed stripe itself; animating the hazard weave; the progress seam; a phosphor bleed paired with solid; animating preview cells to mirror real rows.
*Introduced by*: 260723-wwoi-label-picker-markers-shades (wedge/previews); data rain added in the picker-dismissal follow-up (260723), always-on since 260724

### Sessions-pane scope is explicit state, not the SERVER panel's expansion
**Decision**: The sessions-pane scope (`all | current`) is its own persisted state (`runkit-panel-sessions-scope`), read via `useSessionsScope`, with no migration from the old `runkit-panel-server` coupling; the SERVER panel defaults open and its expansion no longer affects the session list.
**Why**: "do I want to see server tiles?" and "which servers' sessions do I want listed?" are unrelated concerns that previously shared one bit — so the SERVER panel couldn't default open without permanently filtering the list, and users couldn't filter the list without paying the tile grid's vertical cost. `runkit-panel-server` encodes panel visibility, not scope intent, so carrying it over would guess wrong as often as right.
**Rejected**: Migrating the old key (encodes the wrong intent); keeping the "Select a server above…" hint with reworded text for the no-current-server case (chose fallback-to-all instead — never a dead-end).
*Introduced by*: 260720-rzg7-sessions-scope-toggle-delink

### Scope state uses a generic enum-typed localStorage hook
**Decision**: Add `useLocalStorageEnum<T extends string>` as a generic sibling of `useLocalStorageBoolean` (same pub/sub + `storage`-event mechanics, unrecognized values → default), plus a thin `useSessionsScope()` wrapper exporting the `SessionsScope` type and key constant.
**Why**: The chip, session list, and palette entry are cross-component siblings needing one reactive source; a generic hook mirrors the established boolean sibling and keeps the scope vocabulary in one importable home.
**Rejected**: Encoding scope as a boolean in the existing hook (values are semantically `all|current`, not true/false); duplicating the pub/sub logic in a scope-specific hook (needless divergence from the boolean sibling).
*Introduced by*: 260720-rzg7-sessions-scope-toggle-delink

### Route guard keys on `serversLoaded`, not `servers.length > 0`
**Decision**: `resolveServerView`'s not-found branch gates on the explicit `serversLoaded` flag, never the `servers.length > 0` proxy.
**Why**: with pre-existing servers the length proxy is already truthy while the list is stale, so the guard fired not-found immediately for a just-created server before the post-create refresh landed. The explicit flag distinguishes "list has loaded" from "list is non-empty".
**Rejected**: `servers.length > 0` as a loaded-proxy — the root-cause bug of the create-server not-found race.
*Introduced by*: 260602-3i5d-fix-create-server-not-found-race

### No bounded-fallback timer on the waiting state
**Decision**: the create-server waiting state persists until the refreshed `servers` list contains the server — cleared by an event-driven effect on the list changing, with no timer and no polling.
**Why**: synchronous near-instant backend create makes a timer speculative; a polling loop would violate the no-client-polling anti-pattern.
**Rejected**: a ~5s bounded-fallback timeout for v1. If ever added it MUST be a single `setTimeout`, never a polling loop.
*Introduced by*: 260602-3i5d-fix-create-server-not-found-race

### Pending-switch identity is `{server, @N}`, never the session name
**Decision**: `PendingSwitchTarget` keys click-intent/URL-match on `{server, windowId}` — excluding the session name, including the server.
**Why**: excluding the session name lets the selection survive a session rename or a cross-session move where `@N` survives but the session name changed (a session-name match would flip false and release pending-click suppression early, bouncing the selection). Including the server is load-bearing because tmux `@N` ids are unique only per server and `AppShell` persists across `$server` route changes without remounting — window-id-alone matching false-positives on a cross-server id collision (serverA `@5` vs serverB `@5`), which would suppress serverB's alignment and let serverA's failure bounce yank the user cross-server with a false toast.
**Rejected**: `{session, windowId}` matching (bounced the selection on rename/move); window-id-alone matching (cross-server collision).
*Introduced by*: 260529-jad6-window-api-stability, 260715-38kg-window-switch-confirmed-motion

### Fresh-switch mask teardown is a bare `tearDownMask()` at the `beginPendingSwitch` seam
**Decision**: `beginPendingSwitch` proactively clears a leftover mask/grace timer with a bare `tearDownMask()` at the top of its callback — enforcing "a fresh switch owns ALL feedback" at the one seam every pending switch passes through.
**Why**: the animated path reaches `beginPendingSwitch` (via `beginWindowSwitchGate → startViewTransition → runSwitch`) with its OWN just-opened gate current; the bare primitive clears a leftover armed mask without settling that gate, so the earned slide survives. The one shared seam means a future gateless caller inherits the fix.
**Rejected**: `abandonSwitchFeedback()` here (settles the animated path's own current gate `"superseded"` → skips the earned slide, `260715-38kg` R8); per-call-site `tearDownMask()` at the two gateless callers (spreads the invariant across callers — a future gateless caller re-introduces the leftover-mask bug).
*Introduced by*: 260719-h0x4-window-switch-stale-mask-teardown

### No `role="tooltip"` on the status detail hover-card
**Decision**: the tier-2 status hover-card (today the sidebar row flyout card) advertises no `useRole({ role: "tooltip" })`.
**Why**: the W3C tooltip pattern requires a tooltip to contain NO interactive content, but this card holds real `<a>` links (PR + docs). Advertising `role="tooltip"` would lie to AT and wire a misleading `aria-describedby`.
**Rejected**: `role="tooltip"` (present in the plan, removed in review).
*Introduced by*: 260616-37ub-status-dot-tooltip, 260805-93dy-window-row-pr-glyph-register-flyout

### Two-tier tooltip boundary
**Decision**: tooltips are exactly two species with a hard boundary — tier-1 `Tip` (names a control: plain text + optional kbd/note, `role="tooltip"` + `aria-describedby`, never interactive) and tier-2 hover-cards (the sidebar row flyout card: state and/or real links, role-less). A tooltip needing a second line of state or anything clickable is tier-2; there is no fatter tier-1. Native `title=` values that are state or a content reveal (not a control name) stay native `title=` — neither converted to tier-1 nor promoted to new hover-cards.
**Why**: the two shells already had incompatible ARIA needs (§ No `role="tooltip"` on the StatusDot hover-card) — one visual language across both tiers, one clear rule for which to reach for, and no accidental middle species that lies to AT. Promoting state/content titles to new tier-2 cards is scope beyond naming a control, and two e2e specs assert those exact `title=` seams.
**Rejected**: a single configurable tooltip spanning both roles (re-introduces the role/interactivity conflict); promoting every native `title=` to a styled surface (builds hover-cards nobody asked for and breaks the asserted state-title seams).
*Introduced by*: 260722-73al-tip-tooltip-system

### One status-detail surface, three triggers — the ROW, not the dot
**Decision**: window-row status detail is served by a single row-anchored flyout card serving fine-pointer row hover, keyboard row focus, and coarse-pointer dot-tap. There is no per-dot hover-card, and the `StatusDot` carries neither a `tabIndex` nor a native `title` on any of its three surfaces.
**Why**: the 7px dot was a precise pointer target and a second tab stop ahead of the row treeitem; the whole row is a large forgiving target and is already the focusable treeitem, so one surface at a fixed x (the sidebar's right edge) covers every input modality with less markup, no jitter, and one fewer tab stop. Keeping a dot tip *and* a row card would mean two surfaces racing on the same hover.
**Rejected**: keeping the dot tip for focus/touch alongside the row card (two overlapping surfaces, the dot tab stop retained); a mouse-following card (jitters and covers the row it describes); a hover preview inside the bottom PANE panel (detail lands far from the pointer).
*Introduced by*: 260805-93dy-window-row-pr-glyph-register-flyout

### Flyout warm window is module state, not a nested `FloatingDelayGroup`
**Decision**: the flyout's shared delay-group scope is module-scoped state (`activeFlyout` + `lastClosedAt`) consumed through floating-ui's function-form `delay`, not a second `FloatingDelayGroup` provider around the sessions tree.
**Why**: `useDelayGroup` binds to the NEAREST `FloatingDelayGroup`, and the session rows render tier-1 `Tip`s inside the same subtree — a nested provider would capture those tips, changing their delay and mixing tier-1/tier-2 warmth (the sidebar-is-one-warm-tip-cluster contract). Module state gives the flyout an independent warm cluster spanning every row, and the function-form `delay` is evaluated at event time so warmth set by another row is seen without a re-render.
**Rejected**: a nested `FloatingDelayGroup` (captures the session-row Tips); a flat per-row delay with no warmth (strobes on a row sweep).
*Introduced by*: 260805-93dy-window-row-pr-glyph-register-flyout

### Fork affordance is double-gated: provider AND an optional handler
**Decision**: the flyout's fork button renders only when `win.chatProvider === "claude"` **and** the consumer supplied an `onFork` callback; a consumer wiring no handler renders no button and throws nothing.
**Why**: the provider half is a data gate (the fork mechanism is Claude Code's `--resume <id> --fork-session`, so a codex window has nothing forkable) and reuses the exact `WindowInfo` field the chat lens already gates on — no new data plumbing. The handler half is a *capability* gate: `useRowFlyout` has no server or navigation access of its own, and the board-route sidebar has no business forking, so the optional-prop idiom (`onSpawnAgent`, `onColorChange`) gates the affordance per call site without the hook growing a dependency it cannot satisfy.
**Rejected**: a provider-only gate with a required handler (forces every consumer, including the board sidebar, to supply one); threading server/navigate into the flyout hook (widens a leaf hook's surface for one button).
*Introduced by*: 260806-s4av-row-flyout-session-fork

### Fork in-flight state is leaf-scoped in `ForkLink`, and the guard is correctness not polish
**Decision**: `ForkLink` owns its own `busy` state with `disabled={busy}`, a redundant early `if (busy) return`, a `mountedRef`-guarded settle, and a clear on **both** outcomes.
**Why**: `onFork` POSTs a mutating endpoint that CREATES a tmux window, so N clicks create N fork windows — the guard is the only thing preventing that, not a loading-spinner nicety. Keeping the flag inside the button (rather than lifting it to the card or the row) means only the button re-renders during a fork, honoring the card's render-performance contract. The `mountedRef` is required rather than defensive: a *successful* fork navigates away and the card unmounts, so settle-after-unmount is the common path.
**Rejected**: lifting `busy` to `RowFlyoutContent` or `WindowRow` (re-renders the whole card / breaks the row's `memo` discipline); relying on `disabled` alone (leaves a click-during-settle window); clearing only on success (a failed fork would be permanently unretryable).
*Introduced by*: 260806-s4av-row-flyout-session-fork

### Rest glyph as an absolute overlay on the last slot, not a third flex slot
**Decision**: the row's rest-state PR glyph is absolutely positioned at the icon cluster's right edge, over the kill button's 24px slot, and hidden by a display swap; the pin/kill buttons keep their opacity gating.
**Why**: the action buttons are opacity-gated precisely so they occupy their slots at rest and stay keyboard-focusable. A display swap of the buttons themselves would make them unfocusable at rest (a keyboard regression), while overlaying the glyph delivers the approved "PR sits exactly where ✕ sits, only the last slot swaps" geometry with zero layout shift and no focus changes.
**Rejected**: a `hidden`/`flex` swap on the buttons (breaks Tab reachability at rest); a third flex slot (the glyph would sit left of the ✕ rather than in its place); an opacity fade over reserved space (the user rejected coexistence — the rest state must go away entirely).
*Introduced by*: 260805-93dy-window-row-pr-glyph-register-flyout

### Draft is a glyph-only gray, not a dot state and not a second icon
**Decision**: draft lives on the row glyph's existing color axis as `text-text-secondary`, and only there — the dot carries no PR state at all, so a draft is invisible to the dot while the glyph reads gray. Draft keeps the shared `GitPullRequestIcon`, varied only by color (the icon axis belongs to closed's ✕ variant, not draft).
**Why**: the glyph's color axis is a single pure function returning a Tailwind token, whereas the dot's hue axis is owned by the two-family channel model (cool = fab pipeline, warm = ad-hoc agent) — a draft hue there would break the family-identity glance rule the whole palette rests on, and a draft *shape* would need new geometry in `status-dot.tsx` plus label-map entries. Gray is the mapping already in the user's head (GitHub renders drafts gray) and `text-text-secondary` is the established inert/no-journey token in this exact model (`PHASE_HUE.none`), at ~4.8:1 in the light theme against the 3:1 WCAG floor for graphical objects. The two channels then answer different questions rather than duplicating one: the glyph says *whose turn is it*, the dot says *what runs here, is it healthy*.
**Rejected**: a new muted token or raw hex (contradicts the glyph's no-new-color-system rule); importing `PHASE_HUE.none` instead of the literal (couples the glyph's color axis to the dot's phase map — precisely the coupling being avoided); GitHub's distinct dashed draft glyph (a second SVG is more maintained surface for one bit color already carries, and the Sidebar Row Icon System pins one icon system per surface).
*Introduced by*: 260807-e30p-draft-pr-row-glyph-color

### The draft glyph branch sits below fail and is gated on `prState === "open"`
**Decision**: `prGlyphColor`'s chain is closed-muted → fail-ish → open-and-draft → open-checks-pending-yellow → open-green → merged-purple, with the draft branch conjoined on `win.prState === "open"`.
**Why**: a draft whose checks fail (or that has changes requested) is a problem first and a draft second, so `isFailish` keeps first position among the open cases — the same dominance `prDotState` encodes by ordering `fail` ahead of `healthy`; draft-first would hide a red signal behind a gray one on exactly the rows still owing work. The open-gate makes the merged→purple path unreachable from the draft branch *by construction* rather than defended only by GitHub's server-side un-draft-on-merge, and confines draft to displacing the open cases below it — the checks-pending yellow included (drafts stay muted while their checks run).
**Rejected**: draft above fail (buries the red signal); an ungated `if (win.prIsDraft)` (behaviorally identical today, but leaves the merged path defended by remote behavior instead of local structure).
*Introduced by*: 260807-e30p-draft-pr-row-glyph-color

### Closed PR earns a muted ✕ glyph, ranked above fail
**Decision**: `prOwnsGlyph`'s allowlist is `open`/`merged`/`closed` (an absent/unknown `prState` still never owns), `prGlyphColor` returns `text-text-secondary` for closed from a branch above the fail branch, and both glyph surfaces (window row, session tile) render the ✕ `GitPullRequestClosedIcon` when `prState === "closed"`. A closed draft reads closed — the draft branch is `open`-gated, so this falls out by construction.
**Why**: a closed-unmerged PR is an outcome worth a glance-level signal ("this branch's PR was rejected/abandoned — the window needs a decision: rework, re-open, or kill"); rendering no glyph made a closed PR indistinguishable from never having had one, which actively misleads during triage. Muted gray keeps a dead PR from drawing rest-state attention, and the ✕ shape is what separates closed from draft at the same inert token (GitHub disambiguates closed by shape, not color). Closed sits above fail because a closed PR's checks/review are historical noise — the same first-match rationale that puts `merged` above `fail` in `prDotState`.
**Rejected**: GitHub-exact red + ✕ (collides visually with fail-ish red and lights up dead PRs — rejected by the user after mock review); red on the same `GitPullRequestIcon` (indistinguishable from failing; GitHub never colors the plain PR icon red); leaving closed glyph-less (the misleading gap); a new color outside the popular PR vocabulary (learning curve).
*Introduced by*: 260810-xuej-closed-pr-row-glyph

### PR is evicted from the dot — dot = local story, glyph = remote story
**Decision**: the status dot renders only the local story (fab journey / ad-hoc agent / floor) with four hues (blue building → green PR-ready, yellow agent, gray floor) and three shapes (ring / solid / failed); PR state — draft, checks-pending, open, merged, failing, closed — lives exclusively on the rest-state row/tile glyph via the six-way `prGlyphColor`. The blue/green split is stage-based (`fabPhase`), never `prNumber`-based.
**Why**: the row glyph made the dot's PR tier redundant (a merged fab PR rendered purple twice, two pixels apart), and the PR tier displaced live pipeline state (a fab window with a PR never showed its stage on the dot). Splitting by story makes shape meaning uniform across every hue — fully compositional, four legend strips replace a 6×5 matrix — and gives every new PR-adjacent state a home on the glyph instead of pressuring the dot. Stage-based hue keeps the dot free of PR fields; alignment with PR existence stays emergent (`/git-pr` creates the PR mid-ship).
**Rejected**: square-only removal (keeps the double-encoding; merged becomes indistinguishable from open on the dot); the maximal one-fab-hue reduction (loses the "still cooking vs out the door" glance); a `prNumber`-based blue/green split (couples the dot to PR fields).
*Introduced by*: 260810-aqo6-statusdot-compositional-vocabulary

### Skipped display-state falls through the whole ladder
**Decision**: `statusDotState` treats `fabDisplayState === "skipped"` as "not a fab-owned dot" and continues down the ladder (fresh agent → yellow, else gray floor), rather than forcing gray inside the fab arm.
**Why**: a skipped change has left its journey; ladder continuation is the least machinery and lands on the gray floor in the common agent-less case, without hiding a live agent on a skipped change's pane.
**Rejected**: a dedicated skipped→gray short-circuit (extra machinery that would also hide a live agent on a skipped change's pane); a fab resting ring for skipped (it is not a resting fab change — it left the journey).
*Introduced by*: 260810-aqo6-statusdot-compositional-vocabulary

### Fab dot labels compose hue-word + displayState-derived status word
**Decision**: `dotLabel` fab labels are `building`/`PR-ready` (the phase word) + a status word derived from `fabDisplayState` (`done` → "parked", `ready` → "ready", `active`/unknown → "active"), never the raw stage word and never a PR word.
**Why**: the shape alone can't distinguish parked from pending (both render the resting ring), so the status word derives from the display-state; the label stays a pure function of what the dot shows while keeping the parked/pending split legible. The exact stage lives in the `fab` register on both register surfaces.
**Rejected**: the raw stage word (`review — active`) — re-encodes per-stage detail the dot no longer renders; a shape-derived status word (`done` would read "pending"); PR-native labels (`PR — merged`) — PR facts belong to the glyph and register surfaces, not the dot's label space.
*Introduced by*: 260810-aqo6-statusdot-compositional-vocabulary

### Clone-child reference API
**Decision**: `Tip` clones its single child element and merges the floating reference props/ref onto it (`useMergeRefs`), rather than rendering a wrapper element or a render-prop API.
**Why**: ~40 migrated call sites demand minimal churn; the top-bar's overflow fit measures `barRender()` widths in a hidden probe, so any wrapper element would distort measured widths; the codebase already uses the floating-ui reference-props idiom.
**Rejected**: a wrapper `<span>` (distorts probe widths and flex layouts); a render-prop shape handing each call site the reference setter (far noisier across 40 call sites).
*Introduced by*: 260722-73al-tip-tooltip-system

### Suppress-by-early-return on coarse pointers
**Decision**: under `pointer: coarse` (live via `useCoarsePointer`), `Tip` returns its child unchanged — no reference props, no portal, no ARIA wiring. Hooks still run unconditionally (parameterized on an `enabled` flag); only the render return is short-circuited.
**Why**: the approved spec gives touch no tooltip layer at all; not attaching the machinery is the cheapest correct implementation and makes the "no render under coarse pointer" assertion direct. The control's `aria-label` carries the name on touch.
**Rejected**: `enabled: false` on the hover/focus hooks only (still wires the `aria-describedby` machinery and keeps dead listeners).
*Introduced by*: 260722-73al-tip-tooltip-system

### One media-query substrate, thin policy wrappers over it
**Decision**: `useMediaQuery(query)` / `evaluateMediaQuery(query)` (`hooks/use-media-query.ts`) own the entire matchMedia mechanism — the non-browser guard, the `typeof`-checked modern-`addEventListener`-with-legacy-`addListener` fallback, the `mql.matches` re-read, the post-subscribe sync, and the `[query]`-keyed resubscribe. Every policy hook is a thin wrapper that contributes only its query string: `useIsMobile` ORs two subscriptions, `useCoarsePointer` is a single one, and `evaluateIsMobile` is the one-shot twin for non-hook readers. Components carry no local media-query hook and no width literal — `MOBILE_BREAKPOINT_PX` in `use-is-mobile.ts` is the single source of the 640px breakpoint, so `max-width: 639px` is always derived (`MOBILE_BREAKPOINT_PX - 1`), never typed.
**Why**: the fallback dance has several plausible-looking variants that differ in robustness — truthiness-checked vs `typeof`-checked listener attachment, trusting the `change` event object vs re-reading `mql.matches` — so any hand-written copy is a drift risk. A hardcoded `639` in a component is worse: it silently splits the app's mobile breakpoint the moment `MOBILE_BREAKPOINT_PX` moves, with nothing to fail. Concentrating the mechanism in one primitive makes the defensive variant the only variant and leaves each wrapper stating just its policy.
**Rejected**: keeping per-consumer copies of the subscription (the drift source); one combined `"(pointer: coarse), (max-width: 639px)"` query string inside `useIsMobile` (a single MQL is cheaper but re-introduces a literal breakpoint inside a string, where `MOBILE_BREAKPOINT_PX` cannot reach it, and loses the two-query shape existing test stubs key on); a CSS-only `coarse:` / `sm:` variant for the sidebar panels (their consumers need the boolean in JS to pick heights and grid-flow objects, not just classes).
*Introduced by*: 260731-tfr1-use-media-query-hook

### Two pointer policies stay unmerged
**Decision**: `useCoarsePointer()` keys on pointer TYPE alone; `useIsMobile()` keys on narrow width OR coarse pointer. Both wrap the same `useMediaQuery` substrate, and the shared substrate is explicitly not a licence to collapse them into one hook — a consumer picks the policy that matches what it is deciding.
**Why**: the two answer different questions. Tooltip suppression and the chat form's autofocus skip are about input capability — a narrow desktop window still has a hardware keyboard and a mouse — while layout topology (sidebar overlay, panel heights, terminal font default) is about available width, where a coarse pointer is a useful additional signal. Merging them would make a narrowed desktop window lose hover tooltips, or a wide touch device render the desktop sidebar.
**Rejected**: one `useIsTouch`/`useIsMobile` hook serving both (couples input policy to viewport width in both directions); routing an input-capability consumer through `useIsMobile` for consistency with the sidebar (the earlier explicit split decision, `260719-mxvw`, exists precisely to prevent this).
*Introduced by*: 260731-tfr1-use-media-query-hook

### Heading identifies the window, not the lens
**Decision**: the terminal-route center heading is a static `Window: <window>` in every lens (tty/web/code/chat); the active lens is shown by the `ViewSwitcher`, not the heading.
**Why**: the heading identifies the WINDOW (the substrate), per window-views.md "rows are substrates, views are lenses". Keeping the prefix constant also stops the name's anchor from jumping on a lens switch (the prefix width no longer changes with the lens).
**Rejected**: "the center page heading follows the lens" (window-views.md R4's original sentence — reversed in-spec within the change).
*Introduced by*: 260714-uco1-topbar-heading-anchor-nav

### `menuOnly` is a registry capability, not a view-switcher hack
**Decision**: the ViewSwitcher's never-in-bar placement is a generic optional `menuOnly?: boolean` on `RegistryEntry`, honored by the candidate pipeline for ANY entry (excluded from bar + probe + fit; its `menuRender()` rows always in the menu; `hidden` keeps priority). Seven entries set it — `view-switcher`, `fixed-width`, `terminal-font`, `close-pane`, `help`, `keyboard`, `theme` — each costing nothing but the flag (§ Right cluster → `menuOnly` demotions).
**Why**: rides the existing overflow machinery (`ViewSwitcherMenuRows` already existed and was wired), so reverting when the chat lens is ready is deleting one flag, and any future control can opt in. The chat lens is not fully functional, and an inline `[tty|chat]` pill advertised a half-finished lens in the most prominent chrome position.
**Rejected**: a one-off "skip the view-switcher" special case in the fit wiring (same diff, not reusable, harder to revert cleanly); removing the `ViewSwitcher` component or feature-flagging `hasChat()` (breaks the web lens / hides chat capability the user wants reachable); splitting the switcher so web stays inline while chat is menu-only (violates spec R4's one-switcher contract). The `[tty|web]` pill on iframe-URL windows moves too — accepted over splitting the unified switcher.
*Introduced by*: 260722-n2n4-view-switcher-menu-only

### Overflow list is candidates-minus-visible, not a menuOnly+overflowed concat
**Decision**: `overflowItems` is computed as `candidates.filter((e) => !visibleIds.has(e.id))` — the full candidate list minus the in-bar (visible) set — rather than concatenating the `menuOnly` entries with the non-fitting fit-candidate prefix.
**Why**: one expression preserves registry (pyramid) order for free — `menuOnly` entries interleave at their registry positions, so the `View:` rows lead the menu — and cannot double-count.
**Rejected**: an explicit concat of `menuOnly` + overflowed-fit-candidate lists (requires a re-sort by registry index to preserve order).
*Introduced by*: 260722-n2n4-view-switcher-menu-only

### Board pane focus tracks by key, not index
**Decision**: board pane focus is tracked by the focused pane's `server:windowId` key (`focusedKeyRef`); `focusedIndex` is reconciled from the key on every order change, and the palette `moveFocusedPane` does not bump `focusedIndex` itself.
**Why**: with `paneRefs` keyed to the authoritative order, bumping `focusedIndex` (`setFocusedIndex(i±1)`) before the SSE echo routed the imperative xterm `.focus()` into the displaced neighbour's terminal (wrong-terminal keystrokes). Key-follow reconcile keeps the focused pane's identity across own moves, own drop echoes, and remote reorders.
**Rejected**: index-follow (index bump alone) — misrouted focus before the order settled.
*Introduced by*: 260708-rmiq-board-pane-reorder

### Board imperative pane focus is gated on a true user-intent flag, not index change
**Decision**: the board's imperative `paneRefs[focusedIndex].focus()` is gated on `shouldFocusPane(intent, prev, current)` = `intent && prev !== current`, where `intent` is a `focusIntentRef` boolean set only by user-driven focus actions (keyboard cycle, palette cycle, different-pane click) and by own-move initiation through the single `reorderWithFollow` seam — never by a passive `board-changed` refetch. Index change alone does not focus.
**Why**: gating on index change alone cannot distinguish an own-move follow (wanted) from a remote-driven index reconcile (a `board-changed` from another client whose reorder/pin/unpin shifts the focused pane's index) — the latter would steal DOM focus into an xterm while the user types elsewhere, violating the "SSE must not steal focus" invariant. A true intent flag, mirroring the sidebar `focusMovedRef` literally, is set at intent sites and consumed once by the focus effect; a remote reconcile bumps the index but never moves DOM focus. The own-move follow survives the async POST→SSE-echo window by setting the flag at move initiation.
**Rejected**: index-change gating (steals focus on remote reconciles); suppressing focus on ALL order-changed re-entries (breaks the own-move follow — R6 of the reorder change); debouncing/time-windowing SSE-adjacent focus (heuristic, racy, unprincipled when a true intent signal is available). Accepted residual: a one-echo-round-trip race where a remote event interleaving before an own echo consumes the flag early — a single benign own-pane focus-follow, not a steal.
*Introduced by*: 260719-6dh9-board-focus-intent-flag

### Pin-indicator navigation is a popover row, not the indicator click
**Decision**: navigating from a pinned sidebar window row to its owning board is a two-step gesture (pin indicator → popover → "Go to {board}" row).
**Why**: the pin indicator keeps its pin/unpin *management* role; putting navigation on a dedicated popover row avoids hijacking the indicator's click.
**Rejected**: making the indicator click navigate (would collide with its management role).
*Introduced by*: 260718-co9z-link-based-board-pinning

### Drag-to-pin gates on a dedicated marker MIME alongside the JSON payload
**Decision**: the sidebar window-row drag carries `application/x-window-drag` (value: the `windowId`) as a second `dataTransfer` type; the `application/json` payload is untouched.
**Why**: during `dragover` a drop target can only inspect `dataTransfer.types` (payload is sealed until drop), and `application/json` is too generic to gate on — every drag surface in the codebase uses a dedicated custom MIME precisely to avoid collisions. Keeping the JSON payload identical leaves the existing reorder/move handlers untouched.
**Rejected**: gating board rows on `application/json` (collides with any other JSON drag); moving the payload into the marker MIME (breaks existing drop handlers).
*Introduced by*: 260811-g0t1-drag-window-board-pin

### Copy cursor for the drag-to-pin drop target
**Decision**: board rows set `dropEffect = "copy"` on a window-drag dragover; the window drag's `effectAllowed` is widened to `"copyMove"`.
**Why**: pinning LINKS the window (SESSIONS + BOARDS dual presence — the window stays in its home session and also appears on the board), so a copy (plus-badge) cursor is truthful; `"move"` remains permitted under `"copyMove"` for the existing reorder/move targets.
**Rejected**: keeping `effectAllowed = "move"` (a move cursor implies the window leaves its session — false for a pin).
*Introduced by*: 260811-g0t1-drag-window-board-pin

### One palette mount in AppLayout, routes register via the slot
**Decision**: The single (lazy) `CommandPalette` mounts in `AppLayout` and renders `[...routeActions, ...globalActions]` — routes publish route-scoped, shortcut-decorated lists through `useRegisterPaletteActions` (`palette-actions-context`, the `top-bar-slot-context` pattern), and the global groups (nav, font, refresh, help, shortcuts, settings, update/check/maintenance/version) build once at the layout in `use-global-palette-actions.ts`. The server create/kill dialogs and the shortcuts overlay follow the same shape: one mount in `AppLayout` (`server-dialogs-context` + `components/server-dialogs.tsx`, the `settings-dialog-context` pattern; layout-owned overlay state), triggered from any route.
**Why**: The board route does not render AppShell, so per-route mounts forced every globally-meaningful action and the server dialogs to be re-implemented inside `BoardPage` — and the twins had drifted in user-visible ways (the board's kill confirm lacked the daemon warning; its create input skipped the safe-name sanitization). A layout mount plus a register channel makes a global unreachable-by-omission impossible and gives every route the same dialog behavior.
**Rejected**: Per-route palette/dialog mounts with duplicated global groups (drift is inevitable — each global tweak must be written twice); rendering AppShell on the board route (AppShell is deliberately server-scoped); shared JSX with per-route state (removes JSX drift but keeps the state twins).
*Introduced by*: 260811-239r-board-twin-dialogs-palette

### The palette slot splits state from a stable API channel
**Decision**: `palette-actions-context` exposes three channels — a reactive state channel (`usePaletteActions`, the merged list, for the layout-level leaves that render it), a referentially-stable API channel (`setRouteActions` / `getAllActions`, identity never changes), and a globals-only channel (`usePaletteGlobals`) that routes may safely subscribe to. Routes resolve ids over `[...localRouteActions, ...globalActions]` at memo-build time, or imperatively via `getAllActions()` at chord/macro-invocation time.
**Why**: A route that both publishes its actions and subscribes to the merged state closes a render loop (publish → state change → re-render → fresh action identities → re-publish → "Maximum update depth exceeded"); and resolving through the slot state would lag one commit (the registration effect lands after the route's memo builds) with no way to trigger a rebuild, so a chord could stay dead. The globals-only prop identity never changes in response to a route registration, so subscribing to it is loop-safe.
**Rejected**: One reactive context for everything (render loop); routing all id resolution through the slot state (one-commit lag, unreachable rebuild).
*Introduced by*: 260811-239r-board-twin-dialogs-palette

### Single static `Update Now` — no dynamic version label
**Decision**: the palette has ONE update action, the static `run-kit: Update Now` (a full-roster force update in `buildMaintenanceActions`); the former qualifying-gated `run-kit: Update to v{X}` entry and its `updateActionLabel` label composition (the single-run-kit / single-sibling / N-tools label shapes) are deleted.
**Why**: the dynamic label was multi-tool ambiguous (the update is toolkit-scoped, not single-tool) and went stale between check passes. A static label always reads true; version detail lives where it is fresh — the check-result toasts (`composeCheckToast`) and the chip's `updateChipToolSummary`. `Update Now` reaching every tool (force = full-roster `shll update`) also picks up patch-only bumps a scoped match set would skip.
**Rejected**: keeping the dynamic `Update to v{X}` label (ambiguous + stale); a 2×2 check×update / minor×patch command matrix (the update axis has only one real action — brew installs the manifest latest regardless; the minor/patch distinction exists only on the check axis).
*Introduced by*: 260720-n2ai-shll-check-updates-delegation-palette-checks

### Notable-set is the single convergence key; sub-threshold verdicts ride alongside
**Decision**: the chip visibility, the composite dismissal `Key`, and the `/api/update` argv stay derived from the NOTABLE set only; the full per-tool verdict list (incl. sub-threshold `notable:false` rows) rides the `update-available` payload but never affects Key/qualification. `useUpdateNotification` filters `tools` to the notable subset (missing flag = notable) with stable referential identity.
**Why**: keeps the chip, dismissal semantics, `use-update-click` completion signal, and `/api/update` argv byte-compatible with the pre-delegation behavior — a patch-only finding is toast-only by design (it must never light or re-dirty the chip). The ambient/released payload still carries the full per-tool verdict list so a released incl.-patches view reads sub-threshold rows off one payload; the incl.-patches command itself queries the fresh github source as a side channel (`260720-wb3n`) that likewise never dirties the chip.
**Rejected**: keying the chip/dismissal on the full `update_available` set (would light + re-dirty the chip on patch bumps and churn dismissals).
*Introduced by*: 260720-n2ai-shll-check-updates-delegation-palette-checks

### Shared check-flow hook instead of per-consumer duplication
**Decision**: the on-demand check POST→toast flow (result composition, gated Update Now action slot, error mapping) lives in ONE `useUpdateCheck` hook consumed by the layout-level palette global builder (`use-global-palette-actions.ts`) and the overflow-menu check affordance.
**Why**: mirrors `use-update-click.ts`'s anti-drift extraction — inline copies of the toast composition + error mapping at each consumer would drift.
**Rejected**: duplicating the flow inline at each consumer (copies of toast/error logic drift).
*Introduced by*: 260720-n2ai-shll-check-updates-delegation-palette-checks

### Github-sourced toast suppresses the sub-threshold annotation
**Decision**: `composeCheckToast` takes the echoed report `source` (optional, default `""`) and drops the `(patch — below notify threshold)` annotation when `source === "github"`; the `updateAvailable`/`notable` filtering is untouched. The frontend keys off the ECHOED report source rather than an invented client boolean.
**Why**: the github backend has no notify policy, so every row decodes `notable=false` — annotating each as a sub-threshold patch would mislabel a real minor bump (`run-kit 3.8.7 → 3.9.1`). Echoing `report.Source` doubles as a defensive check (the client reacts to what actually ran); an old daemon that omits `source` defaults to `""` and keeps today's annotated behavior.
**Rejected**: a client-side `includePatches`-implies-github boolean (fragile — decouples the annotation from what the daemon actually queried); suppressing on `includePatches` alone (a released incl.-patches view SHOULD still annotate its genuine sub-threshold rows).
*Introduced by*: 260720-wb3n-github-source-patches-check

### Dismissed-pending shows the menu update surface, not the ⟳
**Decision**: the overflow menu version row's update-surface condition is `qualifies && (updateOverflowed || !showChip)` (via `asUpdateSurface = tools.length>0 && (updateOverflowed || (qualifies && !showChip))`) — so a dismissed pending update shows the update surface rather than the resting ⟳ check affordance — while the chevron **attention badge** keeps the narrower undismissed-overflowed condition (`updateOverflowed && tools.length>0`).
**Why**: the menu is deliberate discovery (like the palette, which already ignores chip dismissal via its `qualifies`-only gate), so surfacing a known pending verdict there does not violate "dismissal silences the ambient chip" — the badge, which IS ambient, stays silenced. When a verdict is already known the update surface is the stronger, truer affordance; offering "check again" over a known pending update is a blind-affordance smell.
**Rejected**: rendering the ⟳ when dismissed (offers a redundant re-check while a pending verdict already exists); lighting the badge for the dismissed case too (would re-introduce the ambient signal dismissal is meant to silence).
*Introduced by*: 260720-ml7k-unified-toolbar-update-button

### The manual check feed is tab-local frontend state, not a cached backend verdict
**Decision**: a manual ⟳ / palette check result is held in plain, non-persisted `SessionContext` React state (`manualCheck`) and merged into the update surfaces frontend-side. The backend is untouched: `handleUpdatesCheck` still never writes the github verdict into the cached checker slot, and no API/SSE surface grows.
**Why**: the side-channel contract is deliberate — the github source carries no notify policy, so caching its verdict would wipe a legitimate released-manifest chip and starve the scoped `shll update` argv (`Snapshot().Matched`), reopening the argv-scoping question that design closed. Frontend state gives the finding a persistent home without touching any of that. The accepted tradeoff is explicit: the result is **tab-local and a page reload before updating forgets it** — re-clicking ⟳ is one click, and the ambient feed remains the durable cross-tab channel.
**Rejected**: caching the github verdict in a separate backend slot and broadcasting it over SSE to all tabs (grows the API/SSE surface and reopens argv scoping); persisting the manual result to localStorage (would resurrect a stale verdict across reloads with no server signal to invalidate it).
*Introduced by*: 260807-s6zs-manual-check-update-surface

### The two-feed merge lives in `useUpdateNotification` alone, ambient-first
**Decision**: `deriveUpdateFeed` is called from exactly one derivation (plus `dismissUpdate`, which needs the same effective key); the in-bar `UpdateChip`, the overflow-menu version row, and the palette's layout-level global group consume the merged output with **no per-surface or per-mount branching**. Precedence is ambient-first: when the ambient feed shows, every returned field is purely ambient-derived and the manual feed changes nothing.
**Why**: the AppShell↔board palette non-drift constraint is structurally guaranteed only if the merge happens at the single shared derivation — `asUpdateSurface = tools.length > 0 && (updateOverflowed || (qualifies && !showChip))` already picks up the manual feed with zero component edits. Ambient-first because the ambient feed is the durable, policy-driven, cross-tab channel; a tab-local snapshot must never displace a live policy verdict.
**Rejected**: merging at each consumer (four copies of the precedence rule — the exact drift the shared-hook architecture exists to prevent); manual-first or newest-wins precedence (a manual snapshot would shadow the durable ambient verdict and its scoped-update click path).
*Introduced by*: 260807-s6zs-manual-check-update-surface

### Manual-fed clicks force; ambient-fed clicks stay scoped
**Decision**: `useUpdateClick` branches on `manualOnly` — a manual-fed surface triggers `forceUpdateNow()` (full-roster `POST /api/update {"force":true}`), an ambient-fed surface keeps the scoped `updateNow()`.
**Why**: the scoped endpoint moves only the server's `Snapshot().Matched` set, which a side-channel manual verdict never populates — a scoped click on a manual-fed chip would find nothing to do. Force is also exactly what the check toast's own "Update Now" action already runs, so the chip and the toast remediate identically.
**Rejected**: always forcing (would widen the ambient chip's deliberately scoped argv to the full roster); teaching the backend to accept a client-supplied tool list (a new API surface, and it would let a stale tab drive the update argv).
*Introduced by*: 260807-s6zs-manual-check-update-surface

### The watch affordance: already-running navigates, fresh spawn offers a toast action
**Decision**: update/restart triggers resolve `UpdateTriggerResult` (`{status, watch?}`) and one shared pure helper — `consumeUpdateWatchTarget(result, navigate, addToast)` in `hooks/use-update-click.ts`, consumed by both `useUpdateClick` and the palette's restart wrapper — turns the `watch` target into UI: `already-running` navigates straight to the job window's terminal route (`/$server/$window`, window param = the `@N` id), a fresh spawn shows an info toast with a "Watch" action slot, and a `watch`-less (old-daemon) result is a no-op.
**Why**: the job runs in a managed tmux window (architecture § Daemon Lifecycle → `rk-jobs`), so the terminal route already renders it with zero new UI plumbing — the second click on an in-flight update IS the jump to the live job, which is more useful than an error. One helper keeps the chip, the overflow-menu surface, and the palette actions from drifting. Auto-navigating on every fresh spawn would rip the user off their current window for a job they may not want to watch, so fresh spawns only OFFER the jump.
**Rejected**: navigating on every trigger (disruptive); a `409 already-running` (treats a truthful derived answer as an error); new components or SSE additions (the terminal route + relay suffice).
*Introduced by*: 260812-z1ya-update-daemon-tmux-window

### Ambient-key-change clearing baselines at store time, not a raw key watcher
**Decision**: the provider stamps the ambient composite key observed when the manual result is stored (`manualAmbientKeyRef`) and clears the manual state only when a **later** ambient key differs from that baseline.
**Why**: a naive effect on `updateAvailable.key` would clear the manual result on the first ambient render after storage even when the key never changed (or on an initial `null`→`null` render), destroying the feed immediately. Baselining at store time mirrors `use-update-click.ts`'s `clickKeyRef` completion signal (R13) exactly, and treats the ambient verdict moving on as the update-consumed signal — including the cleared empty key.
**Rejected**: clearing on any `updateAvailable` state write (churns the manual result away on idempotent SSE replays); a timeout-based expiry (invents a policy the ambient feed already supplies).
*Introduced by*: 260807-s6zs-manual-check-update-surface

### Check affordance is a non-menuitem control (menu stays open)
**Decision**: the resting version row's ⟳ "Check for updates" affordance is a plain button (no `role="menuitem"`, `tabIndex={-1}`) inside the row, so the container's role-keyed close handler does not fire on click.
**Why**: the in-flight spinner + single-flight state is only meaningful if the menu stays open across the ~1-2s check; the font-stepper precedent (`role="group"` row, plain buttons) established this pattern for non-terminal menu actions. Arrow-key nav still reaches it (the focusables selector matches `button:not([disabled])`).
**Rejected**: `role="menuitem"` with close-on-click (hides the busy state and makes single-flight moot); an intermediate "checking…" toast (already rejected in `260720-n2ai`).
*Introduced by*: 260720-ml7k-unified-toolbar-update-button

### TopBar mounts once at the root layout
**Decision**: `TopBar` mounts a single persistent instance at the pathless `app-layout` route, above the router `<Outlet>`, and re-renders in place across route changes; a `<Suspense fallback={null}>` boundary wraps ONLY the `<Outlet>` content region, not the header.
**Why**: mounting `TopBar` per-page unmounts one instance and mounts a fresh one on cross-page navigation — a visible navbar "reload"/flicker (a component-identity problem, not an MPA→SPA gap; the app is already an SPA). A single persistent mount keeps the bar continuously present with a per-route heading; scoping Suspense to the Outlet keeps the bar painted while a lazy page chunk loads.
**Rejected**: per-page `TopBar` mounts (three instances — AppShell, HostOverviewPage, BoardPage — caused the flicker); wrapping the header in the Suspense boundary (blanked the bar on lazy load); hosting the bar directly in `RootWrapper` (the index route `/` is a direct child of the root at the same pathname, so root→index is structurally shorter than root→serverLayout→…, and React remounted the root subtree on the index transition — the pathless layout normalizes tree depth).
*Introduced by*: 260707-4vq2-lift-topbar-root-layout

### Refresh is a full page reload
**Decision**: the top-bar RefreshButton does a full page reload (`window.location.reload()`; Shift+click force-reloads), not a targeted relay reconnect.
**Why**: the reload is non-destructive by design (all client state re-derives on load; tmux sessions are unaffected) and needs no spinner because the page unloads synchronously.
**Rejected**: a `TerminalClient` `key`-bump remount to reconnect just the relay (deferred as a possible follow-up); tmux `respawn-pane` (destructive — kills the running process).
*Introduced by*: 260703-p29w-topbar-refresh-button

### Infra servers are greyed, not hidden; identified frontend-side by name
**Decision**: infrastructure tmux servers (`rk-daemon`, `rk-test-*`) are de-emphasized (greyed + sorted-last) but never hidden; they are identified frontend-side by name (`isInfraServer`), not by a backend flag.
**Why**: the backend deliberately surfaces every server so the operator sees exactly what `rk reaper` would reap (the contract at `tmux.go:1332-1341`); greying + sorting-last preserves that visibility while making the infra/workspace distinction legible. A name-based check keeps a pure display concern out of the API, and the socket name is effectively frozen (renaming it orphans deployed daemons).
**Rejected**: hiding infra servers (loses the reaper-visibility contract); a backend `isDaemon` flag on `/api/servers` (API surface for a display concern).
*Introduced by*: 260705-uh8f-deemphasize-infra-servers

### HostMetrics is a shared component
**Decision**: the host metric-row rendering lives in one layout-agnostic `HostMetrics` presentational component consumed by both the sidebar `HostPanel` and the `/` HOST HEALTH zone.
**Why**: the two host surfaces must stay visually consistent; a shared component avoids drift.
**Rejected**: duplicating the ~120 lines across the two surfaces (drift risk).
*Introduced by*: 260701-vshd-cockpit-host-console-home

### Focused-pane context carries thin board-entry data, not just identity
**Decision**: the focused-pane context value is `{ server, windowId, windowName, panes }` (tile identity + the board entry's thin pane data), not the bare `{ server, windowId, cwd }` triple the top-bar slot's `focusedPane` carries. The board route's board-entry enrichment reaches the sidebar PANE fallback through this one channel.
**Why**: the pin-only thin render (a window whose home session died while pinned, so the `windowId` lookup in `sessionsByServer` misses) needs `windowName` + `panes` at the consumer; publishing them with the identity keeps `BottomPanels` free of any board-API knowledge and avoids a second data channel. Server-side enrichment of `GET /api/boards/{name}` was rejected upstream (staleness between `board-changed` refetches + duplicates the live SSE enrichment the home-session copy already delivers), so the fill is a pure client-side join.
**Rejected**: publishing only `{server, windowId}` and having the sidebar fetch/join board entries (reintroduces board-data plumbing into the sidebar plus a fetch the board page already holds); a dedicated thin-render branch inside `WindowPanel` (would duplicate row markup and couple the pure panel to board types — instead the fallback synthesizes a partial `WindowInfo` with `activityTimestamp: 0` and renders through the unchanged `WindowPanel`).
*Introduced by*: 260720-zx4i-board-route-pane-host-panels

### Window-switch feedback stays optimistic
**Decision**: window-switch navigation stays optimistic — the heading/URL flip at click (acknowledged intent) and the sidebar highlight stays SSE-derived (confirmation); the slide plays only on confirmed-fast arrival, with a pending mask + failure bounce-back layered on.
**Why**: keeps the terminal rendering immediately while giving each visual state an honest, distinct signal.
**Rejected**: pessimistic navigation (waits for confirmation before flipping); a dimmed overlay.
*Introduced by*: 260715-38kg-window-switch-confirmed-motion

### Single theme-color meta writer module
**Decision**: All `meta[name="theme-color"]` writes funnel through one module (`src/instance-accent.ts`) holding module-level `currentAccentHex` + `lastBackground` state; `theme-context.tsx` (`applyThemeColorMeta`) and the instance-accent provider (`setAccentThemeColor`) both write through it, and the content resolves to the accent hex when set, else the theme background.
**Why**: React passive effects run child-first, so `ThemeProvider`'s `applyThemeToDOM` effect fires AFTER any child accent effect on a theme switch and would clobber an accent-tinted meta tag with the bare background. One writer with shared state removes the ordering race at its root rather than papering over it.
**Rejected**: having the accent provider re-write the meta in its own effect after theme changes — loses deterministically to the parent effect's ordering (fixes the symptom, not the cause).
*Introduced by*: 260721-1etw-instance-accent-host-color

### Instance-accent echo carries the precomputed meta hex, not just the descriptor
**Decision**: `runkit-instance-color` stores `{"value","hex"}` where `hex` is the final computed theme-color meta content; the `index.html` blocking pre-paint script applies `hex` verbatim. The echo is a paint cache only — never authoritative (the runtime resolution overwrites it every load).
**Why**: deriving a hex from a descriptor needs the OKLCH/palette machinery (`colorValueToHex` + contrast guard), far too heavy to inline in a blocking script; precomputing at echo time keeps the script a validate-and-apply read. A cross-theme-mode load shows a transient mismatch that self-corrects post-fetch (accepted).
**Rejected**: inlining the derivation in `index.html` (duplicates themes.ts logic in unlintable inline JS); echoing only the descriptor (leaves the script unable to tint).
*Introduced by*: 260721-1etw-instance-accent-host-color

### PWA titlebar carries a subtle blend, not the full accent hue
**Decision**: The theme-color meta content (and the echo's `hex`) is `titlebarHex` — a `blendHex(src, background, INSTANCE_TITLEBAR_RATIO = 0.35)` accent-into-background blend — not the contrast-guarded `stripeHex`. `stripeHex` stays the 2px top-bar stripe and the HOST hostname tint; `washHex` (~6.5%) stays the top-bar background wash. Three distinct derived hexes, one per surface class.
**Why**: mock parity. The design mock tinted the installed-PWA titlebar with a subtle dark blend of the accent into the theme background and relied on the full-brightness 2px stripe sitting directly below it as the vivid accent line. Painting the meta with the full-hue `stripeHex` made the whole titlebar a loud color band and rendered the stripe invisible — it abutted a titlebar painted the identical hex. The blend restores the tinted-titlebar + bright-line pairing the mock designed. Reuses the existing `blendHex` machinery (theme-aware by construction, no hardcoded hexes); `index.html` needs no change because the pre-paint script applies the echoed `hex` verbatim, so swapping the echoed value retints cold start for free.
**Rejected**: keeping the saturated titlebar and instead removing/relocating the stripe (the mock's whole point is the tinted-titlebar + bright-line pairing); a dynamic manifest / tinted dock icons / Badging API (separate follow-up `260722-eo8e-accent-dock-icon`).
*Introduced by*: 260722-y5c3-instance-accent-titlebar-wash

### `titlebarHex` is exposed from the accent context, not re-derived in the strip
**Decision**: `InstanceAccent` carries a nullable `titlebarHex` alongside `stripeHex`/`washHex`, and the shell titlebar strip reads it from the context. It does not call `deriveAccentHexes(color, theme)` itself, and it does not read the rendered `theme-color` meta.
**Why**: The provider already computes all three hexes for the meta bridge, so a second derivation in the strip would be one formula in two places — and the failure mode of drift is silent (a strip and a PWA titlebar that are subtly different colors for the same accent, on surfaces users rarely see side by side). Exposing the field costs a line in the context value and a mechanical fixture update; the field was nullable-by-construction already.
**Rejected**: A strip-local `deriveAccentHexes` call (duplicate derivation, silent drift); reading `document.querySelector('meta[name=theme-color]').content` (a DOM round-trip for state React already holds, and it inverts the dependency — the meta is an *output* of the accent state); passing the hex down as a prop from `AppLayout` (threads accent state through a layout that otherwise does not care).
*Introduced by*: 260731-ofws-shell-accent-titlebar-dock-badge

### Drag regions are a CSS utility class, not an inline-style cast
**Decision**: `globals.css` defines `.rk-shell-drag { -webkit-app-region: drag; }` and `.rk-shell-no-drag { -webkit-app-region: no-drag; }`; the shell titlebar strip applies them as classes. No component writes `-webkit-app-region` inline.
**Why**: `WebkitAppRegion` is not in React's `CSSProperties`, so an inline style needs an `as CSSProperties` assertion — exactly the type-assertion-over-narrowing pattern the project's code-quality rules forbid. A class sidesteps the type system entirely, is inert in browsers, and lands in the `rk-*` utility vocabulary the codebase already uses for non-Tailwind primitives (`rk-scanlines`, `rk-hazard`, `rk-waiting-seam`). The `no-drag` sibling exists for the same reason as the `drag` one and serves the strip's single interactive island (→ The strip's switcher trigger is the band's only no-drag island).
**Rejected**: `style={{ WebkitAppRegion: "drag" } as CSSProperties}` (the banned assertion); a `<DragRegion>` wrapper component (a component to carry one declaration); augmenting React's `CSSProperties` type globally (a global type change to serve one property in one component).
*Introduced by*: 260731-ofws-shell-accent-titlebar-dock-badge

### The strip's switcher trigger is the band's only no-drag island
**Decision**: The titlebar strip band holds exactly ONE interactive element — the host-switcher trigger and its open menu, carrying `.rk-shell-no-drag`. Everything else in the band is a pure drag surface, and no other chrome is admitted into it. The one-island scope IS the rule: the band's job is drag + host identity, and the switcher is admitted only because it IS that identity ([desktop-shell](/run-kit/desktop-shell.md) § Design Decisions → The titlebar is colored by a page-drawn strip).
**Why**: The strip's centered label *names* the switchable identity, so an inert label is a discoverability dead end for the shell's most identity-defining action — otherwise reachable only through the native menu, the ⌥⌘1–9 accelerators, and the palette, all keyboard-or-menu paths where Constitution V asks for a mouse-secondary one. A single carve-out costs nothing the whole-band drag rule protects: the cost that rules out a titlebar merge is `no-drag` exceptions threaded through *every* control of live chrome, whereas one pill on an otherwise unchanged band leaves the drag surface the whole band minus that pill and the SPA top bar below untouched. It also needs zero new IPC — `servers.list()` and `servers.switch` already serve the palette block.
**Rejected**: Leaving the band inert so switching stays keyboard/menu-only (the discoverability dead end above); merging the top bar into the titlebar so its controls become the chrome (the option the strip decision rejects, at the same cost, over a much larger surface); putting the switcher in the top bar below the strip (the strip's label is where the eye already rests for host identity, and duplicating the host name in two bars breaks the move-don't-copy rule); admitting further chrome into the band on this precedent (the island is scoped to identity, not opened to chrome generally).
*Introduced by*: 260731-4bqi-shell-strip-host-switcher-dropdown

### Host-switcher accelerator knowledge lives in three shapes, not one helper
**Decision**: The mac-vs-other host-switcher accelerator knowledge is intentionally triplicated: `app/desktop/src/menu.ts` holds `MAX_SWITCHER_ACCELERATORS` plus Electron's `Alt+Cmd+{n}` / `Shift+Ctrl+{n}` binding syntax; `components/shortcuts-overlay.tsx` holds the `switcherCaps` segmented keycap array keyed on `"mac"`; and `lib/shell-strip.ts`'s `hostAcceleratorHint` holds the `⌥⌘{n}` / `⇧Ctrl+{n}` display string keyed on `"darwin"`. No shared constant spans them.
**Why**: The three consumers span the Electron main/renderer process boundary — `menu.ts` runs in main and cannot import from the SPA bundle — and each needs a genuinely different *shape*: Electron accelerator syntax, an array of keycap segments for the overlay's rendering vocabulary, and a single glyph string for a menu row. Their platform vocabularies also differ (`"mac"` vs `"darwin"`), following each side's own convention. Extracting a shared source would mean a cross-process package plus a shape-conversion layer for three call sites.
**Rejected**: A shared accelerator module imported by all three (crosses the main/renderer boundary the shell's electron-free-module discipline maintains); deriving the display strings from the Electron syntax at runtime (a parser for three literals); collapsing the overlay's segmented array to a plain string so two of the three could share (loses the keycap rendering the overlay is built on). A future change to the binding tier must update all three deliberately — this entry is the pointer that names them.
*Introduced by*: 260731-4bqi-shell-strip-host-switcher-dropdown

### Shell chrome mounts on `isShell()`; switch affordances gate on list emptiness
**Decision**: The titlebar strip and the badge reporter are *mounted* from `AppLayout` behind `isShell()`. Every affordance that actually *switches* hosts — the shell server-switch palette block and the strip's own switcher trigger inside the mounted band — gates instead on `listShellServers()` resolving a non-empty list (`stripSwitcherEnabled`). The two gates nest: the band mounts on `isShell()`, its trigger appears on the list.
**Why**: The gates answer different questions and both answers are right for their surface. A switch affordance asks "does the `servers` bridge group actually answer?" — an older shell exposes `version`/`platform` without the group, and offering a switch that cannot switch is worse than offering none. Window chrome asks "is this a shell window at all?" — the strip must exist whenever the native titlebar is hidden, even if `servers.list()` fails (the label falls back to `location.hostname`), and the reporter is harmless when the `badge` group is absent (`setShellBadge` resolves `false`). Splitting the strip's mount gate from its trigger gate is what lets both answers hold on one surface: a denied list degrades the band to a static label instead of removing the window's drag surface.
**Rejected**: Gating the strip's *mount* on the `servers` list (a failed or denied list call would remove the window's only drag surface — a bridge hiccup becomes an unusable window); gating the strip's trigger on `isShell()` alone (an older shell would render a chevron whose click can never switch); gating the palette on `isShell()` (the less accurate signal for that surface); a single shared gate for all of it (the surfaces have genuinely different failure semantics).
*Introduced by*: 260731-ofws-shell-accent-titlebar-dock-badge

### The emptied-list arrow guard reads the live row count from a commit-synced ref
**Decision**: The shell titlebar strip's capture-phase keydown handler reads the host-row count from `hostCountRef.current` inside `handleKey` — both the emptied-list guard (`if (hostCount === 0) return`) and the wraparound modulus key on that live read — and a `useLayoutEffect` keyed on `rows.length` keeps the ref current at commit time.
**Why**: A capture-phase handler stays attached from a commit until the passive-effect flush swaps the subscription, so a closure-captured count reads stale exactly in that window — after an open-time refetch empties the list, the stale handler would `preventDefault` ArrowDown/ArrowUp app-wide until the flush. The ref read makes the guard subscription-staleness-proof: it reflects the committed count regardless of which render subscribed the handler. The layout-effect write satisfies the no-later-than-commit contract without mutating a ref during render (unsafe under discarded concurrent renders); a passive `useEffect` write would flush in the same phase it is meant to beat. This also retired a CI-only flake in the colocated suite, whose synchronous `fireEvent.keyDown` could land inside that window.
**Rejected**: A closure-captured count (the stale-subscription window itself); a passive-effect ref write (same-phase flush); forcing synchronous effect teardown (fights React's effect model); wrapping the test's timing in `act` (fixes the test while leaving the production arrow-swallow window in place).
*Introduced by*: 260811-fgzi-shell-titlebar-arrow-release-flake

### Open split-button: last-used primary via localStorage, menu when no preference
**Decision**: the Open control's primary segment re-runs the last-used target (persisted kind-qualified to `localStorage["runkit-open-last-used"]`, the `runkit-*` preference convention); with no stored (or no-longer-available) preference the primary click opens the menu instead, and the chevron always opens the menu.
**Why**: mirrors Conductor's split-button UX and the existing `runkit-terminal-font-size` / `runkit-last-pinned-board` preference pattern — a one-click repeat of the common action with the full list one chevron away; a menu-on-first-use avoids firing an arbitrary default. Kind-qualifying the id (`deeplink:vscode` / `host:vscode`) stops a deeplink and host entry for the same editor from colliding on the shared key.
**Rejected**: a fixed default editor (arbitrary, wrong for most); primary always opens the menu (loses the one-click repeat); an un-qualified last-used id (deeplink/host collision for the same editor).
*Introduced by*: 260722-6d0f-navbar-open-in-app

### Open control owns its data via an invalidatable external store; TopBar props untouched
**Decision**: a `useOpenTargets` hook backed by a module-level external store (health + open-apps fetched once, then static between settings commits) feeds both the TopBar registry entry and the palette builder; no new TopBar props or slot-context fields are added. Freshness comes from an exported `invalidateOpenContext()` invoked at the ONE seam the data changes — the Settings dialog's successful SSH-host commit (§ Settings-commit invalidation seam) — NOT from client polling or per-render refetch.
**Why**: keeps the TopBar prop/slot surface stable (the e2e suite asserts chrome details) and avoids client polling (a code-quality anti-pattern). The open context genuinely only changes at the SSH-host commit, so invalidating exactly there is precise and cheap; everywhere else the once-per-load fetch is correct.
**Rejected**: threading `openTargets` through the slot context (prop churn across app.tsx/TopBar/tests); a per-menu-open or per-render refetch (needless requests, popup latency, and it re-fetches even when nothing changed); client polling for freshness (a code-quality anti-pattern — the SSE stream / event seam is the sanctioned path).
*Introduced by*: 260722-6d0f-navbar-open-in-app; invalidatable store 260723-l317-open-button-ssh-refresh-icon

### Settings-commit invalidation seam refreshes the open context, over polling or per-render refetch
**Decision**: the open context is refreshed by an exported `invalidateOpenContext()` called from the Settings dialog's SSH-host `commit` handler after `await setSSHHost(...)` **succeeds** (success only — a rejected commit leaves the server value unchanged, so it must not invalidate). The store is React's `useSyncExternalStore` over a module-level listener `Set` + `getSnapshot`. Invalidation drops the cache and, when subscribers are mounted, eagerly refetches and notifies (mounted consumers re-render fresh, no reload); with zero subscribers it plain-drops the cache for the next mount.
**Why**: the SSH host is runtime-mutable via the Settings dialog (`POST /api/settings/ssh-host`) and the backend serves the fresh value per `GET /api/health` request, but the frontend cached it once per load — so a saved host only reached editor deeplinks after a full page reload, embedding the stale derived `${sshUser}@${location.hostname}` fallback until then. Invalidating at the single commit seam is the precise fix. `useSyncExternalStore` is React 19's canonical external-store primitive — tear-free snapshots, automatic unsubscription on unmount, no `alive`-flag bookkeeping.
**Rejected**: refetching health on every Open-button render (wasteful — the once-per-load cache is correct everywhere except this one seam); client polling (a code-quality anti-pattern — use the event seam); a hand-rolled subscriber array driving per-consumer `useState` (more code for the same behavior, re-implements what the hook provides); invalidating on a failed `setSSHHost` (the commit failed; the server value is unchanged).
*Introduced by*: 260723-l317-open-button-ssh-refresh-icon

### Epoch guard discards in-flight open-context fetches that predate an invalidation
**Decision**: a module-level `epoch` counter is bumped on every `invalidateOpenContext()` (and `resetOpenTargetsCacheForTest()`); a fetch records the epoch at start and writes `cached` / notifies subscribers / clears `pending` **only** when the epoch still matches at resolve time.
**Why**: two rapid SSH-host commits would otherwise let the first (now-stale) refetch resolve after the second invalidation and resurrect exactly the pre-commit data this change exists to kill.
**Rejected**: ignoring the race (small window, but silently serves the stale value the invalidation dropped); aborting via `AbortController` (heavier — the fetch helpers take no signal).
*Introduced by*: 260723-l317-open-button-ssh-refresh-icon

### Remote client shows deeplinks; the derived `user@hostname` replaces the `RK_SSH_HOST` gate
**Decision**: the deeplink section shows for any remote client (no `RK_SSH_HOST` gate). The effective SSH host resolves as `RK_SSH_HOST` verbatim (alias — no `user@` prefix), else a derived `${sshUser}@${location.hostname}` on remote clients (prefix omitted when `sshUser` is empty), else empty for local.
**Why**: on a tailnet `location.hostname` (the name the browser reached the host by) is itself SSH-reachable, and the one missing ingredient — the SSH username — is derivable server-side (`os/user.Current()`, per constitution X, no new config). Gating on `RK_SSH_HOST` left the entire remote story permanently invisible for zero-config Tailscale users. The accepted trade is "shown ⇒ works on tailnets, errors on tunnels": behind an HTTP-only tunnel the derived host is not SSH-reachable and the editor shows its own connect error rather than the section staying hidden. `RK_SSH_HOST` remains the override for aliases, non-standard users, ports, and keys.
**Rejected**: keeping the `RK_SSH_HOST` gate (permanently-dark feature for the common case); probing reachability of the derived host before showing (guessing; the connect-error trade is accepted); an explicit-disable sentinel (`RK_SSH_HOST=none`) in v1 (deferred — most env loaders can't distinguish empty from unset, and the gate change makes disable rarely needed).
*Introduced by*: 260722-fc3b-ssh-host-fallback-open-logos

### Open-row icons are monochrome `currentColor` inline SVGs, resolved id-then-kind
**Decision**: Open menu rows lead with monochrome `currentColor` inline SVG glyphs (~14px, `aria-hidden`, `data-icon` test seam), resolved by an id map (`vscode`+`code` → VS Code glyph, `cursor`, `windsurf`) then a kind-based generic fallback (editor/terminal/file-manager), then a neutral glyph. Palette rows stay text-only.
**Why**: full-color brand marks would clash with the terminal aesthetic, and `currentColor` lets the existing accent-green hover treatments apply to the glyph for free with no new dependency and no image fetches. The kind fallback keeps unknown/future registry entries rendering; both `vscode` (deeplink id) and `code` (wt's VS Code host id) must map to the same glyph because wt's registry id for VS Code is `code`, not `vscode`.
**Rejected**: full-color brand assets (aesthetic clash, licensing/asset pipeline); asserting raw SVG path data (brittle) or per-glyph `aria-label` (icons are decorative — `aria-hidden` keeps row accessible names clean); adding icons to the command palette (no icon affordance today, separate change).
*Introduced by*: 260722-fc3b-ssh-host-fallback-open-logos

### Mirrored menu rows lead with the in-bar glyph; toggle state stays on the trailing ✓
**Decision**: Every chevron-menu / SplitControl-popover row that mirrors an in-bar icon control leads with that control's glyph as its first child, and the glyph is extracted into a shared component so bar and menu render ONE definition each (`top-bar-icons.tsx`). On the two stateful toggle rows the leading icon is a **static identity variant** that never flips with state; the trailing ✓ remains the sole state marker. `ViewSwitcherMenuRows` is excluded, and the command palette stays text-only.
**Why**: the registry already guaranteed bar↔menu *behavior* can never drift (one ordered source drives both renderings), but the menu mixed icon-bearing Open rows with text-only rows — so users matched rows by reading, never learned the icon↔action mapping, and each new `menuOnly` demotion widened the gap. Sharing one component makes visual parity structural rather than copy-paste, and dedupes the direction-glyph SVGs that `SplitControl` alone inlined twice. Leading-icon-as-identity plus trailing-✓-as-state is the macOS menu convention; a leading icon that flipped with state would make the row carry two competing state signals and defeat the identity read. The View switcher is excluded because its in-bar form is a text pill — there is no glyph to mirror.
**Rejected**: icons on action rows only, toggles left text-only (the mixed look the change exists to remove — rejected in favor of the mock-approved "toggles too" variant); a state-flipping leading icon on toggle rows (duplicates the ✓ and breaks the identity read); iconifying the View-switcher rows (nothing to mirror); inventing new glyphs for menu rows instead of mirroring (the bar↔menu mapping is the entire point).
*Introduced by*: 260801-3q1z-top-bar-menu-leading-icons

### A dedicated `top-bar-icons.tsx`, and per-glyph exact SVG attributes over one uniform wrapper
**Decision**: Host the shared control glyphs in a new sibling module `top-bar-icons.tsx` (not in `open-app-icons.tsx`, not in `top-bar-overflow-menu.tsx`), and give it a local `ControlGlyph` wrapper parameterized by `viewBox`/`strokeWidth`/`join` so each glyph byte-preserves its in-bar original's SVG attributes. Stateful toggles take a variant prop defaulting to the static identity form. The terminal-font glyph is an `aria-hidden` text span, not an SVG.
**Why**: `open-app-icons.tsx` is Open-target-specific (its API takes an `OpenTarget`) and `top-bar-overflow-menu.tsx` hosts row classes and sizing tokens, not glyph definitions — a dedicated module mirrors the `open-app-icons.tsx` shape and imports into `top-bar.tsx` with no cycle. The originals genuinely differ (24-viewBox/strokeWidth-2 for split/close/refresh; 14-viewBox/strokeWidth-1.5 round-caps-only for fixed-width; 14-viewBox/strokeWidth-1.5 caps+joins for autofit), so normalizing them onto one wrapper would visibly change stroke weight in the bar — the refactor's whole premise is zero visual change there. A defaulted variant prop is the minimal seam that lets one definition serve both the state-driven bar form and the static menu form. "Aa" is a text glyph in the bar, so a span is the direct match and keeps `currentColor` behavior.
**Rejected**: extending `open-app-icons.tsx` (couples control glyphs to the OpenTarget resolution API); inlining into `top-bar-overflow-menu.tsx` (mixes glyph definitions into the menu-shell component); reusing that module's fixed 24-viewBox/1.8-stroke `Glyph` (changes every glyph's weight); two separate components per stateful toggle (duplicates the definition the change exists to unify); rendering "Aa" as an SVG (needlessly diverges from the in-bar trigger's text styling).
*Introduced by*: 260801-3q1z-top-bar-menu-leading-icons

### `Open: PR #{n}` is a client-side palette action, not an Open target
**Decision**: The PR palette entry is a standalone `openActions` member built by `buildOpenPrAction` — `window.open` in the viewer's own browser, gated on `currentWindow.prUrl`, terminal route only. It is NOT an `OpenTarget`, so it never reaches `buildOpenTargets`, the top-bar Open split-button, or `OpenMenuRows`, and it registers no keyboard chord.
**Why**: `prUrl` is a public GitHub URL already on the client (derived server-side per constitution X), so a client-side open behaves identically for local and remote viewers with no backend surface at all. Keeping it out of `OpenTarget` preserves the documented palette↔menu mirror, which is a mirror of Open *targets* (folder-in-an-app), a different thing from "open this window's PR". The palette entry alone satisfies §V — the sidebar `PrLinkRow` anchor is pointer-only and stays as the mouse path.
**Rejected**: a host-side open via `POST /api/open`/exec (opens a browser on the host where nobody may be looking, fails headless, adds an exec surface for zero benefit); an `OpenTarget` entry plus a top-bar menu row (breaks the target mirror and puts a per-window action in server-wide chrome); a dedicated keyboard chord (the `Open:` family registers none — palette reachability is the §V path); board/tmux-Server-page variants (no unambiguous current window there).
*Introduced by*: 260727-w2d8-palette-open-pr

### Settings dialog mounts at `AppLayout`, not `AppShell`
**Decision**: The settings dialog + its `SettingsDialogContext` mount once at `AppLayout` (the persistent every-page layer), not `AppShell`. Instance display-name delivery lives in a separate root-mounted `InstanceNameProvider` (in `RootWrapper` beside `InstanceAccentProvider`), not in `SettingsDialogContext`.
**Why**: `AppShell` is server-scoped (assumes non-null `currentServer` throughout `app.tsx`), so mounting there would leave the dialog unavailable on `/board/$name`, which renders inside `AppLayout` without `AppShell` — and board coverage is the whole point of the every-page mount. Three display surfaces plus the dialog editor need one live-updating `{hostname, instanceName}` state — the exact fetch-once + optimistic-set + multi-surface-repaint shape `InstanceAccentProvider` already established; keeping it out of the chrome-only `SettingsDialogContext` avoids mixing chrome state with instance data and keeps the provider above the `RootTopBar` consumers anyway.
**Rejected**: mounting at `AppShell` (dead on boards); a module-level name cache (no reactivity path for the dialog's live edit); putting the name pair in `SettingsDialogContext` (mixes chrome and instance data).
*Introduced by*: 260723-o7q8-settings-dialog

### App-global actions live in the top bar; the sidebar footer is a passive status row
**Decision**: Settings is a persistent top-bar right-cluster chip (the L3-tail fit candidate, immediately before the exempt chevron; the right-rail toggle stays outermost); Help, Keyboard shortcuts, and Theme… are `menuOnly` App-section rows in the chevron menu, above the fixed version row; notifications live in the settings dialog. The sidebar footer keeps only passive readouts — the connection dot and the resting version line — plus a right-aligned, truncating, non-interactive status slot whose only content so far is the accent-green update-available hint. Theme switching has no click-to-cycle surface: the "Theme…" row opens the theme selector. The accepted consequence stands: any state that unmounts the sidebar — the Host route `/`, a collapsed desktop sidebar (`Cmd+\`), a closed mobile drawer — shows no connection indicator, with no mode-gated fallback dot.
**Why**: the top bar is the one chrome surface mounted on EVERY route, so app-global actions parked there stay reachable on the Host page, on mobile, and with the sidebar collapsed — the footer's action cluster vanished exactly when the sidebar did. One home (not split across rail + menu) avoids "where did settings go?" hunting; Settings, the highest-frequency of the four, keeps a one-click chip while the other three tolerate a second click, and all four stay palette-reachable via their existing entries (Constitution V). The freed footer row gains room for one-line status (the update hint), and losing the dot on sidebar-less surfaces remains cheap and reversible — those surfaces have the least live per-page data to report on.
**Rejected**: keeping the four actions in the footer — whether as borderless glyphs (the pre-`260811-cj4b` treatment) or as bordered footer-scale chips (`260811-cj4b`), both spent the sidebar's only always-visible bottom row on low-frequency actions that hid with the sidebar; parking Settings/Keyboard at the bottom of the right rail (a per-window surface rendered only on desktop terminal routes) mixes app-global scope into page chrome and drops the actions on Host/server/board routes and mobile; a settings *page* (constitution §IV — no settings pages).
*Introduced by*: 260812-d1at-relocate-footer-actions-top-bar

### Connection state reaches the Sidebar as a prop, not through the top-bar slot
**Decision**: `isConnected` is a required `SidebarProps` field — AppShell passes its chat-aware `dotConnected`, BoardPage its `boardConnected`. Neither `TopBarSlot` nor `TopBarProps` carries an `isConnected` field, and the Host page registers no slot at all (connection state was its only payload).
**Why**: explicit dataflow, unit-testable without mounting a slot provider and registering a fake slot, and it leaves no dead slot plumbing behind. The footer dot gets exactly the same per-page values (chat-aware on terminal routes, AND-over-servers on boards).
**Rejected**: the Sidebar reading `useTopBarSlot()` (keeps a dead-ish field alive, couples the sidebar to top-bar plumbing, and forces every sidebar test into a slot-provider harness).
*Introduced by*: 260724-6j1v-sidebar-footer-chrome-settings

### Shared chrome definitions live in `global-chrome.tsx`
**Decision**: `HELP_URL`, `NOTIFICATIONS_HELP_URL`, `HelpIcon`, and `ThemeModeIcon` live in `components/global-chrome.tsx`; `top-bar.tsx` exports none of them, and the overflow-menu App-section rows, the settings dialog, `app.tsx`, and `board-page.tsx` all import from that one module.
**Why**: single definitions are the no-drift requirement, and `top-bar.tsx` uses none of these symbols itself — re-exporting from it would be an orphan seam pointing at a file with no stake in them.
**Rejected**: re-exporting from `top-bar.tsx` (dead indirection); duplicating the SVGs in the menu rows (guaranteed drift, which is the whole thing the move prevents).
*Introduced by*: 260724-6j1v-sidebar-footer-chrome-settings

### Footer version is a readout; the overflow menu's version row stays the update surface
**Decision**: the footer's `v…` line is a passive readout with click-to-copy only, rendering nothing when `daemonVersion` is null. The overflow menu's fixed version row keeps both its copy form and its update/⟳-check forms.
**Why**: the footer's job is resting identity ("what am I running") at a glance; the menu row is the *unified update button's* resting form and owns the check affordance and the update flip (§ Steady-state version surfaces → surface 5). Duplicating the update surface would mean two places to keep the updating-state and toast behavior in sync. Rendering nothing on a null version avoids a dead copy target and a `vundefined` placeholder — the same degrade-or-omit rule every version surface follows.
**Rejected**: a `RunKit` text fallback in the footer when the version is unknown (surface 5's form, but here it would be a live-looking button with nothing to copy); moving the update surface into the footer (loses the menu's ⟳ + attention-badge wiring for no gain).
*Introduced by*: 260724-6j1v-sidebar-footer-chrome-settings

### The settings Notifications row explains an unsupported browser instead of hiding
**Decision**: on push `state === "unsupported"` the settings Notifications row stays present and renders a "Not supported in this browser" note with no action buttons.
**Why**: render-nothing is a bar-chip idiom (an inapplicable chip should not reserve space in a fixed cluster), but a settings pane is an inventory of what exists — a silently missing row reads as a bug, while a stated absence answers the question. Blast radius is one branch of one row.
**Rejected**: hiding the row (carries the chip idiom into a pane where it misleads); a disabled Enable button with no explanation (looks broken without saying why).
*Introduced by*: 260724-6j1v-sidebar-footer-chrome-settings

### Dialog theme controls are self-contained, not the `ThemeSelector` modal
**Decision**: The dialog's theme-pair surface is self-contained — a System/Light/Dark mode control plus two `<select>`s (preferred dark / preferred light) driving the existing `setTheme()` — rather than dispatching the `"theme-selector:open"` event to reuse the top-bar `ThemeSelector`.
**Why**: `ThemeSelector` is mounted only inside `AppShell` (`app.tsx`), so the event has no listener on `/board/$name` — the dialog's core every-page-mount promise. `setTheme(id)` already owns slot updates + the partial-merge POST + localStorage sync, so the dialog reuses that wiring (the intake's second-surface requirement) without the modal. The top-bar selector stays; the dialog is additive.
**Rejected**: moving the `ThemeSelector` mount to `AppLayout` (a behavior change to an unrelated surface, out of scope); live-preview machinery inside the dialog (the selector modal already owns preview UX).
*Introduced by*: 260723-o7q8-settings-dialog

### Edge-docked padding keeps the `max()` shape, but the bottom floor is raised on coarse pointers and gated by a JS keyboard signal
**Decision**: Edge-docked chrome keeps the `max(<floor>, env(safe-area-inset-<edge>))` expression under `viewport-fit=cover`, but the bottom bar's floor is a `--bottom-bar-floor` custom property raised from 6px to 1rem under `@media (pointer: coarse)` on `html:not(.kb-open)` — where `kb-open` is toggled on `<html>` by `useVisualViewport` when the on-screen keyboard opens.
**Why**: `env(safe-area-inset-bottom)` resolves to **0** in in-browser iOS Safari for this `position: fixed`, non-scrollable app (device-confirmed 2026-08-05), so the `max()` arm alone leaves the chips under the corner arc — the clearance has to come from the floor. A floor is static, so keyboard-collapse behavior has to be observed explicitly: iOS support for `interactive-widget=resizes-content` is unreliable, and `env()` carries no keyboard signal here (it reads 0 in both states). Keeping `max()` costs nothing and lets a genuine standalone-PWA inset still win. The JS half stays minimal — one class toggle inside the hook's existing rAF-coalesced pass, no new listeners; all padding math stays in CSS.
**Rejected**: a static coarse floor with no keyboard gate (wastes ~10px above the keyboard on an already-short viewport); treating `env()` collapse or the layout-viewport resize as the keyboard signal (untrue on device — this is the premise the earlier CSS-only design rested on); JS-probing `env()` and substituting a measured value (more machinery, still needs a hardcoded magnitude when the probe reads 0); a PWA manifest / standalone route to make `env()` report real insets (changes the install story to fix a browser-tab bug); a fixed 34px pad (wrong on non-notched phones).
*Introduced by*: 260805-fi9m-bottom-bar-keyboard-aware-safe-floor

### Keyboard-open detection is a width-keyed max-height baseline, not an absolute reference height
**Decision**: `useVisualViewport` treats the un-keyboarded baseline as the maximum `visualViewport.height` observed since the last `visualViewport.width` change, and calls the keyboard open when `height < baseline − 150px` (`KEYBOARD_DELTA_PX`).
**Why**: An on-screen keyboard changes height but never width, while rotations and window/split-screen resizes change width — so a width change is a reliable baseline-reset trigger, and a max-height baseline self-heals after any transient shrink. 150px clears iOS URL-bar chrome deltas (~50–114px) and undercuts any real keyboard (~260px+). The two residual gaps are accepted because both fail toward the pre-change 6px floor rather than a wrong pad: rotating or resizing *while* the keyboard is open re-baselines to the keyboarded height (clearing `kb-open` until the next close/open cycle), and a mount with the keyboard already up reads as closed until it first closes.
**Rejected**: comparing against `window.innerHeight` (browsers honoring `interactive-widget=resizes-content` shrink the layout viewport too, erasing the delta); `screen.height` (orientation semantics differ per platform); a dedicated keyboard-detection hook or extra listener (the existing rAF-coalesced pass already sees every geometry change). A desktop height-only window resize can false-positive, which is harmless — the only consumer is `@media (pointer: coarse)`-scoped.
*Introduced by*: 260805-fi9m-bottom-bar-keyboard-aware-safe-floor

### Shell safe-area gate ships without pinning the native paint mechanism
**Decision**: The top bar's `pt-[env(safe-area-inset-top)]` is gated off with `isShell()` inside the desktop shell without first pinning the exact Electron-native mechanism behind the v3.12.12 band-over-toolbar symptom (regression boundary: PR #487, the accent titlebar strip).
**Why**: Whichever agent paints the band, the SPA reserving the titlebar band twice — `ShellTitlebarStrip` plus safe-area padding — is a defect on its own, so removing the second reservation is correct and sufficient regardless of the rendering internals. Coverage is the unit test driving `isShell()` through its real seam (`window.runkitShell` injection/deletion) plus a manual in-shell check post-release — Playwright cannot cover the shell (`isShell()` is false in e2e); if the band survives a release build, the native-paint investigation reopens as a separate change.
**Rejected**: blocking the fix on a DevTools probe from the affected shell window (the double reservation is a defect independent of the probe's result); `vi.mock("@/lib/shell")` in the test (the bridge-injection pattern exercises the real narrowing seam).
*Introduced by*: 260805-9hn1-shell-safe-area-inset-fix

### One stroke-SVG icon system in sidebar rows, sized by ink not by position
**Decision**: Sidebar row action glyphs are all `sidebar/icons.tsx` stroke SVGs on one idiom (24-unit viewBox, `strokeWidth={2}`, 13px) — `PlusIcon` and `CloseIcon` replace the former text `+`/`✕` — and `BotIcon` is optically centered by a `viewBox` y-shift rather than a wrapper transform.
**Why**: The reported "icons aren't equidistant" was optical, not geometric: measured center-to-center gaps were already even to within 0.7px, but a 16px text glyph beside a 13px stroke SVG differs in ink width and weight, which reads as uneven whitespace. Equalizing the ink is the root-cause fix; nudging positions would have chased the symptom. Shifting `BotIcon`'s viewBox keeps the rendered box exactly 13px, which a `translate-y` wrapper class would not guarantee across the cluster's flex centering.
**Rejected**: nudging gaps/padding per button (treats the symptom); resizing the text glyphs down to 13px (still two ink systems); a `translate-y-[0.5px]` wrapper on the bot button (changes the box, not the drawing).
*Introduced by*: 260724-2bmy-mobile-chrome-polish

### Compose textarea owns its own row at a 2-line default
**Decision**: The compose strip stacks — textarea alone on row 1 at `w-full` and `rows={2}`, buttons on row 2 — with the 2-line default applying on desktop as well as touch.
**Why**: Sharing one row left the input ~52% of a 375px strip while typing space is the surface's entire purpose; a stack buys the full width at the cost of one button-row height, which the strip can afford as docked chrome. The 2-row default is the user's explicit call, and it also removes the desktop oddity where a 1-line textarea rendered 2px *shorter* than the buttons beside it.
**Rejected**: keeping one row and shrinking the buttons (recovers little width, hurts touch targets); a coarse-only two-row layout (divergent desktop/mobile DOM for no benefit — the desktop 1-line default was itself the complaint).
*Introduced by*: 260724-2bmy-mobile-chrome-polish

### Panel toggle joins the bordered-chip family instead of staying borderless
**Decision**: The top-bar hamburger is a bordered chip (`rounded border border-border hover:border-text-secondary`) with a 16px icon, and the brand crumb carries an explicit `min-h` on the same height axis as the button controls.
**Why**: The toggle already shared its neighbors' box, but reading as a *different control family* — borderless with a 20px glyph beside bordered chips with 13–14px glyphs — is what made the row look misaligned; matching the established sibling convention (HistoryNav / `LINK_CRUMB_CLASS`) fixes the perception with one class list, and the border finally gives `rk-glint`'s border-flip something to flip. The brand `min-h` is the mechanical half: the anchor measured 4px shorter than the buttons beside it.
**Rejected**: keeping it borderless and only shrinking the icon (leaves two control families in one cluster); putting the `min-h` inside `LINK_CRUMB_CLASS` (inert on the inline server crumb, which is not a flex box).
*Introduced by*: 260724-2bmy-mobile-chrome-polish

### Terminal link-opening is the SPA's job, via the shared `window.open` idiom
**Decision**: `WebLinksAddon` carries an explicit activation handler calling `window.open(uri, "_blank", "noopener,noreferrer")`; the addon's default handler is not used.
**Why**: The default opens a URL-less blank window and then assigns `location.href`, so an embedding shell only ever sees `about:blank` — the URL is gone before any host-side policy can act on it. No amount of Electron-side work can recover it, which makes this a frontend fix by necessity rather than by preference. Routing through `window.open` also puts terminal links on the same three-arg external-open idiom as every other outbound link in the SPA, so the shell needs exactly one window-open policy rather than a per-affordance special case.
**Rejected**: Fixing it in the desktop shell (impossible — `about:blank` carries no URL); a custom `linkHandler` on the `Terminal` options instead of the addon handler (reimplements the addon's URL detection for the same outcome); a Playwright e2e for the click path (xterm renders links on a canvas, so link-region clicks are not reliably automatable).
*Introduced by*: 260730-e9lz-shell-terminal-links-external

### The action tier's LETTERS are uniform; its modifier is per platform and per host
**Decision**: The canonical letters (N/T/W/H/L/A + [/]//) are the same on every platform, but the modifier is not: Windows/Linux uses `Shift+Ctrl` throughout, while macOS demotes to the unshifted ⌘ tier wherever nothing immovable blocks it — [/]// in both mac hosts, N/T/W inside the desktop shell only, with H/L/A staying shifted everywhere. The refinement is declarative data on each binding (`macTier`/`macShellOnly`) resolved once in `defaultComboFor`.
**Why**: Letter consistency is what must not fragment; chord weight is negotiable per environment, and the constraints that force Shift were never symmetric. Plain Ctrl must stay with the pane on Windows/Linux (Ctrl+W delete-word, Ctrl+L clear, Ctrl+T fzf, Ctrl+N/P history) — a macOS-only constraint-free tier gets taxed for nothing by uniformity, and the desktop shell exists precisely to hand the SPA the unshifted ⌘ tier, so a uniformly-shifted tier left that premise unspent. What blocks a demotion is genuinely immovable, not aesthetic: ⌘H is macOS Hide and ⌘A is select-all / the shell's Edit role, and demoting L alone would split the H/L pair across two tiers — the same fragmentation the letter rule exists to prevent. Because the resolver already funnels every consumer through one effective map, expressing the tier as per-key data costs no architecture.
**Rejected**: A uniform shifted tier on every platform (taxes mac users a three-key chord the environment would give them in two, and leaves the shell's whole reason for existing unused); demoting H/L/A too (⌘H/⌘A are dead in both mac hosts — the chord would silently do nothing); demoting N/T/W on mac *browsers* as well (browser-reserved unshifted AND shifted, so they stay palette-only there); per-platform `DEFAULT_BINDINGS` arrays or a resolver-level actionId→tier table (duplicates every binding, or splits the per-key truth across two structures).
*Introduced by*: 260730-n789-macos-cmd-tier-shortcuts

### The tables are the chord authority; the key map is a discovery and rebind tool
**Decision**: The overlay renders ONE keycap grid with a "Holding ⇧⌘ | ⌘" modifier picker, and the browser-vs-desktop divergence lives on the affected table ROWS (a `desktop` badge + the other host's chord) rather than in a second grid or a per-map caption.
**Why**: Two stacked grids named the registry's internal tier taxonomy in user-facing chrome and re-answered per-action chords the rows already carry, at double the vertical mass — which pushed the authoritative tables and the TMUX section below the fold on short viewports. Giving each surface one job removes the duplication instead of restyling it: the map answers the physical question ("hold this — here's what my keyboard does", the model Karabiner-style keyboard viewers use), and the rows carry the per-action truth including where it differs by host. The divergence is genuinely per-binding — exactly the `macTier`+`macShellOnly` trio — so a row is where the fact belongs.
**Rejected**: Keeping both grids with de-jargonized headers (still double the vertical mass, still duplicates the tables); labelling the whole ⌘ layer desktop-only (factually wrong — ⌘K/⌘./⌘[/⌘]//⌘/ all work in a browser tab; only the trio differs); a registry change to carry the new presentation (unnecessary — `macTier`/`macShellOnly` already ride onto `EffectiveBinding`, and `defaultComboFor` already computes the other-host chord).
*Introduced by*: 260801-r8j2-shortcuts-map-modifier-picker

### The picker IS the map label, and the inactive layer falls back by derivation
**Decision**: The map header's left slot is "Holding" plus the segmented control itself (the selected segment supplies the modifier caps), with a static "Holding **Shift Ctrl**" on the Win·Linux display; the rendered layer is derived (`displayPlatform === "mac" ? mapTier : "shifted"`) rather than reset when the display toggles.
**Why**: The picker and the layer label are the same header element — rendering them separately would duplicate the modifier caps and widen the header row, and a one-option segmented control on Win·Linux is dead chrome. Deriving the fallback matches "the option disappears" rather than "the selection is destroyed": a user flipping the display to Win·Linux and back gets their ⌘ selection returned, consistent with the session-scoped view state the fold already establishes.
**Rejected**: A separate "Holding ⇧⌘" title plus a detached picker (duplicates the caps); resetting `mapTier` on display change (loses the selection on a round-trip for no benefit); persisting the selection to localStorage (a reading preference within one sheet, not a device setting).
*Introduced by*: 260801-r8j2-shortcuts-map-modifier-picker

### Chord handlers are the palette action bodies, looked up by id
**Decision**: The AppShell dispatcher resolves `create-session`, `create-window`, `kill-window`, `agent-next-waiting`, `go-back`, `go-forward`, and `settings-open` by finding the palette action with the matching id — over the MERGED route+globals list (`fromPalette` over `[...routeActions, ...usePaletteGlobals()]`, § The palette slot splits state from a stable API channel) — and reusing its `onSelect`.
**Why**: `actionId` doubles as the palette id by design, so the lookup is free — and reuse makes chord/palette drift impossible, including gating: a palette entry that does not exist (e.g. `kill-window` with no active session) yields no handler, and the dispatcher's no-handler fall-through is exactly the desired no-op. Resolving over the merged list keeps chords working when an id's ownership moves between a route group and the layout-level globals.
**Rejected**: A parallel handler implementation per chord — duplicates every gate and drifts from the palette behavior it must mirror.
*Introduced by*: 260730-g40a-keyboard-shortcut-registry-overlay

### Enablement-bearing chords keep their own listeners; the registry owns definition and suppression
**Decision**: ⌘K, ⌘\, ⌘., and `` Ctrl+` `` dispatch from component/hook-local listeners that read their combo from the effective map and gate on the shared suppression predicate; only the board pane-cycle runs through `useKeybindingDispatch`.
**Why**: Centralizing *definition* and *suppression* is the registry's charter; centralizing *enablement* is not — palette open-state, chat capability, and lens availability are local state, and routing them through one dispatcher would need context plumbing for zero user-visible gain.
**Rejected**: One dispatcher for every chord (high regression risk, new context plumbing); leaving these four out of the registry entirely (they would be unrebindable and invisible to the overlay).
*Introduced by*: 260730-g40a-keyboard-shortcut-registry-overlay

### The terminal seam refuses shifted chords everywhere and cmd chords only on mac, metaKey-gated
**Decision**: `shouldRefuseTerminalChord` returns `true` for an enabled shifted-tier match on any platform, plus an enabled cmd-tier match on macOS **only when `event.metaKey` is set**; everything else returns `true` from the handler (reaches the pane), after the ⌘C copy case.
**Why**: Refusal must be exactly as wide as the set of chords the pane cannot use. Legacy TTY encoding cannot distinguish Ctrl+Shift+letter from Ctrl+letter, so the shifted tier costs the pane nothing anywhere; ⌘ chords never reach a pane as control bytes, so refusing them on mac is loss-free and is what lets the demoted ⌘[/⌘]/⌘/ fire under terminal focus. The `metaKey` gate is the load-bearing half: `matchesCombo`'s cmd tier accepts Meta **or** Ctrl, so an ungated refusal would steal Ctrl+[ (ESC) from mac panes — the same hazard that forbids cmd refusal on Windows/Linux, where CmdOrCtrl *is* the pane's own Ctrl. Gating on the platform + `metaKey` therefore keeps the Win/Linux seam byte-identical rather than making it a special case.
**Rejected**: Refusing every registry match (breaks plain-Ctrl pane chords everywhere); refusing all cmd-tier matches on mac ungated (breaks Ctrl+[ = ESC); narrowing `matchesCombo`'s cmd tier to Meta-only on mac (would change the legacy ⌘K/⌘\/⌘. predicates project-wide); adding a `preventDefault` in the handler (xterm's non-preventing refusal is what lets the event bubble to the window dispatcher). Residual: on Windows/Linux an action deliberately rebound *into* the cmd/ctrl tier is not refused and reaches the pane while the terminal has focus.
*Introduced by*: 260730-n789-macos-cmd-tier-shortcuts

### Overrides are localStorage diffs with single-press steal-and-warn
**Decision**: Only differences from the defaults are persisted (`{ [actionId]: {code,tier} | null }`), and one capture press hands the combo over, unbinding the previous owner and flagging it.
**Why**: A diff blob keeps the shipped defaults authoritative — adding or renaming a default binding needs no migration, and re-capturing an action's own **host** default drops its entry rather than freezing a redundant one. Single-press steal keeps rebinding to one gesture and is honest about the consequence, since the victim's `unbound` state is visible in the overlay until it is rebound or reset. Storing only diffs also makes the per-platform tiers free: overrides are per-device by construction, so a device's platform is implicit and no per-platform storage key or migration exists. localStorage matches Constitution II (no database) and the `runkit-terminal-font-size` precedent.
**Rejected**: Persisting the full effective map (defaults freeze into user storage, and the per-platform default would be baked in); a press-again-to-confirm steal (two gestures for one intent, and the warning already carries the information); a backend-stored per-user map (a new persistence surface for a per-device preference); a per-platform override shape (per-device storage already makes per-platform inherent).
*Introduced by*: 260730-g40a-keyboard-shortcut-registry-overlay

### A same-combo global↔scoped pair is precedence for conflicts but still a steal target for capture
**Decision**: `findMatches` orders matches non-global-first and the dispatcher fires the first one with a handler; `findConflicts` flags only **equal**-scope pairs; `applyCapture`'s victim search stays `scopesOverlap`-wide, and its own-host-default check short-circuits **before** that search.
**Why**: Once dispatch is scoped-beats-global, a global↔scoped shadow leaves both bindings fully functional (the scoped one wins on its route, the global one everywhere else), so reporting it as a defaults-integrity conflict would make the shipped mac map permanently "dirty" — the board ⌘[/⌘] pair shadowing back/forward is intended, not a defect. Capture stealing cannot be narrowed the same way: four chords dispatch through component-local listeners that never see `findMatches` precedence, so a cross-scope rebind onto their combos would genuinely double-fire, and steal-with-warning is the visible, reversible guard. The short-circuit ordering follows from the same shape — the mac defaults ship shadow pairs, so a victim search running first would let a *no-op* re-capture of one partner unbind the other.
**Rejected**: Treating the shadow as a conflict (would flag the shipped mac defaults); narrowing `applyCapture` to equal scopes too (silently allows double-fire on the locally-listened chords); a schema flag marking locally-listened bindings (scope creep for one edge); resolving the shared combo by moving the board pane-cycle off ⌘[/⌘] (breaks an established board chord to accommodate a global default).
*Introduced by*: 260730-n789-macos-cmd-tier-shortcuts

### Macro targets are riff presets and palette action ids, never shell strings
**Decision**: `MacroTarget` is a two-arm union — a riff **preset name** or a **palette action id**. Execution reaches process spawn only through the existing `POST /api/riff`, which resolves the name as a map-key lookup against `fab/project/config.yaml`-defined presets; an unknown name is a `ValidationErr` → 400.
**Why**: A shortcut executing user-typed shell text would make a web-reachable endpoint run arbitrary commands — precisely what Constitution I exists to prevent. Both motivating flows ("spawn a worktree with a fab-discuss agent", "spawn a codex loop") are already expressible as riff presets, so the macro tier adds **zero** new process-execution surface: it is a keyboard route to an already-validated spawn, and a localStorage-planted preset name can never reach argv. Arguments belong in the preset definition, inside the committed-config trust boundary.
**Rejected**: An arbitrary command string on the macro (Constitution I); an `args?: string[]` passthrough on the riff target (`POST /api/riff` has no args seam and its `task` replaces preset panes, so it needs a backend extension the "no new exec surface" premise rules out — additive to the union later if that seam appears); a preset-authoring UI in the web app (Constitution IV — presets already have a home and an authoring path in fabconfig).
*Introduced by*: 260730-hbyh-shortcut-macro-riff-bindings

### Macro combos live in the keybindings diff map, not the macro definition
**Decision**: A macro definition carries no combo; its binding is an ordinary `runkit-keybindings` override entry keyed by the macro's actionId, and a macro without an entry resolves unbound (`disabledReason: "user"`).
**Why**: One binding store and one resolution path means steal-with-warning, per-row reset, reset-all, browser-reserved disabling, palette hints, the overlay key map, and the terminal seam all work on macros with the existing machinery — no parallel plumbing, and reusing the steal-victim state gives the overlay's `unbound` affordance for free.
**Rejected**: Storing the combo inside the MacroAction — a second combo store that `applyCapture`/steal/reset could not see without duplicated wiring.
*Introduced by*: 260730-hbyh-shortcut-macro-riff-bindings

### Keyless defaults (`code: ""`) instead of a macro-specific resolver
**Decision**: `resolveBindings` carries one extra rule — an effective combo with an empty `code` resolves unbound — and macros enter resolution as ordinary defaults via `macroToBinding`.
**Why**: One resolver keeps every consumer (dispatch, hints, overlay, terminal seam, capture) macro-agnostic; builtins always carry a code, so they are untouched by the rule.
**Rejected**: A parallel `resolveMacroBindings` merged after the fact — duplicates the override/reserved/steal logic and forks the effective-map contract.
*Introduced by*: 260730-hbyh-shortcut-macro-riff-bindings

### The mac split pair rides a `macCode` default refinement rather than a platform-gated unbound vertical
**Decision**: `macCode?: string` refines a binding's SHIPPED DEFAULT key code on mac hosts, composing with `macTier` inside the one existing mac branch of `defaultComboFor` (`{ code: macCode ?? code, tier: macTier ?? tier }`, under the same `macShellOnly` gate). The split rows ship divider-mnemonic base codes — ⇧Ctrl+\ side-by-side (shift+\ types `|`, the divider the split creates), ⇧Ctrl+- stacked — with `macCode: "KeyD"` refining both to the iTerm2/Warp/Ghostty ⌘D/⇧⌘D pair on mac. Both actions are bound-by-default on every host.
**Why**: The mac pair is unrepresentable with constant codes — both rows on `KeyD` collide on the Win/Linux shifted tier (the test-enforced conflict-free invariant) and plain Ctrl+D is the pane's EOF — so each platform gets its own coherent pair instead of one platform getting half of one. `defaultComboFor` is already the single seam both `resolveBindings` (fallback + `isDefault`) and `applyCapture` (own-default detection) read defaults through, so conflict detection, overlay keycaps, and own-default re-capture handle the refined code with no new branch. The cost is one deliberate, documented exception to 260730-n789's letters-constant rule.
**Rejected**: Shipping `split-vertical` mac-only via a `platform` default gate that resolves keyless off-platform (the first-cut design this replaced in-PR: it left a Win/Linux action unbound-by-default behind an unexplained amber `unbound` row, and forced capture-suite tests off ⇧Ctrl+D); a second constant letter for vertical (breaks the mac ⌘D/⇧⌘D muscle-memory pair the chords exist for); leaving vertical on a colliding ⇧Ctrl+D (the conflict invariant forbids it); claiming plain Ctrl+D (takes EOF from the pane).
*Introduced by*: 260807-rbx5-split-keyboard-shortcuts

### An unrepresentable shell claim is recorded as absence plus the system claim that replaces it
**Decision**: The mac Hosts-switcher claim is expressed by *narrowing* `SHELL_SWITCHER_DIGITS` to `platform: "other"` and adding mac `owner: "system"` ⇧⌘3/4/5 "screenshot" rows — no Option tier is added to `BindingTier`, and the freed mac ⇧⌘1/2/6–9 carry no rows at all. The overlay's decorative digit-run ellipsis cell keys its claimed styling off `Digit3`.
**Why**: The mac accelerator lives on ⌥⌘, which the registry cannot express because Alt is excluded from every tier — and that exclusion is precisely why ⌥⌘ was chosen ([desktop-shell](/run-kit/desktop-shell.md) § Design Decisions → The mac shell tier is ⌥⌘). Adding a tier to represent the claim would undo the property that makes the key safe. What the key map still owes the user is honesty about what a mac ⇧⌘ digit press actually does, which the system screenshot rows supply: shell and system claims never disable a binding (only `browser` claims reserve), so this is pure display data and `resolveBindings` behavior is untouched — a capture onto mac ⇧⌘3/4/5 now warns "claimed by system (screenshot)" instead of "claimed by shell". `Digit3` is the ellipsis key because it is the only digit in the run claimed on **both** displays, so mid-run claims stay visible where `Digit1` would render the mac trio invisible.
**Rejected**: A fourth `BindingTier` for Option-carrying chords (destroys the un-claimable property the accelerator choice rests on, and `captureFromEvent` rejects Alt chords by design); leaving the mac shifted digits claim-free (the key map would show nine free keys where three take screenshots); claiming the freed ⇧⌘1/2/6–9 defensively (they genuinely are page real estate — a claim there would lock keys nothing owns).
*Introduced by*: 260731-nv5r-mac-host-switcher-alt-cmd

### Shared top-bar button token is hosted beside `MENU_ROW_*`
**Decision**: The `TOP_BAR_BUTTON*` / `TOP_BAR_BUTTON_H` / `TOP_BAR_SEGMENT_H` constants live in `top-bar-overflow-menu.tsx` next to `MENU_ROW_*`, and the square token is a FIXED size (`w-[28px] h-[28px] coarse:w-[30px] coarse:h-[30px]`), not a `min-*` floor.
**Why**: Every consumer — `top-bar.tsx`, `open-button.tsx`, `view-switcher.tsx`, the menu itself — already imports that file, so the same no-cycle host precedent `MENU_ROW_*` established applies with no new module. The fixed size addresses the root cause of visible size inconsistency in the cluster: a `min-*` floor lets each box stretch to its own content, so a bare-glyph control renders smaller than a padded sibling and the class string drifts further with every copy-paste. Fixing the size once, in a shared constant, makes that class of defect unrepresentable.
**Rejected**: A new `top-bar-button.ts` module (an extra file for a handful of string constants when an established host exists); keeping `min-*` floors and normalizing per callsite (treats the symptom — the drift returns with the next copy-paste).
*Introduced by*: 260731-oiho-navbar-consolidation-menu-density

### The split ▾ lists both directions; the primary stays a fixed direction
**Decision**: The merged `SplitControl`'s ▾ dropdown lists BOTH `Split horizontal` and `Split vertical`; the primary segment always performs one fixed direction, never last-used.
**Why**: Split-button convention (and the in-bar `OpenButton` precedent) puts the complete option set in the dropdown, so a one-row menu would hide half the control's capability behind the assumption that the user knows the primary's direction. A fixed primary keeps the primary click deterministic — the same guarantee the two per-direction buttons gave.
**Rejected**: A single-row dropdown carrying only the non-default direction (hides the complete option set); a last-used-tracking primary (makes the most-used affordance in the cluster non-deterministic).
*Introduced by*: 260731-oiho-navbar-consolidation-menu-density

### Horizontal is the default split direction, and one direction vocabulary spans every surface
**Decision**: The primary segment's fixed direction is HORIZONTAL (side-by-side, tmux `-h`), and horizontal leads wherever both directions appear — the ▾ menu, the overflow `SplitMenuRow` pair, and the command palette. Every surface names directions after tmux's own flags: horizontal = `horizontal: true` = `split-window -h` = beside; vertical = `horizontal: false` = `split-window -v` = stacked. The terminal route's split entries (`app.tsx`), the board's split entries (`board-page.tsx`), and the `/api/keybindings` whitelist labels are aligned to that vocabulary; the backend `horizontal` boolean contract is untouched.
**Why**: Side-by-side is the dominant working layout, and a split-button's default and its first menu row must agree — so the ordering rides with the default rather than moving on its own. The vocabulary is settled in the same decision because two naming schemes for one action let the same word mean opposite things: a chip labeled "Split horizontal" running `-h` beside a palette entry labeled `Split Horizontal` running `-v` means a user who learns the word from one surface gets the opposite split from the other. The chip's mapping is the internally consistent one — its glyph, its label, and tmux's flag name all agree — which makes it the convention to standardize on rather than the one to relabel.
**Rejected**: Reordering the menus while leaving the primary vertical (the default and the first row would disagree, breaking the split-button convention the control rests on); relabeling the chip to match the palettes (puts the label at odds with both its glyph and `split-window -h`); leaving the palette booleans inverted and documenting the divergence (a same-name-opposite-action bug is not documentable away); persisting a last-used direction (the primary is deliberately deterministic — § above).
*Introduced by*: 260806-2x2h-split-default-horizontal-first

### Menu sections are named on the registry entry, not derived in the menu
**Decision**: Each `RegistryEntry` carries `menuGroup: "view" | "window" | "app"` (surfaced as `group` on `OverflowMenuRow`); `TopBarOverflowMenu` partitions rows by group and renders non-empty sections in the fixed View → Window → App order.
**Why**: The partition preserves registry (pyramid) order *within* each section for free, so the `View:` switcher rows keep leading the menu with zero re-sorting, and a new control declares its section at the one place it is already declaring `modes`/`hidden`/`menuOnly`.
**Rejected**: Grouping inside the menu component by hardcoded id lists (silently drifts the moment the registry gains or renames an entry); sorting rows into groups by tier (tier encodes drop priority, not conceptual grouping — the two answer different questions).
*Introduced by*: 260731-oiho-navbar-consolidation-menu-density

### Demoted in-bar components stay intact rather than being deleted
**Decision**: `ClosePaneButton`, `FixedWidthToggle`, `TerminalFontControl`, and the `ViewSwitcher` pill remain in the codebase — reachable only through their registry entries' `barRender`, which `menuOnly` never calls — instead of being removed with their demotions.
**Why**: The revert contract is what makes a demotion cheap to try: reverting is deleting one flag, and that only holds while the bar form still compiles and renders. The four components are tracked as deletion candidates for a later change that confirms the demotions stick, so the cleanup is a deliberate follow-up rather than an omission.
**Rejected**: Deleting each component with its demotion (turns a one-flag revert into a re-implementation, and would lose the `TerminalFontControl` popover's `Reset terminal font` stepper — the menu row has `−`/`+` only — before its replacement is agreed).
*Introduced by*: 260731-oiho-navbar-consolidation-menu-density

### One shortcuts surface with one filter, not two dialogs or tabs
**Decision**: The registry overlay is the only shortcuts dialog. tmux keybindings are a read-only section inside it, and there are no tabs — a single filter and a single scroll span app rows, macros, and tmux rows, with a sticky jump-nav chip row (live per-section match counts while filtering) standing in for tab navigation.
**Why**: The user's query is "what's the key for X?", and answering it should not require first knowing whether X belongs to the app or to tmux. Two dialogs made the answer depend on that knowledge, and the second dialog's hardcoded App rows drifted stale the moment the registry gained tiers and per-device overrides — the registry surface is self-updating by construction. Tabs would have re-imposed the same "pick the right pane first" cost inside one dialog; the tmux set is small enough that one scroll stays readable, and the chip counts keep hits below the fold discoverable.
**Rejected**: Keeping both dialogs (the staleness and the which-one-do-I-open question are structural, not a copy problem); tabs inside the merged overlay (reintroduces the pre-selection cost the merge exists to remove); making tmux rows rebindable (tmux keys are pressed inside the pane and configured in `configs/tmux/default.conf` — run-kit only documents them).
*Introduced by*: 260801-sm6g-keyboard-shortcuts-consolidation

### The compose strip focuses on open, and only on open
**Decision**: The strip focuses its textarea on the off→on transition, via a module-level focus-on-open flag marked by `toggleComposeStrip` and consumed-and-cleared by the mount effect. After-send, Escape, and route remounts keep the no-steal rule.
**Why**: In practice every open was followed by a manual click into the textarea — opening the strip *is* the intent to type, so the blanket no-steal rule cost a gesture on the one transition where focus was unambiguous. Routing the flag through the single `toggleComposeStrip` seam covers every open path (chip, palette, chord, drag-drop, board twin) without per-affordance wiring, and keys the behavior on the TRANSITION rather than the mount — which is exactly what keeps a route remount with the strip already enabled from stealing focus. Consuming unconditionally (even when the effect declines in the "no target" state) is what stops a stale flag surfacing as a surprise focus grab later.
**Rejected**: Focusing on every mount (steals focus on terminal↔board remounts); a `focusOnOpen` prop threaded to both footer mounts (two wiring sites for one module-level fact, and the drag-drop auto-enable path bypasses them both); calling the existing `focusComposeStrip()` registry from the toggle (the strip is not mounted yet at toggle time — the flag exists precisely to bridge that gap).
*Introduced by*: 260801-sm6g-keyboard-shortcuts-consolidation

### A compose draft belongs to its send target, not to the URL and not to the user
**Decision**: Drafts are keyed by `entryKey(server, windowId)` — the strip's live send target — so each window owns its unsent text and attachments, and focus changes swap the visible draft rather than carrying one draft along.
**Why**: The strip's target is already resolved live at send time (§ Live target), so binding the draft to that same identity is what makes the `→ {window}` label truthful: what you see aimed at a window is what that window will receive. It also makes a cross-worktree attachment structurally impossible — the attachment and the worktree it was uploaded to are two halves of one keyed entry — which is what allows the entire re-home path to be deleted rather than hardened. Keying survives window renames, since `windowId` is tmux-stable, and the same draft appears whether the window is reached via its terminal route or a board pane.
**Rejected**: A single global draft carried across navigation — showing a draft composed for A while B is targeted is a wrong-pane-send footgun that the target label only partially mitigates, and it is the sole reason attachment re-homing had to exist. Keying by URL — equivalent to target keying on terminal routes, but on a board it pins one draft on screen while pane focus cycles underneath, recreating exactly the wrong-pane risk it was meant to remove.
*Introduced by*: 260801-cyth-per-target-persistent-compose-drafts

### Draft text is persisted synchronously; attachments are not persisted at all
**Decision**: Every commit writes the text map through to `localStorage` synchronously (no debounce), and `File` objects are never persisted — a hydrated draft carries text only.
**Why**: Drafts are a few kilobytes at most, so the I/O saved by a debounce is negligible next to what it costs — a flush-on-unload edge case and non-determinism in unit and e2e tests — and the synchronous write closes the lose-a-keystroke-on-instant-reload window that motivated persistence in the first place. Dropping attachments is not a compromise but the honest boundary of the storage: uploads are eager and their path lines live in the persisted text, so the only casualty of a refresh is the preview thumbnails, while the files themselves are already on the target worktree's disk and still send correctly.
**Rejected**: Debounced writes (see above). IndexedDB `File` persistence — real complexity and a second storage substrate to buy back thumbnails. `sessionStorage` — dies with the tab, which fails the browser-restart case the persistence exists for. A `storage`-event multi-tab sync — two tabs composing to the same window is rare enough that last-write-wins is the right trade.
*Introduced by*: 260801-cyth-per-target-persistent-compose-drafts

### One global listener set over per-key snapshots, and one prune pipeline over both persistence paths
**Decision**: The keyed store keeps a single listener set (any commit notifies every subscriber) with per-key stable snapshot objects, and both the write path and the hydrate path call the same `pruneDraftEntries()` helper.
**Why**: At most one strip is mounted at a time, so per-key subscription channels would add bookkeeping for zero benefit; what `useSyncExternalStore` actually needs is snapshot **identity** stability per key, which the snapshot cache provides. Sharing the prune helper matters because the two paths must agree by construction — a divergence between what is written and what is accepted back would let an entry the writer rejects survive a reload, or an entry the reader drops be silently rewritten. The age check is deliberately two-sided so a future-dated `updatedAt` cannot sort to the front and hold a cap slot forever.
**Rejected**: Per-key listener channels (complexity for a single-subscriber store). Separate filter/sort/slice pipelines at each site (the duplication that a review caught — two copies of a policy that must never disagree). A `__resetForTest` private export for the test seam — exporting the real `hydrateComposeDrafts()` instead means the tests reset through the same code path production uses, so a reset can never drift from a real hydration.
*Introduced by*: 260801-cyth-per-target-persistent-compose-drafts

### A chord may name a behavior even when it cannot name a target
**Decision**: `open-last-used` (⇧⌘O) is a builtin binding, while the per-target `Open: <label>` palette entries stay chord-less.
**Why**: The objection to chording Open was that targets are data-driven — the set varies per deployment, so no static chord can name one. "Re-run the last-used target" is a behavior, not an app, so it is nameable and stable across deployments; it also gives the split-button's primary segment the keyboard twin Constitution V wants. The empty case degrades to a toast rather than a popped menu, because a chord has no anchor to pop a mouse menu from.
**Rejected**: Per-target chords (unnameable, and they would collide differently on every deployment); leaving the primary segment keyboard-unreachable (the most-used Open path had no chord at all).
*Introduced by*: 260801-sm6g-keyboard-shortcuts-consolidation

### A tooltip keycap chip is resolved from the registry, never hard-coded
**Decision**: A `Tip`'s `kbd` slot for a registry-bound action is derived at the call site from the effective map (`useKeybindings()` → `byAction.get(id)` → `formatCombo(…, host.platform)` when `enabled`, else `undefined`), and the chip is **omitted** when the binding is unbound, disabled, or browser-reserved. Non-registry `kbd` strings survive only for chords the registry does not own (the compose/chat submit plus the per-surface Insert keys, `Enter` on the strip and `Alt+Enter` in chat — the submit chip still resolves per-platform through the shared `composeSubmitKeycap()` rather than a literal). The ⌘K chip's button FACE keeps its brand glyph regardless.
**Why**: A hard-coded chip is wrong the moment the platform or the user's override differs from the author's machine — a literal `"⌘K"` reads wrong on every Windows and Linux host and survives a rebind unchanged. Deriving it makes hover-discovery teach the chord that actually works (Constitution V), and omitting rather than dimming a dead chord follows the same rule the palette hints and the overlay's header chord apply: advertising a chord that does nothing is worse than advertising none. The derivation stays a 4-line snippet per site rather than a hook because `withShortcutHints` already covers the palette-array shape and a second abstraction for four scalar call sites would be the heavier option.
**Rejected**: A static string per call site for a registry-bound action (lies under rebinds and off-platform); rendering a dimmed/struck chip for a disabled binding (still advertises the chord, and the tip is one line with no room to explain why it is dead); a shared `useActionChord(id)` hook (four call sites and one shared derivation shape do not yet justify a hook — revisit if the count grows).
*Introduced by*: 260801-mqim-tooltip-shortcut-chips-settings-binding

### `settings-open` takes the shifted tier in browsers and ⌘, only in the shell
**Decision**: `settings-open` ships `code: "Comma"`, base `tier: "shifted"`, with `macTier: "cmd"` + `macShellOnly: true`, and ⌘, is added to `MAC_BROWSER_CMD_CLAIMS` as a browser-owned Preferences claim.
**Why**: ⌘, is the macOS convention for preferences, and the desktop shell binds no accelerator on it — so inside the shell the conventional chord is simply available. A mac *browser* owns ⌘, for its own Preferences, so the browser default has to sit one tier up; recording that as claims data (rather than only as a comment) is what makes a user override onto ⌘, resolve `reserved` in a browser instead of silently advertising a dead chord, while the identical combo stays the enabled default in the shell. Open-only semantics (not toggle) match every macOS app: ⌘, never closes preferences, and `openSettings` being `setIsOpen(true)` makes a re-fire a React state bailout for free.
**Rejected**: A single ⇧⌘, default everywhere (gives up the OS convention in the one host where it is free); an unshifted ⌘, default in browsers too (dead chord — the browser consumes it first); toggle semantics on re-fire (contradicts the macOS convention and the action's own `settings-open` / `Settings: Open` open-vocabulary naming); a `Comma` cell in the overlay's tier-map grids (the `Period`/`Backslash`/`Backquote` precedent leaves punctuation off the fixed grid).
*Introduced by*: 260801-mqim-tooltip-shortcut-chips-settings-binding

### Enter transmits a line in the strip, composes in chat — one classifier, deliberately divergent per surface
**Decision**: `classifyComposeEnter(key, surface)` takes a REQUIRED `surface: "strip" | "chat"` and returns the per-surface plain-Enter action: `"insert-line"` on the strip (transmit `text + "\n"`, clear the draft; `enterKeyHint="send"`) and `"default"` (local newline; `enterKeyHint="enter"`) in chat. Everything else is shared and identical on both surfaces — Shift+Enter = newline, Cmd/Ctrl+Enter = the only submit chord, Alt+Enter = byte-exact insert-without-submit, IME-composing Enter never intercepted. The strip additionally sends a bare `"\r"` on an empty/whitespace-only submit; chat's empty send stays a no-op. The classifier still reads no pointer input.
**Why**: The strip overlays the VISIBLE terminal, so a transmitted line lands in the pane's own composer where the user watches it appear — Claude Code treats a raw `"\n"` as newline-insert, so consecutive Enters stage sentence-per-line exactly like typing into the pane, and one Cmd/Ctrl+Enter (or an empty one, the bare `"\r"`) submits. That stage-then-submit loop is the terminal-faithful flow the surface exists to offer, and gating every staged line behind the obscure Alt+Enter chord — with no keyboard way to press Enter afterwards — made the strip a local drafting box instead. The chat lens cannot show the pane's input box, so the same Enter there would make typed text visibly vanish into an invisible target; its premature-send hazard is also the original one (a reflexive Enter firing a half-written prompt at a live agent). Divergence is therefore the correct answer, and putting it INSIDE the classifier — with `surface` required, no default — keeps one authority for both policies: a call site can choose which policy it gets but cannot invent a third, and a new surface must state its choice rather than silently inherit one. Pointer-independence survives because the divergence axis is surface visibility, not input hardware.
**Rejected**: Keeping one shared policy across both surfaces (sacrifices either the strip's terminal-faithfulness or chat's visibility safety — the two surfaces genuinely differ); a default value for `surface` (recreates exactly the silent drift the shared classifier exists to prevent); branching plain Enter at the call sites (the same drift, one layer down); a user preference toggle (a second policy path for a decision with one defensible default per surface); guarding the shell-pane case where the transmitted `"\n"` executes the line (that IS what Enter does in a terminal — terminal-conventional, documented in a code comment).
*Introduced by*: 260802-lj98-compose-enter-insert-line

### Intercept only the readline chords the platform leaves unbound
**Decision**: The shared readline layer claims exactly Ctrl+U, Ctrl+W, Alt+B, Alt+F, Alt+D — matched on `KeyboardEvent.code` with exact modifiers — and leaves every natively-bound Cocoa editing chord alone. Deletions route through `execCommand("delete")` with a controlled-component-safe fallback; an empty computed range consumes the chord without editing.
**Why**: macOS already implements Ctrl+A/E/B/F/P/N/D/H/K/T/O/Y correctly *and* wires them into the system kill buffer, so reimplementing any of them (Ctrl+K especially) would break its pairing with Ctrl+Y — the five claimed chords are precisely the muscle-memory gap. Matching on `code` is mandatory rather than stylistic: macOS composes `∫`/`ƒ`/`∂` into `key` for Alt+B/F/D, so a key-based matcher would miss the exact chords the layer exists for. `execCommand` is deprecated but the only programmatic edit path the browser records on the native undo stack, and Cmd+Z after a Ctrl+W is table stakes for an editing chord. The empty-range guard is a real corruption fix, not defensiveness — `execCommand("delete")` on a collapsed selection deletes a character backward, so Ctrl+U at line start would silently eat text.
**Rejected**: A full readline emulation incl. a kill ring (duplicates working native behavior and cannot feed the system yank buffer); React `setText(...)` for the deletions (breaks native undo); `key`-based matching (blind to macOS Alt composition); polyfilling Ctrl+W on Windows/Linux browsers (browser-reserved — uninterceptable by a page; recorded as a caveat, with macOS and the desktop shell covered).
*Introduced by*: 260801-hsxm-compose-enter-policy-readline-keys

### Recovery over verification — a sent history, not a confirmed clear
**Decision**: The strip records every transmitted text per target and puts it one ↑ away, while the draft-clear stays unconditional and the send semantics stay byte-identical. Delivery is never confirmed before clearing.
**Why**: There is no ack channel through a PTY, and all three hops can lose the text silently — `ws.send()` only queues bytes and an OPEN socket can be half-dead after a sleep or a network switch, the backend discards its `ptmx.Write` error, and the receiving TUI can swallow the bytes outright (Claude Code at a permission prompt, an open autocomplete treating Enter as accept, a shell simply executing the line). A history makes the text recoverable regardless of WHICH hop lost it, which no confirmation scheme can claim, and it buys that without touching the payload bytes, the clear, or the focus contract — so the failure mode of the recovery feature is "the history is empty", never "the send changed".
**Rejected**: Echo-confirmation before clearing (the chat send path's novelty-echo probe) — heuristic, adds latency, and still cannot prove the *application* accepted the bytes rather than the PTY merely echoing them; explicitly deferred by the user, not merely unbuilt. Keeping the draft until confirmed — leaves stale text sitting in an addressed surface, and the confirmation it waits on may never arrive.
*Introduced by*: 260806-kadm-compose-send-history-recall

### Sent history rides a sibling localStorage key, not a widened draft schema
**Decision**: History persists under its own `runkit-compose-sent-history` key with its own type guard and its own map, sharing only the generic `pruneByAgeAndCap` helper with drafts.
**Why**: The shipped draft schema, its `isStoredDraft` guard, and its prune pipeline stay byte-compatible, so an older tab reading `runkit-compose-drafts` is unaffected and corruption in one surface cannot take the other down. The two entries also have genuinely independent lifetimes — a draft dies at send, which is precisely when a history entry is born. Sharing the age/cap helper (rather than the schema) is the right seam: the policy that must not diverge is the pruning, not the shape.
**Rejected**: Folding `entries` into the existing draft entry — it would widen `isStoredDraft`, force the draft's `text !== ""` emptiness prune to reason about history-only entries, and couple two lifetimes that have no reason to move together. Reusing `MAX_PERSISTED_DRAFTS` for the target cap — a shared constant would force a rename the first time either surface wants a different depth.
*Introduced by*: 260806-kadm-compose-send-history-recall

### ↑ recalls only when it cannot mean cursor movement
**Decision**: ArrowUp starts a walk only on an empty textarea, and only when bare and non-composing; ArrowDown is intercepted only mid-walk under the same bare/non-composing rule. Mid-walk, both arrows additionally require the line boundary — ↑ steps only from a collapsed caret on the recalled entry's first line, ↓ only from its last line; off the boundary they move the caret natively within the recalled text without ending the walk.
**Why**: The strip exists to be a real composition surface with IME — hijacking ↑ inside a multi-line draft would destroy in-progress composition, and swallowing an IME-composing arrow would break candidate navigation on the one surface that offers it (xterm.js has no IME). The modifier exclusions follow the same exact-modifier discipline both neighbouring keydown layers already declare: Shift+↑/↓ extends a selection, Alt/Opt and Cmd are macOS paragraph/document jumps, Ctrl is claimed by desktop environments. An empty textarea is the one state where ↑ has no native meaning worth preserving, which is what makes it a safe start gate. Mid-walk, an unconditional intercept proved wrong in use: recalled entries are themselves multi-line, and swallowing every ↑/↓ made them un-navigable — the REPL boundary discipline resolves the same conflict there (native motion inside the entry, history steps at its edges), while caret motion deliberately keeps the walk alive because moving the caret is not an edit.
**Rejected**: First-line gating as the *start* gate (intercept a user-typed draft when the caret sits on line 1) — a cursor-movement trap in composition, where a line-1 ↑ legitimately means "stay put"; the boundary rule is safe only mid-walk, where the visible text is recalled rather than composed. Keying the intercept on `e.key` alone — swallows IME candidate navigation and every modified native motion. Unconditional mid-walk intercept (the initial shape) — made multi-line recalls un-navigable.
*Introduced by*: 260806-kadm-compose-send-history-recall

### The recall walk lives in refs and ends eagerly on a target switch
**Decision**: The walk index and pre-walk stash are `useRef`s (never state), and the target-switch exit fires from a `draftKey` effect rather than from a check at the next keydown.
**Why**: No render reads the index or stash — the visible text is written through `setComposeText` and rendered by the store-controlled textarea, so state would fork the source of truth and cost a re-render per arrow press for no visual benefit. The exit must be eager because the index and stash are meaningless against a stranger target: a keydown-lazy comparison only runs when an arrow is pressed, so an A→B→A round-trip with no arrow on B resumes A's abandoned walk, and an edit on B tears down A's stash. The feared per-focus-change cost is nil — `endRecall()` on a dead walk is two ref writes and no render — and the eagerness is what lets `handleRecallKey` skip a stale-target check entirely.
**Rejected**: `useState` for the index (renders on every arrow press and races the store write). A `recallKeyRef` compared lazily at keydown (it leaves the A→B→A stale walk armed, which is the bug the eager exit exists to prevent). Ending the walk only on user keystrokes — `handleUpload` and `removeFile` mutate the text through the same setter, and leaving the walk armed orphans the attachment: the next arrow drops the path line while the `File` and its preview chip stay mounted, pointing at a path the agent can never resolve.
*Introduced by*: 260806-kadm-compose-send-history-recall

### Brand logo motion is state-based (segment illumination), not a transform spin
**Decision**: The brand crumb's logo hover is a JS-driven white glow "detach, orbit, land" sweep over the six border segments' fills — no geometry moves, no CSS keyframes, and no `prefers-reduced-motion` gate line (the JS skips itself). The glow is **white**, not the accent green the rest of the hover vocabulary animates in.
**Why**: The loading chase already speaks segment illumination, so a hover treatment in the same language gives the logo ONE motion vocabulary across both states instead of two unrelated grammars (illumination while loading, rotation on hover). White is the only color that reads: the glow rides over the lit half's own `#b4b4b4`, and a gray-toned glow is invisible there — accent green was prototyped and rejected outright ("green is not the answer"), which also keeps the Brand row's green cue on the wordmark's glitch where it already lives. Driving fills from JS is the established treatment class here (`typed-label.tsx`, the top-bar boot sweep), and it is what a continuous fractional head position and a per-frame gaussian falloff require — CSS keyframes can only step whole segments.
**Rejected**: The `rk-brand-spin` transform spin it replaces (`.rk-logo-ring` `<g>` + `transform-box: fill-box` — rotating geometry, a second vocabulary for one element); a stepped chase head scheduled as `floor(ease(t)·N)` (whole-segment jitter, and the last step lands only on the exact final frame so the ring parks then snaps); an accent-green or segment-gray glow (invisible over the lit half / off-vocabulary); a CSS-only illumination sweep (no fractional head, no gaussian).
*Introduced by*: 260803-ufqr-logo-animation-polish

### Landing IS the rest state — an integer lap count, not a restore
**Decision**: The sweep runs a whole number of laps (3) with a settle envelope that reaches `1` at both `p = 0` and `p = 1`, so those two frames compute to exactly the literal rest fills. The post-landing write of the static hex values is a numerical safety net, not the mechanism.
**Why**: The seam between "the animation stopped" and "the lit segments reset" is the whole design goal — the eye must not be able to tell them apart. An integer lap count parks the head on segment 0, which is the lit trio's own center, so the blob's landing spot IS the rest pattern's position and the crossfade has nowhere to jump; a fractional lap count would land the glow off-center and make the restore a visible snap no easing could hide. Deriving the lit trio from `BORDER_SEGMENTS[i].staticFill` rather than a duplicated `{5,0,1}` set keeps that alignment true by construction — the rest pattern cannot drift from the math that lands on it.
**Rejected**: A fractional/duration-tuned lap count with a crossfade or eased restore at the end (the snap the mock exhibited); a separate `{5,0,1}` constant beside the static fills (two sources for one pattern, silently divergent); leaving the inline `fill 0.5s` transition live during flight (smears every per-frame fill into a 0.5s crossfade and destroys the timing — hence the capture/suspend/restore).
*Introduced by*: 260803-ufqr-logo-animation-polish

### Chase stagger is negative so the spinner opens in steady state
**Decision**: `LogoSpinner`'s per-segment loading delay is `${i * 0.2 - 1.2}s` — negative across the board — leaving the `logo-chase` keyframes, the 1.2s duration, and the 0.2s stagger interval untouched.
**Why**: A positive stagger holds each segment at its unanimated opacity until its own delay elapses, so the ring mounts as one bright side that expands over the first full lap before the chase establishes. Every spinner mount in the app paid that transient, so it is a bug in the shared loading affordance rather than a per-call-site nicety. Negating the delays starts each segment mid-cycle instead, which is the whole fix — one sign change, no keyframe or timing edit, and every call site inherits it.
**Rejected**: Rewriting the keyframes so frame 0 already reads as mid-chase (encodes the phase offset twice and couples the fix to the six-segment count); a mount-time fade-in over the transient (hides the artifact instead of removing it); per-call-site delay overrides (the transient is shared, so the fix belongs in the component).
*Introduced by*: 260803-ufqr-logo-animation-polish

### Keyboard selection-toggle key is `x`, not Space
**Decision**: The focused-row multi-select toggle in the sidebar tree is `x` (chorded `⌘X`/`Ctrl+X`/`Alt+X` explicitly passed through).
**Why**: `Enter` and `Space` are both already bound to row activation in the tree keydown switch, so repurposing Space would be a behavior regression for every existing keyboard user rather than an addition. `x` is unbound in the tree handler and is the well-precedented list-multiselect key (Gmail-style).
**Rejected**: Space — it already activates the row.
*Introduced by*: 260807-nf9f-sidebar-multiselect-move-windows

### The `x` toggle is documented at the palette registration, not in `DEFAULT_BINDINGS`
**Decision**: `x` (with cmd/shift-click) is surfaced through `SELECTION_GESTURE_HINT` on the `Selection: Select all merged` entry's `shortcut` badge plus the count indicator's hint text, and is deliberately absent from `DEFAULT_BINDINGS`.
**Why**: The project review rule asks that a new shortcut be documented at the palette registration, and `PaletteAction.shortcut` is the seam the palette already renders as a `<kbd>`. `DEFAULT_BINDINGS` is a modifier-chord registry driving a window-level dispatcher (§ Keyboard Shortcuts) — registering a bare `x` there would hijack the key app-wide instead of inside the focused tree, which is a behavior change rather than documentation.
**Rejected**: A `DEFAULT_BINDINGS` entry (app-wide hijack of a bare letter); leaving the gesture undocumented (it appears on no chord map and in no shortcuts overlay, so nothing else would surface it).
*Introduced by*: 260807-nf9f-sidebar-multiselect-move-windows

### Selection state lives in a dedicated store, not sidebar-local state
**Decision**: A small dedicated zustand store (`store/selection-store.ts`) owns the window-row selection.
**Why**: The command palette is composed in `app.tsx` while the tree lives in `components/sidebar/index.tsx`; both must read and write the selection, and a shared store is the project's established seam for exactly that (`window-store.ts`).
**Rejected**: Sidebar-local `useState` plus prop threading — the palette is not a descendant of the sidebar, so it would mean lifting the state into `app.tsx` and threading a setter down through `Sidebar` → `ServerGroup` → `WindowRow`, churning the very memo contract those identity-arg props exist to protect (§ Render Performance).
*Introduced by*: 260807-nf9f-sidebar-multiselect-move-windows

### Escape-to-clear is a CAPTURE-phase handler on the tree
**Decision**: Escape-to-clear lives in a separate `onKeyDownCapture` on the `role="tree"` container, not as a case in the bubble-phase `handleTreeKeyDown`.
**Why**: Each window row spreads the row-flyout card's floating-ui `referenceProps`, whose `useDismiss` contributes an `onKeyDown` that `stopPropagation()`s Escape while the card is open — and the card OPENS on keyboard row focus (`useFocus`), which is exactly the state a keyboard user clearing a selection is in, so a bubble-phase handler never sees the key. Capture gives the tree first refusal. First refusal is not seizure: the handler consumes the event only when there is a selection to clear and only outside an editable target, so the rename input's Escape-cancels and the card's own Escape-dismiss both still work.
**Rejected**: Disabling the flyout's `escapeKey` dismiss, or setting `escapeKeyBubbles` — either degrades the card's own documented Escape-dismiss contract for an unrelated feature.
*Introduced by*: 260807-nf9f-sidebar-multiselect-move-windows

### Selection liveness comes from a DATA key registry, parallel to the visible-row one
**Decision**: Each `ServerGroup` registers two independent things: its visible-row identity slice + signature (`registerGroupRows` → `rowsVersion`, gating roving-key normalization) and its DATA window-key set + signature (`registerGroupDataKeys` → `dataKeysVersion`, gating the selection prune), with `unregisterGroupRows` dropping both on unmount.
**Why**: The two consumers need different notions of "present". Roving navigation is inherently about rendered rows; selection is not — a window folded out of view is still a live tmux window the user meant to select, and `Select all merged` deliberately selects windows inside collapsed sessions. Keying the prune on visibility makes every collapse a silent selection wipe. Both signatures are strings over key sets, so neither counter bumps on the several-per-second passive SSE ticks — the "an SSE tick must not change tree state" invariant holds on both paths — and the data signature is sorted so a pure reorder does not bump it either.
**Rejected**: Reusing `rowsVersion` for both (it bumps on collapse/expand by design, because roving normalization needs it — any consumer riding it inherits that semantics); a `useEffect` watching `sessionsByServer` in the sidebar (re-runs every tick and reintroduces the whole-Map watcher churn the derive-over-store pattern removed); deriving live keys from the raw `sessionsByServer` prop at prune time (it lacks the optimistic ghost/rename overlays the groups actually paint, so it would disagree with the rendered tree).
*Introduced by*: 260807-nf9f-sidebar-multiselect-move-windows

### Bulk move is N sequential calls with continue-on-error, not a new bulk endpoint
**Decision**: The bulk action awaits one existing `move-to-session` POST per selected window in sequence and collects failures rather than aborting.
**Why**: tmux window moves mutate session state serially and the endpoint is already serial-safe, so reusing it adds zero backend surface (Constitution IV). Each window's move is independent, so aborting on the first error would strand the remaining windows for a likely-unrelated failure.
**Rejected**: A batch endpoint (new backend surface for no behavioral gain); parallel `Promise.all` (concurrent tmux session mutation, and a rejected promise loses the per-window outcome detail the aggregate toast reports).
*Introduced by*: 260807-nf9f-sidebar-multiselect-move-windows

### An async batch settles by RECONCILING against its own keys, not by clobbering the store
**Decision**: `executeBulkMove` ends with `settleBatch(batchKeys, retainKeys)` — a subtract-only store action that removes only the keys the batch owned and gave up, leaving every other key (and an anchor the user moved elsewhere) untouched.
**Why**: The palette closes before the batch runs and the batch is fire-and-forget, so a slow move races the user. `clear()` and `selectOnly()` act on whatever the store holds at settle time, so a selection the user started mid-batch is silently destroyed by an operation that had nothing to do with it. Scoping the write to the batch's own keys makes the outcome identical in the uncontended case and correct in the contended one. Subtract-only rather than re-adding the retained keys, so a user who deliberately cleared mid-batch does not have the failures resurrected over their intent.
**Rejected**: An in-flight flag refusing a second batch — it treats a legitimate concurrent action as an error and does not fix the actual race (the user's new SELECTION, not a second batch, is what gets clobbered); reconciling handles both and lets two disjoint batches settle independently.
*Introduced by*: 260807-nf9f-sidebar-multiselect-move-windows

### The move builder owns the server-match guard
**Decision**: `buildSelectionMoveActions` takes the server its `sessions` argument belongs to as an explicit first parameter and returns `[]` on a mismatch against the selection's own `singleSelectedServer`.
**Why**: tmux window ids (`@N`) are unique per server only, so a caller handing over the wrong server's session list would silently produce plausible-looking targets keyed on colliding ids — which is exactly the shape of defect the parameter closes. Fixing only the call site would leave the builder willing to re-key ANY caller's sessions under the selection's server. A cheap equality guard makes the contract enforceable at the seam and directly unit-testable, at the cost of one extra argument at the single call site.
**Rejected**: Trusting `app.tsx` to feed the right list (the contract stays unenforceable, and the failure mode is silent wrong targets rather than an error).
*Introduced by*: 260807-nf9f-sidebar-multiselect-move-windows

### The destructive bulk action confirms INSIDE the palette, not in a dialog
**Decision**: `PaletteAction` carries an optional `confirmLabel`; `CommandPalette` turns a selection of such an action into a one-row confirmation sub-step (results replaced by the single `${id}-confirm` row, search input `readOnly`, Enter runs it once, Escape / backdrop / the toggle chord cancel), and bulk close is its only user.
**Why**: Closing N windows is irreversible and the `Selection:` family had no confirm affordance, but the palette is the family's sole action surface (Constitution IV + V) and already owns a two-step shape — the move entries' fuzzy-filter session picker. Keeping the second step inside the palette means the confirm is reachable with the same keystrokes that opened it, adds no component, and leaves the guard swappable later. Deriving the confirm row (rather than storing a second action list) makes recursion impossible: the derived row carries `confirmLabel: undefined`.
**Rejected**: A `<Dialog>` confirm (a new modal surface for one action, and it breaks the keyboard flow the palette establishes); no confirmation at all (the first destructive bulk action deserves one); a typed-name confirmation (disproportionate to a window close, whose contents survive in git/worktrees anyway).
*Introduced by*: 260808-ebgs-selection-bulk-close-send-prompt

### Close and send are cross-server; only move keeps its single-server gate
**Decision**: `buildSelectionCloseAction` and `buildSelectionSendPromptAction` gate on a non-empty selection alone, and their executors derive each request's server from the composite key via `splitSelectionKey`. Move keeps `singleSelectedServer` as its eligibility gate.
**Why**: Move's gate encodes a real tmux constraint — a window cannot move between tmux servers — not a family convention. Close and send are per-window requests that each carry their own `?server=`, so a cross-server selection is fully executable; refusing it would be a self-imposed restriction on exactly the cleanup case the feature exists for (merged windows scattered across servers under sessions scope `all`).
**Rejected**: Applying the single-server gate uniformly for consistency (it makes the family's most common selection ineligible for its most useful actions); splitting a cross-server batch into per-server sub-batches (the sequential per-key loop already handles it with no grouping).
*Introduced by*: 260808-ebgs-selection-bulk-close-send-prompt

### Broadcast recipients are FROZEN when the palette action is chosen
**Decision**: `Selection: Send prompt…` snapshots the selected composite keys into `selectionBroadcastKeys`; the compose target's label, draft key, requests, and `settleBatch` reconciliation all read that snapshot, not the live store.
**Why**: Composing a prompt takes seconds to minutes, and the tree stays interactive throughout — a live read would let the set the user confirmed in the palette (`Send prompt to 5 agents`) silently differ from the set that receives it. The snapshot keeps the visible `→ N selected` label honest end-to-end and keeps the batch's settle keys aligned with what it actually attempted. It is the deliberate exception to the strip's live-target rule (§ Live target), which exists to avoid sending to a stale pane — a frozen SET of windows has no equivalent staleness, since each request re-resolves its pane server-side.
**Rejected**: Reading the live selection at Send time (delivers to recipients the user never confirmed); disabling selection gestures while a broadcast is composing (freezes the tree for a surface the user may abandon).
*Introduced by*: 260808-ebgs-selection-bulk-close-send-prompt

### Selection broadcast is submit-only and takes the CHAT half of the Enter classifier
**Decision**: In selection mode the strip enables only its submitted-text path — insert-line, raw insert, and upload are unavailable — and classifies Enter with `surface: "chat"` rather than `"strip"`, so plain Enter inserts a local newline and Cmd/Ctrl+Enter is the sole submit.
**Why**: Both halves follow from the transport. The strip's terminal modes ride the focused pane's `wsRef` and uploads scope to that pane's worktree; a frozen cross-server recipient set has neither, so those affordances would target the wrong window or an invalid path. And the classifier's own stated reason for `insert-line` is VISIBILITY — the strip overlays the pane a staged line lands in — which is exactly what a broadcast lacks, making chat's newline-Enter the correct half (§ Enter transmits a line in the strip, composes in chat). The divergence stays inside the classifier's surface parameter; only the argument changes here.
**Rejected**: Keeping `surface: "strip"` and returning early on the non-submit actions (the keydown is already consumed at that point, so plain Enter becomes a dead key under an `enterKeyHint="send"` that promises otherwise); broadcasting raw bytes over the focused pane's socket (wrong window, and no socket at all for other servers).
*Introduced by*: 260808-ebgs-selection-bulk-close-send-prompt

### A broadcast clears its draft on DELIVERY, not on resolve
**Decision**: `ComposeSelectionTarget.onSend` resolves with the delivered recipient count; the strip pushes sent history and clears the draft only when that count is non-zero, and `app.tsx` likewise keeps the frozen target when it is zero.
**Why**: The executor absorbs every per-recipient failure into one aggregate toast and always resolves, so a resolve-means-sent rule would destroy the prompt at 0-of-N delivery — the exact case where the user needs the text most, and where the failed recipients are still selected as the retry affordance. A partial delivery clears like any other send: the text did reach agents, and `↑` recalls it from sent history. Keeping the count in the resolution (rather than rejecting on total failure) leaves one return channel for a spectrum of outcomes.
**Rejected**: Unconditional clear (matches the terminal modes but loses an undeliverable prompt); rejecting on zero delivery (turns an ordinary outcome into an exception the caller must distinguish from a real crash — the strip catches those separately and preserves the draft too).
*Introduced by*: 260808-ebgs-selection-bulk-close-send-prompt

### Collapse exceptions are a single map key, not per-session scalars
**Decision**: The per-session window-list collapse state persists as ONE localStorage key `runkit-session-collapsed` holding a JSON object of `true`-only exceptions keyed `` `${server}:${session}` ``, sitting beside (never replacing) the per-server section scalars `runkit-panel-sessions-{server}`. Expanding deletes the entry and an emptied map removes the key. Neither stale-entry pruning nor cross-tab sync is implemented.
**Why**: A keyed boolean set is the natural shape for a map — one key reads, parses, and rewrites atomically, and the whole surface stays enumerable for a future cleanup. Exceptions-only means the expanded default keeps applying to every session the user never touched, so the map never has to be reconciled against the live session set. The two omissions are priced deliberately: orphan entries are single booleans (the cost is that a recreated session reusing a collapsed name inherits the collapse), and the existing per-server persistence already sets the no-`storage`-listener precedent (the cost, sharper here than for the scalars, is that a whole-map write makes concurrent tabs last-write-wins across every server's exceptions).
**Rejected**: One key per session (`runkit-session-collapsed-{server}:{session}`) — sprawls unboundedly across killed sessions and cannot be enumerated for pruning; the per-server pattern gets away with scalars only because its value is a single boolean per server. Also rejected: storing `false` for an expanded session (turns the default into data that must be migrated if it ever changes), and pruning on load against the visible session set (the client has no session list for non-attached servers, so it would delete live entries).
*Introduced by*: 260807-kddk-persist-sidebar-session-collapse

### The collapse write is a ref-derived value committed outside the updater
**Decision**: `toggleSession` reads a `collapsedRef` synchronous mirror, computes `next`, advances the ref, writes localStorage, then commits `setCollapsed(next)` — a plain value, not a functional updater, with no side effect and no derivation inside one.
**Why**: React 19 StrictMode (active via `main.tsx` in dev and e2e) double-invokes state updaters, so a `localStorage.setItem` inside one runs twice and the second pass observes the first pass's write, making a single click a net no-op — the same failure `toggleServerSection` documents. The ref covers the second hazard a plain snapshot does not: back-to-back toggles batched into one render would both read the same stale closure-captured `collapsed`, persisting a map that disagrees with committed state.
**Rejected**: A `useEffect` keyed on `collapsed` — inherently StrictMode-safe, but it re-serializes the hydrated map on every mount, silently rewrites a malformed stored value, and moves the write off the user action that caused it.
*Introduced by*: 260807-kddk-persist-sidebar-session-collapse

### Placeholder over inline legend for compose education
**Decision**: The compose strip teaches its Enter policy through the textarea placeholder, not an inline legend row.
**Why**: A placeholder costs no layout and cannot lie about mode — it is computed where the mode (terminal target / selection broadcast / no target, pointer type) is known; a legend costs layout and can go stale on mode.
**Rejected**: An inline legend row in the compose strip (layout cost, can misdescribe the active mode).
*Introduced by*: 260811-ke2s-hidden-feature-education-microcopy

### Hints render from current state alone (no first-run tracking)
**Decision**: Every education hint (§ Education micro-copy) renders from current state — selection active, binding bound, pointer type — with no first-time/dismissal tracking.
**Why**: The change's stated constraint excludes persistent state; stateless hints cannot drift out of sync with a dismissal flag and cost no storage.
**Rejected**: First-run onboarding tours and dismissible banners (state to maintain, against the terse aesthetic); rotating tip carousels (gimmicky).
*Introduced by*: 260811-ke2s-hidden-feature-education-microcopy

### The panel toggle takes the shifted tier of `⌘.`
**Decision**: `panel-toggle` ships on `⇧⌘.` (shifted tier, code `Period`, terminal scope) — the same keycap as `view-cycle`'s `⌘.`, one tier up; the lens cycle is untouched.
**Why**: `⌘.` is the shipped lens cycle (`view-cycle`, `keybindings.ts`); the shifted tier of the same key is the least-surprise free chord, the tiers are disjoint so `findConflicts` over the default registry stays clean, and the per-device override layer lets a user rebind either action.
**Rejected**: reassigning `⌘.` to the panel and rebinding the lens cycle — changes shipped muscle memory for a chord spec right-panel.md P7 named before the collision with `view-cycle` was known.
*Introduced by*: 260811-2r1w-right-panel-shell-web-surface

### `IframeWindow`'s `onSwitchToTty` is a callback prop, never an `@rk_type` mutation
**Decision**: `IframeWindow.onSwitchToTty` renders the `>_` "Switch to terminal" button, and the callback applies a per-viewer layout switch (`single:tty` through `applyLayout`); the web tile passes it, so the button renders on every web tile.
**Why**: switching to the terminal is view state, not substrate state — a callback keeps `IframeWindow` presentational and free of the view/layout import graph, and the flip fires zero `/options` POSTs (e2e-asserted). Every mount passes the callback (tiles sit side by side, so a "suppress beside the tty" gate has no context to serve).
**Rejected**: `updateWindowType(server, windowId, "")` from the renderer — mutates the window's identity for every viewer to serve one viewer's arrangement.
*Introduced by*: 260811-2r1w-right-panel-shell-web-surface; tile-context semantics: 260812-ab5v-surface-layout-core

### `SurfaceLayout` owns zoom/drag/mount-once; `app.tsx` owns layout state
**Decision**: the tile renderer receives the resolved layout as a prop and owns only transient interaction state (zoom, the in-flight ratio drag, the ever-opened mount bookkeeping); layout resolution (the ladder), persistence, and URL mirroring live in `app.tsx` behind the single `applyLayout` mutation path.
**Why**: layout state is route-integrated (search params, history semantics, navigation seams) and belongs beside the lens machinery; zoom and drag are self-contained interaction state with no URL/localStorage footprint. Prop-in/callback-out composition keeps the renderer free of the router/storage import graph — tile chrome, verbs, and hide-never-unmount are unit-testable in jsdom — and new surface kinds slot in without touching the shell.
**Rejected**: the renderer importing storage/router helpers and owning layout resolution — two sources of truth for the layout (component + route) and untestable without mocking the router.
*Introduced by*: 260811-2r1w-right-panel-shell-web-surface (as the panel shell's contract); generalized to the tile renderer: 260812-ab5v-surface-layout-core

### Code-surface availability gates on stable signals; reachability gates content
**Decision**: `hasCode` — the availability gate for the `code` lens/tile — is exactly one stable signal: the window's `gitRoot` derived non-empty (the code-server port is always resolvable by convention — preset `RK_CODE_SERVER_PORT` else `RK_PORT+2` — so a configured-port leg would be vacuous). The TTL-cached reachability probe never gates availability; it selects the surface's CONTENT (live iframe when up, the `code-server not running — check rk doctor` empty state when down).
**Why**: reachability fluctuates with code-server restarts — gating the rail button / switcher segment on it would strobe the affordances, and the not-running state could never render (the affordance would vanish exactly when its message is needed). Capability signals are stable per window; content states may fluctuate.
**Rejected**: availability = configured ∧ reachable (the spec's original wording — internally contradictory with the not-running state); availability = gitRoot ∧ port-configured (vacuous once the port resolves by convention).
*Introduced by*: 260811-k3vp-right-panel-code-lens; gitRoot-only gate: 260811-a2bo-daemon-code-server-stable-route

### The code surface is a lean `CodeSurface`, not an `IframeWindow` reuse
**Decision**: the `code` lens/surface renders a new minimal component — an iframe of `/code/?folder=<gitRoot>` plus the not-running empty state — while `web` keeps `IframeWindow` with its URL bar and refresh chrome.
**Why**: the code-server URL is fully derived (the stable `/code/` path plus the git root — no port, no editable address), so the URL bar (`@rk_url` substrate state — a shared, POSTed content address) is meaningless; reusing `IframeWindow` would drag inapplicable chrome and substrate semantics into a surface whose content address nobody edits. The relative-path discipline (`toProxySrc`'s convention — never compose an absolute origin) is the one piece reused.
**Rejected**: `IframeWindow` with a hidden URL bar (dead chrome, plus `@rk_url` semantics that do not apply to a derived URL).
*Introduced by*: 260811-k3vp-right-panel-code-lens; stable `/code/` src: 260811-a2bo-daemon-code-server-stable-route

### Chord-reclaim via a capture-phase iframe `contentDocument` listener
**Decision**: `CodeSurface` attaches a capture-phase `keydown` listener to the same-origin iframe's `contentDocument` after every load (re-attached on each navigation, removed on cleanup, `contentDocument` access try/catch-guarded), reclaims exactly the events matching an injected `shouldReclaimChord` predicate (built in `app.tsx` over the registry's pure `hasReclaimableMatch`, which carves out keydowns whose only matches are `ttyOnly` bindings), and re-dispatches a synthetic bubbling `KeyboardEvent` on the parent DOCUMENT.
**Why**: the `/proxy/` embed is same-origin by design, so a focused iframe's key events are reachable before the embedded app's keybinding service sees them; dispatching on the parent document reaches both document-level listeners (the palette's chord) and window-level ones (the keybinding dispatcher, the ⌘. view cycle) in one event. Injecting the predicate keeps the component free of the registry import graph, and reclaiming only REGISTRY chords leaves the embedded app's own Ctrl/⌘ bindings working.
**Rejected**: dispatching on `window` (misses document-level listeners); reclaiming every chord (breaks VS Code's own bindings); shipping without reclaim (click-out-only escape leaves ⌘K dead whenever the editor has focus).
*Introduced by*: 260811-k3vp-right-panel-code-lens

### `⇧⌘.` toggles the first non-tty surface's tile
**Decision**: the `panel-toggle` chord handler is `togglePanel(panelSurfaces.find(s => s !== "tty"))` — add the first non-tty available surface's tile, or close it when already open (through `addSurface`/`closeSurface` → `applyLayout`) — gated on a desktop window route with ≥1 available non-tty surface; the target surface's `Layout: Add/Close <Surface>` palette entry carries the ⇧⌘. hint so the chord stays discoverable.
**Why**: the tty tile is always present by default and closing it is layout collapse, so the chord's meaningful target is the first CONTENT surface; a single deterministic target keeps the chord predictable across capability sets (on a web-only window it toggles `web`).
**Rejected**: one chord per surface (chord-space cost, no muscle-memory win); toggling "the open surface" (with per-tile toggle semantics there is no single open surface to target).
*Introduced by*: 260811-k3vp-right-panel-code-lens; retargeted to tiles: 260812-ab5v-surface-layout-core

### Content-sized split direction menu (`w-max` + `min-w-[170px]` floor)
**Decision**: The SplitControl direction popover sizes to its content (`w-max`) with the `min-w-[170px]` floor kept only as a guard for the unbound (no-keycap) case.
**Why**: The menu is absolutely positioned inside the tiny chip container, so without help it shrink-wraps and wraps rows; with trailing keycaps the rows' content width varies per platform (short ⇧⌘D on mac, long Shift+Ctrl+\ elsewhere), and content-sizing fits every tier without a per-platform fixed bump.
**Rejected**: A larger fixed `min-w` — wastes space on mac's short chords and still risks wrapping if copy changes.
*Introduced by*: 260811-0f3d-split-menu-keycaps-compose-hint

### Compose hint hides once the strip is on
**Decision**: The bottom-bar compose education hint renders only while `composeStripEnabled === false`.
**Why**: The hint is educate-toward copy — once the strip is open the feature has been found and the hint becomes redundant noise.
**Rejected**: An always-on hint (reads as a label, not education); first-run/dismissal tracking (violates the stateless-hints rule, § Design Decisions → Hints render from current state alone).
*Introduced by*: 260811-0f3d-split-menu-keycaps-compose-hint

### Signal hues are per-theme tokens; only the semantic is constant
**Decision**: The four signal hues are theme tokens — `--color-signal-{yellow,purple,blue,red}` defined in the `@theme` block and both `html[data-theme]` blocks of `globals.css` exactly like `--color-accent-green`, consumed through the Tailwind-generated `text-signal-*` / `bg-signal-*` / `border-signal-*` utilities (opacity modifiers via color-mix). Dark values are the legacy Tailwind `-400` hexes (`#facc15` / `#c084fc` / `#60a5fa` / `#f87171`), so dark renders pixel-identical; light values darken to `#b07d02` (custom gold) / `#9333ea` / `#2563eb` / `#dc2626`, each meeting the WCAG 1.4.11 ≥3:1 graphics bar on the light backgrounds. The **semantic** is constant — yellow = "an agent needs you now" in every theme — while the **value** re-themes. `pr-status-model.ts` remains the single edit point for the shared PR/phase color vocabulary; every consuming surface (sidebar rows, session tiles, row flyout card, pane panel, waiting badge/halo/seam, chat warning banner, error foregrounds, kill affordances) inherits from it or references the tokens directly. The chat warning body text collapses into the single `signal-yellow` token; the `border-red-500/50` / `bg-red-500/10` washes stay raw classes (not contrast-critical foregrounds).
**Why**: The fixed `-400` utilities measured 1.3:1–2.8:1 on the light backgrounds — below the ≥3:1 bar for UI graphics — so the highest-priority signals (waiting halos, pending checks, failing PRs, error text) were effectively dark-theme-only. The three-block token pattern was already proven by `--color-accent-green`, and one token per hue keeps the vocabulary centralized.
**Rejected**: `yellow-700` `#a16207` for the light yellow — passes 4.5:1 but reads brown, killing the yellow chroma that carries the attention semantic; the accepted single-token trade-off leaves text surfaces reusing the gold (waiting badge, chat warning body) below the 4.5:1 AA text threshold. A two-tone yellow split (brighter gold for glyphs, darker shade for text-only surfaces) — set aside for single-token simplicity. Moving the red washes to `signal-red` derivatives — deferred as an aesthetic call.
*Introduced by*: 260811-m3f3-light-theme-signal-color-tokens

### Collapse is Shell grid-column gating, not a RightPanel `visible` prop
**Decision**: The rail column hides via the Shell grid slot's `rightPanelVisible` gating at display level (`hidden`), children staying mounted.
**Why**: With the full-height column, `railOpen` collapses the third column exactly like `sidebarOpen` collapses the first — one mechanism, a true mirror. (The panel-iframe-state rationale moved with the surfaces into the tile grid's own hide-never-unmount; the display-level column hide remains cheap and symmetric.)
**Rejected**: A `visible` prop on RightPanel hiding its root div — correct only in the old inside-content flex-row layout; building it first would have meant implementing the hide mechanism twice.
*Introduced by*: 260812-nm4p-top-bar-rail-toggle (panel-slot halves retired by 260812-ab5v)

### Chat is a tileable surface kind
**Decision**: The layout's tileable set is `tty | web | chat | code` (`SurfaceKind = ViewName`) — chat tiles mount `ChatView` like any other surface.
**Why**: `chat` is a shipped lens in the `window-view.ts` registry; excluding it would leave the ViewSwitcher able to show a surface the layout cannot tile, breaking the R12 shim (switcher selections set `single:<view>` through the same mutation path).
**Rejected**: tty/web/code only (the intake's shorthand) — would regress chat users.
*Introduced by*: 260812-ab5v-surface-layout-core

### Two-tile growth and the legacy shim both map to `split-h`
**Decision**: `?view=X&panel=Y` translates to `split-h:X,Y` (X in slot A); growth appends 1→2 as `split-h` and 2→3 as `main-left` (close collapses 3→2 `split-h`, 2→1 `single`, slot A kept).
**Why**: `split-h` is the visual continuation of the shipped main+panel split, so legacy deep links and stored prefs land on the arrangement their authors saw; `main-left` keeps the incumbent slot-A tile dominant on growth.
**Rejected**: mapping 2-tile to `main-left` — no third tile justifies the asymmetric shape.
*Introduced by*: 260812-ab5v-surface-layout-core

### Layout helpers live in a new pure module; the panel module is minimally consolidated
**Decision**: `lib/surface-layout.ts` owns parse/serialize, the resolution ladder, the legacy shim, and the mutation helpers as a pure DOM-free module with colocated tests; `lib/right-panel.ts` keeps the ratio clamps and the shared registry (gaining `tty`, first) rather than being rewritten.
**Why**: mirrors the shipped `window-view.ts` pure-helper pattern (unit-testable, drift-free single source) and minimizes churn in shipped, tested code; one registry (`SurfaceName = SurfaceKind`) keeps rail, layout, and switcher availability from drifting.
**Rejected**: rewriting `right-panel.ts` wholesale into the new module — bigger diff, no behavioral gain this phase.
*Introduced by*: 260812-ab5v-surface-layout-core

### Layout verbs are palette-reachable; only the shape cycle is a direct chord
**Decision**: ⛶ zoom / ◧ promote / ⇄ swap / ✕ close ship as boxed, rest-visible tile-header buttons + `Layout:` palette entries with NO direct keybindings; the one direct-bound layout chord is `layout-cycle` ⌘; (next same-arity preset, order kept). Per-verb direct bindings remain open to a later phase if palette latency proves irritating.
**Why**: one cycle chord plus palette rows covers keyboard-first (Constitution V — every action is keyboard-reachable) with far less chord-surface to defend against collisions and to learn; the cycle chord earns its binding because it is the tmux `next-layout` muscle memory.
**Rejected**: direct-binding all four verb families (the spec's original § Verbs text — per-verb + directional-swap chords) — a large chord-surface commitment before the verbs have proven their usage frequency.
*Introduced by*: 260812-ab5v-surface-layout-core

### Focused slot owned by SurfaceLayout, kind mirrored to app.tsx
**Decision**: The focused **slot** index is `SurfaceLayout` component state; `app.tsx` receives only the focused **kind** via `onFocusedKindChange` and triggers focus via a registered ref (`focusTileRef`, the `zoomToggleRef` pattern).
**Why**: Per-window reset comes free from the existing `${server}:${windowId}` key (the same reason zoom lives there); the `ttyOnly` gate and the palette only need the kind; duplicate-tty highlight needs the slot, which only the component knows.
**Rejected**: Lifting the slot into `app.tsx` — it would need manual reset on window switches and layout mutations, re-deriving what the component key already guarantees.
*Introduced by*: 260812-wfic-surface-titlebar-redesign-shortcut-scoping

### Chord scope gates at handler presence, not inside the dispatcher
**Decision**: The `ttyOnly` shortcut gate works by making the handler `undefined` in the `app.tsx` handler map when the tty tile is unfocused, keyed off the registry's `ttyOnly` data flag.
**Why**: "Scope is descriptive; handler presence gates" is the established dispatcher contract — a match with no handler falls through without `preventDefault` for free, so the chord reaches the focused surface (e.g. code-server's own ⌘D).
**Rejected**: Teaching `useKeybindingDispatch` about tile focus — the dispatcher is deliberately generic and route-agnostic.
*Introduced by*: 260812-wfic-surface-titlebar-redesign-shortcut-scoping
