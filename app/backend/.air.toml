root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/run-kit ./cmd/run-kit"
  bin = "./tmp/run-kit"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 500

[misc]
  clean_on_exit = true
