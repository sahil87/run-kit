# Intake: fab-kit Upgrade 2.16.3 → 2.16.4

**Change**: 260719-l0l3-kit-upgrade-2-16-4
**Created**: 2026-07-19

## Origin

Promptless dispatch (Create-Intake Procedure, `{questioning-mode} = promptless-defer`) from an orchestrator-synthesized description. The upgrade itself was **already executed**: `fab upgrade-repo` was run on 2026-07-19 in this worktree (happy-dingo) and completed successfully — resolved kit v2.16.4 from cache, ran sync (Claude Code skill assets 34/34 already valid — created 0, repaired 0; OpenCode/Codex/Gemini providers skipped, not installed on this machine), and regenerated `fab/project/config.yaml`'s reference fence for kit 2.16.4.

> Ship the fab-kit upgrade from 2.16.3 to 2.16.4 that has ALREADY been executed in this working tree. Implementation is verification-only — the apply stage must NOT re-run the upgrade and must NOT edit any of the three files. After review, the change ships as a normal draft PR (commit + push + PR) through the standard pipeline stages.

Key decision from the dispatch: the previous kit bump (2.16.2→2.16.3) was committed directly to main as 745bc5b0 "Updating fab version"; this one goes through the full fab pipeline **per the operator's explicit request**.

## Why

1. **Problem**: the working tree carries an uncommitted, already-applied toolkit upgrade (kit 2.16.3 → 2.16.4). Until it is committed and merged, the repo's recorded kit version disagrees with the deployed skill assets, and the diff sits at risk of being clobbered or entangled with unrelated work.
2. **If not done**: the version-metadata drift persists; any other change branched from this worktree would accidentally absorb the upgrade diff, and `fab` tooling that reads `.fab-version` / `.kit-migration-version` would report a version whose bump was never shipped.
3. **Why this approach**: the upgrade has already run cleanly, so re-running it (or hand-editing the files) adds only risk. A verification-only apply confirms the tree matches the expected post-upgrade state, then the standard pipeline (review → hydrate → ship → review-pr) carries it to a draft PR. This replaces the direct-to-main precedent (745bc5b0) with an auditable pipeline pass, per the operator's explicit request.

## What Changes

The **entire intended change is already present, uncommitted, in the working tree**. Nothing further is to be generated or edited. The complete diff:

### `fab/.fab-version`

```diff
-2.16.3
+2.16.4
```

### `fab/.kit-migration-version`

```diff
-2.16.3
+2.16.4
```

### `fab/project/config.yaml` — reference-fence header only

The auto-regenerated reference-fence header comment (regenerated by `fab config upgrade` on every upgrade) now names kit 2.16.4. A single-line comment change; **no config values changed**:

```diff
-# >>> fab reference (kit 2.16.3) >>> ---------------------------------------
+# >>> fab reference (kit 2.16.4) >>> ---------------------------------------
```

### Apply stage: verification-only

The apply stage MUST NOT re-run `fab upgrade-repo` and MUST NOT edit any of the three files. Its tasks are to verify the working tree matches the expected post-upgrade state:

1. `fab/.fab-version` reads exactly `2.16.4`
2. `fab/.kit-migration-version` reads exactly `2.16.4`
3. The `fab/project/config.yaml` reference-fence header comment names kit 2.16.4 (`# >>> fab reference (kit 2.16.4) >>>`)
4. `git status` shows no tracked-file modifications beyond these three files plus this change's own `fab/changes/260719-l0l3-kit-upgrade-2-16-4/` artifacts

If any verification fails, the apply task fails per the standard pipeline failure path — no auto-repair edits.

### Out of scope

- **Removing the `providers:` field from config.yaml**: the upgrade output noted that `providers` equals the current default and could be removed to inherit — it is **deliberately kept as-is** (presence=intent). Removing it is explicitly OUT of scope for this change.
- No source code, no tests, no docs/site content changes.
- No project test suites need to run: `source_paths` (app/backend/, app/frontend/src/, app/frontend/tests/, scripts/) are untouched, and `fab/` is listed in `true_impact_exclude` in config.yaml.

## Affected Memory

None — this is toolkit metadata under `fab/`, excluded from true-impact paths in config.yaml (`true_impact_exclude: fab/, docs/`). No `docs/memory/` updates are expected at hydrate.

## Impact

- **Files**: exactly three tracked files, all under `fab/` — `fab/.fab-version`, `fab/.kit-migration-version`, `fab/project/config.yaml` (one comment line). Plus this change's own `fab/changes/260719-l0l3-kit-upgrade-2-16-4/` artifacts.
- **Code/APIs/dependencies**: none. No behavior change anywhere in run-kit.
- **Tests**: none run, none added — outside `source_paths`, inside `true_impact_exclude`.
- **Ship**: normal draft PR (commit + push + PR) through the standard pipeline stages.

## Open Questions

None — the dispatch description resolves all decision points.

## Assumptions

| # | Grade | Decision | Rationale | Scores |
|---|-------|----------|-----------|--------|
| 1 | Certain | Scope is exactly the existing three-file uncommitted diff; apply is verification-only (no re-run of `fab upgrade-repo`, no edits) | Explicit and verbatim in the dispatch description; verified against `git status`/`git diff` — the tree matches exactly | S:95 R:90 A:95 D:95 |
| 2 | Certain | Keep the `providers:` field in config.yaml as-is (presence=intent); removing it to inherit is out of scope | Explicitly stated in the dispatch; trivially reversible later as its own change | S:95 R:95 A:90 D:95 |
| 3 | Certain | Change type is `chore`, pinned via `fab status set-change-type` (explicit source survives refresh re-inference) | Explicit in the dispatch; toolkit-metadata bump is canonically chore | S:95 R:90 A:95 D:95 |
| 4 | Certain | No project test suites run at apply/review | Explicit in the dispatch and derivable from config.yaml: `source_paths` untouched, `fab/` in `true_impact_exclude` | S:90 R:85 A:95 D:90 |
| 5 | Certain | Affected Memory is none; hydrate makes no `docs/memory/` updates | Explicit in the dispatch; `fab/` is excluded from true-impact paths in config.yaml | S:90 R:85 A:95 D:90 |
| 6 | Certain | Ship as a normal draft PR through the standard pipeline stages — not direct-to-main like precedent 745bc5b0 | Operator's explicit request, quoted in the dispatch | S:95 R:80 A:90 D:95 |
| 7 | Certain | Tree-cleanliness acceptance: `git status` shows no tracked-file modifications beyond the three files plus this change's own `fab/changes/260719-l0l3-kit-upgrade-2-16-4/` artifacts | Explicit verification criterion in the dispatch; confirmed true at intake time | S:95 R:90 A:90 D:90 |
| 8 | Certain | A failed verification fails the apply task per the standard pipeline failure path — no auto-repair edits | Entailed by the explicit "must NOT re-run / must NOT edit" constraint; failing is the only compliant behavior | S:75 R:85 A:90 D:90 |
| 9 | Confident | Commit message and PR title/body follow the standard `/git-pr` skill defaults | Not specified beyond "normal draft PR"; one obvious interpretation, low-stakes and reversible before merge | S:55 R:90 A:80 D:75 |

9 assumptions (8 certain, 1 confident, 0 tentative, 0 unresolved).
