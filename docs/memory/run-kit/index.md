---
description: "Web-based agent orchestration dashboard"
---
# run-kit Memory Domain

> **Generated by `fab docs-index`** — everything outside the manual block below is regenerated from the tree and each file's own metadata (`description:` frontmatter, HTML `<title>`/`<meta name="description">`): don't edit it, re-run the command. The manual block is the one hand-managed region: edit it, it is preserved verbatim.

<!-- fab docs-index:manual:start -->
<!-- Hand-managed; preserved verbatim on regeneration. Add rows the generator
     cannot produce: non-markdown files' descriptions (HTML, PDF), external links,
     custom groupings. A row for a file the walker also indexes keeps its place and
     label here, but its description follows that file's frontmatter. A row
     whose target no longer exists is flagged by `fab docs-index --check`. -->
<!-- fab docs-index:manual:end -->

| File | Description |
|------|-------------|
| [agent-messaging](agent-messaging.md) | The `rk mux` family — 13 tmux-substrate verbs, no daemon dependency, grouped as messaging (`send`/`await`) / pane mechanics (`capture`/`kill`/`process`, `panes`/`sessions`) / server ops (`new`/`adopt`/`reap`/`snapshot`/`init-conf`/`guard`). Pane-scoped members share the strict %N/@N/=session:window grammar and consume inherited `-L` (server ops reject it); `await --any` wakes on the first of N panes, `--ready` classifies boot-readiness (`ready %N (state|echo)` / `parked %N` / `narrow %N (WxH)`). |
| [agent-send](agent-send.md) | Agent-send backend — internal/transcript registry + transcript.Path (ErrInvalidRef/ErrTranscriptNotFound/ErrNoAdapter; six adapters — claude, codex, gemini, kimi, agy, opencode request-time export; copilot identity-only) for operator actuation, fork/resume, auto-name — plus the pane-typed injection engine (mode guard, sanitized paste, novelty probe, popup guard, gated Enter, recovery) behind POST /api/windows/{id}/send (target:"agent") and the operator-request routes. |
| [agent-state](agent-state.md) | The `@rk_pane_agent_state` pane-option convention: three-state value schema, dual-read/dual-write over the retired unscoped name, shell reconciler, window rollup. Covers the seven-harness runtime registry (claude, codex, gemini, copilot, kimi, opencode, agy), the `rk agent setup` installer (three installer kinds + hidden aliases), the `rk agent hook` binary indirection, the SessionStart boot stamp, and the `@rk_pane_agent_session` agent-session-identity convention. |
| [api-and-sockets](api-and-sockets.md) | HTTP API layer (chi router and resource handlers), the /ws/state socket (subscriptions, heartbeat/pong, pre-rendered fan-out, broadcast-only shell notification events), and the /ws/terminals relay mux (per-stream queues and fair scheduling). |
| [architecture](architecture.md) | System architecture: Go backend libraries (tmux, sessions, metrics, prstatus, ports, update-checker, selfpath, transcript, inject (send engine + boot-ready AwaitReady/DeliverWhenReady), icontint, installers, SSH-remote, codebridge, codeworkspace), repository structure, data model, chrome architecture, SPA serving, CLI subcommands (Cobra), embedded assets, boards feature, testing layers, security. API/sockets, daemon lifecycle, build/release, and PWA/push live in their sibling files. |
| [build-and-release](build-and-release.md) | Version management (VERSION file + ldflags), build pipeline (Vite dist copied into embed.FS), release flow & CI/CD (GitHub Releases, desktop packages as dependent jobs), Homebrew distribution via prebuilt binaries. |
| [code-bridge](code-bridge.md) | The rk-code-bridge code-server extension + the `rk code` CLI family — a same-user Unix-socket channel into the code lens editor's extension host. Covers the socket + host registry under $XDG_STATE_HOME/run-kit/cb/, tab-keyed identity via the derived .code-workspace file (rk.tab/rk.server, the rk.hasTab gate) + the six run-kit editor actions, the NDJSON protocol, exec/hosts/commands + exit codes, tab-direct resolution + liveness pruning, VSIX distribution, the doctor row, and the security stance. |
| [configuration](configuration.md) | run-kit's configuration story: fixed root $HOME/.config/run-kit/ (no XDG_CONFIG_HOME; test-only RK_CONFIG_DIR override); the internal/settings registry and 13-key inventory behind /api/settings; override order code default < config.yaml < env < CLI flag, env limited to RK_PORT/RK_HOST/RK_CODE_SERVER_PORT; value-home boundaries; the rk-owned hash-stamped managed tmux.conf and its `@rk_srv_managed`-gated reloads; breadcrumb migrations, ~/.rk tenants, the cb/ + code/ state tenants. |
| [cron](cron.md) | Cron scheduling substrate — the `rk cron` CLI family (add/list/rm/mute/pin/tick) over per-server intent files under $XDG_STATE_HOME/run-kit/cron/; stateless evaluator (every / backoff anchor-join / 5-field cron + catch_up / wake_on cursor / suppress_while guards); delivery log (missed, held-expired); daemon Ticker; session targets (`--session`, role→session→pane auto-capture); bounded when-idle holds, if_absent dispositions, role + session (closed-ring plain-resume) respawn; orphan TTL GC; circuit breakers; HTTP API (derivations + deliveries; mutations; notify deep-links); watchlist reader. |
| [daemon-lifecycle](daemon-lifecycle.md) | Daemon lifecycle: start/stop/restart sequencer, port-owner helpers and kill-primitive port-release, stale-socket reap, rk-jobs/rk-code-server/rk-remotes sibling sessions (the code-server spawn env carries RK_BIN for the bridge extension), origin stamp on productionDial, respawn daemon gate, tmux ≥3.4 floor, the rk doctor agent-hooks generation row, the serve-time cron ticker goroutine; the internal/tmuxctl control-mode subscription (anchor session, event scope, resurrection guard). |
| [desktop-shell](desktop-shell.md) | Electron viewer shell: one process with many windows, persistent per-window host views, hosts.json/windows.json restore, welcome Host Hub, dead-host interstitial recovery, SSH heal, local-daemon lifecycle controls, keyboard-tier menus, runkitShell bridge, titlebar/badge chrome, navigation security, packaging, and CLI-driven updates. |
| [layout-snapshots](layout-snapshots.md) | Per-server tmux layout snapshots (`internal/snapshot`): capture set + the $XDG_STATE_HOME/run-kit/snapshots store (atomic latest, 10-entry history, content-dedup, zero-session guard, .died-{ts} tombstones), Snapshotter cadence + ephemeral opt-out, the per-window recently-closed ring ({server}.closed/, ClosedRingCap 10; consumers: closed-window api routes + cron session respawn by AgentRef with drop-on-resume), restore/reopen engines + rk mux snapshot CLI (no relaunch), recovery reader. |
| [operator-actuation](operator-actuation.md) | Operator messaging into the server's operator window over three lanes: direct chat (compose-send allow+probe), templated chat (`chatDelivery` templates — source envelope + delimited text, busy gate and queue skipped in the shared core), and templated requests (busy ⇒ enqueue 202, drained on idle). Covers the closed template registry, fact derivation with best-effort transcript degradation, the server-derived `conversationAvailable` gate, structured 409s, and auto-name dispatch. |
| [pwa-and-push](pwa-and-push.md) | PWA layer (manifest, service worker, install), identity assets, and environment-partitioned OS notifications: browser Web Push plus live state-socket delivery in the desktop shell. |
| [remote-hosts](remote-hosts.md) | SSH-only remote hosts — the `rk remote` six-verb family (add/connect/list/status/disconnect/remove, no `update`) over `internal/remote`: remotes.yaml v1 with immutable 3100–3199 ports, the `ssh -N -L` tunnel as a window in the `rk-remotes` tmux session on the rk-daemon socket, idempotent connect (curl-installer bootstrap, older-than-local update, remote daemon start, origin via `rk url`), loopback binding with SSH as sole auth, and read-path validation of stored entries. |
| [rk-riff](rk-riff.md) | `rk riff` spawn engine — worktree + tmux window + agent launcher, argv-ordered pane arrays, presets, layouts, fan-out. Lives in internal/riff on explicit {server, session, repoRoot}, driven by CLI/web UI (POST /api/riff, fork/closed-resume, macros). Spawn-shaping inputs (Where, WorktreeName, WindowNameBase, Tier), ResumeSessionRef fork/plain-resume seam, skill-prefix rendering (fab agent -o yaml → RenderSkillRef), claude-gated task injection (inject.DeliverWhenReady), exit codes + security. |
| [test-sockets](test-sockets.md) | Test-socket isolation on the tmux substrate: unified rk-test-<role>-<pid>-<ns> naming, the seven-package TestMain POST-sweep (e2e family excluded), rk mux reap (dry-run default, --force, --ephemeral), kill-paired socket-file removal, /api/servers listing every server, the RK_SERVER_ALLOWLIST enumeration bound, the e2e rk-test-e2e-<token>- family anchor + E2E_TMUX_FAMILY with its bare-anchor teardown refusal, the RK_CONFIG_DIR per-run config-root leg, and the port-fallback rule. |
| [tmux-guard-shim](tmux-guard-shim.md) | The tmux guard PATH shim — `rk mux guard` (+ permanent hidden root alias `tmux-guard`) fronts the real tmux and refuses `kill-server` without an explicit `-L`/`-S` (a bare kill destroys the HOST server). Covers the argv decision, shim-skipping resolution, exec passthrough (`$TMUX` restored, `RK_TMUX_GUARD` stripped), the self-healing shim script (probe → guard exec → fail-open walk behind a crude backstop), the `rk agent setup` install contract + shim generations, and doctor states. |
| [tmux-sessions](tmux-sessions.md) | Session enumeration and group filtering; list-clients viewer derivation; direct-attach relay; pin-sessions (_rk-pin-*); operator home _rk-operator; session-role taxonomy (_rk-* reserved); server-birth CWD pin; window @N addressing; exact-match =name: targets; sort-windows; auto-naming; SSE dead-server reap; @rk_srv_protected/@rk_srv_managed provenance; @rk_<scope>_<name> registry + migration sweep; fab-tier + operator-watchlist window derivations. Test sockets + rk mux reap in test-sockets.md. |
| [toolkit-standards](toolkit-standards.md) | run-kit's shll-toolkit-standards conformance posture — constitution binding, audit-against-HEAD-build rule, per-standard PASS status (help-dump, skill, principles, update, version, install-composition). Covers Principle 9 `--quiet`/reaper caps, brew-mutation grace, and the help-dump + P9 new-surface check over `rk desktop`/`remote`/`daemon run`/`role`/`code-server`/`present`/`tab`/`agent`/`code`/`tutorial`/`operator` + the `mux` (`new`/`reap --ephemeral`/`adopt`/`sessions`) and `cron` families. |
| [ui-patterns](ui-patterns.md) | Map file — the UI patterns moved to the ui/ sub-domain; routes old ui-patterns.md section references (historical logs, code comments) to their new homes. |

## Sub-Domains

| Sub-Domain | Description |
|------------|-------------|
| [ui](ui/index.md) | Frontend UI patterns, split by surface: routes + shell, top bar, sidebar, status signals, lenses + surface layout, boards, compose strip, keyboard + palette, terminal frontend, visual design, dialogs + client state, updates + notifications. |
