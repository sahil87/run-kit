---
description: "Web-based agent orchestration dashboard"
---
# run-kit Memory Domain

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [architecture](architecture.md) | System architecture, component responsibilities, data flow — backend libraries (incl. in-memory metrics + prstatus + ports/listening-services collectors, internal/push Web Push subsystem), API endpoints, SSE hub + PR-status join + metrics-only server-neutral stream sentinel + server-independent event: services listening-port broadcast + session-tile previews (dedicated event: preview with per-connection expanded-set scope via POST /api/preview-scope, reusing tmux.CapturePane), server-independent host-metrics subscription (useHostMetrics), tmux/sessions enrichment, control-mode subscription, Web Push notifications (rk notify → /api/notify → webpush-go → service worker, VAPID + subscriptions under ~/.rk/) |
| [rk-riff](rk-riff.md) | `rk riff` subcommand — worktree + tmux window + Claude launcher with argv-ordered pane arrays, presets, named layouts, and parallel fan-out |
| [tmux-sessions](tmux-sessions.md) | Session enumeration, group filtering, direct-attach relay + move-based board pin-sessions (`_rk-pin-*`), window addressing, SSE poll-set reap on dead-server fetch error, unified test-socket naming + `rk reaper`, env-gated `RK_SERVER_ALLOWLIST` test-scoping |
| [ui-patterns](ui-patterns.md) | URL structure + canonical route page names (Cockpit / Server Cabin / Terminal / Board / Not Found — the spoken/doc vocabulary), three-way server route guard + create-server/server-gone lifecycle, Cockpit host-console home (HOST HEALTH zone on `/` via server-independent useHostMetrics + shared HostMetrics component; SERVICES zone of listening-port tiles via server-independent useHostServices, "Open in window" → @rk_type=iframe /proxy/{port}/ window with NON_HTTP_PORTS denylist gate), SessionTiles density view (session→window tile grid on /$server with static capture-pane text previews via SSE event: preview; replaced the deleted Dashboard) + sidebar/boards components, shared PR-status line (Pane-panel only) + unified StatusDot (one lifecycle-journey dot — PR > fab > tmux precedence, hue=phase / shape=status — reused on sidebar row, SessionTiles window tile, pane-panel header) + custom StatusDotTip floating-ui hover-card (replaces native title; docs link + PR-phase Open-PR link; hover/focus open, no tooltip ARIA role), Web Push opt-in (terminal-themed Cmd+K palette entry, not a bell), chrome, theme system, user-controllable global terminal font size (ChromeContext preference + top-bar combo + palette actions), adaptive terminal write flush + deferred per-connection reset + (server, owning session)-keyed relay connection identity, keyboard shortcuts, sidebar tree keyboard navigation + W3C-APG tree ARIA, component conventions |
