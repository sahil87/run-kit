---
description: "Map file — the architecture content moved to the architecture/ sub-domain (overview, repo-layout, backend-packages, tmux-runner, pr-status, cli, testing) plus ui/routes-and-shell and ui/boards; routes old architecture.md section references (historical logs, code comments) to their new homes."
type: memory
---
# run-kit Architecture (moved)

This file's content was split into the [architecture/](/run-kit/architecture/index.md) sub-domain, with the frontend
shell and boards sections folded into the [ui/](/run-kit/ui/index.md) sub-domain. It remains only so historical
references (`log.md` entries, code comments citing `architecture.md § …`) keep resolving.
Do not add content here — write to the topic file instead.

| Old section (§) | New home |
|---|---|
| System Overview, Data Model (+ Performance Caching), SPA Static Serving (+ Two-tier cache policy), Embedded Frontend Assets, Security | [architecture/overview](/run-kit/architecture/overview.md) |
| Repository Structure | [architecture/repo-layout](/run-kit/architecture/repo-layout.md) (directory granularity; per-file detail lives in cli / backend-packages / api-and-sockets) |
| Backend Libraries (Go Modules) — the `internal/` package table, External Go Dependencies | [architecture/backend-packages](/run-kit/architecture/backend-packages.md) |
| Backend Libraries → `internal/tmux` row, tmux Runner Core | [architecture/tmux-runner](/run-kit/architecture/tmux-runner.md) |
| Backend Libraries → `internal/prstatus` row | [architecture/pr-status](/run-kit/architecture/pr-status.md) |
| CLI Subcommands (Cobra) | [architecture/cli](/run-kit/architecture/cli.md) |
| Chrome Architecture | [ui/routes-and-shell](/run-kit/ui/routes-and-shell.md) § Chrome Architecture |
| Boards Feature (Route Placement, Backend Surface, Constitution Alignment) | [ui/boards](/run-kit/ui/boards.md) § Architecture Placement |
| Testing (Go Unit Tests, Frontend Unit Tests, Shared test scaffolding, Playwright E2E Tests) | [architecture/testing](/run-kit/architecture/testing.md) |

## Design Decisions (moved)

The 107 entries of the old `## Design Decisions` section were distributed to the `## Design Decisions` section of the file that owns their topic:

| New home | Entries |
|---|---|
| [api-and-sockets](/run-kit/api-and-sockets.md) (1) | The code-server embed rides a stable `/code/*` route; the port never reaches the frontend |
| [architecture/backend-packages](/run-kit/architecture/backend-packages.md) (17) | The readiness state reader is injected into `internal/inject`; The layout verb port lives in the existing `internal/layoutspec`; Shared tmux install-hint helper in `internal/tmux` with injected lookPath; The managed code-server is curated through launch flags, backed by an rk-owned seeded profile for settings-only behavior; The rk-owned profile lives in `~/.rk/code-server-profile`, seeded write-once, with extensions pinned to the default location; The layout grammar lives in `internal/layoutspec`; One validator table, two writers; procfs direct reading over external monitoring tools; Toolkit update-notify + one-click upgrade: delegated `shll check-updates` detection + scoped `shll update`, no new deps/routes/DB; A dedicated `internal/codeserver` package owns the code-server install + layout; Long-lived spawns carry the version-stable rk path; The install job runs the literal shell chain, exe path quoted; Staged extract + rename promotion, temp-symlink flip; Install is the migration verb; foreign-session detection is a path-string match, not a spawn-argv version marker; No HTTP probe on enumerated ports; `sanitizeEnv` restores the pre-direnv PATH; `cleanPATH` is a last-resort guard only; `layoutspec` surface kinds are exactly the frontend `ViewName` registry |
| [architecture/cli](/run-kit/architecture/cli.md) (17) | Argv after `--`, shell expansion opt-in; `--ready` requires `--json`; Separate seam for tab new's widened creation call; `rk tutorial` types its kickoff into the booted agent, never a positional argument; `rk tutorial`'s singleton is session-scoped; `rk tutorial` defaults to the `fast` tier; Operator launcher mechanics owned by rk, kickoff typed; `rk operator` hard-refuses without fab on PATH; Same-origin present targets use site-relative slot URLs; Doctor's ephemeral-servers row is always present and always OK-shaped; Tmux-option-derived serving for `rk present`; `rk present` keeps the URL as stdout data while `rk tab web add` prints the address; `--show` on a full 3-tile layout replaces the last slot; `rk role` acts on the current window via `$TMUX_PANE`; Cobra over custom CLI parsing; `rk code-server update` skips when nothing is managed; Binary/server version drift is a doctor-only informational note, probed per live server |
| [configuration](/run-kit/configuration.md) (1) | Backend settings file over localStorage-only |
| [architecture/overview](/run-kit/architecture/overview.md) (23) | Legacy marker values are normalized at every boundary, never migrated in place; Flair settings normalize is a membership check, not a canonicalizer; Go backend + Vite SPA over a Next.js monolith; Single port architecture; chi over stdlib ServeMux; TanStack Router over React Router; Vite proxy in dev (not CORS); SPA fallback in Go; Every tmux session is a project; Config via env vars (not YAML) for deployment binding; Dedicated tmux server (`-L runkit`); The embedded tmux.conf reports extended keys in the kitty CSI-u form; Multi-server session enumeration; Role as a tmux window option, not tmux index mutation; Server-scoped `@rk_win_role` radio enforced in the write path; SPA handler dual-mode (embedded FS vs filesystem); Two-tier SPA cache policy keyed on the `/assets/` prefix, with a content-derived embedded ETag; Per-window fab enrichment, derived natively (no subprocess); Dual storage: tmux `@rk_ses_color` for session color + `@rk_win_color` for window color; Tmux user-defined options for web content state over database/config file; The layout option is a default-view HINT, not identity; Web-tab family density holds on every write path; `@rk_win_url` dual-reads instead of a migration-sweep row |
| [architecture/pr-status](/run-kit/architecture/pr-status.md) (14) | PR-status collector with wholesale-rebuild cleanup + network-isolated three-layer split; Two-tier agent-state + branch-derived PR links (own-your-signals over consuming `fab pane map`); First-sight-only registration wake, debounced into one pass; The collector pushes a STORED head-index and wakes on store, rather than seeding entries once; An index miss is never an authoritative negative; Origin identity is HOST-QUALIFIED, and accepted by URL FORM rather than a dotted-segment heuristic; `parseOriginRepo` accepts EXACTLY two path segments; `BranchRefresher.refresh` is single-flighted, mirroring the collector; The PR-status seed cache pre-fills startup state and is never a source of truth; Account-switch invalidation lands at the NEXT successful fetch, carried by a seed-provenance mark; The cache's dedup key zeroes every freshness-only stamp; the written document keeps them; Seeded entries restamp `observedAt` and preserve `FetchedAt`; Branch→PR presence continuity: gitRoot keying + detached-HEAD grace + entry retention; Merged-PR durability is derived statelessly from `gh --state all`, never from a grace clock |
| [architecture/testing](/run-kit/architecture/testing.md) (7) | The e2e palette opener defocuses only on retry, never on the first attempt; Shared frontend test scaffolding is imported per-test, never auto-installed via `setupFiles`; `stubMatchMedia` covers only the predicate family; controllable-MQL stubs stay bespoke; Shared Go test scaffolding is one `internal/testutil` package with layered opt-in helpers, not per-package `testhelpers_test.go`; Board e2e pin cleanup is an opt-in registry layer over universal ok-asserted base helpers, and `finally`-block cleanup stays non-asserted; Intent lives in a JSDoc block above each test; Valid-layout fixtures use surviving surface kinds |
| [architecture/tmux-runner](/run-kit/architecture/tmux-runner.md) (2) | One exported tmux runner core, per-site socket targeting; The runner core scrubs tmux client context from the child env |
| [ui/compose-and-bottom-bar](/run-kit/ui/compose-and-bottom-bar.md) (3) | Sticky modifier state via useRef + forceUpdate; Docked compose strip as native textarea (not xterm input); Armed modifiers bridge to physical keyboard |
| [ui/lenses-and-layout](/run-kit/ui/lenses-and-layout.md) (2) | The code lens keys on the derived folder — git toplevel when found, else the pane's raw cwd; The code lens widens `deriveGitRoot`'s derivation rather than changing `config.FindGitRoot` or adding a field |
| [ui/routes-and-shell](/run-kit/ui/routes-and-shell.md) (15) | Derived chrome (not slot injection); Single-view layout (sidebar + terminal/dashboard); Dashboard as inline component in app.tsx (not separate route layout); Sidebar + drawer pattern on mobile; Active window sync — tmux truth, URL as bookmark; `isActiveWindow` is event-derived per session group; `currentWindowEverSeen` gate on kill-redirect over unconditional "gone → redirect"; CSS Grid topology for the rotated shell over nested flex; Sidebar collapses to 0px, not a 48px rail; Single `sidebarOpen` boolean over `sidebarOpen + drawerOpen`; `<Shell>` as a shared wrapper over per-route shell components; `Shell` owns the desktop sidebar aside; the resize handle rides an opaque node slot; `FocusedTerminalContext` as a separate, dedicated context (not a slice of ChromeContext); Read-only setter pair for in-memory + persisted state (`setSidebarWidth` + `persistSidebarWidth`); Mobile sidebar overlay positioned via grid (`gridRow: "2/4"`), not `fixed inset-0` |
| [ui/top-bar](/run-kit/ui/top-bar.md) (3) | Hamburger statically rendered at TopBar.left; Open-in-app: wrap `wt` for the host, keep deeplink templates frontend-only, degrade fail-silent; Derived `${sshUser}@${location.hostname}` is the deeplink host; the section shows on any remote client |
| [ui/visual-design](/run-kit/ui/visual-design.md) (2) | Full ANSI palette over minimal color set; Static tmux.conf with ANSI indices over runtime `tmux set -g` |

## Sections that left this file earlier

Citations of the form `architecture § …` for these sections predate this split; their homes are:

| Old section (§) | Home |
|---|---|
| API Layer, REST API, API Endpoints, SSE Hub, State Socket, Conversation Fork Endpoint, Server-independent host-metrics subscription | [api-and-sockets](/run-kit/api-and-sockets.md) |
| Daemon Lifecycle | [daemon-lifecycle](/run-kit/daemon-lifecycle.md) |
| Version Management, Release Flow & CI/CD, Homebrew Distribution | [build-and-release](/run-kit/build-and-release.md) |
| PWA Identity Assets, Web Push | [pwa-and-push](/run-kit/pwa-and-push.md) |
| Branch→PR Derivation, PR-Status SSE Join | [architecture/pr-status](/run-kit/architecture/pr-status.md) (collector) and [api-and-sockets](/run-kit/api-and-sockets.md) (the socket join) |
