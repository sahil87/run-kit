# Log — run-kit Memory Domain
<!-- Generated by `fab memory-index` from git history + per-change summaries. Do not hand-edit. -->

## 2026-06-16
- **Update** [architecture](/run-kit/architecture.md) — feat: Expand swatch palette 6→10 with two-hue blends and contrast guardrail (#271)
- **Update** [architecture](/run-kit/architecture.md) — feat: Web Push notifications — service worker, VAPID backend, rk notify CLI (#270)
- **Update** [architecture](/run-kit/architecture.md) — refactor: Unified Status Dot (#269)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: unify status-dot sizes so the done square doesn't dominate the circles (#278)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: square the done status dot (rounded-[3px] → rounded-[1px]) (#277)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Status Dot Lifecycle Journey (#276)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Expand swatch palette 6→10 with two-hue blends and contrast guardrail (#271)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Web Push notifications — service worker, VAPID backend, rk notify CLI (#270)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Unified Status Dot (#269)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Status dot lifecycle color journey — three inputs (PR > fab > tmux), hue=phase / shape=status.** Redesigned the unified `StatusDot` (`status-dot.tsx`) from `260615-yg7f`'s two-input PR-or-activity model into a three-input lifecycle journey that adds the **fab pipeline** as a first-class middle input. Reworked § Status Dot (full rewrite), § PR Status (exports + dot cross-refs), § Dashboard, § Window rows (items 1+3), the `window-row.tsx`/`status-panel.tsx` file-roster + § Collapsible Panels entries. **Precedence** (`statusDotState`): PR wins when `fabChange && prNumber` (→ `{phase:"pr", shape:prShape(win)}`); else fab when `fabChange` (→ `{phase:fabPhase(fabStage), shape:fabShape(fabDisplayState)}`); else tmux (`{phase:"none", shape: active?"solid":"ring"}`). **Hue = phase** via `PHASE_HUE`, honoring the fab-kit README's 4-phase grouping (`fabPhase`: intake→intake; apply,review→execution; hydrate→completion; ship,review-pr→shipping) with the run-kit palette — intake=`text-blue-400`, execution+completion BOTH=`text-amber-400` (apply/review/hydrate render identically), shipping=`text-accent-green`, pr=`text-purple-400`, none=`text-text-secondary`. Documented as a palette mapping over the same grouping, NOT a divergence. **Shape = status** — ONE vocabulary across fab AND PR: ring (pending), solid (active/ready), failed = DASHED ring in phase hue + a small red (`bg-red-400`) center dot, done = rounded square (`rounded-[1px]`), skipped = gray hollow ring; failed+done render at 8px (`w-2 h-2`), ring/solid at ~6px. `fabShape`: pending→ring, active/ready→solid, failed→failed, done→done, skipped→skipped, unknown→solid. `prShape` reuses `prDotState`: merged→done, fail→failed, pending→ring, healthy→solid; `neutral` splits on `prState` — closed-unmerged → the gray `skipped` ring ("PR — closed", matching the spec matrix), else → solid. The `win.prState === "closed"` check lives in `prShape`'s `neutral` fall-through only; `prDotState` still returns `neutral` for closed PRs (R9 unchanged). **Red is used in exactly ONE way**: the center dot of a failed dashed ring — the old `fabDisplayState === "failed"` whole-dot red tint AND the old solid-red PR fail dot are both REMOVED. **a11y**: `role="img"`+`aria-label`+`title` from phase+status — fab uses the stage word (`SHAPE_LABEL`: "apply — active"/"review — failed"), the PR branch uses PR-native words (`PR_SHAPE_LABEL`: "PR — merged"/"PR — failing"/"PR — checks running"/"PR — open"/"PR — closed"), tmux uses bare "active"/"idle"; `dotLabel` composes it. New impl symbols in `pr-status-line.tsx`: `DotShape`, `DotPhase` (intake|execution|completion|shipping|pr|none), `StatusDotState = {phase, shape}`, `fabPhase`, `fabShape`, `prShape`, `PHASE_HUE`; in `status-dot.tsx`: `SHAPE_LABEL`, `PR_SHAPE_LABEL`, `dotLabel`. **`PR_DOT_COLOR` and `PR_DOT_LABEL` were DELETED** (zero-call-site once the dot stopped using them) — corrects the `260615-yg7f` rows' "all stay live" claim; the dot now renders from `PHASE_HUE` + the shape maps. `prDotState`/`PrDotState`/`isFailish`/`PR_STATE_COLORS`/`PR_CHECKS_COLORS`/`PR_REVIEW_COLORS`/`PrStatusLine` are UNCHANGED (dashboard PR text line + Pane-panel pr segments; `prShape` reuses `prDotState`). Frontend-only; no backend/SSE/tmux change; no new color tokens beyond standard Tailwind `text-blue-400`/`text-amber-400`. Call sites unchanged (redesign is entirely inside `StatusDot` + `statusDotState`). New spec page `docs/specs/status-dot.md` + `docs/img/status-dot-matrix.svg` + a `docs/specs/index.md` row. Tests: `status-dot.test.tsx` (three-way precedence + `fabPhase`/`fabShape`/`prShape` + tmux fallback + a11y), conformed `window-row.test.tsx`/`dashboard.test.tsx`; `pr-status-line.test.tsx` unmodified. (0hsz)

## 2026-06-15
- **Update** [architecture](/run-kit/architecture.md) — **Color storage int → string descriptor (swatch palette 6 → 10).** A color value is now a STRING descriptor (`"4"` single ANSI index / `"1+3"` two-hue blend) end-to-end. **tmux options**: `WindowInfo.Color` and `SessionInfo.Color` are `*string` (parsed from `@color` / `@session_color` via the new `validate.NormalizeColorValue`, which accepts a legacy bare int OR a descriptor and drops malformed). **settings**: `settings.ServerColors` became `map[string]string` — tolerant read (int-or-string), always writes quoted strings, no migration step; `Get/SetServerColor` now string-typed. **Shared validator**: new `validate.ValidateColorValue` (single 0–15 OR `a+b` each 0–15) + `NormalizeColorValue`, reused by the window-`/options` `@color` rule (`validateWindowOption`), `handleSessionColor`, and `handleSetServerColor` (no triplication). **Session-color path moved**: session color now flows through tmux `@session_color` → `SessionInfo.Color` → `ProjectSession.SessionColor`; `tmux.SetSessionColor`/`UnsetSessionColor` added. The `internal/config/runkit_yaml.go` `Read/WriteSessionColor`/`parseSessionColor` (`*int`) are confirmed DEAD (zero non-test callers) — left in place as deletion candidates, not migrated. All mutations stay POST (§IX); validated input before any `tmux set-option` (§I). Frontend wire types `WindowInfo.color?` / `ProjectSession.sessionColor?` → `string`. Go tests: `settings_test.go` (tolerant read / always-write-string), `validate_test.go` (`ValidateColorValue`/`NormalizeColorValue`), `settings_test.go`/`windows_test.go`/`sessions_test.go` (400s). See `ui-patterns.md` § Color Tinting for the picker palette + OKLab/WCAG contrast guardrail. (6rnr)
- **Update** [architecture](/run-kit/architecture.md) — **Web Push notifications — `rk notify` → closed-tab OS notification.** New `internal/push` package owns `~/.rk/vapid.json` (VAPID keypair, private key mode `0600`, lazy-generated via `webpush.GenerateVAPIDKeys`, never served) + `~/.rk/push-subscriptions.json` (subscription array, de-duped by `endpoint`, tolerant load) — filesystem state, no DB (§II); plus `Notify(ctx, title, body)` fanning out `{title, body, icon}` payloads via `webpush.SendNotificationWithContext` and pruning `404`/`410` dead endpoints. New dep `github.com/SherClockHolmes/webpush-go@v1.4.0`. Three routes in `api/push.go` + `router.go` (CORS unchanged `[GET, POST, OPTIONS]`): `GET /api/push/vapid-public-key` (`{"key":…}`, lazy keygen), `POST /api/push/subscribe` (store, de-dupe, 64 KiB bound, `400` on empty endpoint/bad JSON), `POST /api/notify` (`{title?, body}` → fan-out → `{sent, pruned}`, `400` on empty body). New CLI `rk notify <message> [--title]` (`cmd/rk/notify.go`, registered in `root.go`) resolves the base URL via `config.Load()` and POSTs `{title, body}` to `/api/notify` under an 8s timeout, **fail-silent** (exit 0 / no output on any error). `rk notify` line added to `writeCapabilities` under **Workflow** in `context.go`. Frontend: hand-written `public/sw.js` (`push` → `showNotification`, `notificationclick` → focus/open `/`), registered on load from `main.tsx` guarded by `'serviceWorker' in navigator`; `lib/push.ts` (`registerServiceWorker`/`enablePushSubscription`/`getPushState`, fail-silent); `use-push-subscription.ts` hook + `pushActions` Cmd+K palette block (`Notifications: Enable push` / `Notifications: Enabled ✓` — terminal-themed label, NOT a bell), threaded into `app.tsx`'s `paletteActions`; `api/client.ts` `getVapidPublicKey`/`subscribePush`. Requires a secure context (HTTPS or `localhost`) for SW + `PushManager`. Constitution: §II (JSON under `~/.rk/`, no DB), §IX (mutations POST), §IV (no new route/settings page — Cmd+K only), §V (keyboard-first), §I (0600 private key, never served, request-body validation, no subprocess). (xd9r)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: PR status dot traffic-light (5-state colored indicator) (#267)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **PR status dot — 5-state traffic-light in the sidebar window row.** Reworked § Window rows item 3 + § PR Status. Generalized `260613-o20f`'s single red fail-only `●` into a colored dot rendered for EVERY change-bound PR window (gate UNCHANGED: `win.fabChange && win.prNumber`). New exports in `pr-status-line.tsx` (colocated with `isFailish`, which the `fail` branch reuses — single source of truth preserved): `PrDotState`, `prDotState(win)` (first-match-wins `merged → fail → pending → healthy → neutral`), `PR_DOT_COLOR` (`merged`→`text-purple-400`, `fail`→`text-red-400`, `pending`→`text-yellow-400`, `healthy`→`text-accent-green`, `neutral`→`text-text-secondary` — no new hex), `PR_DOT_LABEL` (per-state `aria-label`+`title`; the fail label CHANGED to "PR needs attention — checks failing or changes requested"). Load-bearing reframing: **green = HEALTH, not merge-readiness** — `healthy` is purely `prChecks === "pass"` with no `&& approved`/`&& !draft`, so a draft with passing checks reads green. Four live states keep the solid `●` glyph; `neutral` renders a dim hollow ring (activity-dot `border: 1.5px solid currentColor` + transparent, `w-1.5 h-1.5 rounded-full`) so "has a PR, no news" ≠ "no PR". `merged` is the only short-circuit (transient, Constitution II — ages out to `neutral`, never persisted); `closed`-unmerged falls through to `neutral`, and a closed PR with failing checks reads `fail`. Known a11y nuance: the state-agnostic neutral label means a closed PR announces "PR open" (faithful to the Certain-grade spec; a follow-up could derive it from `win.prState`). Cross-surface: `status-panel.tsx` `getPrSegments` dropped the `win.prIsDraft ? "text-text-secondary"` draft override so a draft's state segment follows `PR_STATE_COLORS["open"]` (green) — all three PR surfaces (sidebar dot, status-panel segments, `PrStatusLine`'s shared `isFailish` color) tell ONE color story (`PrStatusLine` itself keeps its binary red/secondary line color). Frontend-only — no backend/SSE/`gh` change (reuses the six `WindowInfo` PR fields). Tests rewritten into per-state assertions: `window-row.test.tsx`, `pr-status-line.test.tsx` (`prDotState` precedence), `status-panel.test.tsx` (draft → green). (2olv)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Swatch palette expanded 6 → 10 (two-hue blends) + WCAG contrast guardrail.** Reworked § Color Tinting. A color value is now a STRING descriptor (`"4"` single ANSI index / `"1+3"` 50/50 blend, theme-derived via `blendHex`), not a bare integer. `themes.ts`: `PICKER_ANSI_INDICES` cut to `[1,2,3,4,5,6]` and superseded by `PICKER_BLEND_PAIRS` (`[[1,3],[1,4],[3,4],[1,2]]` = orange/purple/slate/olive) + `PICKER_COLOR_VALUES` (10 ordered string values); new `PickerColor` type and `parseColorValue`/`formatColorValue`/`colorValueToHex` helpers; brights 9–14 REJECTED (audit: collapse onto 1–6 on most themes). `computeRowTints` now returns `Map<string, RowTint>` (string-keyed, was `Map<number, RowTint>`); tint pipeline unchanged (saturate ×1.5 → blend 0.14/0.22/0.32, gray sentinel `"8"` at 0.5). New `computeRowBorders(palette, category) → Map<string, string>`: each color's 8px left border passes through `adjustBorderForContrast` (nudge ONLY OKLab L until WCAG ≥ `BORDER_MIN_CONTRAST` 3.0, preserve hue/chroma, 24-step cap) — a legibility fix that also shifts SOME existing-6 borders on ~25/70 themes. New ported OKLab/WCAG helpers (`hexToOklab`/`oklabToHex`/`relativeLuminance`/`contrastRatio`/`adjustBorderForContrast`, from the dev-time audit script `scripts/audit-swatch-colors.ts`). `swatch-popover.tsx`: 10-swatch grid, string `selectedColor?`/`onSelect` props, 11-item keyboard nav reaching every blend + Clear. `sidebar/index.tsx` memoizes both `rowTints` + `rowBorders` and threads to `window-row.tsx`/`server-panel.tsx` (border looked up by color value). Wire types `WindowInfo.color?: string` / `ProjectSession.sessionColor?: string` (`types.ts`); `client.ts` color fns string-typed. Tests: `themes.test.ts` (parse/format, string-keyed tints, OKLab round-trip, contrast bounds, adjust clears/preserves/passes-through), `swatch-popover.test.tsx`, `client.test.ts`. No E2E (no spec asserted swatch behavior; covered by Vitest). (6rnr)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Web Push opt-in via Cmd+K (terminal-themed, not a bell).** New § Notifications (Web Push opt-in): the push opt-in is a single command-palette action whose label reflects state — `Notifications: Enable push` (runs `Notification.requestPermission()` → `getVapidPublicKey()` → `pushManager.subscribe({userVisibleOnly: true, applicationServerKey})` → `subscribePush()`) vs `Notifications: Enabled ✓` (Unicode checkmark, terminal-themed glyph — a bell was explicitly rejected). Satisfies §V (keyboard-first) + §IV (no new route/settings page). `usePushSubscription()` hook (`hooks/use-push-subscription.ts`) returns `{ state, actions }`; the `pushActions` block is spread into `app.tsx`'s `paletteActions` just before `windowSwitchActions`. Flow logic in the self-contained `lib/push.ts` (`registerServiceWorker`/`enablePushSubscription`/`getPushState`) + `api/client.ts` (`getVapidPublicKey`/`subscribePush`); `public/sw.js` registered on app load from `main.tsx`. All paths throw-free: registration guarded by `'serviceWorker' in navigator`, enable flow aborts silently on denied/unsupported/insecure context. Requires a secure context (HTTPS or `localhost`) for SW + `PushManager`. Backend/delivery model in `architecture.md` § Web Push Notifications. (xd9r)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Unified status dot — one `StatusDot` subsuming activity + PR, reused on three surfaces.** New § Status Dot + reworked § PR Status, § Dashboard window cards, § Window rows (items 1+3), and the `window-row.tsx`/`status-panel.tsx` file-roster entries. New `app/frontend/src/components/status-dot.tsx` — `StatusDot({ win })` renders ONE dot per window via a new `statusDotState(win)` helper + `StatusDotState` discriminated union (`{ kind: "pr"; pr: PrDotState } | { kind: "activity"; active: boolean }`), both colocated with `prDotState`/`PR_DOT_COLOR`/`isFailish` in `pr-status-line.tsx`. **Precedence**: PR status wins when `win.fabChange && win.prNumber` (→ `prDotState`: merged/fail/pending/healthy/neutral); else falls back to activity (`win.activity === "active"`). **Visual vocabulary — all color = PR meaning, activity = shape-only**: PR states render as the prior sidebar PR dot (solid `●` in `PR_DOT_COLOR`; `neutral` = dim hollow ring); the activity fallback is MONOCHROME fill-vs-ring (active = gray `text-text-secondary` filled, idle = gray hollow ring) — NO green, so green/purple/red/yellow are never ambiguous. The `fabDisplayState === "failed"` red tint is preserved on the ACTIVITY branch ONLY (a PR-branch dot keeps its `PR_DOT_COLOR`). Every state carries `aria-label` + `title` (PR reuses `PR_DOT_LABEL`; activity uses "active"/"idle"). Three call sites collapse onto it: sidebar `window-row.tsx` — the former leading activity dot AND the trailing right-side PR dot become ONE leading `<StatusDot>` (so a change-bound-with-PR window now shows its PR status, incl. purple "merged", in the high-attention LEADING position; non-PR windows show monochrome activity there); dashboard `dashboard.tsx` — the inline green/gray activity dot is replaced by `<StatusDot>` and the "active"/"idle" word is DROPPED (idle duration kept), so the dashboard card now matches the sidebar byte-for-byte and gains PR status; pane panel `status-panel.tsx` — a `<StatusDot>` is added to `WindowPanel` `headerRight` before `{win.name}` (an at-a-glance summary that survives panel collapse; the `pr`/`run` detail rows are untouched). Builds on #267 (sidebar PR dot) and #268 (shared PR color vocabulary); no new color tokens; frontend-only (all fields already on `WindowInfo` via SSE). No now-dead exports — `prDotState`/`PR_DOT_COLOR`/`PR_DOT_LABEL` all stay live. Tests: new `status-dot.test.tsx` (precedence + all 5 PR states + activity active/idle + fab-failed red-on-activity-only + per-state a11y); conformed `window-row.test.tsx` (single-dot structure), `dashboard.test.tsx` (asserts dot aria-label not the dropped word), `pr-status-line.test.tsx`. (yg7f)

## 2026-06-14
- **Update** [architecture](/run-kit/architecture.md) — feat: Sidebar cwd "(deleted)" marker (#265)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Sidebar cwd "(deleted)" marker (#265)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Terminal Font Size Control (#264)

## 2026-06-13
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Sidebar Keyboard Navigation + Tree ARIA (Wave 3) (#263)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Sidebar Render Performance (memoize tree + leaf-scope the now-tick) (#262)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Sidebar Drawer Accessibility (Focus Trap + Escape) (#261)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Palette Window Switch (#260)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Sidebar Triage Signal (#259)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Mobile drawer accessibility — focus trap + Escape + current-row focus.** Reworked § Sidebar → Mobile: the overlay's `role="dialog" aria-modal="true"` contract is now honored. New shared `useFocusTrap(containerRef, active, onEscape)` hook (`app/frontend/src/hooks/use-focus-trap.ts`, extracted from the `dialog.tsx`/`command-palette.tsx` focus-cycle; adopted by the drawer only — refactoring those modals is a follow-up Non-Goal). `Shell` attaches a `drawerRef` to the `<aside>` and drives the hook with `active = isMobile && sidebarOpen && !!sidebarChildren` (desktop sidebar is a grid region, NEVER trapped), wiring Escape → `setSidebarOpen(false)` (additive to backdrop/destination/hamburger dismissals). On activation focuses the first focusable; Tab/Shift+Tab wrap within the `<aside>` (`preventDefault` only at boundaries); listener attaches on `document` only while active; `onEscapeRef` stable-ref; no focus-return on close (matches `Dialog`/`CommandPalette`). R10 nested-modal deference: `hasNestedDialog(container)` detects a `[role="dialog"][aria-modal="true"]` DESCENDANT (`KillDialog`→`Dialog`) and the trap's `handleKeyDown` early-returns before Escape AND Tab, so one Escape dismisses only the topmost modal and the drawer-wide Tab wrap can't pull focus out of it; the `<aside>`'s own `role="dialog"` is excluded (`querySelector` descendants-only + `!== container` guard). The `aria-modal="true"` qualifier is load-bearing (PR review): non-modal `PinPopover` (`role="dialog"`, no `aria-modal`, no own trap) must NOT disable the drawer wrap. Capture-phase `stopPropagation` listeners (`PinPopover`/`SwatchPopover`) suppress the bubble-phase trap Escape, so no collision there. Bonus (§ Window rows): `Sidebar` attaches `navRef` to `<nav aria-label="Sessions">`; on mobile drawer open a `useEffect` queries `[data-window-id] [aria-current="page"]`, `scrollIntoView({ block: "nearest" })` + `focus()`es it (deferred via `requestAnimationFrame` to supersede the trap's first-focus), scoped to WINDOW rows so the active `BoardsSection` row (also `aria-current="page"`, no `[data-window-id]` ancestor) and board routes no-op. Frontend-only; no new dependencies. Tests: `use-focus-trap.test.tsx`, extended `shell.test.tsx`, `sidebar/index.test.tsx` current-row case. (o20f)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **User-controllable terminal font size (global, frontend-only).** Reworked § Terminal Font Scaling → § Terminal Font Size: `ChromeContext` owns a global terminal-font preference stored as `number | null` (null = unset) in `localStorage["runkit-terminal-font-size"]`, exposing the **effective** size `terminalFontSize: number` = preference ?? device default (11px mobile / 13px desktop via the shared `isMobileViewport()` narrow-width-OR-coarse-pointer rule, replacing the old `matchMedia('(min-width: 640px)')` read). Bounds 8–24px / step 1 exported as `TERMINAL_FONT_BOUNDS`; helpers `clampTerminalFont`/`deviceDefaultFontSize`/`readTerminalFontSize` mirror the `fixedWidth`/`SIDEBAR_WIDTH_BOUNDS` shape. Mutators `increaseTerminalFont`/`decreaseTerminalFont` (clamp+persist, try/catch noop) and `resetTerminalFont` = forget preference (`removeItem` → device default, not a fixed value); first step from `null` operates on the device default and persists an adjacent value (desktop 13 → 14). `TerminalClient` reads the effective size for the xterm `fontSize` option + `document.fonts.load` preloads (replacing `isMobile ? 11 : 13`), and an effect keyed on it sets `options.fontSize` + calls `fitAddon.fit()` on change (uninitialized-terminal-guarded) — reflows the PTY; the global value means every live `TerminalClient` reacts (board panes included); touch-scroll `LINE_HEIGHT` reads `options.fontSize` live. Surfaced via a top-bar `TerminalFontControl` combo `[−] {size} [+]` + reset near `FixedWidthToggle` (bounds-disabled buttons, aria-labels, `coarse:36px` sizing) AND three palette actions (`terminal-font-increase/decrease/reset`, no shortcut) wired into BOTH the AppShell palette (`app.tsx`) and the board-route palette (`board-page.tsx`). `Cmd +/-` deliberately NOT intercepted (native zoom retained). Deferred: HTML/UI-zone scaling, per-pane sizing, tmux `@rk_font_size` cross-restart persistence. Tests: `chrome-context.test.tsx` (read/clamp/increase/decrease/reset + unset→first-step), `top-bar.test.tsx` (combo render, bounds-disable, dispatch). Frontend-only — no backend change. (oo89)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Sidebar keyboard navigation + tree ARIA (Wave 3).** New § Sidebar → Keyboard Navigation & Tree ARIA: the session/window tree becomes a W3C-APG two-level disclosure tree with roving-tabindex arrow nav, scoped to the `role="tree"` container (`index.tsx`) — never `document`/terminal, no xterm conflict. ARIA: scrollable Sessions region `role="tree"` (the `<nav aria-label="Sessions">` landmark stays separate); session rows `role="treeitem"` `aria-level={1}` + `aria-expanded`/`aria-setsize`/`aria-posinset` and `aria-controls` emitted ONLY while expanded (the `role="group"` window list mounts only when `!isCollapsed`; SF-5); window rows `role="treeitem"` `aria-level={2}` leaves; window-list container `role="group"` id `windows-${server}-${session}`; server `<section>` headers stay structural wrappers (two levels). Roving nav: exactly one row `tabIndex={0}` (threaded as a single primitive so an arrow press re-renders only the two affected memo'd rows — Wave-2 memo preserved); ArrowDown/Up next/prev, stop at ends (no wrap), continuous across open groups; ArrowRight expand/first-child, ArrowLeft collapse/parent; Enter+Space activate via a typed `RowIdentity` discriminated union calling `onSelectWindow` DIRECTLY (no `.click()` synthesis; ghost/empty-windowId rows no-op — SF-2/SF-3); Home/End first/last; handler early-returns on input/editable targets so rename Enter/Escape stands. LOAD-BEARING: the cursor is a `rovingKey` STRING (`data-window-id`/`data-session-row`), and the key-normalization effect is gated on `[rovingKey, rowsVersion]` where `rowsVersion` bumps ONLY when a group's visible-row-SET signature changes (`registerGroupRows`) — NOT on passive SSE ticks; `focusMovedRef` gates the sole `row.focus()` to keypress-driven moves, so the Wave-2 #262 invariant holds (an SSE tick never changes roving state or steals focus). The mobile-drawer focus effect now `setRovingKey`s the row it focuses (SF-4, tab-stop/focus desync). DOM focus + `scrollIntoView({ block: "nearest" })` reuse the "Keyboard-Navigable List Scroll Pattern" (now a third consumer; § updated) but diverge from its `selectedIndex` numeric model. Frontend-only — `index.tsx` + `session-row.tsx` + `window-row.tsx`. Accepted caveats: inner row buttons stay natively Tab-focusable (keyboard-first); `role="tree"` wraps the server `<section>`s. Tests: `index.test.tsx` (tree ARIA + roving + SSE-tick regression), `session-row.test.tsx`/`window-row.test.tsx` (roles/level/set-pos/tabindex; `aria-controls` present-when-expanded), Playwright `e2e/sidebar-keyboard-nav.spec.ts` + `.spec.md`. (wt1v)

## 2026-06-12
- **Update** [architecture](/run-kit/architecture.md) — feat: Consume fab pane map display_state; quiet parked sidebar rows; harden hover-icon cluster (#257)
- **Update** [architecture](/run-kit/architecture.md) — **`display_state` consumed from `fab pane map` (fab ≥ 2.1.7).** `paneMapEntry` gains `DisplayState *string` (`json:"display_state"`; values `active`/`ready`/`done`/`failed`/`pending`/`skipped`, nil on JSON `null`/absent key) and `WindowInfo` gains `FabDisplayState string` (`json:"fabDisplayState,omitempty"`), mapped via `derefStr` in the same WindowID-keyed enrichment join as `FabStage` — additive, request-time, no new endpoints/caches (§II). Flows through `GET /api/sessions` + SSE unchanged; older fab binaries omit the field → empty string → key omitted (graceful degradation). Consumed by the sidebar's quiet parked-row policy (see `ui-patterns.md` § Window rows). Specced in `docs/specs/api.md` (example + Window fields table). (epqk)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Consume fab pane map display_state; quiet parked sidebar rows; harden hover-icon cluster (#257)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Quiet parked rows + inert hover-icon cluster.** Reworked § Window rows: stage text in the right cluster now renders only when `win.fabDisplayState !== "done"` — a parked finished change (fab pane map `display_state === "done"`) shows duration only (empty right cluster when duration is also absent — intended, no placeholder); any other/unknown/absent value keeps the stage text (backward/forward compatible with fab < 2.1.7). The field plumbs `fab pane map`'s `display_state` (fab ≥ 2.1.7): `paneMapEntry.DisplayState *string` → `WindowInfo.FabDisplayState` (`fabDisplayState,omitempty`) via `derefStr` in the same WindowID-keyed enrichment join as `FabStage` → frontend `WindowInfo.fabDisplayState?: string` (`types.ts`). Independent hardening in the same component: the hover-icon container (pin / color swatch / kill) gains `pointer-events-none group-hover:pointer-events-auto coarse:pointer-events-auto has-[:focus-visible]:pointer-events-auto` (inert at rest — stray right-edge clicks fall through to the row-select button) and each hover-revealed `opacity-0` button gains `focus-visible:opacity-100` (keyboard focus never sits on an invisible control; per-button because container opacity can't reveal children's own `opacity-0`). First codebase use of Tailwind v4 `has-[]`. No geometry change. Tests: `sessions_test.go` (value/null/absent parsing + join), `window-row.test.tsx` (suppression predicate + hardening class assertions), `sidebar.test.tsx` (visible + hidden branches). Spec: `docs/specs/api.md` example + Window fields table. (epqk)

## 2026-06-10
- **Update** [architecture](/run-kit/architecture.md) — fix: Defer terminal reset to first write — no black frame on window switch
- **Update** [architecture](/run-kit/architecture.md) — fix: daemon restart aborting on healthy graceful shutdown (#249)
- **Update** [architecture](/run-kit/architecture.md) — feat: Live PR Status in Sidebar (#241)
- **Update** [architecture](/run-kit/architecture.md) — fix: Restore O(1) display-message lookup in ResolveWindowSession (#239)
- **Update** [architecture](/run-kit/architecture.md) — **Live PR status in sidebar (three network-isolated layers).** New `internal/prstatus` package: `Collector` (mirrors `internal/metrics.Collector`) holds `byNumber map[int]PRStatus` under `sync.RWMutex` + `interval`; `NewCollector`/`Start(ctx)` (warm-on-start then ticks)/`Snapshot()` (deep copy under RLock)/`RefreshNow(ctx)`/private `refresh(ctx)`. `refresh` makes ONE `gh api graphql viewer.pullRequests(first:100, states:OPEN)` call via the injectable `ghExec` seam (`exec.CommandContext` + arg slice + 10s `ghTimeout`, no shell string — §I), gated by the injectable `available` seam (`ghAvailable` = `LookPath("gh")` + `gh auth status`; absent/unauth → silent no-op). Successful refresh REPLACES `byNumber` wholesale (merged/closed PRs drop out next cycle = cleanup, no eviction logic); a `gh`/parse error keeps the last-good map (stale-while-revalidate). Pure enum-collapse helpers `mapState`/`mapChecks`/`mapReview` map GitHub's `state`+`isDraft`/`statusCheckRollup.state`/`reviewDecision` to `open|merged|closed` / `pass|fail|pending|none` / `approved|changes_requested|review_required|none`. In-memory only — no DB/disk/tmux-option (§II). **Apply-time correction**: the intake's literal `gh search prs --author @me` argv was replaced by `gh api graphql` because `gh search prs` doesn't expose the rollup/review JSON fields (errors). **Layer 1** (`internal/sessions`): `paneMapEntry` gains `PrURL`/`PrNumber` (`pr_url`/`pr_number` from `fab pane map`, released fab 2.1.2), assigned onto the window in the `enrichByWindowID` join. `tmux.WindowInfo` gains `PrURL *string`/`PrNumber *int` (Layer 1) + `PrState`/`PrChecks`/`PrReview string` + `PrIsDraft bool` (Layer 3), all with `omitempty` json tags. **Layer 3** (`api/sse.go`): new one-method `PRStatusSnapshotter` seam (`Snapshot() map[int]prstatus.PRStatus`, implemented by `*prstatus.Collector`), held on the hub via `newSSEHub(fetcher, mc, pc)`; `attachPRStatus(sess)` runs in `poll()` after the cache write as a PURE in-memory read (zero network on the 2.5s hot path), reset-first (zeroes the four display fields, safe to mutate the 500ms-cached slice) then attaches only under the change-bound gate (`FabChange != "" && PrNumber != nil`). `api/router.go` constructs/starts `pc := prstatus.NewCollector(prStatusPollInterval)` next to the metrics collector, stores it on `Server.prStatus`, and passes a true-nil interface through `initSSEHub` when unset (avoids the typed-nil trap). New const `prStatusPollInterval = 90 * time.Second` (`api/sse.go`). New `POST /api/pr-status/refresh` (`api/pr_status.go` → `handlePRStatusRefresh` → `s.prStatus.RefreshNow(ctx)` → `200 {"ok":true}`, nil-collector safe; §IX). Frontend: `WindowInfo` (`src/types.ts`) gains the six PR fields with literal-union types; `refreshPrStatus()` POST wrapper (`src/api/client.ts`); shared `PrStatusLine` component (`src/components/pr-status-line.tsx`) reused by sidebar `WindowRow` + dashboard cards; dashboard card refactored from `<button>` to `<div>` wrapper so the PR `<a>` link sits outside the button (valid HTML). Tests: `prstatus_test.go` (wholesale rebuild, stale-while-revalidate, gh-absent no-op, enum collapse), `sessions_test.go` (pane-map PR enrichment by WindowID), `pr_status_test.go`, `pr-status-line.test.tsx` + `window-row.test.tsx` (gate + link), Playwright `pr-status-sidebar.spec.ts` + sibling `.spec.md`. External runtime dep: `gh` CLI (optional — fails silently if absent/unauth; ~40 calls/hr vs. 5000/hr limit). (596o)
- **Update** [rk-riff](/run-kit/rk-riff.md) — feat: Live PR Status in Sidebar (#241)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix: Defer terminal reset to first write — no black frame on window switch
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — feat: Live PR Status in Sidebar (#241)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix: Restore O(1) display-message lookup in ResolveWindowSession (#239)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Skip redundant relay reconnect on same-session window switch
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Defer terminal reset to first write — no black frame on window switch
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Move live PR status from sidebar tree to Pane panel (#246)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Live PR Status in Sidebar (#241)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Derive sidebar session display-order instead of storing localOrderByServer in state (#240)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Live PR status line in sidebar + dashboard.** New § PR Status Line documents the shared `PrStatusLine({ win })` component (`src/components/pr-status-line.tsx`) reused by the sidebar `WindowRow` and dashboard window cards. Renders `PR #<n> <glyph> <state>[ (draft)] · <checks/review summary>` ONLY under the change-bound gate `fabChange && prNumber` (early-returns null otherwise — the display mirror of the backend's attach gate). `PR #<n>` is an `<a href={prUrl} target="_blank">` with `onClick` stopPropagation (opens the PR, doesn't select the window); clicking the rest of the line triggers best-effort `refreshPrStatus()` (`POST /api/pr-status/refresh`, errors swallowed, refreshed status arrives via SSE). Color tokens: `text-text-secondary` default, `text-red-400` when fail-ish (`prChecks === "fail" || prReview === "changes_requested"`) — established convention, no new hex; link hover `hover:text-text-primary hover:underline`; `coarse:py-1` touch target. State glyphs `merged`→`✓`/`closed`→`✗`/else `●`. Sidebar places it below the name/`fabStage` row (`pl-[18px] pr-11`, non-ghost); dashboard under the fab-stage badge (`px-2 pb-2 -mt-1`, non-ghost). Dashboard window card refactored from a `<button>` root to an outer `<div>` wrapping the `<button>` so the PR `<a>` link sits OUTSIDE the button (valid HTML — no `<a>`-in-`<button>`); sidebar likewise renders the line outside the row `<button>`. `WindowInfo` (`src/types.ts`) gains six PR fields (`prUrl?`, `prNumber?`, `prState?`, `prChecks?`, `prReview?`, `prIsDraft?` with literal-union types); new `refreshPrStatus()` client wrapper. Tests: `pr-status-line.test.tsx`, `window-row.test.tsx` (gate + `prUrl` link), Playwright `pr-status-sidebar.spec.ts` + sibling `.spec.md` (PR line for change-bound window, absent for scratch, 375px + 1024px). (596o)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Same-session relay reconnect eliminated — connection identity keyed on (server, owning session), part 2.** Reworked § Terminal Write Batching's closing paragraph: part 1's "named follow-up" is now implemented. The connect effect's deps changed from `[terminalReady, sessionName, windowId, server, wsRef]` to `[terminalReady, server, wsRef, connectionEpoch]`; same-session windowId switches ride the existing socket (the relay URL reads `windowIdRef.current`; xterm scrollback preserved — native tmux-attach behavior, since the directly-attached PTY redraws in place via the REST `selectWindow` paths already in app.tsx). New session-identity watcher on `[sessionName, server]`, declared BEFORE the connect effect (must read the pre-change server from `connectedServerRef` to skip its bump on a same-commit server+session change — the `server` dep already reconnects; bumping too would double-reconnect): `""`→resolved is absorption-only (cold deep-link — the relay already attached to the owning session server-side, no reconnect); resolved→resolved bumps `connectionEpoch` (cross-session nav, window move; session RENAME also reconnects — accepted tradeoff, no stable session id in the SSE snapshot); resolved→`""` is a LOSS-OF-IDENTITY signal that records `""` AND bumps for a probe reconnect — the window left the snapshot (killed externally, pinned to a filtered `_rk-pin-*` session, dead deep link) and the probe either re-resolves server-side (X→`""`→X ghost gap costs one flicker-free reconnect) or closes 4004 → `onSessionNotFound` redirect — the ONLY reachable recovery path in that state (`computeKillRedirect` and the URL writeback are gated on non-empty `sessionName`; review finding M1, fixed in rework cycle 1); `""`→`""` is a no-op. Part-1 deferred-reset + adaptive-flush semantics unchanged on every connection that IS established. New "TerminalClient connection identity" unit tests in `terminal-client.test.tsx`; part-1 teardown test re-triggered via a cross-session change (spec-conformant per constitution § Test Integrity). Frontend-only — `terminal-client.tsx` + its test file. (9umy)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **PR status moved to the Pane panel; removed from the sidebar tree.** Reworked § PR Status: the primary PR surface is now a copyable `pr` row in `WindowPanel` (`status-panel.tsx`), appended after `fab` in the `tmx/cwd/git/run/agt/fab` idiom — gated change-bound, copies the PR URL on click, `text-red-400` when checks fail / changes requested, Nerd Font git-pull-request icon. The Pane panel reflects the *selected* window (URL `/$server/$window`), so it's per-window detail rather than a tree-wide glance. `PrStatusLine` was REMOVED from `window-row.tsx` (the sidebar tree no longer shows PR status) but KEPT on the dashboard window cards (the dashboard route has no Pane panel). `window-row.test.tsx` updated to assert the row is absent; new `status-panel.test.tsx` `pr row` describe block (renders/gated/copies-URL/red-on-fail); `pr-status-sidebar.spec.ts` + `.spec.md` rewritten to select a window and assert the Pane panel `pr` row (mobile + desktop). Composes with the PR #241 review fixes (open-only state, no `role="button"` on the dashboard line). (obky)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Deferred terminal reset — window-switch flicker fix.** Reworked § Terminal Write Batching: now documents the adaptive flush (immediate synchronous path for ≤64-UTF-8-byte idle chunks, one per frame via `wroteImmediatelyThisFrame`; rAF coalescing under load; once-buffering-always-buffer ordering — PR #244/#245, shipped outside fab) plus the NEW per-connection deferred reset: each `connect()` arms an effect-scoped `pendingReset`, consumed exactly once by `consumePendingReset()` immediately before the connection's first chunk write (same tick on the immediate path / top of the same rAF callback on the coalesced path) so clear + repaint land in one presented frame — the old receipt-time `terminal.reset()` in `ws.onmessage` guaranteed ≥1 fully-cleared frame (the window-switch black-frame flicker) because the >64-byte first redraw chunk always coalesced to the next frame; old content now persists until the new redraw paints. Handoff semantics: empty flush neither consumes nor executes the pending reset (zero-message close-drain no-op; next `connect()` re-arms idempotently); effect cleanup neutralizes `pendingReset`/`textBuffer`/`binaryBuffers` so a dead connection's late-delivered onclose drain (which still runs before the `cancelled` check, keeping same-effect transient-drop tail drains working) can neither reset nor write stale content into a successor effect's terminal. Stale WS-effect comment (260508-hdjr ephemeral-grouped-session rationale) rewritten to the move-based direct-attach model (`260602-qn62`); same-session reconnects noted as redundant, with a named follow-up to key the effect teardown on the resolved owning session instead of `windowId`. Adaptive-flush machinery untouched (thresholds, `textByteLength`, frame guard, ordering). Reset-ordering unit tests added to `terminal-client.test.tsx`. Frontend-only. (qf25)

## 2026-06-09
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — **`ResolveWindowSession` restored to O(1) targeted lookup.** The body went back from the O(n) `list-windows -a -F '#{session_name}\t#{window_id}'` enumerate + first-match scan to a single targeted `display-message -t <windowID> -p '#{session_name}'` call via `tmuxExecServer` (mirroring `resolveWindowSessionIndex`). The enumeration scan was a #205 workaround for a session-group ambiguity (a window appeared under every group member, so `display-message -t @N` could resolve to an `rk-relay-*` ephemeral); `260602-qn62`'s move-based one-session-per-window model removed window sharing and the ephemerals, so the targeted lookup is now unambiguous — no `RelaySessionPrefix` filtering. Signature `(ctx, server, windowID) (string, error)` and the `fmt.Errorf("window %q not found", windowID)` not-found contract are unchanged: a tmux non-zero exit (missing `-t @N`) and an empty result both map to not-found, so the relay's "any error ⇒ close 4004" path is preserved. Fixes a relay-connect latency regression — the O(n) scan could approach the 5s connect-time budget on busy servers (many sessions + `_rk-pin-*` + `_rk-ctl`), spuriously closing healthy windows with 4004. The 5s relay timeout was intentionally left unchanged (the algorithm, not the duration, was the root cause). Scope was `tmux.go` + a `tmux_test.go` not-found-branch test only; `relay.go`/`windows.go` call sites untouched. See [[resolve-window-session-on-relay-connect]]. (enic)

## 2026-06-04
- **Update** [architecture](/run-kit/architecture.md) — ci: Teardown shll.ai Help-Tree Publish CI (Push → Pull Migration) (#236)

## 2026-06-03
- **Update** [architecture](/run-kit/architecture.md) — refactor: Move-Based Server-Scoped Boards (Pin Sessions) (#233)
- **Update** [architecture](/run-kit/architecture.md) — fix: Fix shll.ai help-tree publish — run-kit.json never lands (#232)
- **Update** [architecture](/run-kit/architecture.md) — **Teardown shll.ai help-tree publish CI (push → pull migration).** Removed the `Publish help tree to shll.ai` step from `.github/workflows/release.yml` (the deliberately-last step, ~95 lines incl. its leading "Placed LAST…" comment): producer CI (`rk help-dump help/run-kit.json` + `jq empty`), PR-opening logic (`publish_to_shllai()` token-clone + `gh pr create`), auto-merge wiring (`gh pr merge --auto --squash`), and all `SHLLAI_TOKEN` usage (`env:` + every `$SHLLAI_TOKEN`) deleted in one cut. shll.ai moved from a **push** model (each toolkit CLI PR'd its help JSON into shll.ai) to a **pull** model (shll.ai now runs `rk help-dump` itself on a schedule and captures the output), so the push transport was redundant and a redundant cross-repo write attack surface (dual writers to `help/run-kit.json` could race). The hidden `rk help-dump` subcommand (`cmd/rk/help_dump.go`) and its tests are **preserved unchanged** — it is now the single contract surface shll.ai pulls from. The `release` job's final step is now **Update Homebrew tap**; the GitHub release/tap path is unaffected (the removed step was last + best-effort). No code references to `secrets.SHLLAI_TOKEN` remain; deleting the orphaned GitHub repo secret is an out-of-tree operator follow-up (least-privilege). Reduces attack surface per Constitution §I. (iak3)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix: Reap Dead Tmux Servers from SSE Poll Set (#235)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix: Guard createAnchor against resurrecting a killed tmux server (#234)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — refactor: Move-Based Server-Scoped Boards (Pin Sessions) (#233)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Reap Dead Tmux Servers from SSE Poll Set (#235)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Move-Based Server-Scoped Boards (Pin Sessions) (#233)

## 2026-06-02
- **Update** [architecture](/run-kit/architecture.md) — feat: Build-time help-dump — emit rk CLI help tree as help/run-kit.json (#230)
- **Update** [architecture](/run-kit/architecture.md) — fix: Prevent exit-empty tmux server death (#226)
- **Update** [architecture](/run-kit/architecture.md) — **`exit-empty` server-death prevention (Constitution VI).** The `_rk-ctl` control-mode anchor is now created **unconditionally** on every server the tmuxctl Supervisor observes — a permanent session floor that keeps the count above zero so tmux's default `exit-empty on` can't reap the server. Previously `resolveBootstrap` created it only on servers empty at first connect, so servers with real sessions got no floor and collapsed once their last real session closed (relays-only → next disconnect → zero → whole-server death; killed live agent sessions, ≥3×). The floor is decoupled from the attach target (still prefers a real session; `firstSessionName` skips the anchor). `tmux.SetExitEmptyOff` sets `exit-empty off` server-globally, imperatively in `productionDial` before the anchor on every dial/reconnect (ordering closes the restart-window sliver); embedded conf sets it too. **Corrects the prior §VI compliance claim in this file**: tmuxctl now *does* create a session on servers with live user sessions (incl. `default`) — but it remains harmless (filtered from UIs via `ControlAnchorSessionName`, ungrouped, read-only `-r` attach), and `exit-empty off` is the deliberate §VI *prevention*, not a violation. Lifetime is explicit-kill-only (server dies only via `kill-server`/`rk reaper`; anchor-only servers persist by design; no cross-process state, §II). Test/e2e sockets still excluded by `isTmuxSocketCandidate`→`IsTestServerName` (no resurrection). (a1wo)
- **Update** [architecture](/run-kit/architecture.md) — **Build-time help-dump — emit rk CLI help tree as `help/run-kit.json`** — New hidden Cobra subcommand `help-dump` in `cmd/rk/help_dump.go` (`Hidden: true`, registered in `root.go` `init()`). `captureNode` walks `rootCmd.Commands()` recursively to full depth, dropping `completion`, `help`, and any `Hidden` node at every level via `includeInDump` (self-excludes `help-dump`); leaves serialize `"commands":[]` (non-nil slice, never `null`). Per-node `name`/`path`/`short`/`usage`/`text` from `cmd.Name()`/`CommandPath()`/`Short`/`UseLine()`/`UsageString()` (raw `text`, newlines preserved). Frozen top-level contract `{tool:"rk", version, captured_at (RFC3339 UTC), schema_version:1, root:node}`, mirroring `sahil87/shll.ai` `help/wt.json`. `version` from `displayVersion()` (ldflags, never hardcoded); `captured_at` via injectable package-level `nowUTC` var + pure `buildDump(root, version, now)` builder (test seam, no global time freeze). Writes to `[output-path]` arg via `os.WriteFile` (0o644) else stdout — pure in-process introspection, no subprocess/shell-string (§I Security First). `release.yml` gains a **Publish help tree to shll.ai** step placed LAST (after the GitHub Release + Homebrew tap, so a fatal in-repo failure can't preempt the published release): `dist/rk-linux-amd64/rk help-dump help/run-kit.json` (versioned artifact) + `jq empty` validation (in-repo, fails the job on bad JSON), then a best-effort auto-merge PR (not a direct push) into `sahil87/shll.ai` via `SHLLAI_TOKEN` mirroring the Homebrew-tap token-clone pattern — skips when unchanged, and logs `::warning::` (PR left open) rather than failing the release when shll.ai is unreachable / auto-merge is disabled. rk's slice of a 7-tool "Command reference" rollout; the shll.ai site-side consumer is a separate repo (out of scope). Tests: `cmd/rk/help_dump_test.go` (shape, filtering via synthetic + real tree, self-exclusion, full-depth recursion, leaf `[]` not `null`, version-from-`displayVersion()`, injected/parseable `captured_at`). (a36m)
- **Update** [architecture](/run-kit/architecture.md) — **Move-based server-scoped boards (pin sessions); relay ephemerals removed.** Boards switched from per-WebSocket `rk-relay-*` ephemeral isolation + `@rk_board` server-option encoding to a move-based pin-session model. A pinned window is `move-window`'d into its own single-window session `_rk-pin-<id>` (helpers `PinSessionName`/`WindowIDFromPinSession` in `tmux.go`); a board = the set of pin-sessions sharing an `@rk_board` SESSION var (server-scoped), with `@rk_home` (restore) + `@rk_board_order` (fractional `ComputeOrderKey`). `board.go` rewritten: pin-session-derived `ListBoardEntries`/`ListBoards`/`GetBoard` (no write-back), move-based `Pin`/`Unpin`/`Reorder` (Pin stamps 3 vars with `context.Background()`-rooted rollback + double-fault guard; idempotent incl. wrong-board re-stamp; Unpin restores to `@rk_home` or recreates a dead home via `rename-session`, no placeholder); kept `ComputeOrderKey`/`nextAppendKey`/validators/`BoardOption` (now the session-var key); deleted `parseBoardValue`/`serializeBoardValue`/`setBoardValue`/`ListAllBoardEntries`/`RemoveAllByWindowID`. **Relay** (`api/relay.go`) attaches the PTY directly to `ResolveWindowSession`'s result (home or `_rk-pin-*`) — removed the ephemeral allocation, `@rk_owner_pid` stamp, ephemeral select/kill; `ResolveWindowSession` no longer filters `rk-relay-*`. **Deleted**: `cmd/rk/serve_sweep.go` + its `serve.go` wiring; `RelaySessionPrefix`/`OwnerPIDOption`/`NewGroupedSession`/`SetSessionOwnerPID`/`GetSessionOwnerPID`/`ListRawSessionNames` from `tmux.go`; `TmuxOps.NewGroupedSession`/`SetSessionOwnerPID`. `parseSessions` skips `_rk-pin-*`+`_rk-ctl`, NO LONGER `rk-relay-*`. **SSE** (`api/sse.go`): dropped `detectKilledWindowIDs`/`previousWindowIDs`/window-kill `board-changed {cleanup}` diff + `RemoveAllByWindowID` dep, `broadcastBoardBootstrap`/`previousBoardJSON`/first-poll bootstrap/cached-board send; only pin/unpin/reorder `board-changed` events remain (`BoardEntriesFetcher` slimmed to one method). Accepted tradeoffs: multi-client active-window collisions on a shared home session; a pinned window disappears from its home session's sidebar until unpinned. Pins persist across rk restarts → no restore-sweep. `_rk-ctl` anchor + `exit-empty off` (`260602-a1wo`) unchanged — pin-session persistence relies on `exit-empty off`; `tmuxctl/` active-window derivation left untouched (investigate-only). Frontend `boards.ts`/`use-boards.ts` doc/contract updated to server-scoped pin-session derivation; `BoardEntry` shape + `board-pane.tsx`/`board-page.tsx` structure unchanged. (qn62)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix: Prevent exit-empty tmux server death (#226)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — **`exit-empty` server-death prevention — always-on `_rk-ctl` anchor floor + imperative `exit-empty off`.** Fixes the *other* recurring whole-server-death cause (distinct from the `260530-cf3g` `kill 0` grenade): tmux's default `exit-empty on` reaping a server the instant its session count hits zero. Root cause was `tmuxctl.resolveBootstrap` creating the `_rk-ctl` anchor **only** when a server was empty at first control-mode connect — so a server that already had real sessions got no floor, and when its last real session later closed (only `rk-relay-*` ephemerals left) the next relay disconnect drained it to zero and tmux reaped the whole server (no run-kit `kill-server`; killed live agent sessions; ≥3× — runWork/utils/kit). `resolveBootstrap` now **always** creates the anchor as a permanent session floor, decoupled from the control-mode **attach target** (still prefers the first real session; `firstSessionName` now skips `_rk-ctl` so the always-present anchor — which sorts ahead of lowercase names — isn't picked; `%session-window-changed` is global so attach-target choice is a minimal-diff preference, not a correctness need). New `tmux.SetExitEmptyOff(ctx, server)` (`set-option -g exit-empty off`, mirrors `tmuxExecServer`/`serverArgs`, ctx+`TmuxTimeout`) is called in `tmuxctl.productionDial` **BEFORE** `resolveBootstrap`/anchor creation, on the initial dial **and** every reconnect — ordering is load-bearing: it closes the reapable zero-session sliver during the close-then-reopen restart window before the floor is reinstalled. The embedded `configs/tmux/default.conf` also `set -g exit-empty off` for run-kit-created servers (belt-and-suspenders; imperative set covers hand-created/foreign servers the `-f` conf never reaches). Server-lifetime contract is now **explicit-kill-only**: a server dies only via `kill-server`/`rk reaper`; empty (anchor-only) servers persist by design (no auto-reap, no cross-process refcounting — §II). Concurrent multi-`rk serve` anchor creation stays benign via `isDuplicateSessionError` (now made actually-reachable + correct: `createAnchor` captures stderr since `cmd.Run()` doesn't populate `ExitError.Stderr`, and the classifier matches both the wrapped message and `ee.Stderr`). Test/e2e sockets are still excluded by `isTmuxSocketCandidate`→`IsTestServerName` so the always-create path can't resurrect them. The `sse.go` "real session disappeared" WARN is kept as defense-in-depth (covers external `kill-session`/OOM/shell-exit that prevention can't). (a1wo)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — **Move-based server-scoped boards (pin sessions) — relay ephemerals removed.** Boards switched from a per-WebSocket `rk-relay-*` ephemeral isolation layer + `@rk_board` server-option comma/colon encoding to a **move-based pin-session model**. New `tmux.PinSessionPrefix = "_rk-pin-"` + reversible `PinSessionName`/`WindowIDFromPinSession` helpers (strip/restore the `@`) + `ListPinSessionNames`. A pinned window is physically `move-window`'d into its own single-window session `_rk-pin-<id>`; a board = the set of pin-sessions sharing an `@rk_board` SESSION var (server-scoped — `move-window` can't cross servers), with `@rk_home` (restore target) and `@rk_board_order` (fractional `ComputeOrderKey`). `Pin` stamps the 3 vars after the move with `context.Background()`-rooted rollback on partial failure (double-fault guard: kill the pin-session only if move-back succeeded); idempotent incl. wrong-board re-stamp. `Unpin` restores to `@rk_home` (recreating a dead home via `rename-session`, no placeholder) then kills the pin-session; multi-unpin appends. `board.go` rewritten: kept `ComputeOrderKey`/`nextAppendKey`/`initialAppendKey`/validators/`BoardOption` (repurposed as the `@rk_board` session-var key); **deleted** `parseBoardValue`/`serializeBoardValue`/`setBoardValue`/`ListAllBoardEntries`/`RemoveAllByWindowID`/the server-option encoding. `ListBoardEntries`/`ListBoards`/`GetBoard` now derive from pin-sessions (no stale write-back). **Relay** (`api/relay.go`) attaches the PTY DIRECTLY to the `ResolveWindowSession` result (home or `_rk-pin-*`) — removed `newEphemeralRelayName`/`NewGroupedSession`/`SetSessionOwnerPID`/ephemeral-`SelectWindowInSession`/ephemeral-`KillSessionCtx`; `ResolveWindowSession` no longer filters `rk-relay-*`. **Deleted**: `cmd/rk/serve_sweep.go` (whole file — `sweepOrphanedRelaySessions`/`pidAlive`/`relayOwnerIsDead`) + its `serve.go` wiring; `RelaySessionPrefix`/`OwnerPIDOption`/`NewGroupedSession`/`SetSessionOwnerPID`/`GetSessionOwnerPID`/`ListRawSessionNames` from `tmux.go`. `parseSessions` now skips `_rk-pin-*` + `_rk-ctl`, NO LONGER skips `rk-relay-*`. **SSE** (`api/sse.go`): dropped the window-kill `board-changed {cleanup}` diff + `RemoveAllByWindowID` dep, `detectKilledWindowIDs`/`previousWindowIDs`, and the `broadcastBoardBootstrap`/`previousBoardJSON` first-poll bootstrap; only explicit pin/unpin/reorder `board-changed` events remain. `TmuxOps` lost `NewGroupedSession`/`SetSessionOwnerPID`. Accepted tradeoffs: multi-client active-window collisions on a shared home session; a pinned window disappears from its home session's sidebar until unpinned. Pins persist across rk restarts → NO restore-sweep. `_rk-ctl` anchor + `exit-empty off` backstop UNCHANGED (pin-session persistence relies on `exit-empty off`); `tmuxctl/` active-window derivation left untouched (investigate-only). Frontend `boards.ts`/`use-boards.ts` doc/contract updated to server-scoped pin-session derivation; response shape + component structure unchanged. (qn62)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Fix Transient "Server not found" Flash After Server Create (#229)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix-sidebar-group-expand (mss7)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Fix non-current sidebar group expand (StrictMode purity)** — `toggleServerSection` made pure: the `localStorage.setItem` write to `runkit-panel-sessions-{server}` and the lazy `attachServer(server)` call moved OUT of the `setServerSectionsOpen` updater. Root cause: React 19 StrictMode double-invokes updaters, and the in-updater `localStorage` write was observed by the second pass (which re-read it via `readServerOpen`), inverting `next` and making a single Expand click on a non-current group a no-op (the group never opened — `multi-server-sidebar.spec.ts:70` failed deterministically). Fix snapshots `current = readServerOpen(server)` once, computes `next`, runs side-effects once outside the updater, then commits a pure functional update deriving `next` from `prev` (fallback to the `current` snapshot for untouched groups) for batch-safety. StrictMode-wrapped click-toggle regression test added in `index.test.tsx`. No backend change; existing coupling/persistence behavior preserved. (mss7)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Move-based boards — SESSIONS-vs-BOARDS exclusivity.** New § SESSIONS-vs-BOARDS exclusivity: pinning a window physically MOVES it (`tmux move-window`) into its own `_rk-pin-*` pin-session, so it disappears from its home session's window list in the SESSIONS sidebar until unpinned (which moves it back to `@rk_home`, appending at the next index). "Already true" for the sidebar with no frontend work — the SSE snapshot no longer lists the moved window under its home session (pin-sessions filtered at the `parseSessions` chokepoint). Board pane rendering is UNCHANGED: each `BoardPane` still embeds the same `TerminalClient` on `/relay/{windowId}?server=<entry.server>`, and the relay now resolves `windowId` to the pin-session server-side (transparent — `BoardEntry` shape + `board-pane.tsx`/`board-page.tsx` structure untouched). `boards.ts`/`use-boards.ts` doc comments updated from "boards are explicitly cross-server" to server-scoped derivation (pins live on one server; the board LIST is summarized across servers, so `useBoards`/`useBoardEntries` still attach all known servers for `board-changed` events). Frontend-only doc/contract change; pin icon + active-board accent affordances on SESSIONS rows are unchanged. (qn62)

## 2026-05-31
- **Update** [architecture](/run-kit/architecture.md) — feat: add --skip-brew-update flag to update command (#218)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix: Test-scoped server enumeration (#219)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — **Test-scoped server enumeration — env-gated `RK_SERVER_ALLOWLIST` allowlist in `ListServers`.** New const `tmux.ServerAllowlistEnv = "RK_SERVER_ALLOWLIST"` + pure predicate `matchesServerAllowlist(name, allowlist)` (`internal/tmux/tmux.go`). `ListServers` reads the env directly via `os.Getenv` in-package (matching the `RK_TMUX_CONF`/`OriginalTMUX` precedent — NOT via `internal/config`; `ListServers` is a `ctx`-only free fn) and, when set, narrows the post-probe live-server list to names that `HasPrefix` ANY trimmed, non-empty comma-delimited token. Empty/whitespace-only value = treated as UNSET = admits everything (production no-op, byte-for-byte). Prefix (not exact) because multi-server e2e specs create `rk-test-e2e-<role>-<pid>-<epoch>` secondaries in `beforeAll` that exact match would exclude; `rk-test-relay-*` under the broader `rk-test-` umbrella is NOT admitted. Filter lives in `ListServers` (not `handleServersList`) so ALL enumeration consumers inherit the scope — `/api/servers`, `board.go` `ListAllBoardEntries`/`GetBoard`, the `serve_sweep.go` relay-orphan sweep; the `tmuxctl` supervisor is unaffected (uses `os.ReadDir` + `isTmuxSocketCandidate`, never `ListServers`). The new forward allowlist (hides NORMAL servers from tests) is orthogonal to and does not touch the existing `IsTestServerName` denylist (hides TEST servers from normal operation). `scripts/test-e2e.sh` exports `RK_SERVER_ALLOWLIST=$E2E_TMUX_SERVER` into the `setsid … just dev` backend launch — scoping the backend READ path, distinct from `E2E_TMUX_SERVER` (the WRITE socket). Hermetic unit test `TestMatchesServerAllowlist`. **Why:** bounds board-route SSE to one `EventSource` per test server, removing the load-dependent HTTP/1.1 connection-pool starvation (N live operator servers → N EventSources → N of 6 slots) that made board e2e specs flaky only on busy boxes — the environmental third vector of the board-route hang. No prod behavior change (env unset). (tmnm)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: bound desktop board relay WebSockets on plaintext origins (#216)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Static xterm imports (#215)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Static xterm imports** — the six xterm-family value imports in `terminal-client.tsx` (`@xterm/xterm` `Terminal`, `addon-fit`, `addon-clipboard`, `addon-web-links`, `addon-unicode-graphemes`, `addon-webgl`) converted from per-pane-mount runtime `await import()` to static top-of-file `import` statements. Because the file is already router-lazy, the xterm family now bundles into the deferred terminal-route chunk and loads **once when that chunk loads**, instead of issuing up to `6×N` runtime chunk fetches when a board mounts N panes. This removes the chunk fetches from the browser's HTTP/1.1 ~6-per-origin connection budget at terminal-init time — the confirmed root cause of the board-route E2E hang (`boards-same-session-multi-pane`, `shell-rotation`): on the plaintext dev/test origin a 7th xterm chunk fetch hung pending behind the saturated long-lived streams (Vite HMR, SSE, per-pane relay WS), so `setTerminalReady(true)` never ran and the pane stayed blank. Masked in prod over Tailscale HTTPS (h2 multiplexes). The font-load `await Promise.all([...])` is now the only async boundary in `init()`; the inter-addon `cancelled` re-checks that guarded the removed import awaits were dropped, leaving one post-construction dispose guard. WebGL `new WebglAddon()`/`loadAddon` `try/catch` retained for GPU-context runtime failures (only the module load moved to static). CSS side-effect import + type-only refs unchanged. Implementation-only — no behavior change; `terminal-client.test.tsx` already mocked all six modules, so it passed unmodified. Second drafted fix (bounding desktop board-row relay WebSockets) remains out of scope. (m3pl)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Bounded desktop relay WebSockets** — new § Desktop Relay-Connection Suspension. On plaintext origins (`window.location.protocol === "http:"`) `DesktopRow` drives each `BoardPane`'s `paused` prop from an `IntersectionObserver` rooted on `rowRef` (was hardcoded `paused={false}`), unmounting off-screen panes' `TerminalClient` so the `/relay/<wid>` WebSocket closes and the connection slot frees — the desktop analogue of the mobile carousel's `paused={idx !== carouselIndex}`. Pre-warm `RELAY_PREWARM_ROOT_MARGIN` (one pane-width horizontal `rootMargin`, no debounce) prevents scroll-past thrash. Live panes capped at `MAX_LIVE_RELAY_PANES = 4` via the pure `selectLivePanes` helper (`select-live-panes.ts`, colocated unit tests): focused pane always live (exempt from visibility-pause and the cap, preserving `Cmd+]`/`Cmd+[` cycling + BottomBar targeting), then most-recently-focused visible panes fill remaining slots, least-recently-focused paused first beyond the cap. Pane elements observed via a `data-paneIndex` + `rootRef` callback prop on `BoardPane`, distinct from the `paneRefs` imperative `BoardPaneHandle`. On HTTPS/h2 (production via Tailscale) the feature is OFF — every pane stays live, no observer, no cap — because the ~6-connection ceiling is a plaintext HTTP/1.1 artifact (h2 multiplexes; relay WS limit ~255). Composes with sibling `260531-m3pl-static-xterm-imports` (removes xterm chunk-fetch pressure): together they fit the board route under 6 connections, fixing the plaintext board-route E2E hang. `MobileCarousel` untouched. E2E `boards-desktop-suspend.spec.ts` + companion `.spec.md`. (rus8)

## 2026-05-30
- **Update** [architecture](/run-kit/architecture.md) — fix: Active-Window Event-Driven Derivation (#212)
- **Update** [architecture](/run-kit/architecture.md) — fix: Isolate Relay Sweep & Stop Test Artifact Leaks (#208)
- **Update** [architecture](/run-kit/architecture.md) — refactor: Window-State API Stability Remediation (#207)
- **Update** [architecture](/run-kit/architecture.md) — **Window-state API stability remediation** — completes the index→`@N` convergence and brings the API into §IX conformance. **REST `/select` session-scoped**: `handleWindowSelect` resolves the owning session server-side (`ResolveWindowSession`) and issues `select-window -t <session>:@N` via the new `SelectWindowInSession` TmuxOps method — never a bare `select-window -t @N` (group-member disambiguation; the client never sends the session). **Single `decodeWindowID(r)` helper** (`api/windows.go`) does `url.PathUnescape` + `ValidateWindowID` in one place; `parseWindowID` and `handleRelay` both delegate to it (kills the copy-paste drift that caused #205); the misleading chi-v5 `RawPath` comment was corrected. **`MoveWindow` atomic** — resolves the source index once then emits the full bubble-swap sequence as one `\;`-chained `tmuxExecServer` invocation (no concurrent mutation can interleave mid-reorder); insert-before / sentinel / single-step / no-op semantics and the positional index destination unchanged. **`KillPane(paneID)` removed** from `tmux.go`, the `TmuxOps` interface, `prodTmuxOps`, and the mock (zero call sites; §IV); `KillActivePane`'s silent-success contract documented as the canonical pane-kill behavior. **Unified `POST /api/windows/{windowId}/options`** replaces the three separate `POST /color`, `PUT /url`, `PUT /type` routes — partial-merge body `{"options": map[string]*string}` (present key sets, `null` unsets, absent untouched), key allowlist `{@color, @rk_url, @rk_type}`, per-key validation preserved, validate-all-before-any-tmux, one atomic `\;`-chained invocation via the new `tmux.SetWindowOptions` + `appendOptionOps`/`WindowOptionOp` primitive; `handleWindowCreate`/`CreateWindowWithOptions` reuse the same primitive (no inline option-map path). **Uniform POST (§IX)** — the five `.Put(` routes migrated: `sessions/order`, `settings/theme`, `settings/server-color` (verb-only), `windows/{id}/url`+`windows/{id}/type` folded into `/options`; CORS `AllowedMethods` → `[GET, POST, OPTIONS]`; frontend client (`setWindowOptions`, `setWindowColor`/`updateWindowUrl`/`updateWindowType` delegating to it, `setSessionOrder`/`setThemePreference`/`setServerColor`) all POST. **Frontend identity on `@N` alone** — `pendingClickRef` holds `{windowId}` only and `urlMatchesPending` compares `pending.windowId === windowParam` (selection survives session rename / cross-session move). **Route shape `/$server/$window`** — `$session` dropped from the TanStack route + `parseParams`; `app.tsx` derives the session name from the active window's SSE snapshot, all `navigate` sites target the 2-segment shape, mount-time alignment + URL writeback compare/write window id only; old 3-segment URLs are a hard break (no redirect shim, constitution §II). (jad6)
- **Update** [architecture](/run-kit/architecture.md) — **Active-window event-driven derivation** — the sidebar/URL active-window highlight is now derived from tmux control-mode `%session-window-changed` events per session group, fixing the stale-base-pointer wrong-highlight bug (an `rk-relay-*` ephemeral / `rk riff` / external `new-window` moves a group member's active window without moving the base session pointer the read path used). Fix is purely in the *derivation/read path* — no window creator changed, the ephemeral relay model untouched, frontend unchanged. New `internal/tmuxctl/active_window.go` `ActiveWindowTracker` (per-server, per-Client, in-memory §II; `sync.RWMutex` over `byGroup` group→`@wid` + `sidGroup` `$sid`→group; `Set`/`Get`/`ResolveGroup`/`ReplaceSidGroups`/`SeedGroups`/`Snapshot`). `api/tmuxctl_bridge.go`: `NewHubSink()` **removed**, replaced by `NewHubSinkFactory()` returning a per-socket tracker-bound `*hubSink`; `OnSessionWindowChanged` resolves `$sid`→group (O(1) cached map, unknown-sid tolerated) and records `@wid` (latest-event-wins); `OnSessionsChanged` refreshes the map; `OnConnectionEstablished` re-seeds (map + Tier-1 from current `#{window_active}` per group, because tmux does NOT replay `%session-window-changed` on a fresh `-CC` attach) — both re-seed callbacks offloaded to a goroutine to honor the EventSink non-blocking contract; injectable `listSessionGroups`/`listActiveByGroup` query seams for testing. `internal/tmuxctl/supervisor.go`: `NewSupervisor(factory SinkFactory)` (`Supervisor.sink` field removed; `nil` factory ⇒ `NoOpSink`); one tracker per socket allocated in `openSocket`; new `Supervisor.ActiveWindow(server, group) (wid, ok)` server-keyed read accessor. New tmux helpers `ListSessionGroups`/`parseSessionGroups` (`$sid`→group, ungrouped→name fallback) and `ListActiveWindowsByGroup`/`parseActiveWindowsByGroup` (group→active-`@wid`; honors only the leader row since `list-windows -a` reports `window_active=1` per group member) — `tmuxExecServer` + `context.WithTimeout` + pure-parse-split convention (§I, §VI). Two-tier derivation in `internal/sessions/sessions.go`: `FetchSessions(ctx, server, provider)` gains an `ActiveWindowProvider` (satisfied by `Supervisor`, injected via `prodSessionFetcher` in `api/router.go` + `Server.SetActiveWindowProvider`); pure `applyActiveWindow(windows, trackedWid)` — Tier 1 (tracked `@wid` authoritative, single-highlight enforced, stale-wid falls back to Tier 2) over Tier 2 (base `#{window_active}`); nil provider degrades to Tier-2-only. Wired in `cmd/rk/serve.go`: `NewSupervisor(NewHubSinkFactory())` then `SetActiveWindowProvider(supervisor)` alongside `SetWindowChangeSubscriber`. Tests: `active_window_test.go` (`-race` concurrent read/write), `tmuxctl_bridge_test.go` (event handling + stubbed re-seed), `internal/sessions/active_window_test.go` (`applyActiveWindow` tiers + single-highlight + stale-wid), `tmux_test.go` (new parsers). Frontend verified no-change (existing `app.tsx` writeback follows once `isActiveWindow` is corrected; boards pin explicit IDs). (v6hm)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix: Active-Window Event-Driven Derivation (#212)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — feat(rk): add rk reaper command to reap leaked test tmux servers and stale sockets (#209)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix: Isolate Relay Sweep & Stop Test Artifact Leaks (#208)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — refactor: Window-State API Stability Remediation (#207)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — unify-test-socket-reaping (cf3g)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — **Unified test-socket reaping.** All test tmux-socket names (Go + Playwright) collapsed under one umbrella `rk-test-<role>-<pid>-<ns>` (roles `unit`/`relay`/`tmuxctl`/`daemon`/`e2e`/`e2e-multi`/`e2e-coupling`/`e2e-msb`), replacing the old 5-prefix scheme (`rk-test-`, `rk-relay-test-`, `rk-verify-`, fixed `rk-tmuxctl-test`/`rk-daemon-test`) + the separate `rk-e2e` exclusion. `IsGoTestServerName` (5-prefix allowlist) **deleted** → `IsTestServerName(name) = HasPrefix("rk-test-")`, consumed only by the tmuxctl supervisor resurrection guard. `parseTestSocketPID` now parses the PID as the **second-to-last** hyphen field (was the field right after the prefix) so hyphenated roles parse; `<ns>` is a single hyphen-free token. `TestMain` sweep flipped **pre-sweep → post-sweep** (`code := m.Run(); sweepDeadTestSockets(); os.Exit(code)`) in `internal/tmux` + `api`; still PID-scoped to dead owners only (concurrent `go test` packages spared), pre-sweep dropped. Manual `rk reaper` rewritten **brute-force-by-prefix** (bare ≡ `--prefix rk-test`): no liveness probe / e2e exclusion / `.lock`-inheritance; **dry-run is the default** (both bare and `--prefix`), `--yes`/`--force` to act, only `--force` bypasses the dangerous-prefix guard (empty or ≤3 chars refused); `_rk-ctl` + live `rk-daemon` hard-skipped unconditionally. Operating contract: do NOT run `rk reaper` while tests are running (no live-run protection by design). `/api/servers` test-socket **hide filter deleted** — it now lists every server, incl. leaked `rk-test-*` orphans (accepted cost: one SSE stream per orphan until reaped). E2E (Playwright) socket names embed `process.pid` (was a `Date.now().slice(-6)` epoch); harness server is `rk-test-e2e` (was `rk-e2e`) in `scripts/test-e2e.sh`, `global-teardown.ts`, the `just pw` default, and all specs. **Separate bug fixed (constitution VI):** `scripts/test-e2e.sh` used `kill 0` in its EXIT-trap cleanup, signalling the caller's (non-detached) process group — SIGTERMing live tmux servers / `-CC` clients sharing it when run inline in an interactive/agent session (proven root cause of `kit`/`abbb`/`runWork` dying mid-session, distinct from `exit-empty`). Fixed: dev server launched via `setsid` into its own group; cleanup kills only `kill -- "-$DEV_PGID"`. (cf3g)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Window-addressing follow-ons from the API-stability remediation. **Select scoping**: the REST `/select` handler now resolves the owning session via `ResolveWindowSession` and issues a session-scoped `SelectWindowInSession` (`select-window -t <session>:@N`) instead of the group-ambiguous bare `SelectWindow(@N)`; the relay was already scoped to its ephemeral. `SelectWindow` (bare target) retains no production callers. **Route shape**: `$session` dropped from the URL (`/$server/$session/$window` → `/$server/$window`) — `@N` is the only window identity in the URL; the owning session is derived from the SSE snapshot. **`MoveWindow` atomic**: the adjacent-`swap-window` bubble sequence now executes as one `\;`-chained invocation (source index resolved once) so no concurrent mutation can interleave mid-reorder. **Option setters**: `@color`/`@rk_type`/`@rk_url` are now set through the batched `tmux.SetWindowOptions` / `appendOptionOps` chained primitive (one atomic invocation) behind the unified `POST /api/windows/{windowId}/options`; `@rk_session_order` / theme / server-color HTTP routes migrated PUT→POST (§IX). (jad6)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Relay startup sweep scoped to owner PID; test-artifact leaks closed. Each `rk-relay-*` ephemeral is stamped at creation with the session-scoped `@rk_owner_pid` user option (`tmux.SetSessionOwnerPID` / const `tmux.OwnerPIDOption = "@rk_owner_pid"`, mirroring `SetSessionColor`'s `set-option -t <session> @opt`; read back via `tmux.GetSessionOwnerPID` with `GetSessionOrder`-style unset/no-server tolerance). `sweepOrphanedRelaySessions` (`cmd/rk/serve_sweep.go`) now reaps a relay **only** when its owner is empty/unstamped (`relayOwnerIsDead("")→true`) or names a dead PID — `pidAlive(pid)` via `syscall.Kill(pid,0)` with a leak-not-kill bias (`nil`/`EPERM`/other→spare, `ESRCH`→reap; intentionally opposite to `daemon_portowner.go:processAlive`'s EPERM→dead). Fixes the cross-instance destruction bug where any second `rk serve` (e2e backend, `air` rebuild, manual instance) tore down a live dashboard's open terminal relays. Stamp ordering is load-bearing: after `NewGroupedSession` + cleanup-defer, before `SelectWindowInSession`/attach — an attachable-but-unstamped relay (owner=="") would be wrongly reaped; on stamp failure `handleRelay` aborts clean (slog.Warn + WS close 4001 + return; deferred `KillSessionCtx` reaps the half-owned ephemeral). Read scope is unchanged — `ListServers`/`/api/servers`/`IsGoTestServerName` still surface foreign servers (incl. `rk-e2e-*`) in the UI; only the destructive reap is scoped (single-uid socket model means EPERM is not an expected owner state). `TmuxOps` interface + `mockTmuxOps` gain `SetSessionOwnerPID`. Test-harness leaks closed too: `scripts/test-e2e.sh` trap + `global-teardown.ts` now glob all `rk-e2e*` sockets (not just literal `rk-e2e`) so timestamp-named secondaries don't leak on crash/Ctrl-C; per-package `TestMain` (`internal/tmux`, `api`) pre-sweeps `rk-test-*`/`rk-relay-test-*` sockets whose embedded PID (2nd hyphen field) is dead, self-healing SIGKILL/panic residue while sparing live-PID and fixed-name (`rk-daemon-test`/`rk-tmuxctl-test`) sockets. (wtg4)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Window-State API Stability Remediation (#207)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **`$session` dropped from the route + identity keyed on `@N` alone.** Route shape `/$server/$session/$window` → `/$server/$window` (TanStack `terminalRoute.path: "/$window"`; `parseParams` exposes only `window`). The owning session name is now **derived from the active window's SSE snapshot** (`currentSession = sessions.find(s => s.windows.some(w => w.windowId === windowParam))`, `sessionName = currentSession?.name`) wherever it was previously read from the URL `$session` segment — breadcrumbs, dropdowns, browser title, kill-redirect inputs. `pendingClickRef` holds `{ windowId }` only (dropped `session`) and `urlMatchesPending` is `pending.windowId === windowParam` — so a session rename or cross-session move (where `@N` survives) no longer releases the pending-click suppression early and bounces the selection. Mount-time alignment guard keyed on `${server}|${windowParam}` (window-id-only); URL writeback navigates `{ to: "/$server/$window", params: { server, window } }` (no session param); `navigateToWindow(windowId)` drops the session arg. All `app.tsx` navigate sites target the 2-segment shape. Deep link `/$server/@N` derives the session server-side from the first snapshot for breadcrumb display and aligns tmux to `@N`. Old 3-segment `/$server/$session/$window` URLs are a hard break — they fall through to `NotFoundPage` / the server-dashboard fallback (no redirect shim; constitution §II). The `IframeWindow` URL bar's `updateWindowUrl` now routes through the unified `setWindowOptions` → `POST /api/windows/{windowId}/options` (`{"@rk_url": url}`) instead of the removed `PUT /url`; `setWindowColor`/`updateWindowType` likewise delegate to `/options`. E2E specs migrated to the 2-segment shape with sibling `.spec.md` updates (`sidebar-window-sync`, `multi-server-sidebar`, `mobile-touch-scroll`). (jad6)

## 2026-05-29
- **Update** [architecture](/run-kit/architecture.md) — refactor(windows): identify windows by stable tmux window ID (@N) instead of mutable index (#204)
- **Update** [architecture](/run-kit/architecture.md) — rk-daemon-cli-restructure (5zvv)
- **Update** [architecture](/run-kit/architecture.md) — **Window-ID routing (index→`@N` addressing migration)** — Window *addressing identity* migrated from the mutable tmux window index to the stable window ID (`@N`) across URL, HTTP API, WS relay, and tmux targets; the index is retained only for positional reorder/move, and sessions are still identified by name. URL: `/$server/$session/$windowId` (router percent-encodes `@`). New `validate.ValidateWindowID(id, label)` (`^@[0-9]+$`) — stricter than `ValidateName`, the security boundary for the new path param (constitution §I). HTTP: window-targeting routes moved to `/api/windows/{windowId}/...` (kill, move, move-to-session, rename, color, url PUT, **new** type PUT, keys, select, split, close-pane); create stays `POST /api/sessions/{session}/windows`. `api/windows.go` `parseWindowIndex` → `parseWindowID(r) (string, bool)` (`400` on malformed before any tmux call). `TmuxOps` + `internal/tmux` window methods take `windowID string` (no session:index target string); new `ResolveWindowSession(ctx, server, windowID)` via `display-message -p '#{session_name}'`; `MoveWindow(windowID, dstIndex, server)` resolves the source index internally then bubble-swaps; `MoveWindowToSession(windowID, dstSession, server)` runs `move-window -s <windowID> -t <dstSession>:` (tmux preserves the ID across both). WS relay route `/relay/{windowId}?server=...` resolves the owning session from the ID before creating the grouped ephemeral (malformed → `400` pre-upgrade; unknown → close `4004`); grouped-session mechanism otherwise unchanged. `internal/sessions`: pane-map enrichment + `dedupEntries` re-keyed from `session:index` to windowID (the index→ID translation built from the live `WindowInfo` snapshot, since `fab pane map` emits only session+window_index); `ProjectRoot(ctx, windowID, server)`. Frontend `api/client.ts` window-targeting fns take `windowId: string`, build `/api/windows/${encodeURIComponent(windowId)}/...`, keep `server` first; relay WS URL uses windowId. Old index-based bookmarked URLs are a **hard break** (constitution §II). Tmux-authoritative selection model (260528-nvlp) preserved — only the compared identifier changed (windowId vs `String(index)`). (chgz)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — refactor(windows): identify windows by stable tmux window ID (@N) instead of mutable index (#204)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Window addressing migrated from mutable index to stable window ID (`@N`). New § Window Addressing Identity documents the cross-layer contract: window-targeting tmux commands pass the windowID directly as `-t` (no `session:index` string); the index is retained only for positional reorder/move; sessions are still name-identified. `internal/tmux` window methods take `windowID string` — `SelectWindow`/`RenameWindow`/`SendKeys`/`KillWindow`/`SplitWindow`/`KillActivePane`/`SetWindowColor`/`SetWindowOption` etc. New `ResolveWindowSession(ctx, server, windowID)` (`display-message -p '#{session_name}'`) and `resolveWindowSessionIndex` (adds `#{window_index}` for positional ops). `MoveWindow(windowID, dstIndex, server)` resolves source index then bubble-swaps via adjacent `swap-window`; `MoveWindowToSession(windowID, dstSession, server)` runs `move-window -s <windowID> -t <dstSession>:` — both preserve the window's `@N` (tmux contract, same as the boards pin-stability invariant). WS relay route `/relay/{windowId}` resolves the owning session from the ID before building the grouped ephemeral (malformed → 400 pre-upgrade; unknown → close 4004); grouped-session mechanism otherwise unchanged. HTTP window-targeting routes moved to `/api/windows/{windowId}/...` (create stays session-scoped); new `validate.ValidateWindowID` (`^@[0-9]+$`). `internal/sessions` pane-map enrichment + `ProjectRoot` re-keyed to windowID. Frontend `api/client.ts` window-targeting fns take `windowId` as the 2nd positional arg (server first) per the routing contract. (chgz)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — `rk reaper` operator command — a top-level sibling of `rk serve` (registered in `cmd/rk/root.go`, thin command in `cmd/rk/reaper.go`, logic in `internal/tmux/reaper.go`), NOT wired into startup and NOT replacing `sweepOrphanedRelaySessions`. Reaps three leak shapes in `/tmp/tmux-{uid}/` by iterating RAW candidates via the extracted `ScanSocketDir` (NOT `ListServers`, which drops dead sockets): (a) live orphan test servers → `KillServer`; (b) dead test sockets → `os.Remove`; (c) stale `*.lock` sockets → `os.Remove`. Pure `classifyReap(name, probeAlive) ReapAction` (`ReapActionSkip`/`Kill`/`Remove`) with branch order `.lock` → `_rk-ctl` anchor skip → `IsGoTestServerName`(+probe) → skip; `ReapTestServers(ctx, dryRun)` is the I/O entry, `reapCandidates` the temp-dir test seam. No PID-liveness gate — all five test prefixes (incl. fixed-name `rk-tmuxctl-test`/`rk-daemon-test`) and `.lock` files reap unconditionally; `rk-e2e-*` excluded for free (not in `IsGoTestServerName`), `_rk-ctl` and live non-test servers excluded explicitly. `--dry-run` previews by action and mutates nothing; default prints count/names. Per-entry `slog` log-and-skip + aggregate error mirrors the startup sweep. **Complements `260529-wtg4`'s automatic PID-gated test-socket pre-sweep (`TestMain`): that prevents new leaks during test runs; `rk reaper` is the manual, unconditional janitor for cruft already accumulated on disk.** Refactor: the socket-dir scan/probe inlined in `ListServers` was extracted into shared `socketDirPath()`/`ScanSocketDir(ctx)`/`filterSocketEntries()`/`probeServerAlive(ctx, name)` helpers (single source for the `/tmp/tmux-{uid}` convention); `ListServers` behavior preserved (probe-success-only, sorted). (fww2)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor(windows): identify windows by stable tmux window ID (@N) instead of mutable index (#204)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Window-ID routing** — URL window segment migrated from mutable tmux window *index* to the stable window ID (`@N`): route is now `/$server/$session/$windowId` (`$session` is display-only; router percent-encodes `@` → `%402`). Mount-time alignment and URL writeback compare `activeWindow.windowId` vs the URL `@N` (was `String(index)` vs `windowIndex`) — eliminates the index-shift phantom-navigation race; `selectWindow(server, windowId)` drops the session arg. `WindowRow.isSelected` is now `hasUrlWindow ? currentWindowId === win.windowId : (!ghost && win.isActiveWindow)` (URL window ID primary when present, `isActiveWindow` fallback otherwise — inverts the prior isActiveWindow-primary/URL-fallback ordering, since the URL is now itself written back from `isActiveWindow`). Top-bar "current" detection and breadcrumb hrefs compare/build `windowId`; window/iframe-URL/split/close-pane/rename actions call client fns by `windowId` (`/api/windows/{windowId}/...`). Window-move palette actions navigate back to the same `windowId` (preserved across reorder). Nearest-window-after-kill picks a list-position neighbor and targets its `windowId` (no index-distance arithmetic). Old index-based bookmarked URLs are a hard break (no redirect). Sidebar drag-reorder optimistic action calls `moveWindow(server, srcWindowId, dstIndex)`. (chgz)

## 2026-05-28
- **Update** [architecture](/run-kit/architecture.md) — fix(sync): event-driven active window via tmux control mode (#198)
- **Update** [architecture](/run-kit/architecture.md) — fix(daemon): port-based liveness, stale-socket reap, startup logging (#197)
- **Update** [architecture](/run-kit/architecture.md) — **`rk daemon` CLI restructure** — Daemon lifecycle moved from flag-on-serve (`rk serve -d`/`--restart`/`--stop`) to a top-level `rk daemon` subcommand tree (sibling of `rk serve`, not nested). New files in `app/backend/cmd/rk/`: `daemon.go` (parent), `daemon_start.go`, `daemon_stop.go`, `daemon_restart.go`, `daemon_status.go`, `daemon_portowner.go` (shared `lsof`→`ss` fallback port-owner helper with `exec.CommandContext` 5s timeouts + `syscall.Kill` SIGTERM→5s poll→SIGKILL escalation, no shell strings). `--force` on `start`/`stop`/`restart` overrides the port-claim safety check by SIGTERMing a non-daemon port owner; `start --force` is lazy (only triggers on the port-in-use error path), `stop --force` probes the port after stopping, `restart --force` proactively clears non-daemon holders between stop and start. All three refuse to kill the daemon itself when the holder PID matches the new `daemon.InnerServePID()` helper (tmux `pane_pid` query via `tmux -L rk-daemon list-panes -t =rk-daemon:=serve -F '#{pane_pid}'` through the new `runTmuxOutput` capturing variant of `runTmux`). `rk daemon status [--json]` is read-only — reports daemon state (running / not running, socket, session, window, target) AND port owner (`free` / `held-by-daemon` / `held-by-other`); rejects `--force` via cobra's unknown-flag error. JSON form: `{daemon: {running, socket?, session?, window?, target?, pid?}, port: {host, port, state, holder_pid?, holder_command?}}`. Hard break on the three removed flags — no deprecation forwarders; `rk serve -d` etc. surface cobra's `unknown flag` error. `cmd/rk/serve.go` retains the `internal/daemon` import only for `daemon.LogEnvVar` (PR #197 single-source-of-truth). Naming collision with the existing `rk status` (tmux session summary) is documented in `rk daemon status --help`. `internal/daemon` public API preserved verbatim except for the additive `InnerServePID()` export; `upgrade.go`'s brew-upgrade auto-restart flow (`daemon.RestartWithBinary`) is untouched. Tests: `cmd/rk/daemon_test.go` (cobra subcommand registration, JSON shape, `--force` self-kill refusal via injected stubs, `findPortOwnerImpl` integration gated on `lsof` availability); `internal/daemon/daemon_test.go` extended with `InnerServePID` no-session and running-session cases. Constitution touchpoints: §I (all subprocess via `exec.CommandContext` + `context.WithTimeout`, signal delivery via `syscall.Kill`), §III (existing `Start`/`Stop`/`Restart`/`IsRunning` re-used unchanged), §IV (net flag-surface reduction — three flags removed, one verb with four children added). (5zvv)
- **Update** [architecture](/run-kit/architecture.md) — **Active window sync via tmux control mode** — New package `app/backend/internal/tmuxctl/` owns long-running `tmux -CC -L <socket> attach-session -t =<bootstrap> -r` subscriptions, one Client per tmux server. `Client.Open/Close/Generation/Wait` plus pure `ParseLine` for control-mode notifications (`%session-window-changed`, `%window-add`, `%window-close`, `%window-renamed`, `%sessions-changed`, `%layout-change`) and framing markers. Reconnect FSM with 250ms → 500ms → 1s → 2s → 5s capped backoff and reset on first non-`%begin` event after reconnect. `Supervisor` owns `map[socket]*Client` guarded by `sync.Mutex`, fsnotify-watches `$TMUX_TMPDIR` or `/tmp/tmux-<euid>/` (created with `os.MkdirAll(dir, 0o700)` if missing), and Open/Close-on-create/remove. Hidden `_rk-ctl` anchor session is created via `tmux new-session -d -s _rk-ctl` when a tmux server has zero existing sessions (`tmux -CC` requires an attached session); tagged with server-scoped user-option `@rk_ctl_keepalive 1` (defensive marker, no v1 consumer). New exported `tmux.ControlAnchorSessionName = "_rk-ctl"` filters the anchor from `parseSessions` (single chokepoint mirroring the `rk-relay-*` skip) and from `sweepOrphanedRelaySessions` (defense-in-depth guard). SSE loop in `api/sse.go` refactored to be event-driven via `select` over per-server `subscriber.Wait(after)` channels + safety-net `time.NewTimer(safetyIntervalEffective())`. Three constants: `safetyPollInterval = 12 * time.Second` (production cadence when a subscriber is wired), `legacyPollInterval = 2500 * time.Millisecond` (no-subscriber fallback for PTY-unavailable hosts and tests), `metricsPollInterval = 2500 * time.Millisecond` (decoupled from SSE intervals). New `WindowChangeSubscriber` interface in `api/sse.go`; production adapter `supervisorSubscriber` (`api/tmuxctl_bridge.go`) forwards `Generation`/`Wait` to `Supervisor.Get(server)` and returns a never-closing channel via `neverChan()` when no Client exists for a server (avoids busy-looping when control-mode is unavailable). `hubSink` shipped here as a no-op `tmuxctl.EventSink` (the hub observed change via the Client's generation counter) — *superseded by `260530-v6hm-active-window-event-derivation`, which made `hubSink` record per-group active-window state into a per-Client `ActiveWindowTracker`; the generation-counter path is unchanged*; `Server.SetWindowChangeSubscriber(sub)` lazy-initialises the hub and stores the subscriber. `detectKilledWindowIDs(prev, current)` extracted as a pure function in `api/sse.go` and called from the snapshot-build site (no longer inline in the poll loop); `previousWindowIDs[server]` initialised on the first snapshot per server so cold-start emits no synthetic `board-changed { cleanup }` events. `selectFirst` reads `timer.C` in the outer select to avoid a goroutine-leak where the timer would otherwise block on `Stop`-without-drain. Heartbeat moved to wall-clock cadence (`sseHeartbeatPeriod = 15s`) so the slower safety interval doesn't starve heartbeats. `cmd/rk/serve.go` orders startup as `tmux.EnsureConfig → sweepOrphanedRelaySessions → tmuxctl.NewSupervisor + Start → server.ListenAndServe`; sweep before subscribe so the `_rk-ctl` anchor isn't observed as an orphan. Graceful shutdown stops the Supervisor with a 5s bounded context BEFORE `server.Shutdown`; stop errors logged at `slog.Warn` and never block shutdown. New dep: `github.com/fsnotify/fsnotify`. Constitution alignment: §I (`exec.CommandContext` with argument slices; no shell strings), §II (no persistent state — Supervisor map mirrors kernel-observable sockets), §VI (passive observation, never mutates user sessions), no new network surface. (nvlp)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix(sync): event-driven active window via tmux control mode (#198)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — `_rk-ctl` hidden anchor session for tmuxctl control mode. New exported `tmux.ControlAnchorSessionName = "_rk-ctl"` constant (alongside `RelaySessionPrefix` in `internal/tmux/tmux.go`); `parseSessions` early-skips any line whose `#{session_name}` equals it — single chokepoint mirroring the existing `rk-relay-*` skip, so every consumer (REST `/api/sessions`, SSE `event: sessions`, board derivation, server-aggregate `/api/servers`) automatically excludes the anchor. `app/backend/internal/tmuxctl/Client` creates the anchor via `tmux [-L <socket>] new-session -d -s _rk-ctl` when the target tmux server has zero pre-existing sessions (`tmux -CC` requires an attached session to emit notifications), then tags it with the server-scoped user-option `@rk_ctl_keepalive 1` via `set-option -t =_rk-ctl @rk_ctl_keepalive 1` (exported as `tmuxctl.AnchorKeepaliveOption`; defensive marker only, no v1 runtime consumer). Concurrent-rk "duplicate session" error treated as benign by `isDuplicateSessionError`. The Client invokes `tmux -CC attach-session -t =<bootstrap> -r` with the anchored `=` target form (PR #196 convention) and the `-r` read-only flag (restricts input, notifications still emit). `sweepOrphanedRelaySessions` is excluded by construction (matches only the `rk-relay-` prefix), with an explicit `name == ControlAnchorSessionName` guard added for defense-in-depth. Anchor outlives any single Client lifetime; only `tmux kill-server` removes it. (nvlp)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix(sync): event-driven active window via tmux control mode (#198)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — URL as resumable bookmark; tmux is the truth for current window. New § URL as Resumable Bookmark documents the post-change model: tmux drives selection; the URL is consulted on mount only and rewritten by an SSE-driven `navigate({ replace: true })` whenever `currentSession.windows.find(w => w.isActiveWindow).index` differs from `windowIndex`. Mount-time alignment in `app.tsx` fires exactly one `selectWindow(server, session, Number(urlWindow))` per session-route mount, guarded by `hasAlignedToUrlRef: useRef(false)` plus `lastAlignedSessionRef` (resets the guard when `${server}|${sessionName}` changes). `navigateToWindow(session, windowIdx)` simplified to call `selectWindow` only — no `navigate` at click time; URL follows on the next SSE snapshot (typically sub-500ms via the new tmuxctl control-mode subscription). The 3-second `userNavTimestampRef` / `elapsed < 3000` debounce was deleted entirely (it was protecting client-owned selection state that no longer exists). `dialogOpenRef` retained so the URL writeback effect skips navigation mid-dialog (prevents focus-stealing re-renders). `WindowRow.isSelected` derives from `(!ghost && win.isActiveWindow) || currentWindowIndex === String(win.index)` — primary driver is the server-reported `isActiveWindow`; URL fallback covers ghost windows mid-creation and the initial render before the first SSE payload. Multi-client convergence: same-server tabs yank to the new window via the writeback effect; board-route tabs do NOT navigate; different-server tabs are independent. Stale-URL deep-link tabs yank existing tabs as a deliberate side effect of mount-time alignment. Supersedes the 2026-03-07 `history.replaceState` + SSE-polling sync model. (nvlp)

## 2026-05-17
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Couple Server Pane expand state with Sessions Pane scope (#195)

## 2026-05-10
- **Update** [architecture](/run-kit/architecture.md) — refactor(frontend): pane boards + multi-server sidebar + rotated shell + sidebar polish (#192)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — refactor(frontend): pane boards + multi-server sidebar + rotated shell + sidebar polish (#192)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor(frontend): pane boards + multi-server sidebar + rotated shell + sidebar polish (#192)

## 2026-05-09
- **Update** [architecture](/run-kit/architecture.md) — **Rotated shell layout (CSS Grid topology)** — Shell wrapper at `app/frontend/src/components/shell/shell.tsx` shared by `AppShell` and `BoardPage`. Desktop grid `"sidebar topbar" / "sidebar content" / "sidebar bottombar"` (sidebar full-height, spans 3 rows); columns `${sidebarWidth}px 1fr` ↔ `0 1fr` with `grid-template-columns 150ms ease-out`. Mobile (`< 640px`) collapses to single-column grid; sidebar renders as a grid-row-spanning overlay (`gridRow: "2/4"`, `position: absolute` not `fixed`) with backdrop, `role="dialog" aria-modal="true"` — preserves topbar visibility per existing project convention. Hamburger statically rendered at `TopBar.left` in both states (no component-level relocation; visual movement is a side effect of the column collapse). `Cmd+\` / `Ctrl+\` toggles `sidebarOpen` (registered in `KeyboardShortcuts` modal); chord suppressed on real text inputs but NOT on xterm helper textareas. `ChromeContext` shape changed: `sidebarOpen`/`sidebarWidth` lifted from per-route state; `drawerOpen`/`setDrawerOpen` removed (single boolean covers both desktop column and mobile overlay). New dispatch surface `setSidebarWidth` (in-memory, per-pointermove) + `persistSidebarWidth` (one localStorage write at drag-end) splits live updates from persistence. New `FocusedTerminalContext` (`app/frontend/src/contexts/focused-terminal-context.tsx`) tracks the focused terminal across routes — `TerminalClient` registers on mount via `registerFocus` prop, `BoardPane` registers on focus events (click, cycle, initial pane). `BottomBar` moves up to shell-level (rendered once per route by both AppShell and BoardPage); `wsRef` prop removed in favor of `focused?.wsRef` from context. BoardPage gets a BottomBar for the first time, byte-identical to AppShell's invocation. `composeOpen` lifted to `FocusedTerminalContext` so the shell-level BottomBar can open compose for the focused pane on any route; the focused pane's `TerminalClient` gates `<ComposeBuffer>` rendering on `isFocused && composeOpen`. Compose target frozen at open-time via existing `wsRef` snapshot. `TopBar` accepts `mode: "terminal" | "board" | "root"` (default `"terminal"`); board mode renders `Board ▸ {name} ▾` breadcrumb (the existing `BoardSwitcherDropdown`, now imported by TopBar) + inline info `{N} pane[s] · {M} server[s] · ⌘[⌘] cycle` (`hidden sm:inline`). `FixedWidthToggle` lifted out of the `currentWindow &&` block — now route-agnostic. BoardPage's bespoke `h-screen w-screen flex` root and inline mini-header are gone. Provider tree order is now `ThemeProvider > ChromeProvider > RootWrapper(SessionProvider > OptimisticProvider > FocusedTerminalProvider > Outlet)`. Frontend-only — backend untouched. (17m3)
- **Update** [architecture](/run-kit/architecture.md) — **Multi-server SessionProvider + unified Sidebar** — `SessionProvider` (`app/frontend/src/contexts/session-context.tsx`) promoted from a single-server, layout-mounted provider into a root-mounted multi-server provider. State shape switched to per-server keyed `Map`s: `sessionsByServer`, `sessionOrderByServer`, `isConnectedByServer`, `metricsByServer`, plus `currentServer: string | null` dispatched from the matched route via `useMatches()` (`params.server` for `/$server/...`, `null` for `/board/$name` and `/`). One `EventSource` per server in the pool — current server eager, others via `attachServer(name)`; per-server 3-second disconnect debounce; pool entries closed and map keys removed when `/api/servers` drops a server. `subscribeBoardChange` API multiplexes cross-server `board-changed` events through the same pool so `useBoards` and `useWindowPins` consume them without opening their own connections. Provider mounts in `RootWrapper` above `<Outlet />`; `ServerShell` becomes a thin layout (its `useMatches()` server extraction is consumed only inside AppShell). Sidebar (`app/frontend/src/components/sidebar/index.tsx`) renders one `ServerGroup` per server in `servers` (each a `CollapsiblePanel`-style collapsible) with the current server's group default-open and others default-collapsed; toggles persist per-server in `localStorage["runkit-panel-sessions-{server}"]` (best-effort migration of the legacy `runkit-panel-sessions` key into the current server's slot on first read when `currentServer` is non-null). Current-server header carries the Server panel's selected-tile shade (driven by `serverColors` + `rowTints`); no marker on board routes. Cross-server window navigation: clicking a session/window in a non-current server's tree calls `navigate({ to: "/$server/$session/$window", params: { server: thatServer, ... } })`. Cross-server window drag-and-drop is rejected with toast `"Moving windows across tmux servers isn't supported yet"` (within-server reorder/move unchanged). BoardPage (`app/frontend/src/components/board/board-page.tsx`) drops its mini-sidebar (former `<aside>` lines 202-234) and renders the unified `<Sidebar currentServer={null} ...>`; the `← Sessions` back-link is removed (sessions are reachable via the unified sidebar's per-server groups or the board top-bar `▾` switcher). Provider tree order is now `ThemeProvider > ChromeProvider > RootWrapper(SessionProvider > OptimisticProvider > Outlet)`. All 9 `useSessionContext` consumers consume the keyed shape directly — no transitional accessor remains. Tests: `session-context.test.tsx` rewritten with multi-instance `MockEventSource` (keyed by URL) for per-server SSE isolation and lazy attach assertions; `sidebar.test.tsx` updated to the new prop surface and per-server group expectations; new `multi-server-sidebar.spec.ts` Playwright e2e + companion `.spec.md` spawns a second `rk-e2e-multi-{digits}` tmux server and asserts both render in the sidebar with cross-server navigation. Backend untouched (already per-server). (dc0t)
- **Update** [architecture](/run-kit/architecture.md) — **Relay grouped sessions for board panes** — `app/backend/api/relay.go` now creates a per-WebSocket ephemeral grouped session (`rk-relay-<8 hex>` from `crypto/rand`, via the new `tmux.NewGroupedSession` wrapper around `tmux new-session -d -s <ephemeral> -t <real>`) and attaches to the ephemeral rather than the real session. `defer s.tmux.KillSessionCtx(context.Background(), server, ephemeral)` reaps the ephemeral on every exit path (fresh `context.Background()` because `r.Context()` is cancelled at cleanup time). Fixes the central board bug — N panes targeting different windows of one tmux session now show distinct content because grouped sessions share window membership but maintain independent active-window state — and as a side effect fixes a latent two-tabs-same-session focus-stealing bug. New `tmux.NewGroupedSession`, `tmux.KillSessionCtx`, `tmux.RelaySessionPrefix = "rk-relay-"`, and `tmux.ListRawSessionNames` (unfiltered list-sessions for housekeeping callers); `parseSessions` filters `rk-relay-*` so ephemerals never leak into user-facing session lists at the single chokepoint. `TmuxOps` interface gains `NewGroupedSession` and `KillSessionCtx`; `mockTmuxOps` updated. Startup sweep `sweepOrphanedRelaySessions(ctx)` in `app/backend/cmd/rk/serve_sweep.go` reaps orphan ephemerals across every server returned by `tmux.ListServers(ctx)` before HTTP bind (synchronous, 30s bounded, per-server failures logged and skipped). New tests: `TestNewGroupedSession_*`, `parseSessions` filter cases, `app/backend/api/relay_test.go` (two-relay-distinct-output, missing-session-4004, ephemeral-cleanup-on-close), `boards-same-session-multi-pane.spec.ts` Playwright e2e + companion `.spec.md`. WebSocket URL contract `/relay/{session}/{window}?server={server}` unchanged — ephemeral name is purely backend-internal. (hdjr)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Per-WebSocket ephemeral grouped sessions for the relay. New `tmux.NewGroupedSession(ctx, server, realSession, ephemeral)` wraps `tmux new-session -d -s <ephemeral> -t <realSession>` (with a `has-session` probe to avoid leaking empty-group sessions when the target is missing), `tmux.KillSessionCtx(ctx, server, session)` exposes a ctx-accepting kill so relay cleanup can pass `context.Background()` (existing `KillSession` becomes a thin wrapper), and `tmux.RelaySessionPrefix = "rk-relay-"` reserves the name space. `parseSessions` early-skips any `rk-relay-*` line — single chokepoint for all user-facing session lists (REST, SSE, board derivation, server-aggregate). New `tmux.ListRawSessionNames(ctx, server)` returns the unfiltered list for housekeeping callers (the startup sweep, which would otherwise be unable to see the very ephemerals it needs to reap). Relay handler (`api/relay.go`) generates the ephemeral name via `crypto/rand` (constitution I), creates the grouped session, defers `KillSessionCtx(context.Background(), …)` (request context is cancelled at cleanup time), then `select-window` and `attach-session` against the ephemeral — never against the real session. Fixes the central bug where N board panes targeting different windows of one tmux session showed identical content (tmux's per-session active-window state); fixes a latent two-tabs-same-session focus-stealing bug as a side effect. Startup sweep `sweepOrphanedRelaySessions(ctx)` in `cmd/rk/serve_sweep.go` reaps orphans across all `tmux.ListServers(ctx)` before HTTP bind (synchronous, 30s bounded; per-server failures logged and skipped). `TmuxOps` interface gains `NewGroupedSession` and `KillSessionCtx`; `mockTmuxOps` updated. WebSocket URL contract unchanged. (hdjr)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — **Rotated shell layout** — Shared `<Shell>` CSS Grid wrapper (`app/frontend/src/components/shell/shell.tsx`) used by AppShell and BoardPage; desktop topology `"sidebar topbar" / "sidebar content" / "sidebar bottombar"`, sidebar full-height. Sidebar collapses to 0px (no rail) with `grid-template-columns 150ms ease-out`; drag-resize handle hidden when collapsed. Hamburger statically at TopBar.left; visual relocation on collapse is a side effect of column collapse. `Cmd+\` / `Ctrl+\` toggles sidebar from any Shell-bearing route (registered in KeyboardShortcuts modal); chord suppressed on real text inputs but NOT on xterm helper textareas. Mobile (< 640px) collapses to single-column grid; sidebar renders as `position: absolute` overlay positioned via `gridRow: "2/4"` (below topbar, matches project convention) with `role="dialog" aria-modal="true"`. Sidebar section order is **Boards → Servers → Sessions** (was Servers → Boards → Sessions); BoardsSection is always visible with `Pin a window to start a board` hint when empty (was hidden when empty). New `FocusedTerminalContext` (`app/frontend/src/contexts/focused-terminal-context.tsx`) tracks focused terminal across routes; mounts in `RootWrapper`; producers are `TerminalClient` (registers via `registerFocus` prop) and `BoardPane` (registers on focus events, doesn't clear on focus loss). Compose state (`composeOpen`/`setComposeOpen`) lifted to the same context so the shell-level BottomBar can open compose for the focused pane on the board route; focused pane gates `<ComposeBuffer>` rendering on `isFocused && composeOpen`. BottomBar moves up to `<Shell>`'s `bottombar` grid area — rendered once per route by AppShell and BoardPage (board route gets a BottomBar for the first time). `BottomBar.wsRef` prop removed; reads `focused?.wsRef` from context. `TopBar` accepts `mode: "terminal" | "board" | "root"` prop; board mode renders breadcrumb + inline-info `{N} pane[s] · {M} server[s] · ⌘[⌘] cycle` (hidden on `< 640px`). FixedWidthToggle lifted out of the `currentWindow &&` block — now route-agnostic. `ChromeContext` shape: `sidebarOpen`/`sidebarWidth` lifted from per-route state; `drawerOpen`/`setDrawerOpen` removed. New `setSidebarWidth` (in-memory) + `persistSidebarWidth` (localStorage at drag-end) split. BoardPage's bespoke `h-screen w-screen flex` root and inline mini-header are gone; BoardSwitcherDropdown moves into TopBar board-mode rendering. Three pattern captures: shared chrome wrapper for related routes (`<Shell>`), lift focused-input target to context, two-setter pair for in-memory + persisted state. (17m3)

## 2026-05-07
- **Update** [architecture](/run-kit/architecture.md) — feat(sidebar): persist session reorder to tmux user-option (#185)
- **Update** [architecture](/run-kit/architecture.md) — **Pane boards** — Cross-server pane dashboard at `/board/$name` (root-level peer to `/$server`, not under server). New `internal/tmux/board.go` (parse/serialize `@rk_board`, fractional indexing, lazy stale cleanup, idempotent `Pin`/`Unpin`/`Reorder`, `RemoveAllByWindowID`). New `api/boards.go` with five endpoints (`GET /api/boards`, `GET /api/boards/{name}`, `POST /api/boards/{name}/{pin,unpin,reorder}`); routes registered in `router.go`; `TmuxOps` extended; `mockTmuxOps` updated. `sseHub` extended with `BoardEntriesFetcher` interface, `previousBoardJSON` cache, `broadcastBoardChanged`, `event: board-changed` (kebab-case), once-per-server bootstrap on first poll, cached-snapshot replay to new clients, and window-kill eager cleanup that fires one `cleanup` event per affected board. Frontend: `app/frontend/src/api/boards.ts` (`listBoards`/`getBoard` via `deduplicatedFetch`, `pinWindow`/`unpinWindow`/`reorderPin` mutations with `server` first-arg per the routing contract); `useBoards()` + `useBoardEntries(name)` hooks in `app/frontend/src/hooks/use-boards.ts` (50ms-debounced cross-server SSE re-fetch, error tolerance preserves last good value); `usePinActions`, `usePaneWidths`, `useIsMobile`, `useActiveBoardName`, `useWindowPins` companion hooks; `BoardPage`/`BoardPane`/`BoardHeader` mini-layout (own sidebar + topbar + `<CommandPalette>` mount); `/board/$name` route in `router.tsx` peer to `serverLayoutRoute`. Sidebar `BoardsSection` (above Sessions, hidden when zero boards), pin icon + popover on `WindowRow`, active-board accent border on pinned rows when current route is `/board/<name>`. `boardActions` `PaletteAction[]` in `app.tsx` between `windowActions` and `viewActions` (Switch/Pin/Unpin Current Window only on AppShell; Switch + Leave + Cycle Pane Focus on the BoardPage palette mount; Reorder Pane deferred to v1.1). Pane widths persisted per-board to `localStorage["runkit:board-widths:<name>"]`; pin state persists in tmux. Mobile: single-pane swipe carousel below `min-width: 640px`. (4vuv)
- **Update** [architecture](/run-kit/architecture.md) — **Server-persisted sidebar session order** — Sidebar session reorder now lives in tmux server-scoped user-option `@rk_session_order` (JSON-encoded array of session names), not localStorage, so the order syncs across all browser tabs/devices connected to the same tmux server. New `tmux.GetSessionOrder(ctx, server)` / `tmux.SetSessionOrder(ctx, server, order)` wrappers in `internal/tmux/tmux.go` (both apply `context.WithTimeout(ctx, TmuxTimeout)` and route through `tmuxExecRawServer`, which now also captures stderr in error returns so callers can pattern-match on tmux's "invalid option" / "no server running" diagnostics — `CapturePane` was the only other caller; transparent change). New endpoints `GET /api/sessions/order` (returns `{order:[...]}` — empty on unset/no-server) and `PUT /api/sessions/order` (validates each name via `validate.ValidateName`, persists, then synchronously broadcasts a new `event: session-order` SSE frame). SSE hub (`api/sse.go`) gains `previousOrderJSON` cache, `broadcastSessionOrder(server, order)` method, an `addClient` cached-snapshot send (positioned between `sessions` and `metrics`), and a once-per-server bootstrap in `poll()` that reads tmux via an injected `SessionOrderFetcher` interface (production wires `prodSessionOrderFetcher{}`; tests substitute a stub). `TmuxOps` interface gains the two methods; `mockTmuxOps` updated accordingly. Frontend: new `getSessionOrder` / `setSessionOrder` API client methods (first-arg `server` per the routing contract), `SessionContext` extended with a `sessionOrder: string[]` slice populated from the SSE event, sidebar (`sidebar/index.tsx`) implements drag-and-drop reorder with custom MIME `application/x-session-reorder` (so it doesn't collide with the existing window-drag-to-session feature), 250ms trailing-debounced PUT (one HTTP call per drag op), and a `localOrder` override that survives until the next SSE-delivered `sessionOrder` change (so the UI doesn't snap back to the pre-PUT order during the round-trip). `SessionRow` gains optional `draggable` / `isDragSource` / `onDragStart` / `onDragEnd` props (`opacity-50` while dragging; ghost/optimistic sessions are NOT draggable). `StandaloneSessionContextProvider` added for tests so existing `Sidebar` tests don't need to wrap with the EventSource-opening `SessionProvider`. New tests: `TestGetSessionOrder_*` / `TestSetSessionOrder_*` (5 tmux integration tests, gated on `command -v tmux`); `TestSessionOrder_GET_*` / `TestSessionOrder_PUT_*` (8 handler tests including `_triggersBroadcast` end-to-end via direct hub injection); `TestSSE_BroadcastSessionOrderReachesMatchingClients` / `_SessionOrderCachedOnConnect` / `_HubBootstrapReadsOrderOnFirstPoll`; `client.test.ts` extended; `session-context.test.tsx` (4 SSE-handler tests); Playwright e2e `session-reorder.spec.ts` (PUT + reload + assert order survives). Constitution alignment: I (security — names validated, args slice never shell-interpolated), II (no DB — option lives in tmux), III (process timeouts — explicit `context.WithTimeout(ctx, TmuxTimeout)` on every new tmux call), VI (tmux survives rk-go restart — bootstrap closes the cache-cold-start gap), VII (convention over configuration — uses existing `@rk_*` user-option pattern, existing `withServer` first-arg client convention). (lvon)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — feat(sidebar): persist session reorder to tmux user-option (#185)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — `@rk_board` server-scoped option for pane boards. New `internal/tmux/board.go` exposes `ListBoardEntries`, `ListAllBoardEntries`, `ListBoards`, `GetBoard`, `Pin`, `Unpin`, `Reorder`, `RemoveAllByWindowID`, and `ComputeOrderKey`. Distributed storage: each tmux server stores entries only for its own windows; aggregate views read from every server returned by `ListServers()` and union/tag entries with their source server. Value format: comma-separated `<window_id>:<board>:<order_key>` entries (`,` and `:` reserved separators excluded by the board-name regex `^[A-Za-z0-9_-]{1,32}$`). Order keys use lowercase-`a..z` fractional indexing — inserts never renumber. Lazy stale-entry cleanup at read time intersects with `liveWindowIDs(server)` and best-effort writes back the cleaned slice (read still returns success on write-back failure). Eager cleanup via the SSE poll-tick: window-kill detection scans per-server entries and calls `RemoveAllByWindowID`, then broadcasts one `event: board-changed { change: "cleanup" }` per affected board. SSE hub bootstraps `@rk_board` once per server on first poll and emits a synthetic `board-changed { change: "bootstrap", entries: [...] }` so an rk-go restart with tmux still running rehydrates the boards UI (parity with `@rk_session_order`). All wrappers route through `tmuxExecRawServer` with `context.WithTimeout(ctx, TmuxTimeout)`. (4vuv)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat(sidebar): persist session reorder to tmux user-option (#185)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Pane boards UX — new § Boards View covers `/board/$name` self-contained mini-layout (own sidebar + topbar + main pane area + own `<CommandPalette>` mount; not under AppShell because boards span servers). Pane cards: 480px default, drag-resize 280px–viewport-minus-sidebar, widths persisted per-board to `localStorage["runkit:board-widths:<name>"]`. Click-to-focus + `Cmd+]/Cmd+[` cycle (hover-to-focus OFF in v1). Mobile (< 640px): single-pane swipe carousel with off-screen pause via `TerminalClient` unmount + pagination dots. Sidebar gains `BoardsSection` above Sessions (hidden when zero boards, hint mode when on a now-empty board route), pin icon + `PinPopover` on `WindowRow` (filled state when pinned to ANY board), and active-board accent border on rows pinned to the current board. New `boardActions` `PaletteAction[]` block in `app.tsx`: `Switch to <name>` (one per board, `(current)` on active), `Pin Current Window` (window-route-gated, dispatches `pin-popover:open`), `Unpin Current Window` (unpins from all boards), `Leave Board View`, `Cycle Pane Focus →/←` (board-route-gated). Reorder Pane palette action deferred to v1.1; right-click context menu not implemented (use sidebar pin icon, command palette, or board pane header). Hooks: `useBoards`, `useBoardEntries`, `usePinActions`, `usePaneWidths`, `useIsMobile`, `useActiveBoardName`, `useWindowPins`. (4vuv)

## 2026-05-06
- **Update** [rk-riff](/run-kit/rk-riff.md) — fix(rk): rename --fan-out to --count/-N and fix fan-out bugs (#184)
- **Update** [rk-riff](/run-kit/rk-riff.md) — `--count` rename and fan-out correctness fixes. (1) Hard-rename `--fan-out N` → `--count N` (short form `-N`, uppercase) — no alias, no deprecation warning; `--fan-out` is now an unknown flag. Internal renames track the user-facing flag: `riffFanOutFlag` → `riffCountFlag`, `effectiveSpec.FanOut` → `effectiveSpec.Count`, `runFanOut` → `runCount`. The parallelism-mechanic helpers (`fanOutResult`, `planFanOutRollback`, `rollbackFanOut`, `rollbackPlan`) keep their `fanOut`/`Plan` naming because they describe the mechanic, not the flag. (2) Bug fix: pane focus targets the captured pane id from `tmux new-window -P -F '#{pane_id}'` rather than a hardcoded `<window>.0` index, which was wrong on tmux configs with `pane-base-index 1` (every riff invocation printed `can't find pane: 0` on stderr). New helpers: `buildNewWindowCaptureArgs` (pure argv shape), `parsePaneID` (pure stdout parser — single trimmed line), `runTmuxNewWindowCapturePaneID` (subprocess with `tmuxTimeout`, parent context propagation, `tmuxChildEnv()`). `buildSpawnArgvs` no longer emits the trailing `select-pane` row — pane id is a runtime value, so that step is constructed by the orchestrator (`spawnRiffReturningName`). (3) Bug fix: `runWtDelete` now invokes `wt delete --non-interactive <name>` (positional name; deprecated `--worktree-name` removed; `--non-interactive` suppresses the `Delete this worktree?` prompt that previously caused silent rollback failures because rollback runs without a tty). Argv shape extracted to pure helper `buildWtDeleteArgs`. (4) New tests: `TestParsePaneID`, `TestBuildNewWindowCaptureArgs`, `TestRiffCountShortForm` (`-N`/`--count`/`--count=`/default), `TestRiffFanOutFlagRejected` (regression-protect the rename), `TestBuildWtDeleteArgs` (regression-protect the `--non-interactive` + positional-name argv). `TestBuildSpawnArgvs` updated to drop the `select-pane` row expectation. (lald)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix(rk): rename --fan-out to --count/-N and fix fan-out bugs (#184)

## 2026-04-23
- **Update** [architecture](/run-kit/architecture.md) — feat: rk riff — Workflow Features (pane arrays, layouts, presets, fan-out) (#177)
- **Update** [rk-riff](/run-kit/rk-riff.md) — feat: rk riff — Workflow Features (pane arrays, layouts, presets, fan-out) (#177)
- **Update** [rk-riff](/run-kit/rk-riff.md) — refactor: rk riff — CLI Surface Refinement (#175)
- **Update** [rk-riff](/run-kit/rk-riff.md) — fix: rk riff — correctness and portability fixes (#174)
- **Update** [rk-riff](/run-kit/rk-riff.md) — Correctness and portability fixes — no CLI surface change. (1) New pure `shellWrap(cmd)` helper appends `; exec "${SHELL:-/bin/sh}"`; used by both new-window and split paths so panes stay interactive after their commands exit. (2) Launcher now runs under an interactive `${SHELL:-/bin/sh} -i -c '...'` wrap inside `buildNewWindowArgs` so `.zshrc`/`.bashrc` aliases, functions, and interactive-only PATH tweaks reach the Claude Code launcher (closes Bug 3). (3) Split pane replaces the hardcoded `exec zsh` with `shellWrap(setupCmd)` — bash/fish users land in their own shell (closes Bug 8). (4) SIGINT/SIGTERM propagation — `runRiff` wraps its root context via `signal.NotifyContext(cmd.Context(), os.Interrupt, syscall.SIGTERM)` with `defer stop()`, threaded through all three subprocess call sites; Ctrl-C no longer leaves orphan `wt`/`tmux` children (closes Bug 10). (5) Window-name auto-suffix — `listWindowNames(ctx)` probes `tmux list-windows -F '#W'` and `resolveWindowName(existing, base)` (pure, gap-filling) applies `-2`, `-3`, … on collision; TOCTOU race between list and new-window is accepted (closes Bug 11). (6) Added Security / Trust Boundary section documenting `fab/project/config.yaml` as committed-code-equivalent and naming launcher shell expansion as the intentional exception to constitution §I (addresses Bug 9 via docs only — no code mitigation). `buildNewWindowArgs` signature changed to accept the resolved name; new `TestShellWrap` and `TestResolveWindowName` cover the new pure helpers. (ba9f)
- **Update** [rk-riff](/run-kit/rk-riff.md) — Workflow features — pane-array model, layouts, presets, and fan-out. (1) `--skill` and `--cmd` are now repeatable with argv-ordered pane composition; a custom `paneFlag` pflag.Value + argv pre-processor (`rewritePaneSpaceForm`) support bare / space-form / equals-form syntax uniformly. Pane 0 always gets tmux focus via `select-pane -t <window>.0`. (2) `--layout` flag accepts 6 canonical tmux names + 6 shortforms (`a`/`t`/`h`/`v`/`deck-h`/`deck-v`), resolved via `layoutAliases`; `auto` dispatches by pane count via `autoLayout`. Unicode box-drawing mockups render inline in `rk riff -h` via `layout_help.go`. (3) `--setup-pane` removed outright (no alias, no deprecation warning) — `--cmd` subsumes it. (4) Presets under `riff.presets.<name>` in `fab/project/config.yaml` invokable positionally (`rk riff ship`) or via `--preset`; panes are a typed ordered list of `{skill|cmd: value}` entries; CLI panes replace preset panes entirely; CLI layout overrides preset layout; preset `wt_args` are prepended to user passthrough. Preset with a pane having both `skill` and `cmd` keys is silently discarded (best-effort posture matching `ReadSpawnCommand`). (5) `--list-presets` short-circuits before preconditions and prints presets in YAML source order. (6) `--fan-out N` spawns N parallel worktree/window pairs sharing one pane shape; failure triggers rollback via `wt delete` + `tmux kill-window` for the non-failing goroutines' artifacts (computed by pure `planFanOutRollback`). `fabconfig.ReadPresets` + `ReadPresetsOrdered` added. New pure helpers: `resolveActivePreset`, `resolveEffectiveSpec`, `buildSpawnArgvs`, `buildSkillShellString`, `buildCmdShellString`, `planFanOutRollback`. (jmwu)
- **Update** [rk-riff](/run-kit/rk-riff.md) — CLI surface refinement — hard-rename flags and expand help text, no behavioral change. (1) `--cmd` renamed to `--skill` (hard-rename, no deprecation alias — invocations using `--cmd` fail with cobra's "unknown flag" error). (2) `--split` renamed to `--setup-pane` (hard-rename, no deprecation alias). (3) Package-level Go variables renamed in lockstep: `riffCmdFlag` → `riffSkillFlag`, `riffSplitFlag` → `riffSetupPaneFlag`. (4) `Use:` synopsis expanded from `riff [-- wt-flags...]` to `riff [--skill <name>] [--setup-pane <cmd>] [-- <wt-flags>...]`. (5) `Long:` help expanded to match `serve.go` house style. (6) Bug 2 investigation note — verified positional argv to `claude` dispatches slash-commands correctly. (udhe)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — feat: rk riff — Workflow Features (pane arrays, layouts, presets, fan-out) (#177)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix: rk riff — correctness and portability fixes (#174)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — `rk riff` window creation now multi-pane + fan-out. Each window is built by a `tmux new-window` + N-1 `tmux split-window` sequence, followed by `tmux select-layout <canonical>` and `tmux select-pane -t <window>.0` (focus pane 0 per the argv-order rule). Skill panes preserve the three-layer `<launcher> '<escaped-skill>'` → `sh -i -c` → `shellWrap` composition from earlier changes; cmd panes skip the interactive wrap (would alter user-command argv). Fan-out with N ≥ 2 spawns N goroutines sharing one context; failure triggers rollback via the pure `planFanOutRollback(results, failureIdx)` builder + `wt delete` / `tmux kill-window` calls against a fresh (non-cancelled) context so rollback runs to completion. Worktree names come from `wt`'s generator (no rk-side `-1..-N` scheme); windows named `riff-<wt-basename>` with `resolveWindowName` suffix fallback on collision. `--setup-pane`/`--split` removed entirely — `--cmd` (repeatable) subsumes the single-split use case. (jmwu)

## 2026-04-20
- **Update** [architecture](/run-kit/architecture.md) — fix: Visual Display Recipe documents relative proxy URL for rk iframes (#170)
- **Update** [architecture](/run-kit/architecture.md) — **Visual Display Recipe canonical in `rk context`** — New `### Visual Display Recipe` subsection in `cmd/rk/context.go` `writeCapabilities` output, placed between `### Proxy` and `### CLI Commands`. Documents the 4-step iframe-display flow: generate HTML → loopback `python3 -m http.server --bind 127.0.0.1` → iframe window with **relative** `@rk_url /proxy/<port>/<filename>` → fail-silent on missing prerequisites. Relative path is deliberate — the run-kit frontend resolves it against the user's browser origin, so the recipe works identically direct or behind a reverse proxy (matches the existing pattern in `app/frontend/src/components/iframe-window.tsx:119`). Two new Go tests (`TestVisualDisplayRecipeOrdering`, `TestVisualDisplayRecipeNoAbsoluteProxyURL`) plus 3 assertions in `TestContextCapabilitiesSections`. Cross-repo coordination: fab-kit `src/kit/skills/_preamble.md` `### Visual Display Recipe` collapsed from inline 4-step prose (which had drifted to an absolute `{server_url}/proxy/...` form) to a pointer at `rk context` output — eliminates the drift class. fab-kit commit local only; propagates to `.claude/skills/_preamble/SKILL.md` deployed copies on next `fab sync` after fab-kit release. (l1ja)

## 2026-04-19
- **Update** [architecture](/run-kit/architecture.md) — feat: prefer agent-bearing pane entries in fab pane-map dedup (#169)
- **Update** [architecture](/run-kit/architecture.md) — **Adapt to fab-kit v1.5.0 `_agents` schema** — pane-map dedup rule extended with rule-2 (`Change > AgentState > first-seen`) so discussion-mode agent panes (populated `AgentState`, nil `Change`) win over bare sibling panes in split windows. Dedup logic extracted from `fetchPaneMap` into standalone `dedupEntries(entries) map[string]paneMapEntry` helper in `app/backend/internal/sessions/sessions.go`. Agent state (`agentState`/`agentIdleDuration`) now resolves independently of change state (`fabChange`/`fabStage`) — either can populate or be empty on any given window. Two new tests: `TestPaneMapDedupPrefersAgentState` (both iteration orders) and `TestPaneMapDedupChangeStillWinsOverAgent` (rule-1 preservation). No frontend changes required — existing branching in `status-panel.tsx` and `format.ts` already treats the two fields independently. (afyv)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Sidebar Separator Cursor Fixes (#168)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Sidebar separator cursor polish + corner resize affordance — horizontal separator cursor changed from `cursor-ns-resize` to `cursor-row-resize` (matches vertical's `cursor-col-resize` double-arrow-with-bar vocabulary); vertical hover fixed from `/40` opacity to full `hover:bg-text-secondary`. Both drag handlers now write `document.body.style.cursor` on pointerdown/start and clear to `""` on pointerup/end — document-level override survives the pointer leaving the thin handle (solves implicit-pointer-capture hover loss after first drag). `CollapsiblePanel` unmount cleanup also clears body cursor. New optional corner affordance at separator intersection: `CollapsiblePanel` gains `onCornerPointerDown` prop; when supplied + `showDragHandle` is true, renders a `w-[7px] h-[3px] cursor-nwse-resize` corner flush against the handle's right edge. Corner pointerdown invokes horizontal then vertical handlers, then overrides cursor to `nwse-resize` (last-write-wins). Handlers coexist without coordination because they use independent document listeners (clientY-only vs clientX-only). Prop threaded `app.tsx` → `Sidebar` (`onSidebarResizeStart`) → `ServerPanel` → `CollapsiblePanel` (`onCornerPointerDown`); all optional, mobile drawer omits it. (9ufu)

## 2026-04-18
- **Update** [architecture](/run-kit/architecture.md) — feat: Server panel tile grid (#159)
- **Update** [architecture](/run-kit/architecture.md) — **Server panel tile grid** — `/api/servers` response changed from `[]string` to `[]ServerInfo` (`{name, sessionCount}`). Session counts computed server-side via concurrent `tmux.ListSessions(ctx, server)` fan-out (`sync.WaitGroup` + mutex-protected results map); per-server errors logged and yield `sessionCount: 0` (no 5xx). Response sorted alphabetically. `TmuxOps` interface gains `ListSessions(ctx, server)`. Frontend `listServers()` returns `Promise<ServerInfo[]>`; `ServerInfo` exported from `app/frontend/src/api/client.ts`. All consumers (`SessionProvider`, `Sidebar`, `ServerPanel`, `ServerListPage`, `app.tsx` palette) read `.name`. `ServerListPage` cards show `{sessionCount} sess`. (jpkl)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — fix: thread tmux server through mutation API to fix closure race (#166)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Frontend server-routing contract — `server: string` now threaded as the first positional argument through every `api/client.ts` function that hits a server-scoped endpoint (`getSessions`, `createSession`, `renameSession`, `killSession`, `createWindow`, `renameWindow`, `killWindow`, `moveWindow`, `moveWindowToSession`, `selectWindow`, `splitWindow`, `closePane`, `sendKeys`, `updateWindowUrl`, `updateWindowType`, `setWindowColor`, `setSessionColor`, `reloadTmuxConfig`, `uploadFile`, `getKeybindings`). `withServer(url, server)` is pure — the module-level `_getServer` global and `setServerGetter` export are removed; `SessionProvider` no longer wires a getter. Server-management endpoints (`listServers`, `createServer`, `killServer`) and theme/server-color settings remain server-parameter-free by design. Eliminates a closure-race where mid-action server switches retargeted in-flight rename/kill/create calls at the wrong tmux server. Mirrors the backend `tmuxExecServer(ctx, server, …)` shape. (yadg)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: thread tmux server through mutation API to fix closure race (#166)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: align xterm cell widths with tmux via Unicode 15 graphemes addon (#164)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Sidebar header affordances — collapsed tint + Sessions name (#165)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: right-align server name in Server panel header (#163)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Server panel tile grid (#159)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Right-align server name in ServerPanel header — `ServerPanel` title changed from dynamic `Tmux · {server}` to static `"Server"` (matches WindowPanel/HostPanel convention). Active server name moved into `headerRight` slot with `truncate text-text-primary font-mono` classes (mirrors `host-panel.tsx`); `LogoSpinner` follows the name when `refreshing`. Left-side chevron and title are now visually fixed across server switches — only the right-slot text updates. Playwright spec `server-panel-grid.spec.ts` and its companion `.spec.md` updated to match the new `name: /^Server/` accessible name and `Resize Server panel` separator label. No new patterns introduced — aligns with existing sidebar panel header convention. (2cjc)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Server panel tile grid + resizable CollapsiblePanel — `ServerPanel` rewritten from vertical list to swatch-style tile grid (`repeat(auto-fill, minmax(72px, 1fr))` desktop, single-row horizontal scroll with `scroll-snap-type` on `pointer: coarse` / `<640px`). Tiles: 4px ANSI-tinted top stripe + 11px truncated name + 10px "N sess" meta. Active tile: `aria-current` + inset accent ring + `rowTints.get(color).selected` body tint. Hover-revealed color-picker and kill buttons rendered as siblings to the tile `<button>` (avoids nested-button HTML) with `group-hover:flex`; hidden on coarse pointer. Scrolls internally when tile grid overflows the user-set height. `CollapsiblePanel` gained opt-in `resizable`, `defaultHeight`, `minHeight`, `maxHeight`, `mobileHeight` props: 6px `ns-resize` drag handle persisted to `localStorage[${storageKey}-height]`, height clamping, `calc(100vh - Npx)` maxHeight parsing, mobile drag-handle hide. Window/Host panels unchanged (opt-in preserves legacy behaviour). `/api/servers` now returns `{name, sessionCount}[]` per architecture.md. (jpkl)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — xterm Unicode 15 grapheme widths — added `@xterm/addon-unicode-graphemes` to the Terminal init chain (loads after WebLinks, before WebGL), set `allowProposedApi: true` on the Terminal constructor, and assigned `terminal.unicode.activeVersion = "15-graphemes"` after `loadAddon()`. Aligns xterm's cell-width measurements with tmux's wcwidth-based layout so emojis and other wide graphemes (ZWJ sequences, flag/skin-tone modifiers) render without ghost/overlap artifacts. The `unicodeVersion` constructor option remains a no-op past `"6"` without the addon. (xgl2)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Server-capture-at-trigger convention for optimistic actions — every `useOptimisticAction` instance for a server-scoped mutation now threads `server: string` as the first slot of its argument tuple (Shape B), with `server` read from `useSessionContext()` and listed in the calling handler's `useCallback` deps (Shape A). Async-bridge refs (`lastKillSessionRef`, `lastRenameSessionRef`, `killDialogServerRef`) snapshot `{ server, name }` together so rollback/settle target the originating server. `OptimisticContext` switched session-level `GhostEntry`/`KilledEntry`/`RenamedEntry` to discriminated unions carrying `server`; `markKilled`/`unmarkKilled` overloaded by `type` ("session" requires `server`, "server" is global); `useMergedSessions(real, currentServer)` now filters session-level overlays so cross-server overlays don't leak. Window-store keying unchanged — windows don't migrate across servers. Establishes the rule: ambient module-level state for request parameters is prohibited; request-scoping values travel in the call signature, captured at user-event time. Regression test in `use-dialog-state.test.tsx` flips `SessionProvider.server` between dialog open and submit. (yadg)

## 2026-04-17
- **Update** [architecture](/run-kit/architecture.md) — fix: Mobile tap-to-focus regression after frontend dep upgrade (#151)
- **Update** [architecture](/run-kit/architecture.md) — fix: Restore fab state enrichment in sidebar Pane panel (#147)
- **Update** [architecture](/run-kit/architecture.md) — refactor: Add rk riff command to unify worktree + tmux + Claude Code launcher (#146)
- **Update** [architecture](/run-kit/architecture.md) — **Fix sidebar fab state display** — replaced broken hardcoded `fab/.kit/bin/fab-go` path lookup with `fab` router invocation on PATH (`fab pane map --json --all-sessions` with `cmd.Dir = repoRoot`); changed `findRepoRoot` marker from deleted `fab/.kit/bin/fab-go` to `fab/project/config.yaml` (fab-project identity marker per `fab preflight`); added `TestFetchPaneMapIntegration` (gated via `exec.LookPath("fab")`); renamed `TestFetchPaneMapNonexistentBinary` → `TestFetchPaneMapFabNotOnPath`. (5nok)
- **Update** [architecture](/run-kit/architecture.md) — **Kill-redirect SSE-replay gate** — `computeKillRedirect` (`app/frontend/src/lib/navigation.ts`) gains `currentWindowEverSeen?: boolean` param (defaults to `true`). When false, all "gone" branches short-circuit to `null`, deferring the session-dashboard / nearest-sibling redirect until the URL target has been observed as valid at least once. `app.tsx` tracks the seen state via `currentWindowEverSeenRef` (flipped true whenever memoized `currentWindow` is non-null) plus `lastObservedUrlKeyRef` (string `${server}|${session}|${window}`, resets the seen flag on URL change). Eliminates a startup race where a stale-cached first SSE payload or a session with briefly-empty `windows` array (while tmux enumeration propagates) redirected to the dashboard and unmounted `TerminalClient` mid-init — surfaced as `mobile-touch-scroll.spec.ts` flakes where `.xterm-screen` briefly appeared then disappeared. Two new unit tests in `navigation.test.ts` for the stale-SSE and empty-windows-transient cases. No changes to `TerminalClient` or SSE hub. (g9uo)
- **Update** [architecture](/run-kit/architecture.md) — **`rk riff` — worktree + tmux window + Claude launcher** — New Cobra subcommand `riff` in `cmd/rk/riff.go` unifies the `riff`/`riffs` shell functions into a first-class `rk` command. Wraps `wt create --non-interactive --worktree-open skip [passthrough...]`, parses the `Path:` line, then runs `tmux new-window -c <path> "<launcher> '<cmd>'"` (optionally followed by `tmux split-window -h -c <path> "<setup>; exec zsh"` when `--split` is non-empty). Launcher resolved via new `internal/fabconfig/` package (`ReadSpawnCommand(repoRoot)` — best-effort `yaml.v3` read of `agent.spawn_command` from `fab/project/config.yaml`, falls back to `claude --dangerously-skip-permissions`). Repo-root discovery reuses `config.FindGitRoot`. Preconditions fast-fail: `$TMUX` set (via `tmux.OriginalTMUX`, since `internal/tmux` init strips `$TMUX` from the process env) then `wt` on PATH; local `exitCodeError` wrapper maps 2 (precondition) / 3 (subprocess) without touching `main.execute()`. `exec.CommandContext` timeouts: wt 30s, tmux 10s. Single-quote escaping helper (`escapeSingleQuotes`) keeps `--cmd` values safe in the tmux shell string. `cmd/rk/context.go` CLI Commands section lists `rk riff` under **Workflow**. 13 new tests (8 for `parseWorktreePath`, 6 for `escapeSingleQuotes`, 5 for `resolveLauncher`, plus fabconfig coverage). (r1j6)
- **Update** [rk-riff](/run-kit/rk-riff.md) — feat: Name the tmux window created by `rk riff` after the worktree (#155)
- **Update** [rk-riff](/run-kit/rk-riff.md) — docs: Fix rk riff doc comments and typos from Copilot review (#149)
- **Creation** [rk-riff](/run-kit/rk-riff.md) — refactor: Add rk riff command to unify worktree + tmux + Claude Code launcher (#146)
- **Update** [rk-riff](/run-kit/rk-riff.md) — Initial `rk riff` subcommand — worktree + tmux window + Claude launcher. Unifies the personal-dotfile `riff`/`riffs` shell functions into a first-class `rk` command. `--cmd` (default `/fab-discuss`), `--split <setup-cmd>` (optional horizontal split), `-- <wt-flags>` passthrough to `wt create`. Preconditions: `$TMUX` set + `wt` on PATH (exit 2). Launcher from `agent.spawn_command` in `fab/project/config.yaml` via new `internal/fabconfig/` (falls back to `claude --dangerously-skip-permissions`). Local `exitCodeError` wrapper maps exit codes (2 precondition, 3 subprocess) without touching `main.execute()`. `exec.CommandContext` with 30s/10s timeouts. `tmux.OriginalTMUX` restored in child env so tmux targets the user's current server. (r1j6)
- **Update** [rk-riff](/run-kit/rk-riff.md) — Name the tmux window `riff-<worktree-basename>` via the `-n` flag, and document via the pure helper `buildNewWindowArgs`. (w4af)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — refactor: Add rk riff command to unify worktree + tmux + Claude Code launcher (#146)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — `rk riff` window creation — new subcommand creates `tmux new-window -c <path> "<launcher> '<cmd>'"` on the user's current tmux server (not the managed `runkit`/`default` servers). `$TMUX` recovered via `tmux.OriginalTMUX` and restored in child env (mirrors `context.go`). Optional `--split` appends `tmux split-window -h -c <path> "<setup>; exec \"${SHELL:-/bin/sh}\""` via the shared `shellWrap` helper. Bypasses `internal/tmux` because that package targets specific named servers via `-L <server>`. (r1j6)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Bundle JetBrainsMono Nerd Font as a webfont in the frontend (#152)

## 2026-04-16
- **Update** [architecture](/run-kit/architecture.md) — feat: Add rk context command for agent-optimized environment discovery (#141)
- **Update** [architecture](/run-kit/architecture.md) — feat: Iframe Proxy Windows (#139)
- **Update** [architecture](/run-kit/architecture.md) — feat: Session and Window Color Tinting (#140)
- **Update** [architecture](/run-kit/architecture.md) — **`rk context` — agent discovery** — New Cobra subcommand `context` in `cmd/rk/context.go`. Outputs agent-optimized plain text with Environment (dynamic tmux pane queries), Capabilities (terminal windows, iframe windows with exact `tmux set-option` commands, proxy URL, categorized CLI commands), and Conventions (tmux user options, window lifecycle, SSE reactivity) sections. Uses raw `exec.CommandContext` (5s timeout) for tmux queries — deliberately bypasses `internal/tmux` execution functions because they add `-L <server>` targeting. `tmux.OriginalTMUX` var captures `$TMUX` before `init()` strips it; restored in child process env via `cmd.Env`. Server URL via `config.Load()`. Gracefully degrades outside tmux. 6 Go tests. (0gz9)
- **Update** [architecture](/run-kit/architecture.md) — **Iframe proxy windows** — Reverse proxy at `/proxy/{port}/*` in `api/proxy.go` forwards to `localhost:{port}` with per-port `ReverseProxy` caching (`sync.Map`) and HTML response rewriting (localhost/127.0.0.1 URLs to proxy paths, gzip-aware). Tmux user-defined options `@rk_type` and `@rk_url` store iframe window metadata — included in `ListWindows` format string (fields 8-9), `WindowInfo` gains `RkType`/`RkUrl` fields. New `SetWindowOption` and `CreateWindowWithOptions` in `internal/tmux`. `handleWindowCreate` extended with optional `rkType`/`rkUrl` (atomic `\;`-chained tmux command). New `PUT /api/sessions/{session}/windows/{index}/url` endpoint. Frontend: `IframeWindow` component (URL bar + iframe), rendering branch in `app.tsx` (`rkType === "iframe"` -> IframeWindow, else TerminalClient). "Window: New Iframe Window" command palette action with name/URL dialog. `updateWindowUrl` and extended `createWindow` in API client. Vite dev proxy rule for `/proxy`. SSE reactivity automatic (no new transport). (6b0h)
- **Update** [architecture](/run-kit/architecture.md) — **Session and window color tinting** — New `internal/config/runkit_yaml.go`: `FindGitRoot()` (walk-up `.git` search), `ReadSessionColor()`/`WriteSessionColor()` for `run-kit.yaml` at git root (simple line parser, no yaml.v3). `ProjectSession.SessionColor *int` populated during `FetchSessions()` enrichment. `WindowInfo.Color *int` from `#{@color}` in `ListWindows()` format string (8 fields total). `SetWindowColor()`/`UnsetWindowColor()` in `internal/tmux`. New endpoints: `POST /api/sessions/:session/color` (`handleSessionColor` in `sessions.go`), `POST /api/sessions/:session/windows/:index/color` (`handleWindowColor` in `windows.go`). Frontend: `setWindowColor()`/`setSessionColor()` API client functions. `computeRowTints()` and `PICKER_ANSI_INDICES` in `themes.ts`. `SwatchPopover` component (lazy-loaded). CmdK "Session: Set Color" / "Window: Set Color" actions. `types.ts`: `ProjectSession.sessionColor?: number`, `WindowInfo.color?: number`. (jn4h)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Pane panel spinner animations (BlockPulse + BrailleSnake) (#144)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Iframe Proxy Windows (#139)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Session and Window Color Tinting (#140)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Iframe proxy windows — `IframeWindow` component (`iframe-window.tsx`) renders URL bar chrome + iframe for windows with `rkType === "iframe"`. Rendering branch in `app.tsx`: `currentWindow?.rkType === "iframe" && currentWindow?.rkUrl` renders `IframeWindow`, otherwise `TerminalClient`. URL bar: refresh button (↻), editable URL input (Enter submits via `updateWindowUrl` PUT API), submit indicator (⏎). SSE-driven URL sync via `useEffect` on `rkUrl` with `currentSrcRef` guard (no reload on identical data). `toProxySrc()` converts localhost URLs to `/proxy/{port}/...` paths. New "Window: New Iframe Window" command palette action (id `create-iframe-window`) opens dialog with name + URL inputs. Bottom bar hidden for iframe windows. (6b0h)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Session and window color tinting — ANSI-palette color assignment for sidebar rows with pre-blended `blendHex()` background tints at 12%/18%/22%. `SwatchPopover` component (13 ANSI swatches + Clear). Command palette "Session/Window: Set Color" actions. Hover indicator on sidebar rows. Activity dot changed from green/gray color-based to filled circle/hollow ring shape-based (always `text-text-secondary`). `RowTint` type and `computeRowTints()` in `themes.ts`. `PICKER_ANSI_INDICES` constant (jn4h)

## 2026-04-15
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Pane panel — click rows to copy cwd/git/tmx/fab values (#138)

## 2026-04-12
- **Update** [architecture](/run-kit/architecture.md) — feat: Sidebar Host & Window Panels (#135)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Sidebar Host & Window Panels (#135)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Cross-session drag-drop optimistic update (#134)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Optimistic Sidebar Window Reorder (#133)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Pane panel copy interactions — `tmx`, `cwd`, `git`, `fab` rows in WindowPanel (`status-panel.tsx`) rendered as `<button>` elements with click-to-copy (pane ID, full path, branch, change ID). Inline "copied ✓" label feedback (1000ms, single `copiedRow` state). Hover affordance (`cursor: pointer` + `bg-bg-inset` tint). Keyboard accessible (Enter/Space). Text-selection guard (`window.getSelection()`). `copyToClipboard` extracted from `terminal-client.tsx` to `lib/clipboard.ts` shared utility module (lc2q)

## 2026-04-11
- **Update** [architecture](/run-kit/architecture.md) — **Sidebar Host & Window panels + metrics collector** — New `internal/metrics/` package reads procfs for CPU (ring buffer 60 samples), memory, load, disk, uptime; `Collector` runs background goroutine at `ssePollInterval`. SSE hub gains `event: metrics` event type broadcast to all clients every tick (server-independent, not deduplicated). `Server` struct holds `*metrics.Collector`, created and started in `NewRouter()`. Frontend: `SessionProvider` listens for `metrics` SSE event, exposes `MetricsSnapshot | null` via context. New sidebar components: `CollapsiblePanel` (reusable), `WindowPanel` (refactored from StatusPanel), `HostPanel` (5-line metrics display with braille sparkline, gauge bar, load percentages, disk+uptime). New utility modules: `lib/sparkline.ts` (braille 8-level mapping), `lib/gauge.ts` (block gauge + color thresholds + byte formatting). Hostname removed from bottom bar — now in HostPanel exclusively. (z63r)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Optimistic sidebar window reorder — drag-drop window reorder in sidebar now uses `useOptimisticAction` with `swapWindowOrder` store action to swap window index values immediately on drop. API call fires in background; rollback reverses the swap on failure. Eliminates ~2.5s SSE poll wait. `swapWindowOrder(session, srcIndex, dstIndex)` added to Zustand window store. Unit tests for store swap + rollback, sidebar tests for optimistic drop + API failure rollback. (sl01)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Optimistic cross-session drag — `executeMoveToSession` `useOptimisticAction` instance in sidebar wires compound optimistic update: `killWindow` (hide in source) + `addGhostWindow` (show in target with source window's display name) + immediate navigation to `/$server`. Rollback: `restoreWindow` + `removeGhost`. Removed `onMoveWindowToSession` prop from `SidebarProps` — sidebar imports `moveWindowToSession` API directly. Drag data payload extended with `windowId` and `name`. Unit tests for optimistic lifecycle and rollback. (sl02)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Sidebar collapsible panels — `CollapsiblePanel` reusable component (header + chevron + `max-height` transition + localStorage persistence). `StatusPanel` refactored into `WindowPanel` wrapping content in CollapsiblePanel (`storageKey="runkit-panel-window"`). New `HostPanel` (5 lines: hostname+SSE dot, CPU braille sparkline, memory gauge bar, load percentages, disk+uptime) wrapping in CollapsiblePanel (`storageKey="runkit-panel-host"`). Both panels bottom-aligned in sidebar. Hostname removed from bottom bar. New `lib/sparkline.ts` (8-level braille mapping U+2800-U+28FF) and `lib/gauge.ts` (block gauge with green/yellow/red thresholds, byte formatting). `SessionProvider` extended with `metrics: MetricsSnapshot | null` from SSE `event: metrics`. (z63r)

## 2026-04-06
- **Update** [architecture](/run-kit/architecture.md) — refactor: Pane CWD Tracking and Sidebar Hover Info (#127)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — refactor: Pane CWD Tracking and Sidebar Hover Info (#127)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Pane CWD tracking — `ListWindows` now calls `list-panes -s -t <session>` after `list-windows` to populate `Panes []PaneInfo` on each `WindowInfo`; failure is non-fatal. `parsePanes(lines []string) map[int][]PaneInfo` parses 6-field tab-delimited output (field 0 = `#{window_index}` for grouping, fields 1–5 = pane data). Package-level `paneFormat` var: `#{window_index}\t#{pane_id}\t#{pane_index}\t#{pane_current_path}\t#{pane_current_command}\t#{pane_active}`. `WorktreePath` unchanged — still sourced from `list-windows #{pane_current_path}`. (rx38)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: shorten CWD display in status panel (#130)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Quick Session/Window Launch Without Folder Selection (#128)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Pane CWD Tracking and Sidebar Hover Info (#127)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Sidebar Component Decomposition (#126)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Shorten CWD in StatusPanel — `shortenPath()` in `status-panel.tsx` rewritten to substitute Linux `/home/<user>/` and macOS `/Users/<user>/` (and `/root`) with `~`, then truncate paths with >2 segments to `…/<last-two-segments>`. `title` attribute retains full unmodified path for hover tooltip. Unit tests updated/added in `status-panel.test.tsx`. (65f1)

## 2026-04-05
- **Update** [architecture](/run-kit/architecture.md) — refactor: Sidebar Window State Zustand (#123)
- **Update** [architecture](/run-kit/architecture.md) — fix: left panel windows sync without page refresh (#122)
- **Update** [architecture](/run-kit/architecture.md) — **Left panel window sync fix + E2E** — `use-optimistic-action.ts` fix: `onSettled`/`onRollback` moved before `mountedRef` guard so they always fire, preventing stale `killed` entries when the sidebar component unmounts before the kill API resolves. New E2E test `sidebar-window-sync.spec.ts` (3 scenarios: external window creation, rename, kill-then-create at same index). (2a2k)
- **Update** [architecture](/run-kit/architecture.md) — **Sidebar window state Zustand** — Window optimistic state migrated from index-based `OptimisticContext` to Zustand store (`app/frontend/src/store/window-store.ts`). `zustand` added as production dependency in `app/frontend/package.json`. `WindowInfo` type gains `windowId: string` (non-optional); backend adds `#{window_id}` as first field in `ListWindows()` format string (7 fields total, field 0 = `WindowID`). `OptimisticContext` slimmed to session/server scope. `MergedWindow` moved to `window-store.ts`. Ghost reconciliation uses `snapshotWindowIds` set-difference. Frontend unit tests: `window-store.test.ts` added, `optimistic-context.test.tsx` stripped of window test cases. (x3yt)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Sidebar Window State Zustand (#123)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Session Name Inline Rename (#121)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: left panel windows sync without page refresh (#122)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Fix left panel window sync — introduced `onAlwaysSettled`/`onAlwaysRollback` callbacks to `useOptimisticAction` that fire regardless of mount state (for root-level context cleanup like `unmarkKilled`), while `onSettled`/`onRollback` remain behind `mountedRef` guard (safe for local component state). Kill handlers in `sidebar.tsx` and `use-dialog-state.ts` migrated to `onAlways*`. E2E test `sidebar-window-sync.spec.ts` rewritten to be self-contained per test with unique window names and Scenario 3 using `page.route()` to intercept the kill API and exercise the unmount-before-response path. (2a2k)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Session inline rename — double-click session name in sidebar to edit inline (mirrors window rename pattern). Enter/blur commits (non-empty, changed only), Escape cancels. Optimistic update via `markRenamed("session", ...)` with toast on error. Cross-cancel: starting a session edit cancels any active window edit and vice versa — only one inline edit active at a time. Dialog-based session rename in `app.tsx` unchanged (3mt2)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Sidebar window state Zustand — window optimistic state migrated from index-based `OptimisticContext` to a Zustand store (`app/frontend/src/store/window-store.ts`) keyed by immutable `windowId` (`@N`). Eliminates index-collision bugs from tmux window renumbering. `WindowInfo` gains `windowId: string`. Backend adds `#{window_id}` to tmux format string. `OptimisticContext` slimmed to session/server scope only. `MergedWindow` moved to `window-store.ts`. `sidebar.tsx`, `app.tsx`, `use-dialog-state.ts` updated to use Zustand store actions. Ghost reconciliation uses snapshot `windowId` set-difference instead of count-based heuristics. (x3yt)

## 2026-04-04
- **Update** [architecture](/run-kit/architecture.md) — feat: Move Window Between Sessions (#115)
- **Update** [architecture](/run-kit/architecture.md) — **Window move & reorder** — New `SwapWindow(session, srcIndex, dstIndex, server)` in `internal/tmux` wrapping `tmux swap-window`. New `POST /api/sessions/:session/windows/:index/move` endpoint (`handleWindowMove` in `windows.go`) with `{"targetIndex": N}` body. `TmuxOps` interface extended with `SwapWindow`. New `moveWindow(session, index, targetIndex)` client function. CmdK "Window: Move Left/Right" actions (excluded at boundary). Sidebar drag-and-drop window reordering via native HTML5 DnD (same-session only). (29qz)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — feat: Move Window Between Sessions (#115)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Cross-session window move — `MoveWindowToSession(srcSession, srcIndex, dstSession, server)` wraps `tmux move-window -s {src}:{idx} -t {dst}:` with `tmuxExecServer` and `withTimeout()`. New `POST /api/sessions/{session}/windows/{index}/move-to-session` endpoint with `{ "targetSession" }` body. `TmuxOps` interface extended with `MoveWindowToSession`. Validates source/target differ (400 if same). (dq70)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Sidebar Kill Hides Extra Window (#120)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Move Window Between Sessions (#115)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Optimistic UI Feedback (#111)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Window move & reorder — CmdK "Window: Move Left/Right" actions (boundary-excluded, navigate after swap), sidebar drag-and-drop window reordering via native HTML5 DnD (same-session only, accent drop indicator, no external library) (29qz)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Cross-session window move — CmdK "Window: Move to {name}" actions (one per other session, flat list), cross-session drag-and-drop to session headers (accent border feedback), `moveWindowToSession` API client function, post-move navigation to `/$server` (server dashboard) (dq70)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Fix sidebar kill hides extra window — `onSettled` callbacks added to all three `useOptimisticAction` kill instances (`executeKillWindow` in sidebar, `executeKillFromDialog` in sidebar, `executeKillWindow` in use-dialog-state) to call `unmarkKilled` after success, preventing tmux index-renumbering from causing index collision on next SSE update (dsq9)

## 2026-04-03
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: New Pane Inherits Current Working Directory (#110)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: ctrl+click force kill window/session (#109)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Optimistic UI feedback — `useOptimisticAction` hook replacing all `.catch(() => {})` mutation patterns, `OptimisticProvider` context for ghost entries (create) and optimistic removal (kill) with SSE reconciliation, `ToastProvider` + `Toast` for error/info notifications (auto-dismiss 4s), button loading states (split/close pane spinners), inline progress (upload badge, directory autocomplete spinner, server refresh spinner), `isGhostWindow` type guard (32la)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — New pane inherits active pane CWD — `handleCreateWindow` passes `currentWindow?.worktreePath` to `createWindow()` so new windows start in the active pane's current directory (live via tmux `#{pane_current_path}`) instead of defaulting to `windows[0].WorktreePath`. No backend/API changes needed — `cwd` param already supported end-to-end. All three entry points (sidebar "+", top bar, Cmd+K) covered by single handler (xnq5)

## 2026-04-02
- **Update** [architecture](/run-kit/architecture.md) — chore: migrate org references from wvrdz to sahil87 (#105)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — chore: migrate org references from wvrdz to sahil87 (#105)

## 2026-03-28
- **Update** [architecture](/run-kit/architecture.md) — docs: Multi-file Tmux Config Sourcing (#100)
- **Update** [architecture](/run-kit/architecture.md) — fix: Fix Update Restart (#98)
- **Update** [architecture](/run-kit/architecture.md) — chore: Change tmux Prefix Key (#101)
- **Update** [architecture](/run-kit/architecture.md) — **tmux prefix key change** — Changed tmux prefix from `C-b` to `C-s` in `configs/tmux/default.conf` and `configs/tmux/simple.conf` to avoid conflict with Claude Code's `Ctrl+B` shortcuts. Added `unbind C-b` and `bind-key C-s send-prefix`. Frontend keyboard shortcuts label updated from `Ctrl+B, ` to `Ctrl+S, `. `byobu.conf` (`C-a`) and `poweruser.conf` (`C-s`) unchanged. (d7s5)
- **Update** [architecture](/run-kit/architecture.md) — **Multi-file tmux config sourcing** — `~/.rk/tmux.d/` drop-in directory for user extensions. `configs/tmux/default.conf` appends `source-file -q ~/.rk/tmux.d/*.conf` (lexicographic order, `-q` silences empty/missing dir). `EnsureConfig()`, `ForceWriteConfig()`, and `rk init-conf` all create the directory idempotently. `EnsureConfig()` creates `tmux.d/` even when config already exists. `ReloadConfig()` unchanged — transitive sourcing picks up new drop-ins. (wxrh)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Tmux Commands Dialog (#102)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Mobile Keyboard Scroll Lock (#99)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — chore: Change tmux Prefix Key (#101)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Tmux commands dialog — replaced direct clipboard copy with a dialog showing three tmux commands (attach, new-window, detach) with per-row copy buttons and checkmark feedback. Server-aware command generation includes `-L {server}` flag for named servers, omits it for `"default"` (6xey)

## 2026-03-27
- **Update** [architecture](/run-kit/architecture.md) — refactor: SSE Infrastructure Performance (#96)
- **Update** [architecture](/run-kit/architecture.md) — feat: Performance Phase 4 — Bundle & Loading (#95)
- **Update** [architecture](/run-kit/architecture.md) — refactor: Performance Phase 1 — Backend Hot Paths (#94)
- **Update** [architecture](/run-kit/architecture.md) — refactor: Replace rk version Subcommand with --version / -v Flag (#90)
- **Update** [architecture](/run-kit/architecture.md) — **Bundle & loading performance** — Lazy-load `CommandPalette`, `ThemeSelector`, `CreateSessionDialog` via `React.lazy()` + `<Suspense fallback={null}>` (named export re-wrapping). Vite `manualChunks` splits xterm family and TanStack Router into separate vendor chunks. API request deduplication via `deduplicatedFetch` wrapper with in-flight `Map<string, Promise<Response>>` — GET-only, URL-keyed, `.finally()` cleanup, `Response.clone()` for multi-consumer safety. (uyj5)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Mobile Bar Keyboard Toggle (#97)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Performance Phase 3 — Frontend Rendering (#93)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Copy tmux Attach Command (#89)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Close Pane Button (#88)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fab: Intake for tmux mobile scroll fix (#85)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Mobile keyboard scroll-lock — long-press on keyboard toggle (>= 500ms) activates scroll-lock mode preventing soft keyboard from appearing on terminal tap. Focus prevention via capture-phase `focusin` listener. Tap-in-locked-mode unlocks + summons keyboard in one action. Visual indicator uses modifier armed-state pattern (`bg-accent/20 border-accent text-accent`, lock icon). Session-scoped state, optional haptic feedback (4azv)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Frontend rendering perf — SSE string diff + `startTransition` in SessionProvider (skips ~90% redundant re-renders), `useChromeState()` hook export (state-only consumers avoid merged object allocation), palette actions split into 7 independently memoized groups (session/window/view/theme/config/server/terminal), xterm.js write batching via `requestAnimationFrame` (coalesces WebSocket messages per frame) (cnav)

## 2026-03-26
- **Update** [architecture](/run-kit/architecture.md) — refactor: Shareable URLs (#86)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Shareable URLs (#86)

## 2026-03-25
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Per-Mode Theme Preferences (#82)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Per-mode theme preferences — `theme_dark`/`theme_light` settings stored alongside `theme` in `~/.rk/settings.yaml` and localStorage. System mode resolves to user's preferred dark/light theme instead of hard-coded defaults. Theme selection saves to matching per-mode slot (by category) and stays in system mode. API extended: GET returns all three fields, PUT accepts partial updates. (vxj6)

## 2026-03-24
- **Update** [architecture](/run-kit/architecture.md) — refactor: Icon Generation Pipeline (#81)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Icon Generation Pipeline (#81)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Command Palette Arrow Key Scroll (#80)

## 2026-03-23
- **Update** [architecture](/run-kit/architecture.md) — refactor: ANSI Palette Theme Rework (#79)
- **Update** [architecture](/run-kit/architecture.md) — rename CLI binary and Homebrew distribution from run-kit to rk (#74)
- **Update** [architecture](/run-kit/architecture.md) — **ANSI palette theme rework** — New `internal/settings/` package for backend settings persistence at `~/.rk/settings.yaml` (simple key:value parsing, no yaml.v3). New `GET/PUT /api/settings/theme` endpoints in `api/settings.go`. Frontend: `getThemePreference()`/`setThemePreference()` API client functions (not per-server). ThemeProvider uses API as canonical source with localStorage as synchronous cache. tmux.conf reworked from hardcoded hex to ANSI `colour{N}` indices for auto-theming via xterm.js palette. (7wys)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: ANSI Palette Theme Rework (#79)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: prevent bottom bar buttons from stealing terminal focus on mobile (#76)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Disable Mobile Zoom on Input Focus (#75)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — ANSI palette theme rework — ThemePalette type (22 colors), deriveUIColors/deriveXtermTheme derivation layer, full xterm.js palette integration, tmux.conf ANSI colour indices (auto-theming via xterm.js), backend settings persistence (`~/.rk/settings.yaml`, `GET/PUT /api/settings/theme`), API + localStorage dual persistence in ThemeProvider, multi-color palette swatches in theme selector (7wys)

## 2026-03-22
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: replace hamburger X with back chevron (<) for clearer menu toggle (#70)

## 2026-03-21
- **Update** [architecture](/run-kit/architecture.md) — feat: add split pane buttons and normalize toolbar icon colors
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: compose buffer — image preview, path duplication fix, dialog alignment
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: add split pane buttons and normalize toolbar icon colors
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: OSC 52 clipboard — accept empty selection parameter from tmux
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Fix OSC 52 clipboard — custom `ClipboardProvider` for `ClipboardAddon` that accepts empty selection parameter (`""`) in addition to `"c"`. Fixes tmux copy-mode yank not reaching browser clipboard (tmux sends `]52;;base64`, addon default provider only accepted `]52;c;base64`). Provider exported as `clipboardProvider` for testability (zbdq)

## 2026-03-20
- **Update** [architecture](/run-kit/architecture.md) — Remove Caddyfile and all Caddy references
- **Update** [architecture](/run-kit/architecture.md) — Consolidate tmux config — embedded copy is now canonical
- **Update** [architecture](/run-kit/architecture.md) — Add daemon lifecycle management to run-kit serve
- **Update** [architecture](/run-kit/architecture.md) — Add keyboard shortcuts overlay, enhance tmux config
- **Update** [architecture](/run-kit/architecture.md) — Left-align breadcrumb session name, move embed to app/backend/build/
- **Update** [architecture](/run-kit/architecture.md) — Remove pnpm workspace — single frontend package doesn't need it
- **Update** [architecture](/run-kit/architecture.md) — refactor: single-active-server tmux model with server switcher UI
- **Update** [architecture](/run-kit/architecture.md) — Rename upgrade to update, improve Homebrew update flow
- **Update** [architecture](/run-kit/architecture.md) — feat: add PWA compliance for homescreen install and standalone display
- **Update** [architecture](/run-kit/architecture.md) — Remove BACKEND_PORT/BACKEND_HOST indirection, delete prod.sh
- **Update** [architecture](/run-kit/architecture.md) — feat: show server hostname in browser tab title
- **Update** [architecture](/run-kit/architecture.md) — docs: update memory for multi-server relay, config reload, and stderr capture
- **Update** [architecture](/run-kit/architecture.md) — **Release pipeline + update command** — `release.sh` rewritten (fab-kit/tu style). CI generates `Formula/rk.rb` in `sahil87/homebrew-tap` from `.github/formula-template.rb` (sed placeholder substitution for version + SHA256s) via `BUILD_TOKEN` secret. Public repo — plain URL downloads, no auth token needed. `upgrade` subcommand renamed to `update` (alias: `upgrade`): checks latest via `brew info --json=v2 sahil87/tap/rk`, skips if up-to-date, shows version transition.
- **Update** [architecture](/run-kit/architecture.md) — **Multi-server relay + config reload** — `RK_TMUX_CONF` resolved to absolute path at init (fixes CWD-dependent config loading). Relay and select-window endpoints accept `?server=` query param to route to runkit or default tmux server (fixes default-server sessions not connecting). `SelectWindowOnServer()` added. `ReloadConfig(server)` hot-reloads tmux config via `source-file`. New `POST /api/tmux/reload-config` endpoint + `reloadTmuxConfig(server)` client function + "Reload tmux config" command palette action (targets current session's server). `tmuxExec`/`tmuxExecDefault` capture stderr in error messages. `TerminalClient` accepts `server` prop. (0gjh)
- **Update** [architecture](/run-kit/architecture.md) — **Single-active-server model** — Replaced dual-server merge with single-server-at-a-time. Backend stateless: `?server=` on every request, defaults to `"default"`. Unified `tmuxExec`/`tmuxExecDefault` into `tmuxExecServer`. All tmux functions accept `server` param. `serverFromRequest()` validates names. New `ListServers()` (socket scan), `KillServer()`. SSE hub polls per-server. Removed `SessionInfo.Server`, `ProjectSession.Server`. New `GET/POST /api/servers`, `POST /api/servers/kill`. Frontend: `SessionProvider` manages server state (localStorage `runkit-server`), sidebar server dropdown, palette commands (Create/Kill/Switch server). `setServerGetter()` mechanism appends `?server=` to all API calls. (1335)
- **Update** [architecture](/run-kit/architecture.md) — **UI polish, tmux config, keyboard shortcuts** — Embed restructured from `app/backend/frontend/` to `app/backend/build/` (package `build`, `//go:embed all:frontend`). Breadcrumb left-aligned (removed `justify-center`). `EnsureConfig()` auto-creates `~/.run-kit/tmux.conf` on serve startup. `-f` config flag scoped to `CreateSession`/`ReloadConfig` via `configArgs()`. Enhanced `internal/tmux/tmux.conf`: `escape-time 0`, `history-limit 50000`, `renumber-windows on`, `base-index 1`, explicit `prefix C-s`, pane splits (`prefix+|`/`prefix+-`), pane navigation (`S-F3`/`S-F4`), `F8` rename, `S-F7` copy-mode. Sidebar server dropdown gains `+ tmux server` action. Hostname in bottom bar (hidden on mobile). Aligned sidebar footer and bottom bar heights (`h-[48px]`). Server label changed to "tmux server:". Kill server handles socket teardown gracefully. New `GET /api/keybindings` endpoint — runs `tmux list-keys`, filters via whitelist map, returns `[{key, table, command, label}]`. New "Keyboard Shortcuts" command palette action opens modal showing curated bindings grouped by key table (prefix vs root). (9ldy)
- **Update** [architecture](/run-kit/architecture.md) — **Daemon lifecycle** — Replaced `supervisor.sh` (polling loop + `.restart-requested` signal file) with CLI-driven daemon management. New `internal/daemon/` package with `IsRunning`/`Start`/`Stop`/`Restart` helpers using `exec.CommandContext`. `run-kit serve` gains `-d` (start daemon, errors if running), `--restart` (idempotent stop+start), `--stop` (graceful C-c) flags — mutually exclusive. Daemon runs in dedicated tmux server `rk-daemon` (session `rk`, window `serve`), separate from agent `runkit` server. `run-kit update` auto-restarts daemon after successful `brew upgrade`. Justfile `up`/`down`/`restart` recipes updated. Constitution Self-Improvement Safety section rewritten. (hkm8)
- **Update** [architecture](/run-kit/architecture.md) — **PWA compliance** — `vite-plugin-pwa` with autoUpdate, manifest from plugin config (standalone display, dark theme colors), Workbox service worker (precache static, NetworkOnly for API/WebSocket), iOS meta tags, theme-color sync. No backend changes. (j9a2)
- **Update** [architecture](/run-kit/architecture.md) — **Hostname in browser title** — `Server` struct gains `hostname` field (computed once via `os.Hostname()` in `NewRouter()`, empty string fallback). `/api/health` response extended to `{"status":"ok","hostname":"..."}`. New `getHealth()` API client function. `useBrowserTitle` hook sets `document.title` dynamically: `RunKit — {hostname}` on Dashboard, `{session}/{window} — {hostname}` on terminal pages. Hostname suffix omitted when empty. (uq0k)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Consolidate tmux config — embedded copy is now canonical
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Add keyboard shortcuts overlay, enhance tmux config
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — refactor: single-active-server tmux model with server switcher UI
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — docs: update memory for multi-server relay, config reload, and stderr capture
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Multi-server operations — `SelectWindowOnServer()` routes select-window to correct server. `ReloadConfig(server)` hot-reloads config on specified server. Relay and select-window endpoints accept `?server=` query param. `RK_TMUX_CONF` resolved to absolute path at init. Stderr captured in tmux exec errors. (0gjh)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Single-active-server model — replaced dual-server merge with `?server=` on every request. All tmux functions accept `server` param. Unified `tmuxExec`/`tmuxExecDefault` into `tmuxExecServer`. Added `ListServers()` (socket scan), `KillServer()`. SSE hub polls per-server. Removed `SessionInfo.Server` and `ProjectSession.Server` fields. New endpoints: `GET/POST /api/servers`, `POST /api/servers/kill`. `serverFromRequest()` validates input. (1335)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — tmux config and keybindings — `EnsureConfig()` auto-creates `~/.run-kit/tmux.conf` on serve startup. `-f` config flag scoped to `CreateSession`/`ReloadConfig` via `configArgs()`. Enhanced `internal/tmux/tmux.conf` with agent-optimized defaults and power-user keybindings. `ListKeys(server)` runs `tmux list-keys`, returns raw output (nil on "no server"). New `GET /api/keybindings` endpoint filters `list-keys` via whitelist map. `KillServer()` handles socket teardown gracefully (returns nil on "No such file or directory"). (9ldy)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Add keyboard shortcuts overlay, enhance tmux config
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: single-active-server tmux model with server switcher UI
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: add PWA compliance for homescreen install and standalone display
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — docs: update memory for multi-server relay, config reload, and stderr capture
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Multi-server terminal support — `TerminalClient` accepts `server` prop, WebSocket URL includes `?server=` param. "Reload tmux config" command palette action targets current session's server. `selectWindow` API call passes server for correct routing. (0gjh)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Single-active-server model — Sidebar server selector at bottom (`Server: <dropdown>`, pinned below scrollable session tree). Command palette: "Create tmux server" (name dialog), "Kill tmux server" (confirmation), "Switch tmux server: {name}" per server. Removed `↗` external session marker and `ProjectSession.server` field. `SessionProvider` manages `server`/`setServer`/`servers`/`refreshServers` state. Active server persisted in localStorage `runkit-server` (default: `runkit`). All API calls append `?server=` via `setServerGetter()` mechanism. SSE reconnects on server switch. Navigate to `/` on switch. (1335)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — UI polish + keyboard shortcuts — Breadcrumb left-aligned (removed `justify-center`). Sidebar server dropdown gains `+ tmux server` action. Hostname in bottom bar (hidden on mobile). Sidebar footer and bottom bar aligned at `h-[48px]`. Server label → "tmux server:". Consistent dropdown density (`text-sm py-2`). New "Keyboard Shortcuts" command palette action opens modal fetching `GET /api/keybindings` — shows curated tmux bindings grouped by table (root vs prefix), plus hardcoded `Cmd+K`. (9ldy)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — PWA meta tags and theme-color sync — `theme-color`, `apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style`, `apple-touch-icon` in `index.html`. Theme-color updated by blocking script (initial) and `applyTheme()` (runtime). Dark `#0f1117`, light `#f8f9fb`. Icon set in `public/icons/`. Standalone display mode. (j9a2)

## 2026-03-18
- **Update** [architecture](/run-kit/architecture.md) — feat: Homebrew deployment system with Cobra CLI and embed.FS
- **Update** [architecture](/run-kit/architecture.md) — feat: Replace byobu with dedicated tmux server (-L runkit)
- **Update** [architecture](/run-kit/architecture.md) — fix: Theme context double-read, button type attr, favicon load order, add tests
- **Update** [architecture](/run-kit/architecture.md) — feat: Add light theme support with system/light/dark modes
- **Update** [architecture](/run-kit/architecture.md) — **Dedicated tmux server** — All run-kit sessions live on a named tmux server `runkit` (via `tmux -L runkit`). `internal/tmux` commands prefixed with `-L runkit` and optional `-f` from `RK_TMUX_CONF` env var. `ListSessions()` queries both runkit and default servers, returning `SessionInfo` with `Server` field. `ListWindows()` accepts server parameter. `CreateSession()` uses plain `tmux new-session` on runkit server (byobu dependency removed, `sync.OnceValue` detection deleted). `ProjectSession` type gains `Server` field. Relay attaches to runkit server. New `internal/tmux/tmux.conf` with dark-themed status bar and F2/F3/F4 keybindings. Frontend: `ProjectSession` type gains `server` field, sidebar shows `↗` marker for default-server sessions. (0gjh)
- **Update** [architecture](/run-kit/architecture.md) — **Light theme support** — ThemeProvider context (outermost, split pattern) with three modes (system/light/dark). CSS `data-theme` attribute on `<html>` switches color tokens via `globals.css` selectors. Blocking inline script in `index.html` for no-flicker init. xterm terminal theme updates live. Theme switcher in command palette. Provider tree: `ThemeProvider > ChromeProvider > SessionProvider > AppShell`. (eseg)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — feat: Replace byobu with dedicated tmux server (-L runkit)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — Rewrote for multi-server architecture — dedicated `runkit` tmux server replaces byobu integration. `ListSessions()` queries both runkit and default servers. `parseSessions()` extracted as testable function with server tagging. `CreateSession()` uses plain `tmux new-session` (byobu dependency removed). `ListWindows()` accepts server parameter. (0gjh)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Replace byobu with dedicated tmux server (-L runkit)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Inline tab rename on double-click in sidebar
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Theme context double-read, button type attr, favicon load order, add tests
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Add light theme support with system/light/dark modes
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Sidebar external session marker — `ProjectSession` type gains `server` field (`"runkit"` or `"default"`). Session rows show `↗` marker for default-server sessions (`text-[10px] text-text-secondary/50`, `aria-label="external session"`). Runkit-server sessions have no marker. (0gjh)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Inline tab rename — double-click window name in sidebar to edit inline (Enter/blur commits, Escape cancels, empty input cancels). Local state in Sidebar, no new dependencies. Existing command palette rename unchanged (dcl9)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Light theme support — three-mode theme system (system/light/dark), `data-theme` attribute on `<html>`, CSS custom properties per theme, blocking init script for no-flicker, ThemeProvider context (split pattern), xterm live theme update, command palette theme switcher, `--color-bg-inset` token replaces hardcoded fixed-width bg (eseg)

## 2026-03-17
- **Update** [architecture](/run-kit/architecture.md) — **Homebrew deployment system** — Cobra CLI with subcommands. `embed.FS` bundles frontend into single binary. `VERSION` file + ldflags injection. `scripts/build.sh` (frontend-first build), `scripts/release.sh` (semver bump + tag). GitHub Actions CI/CD cross-compiles 4 targets (darwin/linux x arm64/amd64) on `v*` tag push, creates GitHub Release with tarballs. SPA handler dual-mode: embedded FS (production) or filesystem (dev). (ukyz)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Fix xterm Terminal Copy to Clipboard (#49)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — docs: Default Session Name from Folder Path (#48)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Default session name from folder — Create Session dialog derives name from path when name field is empty at submit time. Create button enabled when path is set (even without explicit name). Derived name collision checked with error display (qiza)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Fix xterm clipboard copy — `copyToClipboard` helper with `navigator.clipboard.writeText()` primary + `document.execCommand('copy')` fallback for non-secure HTTP contexts. Selection cleared via `.finally()`. Exported for testability (rpqx)

## 2026-03-15
- **Update** [architecture](/run-kit/architecture.md) — fix: Per-Region Scroll Behavior (#47)
- **Update** [architecture](/run-kit/architecture.md) — feat: Add Dashboard with project and page views (#45)
- **Update** [architecture](/run-kit/architecture.md) — feat: Top Bar & Bottom Bar UI Refresh (#46)
- **Update** [architecture](/run-kit/architecture.md) — **Dashboard view** — `/` renders `Dashboard` component inline in `app.tsx` terminal area (not a separate route layout). Expandable session cards with window cards, stats line, create buttons. Bottom bar conditionally rendered (terminal pages only). Top bar adapts: shows "Dashboard" text on `/`, breadcrumbs on `/:session/:window`. Sidebar session name click navigates to first window; chevron toggles expand/collapse. All kill operations and stale URL detection redirect to `/`. Auto-redirect from `/` to first session removed. (ll1j)
- **Update** [architecture](/run-kit/architecture.md) — **Per-region scroll behavior** — Dashboard restructured: pinned stats line (`shrink-0`) + scrollable card area (`flex-1 min-h-0 overflow-y-auto`). `useVisualViewport` hook manages `fullbleed` class lifecycle on `<html>` (add on mount, remove on cleanup). Static `fullbleed` in `index.html` serves as FOUC prevention. (lnrb)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Per-Region Scroll Behavior (#47)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Add Dashboard with project and page views (#45)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Top Bar & Bottom Bar UI Refresh (#46)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Dashboard view — `/` renders Dashboard component (session cards grid with expandable window cards, stats line, New Session/New Window buttons) instead of redirecting. Top bar shows "Dashboard" text on `/`, no breadcrumbs. Bottom bar hidden on Dashboard. Sidebar session name click navigates to first window (chevron toggles expand/collapse). All kill operations redirect to `/`. Stale URL detection redirects to `/` (ll1j)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Per-region scroll behavior — Dashboard split into pinned stats line (`shrink-0`) + scrollable card area (`flex-1 min-h-0 overflow-y-auto`). `useVisualViewport` hook now adds `fullbleed` class to `<html>` on mount (lifecycle management). Fullbleed activates `overflow: hidden` on html/body, preventing browser scrollbar on terminal pages (lnrb)

## 2026-03-14
- **Update** [architecture](/run-kit/architecture.md) — feat: Relay session validation, 4004 handling, and memory updates
- **Update** [architecture](/run-kit/architecture.md) — refactor: Rename RUN_KIT_* env vars to RK_*, overhaul supervisor
- **Update** [architecture](/run-kit/architecture.md) — docs: Pane Map Enrichment (#41)
- **Update** [architecture](/run-kit/architecture.md) — **Relay session validation** — relay handler validates session/window exist before attaching PTY. Returns WebSocket close code `4004` for missing session or window (distinct from `4001` PTY failure). Frontend handles `4004` by navigating to `/` instead of reconnecting. Prevents infinite reconnect loops when navigating to a non-existent tmux session.
- **Update** [architecture](/run-kit/architecture.md) — ~~**Byobu session creation**~~ — Superseded by dedicated tmux server (`260318-0gjh-dedicated-tmux-server`). `CreateSession()` now uses plain `tmux new-session` on the `runkit` server with its own config (`internal/tmux/tmux.conf`).
- **Update** [architecture](/run-kit/architecture.md) — **Pane-map enrichment** — replaced per-session `.fab-status.yaml` + `.fab-runtime.yaml` file reading with single `fab-go pane-map --json --all-sessions` subprocess call. Per-window fab state (change, stage, agent state, idle duration) instead of per-session. Deleted `internal/fab/` package (4 files). `internal/sessions` simplified: removed `enrichSession()`, `hasFabKit()`, `runtimeCache sync.Map`. New `fetchPaneMap(repoRoot)` + map join. (3vlx)
- **Update** [architecture](/run-kit/architecture.md) — **Top bar & bottom bar refresh** — Top bar: hamburger icon (☰→✕ animation) replaces logo as toggle, `/` separator replaces `❯`, session/window names are dropdown triggers, session name max 7ch. Right section: logo (decorative) + "Run Kit" + green dot (no text) + toggle + ⌘K + >_ compose. Mobile: ⋯ + >_. Bottom bar: removed Cmd modifier and compose button, sizes increased to 36px/44px. `onOpenCompose` moved from BottomBarProps to TopBarProps. (9raw)
- **Update** [tmux-sessions](/run-kit/tmux-sessions.md) — feat: Relay session validation, 4004 handling, and memory updates
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Relay session validation, 4004 handling, and memory updates
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Remove Top Bar Line 2 (#42)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Top bar & bottom bar refresh — hamburger icon replaces logo as sidebar toggle (animates ☰→✕), `/` separator replaces `❯`, session/window names are dropdown triggers (max 7ch session name). Top bar right: logo (decorative) + "Run Kit" + green dot (no text) + toggle + ⌘K + >_ compose. Mobile right: ⋯ + >_. Bottom bar: removed Cmd modifier and compose button, button sizes increased to 36px desktop / 44px touch (9raw)

## 2026-03-13
- **Update** [architecture](/run-kit/architecture.md) — refactor: Remove Single-Key Keyboard Shortcuts (#40)
- **Update** [architecture](/run-kit/architecture.md) — Update architecture memory for env var config switch
- **Update** [architecture](/run-kit/architecture.md) — feat: rich sidebar window status with agent state and info popover (#38)
- **Update** [architecture](/run-kit/architecture.md) — refactor: UI Chrome & Layout Refinements (#36)
- **Update** [architecture](/run-kit/architecture.md) — **Env var config** — replaced `run-kit.yaml` with `.env`/`.env.local` (direnv). Go and Vite read `RK_PORT`/`RK_HOST` directly. Dev mode: Vite on `RK_PORT`, Go on `PORT+1` (translated by `dev.sh`). Prod: Go on `RK_PORT`. All entry points accept `--port`. Removed CLI flag parsing and YAML config from Go.
- **Update** [architecture](/run-kit/architecture.md) — **Removed single-key shortcuts** — deleted `useKeyboardNav` (j/k/Enter sidebar nav), `useAppShortcuts` (c/r/Esc Esc), sidebar `focusedIndex` prop and focus ring styling. Cmd+K is now the sole keyboard shortcut. Palette actions no longer display shortcut hints. (3brm)
- **Update** [architecture](/run-kit/architecture.md) — **Rich sidebar window status** — Backend: `internal/tmux` adds `PaneCommand` + `ActivityTimestamp` to `WindowInfo` via 6-field tmux format string. New `internal/fab/runtime.go` reads `.fab-runtime.yaml` for agent idle state. `internal/sessions` enriches with runtime state (cached per project root via `sync.Map`). Frontend: sidebar window rows gain activity dot ring, idle duration, info popover. Top bar Line 2 enriched with paneCommand, duration, fab change ID+slug. Shared helpers in `lib/format.ts`. (txna)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Remove Single-Key Keyboard Shortcuts (#40)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: enable xterm clipboard, web-links, and WebGL addons (#39)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Updating docs
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: rich sidebar window status with agent state and info popover (#38)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: sidebar selected styling, bottom bar layout, intake for removing shortcuts
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: UI Chrome & Layout Refinements (#36)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Removed single-key shortcuts — deleted `useKeyboardNav` (j/k/Enter), `useAppShortcuts` (c/r/Esc Esc), sidebar focus ring (`focusedIndex`). Cmd+K command palette is now the sole keyboard shortcut. Palette actions no longer show shortcut hints for create/rename (3brm)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — xterm addon activation — ClipboardAddon (OSC 52), WebLinksAddon (clickable URLs), WebglAddon (GPU rendering with silent canvas fallback), Cmd+C selection-aware copy via `attachCustomKeyEventHandler` (dr60)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Rich sidebar window status — activity dot ring for `isActiveWindow`, idle duration display, info popover (change, process, path, state), shared format helpers (`lib/format.ts`). Top bar Line 2 enriched with paneCommand, duration, fab change ID+slug. Backend: `paneCommand` + `activityTimestamp` from tmux, `.fab-runtime.yaml` reading for agent state (txna)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Remove top bar Line 2 — deleted action bar (+ Session, Rename, Kill, window status). FixedWidthToggle relocated to Line 1 (between connection indicator and ⌘K). BreadcrumbDropdown gains `action` prop for `+ New Session`/`+ New Window` as first dropdown item with divider. Sidebar empty state shows `+ New Session` button. Top bar is now single-line on all viewports (zvgc)

## 2026-03-12
- **Update** [architecture](/run-kit/architecture.md) — chore: Cleanup Old Implementation (#35)
- **Update** [architecture](/run-kit/architecture.md) — refactor: Vite/React Frontend (#34)
- **Update** [architecture](/run-kit/architecture.md) — refactor: Go Backend API (#33)
- **Update** [architecture](/run-kit/architecture.md) — docs: Scaffold app/ Folder Structure (#32)
- **Update** [architecture](/run-kit/architecture.md) — **Cleanup old implementation** — removed legacy backend and frontend directories, `e2e/`, root `playwright.config.ts`. Updated `pnpm-workspace.yaml` to `["app/frontend"]`. Removed legacy test sections and stale path references from memory. (n11e)
- **Update** [architecture](/run-kit/architecture.md) — **Go backend API at `app/backend/`** — handler files split by resource domain (sessions.go, windows.go, etc.). POST-only mutations with path-based intent. `internal/fab` rewritten to read `.fab-status.yaml` directly (no subprocess). Per-session fab enrichment model. `WindowInfo` fields changed: `FabChange`/`FabStage` replace `FabStage`/`FabProgress`. Upload endpoint session from URL path. Handler integration tests via `httptest.NewRecorder` + mock interfaces. SPA serves from `app/frontend/dist/`. (r4t9)
- **Update** [architecture](/run-kit/architecture.md) — **Vite/React frontend at `app/frontend/`** — single-view UI (sidebar + terminal, one route `/:session/:window`), POST-only API client with path-based intent, ChromeProvider derives from selection (no slot injection), sidebar with session/window tree + mobile drawer, MSW-backed Vitest, Playwright E2E at `app/frontend/tests/e2e/` (ux92)
- **Update** [architecture](/run-kit/architecture.md) — **UI chrome layout refinements** — bottom bar moved inside terminal column (width tracks terminal, not viewport). Sidebar drag-resizable (default 220px, min 160, max 400, localStorage persist). Top bar `border-b`, bottom bar `border-t`. Breadcrumbs simplified to `☰ {logo} ❯ session ❯ window`. `[+ Session]` button added to top bar line 2. (y4ci)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — chore: Cleanup Old Implementation (#35)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Vite/React Frontend (#34)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Single-view UI model — sidebar + terminal replaces three-page navigation, POST-only API client with path-based intent, ChromeProvider derives from selection (no slot injection), fullbleed always on, no max-width constraint, sidebar with session/window tree and mobile drawer (ux92)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — UI chrome refinements — simplified breadcrumbs (`☰ {logo} ❯ session ❯ window`, removed `⬡` and `›`), drag-resizable sidebar (default 220px, min 160, max 400, localStorage persist), bottom bar moved inside terminal column (`border-t border-border`, `py-1.5`), top bar `border-b border-border`, `[+ Session]` button in top bar line 2, sidebar footer removed, padding consistency (`px-3 sm:px-6` sidebar, `py-0.5 px-1` terminal container) (y4ci)

## 2026-03-10
- **Update** [architecture](/run-kit/architecture.md) — refactor: Go backend + Vite SPA split (#31)
- **Update** [architecture](/run-kit/architecture.md) — **Go backend + Vite SPA split** — replaced Next.js monolith with Go backend + Vite React SPA. Single-port architecture (API, SSE, WebSocket relay, SPA static serving on one Go binary). chi router, gorilla/websocket, creack/pty. TanStack Router for client-side routing. Typed API client module. Go table-driven tests ported from Vitest. E2E tests updated for Go + Vite dev servers. (8xaq)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Go backend + Vite SPA split (#31)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Go backend + Vite SPA split — removed Server Component patterns, all data fetching via API client + SSE context, TanStack Router for client-side routing, terminal WebSocket on same port (no relay port config) (8xaq)

## 2026-03-07
- **Update** [architecture](/run-kit/architecture.md) — test: Playwright E2E tests for UI design spec (#28)
- **Update** [architecture](/run-kit/architecture.md) — feat: Navbar Breadcrumb Dropdowns (#23)
- **Update** [architecture](/run-kit/architecture.md) — fix: Sync byobu active tab with run-kit UI (#22)
- **Update** [architecture](/run-kit/architecture.md) — fix: iOS Keyboard Viewport Overlap (#21)
- **Update** [architecture](/run-kit/architecture.md) — Add file upload to terminal sessions (#20)
- **Update** [architecture](/run-kit/architecture.md) — fix: Fix iOS Terminal Touch Scroll (#17)
- **Update** [architecture](/run-kit/architecture.md) — refactor: Rename Action + Kill Label Cleanup (#16)
- **Update** [architecture](/run-kit/architecture.md) — docs: Fix memory gaps and fill project context (#14)
- **Update** [architecture](/run-kit/architecture.md) — iOS touch scroll prevention — fullbleed class toggle on html, touch-none on terminal container (8n60)
- **Update** [architecture](/run-kit/architecture.md) — Sync byobu active tab — `isActiveWindow` on `WindowInfo`, breadcrumb/URL/action sync via SSE + `history.replaceState` (f3li)
- **Update** [architecture](/run-kit/architecture.md) — iOS keyboard viewport overlap fix — visualViewport scroll listener, fixed positioning in fullbleed mode (f3o9)
- **Update** [architecture](/run-kit/architecture.md) — File upload: `/api/upload` endpoint, clipboard paste/drag-drop/file picker triggers, compose buffer integration, `.uploads/` auto-gitignore (kqio)
- **Update** [architecture](/run-kit/architecture.md) — Playwright E2E tests — chrome stability, breadcrumbs, bottom bar, compose buffer, kill button, mobile viewport (r7zs)
- **Update** [architecture](/run-kit/architecture.md) — Breadcrumb type extended with `dropdownItems` for project/window switching dropdowns (uzsa)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Mobile bottom bar & breadcrumb cleanup (#26)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Mobile Responsive Polish (#27)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — feat: Navbar Breadcrumb Dropdowns (#23)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Sync byobu active tab with run-kit UI (#22)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: iOS Keyboard Viewport Overlap (#21)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Add file upload to terminal sessions (#20)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — fix: Fix iOS Terminal Touch Scroll (#17)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — refactor: Rename Action + Kill Label Cleanup (#16)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — docs: Fix memory gaps and fill project context (#14)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — iOS touch scroll fix — `touch-none` on terminal container, fullbleed class toggle for body overflow/overscroll prevention (8n60)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Active window sync — breadcrumb, URL, rename/kill targets follow byobu/tmux window switches via SSE + `history.replaceState` (f3li)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — iOS keyboard viewport overlap fix — scroll+resize listeners on visualViewport, fixed positioning for app-shell in fullbleed (f3o9)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — File upload: clipboard paste, drag-and-drop, file picker button, compose buffer path insertion, command palette action (kqio)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Mobile cleanup — merged F-key/ext-key popups, moved upload to compose buffer, added keyboard dismiss button, breadcrumb icons as dropdown triggers (l9jj)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Mobile responsive polish — Line 2 collapse with ⋯ palette trigger, 44px touch targets via `coarse:` variant, responsive padding (px-3/px-6), terminal font scaling (11px/13px) (ol5d)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Rename window action (both pages), kill button label shortened to "Kill" (r3yv)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Breadcrumb dropdown menus — chevron triggers for project/window switching, split click-target pattern (uzsa)

## 2026-03-06
- **Update** [architecture](/run-kit/architecture.md) — Perf: parallel sessions, SSE pub/sub, split ChromeContext, SessionProvider (#12)
- **Update** [architecture](/run-kit/architecture.md) — Add bottom bar with modifier keys, compose buffer, and iOS keyboard support (#10)
- **Update** [architecture](/run-kit/architecture.md) — Replace per-page TopBar with layout-owned chrome architecture
- **Update** [architecture](/run-kit/architecture.md) — Performance: parallel session enrichment, SSE pub/sub singleton, split ChromeContext, layout-level SessionProvider, ResizeObserver debounce, useModifierState memoization, WS reconnection (0ahl)
- **Update** [architecture](/run-kit/architecture.md) — Chrome architecture — layout-owned flex-col skeleton, ChromeProvider context, TopBarChrome, icon breadcrumbs, always-visible kill buttons (emla)
- **Update** [architecture](/run-kit/architecture.md) — Bottom bar (modifier toggles, arrow keys, Fn dropdown, compose buffer), iOS keyboard support via visualViewport, `i` key compose toggle (fjh1)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Perf: parallel sessions, SSE pub/sub, split ChromeContext, SessionProvider (#12)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Add bottom bar with modifier keys, compose buffer, and iOS keyboard support (#10)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Fix Copilot review: breadcrumb separator in docs, spec/impl alignment
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Replace per-page TopBar with layout-owned chrome architecture
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Performance: split ChromeContext (state/dispatch), layout-level SessionProvider, inline dashboard search, memoized shortcuts (0ahl)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Chrome architecture — layout-owned skeleton, ChromeProvider context, TopBarChrome, icon breadcrumbs, always-visible kill buttons (emla)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Bottom bar with modifier toggles, arrow keys, Fn dropdown, compose buffer, iOS keyboard support (fjh1)

## 2026-03-05
- **Update** [architecture](/run-kit/architecture.md) — Add session folder picker with server-side directory autocomplete
- **Update** [architecture](/run-kit/architecture.md) — Add feature tests for tmux, keyboard nav, and sessions API
- **Update** [architecture](/run-kit/architecture.md) — Set up Vitest testing infrastructure with feature tests
- **Update** [architecture](/run-kit/architecture.md) — Added Vitest testing infrastructure with validate, config, and command-palette tests (07iq)
- **Update** [architecture](/run-kit/architecture.md) — Added feature tests for tmux.ts, use-keyboard-nav.ts, and api/sessions POST handler (vq7h)
- **Update** [architecture](/run-kit/architecture.md) — Added `/api/directories` endpoint, `createSession` CWD support, `expandTilde` security boundary (zkem)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Add session folder picker with server-side directory autocomplete
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Create Session dialog with folder picker — quick picks, server-side autocomplete, name auto-derivation (zkem)

## 2026-03-03
- **Update** [architecture](/run-kit/architecture.md) — Filter byobu session-group copies, simplify relay to tmux attach
- **Update** [architecture](/run-kit/architecture.md) — Hydrate architecture memory with configurable port/host binding
- **Update** [architecture](/run-kit/architecture.md) — Unified top bar with breadcrumb navigation, action bars, and kill controls
- **Update** [architecture](/run-kit/architecture.md) — Drop run-kit.yaml config — derive project state from tmux
- **Creation** [architecture](/run-kit/architecture.md) — Add v1 web agent orchestration dashboard
- **Update** [architecture](/run-kit/architecture.md) — Relay port via server component prop (replaced build-time env var)
- **Update** [architecture](/run-kit/architecture.md) — Filter byobu session-group copies from `listSessions()`
- **Update** [architecture](/run-kit/architecture.md) — Configurable port/host binding via `config.ts` + `run-kit.yaml` (q8a9)
- **Update** [architecture](/run-kit/architecture.md) — Added `killSession` API action — kills entire tmux session (vag8)
- **Update** [architecture](/run-kit/architecture.md) — Removed `run-kit.yaml` config — derive project state from tmux (yohq)
- **Creation** [tmux-sessions](/run-kit/tmux-sessions.md) — Filter byobu session-group copies, simplify relay to tmux attach
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Unified top bar with breadcrumb navigation, action bars, and kill controls
- **Creation** [ui-patterns](/run-kit/ui-patterns.md) — Add v1 web agent orchestration dashboard
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Unified top bar — shared breadcrumb + action bar, inline kill controls, command palette on terminal, always-visible search (vag8)

## 2026-03-02
- **Update** [architecture](/run-kit/architecture.md) — Initial architecture — greenfield v1 (fl88)
- **Update** [ui-patterns](/run-kit/ui-patterns.md) — Initial UI patterns — three pages, keyboard-first, dark theme (fl88)
