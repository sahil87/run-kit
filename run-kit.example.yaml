# run-kit configuration — copy to run-kit.yaml and edit as needed.
# All values are optional; defaults shown below.

server:
  port: 3000        # Next.js port
  relay_port: 3001  # Terminal relay WebSocket port
  host: 127.0.0.1   # Bind address (use 0.0.0.0 to expose to network)
