---
description: "Frontend UI patterns, split by surface: routes + shell, top bar, sidebar, status signals, lenses + surface layout, boards, compose strip, keyboard + palette, terminal frontend, visual design, dialogs + client state, updates + notifications."
---
# Ui Documentation

> **Generated by `fab docs-index`** — do not hand-edit; only the manual block below is preserved.

<!-- fab docs-index:manual:start -->
<!-- Hand-managed; preserved verbatim on regeneration. Add rows the generator
     cannot produce: non-markdown files' descriptions (HTML, PDF), external links,
     custom groupings. A row for a file the walker also indexes keeps its place and
     label here, but its description follows that file's frontmatter. A row
     whose target no longer exists is flagged by `fab docs-index --check`. -->
<!-- fab docs-index:manual:end -->

| File | Description |
|------|-------------|
| [boards](boards.md) | Boards: link-based pinning, board page layout, pane cards, kill confirm, autofit, pane reorder, relay-connection suspension, mobile carousel, sidebar boards section, pin entry points, board/pin/view palette actions, frontend hooks. |
| [compose-and-bottom-bar](compose-and-bottom-bar.md) | Bottom bar (coarse-pointer-only) and docked compose strip: pointer-gated bar existence, mechanism-blind mode-carrying send semantics, visible staged-text recovery and in-flight locking, the Enter classifier, drafts + sent history, readline editing chords, file upload, iOS keyboard support, the card/compact layout, edge-docked padding, focus memory, and the stateful compose chord. |
| [cron-activity](cron-activity.md) | The mobile cron Activity feed — the operator terminal route's Terminal|Activity segment (?tab=activity): one time-ordered timeline merging upcoming fires with recent deliveries around a now-divider (muted/orphaned dimmed, never omitted), the pinned operator-staleness banner (operatorStale/operatorLastTickAt off the sessions payload), the entry detail sheet (mute/pin/delete, optimistic toggles), the describeSchedule plain-words helper, and the useCronData hook riding the SSE sessions cadence. |
| [dialogs-and-state](dialogs-and-state.md) | Component conventions; modal dialogs and popup surfaces including the spring-loaded marker pad; clipboard and e2e host-state rules; the Zustand window store, optimistic mutation feedback, and recently-closed mirror. |
| [focus-ownership](focus-ownership.md) | Per-window focus memory (tty/compose/code) and the code-server steal guard: real-focus-only recording asymmetry, the desktop restore router with its tty first-visit default, the guard arm/disarm lifecycle, the in-frame focusin grab detector, the focus-seam consumers (the surface-chord family, the web tile's IframeWindow, the tty ⌘F claim on the mirrored focused kind), and the operator console's own guarded origin ref with the two invariants that make it yield focus to a terminal pane. |
| [keyboard-and-palette](keyboard-and-palette.md) | Keyboard shortcuts: tiers, the e.code rule, defaults, surface restrictions, macros, overrides, claimed keys, and the Shortcuts tab; the single palette mount and action registry, including recovery, server lifecycle and server color, operator requests and operator window identity, the operator-console ⌘J three-state machine chord and the palette's Ask-operator fallback row, session sorting, cron clock mutations, web zoom/tabs, Tab: Marker, and stack-gated Tab: Reopen closed. |
| [lenses-and-layout](lenses-and-layout.md) | The window-view lens model (tty/web/code — `gui` planned), surface layouts, tile/zen chords, top-bar surface controls, and iframe/code renderers. Covers web browser chrome with URL-keyed frames, the address-bar submit ladder (URL/path/port lanes), viewer-local draft tabs, tab reorder and metadata, onboarding, FindBar and zoom; the code surface's derived-workspace src, mount gating, and code-root seed/follow; tty progress/export; and tile focus-memory and steal-guard seams. |
| [operator-console](operator-console.md) | The operator chat console — a desktop quake overlay and mobile navigation to the operator terminal route. Covers the rk:operator-console event seam, the desktop operator-route no-op toast gate, the ⌘J two-state machine, mobile ?from= context and draft seeding, the omnibox and shared compose lanes, file paste, availability gating, and the mobile Terminal|Activity segments. |
| [routes-and-shell](routes-and-shell.md) | Route surface and app shell: URL structure as resumable bookmark, server-gone flip, session tiles, shell grid layout, operator-console overlay mount, live safe-name conversion, session creation flows, instance display name, session-to-project mapping, activity status, host recovery zone, HOST HEALTH system card (daemon + service rows, View deep-links), TMUX SERVERS server-class tile markers (scratch chip, protected shield, external ↗), and the dev-only `/__controls` control gallery. |
| [sidebar](sidebar.md) | Sidebar: row anatomy, marker strip and pad entry, row-hover flyout cards, coarse status rail, flair overlays, render performance, keyboard/tree ARIA, multi-select and broadcast, collapsible panels (PANE/Host + the desktop-only CLOCK cron section), section rail, the watched-row ◉ indicator, host-page health/boards/services, server-class glyphs, kill controls, scroll fade, the operator pinned row, session-row viewer chip, the hover-reveal contract + tappable floors, and footer. |
| [status-signals](status-signals.md) | Status signaling: PR registers and glyphs, the compositional status dot, the server-class glyph axis (protected shield + external ↗), waiting/attention surfacing + the neutral count chip, the tty tile header's progress chip, the row flyout card (one shared shell on four mounts — window/session rows, server group header, server tile — on both pointer classes), the two-tier tooltip taxonomy, and the desktop status bar (window-mirror + host clusters, raw-value copy segments, degradation ladder). |
| [terminal](terminal.md) | Terminal frontend: RelayMux singleton + TerminalClient port, hidden-page stream suspension (visibility grace release/resume), adaptive write batching, window-switch slide transition, xterm addons (incl. addon-search buffer find) + the ⇩ export menu, keyboard Option-as-Meta handling, font size preference + bundling, device-split scrollback constants, unicode width handling, touch scroll, ctrl-wheel/pinch font-zoom gestures, coarse-pointer focus suppression. |
| [top-bar](top-bar.md) | Top-bar chrome: persistent root mount, 3-column grid, center heading + window rename + operator glyph + the operator omnibox (≥lg standing box, md–lg ghost morph) with its live state dot, control cluster + overflow menu, flush surface-toggle group (desktop toggle / pinned mobile switch), Open split-button, refresh, breadcrumb dropdowns + the crumb collapse rung, host switcher, desktop-shell titlebar strip and badge reporter, and the shared control-glyph register. |
| [updates-and-notifications](updates-and-notifications.md) | Browser Web Push and desktop-shell OS notification opt-in, delivery, dedupe, and deep links; plus update-notification feeds, reload guard, UpdateChip, palette actions, maintenance entries, and version surfaces. |
| [visual-design](visual-design.md) | Visual design system: color tokens, borders, themes, hover-animation vocabulary, control-state rules (focus ring, pressed fill, select guard), the Control primitive (controlClass variants × states, structural REST-swap, green latch/open-latch, menu-row scale) owning the control vocabulary, controls.ts survivors (POPOVER_*, SWITCH_*, INPUT_*), --ctl-* tokens, /__controls gallery drift guard, logo/heading treatments, color tinting, marker vocabulary, row textures, flair overlays, touch targets. |
