---
description: "Frontend UI patterns, split by surface: routes + shell, top bar, sidebar, status signals, lenses + surface layout, boards, compose strip, keyboard + palette, terminal frontend, visual design, dialogs + client state, updates + notifications."
---
# Ui Documentation

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [boards](boards.md) | Boards: link-based pinning, board page layout, pane cards, kill confirm, autofit, pane reorder, relay-connection suspension, mobile carousel, sidebar boards section, pin entry points, board/pin/view palette actions, frontend hooks. |
| [compose-and-bottom-bar](compose-and-bottom-bar.md) | Bottom bar (coarse-pointer-only) and docked compose strip: pointer-gated bar existence, mechanism-blind mode-carrying send semantics, visible staged-text recovery and in-flight locking, the Enter classifier, drafts + sent history, readline editing chords, file upload, iOS keyboard support, the card/compact layout, edge-docked padding, focus memory, and the stateful compose chord. |
| [dialogs-and-state](dialogs-and-state.md) | Component conventions; modal dialogs and popup surfaces including the spring-loaded marker pad; clipboard and e2e host-state rules; the Zustand window store, optimistic mutation feedback, and recently-closed mirror. |
| [focus-ownership](focus-ownership.md) | Per-window focus memory (tty/compose/code) and the code-server steal guard: real-focus-only recording asymmetry, the desktop restore router with its tty first-visit default, the guard arm/disarm lifecycle, the in-frame focusin grab detector, and the focus-seam consumers — the surface-chord family (recording only on the tty/compose arms), the web tile's IframeWindow reporting through the same seam shape without recording, and the tty ⌘F claim gating on the mirrored focused kind. |
| [keyboard-and-palette](keyboard-and-palette.md) | Keyboard shortcuts: tiers, the e.code rule, defaults, surface restrictions and reclaiming, macros, overrides, claimed keys, and the Settings Shortcuts tab; the single palette mount and action registry, including recovery, server lifecycle and server color, operator requests and operator window identity, the operator-console ⌘J three-state machine chord and the palette's Ask-operator free-text fallback row, session sorting, web zoom/tabs, Tab: Marker, and stack-gated Tab: Reopen closed. |
| [lenses-and-layout](lenses-and-layout.md) | The window-view lens model (tty/web/code/desktop), surface layouts, tile/zen chords, top-bar surface controls, and iframe/code renderers. Covers web browser chrome with URL-keyed frames, viewer-local draft tabs, tab reorder and metadata, onboarding, FindBar and zoom; code-root seed/follow; tty progress/export; and tile focus-memory and steal-guard seams. |
| [operator-console](operator-console.md) | The operator chat console — a quake-style overlay (slide-animated, resizable glass desktop drawer; mobile full-height sheet) mounted once at the root layout. Covers the rk:operator-console event seam, the ⌘J three-state machine, the omnibox (compose relocated into the top-bar center) driving one shared compose seam with the mobile sheet, the one-input rule (desktop drawer output-only), insert-staged file paste/drop, geometry/opacity stores, server resolution, and degrade-to-absent gating. |
| [routes-and-shell](routes-and-shell.md) | Route surface and app shell: URL structure as resumable bookmark, server-gone flip, session tiles, shell grid layout, the operator-console overlay mount, live safe-name conversion, session creation flows, instance display name, session-to-project mapping, activity status, the host recovery zone, the HOST HEALTH system card (daemon + service rows, View deep-links), and server-class tile markers on the TMUX SERVERS grid (scratch chip, protected shield, external ↗ glyphs). |
| [sidebar](sidebar.md) | Sidebar: row anatomy, interactive marker strip and pad entry, row-hover flyout cards, coarse status rail, flair overlays, render performance, keyboard/tree ARIA, multi-select and broadcast, collapsible panels, section rail, host-page health/boards/services, server-class glyphs, kill controls, scroll fade, the operator pinned row (identity glyph, open-console activation, @rk_win_note pulse), session-row viewer chip, and footer. |
| [status-signals](status-signals.md) | Status signaling: PR registers and glyphs, the compositional status dot, the server-class glyph axis (protected shield + external ↗), waiting/attention surfacing + the neutral count chip, the tty tile header's progress chip, the row flyout card (one shared shell on four mounts — window/session rows, server group header, server tile — on both pointer classes), the two-tier tooltip taxonomy, and the desktop status bar (window-mirror + host clusters, raw-value copy segments, degradation ladder). |
| [terminal](terminal.md) | Terminal frontend: RelayMux singleton + TerminalClient port, hidden-page stream suspension (visibility grace release/resume), adaptive write batching, window-switch slide transition, xterm addons (incl. addon-search buffer find) + the ⇩ export menu, keyboard Option-as-Meta handling, font size preference + bundling, device-split scrollback constants, unicode width handling, touch scroll, ctrl-wheel/pinch font-zoom gestures, coarse-pointer focus suppression. |
| [top-bar](top-bar.md) | Top-bar chrome: persistent root mount, 3-column grid, center heading + window rename + operator glyph + the operator omnibox (≥lg standing box, md–lg ghost morph), control cluster + overflow menu, surface-toggle group (desktop toggle / pinned mobile switch), Open split-button, the ◉ operator-console button with its live state dot, refresh, breadcrumb dropdowns + the crumb collapse rung, host switcher, desktop-shell titlebar strip and badge reporter, and the shared control-glyph register. |
| [updates-and-notifications](updates-and-notifications.md) | Browser Web Push and desktop-shell OS notification opt-in, delivery, dedupe, and deep links; plus update-notification feeds, reload guard, UpdateChip, palette actions, maintenance entries, and version surfaces. |
| [visual-design](visual-design.md) | Visual design system: color tokens, borders, themes, no-flicker init, PWA meta, popup elevation, hover-animation vocabulary, logo and heading treatments, operator role identity, color tinting, marker vocabulary and pad wells, row textures and flair overlays, sparkline/gauge renderers, mobile breakpoints, touch targets, and safe areas. |
