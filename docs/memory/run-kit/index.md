---
description: "Web-based agent orchestration dashboard"
---
# run-kit Memory Domain

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [agent-state](agent-state.md) | The `@rk_agent_state` pane-option convention — two-tier ownership, three-state value schema, writer/reader rules, shell reconciler, window rollup, the `rk agent-setup` installer (now HOOKS-ONLY — settings-hooks merge, plus a one-release cleanup of the legacy marker-owned `rk-display` skill it no longer installs; that context-injection role is superseded by the `rk skill` bundle) + `rk agent-hook` binary indirection (stable settings interface, logic in the binary, comm-validated ancestor walk) — plus the sibling `@rk_chat` chat-session-identity convention (`<provider>:<session-ref>` value, stdin-JSON session-id seam in the hook, stamp-only token + SessionStart row, chat reconciliation sharing agent-state's per-pane liveness) |
| [architecture](architecture.md) | System architecture, component responsibilities, data flow — backend libraries (incl. in-memory metrics + prstatus + ports/passive listening-ports collector (all listening TCP ports, no HTTP probe — procfs on Linux + shared lsof.go parser for darwin enumeration & Linux best-effort process attribution), internal/push Web Push subsystem, internal/updatecheck GitHub-Releases periodic checker (6h tick, hand-rolled minor/major semver, OnQualify re-fire on newer latest, dev/unparseable-suppressed), internal/selfpath shared brew-marker + self-path resolver consumed by cmd/rk/upgrade.go AND api/update.go, internal/chat chat subsystem — rk-neutral Event/Pending schema + Adapter registry + Claude JSONL locate/parse/tail (read side derive-from-disk, nothing cached beyond the connection; send side in api/chat.go types into the pane as a human typist), API endpoints (incl. GET /api/windows/{windowId}/chat backfill (gained an additive byte offset field, 260717-vhvz) — the live chat stream moved onto /ws/state as a kind:chat subscription (retired the dedicated per-view chat SSE) + POST /api/windows/{windowId}/chat/send pane-targeted named-buffer bracketed-paste + NOVELTY echo probe + gated Enter (allow+probe busy, per-(server,paneID) whole-sequence lock, 4s shared deadline), POST /api/servers/order + rank field on /api/servers via @rk_server_rank, POST /api/boards/order + API-layer rank-aware board sort with settings.yaml board_order persistence — GET /api/boards response order IS the display order, no rank field/client comparator; POST /api/riff web-UI agent spawn (dialog-v2 additive body where/worktreeName/tier with 400-before-subprocess validation via validate.ValidateWorktreeName/ValidateTier) + GET /api/riff/presets ({presets, tiers} — tiers from fabconfig.ReadTiers, gated on fabconfig.IsFabProject: [] for non-fab repos) surfacing the extracted internal/riff engine via a dedicated RiffEngine interface, repo-root derived from the target session's active-pane cwd → FindGitRoot with a documented synchronous 5s-rule exception; POST /api/update one-click self-upgrade — 409 not-brew / 409 no-update / 202 + detached setsid rk update spawn behind seams, no GET /api/version, {"force":true} body skips qualify-409 but keeps brew-409, plus POST /api/restart 202 + detached rk daemon restart (dev-409, no brew req) via ONE shared spawnSelfFn(logName, args...) seam), state socket (/ws/state — one WebSocket per tab muxing all session-state + host-metrics streams, retired GET /api/sessions/stream, 260716-qf3j; + a kind:chat subscription carrying the chat live stream via a per-subscription TAIL/DORMANT producer goroutine — chat/chat-state verbatim + lightweight chat-reset, ack-with-offset no-snapshot D5, chat never joins the poll set, retired the chat SSE, 260717-vhvz) + terminals mux (/ws/terminals — one WebSocket per tab carrying ALL pane relay streams, retired GET /relay/{windowId} + handleRelay; binary [u32 BE streamId][payload] frames + JSON open/opened/resize/close/closed control ops, per-stream bounded queues 8×4096B + fair control/short-frame-priority round-robin scheduler with PTY-reader backpressure never dropping bytes, 4004/4001/1000 as per-stream closed events not socket closes, kept SEPARATE from /ws/state per D6 — tab budget now a fixed 2 WS, 260717-803u) + PR-status join + metrics-only server-neutral subscription sentinel + server-independent event: services all-listening-ports broadcast (passive, no probe) + server-global event: server-order broadcast (single cached slot, all clients incl. ?metrics=1) + server-global event: board-order broadcast (single cachedBoardOrderJSON slot, all clients incl. ?metrics=1) + server-global event: version (set-once cachedVersionJSON slot, replay-on-connect only; payload {version,boot,brew} — boot = per-process crypto/rand hex regenerated each start for restart-detect reload, brew = startup IsBrewInstalled) + server-global event: update-available (broadcast + cachedUpdateAvailableJSON replay, all clients incl. ?metrics=1) + server-global event: status-refresh (BROADCAST-ONLY, no cached slot/replay, all clients incl. ?metrics=1 — broadcastStatusRefresh from finishStatusRefresh once per completed on-demand refresh pass; tri-state 202 body started/coalesced/throttled on POST /api/status/refresh; PrFetchedAt plumbed through the attachPRStatus collector join) + session-tile previews (dedicated event: preview with per-connection expanded-set scope via POST /api/preview-scope, reusing tmux.CapturePane), version + update-checker plumbed into api.Server via SetVersion/SetUpdateChecker/WireUpdateAvailableBroadcast in serve.go, server-independent host-metrics subscription (useHostMetrics — the state socket's in-band metrics subscription) + tracked host-metrics health (hostMetricsConnected: dedicated metrics-subscription ack health with 3s disconnect debounce + per-server fan-out fallback, for the Host-page connection dot), tmux/sessions enrichment, control-mode subscription, push-on-sustained-waiting episode watcher at the poll seam (api/waiting_push.go: pure decide() + detached-goroutine send + polled-server-scoped retain; one push per ≥15s waiting episode, in-memory only; carries a deep-link url via waitingPushURL — /{server}/{N}?view=chat for chat-capable windows, else plain window URL), Web Push notifications (rk notify → /api/notify → webpush-go → service worker, VAPID + subscriptions under ~/.rk/; push.Notify gained a 4th url param → payload url,omitempty → sw.js showNotification data.url → notificationclick navigates a focused/opened tab, sameOriginPath-guarded against protocol-relative //-redirects + navigate() rejection .catch fallback, root / fallback when absent), branch→PR derivation with stateless/restart-proof merged-PR durability (prstatus_branch.go — `--state all` lookup + `pickBranchPR` precedence open > merged > closed, most-recent-within-class; a merged PR keeps resolving positive every pass so the done-square survives with NO grace clock or negative-stamp retention; closed-unmerged falls back to the fab tier frontend-side) |
| [chat](chat.md) | The chat subsystem — rk-owned neutral chat event schema (Event/Pending/turn counter), the Adapter interface + provider registry, the Claude JSONL adapter (UUID-glob locate, tolerant line-by-line parse, TailFrom(from) offset-tail), the read surface: GET /api/windows/{windowId}/chat backfill (gained an additive `offset` byte-offset field) + the live stream as a `kind:\"chat\"` subscription on /ws/state (subscribe key=windowId + server + from:<offset> + req; ack carries the tail-start offset, NO snapshot — D5; events chat/chat-state verbatim + lightweight chat-reset on rotation/shrink; per-subscription TAIL/DORMANT producer goroutine, ~2s re-resolve, chat never joins the tmux poll set), the frontend consumer: the `?view=chat` chat view over the terminal route (use-chat-subscription hook composing GET-backfill→subscribe gap-free, react-markdown bubbles + collapsible tool cards + tail pending bubble), AND the SEND path (Change 4, chat-send): POST /api/windows/{windowId}/chat/send — pane-targeted named-buffer paste + NOVELTY echo probe + gated Enter, per-(server,paneID) whole-sequence lock, allow+probe busy policy, and the ChatView send-form input box. Read stays derive-from-disk-per-request (Constitution II); send types into the pane exactly as a human typist (Constitution VI — the pane stays the agent's parent, no SDK/session ownership). The dedicated per-view chat SSE (GET /api/windows/{id}/chat/stream) was RETIRED by 260717-vhvz (socket-unification Change 3): the tab now holds a fixed 2 WebSockets + 0 SSE |
| [rk-riff](rk-riff.md) | `rk riff` spawn engine — worktree + tmux window + Claude launcher with argv-ordered pane arrays, presets, named layouts, and parallel fan-out; extracted into internal/riff (parameterized by explicit {server, session, repoRoot}) with the CLI thinned to flags/preconditions/derivation and the web-UI POST /api/riff as a second frontend; mockup-v2 spawn-shaping inputs (Where worktree|checkout conditional wt-create, WorktreeName --worktree-name passthrough via buildWtCreateArgs, tier-parameterized ResolveLauncher via fabAgentArgs) + fabconfig.ReadTiers enumeration + fabconfig.IsFabProject (os.Stat on config.yaml, gates the presets-endpoint tiers to [] for non-fab repos) — all CLI-inert (worktree-only, empty-tier) |
| [tmux-sessions](tmux-sessions.md) | Session enumeration, group filtering, direct-attach relay (transport is the muxed `/ws/terminals` per-stream attach since 260717-803u — retired `/relay/{windowId}`/`handleRelay`; resolve → session-scoped select → direct attach semantics UNCHANGED) + move-based board pin-sessions (`_rk-pin-*`), window addressing, `MoveWindow` reorder with active-window preservation (captures the pre-shuffle active window ID from the existing `list-windows` call and restores it via a session-qualified `select-window` in the same `\;`-chain — no extra subprocess), name-optional managed window creation (`CreateWindow`/`buildCreateWindowArgs` omit `-n` so tmux auto-names to the folder basename via `automatic-rename-format`), SSE poll-set reap on dead-server fetch error (emits an in-band state-socket `gone` event since 260716-qf3j-state-socket), unified test-socket naming + `rk reaper`, env-gated `RK_SERVER_ALLOWLIST` test-scoping, pane-targeted chat-send primitives (`SetChatSendBufferCtx`/`PasteChatSendBufferCtx`/`SendEnterToPaneCtx` + `ChatSendBuffer` named buffer; `set-buffer -- <text>` terminator for leading-dash safety, `paste-buffer -d -p` bracketed paste, probed pane Enter — distinct from the window-targeted `SendKeys`) |
| [toolkit-standards](toolkit-standards.md) | run-kit's sahil87-toolkit-standards conformance posture — constitution binding (§ Toolkit Standards), the audit-against-HEAD-build rule (installed brew rk is stale), and the per-standard status audited @ shll v0.0.23 (help-dump: 1 envelope fix; readme-extraction: 2 closure fixes; principles: P1/P2/P4/P5 fixed — P4 exit-code convention conformant via 260717-rex1 central classification, only P9 deferred [f8yv]; skill: PASS). Deliverable = the PR-body conformance report. |
| [ui-patterns](ui-patterns.md) | URL structure + canonical route page names (Host / tmux Server / Terminal / Board / Not Found — the spoken/doc vocabulary) + the breadcrumb-ends-at-parent / centered-heading-is-the-leaf vocabulary split, three-way server route guard + create-server/server-gone lifecycle, full-width-topbar navbar mounted ONCE at a persistent pathless-layout root (RootTopBar/AppLayout above the outlet — route-derived mode/boardName + page-registered slot context (top-bar-slot-context) with tolerant-empty defaults + dedicated not-found boolean channel; Shell grid dropped its topbar row, useVisualViewport + .app-root fullbleed pin moved to RootWrapper) + TopBar universal-page-heading navbar (`1fr auto 1fr` grid with a universal `PageType: name` center heading on EVERY mode — static `Window: <window>` editable + ▾ window switcher / `Board: <board>` display + ▾ board switcher / `tmux Server: <server>` display / solo `Host`; a stable `sm:min-w-[28ch]` left-anchored inner container, a hierarchy ▾ (ancestors-only, reuses BreadcrumbDropdown) left of the prefix, and browser-history ◀ ▶ arrows inside the anchored box on all four modes + `Go:` palette actions (lib/palette-nav.ts) — 260714-uco1; window switches through navigateToWindow now PUSH history (260715-m4xy: dropped runSwitch's `replace: true`, no dedup) so Back/Forward retrace within-server hops, while SSE URL-writeback + switchView lens toggles stay `replace`; move-don't-copy so the leaf is the centered heading and the left breadcrumb ends at the parent; brand-as-root-crumb, host mode, `›` separator, crumb affordance vocabulary — LINK_CRUMB_CLASS chip vs persistent ▾ caret) + site-wide hover-animation vocabulary (glitch=brand / boot-sweep=top-bar page heading (one inverse-video cursor over `PageType: name`: TypedLabel-style over prefix flowing into decode glyph-churn over name, shared useBootSweep/SweepCells) / brackets+caret=section headings (SectionHeading) / typed-sweep=section labels (TypedLabel invisible-hand typing, inverse-video cursor, ~350ms fixed pass) / CRT glint=buttons — all behind prefers-reduced-motion; utility classes in globals.css) + shared useWindowRename hook (top-bar heading + sidebar inline rename) + shared bracketed SectionHeading on the tmux Server page's Sessions + the four Host-page zones (retired PageHeading) + two new page-overview SectionHeadings (Host Overview `/`, tmux Server Overview `/$server`) + border-width system (1px hairline / 3px structural seam / 8px selection accent), Host-page host-console home (HOST HEALTH + BOARDS + TMUX SERVERS + SERVICES zones on `/`; server-independent useHostMetrics + shared HostMetrics component; useHostServices listening-ports tiles (ALL listening ports, passive — no HTTP probe; best-effort process attribution both platforms; well-known ports < 1024 de-emphasized grey/sorted-last), "Open in window" → @rk_type=iframe /proxy/{port}/ gated solely on server existence (a non-HTTP port yields a harmless user-initiated failed iframe); useBoards board tiles → /board/$name), SessionTiles density view (session→window tile grid on /$server with static capture-pane text previews via SSE event: preview; replaced the deleted Dashboard) + sidebar/boards components, unified StatusDot (palette-v3 two-family ladder — cool fab blue/green/purple, warm ad-hoc agent yellow/orange, gray floor; core hue=phase / shape=status / additive constant-yellow waiting halo; D1 per-family PR ownership, D2 closed-PR fab fallback + stateless/restart-proof merged durability (backend `--state all` derivation, no grace clock); reused on sidebar row, SessionTiles window tile, pane-panel header) + Row Minimalism (window row = leading dot only; stage word + duration removed) + PANE panel four-register view (out/agt/fab/PR L0–L3 — 3-char prefix vocabulary, orthogonal, PR ungated from fabChange) + custom StatusDotTip floating-ui hover-card (replaces native title; agent line every tier + universal Open-PR link + docs icon; hover/focus open, no tooltip ARIA role) + waiting attention surfacing (lib/waiting rollup helpers + WaitingBadge on session row / Host-page server tile / board header / sidebar SERVER-panel server tile, board-pane pulsing seam, Agent: Next waiting palette nav via lib/palette-agent-nav; WaitingBadge gained an optional onClick — wired only at the sidebar session row — reusing nextWaitingTarget + chatSearchForTarget ?view=chat append) + read-only Chat View as the `chat` LENS of the unified window-view model (`?view=chat` on the terminal route, sole availability gate = non-empty chatProvider folded into availableViews/resolveView; its interim mechanism — chat-view-resolve.ts, use-chat-view-pref.ts key-presence pref, SegmentedViewToggle — RETIRED into the lens machinery; chat-specific bits only: connection dot = chat health in chat mode (socket-connected AND chat subscription acked since 260717-vhvz — the chat stream is now a kind:chat subscription on /ws/state via use-chat-subscription, NOT an EventSource), WaitingBadge `?view=chat` deep-link, frontend renderer/data-layer in chat.md § Chat View Frontend — the heading is NOT lens-specific, it is a static `Window:` in every lens since 260714-uco1) + PrStatusLine component removed (module kept as shared PR color-vocabulary/status-dot-model home for 5 consumers), PANE-header refresh button (CollapsiblePanel headerAction, rk-glint, idle|spinning|check state machine — spins click→status-refresh SSE event not click→POST, 15s fallback armed at click entry, throttled/post-completion checkmark flash, consumes subscribeStatusRefresh) + PR: Refresh Status palette action (pure builder lib/palette-status-refresh.ts) both kicking POST /api/status/refresh; StatusDotTip "checked Xs ago" freshness line (FreshnessLine leaf owning useNow, formatDuration over prFetchedAt, mounted only while the card is open), Web Push opt-in (terminal-themed Cmd+K palette entry, not a bell), update notification (in-app only, NO Web Push — top-bar L3 UpdateChip leading NotificationControl: `⬆ v{latest}` accent + rk-glint, click→POST /api/update→`updating…`, `✕` per-version localStorage dismiss `runkit-update-dismissed`, hidden on no-update/dismissed/dev; `run-kit: Update to v…`/`Dismiss Update Notice` palette actions via pure buildUpdateActions in lib/palette-update.ts, dual-mounted into AppShell updateActions + board boardRouteActions (chip is a registry candidate since 260715-h1ck — no longer `hidden sm:flex`, overflows into the chevron menu's version row); state-socket-reconnect boot-aware auto-reload guard (fed by the state socket's version-slot replay since 260716-qf3j; pure 4-arg shouldReloadOnVersion(firstVersion, firstBoot, nextVersion, nextBoot) — reload on version OR same-version boot change, never on first connect, boot-reload suppressed on dev to dodge the air-recompile storm; firstVersionRef + firstBootRef); two palette-only maintenance entries via pure buildMaintenanceActions (run-kit: Update Now gated brew && !dev = force update reaching patch releases, run-kit: Restart Daemon gated !dev, dual-mounted app.tsx + board-page.tsx, no chip/confirm/toast — reload is the feedback); tolerant useUpdateNotification() hook — qualifies = !dev && latest, showChip adds per-version dismissal gate, also surfaces brew + forceUpdateNow/restartNow + daemonVersion for the maintenance entries) + steady-state version surfaces (260715-ifco — four passive frontend-only surfaces exposing the running daemonVersion when no update is pending, +a fifth from 260715-h1ck: command-palette `run-kit: Version — v…` entry via pure buildVersionAction in lib/palette-version.ts dual-mounted like the update actions (copies the displayed form on select, info/error toast), connection-dot hover title `Connected — run-kit v…`, passive BIOS-style Host-page footer stamp after SERVICES, the UpdateChip title/aria showing the `v{current} → v{latest}` transition via a new `current` field on useUpdateNotification(), and the overflow chevron menu's always-present fixed `Run Kit v{version}` row (copy / doubles as update surface when the chip overflows); shared displayVersion helper (v-prefix numeric / bare dev, mirrors backend root.go) + copyToClipboard widened void→Promise<boolean> success signal), chrome (top-bar right-cluster 3-level button pyramid (L1 splits terminal+board [260715-6jwn, board splits wired to focusedPane] + fixed-width terminal-only / L2 terminal+board Aa+✕ / L3 always update-chip→notification→theme→refresh→help+connection-dot; dot-everywhere = per-page live-data health, board ✕ = close-pane kill of the focused tile [260715-6jwn reversed the prior unpin], unpin moved to the tile-header pin-glyph + Board: Unpin Focused Pane palette action; board split/close/unpin palette parity + useBoardEntries refetch self-heal for the no-board-changed-on-kill gap) now consumed by a priority+ overflow-chevron menu (260715-h1ck — ordered registry {id,modes,hidden,barRender,menuRender} drives bar+menu from one source, killing the per-item `hidden sm:flex` cliff; pyramid consumed L1→L2→L3 from the LEFT so survivors never shift; ViewSwitcher+chevron+dot exempt; always-present chevron LEFT of the dot with an always-present menu whose fixed `Run Kit v{version}` row copies/doubles as the update surface `v{current}→v{latest} ⬆` when the overflowed chip qualifies + a chevron attention badge; measurement = right cell `min-w-0` FILLING the 1fr track (NOT justify-self-end) + one ResizeObserver on cell/probe/exempt nodes + pure computeVisibleCount in lib/top-bar-overflow.ts + collapse-first; shared useUpdateClick()/cycleTheme() so bar↔menu can't drift) + route-agnostic HelpLink docs-anchor chip → HELP_URL, with an AppShell-only "Help: Documentation" palette action), full-page RefreshButton recovery affordance + dual-mounted "View: Refresh Page" palette action (AppShell viewActions + board boardRouteActions; the Host page mounts none), theme system, user-controllable global terminal font size (ChromeContext preference + top-bar combo + palette actions), terminal relay via a RelayMux singleton (src/lib/relay-mux.ts, one /ws/terminals socket per tab, mirrors state-socket.ts; openStream handle send/resize/close/onData/onOpened/onClosed + setWindowId fresh-target seam; backoff reconnect re-opening live streams by REUSING the same per-connection stream id — not fresh ids; non-4004 stream closed prints [reconnecting…] + one probed re-open then [disconnected], 260717-803u) + adaptive terminal write flush + deferred per-STREAM reset (armed on stream onOpened) + (server, owning session)-keyed relay connection identity (same-session ride pushes stream.setWindowId, no close/open) + window-switch slide transition (View Transitions wrapper on navigateToWindow — flattened-sidebar-order direction data attribute, first-paint gate released at the stream's first onData frame receipt (was ws.onmessage; 260717-803u), reduced-motion/no-support instant-switch fallback) now CONFIRMATION-GATED (260715-38kg): the slide is an earned signal — gate settle reasons first-write/timeout/superseded, skipTransition() on EVERY non-first-write settle, a LogoSpinner pending mask (pure module state machine + useSyncExternalStore seam, armed only at the 300ms gate-timeout decision NEVER at click, POST-filtered lift via epoch+liftAccepting/openForNotify/openForLift, abandonSwitchFeedback/confirmSwitchArrived teardown that settles the gate not just the mask, isMaskExemptKey global-chord exemptions, reduced-motion still shows), failure bounce-back (CONFIRMATION_WINDOW_MS 5s + toast, server-scoped PendingSwitchTarget{server,windowId} via isSamePendingTarget at all four seams, server-change/route-leave/unmount teardowns, isRedundantSwitch same-window early-out), and non-VT/reduced-motion grace-timer parity (armGraceMask), keyboard shortcuts, sidebar tree keyboard navigation + W3C-APG tree ARIA, infra-server de-emphasis (isInfraServer/compareServers/DAEMON_SERVER in client.ts + single sort choke point at session-context.tsx fetchServers → infra-last order on all four surfaces + grey text-text-secondary tile names + rk-daemon kill-dialog warning in app.tsx) + rank-aware compareServersRanked composition ((infra-class, rank, name) wrapping compareServers at the same choke point + live re-sort on server-global event: server-order), server-tile drag reorder on both tile grids (shared useServerReorder hook — custom MIME, insert-before, derive-over-store override, debounce + drop-flush, infra excluded; dragover accepts (preventDefault) on the self-target tile so HTML5 DnD registers the drop and no native cancelled-drag snap-back ghost plays — guard order source/scope+MIME before acceptance, self-target no-op after; same fix mirrored on the sidebar session-reorder handler) + board pane drag reorder + Board: Move Focused Pane Left/Right palette parity (useBoardPaneReorder adapts useServerReorder — header-only drag handle + pane-root drop target, application/x-board-pane-reorder MIME, server:windowId key, ONE reorderPin POST per move NO debounce, committed-drop cancel-revert + rollback-on-rejection; focus tracked by server:windowId key with a shouldFocusPane user-intent gate so an SSE board-changed refetch never steals xterm focus — the board form of the sidebar focusMovedRef invariant) + per-board desktop board autofit toggle (useBoardAutofit runkit:board-autofit:{board} "on"-sentinel/key-removed-on-off pref; DesktopRow flex: 1 1 0 + min-width max(BOARD_PANE_MIN_WIDTH, calc(25% - 3px)) equal-share layout ≤4 panes / 25%-floor scroll >4, handles hidden + stored pane-widths untouched while on; board-only L2 top-bar BoardAutofitToggle between Aa and ✕ + Board: Toggle Autofit palette parity, plumbed via slot-context autofit/onToggleAutofit) + command-palette Move up/down for current server/session/window (lib/palette-move.ts pure helpers, boundary-hidden, no wraparound), unnamed `+ New Window` folder auto-naming (all three call sites drop the hardcoded `"zsh"` — `createWindow` name-optional; sidebar ghost = raw cwd basename via `rawBasename`, not the tmux-safe `deriveNameFromPath`), spawn-agent dialog v2 (compact Dialog surfacing rk riff via POST /api/riff, session-named title `Spawn agent in {session}`; fields Task → Preset → Where radio (new worktree default / this checkout) → Worktree name input (blank+placeholder, hidden on checkout) → Agent tier dropdown (default-first, from the presets-fetch `tiers`) — the tier field is FAB-GATED (gsmu): shown only when tiers is non-empty (backend returns tiers:[] for a non-fab repo → field hidden, tier omitted from body), preflight-FAILURE keeps [DEFAULT_TIER] (field shown); Enter-submits, indeterminate worktree→window→agent busy label, in-dialog error, falsy-windowId nav guard, best-effort preflight, defaults-omitted body byte-identical to the shipped path; THREE entry points: Cmd+K `Agent: Spawn` + window-switcher `+ New Agent` via BreadcrumbDropdown `secondaryAction` (both terminal-route, current session) + sidebar session-row 🤖 bot button (gsmu, cluster order palette→bot→+→✕, any listed session/server → cross-server spawn; app.tsx spawnAgentTarget:{server,session} explicit-target state, dialog server prop, cross-server nav branch), window-view lens model (rows are substrates, views are per-viewer lenses; THE single view-state authority incl. chat — three lenses tty/web/chat, orthogonal & stacking — pure lib/window-view.ts availableViews/defaultView(hint precedence desktop>chat>web>tty; chat contributes NO default hint)/resolveView(URL `?view=`→localStorage→default) + hasWebUrl/hasChat gates + value-bearing `runkit-window-view:{server}:{windowId}` key (also stores "chat"), ONE validated `?view=web|chat` search param (validateTerminalSearch), generic tty-first ViewSwitcher chip (lowercase tty/web/chat segments, `view-toggle` testid) — the FIRST overflow-registry candidate since 260717-6anu (drops first, space-driven not breakpoint-gated; collapses into the chevron menu as per-view `View:` rows via ViewSwitcherMenuRows/orderViews), heading is a STATIC `Window:` in every lens (260714-uco1 spec-R4 reversal — terminalHeadingPrefix/WEB_PREFIX/CHAT_PREFIX retired; lens shown by the ViewSwitcher, not the heading), palette parity via ONE pure buildViewActions (Ctrl+` hint for chat, else ⌘.) + `Cmd/Ctrl+.` cycle + `Ctrl+`` tty↔chat toggle (use-chat-view-shortcut, ViewName), IframeWindow `>_`→onSwitchToTty decoupling, render branch + single window-switch transition ungatedIds classification keyed on resolveView!=="tty" not `@rk_type` — no `@rk_type` mutation on view switch), component conventions |
